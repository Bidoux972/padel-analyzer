/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 8351
(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_RESULT__;/*! decimal.js-light v2.5.1 https://github.com/MikeMcl/decimal.js-light/LICENCE */
;(function (globalScope) {
  'use strict';


  /*
   *  decimal.js-light v2.5.1
   *  An arbitrary-precision Decimal type for JavaScript.
   *  https://github.com/MikeMcl/decimal.js-light
   *  Copyright (c) 2020 Michael Mclaughlin <M8ch88l@gmail.com>
   *  MIT Expat Licence
   */


  // -----------------------------------  EDITABLE DEFAULTS  ------------------------------------ //


    // The limit on the value of `precision`, and on the value of the first argument to
    // `toDecimalPlaces`, `toExponential`, `toFixed`, `toPrecision` and `toSignificantDigits`.
  var MAX_DIGITS = 1e9,                        // 0 to 1e9


    // The initial configuration properties of the Decimal constructor.
    Decimal = {

      // These values must be integers within the stated ranges (inclusive).
      // Most of these values can be changed during run-time using `Decimal.config`.

      // The maximum number of significant digits of the result of a calculation or base conversion.
      // E.g. `Decimal.config({ precision: 20 });`
      precision: 20,                         // 1 to MAX_DIGITS

      // The rounding mode used by default by `toInteger`, `toDecimalPlaces`, `toExponential`,
      // `toFixed`, `toPrecision` and `toSignificantDigits`.
      //
      // ROUND_UP         0 Away from zero.
      // ROUND_DOWN       1 Towards zero.
      // ROUND_CEIL       2 Towards +Infinity.
      // ROUND_FLOOR      3 Towards -Infinity.
      // ROUND_HALF_UP    4 Towards nearest neighbour. If equidistant, up.
      // ROUND_HALF_DOWN  5 Towards nearest neighbour. If equidistant, down.
      // ROUND_HALF_EVEN  6 Towards nearest neighbour. If equidistant, towards even neighbour.
      // ROUND_HALF_CEIL  7 Towards nearest neighbour. If equidistant, towards +Infinity.
      // ROUND_HALF_FLOOR 8 Towards nearest neighbour. If equidistant, towards -Infinity.
      //
      // E.g.
      // `Decimal.rounding = 4;`
      // `Decimal.rounding = Decimal.ROUND_HALF_UP;`
      rounding: 4,                           // 0 to 8

      // The exponent value at and beneath which `toString` returns exponential notation.
      // JavaScript numbers: -7
      toExpNeg: -7,                          // 0 to -MAX_E

      // The exponent value at and above which `toString` returns exponential notation.
      // JavaScript numbers: 21
      toExpPos:  21,                         // 0 to MAX_E

      // The natural logarithm of 10.
      // 115 digits
      LN10: '2.302585092994045684017991454684364207601101488628772976033327900967572609677352480235997205089598298341967784042286'
    },


  // ----------------------------------- END OF EDITABLE DEFAULTS ------------------------------- //


    external = true,

    decimalError = '[DecimalError] ',
    invalidArgument = decimalError + 'Invalid argument: ',
    exponentOutOfRange = decimalError + 'Exponent out of range: ',

    mathfloor = Math.floor,
    mathpow = Math.pow,

    isDecimal = /^(\d+(\.\d*)?|\.\d+)(e[+-]?\d+)?$/i,

    ONE,
    BASE = 1e7,
    LOG_BASE = 7,
    MAX_SAFE_INTEGER = 9007199254740991,
    MAX_E = mathfloor(MAX_SAFE_INTEGER / LOG_BASE),    // 1286742750677284

    // Decimal.prototype object
    P = {};


  // Decimal prototype methods


  /*
   *  absoluteValue                       abs
   *  comparedTo                          cmp
   *  decimalPlaces                       dp
   *  dividedBy                           div
   *  dividedToIntegerBy                  idiv
   *  equals                              eq
   *  exponent
   *  greaterThan                         gt
   *  greaterThanOrEqualTo                gte
   *  isInteger                           isint
   *  isNegative                          isneg
   *  isPositive                          ispos
   *  isZero
   *  lessThan                            lt
   *  lessThanOrEqualTo                   lte
   *  logarithm                           log
   *  minus                               sub
   *  modulo                              mod
   *  naturalExponential                  exp
   *  naturalLogarithm                    ln
   *  negated                             neg
   *  plus                                add
   *  precision                           sd
   *  squareRoot                          sqrt
   *  times                               mul
   *  toDecimalPlaces                     todp
   *  toExponential
   *  toFixed
   *  toInteger                           toint
   *  toNumber
   *  toPower                             pow
   *  toPrecision
   *  toSignificantDigits                 tosd
   *  toString
   *  valueOf                             val
   */


  /*
   * Return a new Decimal whose value is the absolute value of this Decimal.
   *
   */
  P.absoluteValue = P.abs = function () {
    var x = new this.constructor(this);
    if (x.s) x.s = 1;
    return x;
  };


  /*
   * Return
   *   1    if the value of this Decimal is greater than the value of `y`,
   *  -1    if the value of this Decimal is less than the value of `y`,
   *   0    if they have the same value
   *
   */
  P.comparedTo = P.cmp = function (y) {
    var i, j, xdL, ydL,
      x = this;

    y = new x.constructor(y);

    // Signs differ?
    if (x.s !== y.s) return x.s || -y.s;

    // Compare exponents.
    if (x.e !== y.e) return x.e > y.e ^ x.s < 0 ? 1 : -1;

    xdL = x.d.length;
    ydL = y.d.length;

    // Compare digit by digit.
    for (i = 0, j = xdL < ydL ? xdL : ydL; i < j; ++i) {
      if (x.d[i] !== y.d[i]) return x.d[i] > y.d[i] ^ x.s < 0 ? 1 : -1;
    }

    // Compare lengths.
    return xdL === ydL ? 0 : xdL > ydL ^ x.s < 0 ? 1 : -1;
  };


  /*
   * Return the number of decimal places of the value of this Decimal.
   *
   */
  P.decimalPlaces = P.dp = function () {
    var x = this,
      w = x.d.length - 1,
      dp = (w - x.e) * LOG_BASE;

    // Subtract the number of trailing zeros of the last word.
    w = x.d[w];
    if (w) for (; w % 10 == 0; w /= 10) dp--;

    return dp < 0 ? 0 : dp;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal divided by `y`, truncated to
   * `precision` significant digits.
   *
   */
  P.dividedBy = P.div = function (y) {
    return divide(this, new this.constructor(y));
  };


  /*
   * Return a new Decimal whose value is the integer part of dividing the value of this Decimal
   * by the value of `y`, truncated to `precision` significant digits.
   *
   */
  P.dividedToIntegerBy = P.idiv = function (y) {
    var x = this,
      Ctor = x.constructor;
    return round(divide(x, new Ctor(y), 0, 1), Ctor.precision);
  };


  /*
   * Return true if the value of this Decimal is equal to the value of `y`, otherwise return false.
   *
   */
  P.equals = P.eq = function (y) {
    return !this.cmp(y);
  };


  /*
   * Return the (base 10) exponent value of this Decimal (this.e is the base 10000000 exponent).
   *
   */
  P.exponent = function () {
    return getBase10Exponent(this);
  };


  /*
   * Return true if the value of this Decimal is greater than the value of `y`, otherwise return
   * false.
   *
   */
  P.greaterThan = P.gt = function (y) {
    return this.cmp(y) > 0;
  };


  /*
   * Return true if the value of this Decimal is greater than or equal to the value of `y`,
   * otherwise return false.
   *
   */
  P.greaterThanOrEqualTo = P.gte = function (y) {
    return this.cmp(y) >= 0;
  };


  /*
   * Return true if the value of this Decimal is an integer, otherwise return false.
   *
   */
  P.isInteger = P.isint = function () {
    return this.e > this.d.length - 2;
  };


  /*
   * Return true if the value of this Decimal is negative, otherwise return false.
   *
   */
  P.isNegative = P.isneg = function () {
    return this.s < 0;
  };


  /*
   * Return true if the value of this Decimal is positive, otherwise return false.
   *
   */
  P.isPositive = P.ispos = function () {
    return this.s > 0;
  };


  /*
   * Return true if the value of this Decimal is 0, otherwise return false.
   *
   */
  P.isZero = function () {
    return this.s === 0;
  };


  /*
   * Return true if the value of this Decimal is less than `y`, otherwise return false.
   *
   */
  P.lessThan = P.lt = function (y) {
    return this.cmp(y) < 0;
  };


  /*
   * Return true if the value of this Decimal is less than or equal to `y`, otherwise return false.
   *
   */
  P.lessThanOrEqualTo = P.lte = function (y) {
    return this.cmp(y) < 1;
  };


  /*
   * Return the logarithm of the value of this Decimal to the specified base, truncated to
   * `precision` significant digits.
   *
   * If no base is specified, return log[10](x).
   *
   * log[base](x) = ln(x) / ln(base)
   *
   * The maximum error of the result is 1 ulp (unit in the last place).
   *
   * [base] {number|string|Decimal} The base of the logarithm.
   *
   */
  P.logarithm = P.log = function (base) {
    var r,
      x = this,
      Ctor = x.constructor,
      pr = Ctor.precision,
      wpr = pr + 5;

    // Default base is 10.
    if (base === void 0) {
      base = new Ctor(10);
    } else {
      base = new Ctor(base);

      // log[-b](x) = NaN
      // log[0](x)  = NaN
      // log[1](x)  = NaN
      if (base.s < 1 || base.eq(ONE)) throw Error(decimalError + 'NaN');
    }

    // log[b](-x) = NaN
    // log[b](0) = -Infinity
    if (x.s < 1) throw Error(decimalError + (x.s ? 'NaN' : '-Infinity'));

    // log[b](1) = 0
    if (x.eq(ONE)) return new Ctor(0);

    external = false;
    r = divide(ln(x, wpr), ln(base, wpr), wpr);
    external = true;

    return round(r, pr);
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal minus `y`, truncated to
   * `precision` significant digits.
   *
   */
  P.minus = P.sub = function (y) {
    var x = this;
    y = new x.constructor(y);
    return x.s == y.s ? subtract(x, y) : add(x, (y.s = -y.s, y));
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal modulo `y`, truncated to
   * `precision` significant digits.
   *
   */
  P.modulo = P.mod = function (y) {
    var q,
      x = this,
      Ctor = x.constructor,
      pr = Ctor.precision;

    y = new Ctor(y);

    // x % 0 = NaN
    if (!y.s) throw Error(decimalError + 'NaN');

    // Return x if x is 0.
    if (!x.s) return round(new Ctor(x), pr);

    // Prevent rounding of intermediate calculations.
    external = false;
    q = divide(x, y, 0, 1).times(y);
    external = true;

    return x.minus(q);
  };


  /*
   * Return a new Decimal whose value is the natural exponential of the value of this Decimal,
   * i.e. the base e raised to the power the value of this Decimal, truncated to `precision`
   * significant digits.
   *
   */
  P.naturalExponential = P.exp = function () {
    return exp(this);
  };


  /*
   * Return a new Decimal whose value is the natural logarithm of the value of this Decimal,
   * truncated to `precision` significant digits.
   *
   */
  P.naturalLogarithm = P.ln = function () {
    return ln(this);
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal negated, i.e. as if multiplied by
   * -1.
   *
   */
  P.negated = P.neg = function () {
    var x = new this.constructor(this);
    x.s = -x.s || 0;
    return x;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal plus `y`, truncated to
   * `precision` significant digits.
   *
   */
  P.plus = P.add = function (y) {
    var x = this;
    y = new x.constructor(y);
    return x.s == y.s ? add(x, y) : subtract(x, (y.s = -y.s, y));
  };


  /*
   * Return the number of significant digits of the value of this Decimal.
   *
   * [z] {boolean|number} Whether to count integer-part trailing zeros: true, false, 1 or 0.
   *
   */
  P.precision = P.sd = function (z) {
    var e, sd, w,
      x = this;

    if (z !== void 0 && z !== !!z && z !== 1 && z !== 0) throw Error(invalidArgument + z);

    e = getBase10Exponent(x) + 1;
    w = x.d.length - 1;
    sd = w * LOG_BASE + 1;
    w = x.d[w];

    // If non-zero...
    if (w) {

      // Subtract the number of trailing zeros of the last word.
      for (; w % 10 == 0; w /= 10) sd--;

      // Add the number of digits of the first word.
      for (w = x.d[0]; w >= 10; w /= 10) sd++;
    }

    return z && e > sd ? e : sd;
  };


  /*
   * Return a new Decimal whose value is the square root of this Decimal, truncated to `precision`
   * significant digits.
   *
   */
  P.squareRoot = P.sqrt = function () {
    var e, n, pr, r, s, t, wpr,
      x = this,
      Ctor = x.constructor;

    // Negative or zero?
    if (x.s < 1) {
      if (!x.s) return new Ctor(0);

      // sqrt(-x) = NaN
      throw Error(decimalError + 'NaN');
    }

    e = getBase10Exponent(x);
    external = false;

    // Initial estimate.
    s = Math.sqrt(+x);

    // Math.sqrt underflow/overflow?
    // Pass x to Math.sqrt as integer, then adjust the exponent of the result.
    if (s == 0 || s == 1 / 0) {
      n = digitsToString(x.d);
      if ((n.length + e) % 2 == 0) n += '0';
      s = Math.sqrt(n);
      e = mathfloor((e + 1) / 2) - (e < 0 || e % 2);

      if (s == 1 / 0) {
        n = '5e' + e;
      } else {
        n = s.toExponential();
        n = n.slice(0, n.indexOf('e') + 1) + e;
      }

      r = new Ctor(n);
    } else {
      r = new Ctor(s.toString());
    }

    pr = Ctor.precision;
    s = wpr = pr + 3;

    // Newton-Raphson iteration.
    for (;;) {
      t = r;
      r = t.plus(divide(x, t, wpr + 2)).times(0.5);

      if (digitsToString(t.d).slice(0, wpr) === (n = digitsToString(r.d)).slice(0, wpr)) {
        n = n.slice(wpr - 3, wpr + 1);

        // The 4th rounding digit may be in error by -1 so if the 4 rounding digits are 9999 or
        // 4999, i.e. approaching a rounding boundary, continue the iteration.
        if (s == wpr && n == '4999') {

          // On the first iteration only, check to see if rounding up gives the exact result as the
          // nines may infinitely repeat.
          round(t, pr + 1, 0);

          if (t.times(t).eq(x)) {
            r = t;
            break;
          }
        } else if (n != '9999') {
          break;
        }

        wpr += 4;
      }
    }

    external = true;

    return round(r, pr);
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal times `y`, truncated to
   * `precision` significant digits.
   *
   */
  P.times = P.mul = function (y) {
    var carry, e, i, k, r, rL, t, xdL, ydL,
      x = this,
      Ctor = x.constructor,
      xd = x.d,
      yd = (y = new Ctor(y)).d;

    // Return 0 if either is 0.
    if (!x.s || !y.s) return new Ctor(0);

    y.s *= x.s;
    e = x.e + y.e;
    xdL = xd.length;
    ydL = yd.length;

    // Ensure xd points to the longer array.
    if (xdL < ydL) {
      r = xd;
      xd = yd;
      yd = r;
      rL = xdL;
      xdL = ydL;
      ydL = rL;
    }

    // Initialise the result array with zeros.
    r = [];
    rL = xdL + ydL;
    for (i = rL; i--;) r.push(0);

    // Multiply!
    for (i = ydL; --i >= 0;) {
      carry = 0;
      for (k = xdL + i; k > i;) {
        t = r[k] + yd[i] * xd[k - i - 1] + carry;
        r[k--] = t % BASE | 0;
        carry = t / BASE | 0;
      }

      r[k] = (r[k] + carry) % BASE | 0;
    }

    // Remove trailing zeros.
    for (; !r[--rL];) r.pop();

    if (carry) ++e;
    else r.shift();

    y.d = r;
    y.e = e;

    return external ? round(y, Ctor.precision) : y;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal rounded to a maximum of `dp`
   * decimal places using rounding mode `rm` or `rounding` if `rm` is omitted.
   *
   * If `dp` is omitted, return a new Decimal whose value is the value of this Decimal.
   *
   * [dp] {number} Decimal places. Integer, 0 to MAX_DIGITS inclusive.
   * [rm] {number} Rounding mode. Integer, 0 to 8 inclusive.
   *
   */
  P.toDecimalPlaces = P.todp = function (dp, rm) {
    var x = this,
      Ctor = x.constructor;

    x = new Ctor(x);
    if (dp === void 0) return x;

    checkInt32(dp, 0, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);

    return round(x, dp + getBase10Exponent(x) + 1, rm);
  };


  /*
   * Return a string representing the value of this Decimal in exponential notation rounded to
   * `dp` fixed decimal places using rounding mode `rounding`.
   *
   * [dp] {number} Decimal places. Integer, 0 to MAX_DIGITS inclusive.
   * [rm] {number} Rounding mode. Integer, 0 to 8 inclusive.
   *
   */
  P.toExponential = function (dp, rm) {
    var str,
      x = this,
      Ctor = x.constructor;

    if (dp === void 0) {
      str = toString(x, true);
    } else {
      checkInt32(dp, 0, MAX_DIGITS);

      if (rm === void 0) rm = Ctor.rounding;
      else checkInt32(rm, 0, 8);

      x = round(new Ctor(x), dp + 1, rm);
      str = toString(x, true, dp + 1);
    }

    return str;
  };


  /*
   * Return a string representing the value of this Decimal in normal (fixed-point) notation to
   * `dp` fixed decimal places and rounded using rounding mode `rm` or `rounding` if `rm` is
   * omitted.
   *
   * As with JavaScript numbers, (-0).toFixed(0) is '0', but e.g. (-0.00001).toFixed(0) is '-0'.
   *
   * [dp] {number} Decimal places. Integer, 0 to MAX_DIGITS inclusive.
   * [rm] {number} Rounding mode. Integer, 0 to 8 inclusive.
   *
   * (-0).toFixed(0) is '0', but (-0.1).toFixed(0) is '-0'.
   * (-0).toFixed(1) is '0.0', but (-0.01).toFixed(1) is '-0.0'.
   * (-0).toFixed(3) is '0.000'.
   * (-0.5).toFixed(0) is '-0'.
   *
   */
  P.toFixed = function (dp, rm) {
    var str, y,
      x = this,
      Ctor = x.constructor;

    if (dp === void 0) return toString(x);

    checkInt32(dp, 0, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);

    y = round(new Ctor(x), dp + getBase10Exponent(x) + 1, rm);
    str = toString(y.abs(), false, dp + getBase10Exponent(y) + 1);

    // To determine whether to add the minus sign look at the value before it was rounded,
    // i.e. look at `x` rather than `y`.
    return x.isneg() && !x.isZero() ? '-' + str : str;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal rounded to a whole number using
   * rounding mode `rounding`.
   *
   */
  P.toInteger = P.toint = function () {
    var x = this,
      Ctor = x.constructor;
    return round(new Ctor(x), getBase10Exponent(x) + 1, Ctor.rounding);
  };


  /*
   * Return the value of this Decimal converted to a number primitive.
   *
   */
  P.toNumber = function () {
    return +this;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal raised to the power `y`,
   * truncated to `precision` significant digits.
   *
   * For non-integer or very large exponents pow(x, y) is calculated using
   *
   *   x^y = exp(y*ln(x))
   *
   * The maximum error is 1 ulp (unit in last place).
   *
   * y {number|string|Decimal} The power to which to raise this Decimal.
   *
   */
  P.toPower = P.pow = function (y) {
    var e, k, pr, r, sign, yIsInt,
      x = this,
      Ctor = x.constructor,
      guard = 12,
      yn = +(y = new Ctor(y));

    // pow(x, 0) = 1
    if (!y.s) return new Ctor(ONE);

    x = new Ctor(x);

    // pow(0, y > 0) = 0
    // pow(0, y < 0) = Infinity
    if (!x.s) {
      if (y.s < 1) throw Error(decimalError + 'Infinity');
      return x;
    }

    // pow(1, y) = 1
    if (x.eq(ONE)) return x;

    pr = Ctor.precision;

    // pow(x, 1) = x
    if (y.eq(ONE)) return round(x, pr);

    e = y.e;
    k = y.d.length - 1;
    yIsInt = e >= k;
    sign = x.s;

    if (!yIsInt) {

      // pow(x < 0, y non-integer) = NaN
      if (sign < 0) throw Error(decimalError + 'NaN');

    // If y is a small integer use the 'exponentiation by squaring' algorithm.
    } else if ((k = yn < 0 ? -yn : yn) <= MAX_SAFE_INTEGER) {
      r = new Ctor(ONE);

      // Max k of 9007199254740991 takes 53 loop iterations.
      // Maximum digits array length; leaves [28, 34] guard digits.
      e = Math.ceil(pr / LOG_BASE + 4);

      external = false;

      for (;;) {
        if (k % 2) {
          r = r.times(x);
          truncate(r.d, e);
        }

        k = mathfloor(k / 2);
        if (k === 0) break;

        x = x.times(x);
        truncate(x.d, e);
      }

      external = true;

      return y.s < 0 ? new Ctor(ONE).div(r) : round(r, pr);
    }

    // Result is negative if x is negative and the last digit of integer y is odd.
    sign = sign < 0 && y.d[Math.max(e, k)] & 1 ? -1 : 1;

    x.s = 1;
    external = false;
    r = y.times(ln(x, pr + guard));
    external = true;
    r = exp(r);
    r.s = sign;

    return r;
  };


  /*
   * Return a string representing the value of this Decimal rounded to `sd` significant digits
   * using rounding mode `rounding`.
   *
   * Return exponential notation if `sd` is less than the number of digits necessary to represent
   * the integer part of the value in normal notation.
   *
   * [sd] {number} Significant digits. Integer, 1 to MAX_DIGITS inclusive.
   * [rm] {number} Rounding mode. Integer, 0 to 8 inclusive.
   *
   */
  P.toPrecision = function (sd, rm) {
    var e, str,
      x = this,
      Ctor = x.constructor;

    if (sd === void 0) {
      e = getBase10Exponent(x);
      str = toString(x, e <= Ctor.toExpNeg || e >= Ctor.toExpPos);
    } else {
      checkInt32(sd, 1, MAX_DIGITS);

      if (rm === void 0) rm = Ctor.rounding;
      else checkInt32(rm, 0, 8);

      x = round(new Ctor(x), sd, rm);
      e = getBase10Exponent(x);
      str = toString(x, sd <= e || e <= Ctor.toExpNeg, sd);
    }

    return str;
  };


  /*
   * Return a new Decimal whose value is the value of this Decimal rounded to a maximum of `sd`
   * significant digits using rounding mode `rm`, or to `precision` and `rounding` respectively if
   * omitted.
   *
   * [sd] {number} Significant digits. Integer, 1 to MAX_DIGITS inclusive.
   * [rm] {number} Rounding mode. Integer, 0 to 8 inclusive.
   *
   */
  P.toSignificantDigits = P.tosd = function (sd, rm) {
    var x = this,
      Ctor = x.constructor;

    if (sd === void 0) {
      sd = Ctor.precision;
      rm = Ctor.rounding;
    } else {
      checkInt32(sd, 1, MAX_DIGITS);

      if (rm === void 0) rm = Ctor.rounding;
      else checkInt32(rm, 0, 8);
    }

    return round(new Ctor(x), sd, rm);
  };


  /*
   * Return a string representing the value of this Decimal.
   *
   * Return exponential notation if this Decimal has a positive exponent equal to or greater than
   * `toExpPos`, or a negative exponent equal to or less than `toExpNeg`.
   *
   */
  P.toString = P.valueOf = P.val = P.toJSON = function () {
    var x = this,
      e = getBase10Exponent(x),
      Ctor = x.constructor;

    return toString(x, e <= Ctor.toExpNeg || e >= Ctor.toExpPos);
  };


  // Helper functions for Decimal.prototype (P) and/or Decimal methods, and their callers.


  /*
   *  add                 P.minus, P.plus
   *  checkInt32          P.todp, P.toExponential, P.toFixed, P.toPrecision, P.tosd
   *  digitsToString      P.log, P.sqrt, P.pow, toString, exp, ln
   *  divide              P.div, P.idiv, P.log, P.mod, P.sqrt, exp, ln
   *  exp                 P.exp, P.pow
   *  getBase10Exponent   P.exponent, P.sd, P.toint, P.sqrt, P.todp, P.toFixed, P.toPrecision,
   *                      P.toString, divide, round, toString, exp, ln
   *  getLn10             P.log, ln
   *  getZeroString       digitsToString, toString
   *  ln                  P.log, P.ln, P.pow, exp
   *  parseDecimal        Decimal
   *  round               P.abs, P.idiv, P.log, P.minus, P.mod, P.neg, P.plus, P.toint, P.sqrt,
   *                      P.times, P.todp, P.toExponential, P.toFixed, P.pow, P.toPrecision, P.tosd,
   *                      divide, getLn10, exp, ln
   *  subtract            P.minus, P.plus
   *  toString            P.toExponential, P.toFixed, P.toPrecision, P.toString, P.valueOf
   *  truncate            P.pow
   *
   *  Throws:             P.log, P.mod, P.sd, P.sqrt, P.pow,  checkInt32, divide, round,
   *                      getLn10, exp, ln, parseDecimal, Decimal, config
   */


  function add(x, y) {
    var carry, d, e, i, k, len, xd, yd,
      Ctor = x.constructor,
      pr = Ctor.precision;

    // If either is zero...
    if (!x.s || !y.s) {

      // Return x if y is zero.
      // Return y if y is non-zero.
      if (!y.s) y = new Ctor(x);
      return external ? round(y, pr) : y;
    }

    xd = x.d;
    yd = y.d;

    // x and y are finite, non-zero numbers with the same sign.

    k = x.e;
    e = y.e;
    xd = xd.slice();
    i = k - e;

    // If base 1e7 exponents differ...
    if (i) {
      if (i < 0) {
        d = xd;
        i = -i;
        len = yd.length;
      } else {
        d = yd;
        e = k;
        len = xd.length;
      }

      // Limit number of zeros prepended to max(ceil(pr / LOG_BASE), len) + 1.
      k = Math.ceil(pr / LOG_BASE);
      len = k > len ? k + 1 : len + 1;

      if (i > len) {
        i = len;
        d.length = 1;
      }

      // Prepend zeros to equalise exponents. Note: Faster to use reverse then do unshifts.
      d.reverse();
      for (; i--;) d.push(0);
      d.reverse();
    }

    len = xd.length;
    i = yd.length;

    // If yd is longer than xd, swap xd and yd so xd points to the longer array.
    if (len - i < 0) {
      i = len;
      d = yd;
      yd = xd;
      xd = d;
    }

    // Only start adding at yd.length - 1 as the further digits of xd can be left as they are.
    for (carry = 0; i;) {
      carry = (xd[--i] = xd[i] + yd[i] + carry) / BASE | 0;
      xd[i] %= BASE;
    }

    if (carry) {
      xd.unshift(carry);
      ++e;
    }

    // Remove trailing zeros.
    // No need to check for zero, as +x + +y != 0 && -x + -y != 0
    for (len = xd.length; xd[--len] == 0;) xd.pop();

    y.d = xd;
    y.e = e;

    return external ? round(y, pr) : y;
  }


  function checkInt32(i, min, max) {
    if (i !== ~~i || i < min || i > max) {
      throw Error(invalidArgument + i);
    }
  }


  function digitsToString(d) {
    var i, k, ws,
      indexOfLastWord = d.length - 1,
      str = '',
      w = d[0];

    if (indexOfLastWord > 0) {
      str += w;
      for (i = 1; i < indexOfLastWord; i++) {
        ws = d[i] + '';
        k = LOG_BASE - ws.length;
        if (k) str += getZeroString(k);
        str += ws;
      }

      w = d[i];
      ws = w + '';
      k = LOG_BASE - ws.length;
      if (k) str += getZeroString(k);
    } else if (w === 0) {
      return '0';
    }

    // Remove trailing zeros of last w.
    for (; w % 10 === 0;) w /= 10;

    return str + w;
  }


  var divide = (function () {

    // Assumes non-zero x and k, and hence non-zero result.
    function multiplyInteger(x, k) {
      var temp,
        carry = 0,
        i = x.length;

      for (x = x.slice(); i--;) {
        temp = x[i] * k + carry;
        x[i] = temp % BASE | 0;
        carry = temp / BASE | 0;
      }

      if (carry) x.unshift(carry);

      return x;
    }

    function compare(a, b, aL, bL) {
      var i, r;

      if (aL != bL) {
        r = aL > bL ? 1 : -1;
      } else {
        for (i = r = 0; i < aL; i++) {
          if (a[i] != b[i]) {
            r = a[i] > b[i] ? 1 : -1;
            break;
          }
        }
      }

      return r;
    }

    function subtract(a, b, aL) {
      var i = 0;

      // Subtract b from a.
      for (; aL--;) {
        a[aL] -= i;
        i = a[aL] < b[aL] ? 1 : 0;
        a[aL] = i * BASE + a[aL] - b[aL];
      }

      // Remove leading zeros.
      for (; !a[0] && a.length > 1;) a.shift();
    }

    return function (x, y, pr, dp) {
      var cmp, e, i, k, prod, prodL, q, qd, rem, remL, rem0, sd, t, xi, xL, yd0, yL, yz,
        Ctor = x.constructor,
        sign = x.s == y.s ? 1 : -1,
        xd = x.d,
        yd = y.d;

      // Either 0?
      if (!x.s) return new Ctor(x);
      if (!y.s) throw Error(decimalError + 'Division by zero');

      e = x.e - y.e;
      yL = yd.length;
      xL = xd.length;
      q = new Ctor(sign);
      qd = q.d = [];

      // Result exponent may be one less than e.
      for (i = 0; yd[i] == (xd[i] || 0); ) ++i;
      if (yd[i] > (xd[i] || 0)) --e;

      if (pr == null) {
        sd = pr = Ctor.precision;
      } else if (dp) {
        sd = pr + (getBase10Exponent(x) - getBase10Exponent(y)) + 1;
      } else {
        sd = pr;
      }

      if (sd < 0) return new Ctor(0);

      // Convert precision in number of base 10 digits to base 1e7 digits.
      sd = sd / LOG_BASE + 2 | 0;
      i = 0;

      // divisor < 1e7
      if (yL == 1) {
        k = 0;
        yd = yd[0];
        sd++;

        // k is the carry.
        for (; (i < xL || k) && sd--; i++) {
          t = k * BASE + (xd[i] || 0);
          qd[i] = t / yd | 0;
          k = t % yd | 0;
        }

      // divisor >= 1e7
      } else {

        // Normalise xd and yd so highest order digit of yd is >= BASE/2
        k = BASE / (yd[0] + 1) | 0;

        if (k > 1) {
          yd = multiplyInteger(yd, k);
          xd = multiplyInteger(xd, k);
          yL = yd.length;
          xL = xd.length;
        }

        xi = yL;
        rem = xd.slice(0, yL);
        remL = rem.length;

        // Add zeros to make remainder as long as divisor.
        for (; remL < yL;) rem[remL++] = 0;

        yz = yd.slice();
        yz.unshift(0);
        yd0 = yd[0];

        if (yd[1] >= BASE / 2) ++yd0;

        do {
          k = 0;

          // Compare divisor and remainder.
          cmp = compare(yd, rem, yL, remL);

          // If divisor < remainder.
          if (cmp < 0) {

            // Calculate trial digit, k.
            rem0 = rem[0];
            if (yL != remL) rem0 = rem0 * BASE + (rem[1] || 0);

            // k will be how many times the divisor goes into the current remainder.
            k = rem0 / yd0 | 0;

            //  Algorithm:
            //  1. product = divisor * trial digit (k)
            //  2. if product > remainder: product -= divisor, k--
            //  3. remainder -= product
            //  4. if product was < remainder at 2:
            //    5. compare new remainder and divisor
            //    6. If remainder > divisor: remainder -= divisor, k++

            if (k > 1) {
              if (k >= BASE) k = BASE - 1;

              // product = divisor * trial digit.
              prod = multiplyInteger(yd, k);
              prodL = prod.length;
              remL = rem.length;

              // Compare product and remainder.
              cmp = compare(prod, rem, prodL, remL);

              // product > remainder.
              if (cmp == 1) {
                k--;

                // Subtract divisor from product.
                subtract(prod, yL < prodL ? yz : yd, prodL);
              }
            } else {

              // cmp is -1.
              // If k is 0, there is no need to compare yd and rem again below, so change cmp to 1
              // to avoid it. If k is 1 there is a need to compare yd and rem again below.
              if (k == 0) cmp = k = 1;
              prod = yd.slice();
            }

            prodL = prod.length;
            if (prodL < remL) prod.unshift(0);

            // Subtract product from remainder.
            subtract(rem, prod, remL);

            // If product was < previous remainder.
            if (cmp == -1) {
              remL = rem.length;

              // Compare divisor and new remainder.
              cmp = compare(yd, rem, yL, remL);

              // If divisor < new remainder, subtract divisor from remainder.
              if (cmp < 1) {
                k++;

                // Subtract divisor from remainder.
                subtract(rem, yL < remL ? yz : yd, remL);
              }
            }

            remL = rem.length;
          } else if (cmp === 0) {
            k++;
            rem = [0];
          }    // if cmp === 1, k will be 0

          // Add the next digit, k, to the result array.
          qd[i++] = k;

          // Update the remainder.
          if (cmp && rem[0]) {
            rem[remL++] = xd[xi] || 0;
          } else {
            rem = [xd[xi]];
            remL = 1;
          }

        } while ((xi++ < xL || rem[0] !== void 0) && sd--);
      }

      // Leading zero?
      if (!qd[0]) qd.shift();

      q.e = e;

      return round(q, dp ? pr + getBase10Exponent(q) + 1 : pr);
    };
  })();


  /*
   * Return a new Decimal whose value is the natural exponential of `x` truncated to `sd`
   * significant digits.
   *
   * Taylor/Maclaurin series.
   *
   * exp(x) = x^0/0! + x^1/1! + x^2/2! + x^3/3! + ...
   *
   * Argument reduction:
   *   Repeat x = x / 32, k += 5, until |x| < 0.1
   *   exp(x) = exp(x / 2^k)^(2^k)
   *
   * Previously, the argument was initially reduced by
   * exp(x) = exp(r) * 10^k  where r = x - k * ln10, k = floor(x / ln10)
   * to first put r in the range [0, ln10], before dividing by 32 until |x| < 0.1, but this was
   * found to be slower than just dividing repeatedly by 32 as above.
   *
   * (Math object integer min/max: Math.exp(709) = 8.2e+307, Math.exp(-745) = 5e-324)
   *
   *  exp(x) is non-terminating for any finite, non-zero x.
   *
   */
  function exp(x, sd) {
    var denominator, guard, pow, sum, t, wpr,
      i = 0,
      k = 0,
      Ctor = x.constructor,
      pr = Ctor.precision;

    if (getBase10Exponent(x) > 16) throw Error(exponentOutOfRange + getBase10Exponent(x));

    // exp(0) = 1
    if (!x.s) return new Ctor(ONE);

    if (sd == null) {
      external = false;
      wpr = pr;
    } else {
      wpr = sd;
    }

    t = new Ctor(0.03125);

    while (x.abs().gte(0.1)) {
      x = x.times(t);    // x = x / 2^5
      k += 5;
    }

    // Estimate the precision increase necessary to ensure the first 4 rounding digits are correct.
    guard = Math.log(mathpow(2, k)) / Math.LN10 * 2 + 5 | 0;
    wpr += guard;
    denominator = pow = sum = new Ctor(ONE);
    Ctor.precision = wpr;

    for (;;) {
      pow = round(pow.times(x), wpr);
      denominator = denominator.times(++i);
      t = sum.plus(divide(pow, denominator, wpr));

      if (digitsToString(t.d).slice(0, wpr) === digitsToString(sum.d).slice(0, wpr)) {
        while (k--) sum = round(sum.times(sum), wpr);
        Ctor.precision = pr;
        return sd == null ? (external = true, round(sum, pr)) : sum;
      }

      sum = t;
    }
  }


  // Calculate the base 10 exponent from the base 1e7 exponent.
  function getBase10Exponent(x) {
    var e = x.e * LOG_BASE,
      w = x.d[0];

    // Add the number of digits of the first word of the digits array.
    for (; w >= 10; w /= 10) e++;
    return e;
  }


  function getLn10(Ctor, sd, pr) {

    if (sd > Ctor.LN10.sd()) {


      // Reset global state in case the exception is caught.
      external = true;
      if (pr) Ctor.precision = pr;
      throw Error(decimalError + 'LN10 precision limit exceeded');
    }

    return round(new Ctor(Ctor.LN10), sd);
  }


  function getZeroString(k) {
    var zs = '';
    for (; k--;) zs += '0';
    return zs;
  }


  /*
   * Return a new Decimal whose value is the natural logarithm of `x` truncated to `sd` significant
   * digits.
   *
   *  ln(n) is non-terminating (n != 1)
   *
   */
  function ln(y, sd) {
    var c, c0, denominator, e, numerator, sum, t, wpr, x2,
      n = 1,
      guard = 10,
      x = y,
      xd = x.d,
      Ctor = x.constructor,
      pr = Ctor.precision;

    // ln(-x) = NaN
    // ln(0) = -Infinity
    if (x.s < 1) throw Error(decimalError + (x.s ? 'NaN' : '-Infinity'));

    // ln(1) = 0
    if (x.eq(ONE)) return new Ctor(0);

    if (sd == null) {
      external = false;
      wpr = pr;
    } else {
      wpr = sd;
    }

    if (x.eq(10)) {
      if (sd == null) external = true;
      return getLn10(Ctor, wpr);
    }

    wpr += guard;
    Ctor.precision = wpr;
    c = digitsToString(xd);
    c0 = c.charAt(0);
    e = getBase10Exponent(x);

    if (Math.abs(e) < 1.5e15) {

      // Argument reduction.
      // The series converges faster the closer the argument is to 1, so using
      // ln(a^b) = b * ln(a),   ln(a) = ln(a^b) / b
      // multiply the argument by itself until the leading digits of the significand are 7, 8, 9,
      // 10, 11, 12 or 13, recording the number of multiplications so the sum of the series can
      // later be divided by this number, then separate out the power of 10 using
      // ln(a*10^b) = ln(a) + b*ln(10).

      // max n is 21 (gives 0.9, 1.0 or 1.1) (9e15 / 21 = 4.2e14).
      //while (c0 < 9 && c0 != 1 || c0 == 1 && c.charAt(1) > 1) {
      // max n is 6 (gives 0.7 - 1.3)
      while (c0 < 7 && c0 != 1 || c0 == 1 && c.charAt(1) > 3) {
        x = x.times(y);
        c = digitsToString(x.d);
        c0 = c.charAt(0);
        n++;
      }

      e = getBase10Exponent(x);

      if (c0 > 1) {
        x = new Ctor('0.' + c);
        e++;
      } else {
        x = new Ctor(c0 + '.' + c.slice(1));
      }
    } else {

      // The argument reduction method above may result in overflow if the argument y is a massive
      // number with exponent >= 1500000000000000 (9e15 / 6 = 1.5e15), so instead recall this
      // function using ln(x*10^e) = ln(x) + e*ln(10).
      t = getLn10(Ctor, wpr + 2, pr).times(e + '');
      x = ln(new Ctor(c0 + '.' + c.slice(1)), wpr - guard).plus(t);

      Ctor.precision = pr;
      return sd == null ? (external = true, round(x, pr)) : x;
    }

    // x is reduced to a value near 1.

    // Taylor series.
    // ln(y) = ln((1 + x)/(1 - x)) = 2(x + x^3/3 + x^5/5 + x^7/7 + ...)
    // where x = (y - 1)/(y + 1)    (|x| < 1)
    sum = numerator = x = divide(x.minus(ONE), x.plus(ONE), wpr);
    x2 = round(x.times(x), wpr);
    denominator = 3;

    for (;;) {
      numerator = round(numerator.times(x2), wpr);
      t = sum.plus(divide(numerator, new Ctor(denominator), wpr));

      if (digitsToString(t.d).slice(0, wpr) === digitsToString(sum.d).slice(0, wpr)) {
        sum = sum.times(2);

        // Reverse the argument reduction.
        if (e !== 0) sum = sum.plus(getLn10(Ctor, wpr + 2, pr).times(e + ''));
        sum = divide(sum, new Ctor(n), wpr);

        Ctor.precision = pr;
        return sd == null ? (external = true, round(sum, pr)) : sum;
      }

      sum = t;
      denominator += 2;
    }
  }


  /*
   * Parse the value of a new Decimal `x` from string `str`.
   */
  function parseDecimal(x, str) {
    var e, i, len;

    // Decimal point?
    if ((e = str.indexOf('.')) > -1) str = str.replace('.', '');

    // Exponential form?
    if ((i = str.search(/e/i)) > 0) {

      // Determine exponent.
      if (e < 0) e = i;
      e += +str.slice(i + 1);
      str = str.substring(0, i);
    } else if (e < 0) {

      // Integer.
      e = str.length;
    }

    // Determine leading zeros.
    for (i = 0; str.charCodeAt(i) === 48;) ++i;

    // Determine trailing zeros.
    for (len = str.length; str.charCodeAt(len - 1) === 48;) --len;
    str = str.slice(i, len);

    if (str) {
      len -= i;
      e = e - i - 1;
      x.e = mathfloor(e / LOG_BASE);
      x.d = [];

      // Transform base

      // e is the base 10 exponent.
      // i is where to slice str to get the first word of the digits array.
      i = (e + 1) % LOG_BASE;
      if (e < 0) i += LOG_BASE;

      if (i < len) {
        if (i) x.d.push(+str.slice(0, i));
        for (len -= LOG_BASE; i < len;) x.d.push(+str.slice(i, i += LOG_BASE));
        str = str.slice(i);
        i = LOG_BASE - str.length;
      } else {
        i -= len;
      }

      for (; i--;) str += '0';
      x.d.push(+str);

      if (external && (x.e > MAX_E || x.e < -MAX_E)) throw Error(exponentOutOfRange + e);
    } else {

      // Zero.
      x.s = 0;
      x.e = 0;
      x.d = [0];
    }

    return x;
  }


  /*
   * Round `x` to `sd` significant digits, using rounding mode `rm` if present (truncate otherwise).
   */
   function round(x, sd, rm) {
    var i, j, k, n, rd, doRound, w, xdi,
      xd = x.d;

    // rd: the rounding digit, i.e. the digit after the digit that may be rounded up.
    // w: the word of xd which contains the rounding digit, a base 1e7 number.
    // xdi: the index of w within xd.
    // n: the number of digits of w.
    // i: what would be the index of rd within w if all the numbers were 7 digits long (i.e. if
    // they had leading zeros)
    // j: if > 0, the actual index of rd within w (if < 0, rd is a leading zero).

    // Get the length of the first word of the digits array xd.
    for (n = 1, k = xd[0]; k >= 10; k /= 10) n++;
    i = sd - n;

    // Is the rounding digit in the first word of xd?
    if (i < 0) {
      i += LOG_BASE;
      j = sd;
      w = xd[xdi = 0];
    } else {
      xdi = Math.ceil((i + 1) / LOG_BASE);
      k = xd.length;
      if (xdi >= k) return x;
      w = k = xd[xdi];

      // Get the number of digits of w.
      for (n = 1; k >= 10; k /= 10) n++;

      // Get the index of rd within w.
      i %= LOG_BASE;

      // Get the index of rd within w, adjusted for leading zeros.
      // The number of leading zeros of w is given by LOG_BASE - n.
      j = i - LOG_BASE + n;
    }

    if (rm !== void 0) {
      k = mathpow(10, n - j - 1);

      // Get the rounding digit at index j of w.
      rd = w / k % 10 | 0;

      // Are there any non-zero digits after the rounding digit?
      doRound = sd < 0 || xd[xdi + 1] !== void 0 || w % k;

      // The expression `w % mathpow(10, n - j - 1)` returns all the digits of w to the right of the
      // digit at (left-to-right) index j, e.g. if w is 908714 and j is 2, the expression will give
      // 714.

      doRound = rm < 4
        ? (rd || doRound) && (rm == 0 || rm == (x.s < 0 ? 3 : 2))
        : rd > 5 || rd == 5 && (rm == 4 || doRound || rm == 6 &&

          // Check whether the digit to the left of the rounding digit is odd.
          ((i > 0 ? j > 0 ? w / mathpow(10, n - j) : 0 : xd[xdi - 1]) % 10) & 1 ||
            rm == (x.s < 0 ? 8 : 7));
    }

    if (sd < 1 || !xd[0]) {
      if (doRound) {
        k = getBase10Exponent(x);
        xd.length = 1;

        // Convert sd to decimal places.
        sd = sd - k - 1;

        // 1, 0.1, 0.01, 0.001, 0.0001 etc.
        xd[0] = mathpow(10, (LOG_BASE - sd % LOG_BASE) % LOG_BASE);
        x.e = mathfloor(-sd / LOG_BASE) || 0;
      } else {
        xd.length = 1;

        // Zero.
        xd[0] = x.e = x.s = 0;
      }

      return x;
    }

    // Remove excess digits.
    if (i == 0) {
      xd.length = xdi;
      k = 1;
      xdi--;
    } else {
      xd.length = xdi + 1;
      k = mathpow(10, LOG_BASE - i);

      // E.g. 56700 becomes 56000 if 7 is the rounding digit.
      // j > 0 means i > number of leading zeros of w.
      xd[xdi] = j > 0 ? (w / mathpow(10, n - j) % mathpow(10, j) | 0) * k : 0;
    }

    if (doRound) {
      for (;;) {

        // Is the digit to be rounded up in the first word of xd?
        if (xdi == 0) {
          if ((xd[0] += k) == BASE) {
            xd[0] = 1;
            ++x.e;
          }

          break;
        } else {
          xd[xdi] += k;
          if (xd[xdi] != BASE) break;
          xd[xdi--] = 0;
          k = 1;
        }
      }
    }

    // Remove trailing zeros.
    for (i = xd.length; xd[--i] === 0;) xd.pop();

    if (external && (x.e > MAX_E || x.e < -MAX_E)) {
      throw Error(exponentOutOfRange + getBase10Exponent(x));
    }

    return x;
  }


  function subtract(x, y) {
    var d, e, i, j, k, len, xd, xe, xLTy, yd,
      Ctor = x.constructor,
      pr = Ctor.precision;

    // Return y negated if x is zero.
    // Return x if y is zero and x is non-zero.
    if (!x.s || !y.s) {
      if (y.s) y.s = -y.s;
      else y = new Ctor(x);
      return external ? round(y, pr) : y;
    }

    xd = x.d;
    yd = y.d;

    // x and y are non-zero numbers with the same sign.

    e = y.e;
    xe = x.e;
    xd = xd.slice();
    k = xe - e;

    // If exponents differ...
    if (k) {
      xLTy = k < 0;

      if (xLTy) {
        d = xd;
        k = -k;
        len = yd.length;
      } else {
        d = yd;
        e = xe;
        len = xd.length;
      }

      // Numbers with massively different exponents would result in a very high number of zeros
      // needing to be prepended, but this can be avoided while still ensuring correct rounding by
      // limiting the number of zeros to `Math.ceil(pr / LOG_BASE) + 2`.
      i = Math.max(Math.ceil(pr / LOG_BASE), len) + 2;

      if (k > i) {
        k = i;
        d.length = 1;
      }

      // Prepend zeros to equalise exponents.
      d.reverse();
      for (i = k; i--;) d.push(0);
      d.reverse();

    // Base 1e7 exponents equal.
    } else {

      // Check digits to determine which is the bigger number.

      i = xd.length;
      len = yd.length;
      xLTy = i < len;
      if (xLTy) len = i;

      for (i = 0; i < len; i++) {
        if (xd[i] != yd[i]) {
          xLTy = xd[i] < yd[i];
          break;
        }
      }

      k = 0;
    }

    if (xLTy) {
      d = xd;
      xd = yd;
      yd = d;
      y.s = -y.s;
    }

    len = xd.length;

    // Append zeros to xd if shorter.
    // Don't add zeros to yd if shorter as subtraction only needs to start at yd length.
    for (i = yd.length - len; i > 0; --i) xd[len++] = 0;

    // Subtract yd from xd.
    for (i = yd.length; i > k;) {
      if (xd[--i] < yd[i]) {
        for (j = i; j && xd[--j] === 0;) xd[j] = BASE - 1;
        --xd[j];
        xd[i] += BASE;
      }

      xd[i] -= yd[i];
    }

    // Remove trailing zeros.
    for (; xd[--len] === 0;) xd.pop();

    // Remove leading zeros and adjust exponent accordingly.
    for (; xd[0] === 0; xd.shift()) --e;

    // Zero?
    if (!xd[0]) return new Ctor(0);

    y.d = xd;
    y.e = e;

    //return external && xd.length >= pr / LOG_BASE ? round(y, pr) : y;
    return external ? round(y, pr) : y;
  }


  function toString(x, isExp, sd) {
    var k,
      e = getBase10Exponent(x),
      str = digitsToString(x.d),
      len = str.length;

    if (isExp) {
      if (sd && (k = sd - len) > 0) {
        str = str.charAt(0) + '.' + str.slice(1) + getZeroString(k);
      } else if (len > 1) {
        str = str.charAt(0) + '.' + str.slice(1);
      }

      str = str + (e < 0 ? 'e' : 'e+') + e;
    } else if (e < 0) {
      str = '0.' + getZeroString(-e - 1) + str;
      if (sd && (k = sd - len) > 0) str += getZeroString(k);
    } else if (e >= len) {
      str += getZeroString(e + 1 - len);
      if (sd && (k = sd - e - 1) > 0) str = str + '.' + getZeroString(k);
    } else {
      if ((k = e + 1) < len) str = str.slice(0, k) + '.' + str.slice(k);
      if (sd && (k = sd - len) > 0) {
        if (e + 1 === len) str += '.';
        str += getZeroString(k);
      }
    }

    return x.s < 0 ? '-' + str : str;
  }


  // Does not strip trailing zeros.
  function truncate(arr, len) {
    if (arr.length > len) {
      arr.length = len;
      return true;
    }
  }


  // Decimal methods


  /*
   *  clone
   *  config/set
   */


  /*
   * Create and return a Decimal constructor with the same configuration properties as this Decimal
   * constructor.
   *
   */
  function clone(obj) {
    var i, p, ps;

    /*
     * The Decimal constructor and exported function.
     * Return a new Decimal instance.
     *
     * value {number|string|Decimal} A numeric value.
     *
     */
    function Decimal(value) {
      var x = this;

      // Decimal called without new.
      if (!(x instanceof Decimal)) return new Decimal(value);

      // Retain a reference to this Decimal constructor, and shadow Decimal.prototype.constructor
      // which points to Object.
      x.constructor = Decimal;

      // Duplicate.
      if (value instanceof Decimal) {
        x.s = value.s;
        x.e = value.e;
        x.d = (value = value.d) ? value.slice() : value;
        return;
      }

      if (typeof value === 'number') {

        // Reject Infinity/NaN.
        if (value * 0 !== 0) {
          throw Error(invalidArgument + value);
        }

        if (value > 0) {
          x.s = 1;
        } else if (value < 0) {
          value = -value;
          x.s = -1;
        } else {
          x.s = 0;
          x.e = 0;
          x.d = [0];
          return;
        }

        // Fast path for small integers.
        if (value === ~~value && value < 1e7) {
          x.e = 0;
          x.d = [value];
          return;
        }

        return parseDecimal(x, value.toString());
      } else if (typeof value !== 'string') {
        throw Error(invalidArgument + value);
      }

      // Minus sign?
      if (value.charCodeAt(0) === 45) {
        value = value.slice(1);
        x.s = -1;
      } else {
        x.s = 1;
      }

      if (isDecimal.test(value)) parseDecimal(x, value);
      else throw Error(invalidArgument + value);
    }

    Decimal.prototype = P;

    Decimal.ROUND_UP = 0;
    Decimal.ROUND_DOWN = 1;
    Decimal.ROUND_CEIL = 2;
    Decimal.ROUND_FLOOR = 3;
    Decimal.ROUND_HALF_UP = 4;
    Decimal.ROUND_HALF_DOWN = 5;
    Decimal.ROUND_HALF_EVEN = 6;
    Decimal.ROUND_HALF_CEIL = 7;
    Decimal.ROUND_HALF_FLOOR = 8;

    Decimal.clone = clone;
    Decimal.config = Decimal.set = config;

    if (obj === void 0) obj = {};
    if (obj) {
      ps = ['precision', 'rounding', 'toExpNeg', 'toExpPos', 'LN10'];
      for (i = 0; i < ps.length;) if (!obj.hasOwnProperty(p = ps[i++])) obj[p] = this[p];
    }

    Decimal.config(obj);

    return Decimal;
  }


  /*
   * Configure global settings for a Decimal constructor.
   *
   * `obj` is an object with one or more of the following properties,
   *
   *   precision  {number}
   *   rounding   {number}
   *   toExpNeg   {number}
   *   toExpPos   {number}
   *
   * E.g. Decimal.config({ precision: 20, rounding: 4 })
   *
   */
  function config(obj) {
    if (!obj || typeof obj !== 'object') {
      throw Error(decimalError + 'Object expected');
    }
    var i, p, v,
      ps = [
        'precision', 1, MAX_DIGITS,
        'rounding', 0, 8,
        'toExpNeg', -1 / 0, 0,
        'toExpPos', 0, 1 / 0
      ];

    for (i = 0; i < ps.length; i += 3) {
      if ((v = obj[p = ps[i]]) !== void 0) {
        if (mathfloor(v) === v && v >= ps[i + 1] && v <= ps[i + 2]) this[p] = v;
        else throw Error(invalidArgument + p + ': ' + v);
      }
    }

    if ((v = obj[p = 'LN10']) !== void 0) {
        if (v == Math.LN10) this[p] = new this(v);
        else throw Error(invalidArgument + p + ': ' + v);
    }

    return this;
  }


  // Create and configure initial Decimal constructor.
  Decimal = clone(Decimal);

  Decimal['default'] = Decimal.Decimal = Decimal;

  // Internal constant.
  ONE = new Decimal(1);


  // Export.


  // AMD.
  if (true) {
    !(__WEBPACK_AMD_DEFINE_RESULT__ = (function () {
      return Decimal;
    }).call(exports, __webpack_require__, exports, module),
		__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));

  // Node and other environments that support module.exports.
  } else // removed by dead control flow
{}
})(this);


/***/ },

/***/ 305
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(4200).get;


/***/ },

/***/ 2938
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(8695).isPlainObject;


/***/ },

/***/ 25
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(1334).last;


/***/ },

/***/ 4338
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(5938).maxBy;


/***/ },

/***/ 2972
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(924).minBy;


/***/ },

/***/ 3412
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(5012).range;


/***/ },

/***/ 184
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(4259).sortBy;


/***/ },

/***/ 4297
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(5259).throttle;


/***/ },

/***/ 1081
(module, __unused_webpack_exports, __webpack_require__) {

module.exports = __webpack_require__(2810).uniqBy;


/***/ },

/***/ 4534
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isEqualsSameValueZero(value, other) {
    return value === other || (Number.isNaN(value) && Number.isNaN(other));
}

exports.isEqualsSameValueZero = isEqualsSameValueZero;


/***/ },

/***/ 8193
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isUnsafeProperty(key) {
    return key === '__proto__';
}

exports.isUnsafeProperty = isUnsafeProperty;


/***/ },

/***/ 5711
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function flatten(arr, depth = 1) {
    const result = [];
    const flooredDepth = Math.floor(depth);
    const recursive = (arr, currentDepth) => {
        for (let i = 0; i < arr.length; i++) {
            const item = arr[i];
            if (Array.isArray(item) && currentDepth < flooredDepth) {
                recursive(item, currentDepth + 1);
            }
            else {
                result.push(item);
            }
        }
    };
    recursive(arr, 0);
    return result;
}

exports.flatten = flatten;


/***/ },

/***/ 645
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function last(arr) {
    return arr[arr.length - 1];
}

exports.last = last;


/***/ },

/***/ 334
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function maxBy(items, getValue) {
    if (items.length === 0) {
        return undefined;
    }
    let maxElement = items[0];
    let max = getValue(maxElement, 0, items);
    for (let i = 1; i < items.length; i++) {
        const element = items[i];
        const value = getValue(element, i, items);
        if (value > max) {
            max = value;
            maxElement = element;
        }
    }
    return maxElement;
}

exports.maxBy = maxBy;


/***/ },

/***/ 8240
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function minBy(items, getValue) {
    if (items.length === 0) {
        return undefined;
    }
    let minElement = items[0];
    let min = getValue(minElement, 0, items);
    for (let i = 1; i < items.length; i++) {
        const element = items[i];
        const value = getValue(element, i, items);
        if (value < min) {
            min = value;
            minElement = element;
        }
    }
    return minElement;
}

exports.minBy = minBy;


/***/ },

/***/ 8805
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function uniqBy(arr, mapper) {
    const map = new Map();
    for (let i = 0; i < arr.length; i++) {
        const item = arr[i];
        const key = mapper(item, i, arr);
        if (!map.has(key)) {
            map.set(key, item);
        }
    }
    return Array.from(map.values());
}

exports.uniqBy = uniqBy;


/***/ },

/***/ 3500
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function getPriority(a) {
    if (typeof a === 'symbol') {
        return 1;
    }
    if (a === null) {
        return 2;
    }
    if (a === undefined) {
        return 3;
    }
    if (a !== a) {
        return 4;
    }
    return 0;
}
const compareValues = (a, b, order) => {
    if (a !== b) {
        const aPriority = getPriority(a);
        const bPriority = getPriority(b);
        if (aPriority === bPriority && aPriority === 0) {
            if (a < b) {
                return order === 'desc' ? 1 : -1;
            }
            if (a > b) {
                return order === 'desc' ? -1 : 1;
            }
        }
        return order === 'desc' ? bPriority - aPriority : aPriority - bPriority;
    }
    return 0;
};

exports.compareValues = compareValues;


/***/ },

/***/ 6012
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function getSymbols(object) {
    return Object.getOwnPropertySymbols(object).filter(symbol => Object.prototype.propertyIsEnumerable.call(object, symbol));
}

exports.getSymbols = getSymbols;


/***/ },

/***/ 2049
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function getTag(value) {
    if (value == null) {
        return value === undefined ? '[object Undefined]' : '[object Null]';
    }
    return Object.prototype.toString.call(value);
}

exports.getTag = getTag;


/***/ },

/***/ 5112
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isDeepKey(key) {
    switch (typeof key) {
        case 'number':
        case 'symbol': {
            return false;
        }
        case 'string': {
            return key.includes('.') || key.includes('[') || key.includes(']');
        }
    }
}

exports.isDeepKey = isDeepKey;


/***/ },

/***/ 8509
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const IS_UNSIGNED_INTEGER = /^(?:0|[1-9]\d*)$/;
function isIndex(value, length = Number.MAX_SAFE_INTEGER) {
    switch (typeof value) {
        case 'number': {
            return Number.isInteger(value) && value >= 0 && value < length;
        }
        case 'symbol': {
            return false;
        }
        case 'string': {
            return IS_UNSIGNED_INTEGER.test(value);
        }
    }
}

exports.isIndex = isIndex;


/***/ },

/***/ 316
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isIndex = __webpack_require__(8509);
const isArrayLike = __webpack_require__(58);
const isObject = __webpack_require__(4905);
const isEqualsSameValueZero = __webpack_require__(4534);

function isIterateeCall(value, index, object) {
    if (!isObject.isObject(object)) {
        return false;
    }
    if ((typeof index === 'number' && isArrayLike.isArrayLike(object) && isIndex.isIndex(index) && index < object.length) ||
        (typeof index === 'string' && index in object)) {
        return isEqualsSameValueZero.isEqualsSameValueZero(object[index], value);
    }
    return false;
}

exports.isIterateeCall = isIterateeCall;


/***/ },

/***/ 3998
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isSymbol = __webpack_require__(1366);

const regexIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
const regexIsPlainProp = /^\w*$/;
function isKey(value, object) {
    if (Array.isArray(value)) {
        return false;
    }
    if (typeof value === 'number' || typeof value === 'boolean' || value == null || isSymbol.isSymbol(value)) {
        return true;
    }
    return ((typeof value === 'string' && (regexIsPlainProp.test(value) || !regexIsDeepProp.test(value))) ||
        (object != null && Object.hasOwn(object, value)));
}

exports.isKey = isKey;


/***/ },

/***/ 9184
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const regexpTag = '[object RegExp]';
const stringTag = '[object String]';
const numberTag = '[object Number]';
const booleanTag = '[object Boolean]';
const argumentsTag = '[object Arguments]';
const symbolTag = '[object Symbol]';
const dateTag = '[object Date]';
const mapTag = '[object Map]';
const setTag = '[object Set]';
const arrayTag = '[object Array]';
const functionTag = '[object Function]';
const arrayBufferTag = '[object ArrayBuffer]';
const objectTag = '[object Object]';
const errorTag = '[object Error]';
const dataViewTag = '[object DataView]';
const uint8ArrayTag = '[object Uint8Array]';
const uint8ClampedArrayTag = '[object Uint8ClampedArray]';
const uint16ArrayTag = '[object Uint16Array]';
const uint32ArrayTag = '[object Uint32Array]';
const bigUint64ArrayTag = '[object BigUint64Array]';
const int8ArrayTag = '[object Int8Array]';
const int16ArrayTag = '[object Int16Array]';
const int32ArrayTag = '[object Int32Array]';
const bigInt64ArrayTag = '[object BigInt64Array]';
const float32ArrayTag = '[object Float32Array]';
const float64ArrayTag = '[object Float64Array]';

exports.argumentsTag = argumentsTag;
exports.arrayBufferTag = arrayBufferTag;
exports.arrayTag = arrayTag;
exports.bigInt64ArrayTag = bigInt64ArrayTag;
exports.bigUint64ArrayTag = bigUint64ArrayTag;
exports.booleanTag = booleanTag;
exports.dataViewTag = dataViewTag;
exports.dateTag = dateTag;
exports.errorTag = errorTag;
exports.float32ArrayTag = float32ArrayTag;
exports.float64ArrayTag = float64ArrayTag;
exports.functionTag = functionTag;
exports.int16ArrayTag = int16ArrayTag;
exports.int32ArrayTag = int32ArrayTag;
exports.int8ArrayTag = int8ArrayTag;
exports.mapTag = mapTag;
exports.numberTag = numberTag;
exports.objectTag = objectTag;
exports.regexpTag = regexpTag;
exports.setTag = setTag;
exports.stringTag = stringTag;
exports.symbolTag = symbolTag;
exports.uint16ArrayTag = uint16ArrayTag;
exports.uint32ArrayTag = uint32ArrayTag;
exports.uint8ArrayTag = uint8ArrayTag;
exports.uint8ClampedArrayTag = uint8ClampedArrayTag;


/***/ },

/***/ 4483
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function toArray(value) {
    return Array.isArray(value) ? value : Array.from(value);
}

exports.toArray = toArray;


/***/ },

/***/ 1465
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function toKey(value) {
    if (typeof value === 'string' || typeof value === 'symbol') {
        return value;
    }
    if (Object.is(value?.valueOf?.(), -0)) {
        return '-0';
    }
    return String(value);
}

exports.toKey = toKey;


/***/ },

/***/ 1334
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const last$1 = __webpack_require__(645);
const toArray = __webpack_require__(4483);
const isArrayLike = __webpack_require__(58);

function last(array) {
    if (!isArrayLike.isArrayLike(array)) {
        return undefined;
    }
    return last$1.last(toArray.toArray(array));
}

exports.last = last;


/***/ },

/***/ 3097
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const compareValues = __webpack_require__(3500);
const isKey = __webpack_require__(3998);
const toPath = __webpack_require__(3025);

function orderBy(collection, criteria, orders, guard) {
    if (collection == null) {
        return [];
    }
    orders = guard ? undefined : orders;
    if (!Array.isArray(collection)) {
        collection = Object.values(collection);
    }
    if (!Array.isArray(criteria)) {
        criteria = criteria == null ? [null] : [criteria];
    }
    if (criteria.length === 0) {
        criteria = [null];
    }
    if (!Array.isArray(orders)) {
        orders = orders == null ? [] : [orders];
    }
    orders = orders.map(order => String(order));
    const getValueByNestedPath = (object, path) => {
        let target = object;
        for (let i = 0; i < path.length && target != null; ++i) {
            target = target[path[i]];
        }
        return target;
    };
    const getValueByCriterion = (criterion, object) => {
        if (object == null || criterion == null) {
            return object;
        }
        if (typeof criterion === 'object' && 'key' in criterion) {
            if (Object.hasOwn(object, criterion.key)) {
                return object[criterion.key];
            }
            return getValueByNestedPath(object, criterion.path);
        }
        if (typeof criterion === 'function') {
            return criterion(object);
        }
        if (Array.isArray(criterion)) {
            return getValueByNestedPath(object, criterion);
        }
        if (typeof object === 'object') {
            return object[criterion];
        }
        return object;
    };
    const preparedCriteria = criteria.map((criterion) => {
        if (Array.isArray(criterion) && criterion.length === 1) {
            criterion = criterion[0];
        }
        if (criterion == null || typeof criterion === 'function' || Array.isArray(criterion) || isKey.isKey(criterion)) {
            return criterion;
        }
        return { key: criterion, path: toPath.toPath(criterion) };
    });
    const preparedCollection = collection.map(item => ({
        original: item,
        criteria: preparedCriteria.map((criterion) => getValueByCriterion(criterion, item)),
    }));
    return preparedCollection
        .slice()
        .sort((a, b) => {
        for (let i = 0; i < preparedCriteria.length; i++) {
            const comparedResult = compareValues.compareValues(a.criteria[i], b.criteria[i], orders[i]);
            if (comparedResult !== 0) {
                return comparedResult;
            }
        }
        return 0;
    })
        .map(item => item.original);
}

exports.orderBy = orderBy;


/***/ },

/***/ 4259
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const orderBy = __webpack_require__(3097);
const flatten = __webpack_require__(5711);
const isIterateeCall = __webpack_require__(316);

function sortBy(collection, ...criteria) {
    const length = criteria.length;
    if (length > 1 && isIterateeCall.isIterateeCall(collection, criteria[0], criteria[1])) {
        criteria = [];
    }
    else if (length > 2 && isIterateeCall.isIterateeCall(criteria[0], criteria[1], criteria[2])) {
        criteria = [criteria[0]];
    }
    return orderBy.orderBy(collection, flatten.flatten(criteria), ['asc']);
}

exports.sortBy = sortBy;


/***/ },

/***/ 2810
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const uniqBy$1 = __webpack_require__(8805);
const ary = __webpack_require__(6858);
const identity = __webpack_require__(6440);
const isArrayLikeObject = __webpack_require__(8161);
const iteratee = __webpack_require__(8202);

function uniqBy(array, iteratee$1 = identity.identity) {
    if (!isArrayLikeObject.isArrayLikeObject(array)) {
        return [];
    }
    return uniqBy$1.uniqBy(Array.from(array), ary.ary(iteratee.iteratee(iteratee$1), 1));
}

exports.uniqBy = uniqBy;


/***/ },

/***/ 8
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const debounce$1 = __webpack_require__(6773);

function debounce(func, debounceMs = 0, options = {}) {
    if (typeof options !== 'object') {
        options = {};
    }
    const { leading = false, trailing = true, maxWait } = options;
    const edges = Array(2);
    if (leading) {
        edges[0] = 'leading';
    }
    if (trailing) {
        edges[1] = 'trailing';
    }
    let result = undefined;
    let pendingAt = null;
    const _debounced = debounce$1.debounce(function (...args) {
        result = func.apply(this, args);
        pendingAt = null;
    }, debounceMs, { edges });
    const debounced = function (...args) {
        if (maxWait != null) {
            if (pendingAt === null) {
                pendingAt = Date.now();
            }
            if (Date.now() - pendingAt >= maxWait) {
                result = func.apply(this, args);
                pendingAt = Date.now();
                _debounced.cancel();
                _debounced.schedule();
                return result;
            }
        }
        _debounced.apply(this, args);
        return result;
    };
    const flush = () => {
        _debounced.flush();
        return result;
    };
    debounced.cancel = _debounced.cancel;
    debounced.flush = flush;
    return debounced;
}

exports.debounce = debounce;


/***/ },

/***/ 5259
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const debounce = __webpack_require__(8);

function throttle(func, throttleMs = 0, options = {}) {
    const { leading = true, trailing = true } = options;
    return debounce.debounce(func, throttleMs, {
        leading,
        maxWait: throttleMs,
        trailing,
    });
}

exports.throttle = throttle;


/***/ },

/***/ 5938
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const maxBy$1 = __webpack_require__(334);
const identity = __webpack_require__(6440);
const iteratee = __webpack_require__(8202);

function maxBy(items, iteratee$1) {
    if (items == null) {
        return undefined;
    }
    return maxBy$1.maxBy(Array.from(items), iteratee.iteratee(iteratee$1 ?? identity.identity));
}

exports.maxBy = maxBy;


/***/ },

/***/ 924
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const minBy$1 = __webpack_require__(8240);
const identity = __webpack_require__(6440);
const iteratee = __webpack_require__(8202);

function minBy(items, iteratee$1) {
    if (items == null) {
        return undefined;
    }
    return minBy$1.minBy(Array.from(items), iteratee.iteratee(iteratee$1 ?? identity.identity));
}

exports.minBy = minBy;


/***/ },

/***/ 5012
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isIterateeCall = __webpack_require__(316);
const toFinite = __webpack_require__(4569);

function range(start, end, step) {
    if (step && typeof step !== 'number' && isIterateeCall.isIterateeCall(start, end, step)) {
        end = step = undefined;
    }
    start = toFinite.toFinite(start);
    if (end === undefined) {
        end = start;
        start = 0;
    }
    else {
        end = toFinite.toFinite(end);
    }
    step = step === undefined ? (start < end ? 1 : -1) : toFinite.toFinite(step);
    const length = Math.max(Math.ceil((end - start) / (step || 1)), 0);
    const result = new Array(length);
    for (let index = 0; index < length; index++) {
        result[index] = start;
        start += step;
    }
    return result;
}

exports.range = range;


/***/ },

/***/ 3923
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const cloneDeepWith = __webpack_require__(9467);

function cloneDeep(obj) {
    return cloneDeepWith.cloneDeepWith(obj);
}

exports.cloneDeep = cloneDeep;


/***/ },

/***/ 9467
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const cloneDeepWith$1 = __webpack_require__(3964);
const getTag = __webpack_require__(2049);
const tags = __webpack_require__(9184);

function cloneDeepWith(obj, customizer) {
    return cloneDeepWith$1.cloneDeepWith(obj, (value, key, object, stack) => {
        const cloned = customizer?.(value, key, object, stack);
        if (cloned !== undefined) {
            return cloned;
        }
        if (typeof obj !== 'object') {
            return undefined;
        }
        if (getTag.getTag(obj) === tags.objectTag && typeof obj.constructor !== 'function') {
            const result = {};
            stack.set(obj, result);
            cloneDeepWith$1.copyProperties(result, obj, object, stack);
            return result;
        }
        switch (Object.prototype.toString.call(obj)) {
            case tags.numberTag:
            case tags.stringTag:
            case tags.booleanTag: {
                const result = new obj.constructor(obj?.valueOf());
                cloneDeepWith$1.copyProperties(result, obj);
                return result;
            }
            case tags.argumentsTag: {
                const result = {};
                cloneDeepWith$1.copyProperties(result, obj);
                result.length = obj.length;
                result[Symbol.iterator] = obj[Symbol.iterator];
                return result;
            }
            default: {
                return undefined;
            }
        }
    });
}

exports.cloneDeepWith = cloneDeepWith;


/***/ },

/***/ 4200
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isUnsafeProperty = __webpack_require__(8193);
const isDeepKey = __webpack_require__(5112);
const toKey = __webpack_require__(1465);
const toPath = __webpack_require__(3025);

function get(object, path, defaultValue) {
    if (object == null) {
        return defaultValue;
    }
    switch (typeof path) {
        case 'string': {
            if (isUnsafeProperty.isUnsafeProperty(path)) {
                return defaultValue;
            }
            const result = object[path];
            if (result === undefined) {
                if (isDeepKey.isDeepKey(path)) {
                    return get(object, toPath.toPath(path), defaultValue);
                }
                else {
                    return defaultValue;
                }
            }
            return result;
        }
        case 'number':
        case 'symbol': {
            if (typeof path === 'number') {
                path = toKey.toKey(path);
            }
            const result = object[path];
            if (result === undefined) {
                return defaultValue;
            }
            return result;
        }
        default: {
            if (Array.isArray(path)) {
                return getWithPath(object, path, defaultValue);
            }
            if (Object.is(path?.valueOf(), -0)) {
                path = '-0';
            }
            else {
                path = String(path);
            }
            if (isUnsafeProperty.isUnsafeProperty(path)) {
                return defaultValue;
            }
            const result = object[path];
            if (result === undefined) {
                return defaultValue;
            }
            return result;
        }
    }
}
function getWithPath(object, path, defaultValue) {
    if (path.length === 0) {
        return defaultValue;
    }
    let current = object;
    for (let index = 0; index < path.length; index++) {
        if (current == null) {
            return defaultValue;
        }
        if (isUnsafeProperty.isUnsafeProperty(path[index])) {
            return defaultValue;
        }
        current = current[path[index]];
    }
    if (current === undefined) {
        return defaultValue;
    }
    return current;
}

exports.get = get;


/***/ },

/***/ 7324
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isDeepKey = __webpack_require__(5112);
const isIndex = __webpack_require__(8509);
const isArguments = __webpack_require__(2984);
const toPath = __webpack_require__(3025);

function has(object, path) {
    let resolvedPath;
    if (Array.isArray(path)) {
        resolvedPath = path;
    }
    else if (typeof path === 'string' && isDeepKey.isDeepKey(path) && object?.[path] == null) {
        resolvedPath = toPath.toPath(path);
    }
    else {
        resolvedPath = [path];
    }
    if (resolvedPath.length === 0) {
        return false;
    }
    let current = object;
    for (let i = 0; i < resolvedPath.length; i++) {
        const key = resolvedPath[i];
        if (current == null || !Object.hasOwn(current, key)) {
            const isSparseIndex = (Array.isArray(current) || isArguments.isArguments(current)) && isIndex.isIndex(key) && key < current.length;
            if (!isSparseIndex) {
                return false;
            }
        }
        current = current[key];
    }
    return true;
}

exports.has = has;


/***/ },

/***/ 3403
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const get = __webpack_require__(4200);

function property(path) {
    return function (object) {
        return get.get(object, path);
    };
}

exports.property = property;


/***/ },

/***/ 2984
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const getTag = __webpack_require__(2049);

function isArguments(value) {
    return value !== null && typeof value === 'object' && getTag.getTag(value) === '[object Arguments]';
}

exports.isArguments = isArguments;


/***/ },

/***/ 58
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isLength = __webpack_require__(9181);

function isArrayLike(value) {
    return value != null && typeof value !== 'function' && isLength.isLength(value.length);
}

exports.isArrayLike = isArrayLike;


/***/ },

/***/ 8161
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isArrayLike = __webpack_require__(58);
const isObjectLike = __webpack_require__(1846);

function isArrayLikeObject(value) {
    return isObjectLike.isObjectLike(value) && isArrayLike.isArrayLike(value);
}

exports.isArrayLikeObject = isArrayLikeObject;


/***/ },

/***/ 717
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isMatchWith = __webpack_require__(8273);

function isMatch(target, source) {
    return isMatchWith.isMatchWith(target, source, () => undefined);
}

exports.isMatch = isMatch;


/***/ },

/***/ 8273
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isObject = __webpack_require__(4905);
const isPrimitive = __webpack_require__(2520);
const isEqualsSameValueZero = __webpack_require__(4534);

function isMatchWith(target, source, compare) {
    if (typeof compare !== 'function') {
        return isMatchWith(target, source, () => undefined);
    }
    return isMatchWithInternal(target, source, function doesMatch(objValue, srcValue, key, object, source, stack) {
        const isEqual = compare(objValue, srcValue, key, object, source, stack);
        if (isEqual !== undefined) {
            return Boolean(isEqual);
        }
        return isMatchWithInternal(objValue, srcValue, doesMatch, stack);
    }, new Map());
}
function isMatchWithInternal(target, source, compare, stack) {
    if (source === target) {
        return true;
    }
    switch (typeof source) {
        case 'object': {
            return isObjectMatch(target, source, compare, stack);
        }
        case 'function': {
            const sourceKeys = Object.keys(source);
            if (sourceKeys.length > 0) {
                return isMatchWithInternal(target, { ...source }, compare, stack);
            }
            return isEqualsSameValueZero.isEqualsSameValueZero(target, source);
        }
        default: {
            if (!isObject.isObject(target)) {
                return isEqualsSameValueZero.isEqualsSameValueZero(target, source);
            }
            if (typeof source === 'string') {
                return source === '';
            }
            return true;
        }
    }
}
function isObjectMatch(target, source, compare, stack) {
    if (source == null) {
        return true;
    }
    if (Array.isArray(source)) {
        return isArrayMatch(target, source, compare, stack);
    }
    if (source instanceof Map) {
        return isMapMatch(target, source, compare, stack);
    }
    if (source instanceof Set) {
        return isSetMatch(target, source, compare, stack);
    }
    const keys = Object.keys(source);
    if (target == null || isPrimitive.isPrimitive(target)) {
        return keys.length === 0;
    }
    if (keys.length === 0) {
        return true;
    }
    if (stack?.has(source)) {
        return stack.get(source) === target;
    }
    stack?.set(source, target);
    try {
        for (let i = 0; i < keys.length; i++) {
            const key = keys[i];
            if (!isPrimitive.isPrimitive(target) && !(key in target)) {
                return false;
            }
            if (source[key] === undefined && target[key] !== undefined) {
                return false;
            }
            if (source[key] === null && target[key] !== null) {
                return false;
            }
            const isEqual = compare(target[key], source[key], key, target, source, stack);
            if (!isEqual) {
                return false;
            }
        }
        return true;
    }
    finally {
        stack?.delete(source);
    }
}
function isMapMatch(target, source, compare, stack) {
    if (source.size === 0) {
        return true;
    }
    if (!(target instanceof Map)) {
        return false;
    }
    for (const [key, sourceValue] of source.entries()) {
        const targetValue = target.get(key);
        const isEqual = compare(targetValue, sourceValue, key, target, source, stack);
        if (isEqual === false) {
            return false;
        }
    }
    return true;
}
function isArrayMatch(target, source, compare, stack) {
    if (source.length === 0) {
        return true;
    }
    if (!Array.isArray(target)) {
        return false;
    }
    const countedIndex = new Set();
    for (let i = 0; i < source.length; i++) {
        const sourceItem = source[i];
        let found = false;
        for (let j = 0; j < target.length; j++) {
            if (countedIndex.has(j)) {
                continue;
            }
            const targetItem = target[j];
            let matches = false;
            const isEqual = compare(targetItem, sourceItem, i, target, source, stack);
            if (isEqual) {
                matches = true;
            }
            if (matches) {
                countedIndex.add(j);
                found = true;
                break;
            }
        }
        if (!found) {
            return false;
        }
    }
    return true;
}
function isSetMatch(target, source, compare, stack) {
    if (source.size === 0) {
        return true;
    }
    if (!(target instanceof Set)) {
        return false;
    }
    return isArrayMatch([...target], [...source], compare, stack);
}

exports.isMatchWith = isMatchWith;
exports.isSetMatch = isSetMatch;


/***/ },

/***/ 4905
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isObject(value) {
    return value !== null && (typeof value === 'object' || typeof value === 'function');
}

exports.isObject = isObject;


/***/ },

/***/ 1846
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isObjectLike(value) {
    return typeof value === 'object' && value !== null;
}

exports.isObjectLike = isObjectLike;


/***/ },

/***/ 8695
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isPlainObject(object) {
    if (typeof object !== 'object') {
        return false;
    }
    if (object == null) {
        return false;
    }
    if (Object.getPrototypeOf(object) === null) {
        return true;
    }
    if (Object.prototype.toString.call(object) !== '[object Object]') {
        const tag = object[Symbol.toStringTag];
        if (tag == null) {
            return false;
        }
        const isTagReadonly = !Object.getOwnPropertyDescriptor(object, Symbol.toStringTag)?.writable;
        if (isTagReadonly) {
            return false;
        }
        return object.toString() === `[object ${tag}]`;
    }
    let proto = object;
    while (Object.getPrototypeOf(proto) !== null) {
        proto = Object.getPrototypeOf(proto);
    }
    return Object.getPrototypeOf(object) === proto;
}

exports.isPlainObject = isPlainObject;


/***/ },

/***/ 1366
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isSymbol(value) {
    return typeof value === 'symbol' || value instanceof Symbol;
}

exports.isSymbol = isSymbol;


/***/ },

/***/ 7861
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isMatch = __webpack_require__(717);
const cloneDeep = __webpack_require__(3844);

function matches(source) {
    source = cloneDeep.cloneDeep(source);
    return (target) => {
        return isMatch.isMatch(target, source);
    };
}

exports.matches = matches;


/***/ },

/***/ 3036
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isMatch = __webpack_require__(717);
const toKey = __webpack_require__(1465);
const cloneDeep = __webpack_require__(3923);
const get = __webpack_require__(4200);
const has = __webpack_require__(7324);

function matchesProperty(property, source) {
    switch (typeof property) {
        case 'object': {
            if (Object.is(property?.valueOf(), -0)) {
                property = '-0';
            }
            break;
        }
        case 'number': {
            property = toKey.toKey(property);
            break;
        }
    }
    source = cloneDeep.cloneDeep(source);
    return function (target) {
        const result = get.get(target, property);
        if (result === undefined) {
            return has.has(target, property);
        }
        if (source === undefined) {
            return result === undefined;
        }
        return isMatch.isMatch(result, source);
    };
}

exports.matchesProperty = matchesProperty;


/***/ },

/***/ 8202
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const identity = __webpack_require__(6440);
const property = __webpack_require__(3403);
const matches = __webpack_require__(7861);
const matchesProperty = __webpack_require__(3036);

function iteratee(value) {
    if (value == null) {
        return identity.identity;
    }
    switch (typeof value) {
        case 'function': {
            return value;
        }
        case 'object': {
            if (Array.isArray(value) && value.length === 2) {
                return matchesProperty.matchesProperty(value[0], value[1]);
            }
            return matches.matches(value);
        }
        case 'string':
        case 'symbol':
        case 'number': {
            return property.property(value);
        }
    }
}

exports.iteratee = iteratee;


/***/ },

/***/ 4569
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const toNumber = __webpack_require__(8919);

function toFinite(value) {
    if (!value) {
        return value === 0 ? value : 0;
    }
    value = toNumber.toNumber(value);
    if (value === Infinity || value === -Infinity) {
        const sign = value < 0 ? -1 : 1;
        return sign * Number.MAX_VALUE;
    }
    return value === value ? value : 0;
}

exports.toFinite = toFinite;


/***/ },

/***/ 8919
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const isSymbol = __webpack_require__(1366);

function toNumber(value) {
    if (isSymbol.isSymbol(value)) {
        return NaN;
    }
    return Number(value);
}

exports.toNumber = toNumber;


/***/ },

/***/ 3025
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const toString = __webpack_require__(1863);
const toKey = __webpack_require__(1465);

function toPath(deepKey) {
    if (Array.isArray(deepKey)) {
        return deepKey.map(toKey.toKey);
    }
    if (typeof deepKey === 'symbol') {
        return [deepKey];
    }
    deepKey = toString.toString(deepKey);
    const result = [];
    const length = deepKey.length;
    if (length === 0) {
        return result;
    }
    let index = 0;
    let key = '';
    let quoteChar = '';
    let bracket = false;
    if (deepKey.charCodeAt(0) === 46) {
        result.push('');
        index++;
    }
    while (index < length) {
        const char = deepKey[index];
        if (quoteChar) {
            if (char === '\\' && index + 1 < length) {
                index++;
                key += deepKey[index];
            }
            else if (char === quoteChar) {
                quoteChar = '';
            }
            else {
                key += char;
            }
        }
        else if (bracket) {
            if (char === '"' || char === "'") {
                quoteChar = char;
            }
            else if (char === ']') {
                bracket = false;
                result.push(key);
                key = '';
            }
            else {
                key += char;
            }
        }
        else {
            if (char === '[') {
                bracket = true;
                if (key) {
                    result.push(key);
                    key = '';
                }
            }
            else if (char === '.') {
                if (key) {
                    result.push(key);
                    key = '';
                }
            }
            else {
                key += char;
            }
        }
        index++;
    }
    if (key) {
        result.push(key);
    }
    return result;
}

exports.toPath = toPath;


/***/ },

/***/ 1863
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function toString(value) {
    if (value == null) {
        return '';
    }
    if (typeof value === 'string') {
        return value;
    }
    if (Array.isArray(value)) {
        return value.map(toString).join(',');
    }
    const result = String(value);
    if (result === '0' && Object.is(Number(value), -0)) {
        return '-0';
    }
    return result;
}

exports.toString = toString;


/***/ },

/***/ 6858
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function ary(func, n) {
    return function (...args) {
        return func.apply(this, args.slice(0, n));
    };
}

exports.ary = ary;


/***/ },

/***/ 6773
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function debounce(func, debounceMs, { signal, edges } = {}) {
    let pendingThis = undefined;
    let pendingArgs = null;
    const leading = edges != null && edges.includes('leading');
    const trailing = edges == null || edges.includes('trailing');
    const invoke = () => {
        if (pendingArgs !== null) {
            func.apply(pendingThis, pendingArgs);
            pendingThis = undefined;
            pendingArgs = null;
        }
    };
    const onTimerEnd = () => {
        if (trailing) {
            invoke();
        }
        cancel();
    };
    let timeoutId = null;
    const schedule = () => {
        if (timeoutId != null) {
            clearTimeout(timeoutId);
        }
        timeoutId = setTimeout(() => {
            timeoutId = null;
            onTimerEnd();
        }, debounceMs);
    };
    const cancelTimer = () => {
        if (timeoutId !== null) {
            clearTimeout(timeoutId);
            timeoutId = null;
        }
    };
    const cancel = () => {
        cancelTimer();
        pendingThis = undefined;
        pendingArgs = null;
    };
    const flush = () => {
        invoke();
    };
    const debounced = function (...args) {
        if (signal?.aborted) {
            return;
        }
        pendingThis = this;
        pendingArgs = args;
        const isFirstCall = timeoutId == null;
        schedule();
        if (leading && isFirstCall) {
            invoke();
        }
    };
    debounced.schedule = schedule;
    debounced.cancel = cancel;
    debounced.flush = flush;
    signal?.addEventListener('abort', cancel, { once: true });
    return debounced;
}

exports.debounce = debounce;


/***/ },

/***/ 6440
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function identity(x) {
    return x;
}

exports.identity = identity;


/***/ },

/***/ 3844
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const cloneDeepWith = __webpack_require__(3964);

function cloneDeep(obj) {
    return cloneDeepWith.cloneDeepWithImpl(obj, undefined, obj, new Map(), undefined);
}

exports.cloneDeep = cloneDeep;


/***/ },

/***/ 3964
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const getSymbols = __webpack_require__(6012);
const getTag = __webpack_require__(2049);
const tags = __webpack_require__(9184);
const isPrimitive = __webpack_require__(2520);
const isTypedArray = __webpack_require__(3908);

function cloneDeepWith(obj, cloneValue) {
    return cloneDeepWithImpl(obj, undefined, obj, new Map(), cloneValue);
}
function cloneDeepWithImpl(valueToClone, keyToClone, objectToClone, stack = new Map(), cloneValue = undefined) {
    const cloned = cloneValue?.(valueToClone, keyToClone, objectToClone, stack);
    if (cloned !== undefined) {
        return cloned;
    }
    if (isPrimitive.isPrimitive(valueToClone)) {
        return valueToClone;
    }
    if (stack.has(valueToClone)) {
        return stack.get(valueToClone);
    }
    if (Array.isArray(valueToClone)) {
        const result = new Array(valueToClone.length);
        stack.set(valueToClone, result);
        for (let i = 0; i < valueToClone.length; i++) {
            result[i] = cloneDeepWithImpl(valueToClone[i], i, objectToClone, stack, cloneValue);
        }
        if (Object.hasOwn(valueToClone, 'index')) {
            result.index = valueToClone.index;
        }
        if (Object.hasOwn(valueToClone, 'input')) {
            result.input = valueToClone.input;
        }
        return result;
    }
    if (valueToClone instanceof Date) {
        return new Date(valueToClone.getTime());
    }
    if (valueToClone instanceof RegExp) {
        const result = new RegExp(valueToClone.source, valueToClone.flags);
        result.lastIndex = valueToClone.lastIndex;
        return result;
    }
    if (valueToClone instanceof Map) {
        const result = new Map();
        stack.set(valueToClone, result);
        for (const [key, value] of valueToClone) {
            result.set(key, cloneDeepWithImpl(value, key, objectToClone, stack, cloneValue));
        }
        return result;
    }
    if (valueToClone instanceof Set) {
        const result = new Set();
        stack.set(valueToClone, result);
        for (const value of valueToClone) {
            result.add(cloneDeepWithImpl(value, undefined, objectToClone, stack, cloneValue));
        }
        return result;
    }
    if (typeof Buffer !== 'undefined' && Buffer.isBuffer(valueToClone)) {
        return valueToClone.subarray();
    }
    if (isTypedArray.isTypedArray(valueToClone)) {
        const result = new (Object.getPrototypeOf(valueToClone).constructor)(valueToClone.length);
        stack.set(valueToClone, result);
        for (let i = 0; i < valueToClone.length; i++) {
            result[i] = cloneDeepWithImpl(valueToClone[i], i, objectToClone, stack, cloneValue);
        }
        return result;
    }
    if (valueToClone instanceof ArrayBuffer ||
        (typeof SharedArrayBuffer !== 'undefined' && valueToClone instanceof SharedArrayBuffer)) {
        return valueToClone.slice(0);
    }
    if (valueToClone instanceof DataView) {
        const result = new DataView(valueToClone.buffer.slice(0), valueToClone.byteOffset, valueToClone.byteLength);
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (typeof File !== 'undefined' && valueToClone instanceof File) {
        const result = new File([valueToClone], valueToClone.name, {
            type: valueToClone.type,
        });
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (typeof Blob !== 'undefined' && valueToClone instanceof Blob) {
        const result = new Blob([valueToClone], { type: valueToClone.type });
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (valueToClone instanceof Error) {
        const result = new valueToClone.constructor();
        stack.set(valueToClone, result);
        result.message = valueToClone.message;
        result.name = valueToClone.name;
        result.stack = valueToClone.stack;
        result.cause = valueToClone.cause;
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (valueToClone instanceof Boolean) {
        const result = new Boolean(valueToClone.valueOf());
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (valueToClone instanceof Number) {
        const result = new Number(valueToClone.valueOf());
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (valueToClone instanceof String) {
        const result = new String(valueToClone.valueOf());
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    if (typeof valueToClone === 'object' && isCloneableObject(valueToClone)) {
        const result = Object.create(Object.getPrototypeOf(valueToClone));
        stack.set(valueToClone, result);
        copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
        return result;
    }
    return valueToClone;
}
function copyProperties(target, source, objectToClone = target, stack, cloneValue) {
    const keys = [...Object.keys(source), ...getSymbols.getSymbols(source)];
    for (let i = 0; i < keys.length; i++) {
        const key = keys[i];
        const descriptor = Object.getOwnPropertyDescriptor(target, key);
        if (descriptor == null || descriptor.writable) {
            target[key] = cloneDeepWithImpl(source[key], key, objectToClone, stack, cloneValue);
        }
    }
}
function isCloneableObject(object) {
    switch (getTag.getTag(object)) {
        case tags.argumentsTag:
        case tags.arrayTag:
        case tags.arrayBufferTag:
        case tags.dataViewTag:
        case tags.booleanTag:
        case tags.dateTag:
        case tags.float32ArrayTag:
        case tags.float64ArrayTag:
        case tags.int8ArrayTag:
        case tags.int16ArrayTag:
        case tags.int32ArrayTag:
        case tags.mapTag:
        case tags.numberTag:
        case tags.objectTag:
        case tags.regexpTag:
        case tags.setTag:
        case tags.stringTag:
        case tags.symbolTag:
        case tags.uint8ArrayTag:
        case tags.uint8ClampedArrayTag:
        case tags.uint16ArrayTag:
        case tags.uint32ArrayTag: {
            return true;
        }
        default: {
            return false;
        }
    }
}

exports.cloneDeepWith = cloneDeepWith;
exports.cloneDeepWithImpl = cloneDeepWithImpl;
exports.copyProperties = copyProperties;


/***/ },

/***/ 9181
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isLength(value) {
    return Number.isSafeInteger(value) && value >= 0;
}

exports.isLength = isLength;


/***/ },

/***/ 2520
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isPrimitive(value) {
    return value == null || (typeof value !== 'object' && typeof value !== 'function');
}

exports.isPrimitive = isPrimitive;


/***/ },

/***/ 3908
(__unused_webpack_module, exports) {

"use strict";


Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

function isTypedArray(x) {
    return ArrayBuffer.isView(x) && !(x instanceof DataView);
}

exports.isTypedArray = isTypedArray;


/***/ },

/***/ 228
(module) {

"use strict";


var has = Object.prototype.hasOwnProperty
  , prefix = '~';

/**
 * Constructor to create a storage for our `EE` objects.
 * An `Events` instance is a plain object whose properties are event names.
 *
 * @constructor
 * @private
 */
function Events() {}

//
// We try to not inherit from `Object.prototype`. In some engines creating an
// instance in this way is faster than calling `Object.create(null)` directly.
// If `Object.create(null)` is not supported we prefix the event names with a
// character to make sure that the built-in object properties are not
// overridden or used as an attack vector.
//
if (Object.create) {
  Events.prototype = Object.create(null);

  //
  // This hack is needed because the `__proto__` property is still inherited in
  // some old browsers like Android 4, iPhone 5.1, Opera 11 and Safari 5.
  //
  if (!new Events().__proto__) prefix = false;
}

/**
 * Representation of a single event listener.
 *
 * @param {Function} fn The listener function.
 * @param {*} context The context to invoke the listener with.
 * @param {Boolean} [once=false] Specify if the listener is a one-time listener.
 * @constructor
 * @private
 */
function EE(fn, context, once) {
  this.fn = fn;
  this.context = context;
  this.once = once || false;
}

/**
 * Add a listener for a given event.
 *
 * @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
 * @param {(String|Symbol)} event The event name.
 * @param {Function} fn The listener function.
 * @param {*} context The context to invoke the listener with.
 * @param {Boolean} once Specify if the listener is a one-time listener.
 * @returns {EventEmitter}
 * @private
 */
function addListener(emitter, event, fn, context, once) {
  if (typeof fn !== 'function') {
    throw new TypeError('The listener must be a function');
  }

  var listener = new EE(fn, context || emitter, once)
    , evt = prefix ? prefix + event : event;

  if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
  else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
  else emitter._events[evt] = [emitter._events[evt], listener];

  return emitter;
}

/**
 * Clear event by name.
 *
 * @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
 * @param {(String|Symbol)} evt The Event name.
 * @private
 */
function clearEvent(emitter, evt) {
  if (--emitter._eventsCount === 0) emitter._events = new Events();
  else delete emitter._events[evt];
}

/**
 * Minimal `EventEmitter` interface that is molded against the Node.js
 * `EventEmitter` interface.
 *
 * @constructor
 * @public
 */
function EventEmitter() {
  this._events = new Events();
  this._eventsCount = 0;
}

/**
 * Return an array listing the events for which the emitter has registered
 * listeners.
 *
 * @returns {Array}
 * @public
 */
EventEmitter.prototype.eventNames = function eventNames() {
  var names = []
    , events
    , name;

  if (this._eventsCount === 0) return names;

  for (name in (events = this._events)) {
    if (has.call(events, name)) names.push(prefix ? name.slice(1) : name);
  }

  if (Object.getOwnPropertySymbols) {
    return names.concat(Object.getOwnPropertySymbols(events));
  }

  return names;
};

/**
 * Return the listeners registered for a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @returns {Array} The registered listeners.
 * @public
 */
EventEmitter.prototype.listeners = function listeners(event) {
  var evt = prefix ? prefix + event : event
    , handlers = this._events[evt];

  if (!handlers) return [];
  if (handlers.fn) return [handlers.fn];

  for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) {
    ee[i] = handlers[i].fn;
  }

  return ee;
};

/**
 * Return the number of listeners listening to a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @returns {Number} The number of listeners.
 * @public
 */
EventEmitter.prototype.listenerCount = function listenerCount(event) {
  var evt = prefix ? prefix + event : event
    , listeners = this._events[evt];

  if (!listeners) return 0;
  if (listeners.fn) return 1;
  return listeners.length;
};

/**
 * Calls each of the listeners registered for a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @returns {Boolean} `true` if the event had listeners, else `false`.
 * @public
 */
EventEmitter.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
  var evt = prefix ? prefix + event : event;

  if (!this._events[evt]) return false;

  var listeners = this._events[evt]
    , len = arguments.length
    , args
    , i;

  if (listeners.fn) {
    if (listeners.once) this.removeListener(event, listeners.fn, undefined, true);

    switch (len) {
      case 1: return listeners.fn.call(listeners.context), true;
      case 2: return listeners.fn.call(listeners.context, a1), true;
      case 3: return listeners.fn.call(listeners.context, a1, a2), true;
      case 4: return listeners.fn.call(listeners.context, a1, a2, a3), true;
      case 5: return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
      case 6: return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
    }

    for (i = 1, args = new Array(len -1); i < len; i++) {
      args[i - 1] = arguments[i];
    }

    listeners.fn.apply(listeners.context, args);
  } else {
    var length = listeners.length
      , j;

    for (i = 0; i < length; i++) {
      if (listeners[i].once) this.removeListener(event, listeners[i].fn, undefined, true);

      switch (len) {
        case 1: listeners[i].fn.call(listeners[i].context); break;
        case 2: listeners[i].fn.call(listeners[i].context, a1); break;
        case 3: listeners[i].fn.call(listeners[i].context, a1, a2); break;
        case 4: listeners[i].fn.call(listeners[i].context, a1, a2, a3); break;
        default:
          if (!args) for (j = 1, args = new Array(len -1); j < len; j++) {
            args[j - 1] = arguments[j];
          }

          listeners[i].fn.apply(listeners[i].context, args);
      }
    }
  }

  return true;
};

/**
 * Add a listener for a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @param {Function} fn The listener function.
 * @param {*} [context=this] The context to invoke the listener with.
 * @returns {EventEmitter} `this`.
 * @public
 */
EventEmitter.prototype.on = function on(event, fn, context) {
  return addListener(this, event, fn, context, false);
};

/**
 * Add a one-time listener for a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @param {Function} fn The listener function.
 * @param {*} [context=this] The context to invoke the listener with.
 * @returns {EventEmitter} `this`.
 * @public
 */
EventEmitter.prototype.once = function once(event, fn, context) {
  return addListener(this, event, fn, context, true);
};

/**
 * Remove the listeners of a given event.
 *
 * @param {(String|Symbol)} event The event name.
 * @param {Function} fn Only remove the listeners that match this function.
 * @param {*} context Only remove the listeners that have this context.
 * @param {Boolean} once Only remove one-time listeners.
 * @returns {EventEmitter} `this`.
 * @public
 */
EventEmitter.prototype.removeListener = function removeListener(event, fn, context, once) {
  var evt = prefix ? prefix + event : event;

  if (!this._events[evt]) return this;
  if (!fn) {
    clearEvent(this, evt);
    return this;
  }

  var listeners = this._events[evt];

  if (listeners.fn) {
    if (
      listeners.fn === fn &&
      (!once || listeners.once) &&
      (!context || listeners.context === context)
    ) {
      clearEvent(this, evt);
    }
  } else {
    for (var i = 0, events = [], length = listeners.length; i < length; i++) {
      if (
        listeners[i].fn !== fn ||
        (once && !listeners[i].once) ||
        (context && listeners[i].context !== context)
      ) {
        events.push(listeners[i]);
      }
    }

    //
    // Reset the array, or remove it completely if we have no more listeners.
    //
    if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
    else clearEvent(this, evt);
  }

  return this;
};

/**
 * Remove all listeners, or those of the specified event.
 *
 * @param {(String|Symbol)} [event] The event name.
 * @returns {EventEmitter} `this`.
 * @public
 */
EventEmitter.prototype.removeAllListeners = function removeAllListeners(event) {
  var evt;

  if (event) {
    evt = prefix ? prefix + event : event;
    if (this._events[evt]) clearEvent(this, evt);
  } else {
    this._events = new Events();
    this._eventsCount = 0;
  }

  return this;
};

//
// Alias methods names because people roll like that.
//
EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
EventEmitter.prototype.addListener = EventEmitter.prototype.on;

//
// Expose the prefix.
//
EventEmitter.prefixed = prefix;

//
// Allow `EventEmitter` to be imported as module namespace.
//
EventEmitter.EventEmitter = EventEmitter;

//
// Expose the module.
//
if (true) {
  module.exports = EventEmitter;
}


/***/ },

/***/ 4405
(__unused_webpack_module, exports) {

"use strict";
/**
 * @license React
 * react-is.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"),
  REACT_PORTAL_TYPE = Symbol.for("react.portal"),
  REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"),
  REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"),
  REACT_PROFILER_TYPE = Symbol.for("react.profiler"),
  REACT_CONSUMER_TYPE = Symbol.for("react.consumer"),
  REACT_CONTEXT_TYPE = Symbol.for("react.context"),
  REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"),
  REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"),
  REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"),
  REACT_MEMO_TYPE = Symbol.for("react.memo"),
  REACT_LAZY_TYPE = Symbol.for("react.lazy"),
  REACT_VIEW_TRANSITION_TYPE = Symbol.for("react.view_transition"),
  REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
function typeOf(object) {
  if ("object" === typeof object && null !== object) {
    var $$typeof = object.$$typeof;
    switch ($$typeof) {
      case REACT_ELEMENT_TYPE:
        switch (((object = object.type), object)) {
          case REACT_FRAGMENT_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_SUSPENSE_TYPE:
          case REACT_SUSPENSE_LIST_TYPE:
          case REACT_VIEW_TRANSITION_TYPE:
            return object;
          default:
            switch (((object = object && object.$$typeof), object)) {
              case REACT_CONTEXT_TYPE:
              case REACT_FORWARD_REF_TYPE:
              case REACT_LAZY_TYPE:
              case REACT_MEMO_TYPE:
                return object;
              case REACT_CONSUMER_TYPE:
                return object;
              default:
                return $$typeof;
            }
        }
      case REACT_PORTAL_TYPE:
        return $$typeof;
    }
  }
}
exports.ContextConsumer = REACT_CONSUMER_TYPE;
exports.ContextProvider = REACT_CONTEXT_TYPE;
exports.Element = REACT_ELEMENT_TYPE;
exports.ForwardRef = REACT_FORWARD_REF_TYPE;
exports.Fragment = REACT_FRAGMENT_TYPE;
exports.Lazy = REACT_LAZY_TYPE;
exports.Memo = REACT_MEMO_TYPE;
exports.Portal = REACT_PORTAL_TYPE;
exports.Profiler = REACT_PROFILER_TYPE;
exports.StrictMode = REACT_STRICT_MODE_TYPE;
exports.Suspense = REACT_SUSPENSE_TYPE;
exports.SuspenseList = REACT_SUSPENSE_LIST_TYPE;
exports.isContextConsumer = function (object) {
  return typeOf(object) === REACT_CONSUMER_TYPE;
};
exports.isContextProvider = function (object) {
  return typeOf(object) === REACT_CONTEXT_TYPE;
};
exports.isElement = function (object) {
  return (
    "object" === typeof object &&
    null !== object &&
    object.$$typeof === REACT_ELEMENT_TYPE
  );
};
exports.isForwardRef = function (object) {
  return typeOf(object) === REACT_FORWARD_REF_TYPE;
};
exports.isFragment = function (object) {
  return typeOf(object) === REACT_FRAGMENT_TYPE;
};
exports.isLazy = function (object) {
  return typeOf(object) === REACT_LAZY_TYPE;
};
exports.isMemo = function (object) {
  return typeOf(object) === REACT_MEMO_TYPE;
};
exports.isPortal = function (object) {
  return typeOf(object) === REACT_PORTAL_TYPE;
};
exports.isProfiler = function (object) {
  return typeOf(object) === REACT_PROFILER_TYPE;
};
exports.isStrictMode = function (object) {
  return typeOf(object) === REACT_STRICT_MODE_TYPE;
};
exports.isSuspense = function (object) {
  return typeOf(object) === REACT_SUSPENSE_TYPE;
};
exports.isSuspenseList = function (object) {
  return typeOf(object) === REACT_SUSPENSE_LIST_TYPE;
};
exports.isValidElementType = function (type) {
  return "string" === typeof type ||
    "function" === typeof type ||
    type === REACT_FRAGMENT_TYPE ||
    type === REACT_PROFILER_TYPE ||
    type === REACT_STRICT_MODE_TYPE ||
    type === REACT_SUSPENSE_TYPE ||
    type === REACT_SUSPENSE_LIST_TYPE ||
    ("object" === typeof type &&
      null !== type &&
      (type.$$typeof === REACT_LAZY_TYPE ||
        type.$$typeof === REACT_MEMO_TYPE ||
        type.$$typeof === REACT_CONTEXT_TYPE ||
        type.$$typeof === REACT_CONSUMER_TYPE ||
        type.$$typeof === REACT_FORWARD_REF_TYPE ||
        type.$$typeof === REACT_CLIENT_REFERENCE ||
        void 0 !== type.getModuleId))
    ? !0
    : !1;
};
exports.typeOf = typeOf;


/***/ },

/***/ 8493
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
/**
 * @license React
 * use-sync-external-store-shim.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


var React = __webpack_require__(1594);
function is(x, y) {
  return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
}
var objectIs = "function" === typeof Object.is ? Object.is : is,
  useState = React.useState,
  useEffect = React.useEffect,
  useLayoutEffect = React.useLayoutEffect,
  useDebugValue = React.useDebugValue;
function useSyncExternalStore$2(subscribe, getSnapshot) {
  var value = getSnapshot(),
    _useState = useState({ inst: { value: value, getSnapshot: getSnapshot } }),
    inst = _useState[0].inst,
    forceUpdate = _useState[1];
  useLayoutEffect(
    function () {
      inst.value = value;
      inst.getSnapshot = getSnapshot;
      checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
    },
    [subscribe, value, getSnapshot]
  );
  useEffect(
    function () {
      checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
      return subscribe(function () {
        checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
      });
    },
    [subscribe]
  );
  useDebugValue(value);
  return value;
}
function checkIfSnapshotChanged(inst) {
  var latestGetSnapshot = inst.getSnapshot;
  inst = inst.value;
  try {
    var nextValue = latestGetSnapshot();
    return !objectIs(inst, nextValue);
  } catch (error) {
    return !0;
  }
}
function useSyncExternalStore$1(subscribe, getSnapshot) {
  return getSnapshot();
}
var shim =
  "undefined" === typeof window ||
  "undefined" === typeof window.document ||
  "undefined" === typeof window.document.createElement
    ? useSyncExternalStore$1
    : useSyncExternalStore$2;
exports.useSyncExternalStore =
  void 0 !== React.useSyncExternalStore ? React.useSyncExternalStore : shim;


/***/ },

/***/ 2162
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
/**
 * @license React
 * use-sync-external-store-shim/with-selector.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


var React = __webpack_require__(1594),
  shim = __webpack_require__(9888);
function is(x, y) {
  return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
}
var objectIs = "function" === typeof Object.is ? Object.is : is,
  useSyncExternalStore = shim.useSyncExternalStore,
  useRef = React.useRef,
  useEffect = React.useEffect,
  useMemo = React.useMemo,
  useDebugValue = React.useDebugValue;
exports.useSyncExternalStoreWithSelector = function (
  subscribe,
  getSnapshot,
  getServerSnapshot,
  selector,
  isEqual
) {
  var instRef = useRef(null);
  if (null === instRef.current) {
    var inst = { hasValue: !1, value: null };
    instRef.current = inst;
  } else inst = instRef.current;
  instRef = useMemo(
    function () {
      function memoizedSelector(nextSnapshot) {
        if (!hasMemo) {
          hasMemo = !0;
          memoizedSnapshot = nextSnapshot;
          nextSnapshot = selector(nextSnapshot);
          if (void 0 !== isEqual && inst.hasValue) {
            var currentSelection = inst.value;
            if (isEqual(currentSelection, nextSnapshot))
              return (memoizedSelection = currentSelection);
          }
          return (memoizedSelection = nextSnapshot);
        }
        currentSelection = memoizedSelection;
        if (objectIs(memoizedSnapshot, nextSnapshot)) return currentSelection;
        var nextSelection = selector(nextSnapshot);
        if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
          return (memoizedSnapshot = nextSnapshot), currentSelection;
        memoizedSnapshot = nextSnapshot;
        return (memoizedSelection = nextSelection);
      }
      var hasMemo = !1,
        memoizedSnapshot,
        memoizedSelection,
        maybeGetServerSnapshot =
          void 0 === getServerSnapshot ? null : getServerSnapshot;
      return [
        function () {
          return memoizedSelector(getSnapshot());
        },
        null === maybeGetServerSnapshot
          ? void 0
          : function () {
              return memoizedSelector(maybeGetServerSnapshot());
            }
      ];
    },
    [getSnapshot, getServerSnapshot, selector, isEqual]
  );
  var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
  useEffect(
    function () {
      inst.hasValue = !0;
      inst.value = value;
    },
    [value]
  );
  useDebugValue(value);
  return value;
};


/***/ },

/***/ 5160
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
/**
 * @license React
 * use-sync-external-store-with-selector.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


var React = __webpack_require__(1594);
function is(x, y) {
  return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
}
var objectIs = "function" === typeof Object.is ? Object.is : is,
  useSyncExternalStore = React.useSyncExternalStore,
  useRef = React.useRef,
  useEffect = React.useEffect,
  useMemo = React.useMemo,
  useDebugValue = React.useDebugValue;
exports.useSyncExternalStoreWithSelector = function (
  subscribe,
  getSnapshot,
  getServerSnapshot,
  selector,
  isEqual
) {
  var instRef = useRef(null);
  if (null === instRef.current) {
    var inst = { hasValue: !1, value: null };
    instRef.current = inst;
  } else inst = instRef.current;
  instRef = useMemo(
    function () {
      function memoizedSelector(nextSnapshot) {
        if (!hasMemo) {
          hasMemo = !0;
          memoizedSnapshot = nextSnapshot;
          nextSnapshot = selector(nextSnapshot);
          if (void 0 !== isEqual && inst.hasValue) {
            var currentSelection = inst.value;
            if (isEqual(currentSelection, nextSnapshot))
              return (memoizedSelection = currentSelection);
          }
          return (memoizedSelection = nextSnapshot);
        }
        currentSelection = memoizedSelection;
        if (objectIs(memoizedSnapshot, nextSnapshot)) return currentSelection;
        var nextSelection = selector(nextSnapshot);
        if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
          return (memoizedSnapshot = nextSnapshot), currentSelection;
        memoizedSnapshot = nextSnapshot;
        return (memoizedSelection = nextSelection);
      }
      var hasMemo = !1,
        memoizedSnapshot,
        memoizedSelection,
        maybeGetServerSnapshot =
          void 0 === getServerSnapshot ? null : getServerSnapshot;
      return [
        function () {
          return memoizedSelector(getSnapshot());
        },
        null === maybeGetServerSnapshot
          ? void 0
          : function () {
              return memoizedSelector(maybeGetServerSnapshot());
            }
      ];
    },
    [getSnapshot, getServerSnapshot, selector, isEqual]
  );
  var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
  useEffect(
    function () {
      inst.hasValue = !0;
      inst.value = value;
    },
    [value]
  );
  useDebugValue(value);
  return value;
};


/***/ },

/***/ 9888
(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


if (true) {
  module.exports = __webpack_require__(8493);
} else // removed by dead control flow
{}


/***/ },

/***/ 9242
(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


if (true) {
  module.exports = __webpack_require__(2162);
} else // removed by dead control flow
{}


/***/ },

/***/ 8418
(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


if (true) {
  module.exports = __webpack_require__(5160);
} else // removed by dead control flow
{}


/***/ },

/***/ 1594
(module) {

"use strict";
module.exports = React;

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be in strict mode.
(() => {
"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": () => (/* binding */ PadelAnalyzer)
});

// NAMESPACE OBJECT: ./node_modules/victory-vendor/es/d3-scale.js
var d3_scale_namespaceObject = {};
__webpack_require__.r(d3_scale_namespaceObject);
__webpack_require__.d(d3_scale_namespaceObject, {
  scaleBand: () => (band),
  scaleDiverging: () => (diverging),
  scaleDivergingLog: () => (divergingLog),
  scaleDivergingPow: () => (divergingPow),
  scaleDivergingSqrt: () => (divergingSqrt),
  scaleDivergingSymlog: () => (divergingSymlog),
  scaleIdentity: () => (identity_identity),
  scaleImplicit: () => (implicit),
  scaleLinear: () => (linear_linear),
  scaleLog: () => (log),
  scaleOrdinal: () => (ordinal),
  scalePoint: () => (point),
  scalePow: () => (pow),
  scaleQuantile: () => (quantile_quantile),
  scaleQuantize: () => (quantize),
  scaleRadial: () => (radial),
  scaleSequential: () => (sequential),
  scaleSequentialLog: () => (sequentialLog),
  scaleSequentialPow: () => (sequentialPow),
  scaleSequentialQuantile: () => (sequentialQuantile),
  scaleSequentialSqrt: () => (sequentialSqrt),
  scaleSequentialSymlog: () => (sequentialSymlog),
  scaleSqrt: () => (sqrt),
  scaleSymlog: () => (symlog),
  scaleThreshold: () => (threshold),
  scaleTime: () => (time),
  scaleUtc: () => (utcTime),
  tickFormat: () => (tickFormat)
});

// EXTERNAL MODULE: external "React"
var external_React_ = __webpack_require__(1594);
var external_React_default = /*#__PURE__*/__webpack_require__.n(external_React_);
;// ./node_modules/clsx/dist/clsx.mjs
function r(e){var t,f,n="";if("string"==typeof e||"number"==typeof e)n+=e;else if("object"==typeof e)if(Array.isArray(e)){var o=e.length;for(t=0;t<o;t++)e[t]&&(f=r(e[t]))&&(n&&(n+=" "),n+=f)}else for(f in e)e[f]&&(n&&(n+=" "),n+=f);return n}function clsx(){for(var e,t,f=0,n="",o=arguments.length;f<o;f++)(e=arguments[f])&&(t=r(e))&&(n&&(n+=" "),n+=t);return n}/* harmony default export */ const dist_clsx = (clsx);
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/throttle.js
var throttle = __webpack_require__(4297);
var throttle_default = /*#__PURE__*/__webpack_require__.n(throttle);
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/get.js
var get = __webpack_require__(305);
var get_default = /*#__PURE__*/__webpack_require__.n(get);
;// ./node_modules/recharts/es6/util/round.js
// if you go lower than 3, wild wild things happen during rendering
var defaultRoundPrecision = 4;
function round(num) {
  var roundPrecision = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultRoundPrecision;
  var factor = 10 ** roundPrecision;
  var rounded = Math.round(num * factor) / factor;
  if (Object.is(rounded, -0)) {
    return 0;
  }
  return rounded;
}

/**
 * This function will accept a string template literal and for each
 * variable placeholder, it will round the value to avoid long float numbers in
 * the SVG path which might cause rendering issues in some browsers.
 */
function roundTemplateLiteral(strings) {
  for (var _len = arguments.length, values = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    values[_key - 1] = arguments[_key];
  }
  return strings.reduce((result, string, i) => {
    var value = values[i - 1];
    if (typeof value === 'string') {
      return result + value + string;
    }
    if (value !== undefined) {
      return result + round(value) + string;
    }
    return result + string;
  }, '');
}
;// ./node_modules/recharts/es6/util/DataUtils.js


var mathSign = value => {
  if (value === 0) {
    return 0;
  }
  if (value > 0) {
    return 1;
  }
  return -1;
};
var isNan = value => {
  // eslint-disable-next-line eqeqeq
  return typeof value == 'number' && value != +value;
};
var isPercent = value => typeof value === 'string' && value.indexOf('%') === value.length - 1;
var isNumber = value => (typeof value === 'number' || value instanceof Number) && !isNan(value);
var isNumOrStr = value => isNumber(value) || typeof value === 'string';
var idCounter = 0;
var uniqueId = prefix => {
  var id = ++idCounter;
  return "".concat(prefix || '').concat(id);
};

/**
 * Calculates the numeric value represented by a percent string or number, based on a total value.
 *
 * - If `percent` is not a number or string, returns `defaultValue`.
 * - If `percent` is a percent string but `totalValue` is null/undefined, returns `defaultValue`.
 * - If the result is NaN, returns `defaultValue`.
 * - If `validate` is true and the result exceeds `totalValue`, returns `totalValue`.
 *
 * @param percent - The percent value to convert. Can be a number (e.g. 25) or a string ending with '%' (e.g. '25%').
 *                  If a string, it must end with '%' to be treated as a percent; otherwise, it is parsed as a number.
 * @param totalValue - The total value to calculate the percent of. Required if `percent` is a percent string.
 * @param defaultValue - The value returned if `percent` is undefined, invalid, or cannot be converted to a number.
 * @param validate - If true, ensures the result does not exceed `totalValue` (when provided).
 * @returns The calculated value, or `defaultValue` for invalid input.
 */
var getPercentValue = function getPercentValue(percent, totalValue) {
  var defaultValue = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 0;
  var validate = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  if (!isNumber(percent) && typeof percent !== 'string') {
    return defaultValue;
  }
  var value;
  if (isPercent(percent)) {
    if (totalValue == null) {
      return defaultValue;
    }
    var index = percent.indexOf('%');
    value = totalValue * parseFloat(percent.slice(0, index)) / 100;
  } else {
    value = +percent;
  }
  if (isNan(value)) {
    value = defaultValue;
  }
  if (validate && totalValue != null && value > totalValue) {
    value = totalValue;
  }
  return value;
};
var hasDuplicate = ary => {
  if (!Array.isArray(ary)) {
    return false;
  }
  var len = ary.length;
  var cache = {};
  for (var i = 0; i < len; i++) {
    if (!cache[String(ary[i])]) {
      cache[String(ary[i])] = true;
    } else {
      return true;
    }
  }
  return false;
};
function interpolate(start, end, t) {
  if (isNumber(start) && isNumber(end)) {
    return round(start + t * (end - start));
  }
  return end;
}
function findEntryInArray(ary, specifiedKey, specifiedValue) {
  if (!ary || !ary.length) {
    return undefined;
  }
  return ary.find(entry => entry && (typeof specifiedKey === 'function' ? specifiedKey(entry) : get_default()(entry, specifiedKey)) === specifiedValue);
}
/**
 * The least square linear regression
 * @param {Array} data The array of points
 * @returns {Object} The domain of x, and the parameter of linear function
 */
var getLinearRegression = data => {
  var len = data.length;
  var xsum = 0;
  var ysum = 0;
  var xysum = 0;
  var xxsum = 0;
  var xmin = Infinity;
  var xmax = -Infinity;
  var xcurrent = 0;
  var ycurrent = 0;
  for (var i = 0; i < len; i++) {
    var _data$i, _data$i2;
    xcurrent = ((_data$i = data[i]) === null || _data$i === void 0 ? void 0 : _data$i.cx) || 0;
    ycurrent = ((_data$i2 = data[i]) === null || _data$i2 === void 0 ? void 0 : _data$i2.cy) || 0;
    xsum += xcurrent;
    ysum += ycurrent;
    xysum += xcurrent * ycurrent;
    xxsum += xcurrent * xcurrent;
    xmin = Math.min(xmin, xcurrent);
    xmax = Math.max(xmax, xcurrent);
  }
  var a = len * xxsum !== xsum * xsum ? (len * xysum - xsum * ysum) / (len * xxsum - xsum * xsum) : 0;
  return {
    xmin,
    xmax,
    a,
    b: (ysum - a * xsum) / len
  };
};
/**
 * Checks if the value is null or undefined
 * @param value The value to check
 * @returns true if the value is null or undefined
 */
var isNullish = value => {
  return value === null || typeof value === 'undefined';
};

/**
 * Uppercase the first letter of a string
 * @param {string} value The string to uppercase
 * @returns {string} The uppercased string
 */
var upperFirst = value => {
  if (isNullish(value)) {
    return value;
  }
  return "".concat(value.charAt(0).toUpperCase()).concat(value.slice(1));
};

/**
 * Checks if the value is not null nor undefined.
 * @param value The value to check
 * @returns true if the value is not null nor undefined
 */
function isNotNil(value) {
  return value != null;
}

/**
 * No-operation function that does nothing.
 * Useful as a placeholder or default callback function.
 */
function noop() {}
;// ./node_modules/recharts/es6/util/LogUtils.js
/* eslint no-console: 0 */
var isDev = true;
var warn = function warn(condition, format) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }
  if (isDev && typeof console !== 'undefined' && console.warn) {
    if (format === undefined) {
      console.warn('LogUtils requires an error message argument');
    }
    if (!condition) {
      if (format === undefined) {
        console.warn('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
      } else {
        var argIndex = 0;
        console.warn(format.replace(/%s/g, () => args[argIndex++]));
      }
    }
  }
};
;// ./node_modules/recharts/es6/component/responsiveContainerUtils.js

var defaultResponsiveContainerProps = {
  width: '100%',
  height: '100%',
  debounce: 0,
  minWidth: 0,
  initialDimension: {
    width: -1,
    height: -1
  }
};
var calculateChartDimensions = (containerWidth, containerHeight, props) => {
  var {
    width = defaultResponsiveContainerProps.width,
    height = defaultResponsiveContainerProps.height,
    aspect,
    maxHeight
  } = props;

  /*
   * The containerWidth and containerHeight are already percentage based because it's set as that percentage in CSS.
   * Means we don't have to calculate percentages here.
   */
  var calculatedWidth = isPercent(width) ? containerWidth : Number(width);
  var calculatedHeight = isPercent(height) ? containerHeight : Number(height);
  if (aspect && aspect > 0) {
    // Preserve the desired aspect ratio
    if (calculatedWidth) {
      // Will default to using width for aspect ratio
      calculatedHeight = calculatedWidth / aspect;
    } else if (calculatedHeight) {
      // But we should also take height into consideration
      calculatedWidth = calculatedHeight * aspect;
    }

    // if maxHeight is set, overwrite if calculatedHeight is greater than maxHeight
    if (maxHeight && calculatedHeight != null && calculatedHeight > maxHeight) {
      calculatedHeight = maxHeight;
    }
  }
  return {
    calculatedWidth,
    calculatedHeight
  };
};
var bothOverflow = {
  width: 0,
  height: 0,
  overflow: 'visible'
};
var overflowX = {
  width: 0,
  overflowX: 'visible'
};
var overflowY = {
  height: 0,
  overflowY: 'visible'
};
var noStyle = {};

/**
 * This zero-size, overflow-visible is required to allow the chart to shrink.
 * Without it, the chart itself will fill the ResponsiveContainer, and while it allows the chart to grow,
 * it would always keep the container at the size of the chart,
 * and ResizeObserver would never fire.
 * With this zero-size element, the chart itself never actually fills the container,
 * it just so happens that it is visible because it overflows.
 * I learned this trick from the `react-virtualized` library: https://github.com/bvaughn/react-virtualized-auto-sizer/blob/master/src/AutoSizer.ts
 * See https://github.com/recharts/recharts/issues/172 and also https://github.com/bvaughn/react-virtualized/issues/68
 *
 * Also, we don't need to apply the zero-size style if the dimension is a fixed number (or undefined),
 * because in that case the chart can't shrink in that dimension anyway.
 * This fixes defining the dimensions using aspect ratio: https://github.com/recharts/recharts/issues/6245
 */
var getInnerDivStyle = props => {
  var {
    width,
    height
  } = props;
  var isWidthPercent = isPercent(width);
  var isHeightPercent = isPercent(height);
  if (isWidthPercent && isHeightPercent) {
    return bothOverflow;
  }
  if (isWidthPercent) {
    return overflowX;
  }
  if (isHeightPercent) {
    return overflowY;
  }
  return noStyle;
};
function getDefaultWidthAndHeight(_ref) {
  var {
    width,
    height,
    aspect
  } = _ref;
  var calculatedWidth = width;
  var calculatedHeight = height;
  if (calculatedWidth === undefined && calculatedHeight === undefined) {
    calculatedWidth = defaultResponsiveContainerProps.width;
    calculatedHeight = defaultResponsiveContainerProps.height;
  } else if (calculatedWidth === undefined) {
    calculatedWidth = aspect && aspect > 0 ? undefined : defaultResponsiveContainerProps.width;
  } else if (calculatedHeight === undefined) {
    calculatedHeight = aspect && aspect > 0 ? undefined : defaultResponsiveContainerProps.height;
  }
  return {
    width: calculatedWidth,
    height: calculatedHeight
  };
}
;// ./node_modules/recharts/es6/util/isWellBehavedNumber.js
function isWellBehavedNumber(n) {
  return Number.isFinite(n);
}
function isPositiveNumber(n) {
  return typeof n === 'number' && n > 0 && Number.isFinite(n);
}
;// ./node_modules/recharts/es6/component/ResponsiveContainer.js
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }








var ResponsiveContainerContext = /*#__PURE__*/(0,external_React_.createContext)(defaultResponsiveContainerProps.initialDimension);
function isAcceptableSize(size) {
  return isPositiveNumber(size.width) && isPositiveNumber(size.height);
}
function ResponsiveContainerContextProvider(_ref) {
  var {
    children,
    width,
    height
  } = _ref;
  var size = (0,external_React_.useMemo)(() => ({
    width,
    height
  }), [width, height]);
  if (!isAcceptableSize(size)) {
    /*
     * Don't render the container if width or height is non-positive because
     * in that case the chart will not be rendered properly anyway.
     * We will instead wait for the next resize event to provide the correct dimensions.
     */
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(ResponsiveContainerContext.Provider, {
    value: size
  }, children);
}
var useResponsiveContainerContext = () => (0,external_React_.useContext)(ResponsiveContainerContext);
var SizeDetectorContainer = /*#__PURE__*/(0,external_React_.forwardRef)((_ref2, ref) => {
  var {
    aspect,
    initialDimension = defaultResponsiveContainerProps.initialDimension,
    width,
    height,
    /*
     * default min-width to 0 if not specified - 'auto' causes issues with flexbox
     * https://github.com/recharts/recharts/issues/172
     */
    minWidth = defaultResponsiveContainerProps.minWidth,
    minHeight,
    maxHeight,
    children,
    debounce = defaultResponsiveContainerProps.debounce,
    id,
    className,
    onResize,
    style = {}
  } = _ref2;
  var containerRef = (0,external_React_.useRef)(null);
  /*
   * We are using a ref to avoid re-creating the ResizeObserver when the onResize function changes.
   * The ref is updated on every render, so the latest onResize function is always available in the effect.
   */
  var onResizeRef = (0,external_React_.useRef)();
  onResizeRef.current = onResize;
  (0,external_React_.useImperativeHandle)(ref, () => containerRef.current);
  var [sizes, setSizes] = (0,external_React_.useState)({
    containerWidth: initialDimension.width,
    containerHeight: initialDimension.height
  });
  var setContainerSize = (0,external_React_.useCallback)((newWidth, newHeight) => {
    setSizes(prevState => {
      var roundedWidth = Math.round(newWidth);
      var roundedHeight = Math.round(newHeight);
      if (prevState.containerWidth === roundedWidth && prevState.containerHeight === roundedHeight) {
        return prevState;
      }
      return {
        containerWidth: roundedWidth,
        containerHeight: roundedHeight
      };
    });
  }, []);
  (0,external_React_.useEffect)(() => {
    if (containerRef.current == null || typeof ResizeObserver === 'undefined') {
      return noop;
    }
    var callback = entries => {
      var _onResizeRef$current;
      var entry = entries[0];
      if (entry == null) {
        return;
      }
      var {
        width: containerWidth,
        height: containerHeight
      } = entry.contentRect;
      setContainerSize(containerWidth, containerHeight);
      (_onResizeRef$current = onResizeRef.current) === null || _onResizeRef$current === void 0 || _onResizeRef$current.call(onResizeRef, containerWidth, containerHeight);
    };
    if (debounce > 0) {
      callback = throttle_default()(callback, debounce, {
        trailing: true,
        leading: false
      });
    }
    var observer = new ResizeObserver(callback);
    var {
      width: containerWidth,
      height: containerHeight
    } = containerRef.current.getBoundingClientRect();
    setContainerSize(containerWidth, containerHeight);
    observer.observe(containerRef.current);
    return () => {
      observer.disconnect();
    };
  }, [setContainerSize, debounce]);
  var {
    containerWidth,
    containerHeight
  } = sizes;
  warn(!aspect || aspect > 0, 'The aspect(%s) must be greater than zero.', aspect);
  var {
    calculatedWidth,
    calculatedHeight
  } = calculateChartDimensions(containerWidth, containerHeight, {
    width,
    height,
    aspect,
    maxHeight
  });
  warn(calculatedWidth != null && calculatedWidth > 0 || calculatedHeight != null && calculatedHeight > 0, "The width(%s) and height(%s) of chart should be greater than 0,\n       please check the style of container, or the props width(%s) and height(%s),\n       or add a minWidth(%s) or minHeight(%s) or use aspect(%s) to control the\n       height and width.", calculatedWidth, calculatedHeight, width, height, minWidth, minHeight, aspect);
  return /*#__PURE__*/external_React_.createElement("div", {
    id: id ? "".concat(id) : undefined,
    className: clsx('recharts-responsive-container', className),
    style: _objectSpread(_objectSpread({}, style), {}, {
      width,
      height,
      minWidth,
      minHeight,
      maxHeight
    }),
    ref: containerRef
  }, /*#__PURE__*/external_React_.createElement("div", {
    style: getInnerDivStyle({
      width,
      height
    })
  }, /*#__PURE__*/external_React_.createElement(ResponsiveContainerContextProvider, {
    width: calculatedWidth,
    height: calculatedHeight
  }, children)));
});

/**
 * The `ResponsiveContainer` component is a container that adjusts its width and height based on the size of its parent element.
 * It is used to create responsive charts that adapt to different screen sizes.
 *
 * This component uses the {@link https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver ResizeObserver} API to monitor changes to the size of its parent element.
 * If you need to support older browsers that do not support this API, you may need to include a polyfill.
 *
 * @see {@link https://recharts.github.io/en-US/guide/sizes/ Chart size guide}
 *
 * @provides ResponsiveContainerContext
 */
var ResponsiveContainer = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var responsiveContainerContext = useResponsiveContainerContext();
  if (isPositiveNumber(responsiveContainerContext.width) && isPositiveNumber(responsiveContainerContext.height)) {
    /*
     * If we detect that we are already inside another ResponsiveContainer,
     * we do not attempt to add another layer of responsiveness.
     */
    return props.children;
  }
  var {
    width,
    height
  } = getDefaultWidthAndHeight({
    width: props.width,
    height: props.height,
    aspect: props.aspect
  });

  /*
   * Let's try to get the calculated dimensions without having the div container set up.
   * Sometimes this does produce fixed, positive dimensions. If so, we can skip rendering the div and monitoring its size.
   */
  var {
    calculatedWidth,
    calculatedHeight
  } = calculateChartDimensions(undefined, undefined, {
    width,
    height,
    aspect: props.aspect,
    maxHeight: props.maxHeight
  });
  if (isNumber(calculatedWidth) && isNumber(calculatedHeight)) {
    /*
     * If it just so happens that the combination of width, height, and aspect ratio
     * results in fixed dimensions, then we don't need to monitor the container's size.
     * We can just provide these fixed dimensions to the context.
     *
     * Note that here we are not checking for positive numbers;
     * if the user provides a zero or negative width/height, we will just pass that along
     * as whatever size we detect won't be helping anyway.
     */
    return /*#__PURE__*/external_React_.createElement(ResponsiveContainerContextProvider, {
      width: calculatedWidth,
      height: calculatedHeight
    }, props.children);
  }
  /*
   * Static analysis did not produce fixed dimensions,
   * so we need to render a special div and monitor its size.
   */
  return /*#__PURE__*/external_React_.createElement(SizeDetectorContainer, _extends({}, props, {
    width: width,
    height: height,
    ref: ref
  }));
});
;// ./node_modules/redux/dist/redux.mjs
// src/utils/formatProdErrorMessage.ts
function formatProdErrorMessage(code) {
  return `Minified Redux error #${code}; visit https://redux.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}

// src/utils/symbol-observable.ts
var $$observable = /* @__PURE__ */ (() => typeof Symbol === "function" && Symbol.observable || "@@observable")();
var symbol_observable_default = $$observable;

// src/utils/actionTypes.ts
var randomString = () => Math.random().toString(36).substring(7).split("").join(".");
var ActionTypes = {
  INIT: `@@redux/INIT${/* @__PURE__ */ randomString()}`,
  REPLACE: `@@redux/REPLACE${/* @__PURE__ */ randomString()}`,
  PROBE_UNKNOWN_ACTION: () => `@@redux/PROBE_UNKNOWN_ACTION${randomString()}`
};
var actionTypes_default = ActionTypes;

// src/utils/isPlainObject.ts
function isPlainObject(obj) {
  if (typeof obj !== "object" || obj === null)
    return false;
  let proto = obj;
  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }
  return Object.getPrototypeOf(obj) === proto || Object.getPrototypeOf(obj) === null;
}

// src/utils/kindOf.ts
function miniKindOf(val) {
  if (val === void 0)
    return "undefined";
  if (val === null)
    return "null";
  const type = typeof val;
  switch (type) {
    case "boolean":
    case "string":
    case "number":
    case "symbol":
    case "function": {
      return type;
    }
  }
  if (Array.isArray(val))
    return "array";
  if (isDate(val))
    return "date";
  if (isError(val))
    return "error";
  const constructorName = ctorName(val);
  switch (constructorName) {
    case "Symbol":
    case "Promise":
    case "WeakMap":
    case "WeakSet":
    case "Map":
    case "Set":
      return constructorName;
  }
  return Object.prototype.toString.call(val).slice(8, -1).toLowerCase().replace(/\s/g, "");
}
function ctorName(val) {
  return typeof val.constructor === "function" ? val.constructor.name : null;
}
function isError(val) {
  return val instanceof Error || typeof val.message === "string" && val.constructor && typeof val.constructor.stackTraceLimit === "number";
}
function isDate(val) {
  if (val instanceof Date)
    return true;
  return typeof val.toDateString === "function" && typeof val.getDate === "function" && typeof val.setDate === "function";
}
function kindOf(val) {
  let typeOfVal = typeof val;
  if (false) // removed by dead control flow
{}
  return typeOfVal;
}

// src/createStore.ts
function createStore(reducer, preloadedState, enhancer) {
  if (typeof reducer !== "function") {
    throw new Error( true ? formatProdErrorMessage(2) : 0);
  }
  if (typeof preloadedState === "function" && typeof enhancer === "function" || typeof enhancer === "function" && typeof arguments[3] === "function") {
    throw new Error( true ? formatProdErrorMessage(0) : 0);
  }
  if (typeof preloadedState === "function" && typeof enhancer === "undefined") {
    enhancer = preloadedState;
    preloadedState = void 0;
  }
  if (typeof enhancer !== "undefined") {
    if (typeof enhancer !== "function") {
      throw new Error( true ? formatProdErrorMessage(1) : 0);
    }
    return enhancer(createStore)(reducer, preloadedState);
  }
  let currentReducer = reducer;
  let currentState = preloadedState;
  let currentListeners = /* @__PURE__ */ new Map();
  let nextListeners = currentListeners;
  let listenerIdCounter = 0;
  let isDispatching = false;
  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = /* @__PURE__ */ new Map();
      currentListeners.forEach((listener, key) => {
        nextListeners.set(key, listener);
      });
    }
  }
  function getState() {
    if (isDispatching) {
      throw new Error( true ? formatProdErrorMessage(3) : 0);
    }
    return currentState;
  }
  function subscribe(listener) {
    if (typeof listener !== "function") {
      throw new Error( true ? formatProdErrorMessage(4) : 0);
    }
    if (isDispatching) {
      throw new Error( true ? formatProdErrorMessage(5) : 0);
    }
    let isSubscribed = true;
    ensureCanMutateNextListeners();
    const listenerId = listenerIdCounter++;
    nextListeners.set(listenerId, listener);
    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }
      if (isDispatching) {
        throw new Error( true ? formatProdErrorMessage(6) : 0);
      }
      isSubscribed = false;
      ensureCanMutateNextListeners();
      nextListeners.delete(listenerId);
      currentListeners = null;
    };
  }
  function dispatch(action) {
    if (!isPlainObject(action)) {
      throw new Error( true ? formatProdErrorMessage(7) : 0);
    }
    if (typeof action.type === "undefined") {
      throw new Error( true ? formatProdErrorMessage(8) : 0);
    }
    if (typeof action.type !== "string") {
      throw new Error( true ? formatProdErrorMessage(17) : 0);
    }
    if (isDispatching) {
      throw new Error( true ? formatProdErrorMessage(9) : 0);
    }
    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }
    const listeners = currentListeners = nextListeners;
    listeners.forEach((listener) => {
      listener();
    });
    return action;
  }
  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== "function") {
      throw new Error( true ? formatProdErrorMessage(10) : 0);
    }
    currentReducer = nextReducer;
    dispatch({
      type: actionTypes_default.REPLACE
    });
  }
  function observable() {
    const outerSubscribe = subscribe;
    return {
      /**
       * The minimal observable subscription method.
       * @param observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe(observer) {
        if (typeof observer !== "object" || observer === null) {
          throw new Error( true ? formatProdErrorMessage(11) : 0);
        }
        function observeState() {
          const observerAsObserver = observer;
          if (observerAsObserver.next) {
            observerAsObserver.next(getState());
          }
        }
        observeState();
        const unsubscribe = outerSubscribe(observeState);
        return {
          unsubscribe
        };
      },
      [symbol_observable_default]() {
        return this;
      }
    };
  }
  dispatch({
    type: actionTypes_default.INIT
  });
  const store = {
    dispatch,
    subscribe,
    getState,
    replaceReducer,
    [symbol_observable_default]: observable
  };
  return store;
}
function legacy_createStore(reducer, preloadedState, enhancer) {
  return createStore(reducer, preloadedState, enhancer);
}

// src/utils/warning.ts
function warning(message) {
  if (typeof console !== "undefined" && typeof console.error === "function") {
    console.error(message);
  }
  try {
    throw new Error(message);
  } catch (e) {
  }
}

// src/combineReducers.ts
function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  const reducerKeys = Object.keys(reducers);
  const argumentName = action && action.type === actionTypes_default.INIT ? "preloadedState argument passed to createStore" : "previous state received by the reducer";
  if (reducerKeys.length === 0) {
    return "Store does not have a valid reducer. Make sure the argument passed to combineReducers is an object whose values are reducers.";
  }
  if (!isPlainObject(inputState)) {
    return `The ${argumentName} has unexpected type of "${kindOf(inputState)}". Expected argument to be an object with the following keys: "${reducerKeys.join('", "')}"`;
  }
  const unexpectedKeys = Object.keys(inputState).filter((key) => !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key]);
  unexpectedKeys.forEach((key) => {
    unexpectedKeyCache[key] = true;
  });
  if (action && action.type === actionTypes_default.REPLACE)
    return;
  if (unexpectedKeys.length > 0) {
    return `Unexpected ${unexpectedKeys.length > 1 ? "keys" : "key"} "${unexpectedKeys.join('", "')}" found in ${argumentName}. Expected to find one of the known reducer keys instead: "${reducerKeys.join('", "')}". Unexpected keys will be ignored.`;
  }
}
function assertReducerShape(reducers) {
  Object.keys(reducers).forEach((key) => {
    const reducer = reducers[key];
    const initialState = reducer(void 0, {
      type: actionTypes_default.INIT
    });
    if (typeof initialState === "undefined") {
      throw new Error( true ? formatProdErrorMessage(12) : 0);
    }
    if (typeof reducer(void 0, {
      type: actionTypes_default.PROBE_UNKNOWN_ACTION()
    }) === "undefined") {
      throw new Error( true ? formatProdErrorMessage(13) : 0);
    }
  });
}
function combineReducers(reducers) {
  const reducerKeys = Object.keys(reducers);
  const finalReducers = {};
  for (let i = 0; i < reducerKeys.length; i++) {
    const key = reducerKeys[i];
    if (false) // removed by dead control flow
{}
    if (typeof reducers[key] === "function") {
      finalReducers[key] = reducers[key];
    }
  }
  const finalReducerKeys = Object.keys(finalReducers);
  let unexpectedKeyCache;
  if (false) // removed by dead control flow
{}
  let shapeAssertionError;
  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }
  return function combination(state = {}, action) {
    if (shapeAssertionError) {
      throw shapeAssertionError;
    }
    if (false) // removed by dead control flow
{}
    let hasChanged = false;
    const nextState = {};
    for (let i = 0; i < finalReducerKeys.length; i++) {
      const key = finalReducerKeys[i];
      const reducer = finalReducers[key];
      const previousStateForKey = state[key];
      const nextStateForKey = reducer(previousStateForKey, action);
      if (typeof nextStateForKey === "undefined") {
        const actionType = action && action.type;
        throw new Error( true ? formatProdErrorMessage(14) : 0);
      }
      nextState[key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }
    hasChanged = hasChanged || finalReducerKeys.length !== Object.keys(state).length;
    return hasChanged ? nextState : state;
  };
}

// src/bindActionCreators.ts
function bindActionCreator(actionCreator, dispatch) {
  return function(...args) {
    return dispatch(actionCreator.apply(this, args));
  };
}
function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === "function") {
    return bindActionCreator(actionCreators, dispatch);
  }
  if (typeof actionCreators !== "object" || actionCreators === null) {
    throw new Error( true ? formatProdErrorMessage(16) : 0);
  }
  const boundActionCreators = {};
  for (const key in actionCreators) {
    const actionCreator = actionCreators[key];
    if (typeof actionCreator === "function") {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }
  return boundActionCreators;
}

// src/compose.ts
function compose(...funcs) {
  if (funcs.length === 0) {
    return (arg) => arg;
  }
  if (funcs.length === 1) {
    return funcs[0];
  }
  return funcs.reduce((a, b) => (...args) => a(b(...args)));
}

// src/applyMiddleware.ts
function applyMiddleware(...middlewares) {
  return (createStore2) => (reducer, preloadedState) => {
    const store = createStore2(reducer, preloadedState);
    let dispatch = () => {
      throw new Error( true ? formatProdErrorMessage(15) : 0);
    };
    const middlewareAPI = {
      getState: store.getState,
      dispatch: (action, ...args) => dispatch(action, ...args)
    };
    const chain = middlewares.map((middleware) => middleware(middlewareAPI));
    dispatch = compose(...chain)(store.dispatch);
    return {
      ...store,
      dispatch
    };
  };
}

// src/utils/isAction.ts
function isAction(action) {
  return isPlainObject(action) && "type" in action && typeof action.type === "string";
}

//# sourceMappingURL=redux.mjs.map
;// ./node_modules/@reduxjs/toolkit/node_modules/immer/dist/immer.mjs
// src/utils/env.ts
var NOTHING = Symbol.for("immer-nothing");
var DRAFTABLE = Symbol.for("immer-draftable");
var DRAFT_STATE = Symbol.for("immer-state");

// src/utils/errors.ts
var errors =  false ? 0 : [];
function die(error, ...args) {
  if (false) // removed by dead control flow
{}
  throw new Error(
    `[Immer] minified error nr: ${error}. Full error at: https://bit.ly/3cXEKWf`
  );
}

// src/utils/common.ts
var O = Object;
var getPrototypeOf = O.getPrototypeOf;
var CONSTRUCTOR = "constructor";
var PROTOTYPE = "prototype";
var CONFIGURABLE = "configurable";
var ENUMERABLE = "enumerable";
var WRITABLE = "writable";
var VALUE = "value";
var isDraft = (value) => !!value && !!value[DRAFT_STATE];
function isDraftable(value) {
  if (!value)
    return false;
  return immer_isPlainObject(value) || isArray(value) || !!value[DRAFTABLE] || !!value[CONSTRUCTOR]?.[DRAFTABLE] || isMap(value) || isSet(value);
}
var objectCtorString = O[PROTOTYPE][CONSTRUCTOR].toString();
var cachedCtorStrings = /* @__PURE__ */ new WeakMap();
function immer_isPlainObject(value) {
  if (!value || !isObjectish(value))
    return false;
  const proto = getPrototypeOf(value);
  if (proto === null || proto === O[PROTOTYPE])
    return true;
  const Ctor = O.hasOwnProperty.call(proto, CONSTRUCTOR) && proto[CONSTRUCTOR];
  if (Ctor === Object)
    return true;
  if (!isFunction(Ctor))
    return false;
  let ctorString = cachedCtorStrings.get(Ctor);
  if (ctorString === void 0) {
    ctorString = Function.toString.call(Ctor);
    cachedCtorStrings.set(Ctor, ctorString);
  }
  return ctorString === objectCtorString;
}
function original(value) {
  if (!isDraft(value))
    die(15, value);
  return value[DRAFT_STATE].base_;
}
function each(obj, iter, strict = true) {
  if (getArchtype(obj) === 0 /* Object */) {
    const keys = strict ? Reflect.ownKeys(obj) : O.keys(obj);
    keys.forEach((key) => {
      iter(key, obj[key], obj);
    });
  } else {
    obj.forEach((entry, index) => iter(index, entry, obj));
  }
}
function getArchtype(thing) {
  const state = thing[DRAFT_STATE];
  return state ? state.type_ : isArray(thing) ? 1 /* Array */ : isMap(thing) ? 2 /* Map */ : isSet(thing) ? 3 /* Set */ : 0 /* Object */;
}
var has = (thing, prop, type = getArchtype(thing)) => type === 2 /* Map */ ? thing.has(prop) : O[PROTOTYPE].hasOwnProperty.call(thing, prop);
var immer_get = (thing, prop, type = getArchtype(thing)) => (
  // @ts-ignore
  type === 2 /* Map */ ? thing.get(prop) : thing[prop]
);
var set = (thing, propOrOldValue, value, type = getArchtype(thing)) => {
  if (type === 2 /* Map */)
    thing.set(propOrOldValue, value);
  else if (type === 3 /* Set */) {
    thing.add(value);
  } else
    thing[propOrOldValue] = value;
};
function is(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
var isArray = Array.isArray;
var isMap = (target) => target instanceof Map;
var isSet = (target) => target instanceof Set;
var isObjectish = (target) => typeof target === "object";
var isFunction = (target) => typeof target === "function";
var isBoolean = (target) => typeof target === "boolean";
function isArrayIndex(value) {
  const n = +value;
  return Number.isInteger(n) && String(n) === value;
}
var getProxyDraft = (value) => {
  if (!isObjectish(value))
    return null;
  return value?.[DRAFT_STATE];
};
var latest = (state) => state.copy_ || state.base_;
var getValue = (value) => {
  const proxyDraft = getProxyDraft(value);
  return proxyDraft ? proxyDraft.copy_ ?? proxyDraft.base_ : value;
};
var getFinalValue = (state) => state.modified_ ? state.copy_ : state.base_;
function shallowCopy(base, strict) {
  if (isMap(base)) {
    return new Map(base);
  }
  if (isSet(base)) {
    return new Set(base);
  }
  if (isArray(base))
    return Array[PROTOTYPE].slice.call(base);
  const isPlain = immer_isPlainObject(base);
  if (strict === true || strict === "class_only" && !isPlain) {
    const descriptors = O.getOwnPropertyDescriptors(base);
    delete descriptors[DRAFT_STATE];
    let keys = Reflect.ownKeys(descriptors);
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const desc = descriptors[key];
      if (desc[WRITABLE] === false) {
        desc[WRITABLE] = true;
        desc[CONFIGURABLE] = true;
      }
      if (desc.get || desc.set)
        descriptors[key] = {
          [CONFIGURABLE]: true,
          [WRITABLE]: true,
          // could live with !!desc.set as well here...
          [ENUMERABLE]: desc[ENUMERABLE],
          [VALUE]: base[key]
        };
    }
    return O.create(getPrototypeOf(base), descriptors);
  } else {
    const proto = getPrototypeOf(base);
    if (proto !== null && isPlain) {
      return { ...base };
    }
    const obj = O.create(proto);
    return O.assign(obj, base);
  }
}
function freeze(obj, deep = false) {
  if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj))
    return obj;
  if (getArchtype(obj) > 1) {
    O.defineProperties(obj, {
      set: dontMutateMethodOverride,
      add: dontMutateMethodOverride,
      clear: dontMutateMethodOverride,
      delete: dontMutateMethodOverride
    });
  }
  O.freeze(obj);
  if (deep)
    each(
      obj,
      (_key, value) => {
        freeze(value, true);
      },
      false
    );
  return obj;
}
function dontMutateFrozenCollections() {
  die(2);
}
var dontMutateMethodOverride = {
  [VALUE]: dontMutateFrozenCollections
};
function isFrozen(obj) {
  if (obj === null || !isObjectish(obj))
    return true;
  return O.isFrozen(obj);
}

// src/utils/plugins.ts
var PluginMapSet = "MapSet";
var PluginPatches = "Patches";
var PluginArrayMethods = "ArrayMethods";
var plugins = {};
function getPlugin(pluginKey) {
  const plugin = plugins[pluginKey];
  if (!plugin) {
    die(0, pluginKey);
  }
  return plugin;
}
var isPluginLoaded = (pluginKey) => !!plugins[pluginKey];
function loadPlugin(pluginKey, implementation) {
  if (!plugins[pluginKey])
    plugins[pluginKey] = implementation;
}

// src/core/scope.ts
var currentScope;
var getCurrentScope = () => currentScope;
var createScope = (parent_, immer_) => ({
  drafts_: [],
  parent_,
  immer_,
  // Whenever the modified draft contains a draft from another scope, we
  // need to prevent auto-freezing so the unowned draft can be finalized.
  canAutoFreeze_: true,
  unfinalizedDrafts_: 0,
  handledSet_: /* @__PURE__ */ new Set(),
  processedForPatches_: /* @__PURE__ */ new Set(),
  mapSetPlugin_: isPluginLoaded(PluginMapSet) ? getPlugin(PluginMapSet) : void 0,
  arrayMethodsPlugin_: isPluginLoaded(PluginArrayMethods) ? getPlugin(PluginArrayMethods) : void 0
});
function usePatchesInScope(scope, patchListener) {
  if (patchListener) {
    scope.patchPlugin_ = getPlugin(PluginPatches);
    scope.patches_ = [];
    scope.inversePatches_ = [];
    scope.patchListener_ = patchListener;
  }
}
function revokeScope(scope) {
  leaveScope(scope);
  scope.drafts_.forEach(revokeDraft);
  scope.drafts_ = null;
}
function leaveScope(scope) {
  if (scope === currentScope) {
    currentScope = scope.parent_;
  }
}
var enterScope = (immer2) => currentScope = createScope(currentScope, immer2);
function revokeDraft(draft) {
  const state = draft[DRAFT_STATE];
  if (state.type_ === 0 /* Object */ || state.type_ === 1 /* Array */)
    state.revoke_();
  else
    state.revoked_ = true;
}

// src/core/finalize.ts
function processResult(result, scope) {
  scope.unfinalizedDrafts_ = scope.drafts_.length;
  const baseDraft = scope.drafts_[0];
  const isReplaced = result !== void 0 && result !== baseDraft;
  if (isReplaced) {
    if (baseDraft[DRAFT_STATE].modified_) {
      revokeScope(scope);
      die(4);
    }
    if (isDraftable(result)) {
      result = finalize(scope, result);
    }
    const { patchPlugin_ } = scope;
    if (patchPlugin_) {
      patchPlugin_.generateReplacementPatches_(
        baseDraft[DRAFT_STATE].base_,
        result,
        scope
      );
    }
  } else {
    result = finalize(scope, baseDraft);
  }
  maybeFreeze(scope, result, true);
  revokeScope(scope);
  if (scope.patches_) {
    scope.patchListener_(scope.patches_, scope.inversePatches_);
  }
  return result !== NOTHING ? result : void 0;
}
function finalize(rootScope, value) {
  if (isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  if (!state) {
    const finalValue = handleValue(value, rootScope.handledSet_, rootScope);
    return finalValue;
  }
  if (!isSameScope(state, rootScope)) {
    return value;
  }
  if (!state.modified_) {
    return state.base_;
  }
  if (!state.finalized_) {
    const { callbacks_ } = state;
    if (callbacks_) {
      while (callbacks_.length > 0) {
        const callback = callbacks_.pop();
        callback(rootScope);
      }
    }
    generatePatchesAndFinalize(state, rootScope);
  }
  return state.copy_;
}
function maybeFreeze(scope, value, deep = false) {
  if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
    freeze(value, deep);
  }
}
function markStateFinalized(state) {
  state.finalized_ = true;
  state.scope_.unfinalizedDrafts_--;
}
var isSameScope = (state, rootScope) => state.scope_ === rootScope;
var EMPTY_LOCATIONS_RESULT = [];
function updateDraftInParent(parent, draftValue, finalizedValue, originalKey) {
  const parentCopy = latest(parent);
  const parentType = parent.type_;
  if (originalKey !== void 0) {
    const currentValue = immer_get(parentCopy, originalKey, parentType);
    if (currentValue === draftValue) {
      set(parentCopy, originalKey, finalizedValue, parentType);
      return;
    }
  }
  if (!parent.draftLocations_) {
    const draftLocations = parent.draftLocations_ = /* @__PURE__ */ new Map();
    each(parentCopy, (key, value) => {
      if (isDraft(value)) {
        const keys = draftLocations.get(value) || [];
        keys.push(key);
        draftLocations.set(value, keys);
      }
    });
  }
  const locations = parent.draftLocations_.get(draftValue) ?? EMPTY_LOCATIONS_RESULT;
  for (const location of locations) {
    set(parentCopy, location, finalizedValue, parentType);
  }
}
function registerChildFinalizationCallback(parent, child, key) {
  parent.callbacks_.push(function childCleanup(rootScope) {
    const state = child;
    if (!state || !isSameScope(state, rootScope)) {
      return;
    }
    rootScope.mapSetPlugin_?.fixSetContents(state);
    const finalizedValue = getFinalValue(state);
    updateDraftInParent(parent, state.draft_ ?? state, finalizedValue, key);
    generatePatchesAndFinalize(state, rootScope);
  });
}
function generatePatchesAndFinalize(state, rootScope) {
  const shouldFinalize = state.modified_ && !state.finalized_ && (state.type_ === 3 /* Set */ || state.type_ === 1 /* Array */ && state.allIndicesReassigned_ || (state.assigned_?.size ?? 0) > 0);
  if (shouldFinalize) {
    const { patchPlugin_ } = rootScope;
    if (patchPlugin_) {
      const basePath = patchPlugin_.getPath(state);
      if (basePath) {
        patchPlugin_.generatePatches_(state, basePath, rootScope);
      }
    }
    markStateFinalized(state);
  }
}
function handleCrossReference(target, key, value) {
  const { scope_ } = target;
  if (isDraft(value)) {
    const state = value[DRAFT_STATE];
    if (isSameScope(state, scope_)) {
      state.callbacks_.push(function crossReferenceCleanup() {
        prepareCopy(target);
        const finalizedValue = getFinalValue(state);
        updateDraftInParent(target, value, finalizedValue, key);
      });
    }
  } else if (isDraftable(value)) {
    target.callbacks_.push(function nestedDraftCleanup() {
      const targetCopy = latest(target);
      if (target.type_ === 3 /* Set */) {
        if (targetCopy.has(value)) {
          handleValue(value, scope_.handledSet_, scope_);
        }
      } else {
        if (immer_get(targetCopy, key, target.type_) === value) {
          if (scope_.drafts_.length > 1 && (target.assigned_.get(key) ?? false) === true && target.copy_) {
            handleValue(
              immer_get(target.copy_, key, target.type_),
              scope_.handledSet_,
              scope_
            );
          }
        }
      }
    });
  }
}
function handleValue(target, handledSet, rootScope) {
  if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
    return target;
  }
  if (isDraft(target) || handledSet.has(target) || !isDraftable(target) || isFrozen(target)) {
    return target;
  }
  handledSet.add(target);
  each(target, (key, value) => {
    if (isDraft(value)) {
      const state = value[DRAFT_STATE];
      if (isSameScope(state, rootScope)) {
        const updatedValue = getFinalValue(state);
        set(target, key, updatedValue, target.type_);
        markStateFinalized(state);
      }
    } else if (isDraftable(value)) {
      handleValue(value, handledSet, rootScope);
    }
  });
  return target;
}

// src/core/proxy.ts
function createProxyProxy(base, parent) {
  const baseIsArray = isArray(base);
  const state = {
    type_: baseIsArray ? 1 /* Array */ : 0 /* Object */,
    // Track which produce call this is associated with.
    scope_: parent ? parent.scope_ : getCurrentScope(),
    // True for both shallow and deep changes.
    modified_: false,
    // Used during finalization.
    finalized_: false,
    // Track which properties have been assigned (true) or deleted (false).
    // actually instantiated in `prepareCopy()`
    assigned_: void 0,
    // The parent draft state.
    parent_: parent,
    // The base state.
    base_: base,
    // The base proxy.
    draft_: null,
    // set below
    // The base copy with any updated values.
    copy_: null,
    // Called by the `produce` function.
    revoke_: null,
    isManual_: false,
    // `callbacks` actually gets assigned in `createProxy`
    callbacks_: void 0
  };
  let target = state;
  let traps = objectTraps;
  if (baseIsArray) {
    target = [state];
    traps = arrayTraps;
  }
  const { revoke, proxy } = Proxy.revocable(target, traps);
  state.draft_ = proxy;
  state.revoke_ = revoke;
  return [proxy, state];
}
var objectTraps = {
  get(state, prop) {
    if (prop === DRAFT_STATE)
      return state;
    let arrayPlugin = state.scope_.arrayMethodsPlugin_;
    const isArrayWithStringProp = state.type_ === 1 /* Array */ && typeof prop === "string";
    if (isArrayWithStringProp) {
      if (arrayPlugin?.isArrayOperationMethod(prop)) {
        return arrayPlugin.createMethodInterceptor(state, prop);
      }
    }
    const source = latest(state);
    if (!has(source, prop, state.type_)) {
      return readPropFromProto(state, source, prop);
    }
    const value = source[prop];
    if (state.finalized_ || !isDraftable(value)) {
      return value;
    }
    if (isArrayWithStringProp && state.operationMethod && arrayPlugin?.isMutatingArrayMethod(
      state.operationMethod
    ) && isArrayIndex(prop)) {
      return value;
    }
    if (value === peek(state.base_, prop)) {
      prepareCopy(state);
      const childKey = state.type_ === 1 /* Array */ ? +prop : prop;
      const childDraft = createProxy(state.scope_, value, state, childKey);
      return state.copy_[childKey] = childDraft;
    }
    return value;
  },
  has(state, prop) {
    return prop in latest(state);
  },
  ownKeys(state) {
    return Reflect.ownKeys(latest(state));
  },
  set(state, prop, value) {
    const desc = getDescriptorFromProto(latest(state), prop);
    if (desc?.set) {
      desc.set.call(state.draft_, value);
      return true;
    }
    if (!state.modified_) {
      const current2 = peek(latest(state), prop);
      const currentState = current2?.[DRAFT_STATE];
      if (currentState && currentState.base_ === value) {
        state.copy_[prop] = value;
        state.assigned_.set(prop, false);
        return true;
      }
      if (is(value, current2) && (value !== void 0 || has(state.base_, prop, state.type_)))
        return true;
      prepareCopy(state);
      markChanged(state);
    }
    if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
    (value !== void 0 || prop in state.copy_) || // special case: NaN
    Number.isNaN(value) && Number.isNaN(state.copy_[prop]))
      return true;
    state.copy_[prop] = value;
    state.assigned_.set(prop, true);
    handleCrossReference(state, prop, value);
    return true;
  },
  deleteProperty(state, prop) {
    prepareCopy(state);
    if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
      state.assigned_.set(prop, false);
      markChanged(state);
    } else {
      state.assigned_.delete(prop);
    }
    if (state.copy_) {
      delete state.copy_[prop];
    }
    return true;
  },
  // Note: We never coerce `desc.value` into an Immer draft, because we can't make
  // the same guarantee in ES5 mode.
  getOwnPropertyDescriptor(state, prop) {
    const owner = latest(state);
    const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
    if (!desc)
      return desc;
    return {
      [WRITABLE]: true,
      [CONFIGURABLE]: state.type_ !== 1 /* Array */ || prop !== "length",
      [ENUMERABLE]: desc[ENUMERABLE],
      [VALUE]: owner[prop]
    };
  },
  defineProperty() {
    die(11);
  },
  getPrototypeOf(state) {
    return getPrototypeOf(state.base_);
  },
  setPrototypeOf() {
    die(12);
  }
};
var arrayTraps = {};
for (let key in objectTraps) {
  let fn = objectTraps[key];
  arrayTraps[key] = function() {
    const args = arguments;
    args[0] = args[0][0];
    return fn.apply(this, args);
  };
}
arrayTraps.deleteProperty = function(state, prop) {
  if (false)
    // removed by dead control flow
{}
  return arrayTraps.set.call(this, state, prop, void 0);
};
arrayTraps.set = function(state, prop, value) {
  if (false)
    // removed by dead control flow
{}
  return objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function peek(draft, prop) {
  const state = draft[DRAFT_STATE];
  const source = state ? latest(state) : draft;
  return source[prop];
}
function readPropFromProto(state, source, prop) {
  const desc = getDescriptorFromProto(source, prop);
  return desc ? VALUE in desc ? desc[VALUE] : (
    // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_)
  ) : void 0;
}
function getDescriptorFromProto(source, prop) {
  if (!(prop in source))
    return void 0;
  let proto = getPrototypeOf(source);
  while (proto) {
    const desc = Object.getOwnPropertyDescriptor(proto, prop);
    if (desc)
      return desc;
    proto = getPrototypeOf(proto);
  }
  return void 0;
}
function markChanged(state) {
  if (!state.modified_) {
    state.modified_ = true;
    if (state.parent_) {
      markChanged(state.parent_);
    }
  }
}
function prepareCopy(state) {
  if (!state.copy_) {
    state.assigned_ = /* @__PURE__ */ new Map();
    state.copy_ = shallowCopy(
      state.base_,
      state.scope_.immer_.useStrictShallowCopy_
    );
  }
}

// src/core/immerClass.ts
var Immer2 = class {
  constructor(config) {
    this.autoFreeze_ = true;
    this.useStrictShallowCopy_ = false;
    this.useStrictIteration_ = false;
    /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */
    this.produce = (base, recipe, patchListener) => {
      if (isFunction(base) && !isFunction(recipe)) {
        const defaultBase = recipe;
        recipe = base;
        const self = this;
        return function curriedProduce(base2 = defaultBase, ...args) {
          return self.produce(base2, (draft) => recipe.call(this, draft, ...args));
        };
      }
      if (!isFunction(recipe))
        die(6);
      if (patchListener !== void 0 && !isFunction(patchListener))
        die(7);
      let result;
      if (isDraftable(base)) {
        const scope = enterScope(this);
        const proxy = createProxy(scope, base, void 0);
        let hasError = true;
        try {
          result = recipe(proxy);
          hasError = false;
        } finally {
          if (hasError)
            revokeScope(scope);
          else
            leaveScope(scope);
        }
        usePatchesInScope(scope, patchListener);
        return processResult(result, scope);
      } else if (!base || !isObjectish(base)) {
        result = recipe(base);
        if (result === void 0)
          result = base;
        if (result === NOTHING)
          result = void 0;
        if (this.autoFreeze_)
          freeze(result, true);
        if (patchListener) {
          const p = [];
          const ip = [];
          getPlugin(PluginPatches).generateReplacementPatches_(base, result, {
            patches_: p,
            inversePatches_: ip
          });
          patchListener(p, ip);
        }
        return result;
      } else
        die(1, base);
    };
    this.produceWithPatches = (base, recipe) => {
      if (isFunction(base)) {
        return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
      }
      let patches, inversePatches;
      const result = this.produce(base, recipe, (p, ip) => {
        patches = p;
        inversePatches = ip;
      });
      return [result, patches, inversePatches];
    };
    if (isBoolean(config?.autoFreeze))
      this.setAutoFreeze(config.autoFreeze);
    if (isBoolean(config?.useStrictShallowCopy))
      this.setUseStrictShallowCopy(config.useStrictShallowCopy);
    if (isBoolean(config?.useStrictIteration))
      this.setUseStrictIteration(config.useStrictIteration);
  }
  createDraft(base) {
    if (!isDraftable(base))
      die(8);
    if (isDraft(base))
      base = current(base);
    const scope = enterScope(this);
    const proxy = createProxy(scope, base, void 0);
    proxy[DRAFT_STATE].isManual_ = true;
    leaveScope(scope);
    return proxy;
  }
  finishDraft(draft, patchListener) {
    const state = draft && draft[DRAFT_STATE];
    if (!state || !state.isManual_)
      die(9);
    const { scope_: scope } = state;
    usePatchesInScope(scope, patchListener);
    return processResult(void 0, scope);
  }
  /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */
  setAutoFreeze(value) {
    this.autoFreeze_ = value;
  }
  /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */
  setUseStrictShallowCopy(value) {
    this.useStrictShallowCopy_ = value;
  }
  /**
   * Pass false to use faster iteration that skips non-enumerable properties
   * but still handles symbols for compatibility.
   *
   * By default, strict iteration is enabled (includes all own properties).
   */
  setUseStrictIteration(value) {
    this.useStrictIteration_ = value;
  }
  shouldUseStrictIteration() {
    return this.useStrictIteration_;
  }
  applyPatches(base, patches) {
    let i;
    for (i = patches.length - 1; i >= 0; i--) {
      const patch = patches[i];
      if (patch.path.length === 0 && patch.op === "replace") {
        base = patch.value;
        break;
      }
    }
    if (i > -1) {
      patches = patches.slice(i + 1);
    }
    const applyPatchesImpl = getPlugin(PluginPatches).applyPatches_;
    if (isDraft(base)) {
      return applyPatchesImpl(base, patches);
    }
    return this.produce(
      base,
      (draft) => applyPatchesImpl(draft, patches)
    );
  }
};
function createProxy(rootScope, value, parent, key) {
  const [draft, state] = isMap(value) ? getPlugin(PluginMapSet).proxyMap_(value, parent) : isSet(value) ? getPlugin(PluginMapSet).proxySet_(value, parent) : createProxyProxy(value, parent);
  const scope = parent?.scope_ ?? getCurrentScope();
  scope.drafts_.push(draft);
  state.callbacks_ = parent?.callbacks_ ?? [];
  state.key_ = key;
  if (parent && key !== void 0) {
    registerChildFinalizationCallback(parent, state, key);
  } else {
    state.callbacks_.push(function rootDraftCleanup(rootScope2) {
      rootScope2.mapSetPlugin_?.fixSetContents(state);
      const { patchPlugin_ } = rootScope2;
      if (state.modified_ && patchPlugin_) {
        patchPlugin_.generatePatches_(state, [], rootScope2);
      }
    });
  }
  return draft;
}

// src/core/current.ts
function current(value) {
  if (!isDraft(value))
    die(10, value);
  return currentImpl(value);
}
function currentImpl(value) {
  if (!isDraftable(value) || isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  let copy;
  let strict = true;
  if (state) {
    if (!state.modified_)
      return state.base_;
    state.finalized_ = true;
    copy = shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
    strict = state.scope_.immer_.shouldUseStrictIteration();
  } else {
    copy = shallowCopy(value, true);
  }
  each(
    copy,
    (key, childValue) => {
      set(copy, key, currentImpl(childValue));
    },
    strict
  );
  if (state) {
    state.finalized_ = false;
  }
  return copy;
}

// src/plugins/patches.ts
function enablePatches() {
  const errorOffset = 16;
  if (false) // removed by dead control flow
{}
  function getPath(state, path = []) {
    if (state.key_ !== void 0) {
      const parentCopy = state.parent_.copy_ ?? state.parent_.base_;
      const proxyDraft = getProxyDraft(immer_get(parentCopy, state.key_));
      const valueAtKey = immer_get(parentCopy, state.key_);
      if (valueAtKey === void 0) {
        return null;
      }
      if (valueAtKey !== state.draft_ && valueAtKey !== state.base_ && valueAtKey !== state.copy_) {
        return null;
      }
      if (proxyDraft != null && proxyDraft.base_ !== state.base_) {
        return null;
      }
      const isSet2 = state.parent_.type_ === 3 /* Set */;
      let key;
      if (isSet2) {
        const setParent = state.parent_;
        key = Array.from(setParent.drafts_.keys()).indexOf(state.key_);
      } else {
        key = state.key_;
      }
      if (!(isSet2 && parentCopy.size > key || has(parentCopy, key))) {
        return null;
      }
      path.push(key);
    }
    if (state.parent_) {
      return getPath(state.parent_, path);
    }
    path.reverse();
    try {
      resolvePath(state.copy_, path);
    } catch (e) {
      return null;
    }
    return path;
  }
  function resolvePath(base, path) {
    let current2 = base;
    for (let i = 0; i < path.length - 1; i++) {
      const key = path[i];
      current2 = immer_get(current2, key);
      if (!isObjectish(current2) || current2 === null) {
        throw new Error(`Cannot resolve path at '${path.join("/")}'`);
      }
    }
    return current2;
  }
  const REPLACE = "replace";
  const ADD = "add";
  const REMOVE = "remove";
  function generatePatches_(state, basePath, scope) {
    if (state.scope_.processedForPatches_.has(state)) {
      return;
    }
    state.scope_.processedForPatches_.add(state);
    const { patches_, inversePatches_ } = scope;
    switch (state.type_) {
      case 0 /* Object */:
      case 2 /* Map */:
        return generatePatchesFromAssigned(
          state,
          basePath,
          patches_,
          inversePatches_
        );
      case 1 /* Array */:
        return generateArrayPatches(
          state,
          basePath,
          patches_,
          inversePatches_
        );
      case 3 /* Set */:
        return generateSetPatches(
          state,
          basePath,
          patches_,
          inversePatches_
        );
    }
  }
  function generateArrayPatches(state, basePath, patches, inversePatches) {
    let { base_, assigned_ } = state;
    let copy_ = state.copy_;
    if (copy_.length < base_.length) {
      ;
      [base_, copy_] = [copy_, base_];
      [patches, inversePatches] = [inversePatches, patches];
    }
    const allReassigned = state.allIndicesReassigned_ === true;
    for (let i = 0; i < base_.length; i++) {
      const copiedItem = copy_[i];
      const baseItem = base_[i];
      const isAssigned = allReassigned || assigned_?.get(i.toString());
      if (isAssigned && copiedItem !== baseItem) {
        const childState = copiedItem?.[DRAFT_STATE];
        if (childState && childState.modified_) {
          continue;
        }
        const path = basePath.concat([i]);
        patches.push({
          op: REPLACE,
          path,
          // Need to maybe clone it, as it can in fact be the original value
          // due to the base/copy inversion at the start of this function
          value: clonePatchValueIfNeeded(copiedItem)
        });
        inversePatches.push({
          op: REPLACE,
          path,
          value: clonePatchValueIfNeeded(baseItem)
        });
      }
    }
    for (let i = base_.length; i < copy_.length; i++) {
      const path = basePath.concat([i]);
      patches.push({
        op: ADD,
        path,
        // Need to maybe clone it, as it can in fact be the original value
        // due to the base/copy inversion at the start of this function
        value: clonePatchValueIfNeeded(copy_[i])
      });
    }
    for (let i = copy_.length - 1; base_.length <= i; --i) {
      const path = basePath.concat([i]);
      inversePatches.push({
        op: REMOVE,
        path
      });
    }
  }
  function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
    const { base_, copy_, type_ } = state;
    each(state.assigned_, (key, assignedValue) => {
      const origValue = immer_get(base_, key, type_);
      const value = immer_get(copy_, key, type_);
      const op = !assignedValue ? REMOVE : has(base_, key) ? REPLACE : ADD;
      if (origValue === value && op === REPLACE)
        return;
      const path = basePath.concat(key);
      patches.push(
        op === REMOVE ? { op, path } : { op, path, value: clonePatchValueIfNeeded(value) }
      );
      inversePatches.push(
        op === ADD ? { op: REMOVE, path } : op === REMOVE ? { op: ADD, path, value: clonePatchValueIfNeeded(origValue) } : { op: REPLACE, path, value: clonePatchValueIfNeeded(origValue) }
      );
    });
  }
  function generateSetPatches(state, basePath, patches, inversePatches) {
    let { base_, copy_ } = state;
    let i = 0;
    base_.forEach((value) => {
      if (!copy_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: REMOVE,
          path,
          value
        });
        inversePatches.unshift({
          op: ADD,
          path,
          value
        });
      }
      i++;
    });
    i = 0;
    copy_.forEach((value) => {
      if (!base_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: ADD,
          path,
          value
        });
        inversePatches.unshift({
          op: REMOVE,
          path,
          value
        });
      }
      i++;
    });
  }
  function generateReplacementPatches_(baseValue, replacement, scope) {
    const { patches_, inversePatches_ } = scope;
    patches_.push({
      op: REPLACE,
      path: [],
      value: replacement === NOTHING ? void 0 : replacement
    });
    inversePatches_.push({
      op: REPLACE,
      path: [],
      value: baseValue
    });
  }
  function applyPatches_(draft, patches) {
    patches.forEach((patch) => {
      const { path, op } = patch;
      let base = draft;
      for (let i = 0; i < path.length - 1; i++) {
        const parentType = getArchtype(base);
        let p = path[i];
        if (typeof p !== "string" && typeof p !== "number") {
          p = "" + p;
        }
        if ((parentType === 0 /* Object */ || parentType === 1 /* Array */) && (p === "__proto__" || p === CONSTRUCTOR))
          die(errorOffset + 3);
        if (isFunction(base) && p === PROTOTYPE)
          die(errorOffset + 3);
        base = immer_get(base, p);
        if (!isObjectish(base))
          die(errorOffset + 2, path.join("/"));
      }
      const type = getArchtype(base);
      const value = deepClonePatchValue(patch.value);
      const key = path[path.length - 1];
      switch (op) {
        case REPLACE:
          switch (type) {
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              die(errorOffset);
            default:
              return base[key] = value;
          }
        case ADD:
          switch (type) {
            case 1 /* Array */:
              return key === "-" ? base.push(value) : base.splice(key, 0, value);
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              return base.add(value);
            default:
              return base[key] = value;
          }
        case REMOVE:
          switch (type) {
            case 1 /* Array */:
              return base.splice(key, 1);
            case 2 /* Map */:
              return base.delete(key);
            case 3 /* Set */:
              return base.delete(patch.value);
            default:
              return delete base[key];
          }
        default:
          die(errorOffset + 1, op);
      }
    });
    return draft;
  }
  function deepClonePatchValue(obj) {
    if (!isDraftable(obj))
      return obj;
    if (isArray(obj))
      return obj.map(deepClonePatchValue);
    if (isMap(obj))
      return new Map(
        Array.from(obj.entries()).map(([k, v]) => [k, deepClonePatchValue(v)])
      );
    if (isSet(obj))
      return new Set(Array.from(obj).map(deepClonePatchValue));
    const cloned = Object.create(getPrototypeOf(obj));
    for (const key in obj)
      cloned[key] = deepClonePatchValue(obj[key]);
    if (has(obj, DRAFTABLE))
      cloned[DRAFTABLE] = obj[DRAFTABLE];
    return cloned;
  }
  function clonePatchValueIfNeeded(obj) {
    if (isDraft(obj)) {
      return deepClonePatchValue(obj);
    } else
      return obj;
  }
  loadPlugin(PluginPatches, {
    applyPatches_,
    generatePatches_,
    generateReplacementPatches_,
    getPath
  });
}

// src/plugins/mapset.ts
function enableMapSet() {
  class DraftMap extends Map {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 2 /* Map */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        assigned_: void 0,
        base_: target,
        draft_: this,
        isManual_: false,
        revoked_: false,
        callbacks_: []
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(key) {
      return latest(this[DRAFT_STATE]).has(key);
    }
    set(key, value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!latest(state).has(key) || latest(state).get(key) !== value) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_.set(key, true);
        state.copy_.set(key, value);
        state.assigned_.set(key, true);
        handleCrossReference(state, key, value);
      }
      return this;
    }
    delete(key) {
      if (!this.has(key)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareMapCopy(state);
      markChanged(state);
      if (state.base_.has(key)) {
        state.assigned_.set(key, false);
      } else {
        state.assigned_.delete(key);
      }
      state.copy_.delete(key);
      return true;
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_ = /* @__PURE__ */ new Map();
        each(state.base_, (key) => {
          state.assigned_.set(key, false);
        });
        state.copy_.clear();
      }
    }
    forEach(cb, thisArg) {
      const state = this[DRAFT_STATE];
      latest(state).forEach((_value, key, _map) => {
        cb.call(thisArg, this.get(key), key, this);
      });
    }
    get(key) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      const value = latest(state).get(key);
      if (state.finalized_ || !isDraftable(value)) {
        return value;
      }
      if (value !== state.base_.get(key)) {
        return value;
      }
      const draft = createProxy(state.scope_, value, state, key);
      prepareMapCopy(state);
      state.copy_.set(key, draft);
      return draft;
    }
    keys() {
      return latest(this[DRAFT_STATE]).keys();
    }
    values() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.values(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value
          };
        }
      };
    }
    entries() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.entries(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value: [r.value, value]
          };
        }
      };
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.entries();
    }
  }
  function proxyMap_(target, parent) {
    const map = new DraftMap(target, parent);
    return [map, map[DRAFT_STATE]];
  }
  function prepareMapCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = new Map(state.base_);
    }
  }
  class DraftSet extends Set {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 3 /* Set */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        base_: target,
        draft_: this,
        drafts_: /* @__PURE__ */ new Map(),
        revoked_: false,
        isManual_: false,
        assigned_: void 0,
        callbacks_: []
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!state.copy_) {
        return state.base_.has(value);
      }
      if (state.copy_.has(value))
        return true;
      if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value)))
        return true;
      return false;
    }
    add(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!this.has(value)) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.add(value);
        handleCrossReference(state, value, value);
      }
      return this;
    }
    delete(value) {
      if (!this.has(value)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      markChanged(state);
      return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : (
        /* istanbul ignore next */
        false
      ));
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.clear();
      }
    }
    values() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.values();
    }
    entries() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.entries();
    }
    keys() {
      return this.values();
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.values();
    }
    forEach(cb, thisArg) {
      const iterator = this.values();
      let result = iterator.next();
      while (!result.done) {
        cb.call(thisArg, result.value, result.value, this);
        result = iterator.next();
      }
    }
  }
  function proxySet_(target, parent) {
    const set2 = new DraftSet(target, parent);
    return [set2, set2[DRAFT_STATE]];
  }
  function prepareSetCopy(state) {
    if (!state.copy_) {
      state.copy_ = /* @__PURE__ */ new Set();
      state.base_.forEach((value) => {
        if (isDraftable(value)) {
          const draft = createProxy(state.scope_, value, state, value);
          state.drafts_.set(value, draft);
          state.copy_.add(draft);
        } else {
          state.copy_.add(value);
        }
      });
    }
  }
  function assertUnrevoked(state) {
    if (state.revoked_)
      die(3, JSON.stringify(latest(state)));
  }
  function fixSetContents(target) {
    if (target.type_ === 3 /* Set */ && target.copy_) {
      const copy = new Set(target.copy_);
      target.copy_.clear();
      copy.forEach((value) => {
        target.copy_.add(getValue(value));
      });
    }
  }
  loadPlugin(PluginMapSet, { proxyMap_, proxySet_, fixSetContents });
}

// src/plugins/arrayMethods.ts
function enableArrayMethods() {
  const SHIFTING_METHODS = /* @__PURE__ */ new Set(["shift", "unshift"]);
  const QUEUE_METHODS = /* @__PURE__ */ new Set(["push", "pop"]);
  const RESULT_RETURNING_METHODS = /* @__PURE__ */ new Set([
    ...QUEUE_METHODS,
    ...SHIFTING_METHODS
  ]);
  const REORDERING_METHODS = /* @__PURE__ */ new Set(["reverse", "sort"]);
  const MUTATING_METHODS = /* @__PURE__ */ new Set([
    ...RESULT_RETURNING_METHODS,
    ...REORDERING_METHODS,
    "splice"
  ]);
  const FIND_METHODS = /* @__PURE__ */ new Set(["find", "findLast"]);
  const NON_MUTATING_METHODS = /* @__PURE__ */ new Set([
    "filter",
    "slice",
    "concat",
    "flat",
    ...FIND_METHODS,
    "findIndex",
    "findLastIndex",
    "some",
    "every",
    "indexOf",
    "lastIndexOf",
    "includes",
    "join",
    "toString",
    "toLocaleString"
  ]);
  function isMutatingArrayMethod(method) {
    return MUTATING_METHODS.has(method);
  }
  function isNonMutatingArrayMethod(method) {
    return NON_MUTATING_METHODS.has(method);
  }
  function isArrayOperationMethod(method) {
    return isMutatingArrayMethod(method) || isNonMutatingArrayMethod(method);
  }
  function enterOperation(state, method) {
    state.operationMethod = method;
  }
  function exitOperation(state) {
    state.operationMethod = void 0;
  }
  function executeArrayMethod(state, operation, markLength = true) {
    prepareCopy(state);
    const result = operation();
    markChanged(state);
    if (markLength)
      state.assigned_.set("length", true);
    return result;
  }
  function markAllIndicesReassigned(state) {
    state.allIndicesReassigned_ = true;
  }
  function normalizeSliceIndex(index, length) {
    if (index < 0) {
      return Math.max(length + index, 0);
    }
    return Math.min(index, length);
  }
  function handleInsertedValues(state, startIndex, values) {
    for (let i = 0; i < values.length; i++) {
      const index = startIndex + i;
      state.assigned_.set(index, true);
      handleCrossReference(state, index, values[i]);
    }
  }
  function handleSimpleOperation(state, method, args) {
    return executeArrayMethod(state, () => {
      const lengthBefore = state.copy_.length;
      const result = state.copy_[method](...args);
      if (SHIFTING_METHODS.has(method)) {
        markAllIndicesReassigned(state);
      }
      if (method === "push" && args.length > 0) {
        handleInsertedValues(state, lengthBefore, args);
      } else if (method === "unshift" && args.length > 0) {
        handleInsertedValues(state, 0, args);
      }
      return RESULT_RETURNING_METHODS.has(method) ? result : state.draft_;
    });
  }
  function handleReorderingOperation(state, method, args) {
    return executeArrayMethod(
      state,
      () => {
        ;
        state.copy_[method](...args);
        markAllIndicesReassigned(state);
        return state.draft_;
      },
      false
    );
  }
  function createMethodInterceptor(state, originalMethod) {
    return function interceptedMethod(...args) {
      const method = originalMethod;
      enterOperation(state, method);
      try {
        if (isMutatingArrayMethod(method)) {
          if (RESULT_RETURNING_METHODS.has(method)) {
            return handleSimpleOperation(state, method, args);
          }
          if (REORDERING_METHODS.has(method)) {
            return handleReorderingOperation(state, method, args);
          }
          if (method === "splice") {
            const res = executeArrayMethod(
              state,
              () => state.copy_.splice(...args)
            );
            markAllIndicesReassigned(state);
            if (args.length > 2) {
              const startIndex = normalizeSliceIndex(
                args[0] ?? 0,
                state.copy_.length
              );
              handleInsertedValues(state, startIndex, args.slice(2));
            }
            return res;
          }
        } else {
          return handleNonMutatingOperation(state, method, args);
        }
      } finally {
        exitOperation(state);
      }
    };
  }
  function handleNonMutatingOperation(state, method, args) {
    const source = latest(state);
    if (method === "filter") {
      const predicate = args[0];
      const result = [];
      for (let i = 0; i < source.length; i++) {
        if (predicate(source[i], i, source)) {
          result.push(state.draft_[i]);
        }
      }
      return result;
    }
    if (FIND_METHODS.has(method)) {
      const predicate = args[0];
      const isForward = method === "find";
      const step = isForward ? 1 : -1;
      const start = isForward ? 0 : source.length - 1;
      for (let i = start; i >= 0 && i < source.length; i += step) {
        if (predicate(source[i], i, source)) {
          return state.draft_[i];
        }
      }
      return void 0;
    }
    if (method === "slice") {
      const rawStart = args[0] ?? 0;
      const rawEnd = args[1] ?? source.length;
      const start = normalizeSliceIndex(rawStart, source.length);
      const end = normalizeSliceIndex(rawEnd, source.length);
      const result = [];
      for (let i = start; i < end; i++) {
        result.push(state.draft_[i]);
      }
      return result;
    }
    return source[method](...args);
  }
  loadPlugin(PluginArrayMethods, {
    createMethodInterceptor,
    isArrayOperationMethod,
    isMutatingArrayMethod
  });
}

// src/immer.ts
var immer = new Immer2();
var produce = immer.produce;
var produceWithPatches = /* @__PURE__ */ immer.produceWithPatches.bind(
  immer
);
var setAutoFreeze = /* @__PURE__ */ immer.setAutoFreeze.bind(immer);
var setUseStrictShallowCopy = /* @__PURE__ */ immer.setUseStrictShallowCopy.bind(
  immer
);
var setUseStrictIteration = /* @__PURE__ */ immer.setUseStrictIteration.bind(
  immer
);
var applyPatches = /* @__PURE__ */ immer.applyPatches.bind(immer);
var createDraft = /* @__PURE__ */ immer.createDraft.bind(immer);
var finishDraft = /* @__PURE__ */ immer.finishDraft.bind(immer);
var castDraft = (value) => value;
var castImmutable = (value) => value;

//# sourceMappingURL=immer.mjs.map
;// ./node_modules/reselect/dist/reselect.mjs
// src/devModeChecks/identityFunctionCheck.ts
var runIdentityFunctionCheck = (resultFunc, inputSelectorsResults, outputSelectorResult) => {
  if (inputSelectorsResults.length === 1 && inputSelectorsResults[0] === outputSelectorResult) {
    let isInputSameAsOutput = false;
    try {
      const emptyObject = {};
      if (resultFunc(emptyObject) === emptyObject)
        isInputSameAsOutput = true;
    } catch {
    }
    if (isInputSameAsOutput) {
      let stack = void 0;
      try {
        throw new Error();
      } catch (e) {
        ;
        ({ stack } = e);
      }
      console.warn(
        "The result function returned its own inputs without modification. e.g\n`createSelector([state => state.todos], todos => todos)`\nThis could lead to inefficient memoization and unnecessary re-renders.\nEnsure transformation logic is in the result function, and extraction logic is in the input selectors.",
        { stack }
      );
    }
  }
};

// src/devModeChecks/inputStabilityCheck.ts
var runInputStabilityCheck = (inputSelectorResultsObject, options, inputSelectorArgs) => {
  const { memoize, memoizeOptions } = options;
  const { inputSelectorResults, inputSelectorResultsCopy } = inputSelectorResultsObject;
  const createAnEmptyObject = memoize(() => ({}), ...memoizeOptions);
  const areInputSelectorResultsEqual = createAnEmptyObject.apply(null, inputSelectorResults) === createAnEmptyObject.apply(null, inputSelectorResultsCopy);
  if (!areInputSelectorResultsEqual) {
    let stack = void 0;
    try {
      throw new Error();
    } catch (e) {
      ;
      ({ stack } = e);
    }
    console.warn(
      "An input selector returned a different result when passed same arguments.\nThis means your output selector will likely run more frequently than intended.\nAvoid returning a new reference inside your input selector, e.g.\n`createSelector([state => state.todos.map(todo => todo.id)], todoIds => todoIds.length)`",
      {
        arguments: inputSelectorArgs,
        firstInputs: inputSelectorResults,
        secondInputs: inputSelectorResultsCopy,
        stack
      }
    );
  }
};

// src/devModeChecks/setGlobalDevModeChecks.ts
var globalDevModeChecks = {
  inputStabilityCheck: "once",
  identityFunctionCheck: "once"
};
var setGlobalDevModeChecks = (devModeChecks) => {
  Object.assign(globalDevModeChecks, devModeChecks);
};

// src/utils.ts
var NOT_FOUND = /* @__PURE__ */ Symbol("NOT_FOUND");
function assertIsFunction(func, errorMessage = `expected a function, instead received ${typeof func}`) {
  if (typeof func !== "function") {
    throw new TypeError(errorMessage);
  }
}
function assertIsObject(object, errorMessage = `expected an object, instead received ${typeof object}`) {
  if (typeof object !== "object") {
    throw new TypeError(errorMessage);
  }
}
function assertIsArrayOfFunctions(array, errorMessage = `expected all items to be functions, instead received the following types: `) {
  if (!array.every((item) => typeof item === "function")) {
    const itemTypes = array.map(
      (item) => typeof item === "function" ? `function ${item.name || "unnamed"}()` : typeof item
    ).join(", ");
    throw new TypeError(`${errorMessage}[${itemTypes}]`);
  }
}
var ensureIsArray = (item) => {
  return Array.isArray(item) ? item : [item];
};
function getDependencies(createSelectorArgs) {
  const dependencies = Array.isArray(createSelectorArgs[0]) ? createSelectorArgs[0] : createSelectorArgs;
  assertIsArrayOfFunctions(
    dependencies,
    `createSelector expects all input-selectors to be functions, but received the following types: `
  );
  return dependencies;
}
function collectInputSelectorResults(dependencies, inputSelectorArgs) {
  const inputSelectorResults = [];
  const { length } = dependencies;
  for (let i = 0; i < length; i++) {
    inputSelectorResults.push(dependencies[i].apply(null, inputSelectorArgs));
  }
  return inputSelectorResults;
}
var getDevModeChecksExecutionInfo = (firstRun, devModeChecks) => {
  const { identityFunctionCheck, inputStabilityCheck } = {
    ...globalDevModeChecks,
    ...devModeChecks
  };
  return {
    identityFunctionCheck: {
      shouldRun: identityFunctionCheck === "always" || identityFunctionCheck === "once" && firstRun,
      run: runIdentityFunctionCheck
    },
    inputStabilityCheck: {
      shouldRun: inputStabilityCheck === "always" || inputStabilityCheck === "once" && firstRun,
      run: runInputStabilityCheck
    }
  };
};

// src/autotrackMemoize/autotracking.ts
var $REVISION = 0;
var CURRENT_TRACKER = null;
var Cell = class {
  revision = $REVISION;
  _value;
  _lastValue;
  _isEqual = tripleEq;
  constructor(initialValue, isEqual = tripleEq) {
    this._value = this._lastValue = initialValue;
    this._isEqual = isEqual;
  }
  // Whenever a storage value is read, it'll add itself to the current tracker if
  // one exists, entangling its state with that cache.
  get value() {
    CURRENT_TRACKER?.add(this);
    return this._value;
  }
  // Whenever a storage value is updated, we bump the global revision clock,
  // assign the revision for this storage to the new value, _and_ we schedule a
  // rerender. This is important, and it's what makes autotracking  _pull_
  // based. We don't actively tell the caches which depend on the storage that
  // anything has happened. Instead, we recompute the caches when needed.
  set value(newValue) {
    if (this.value === newValue)
      return;
    this._value = newValue;
    this.revision = ++$REVISION;
  }
};
function tripleEq(a, b) {
  return a === b;
}
var TrackingCache = class {
  _cachedValue;
  _cachedRevision = -1;
  _deps = [];
  hits = 0;
  fn;
  constructor(fn) {
    this.fn = fn;
  }
  clear() {
    this._cachedValue = void 0;
    this._cachedRevision = -1;
    this._deps = [];
    this.hits = 0;
  }
  get value() {
    if (this.revision > this._cachedRevision) {
      const { fn } = this;
      const currentTracker = /* @__PURE__ */ new Set();
      const prevTracker = CURRENT_TRACKER;
      CURRENT_TRACKER = currentTracker;
      this._cachedValue = fn();
      CURRENT_TRACKER = prevTracker;
      this.hits++;
      this._deps = Array.from(currentTracker);
      this._cachedRevision = this.revision;
    }
    CURRENT_TRACKER?.add(this);
    return this._cachedValue;
  }
  get revision() {
    return Math.max(...this._deps.map((d) => d.revision), 0);
  }
};
function reselect_getValue(cell) {
  if (!(cell instanceof Cell)) {
    console.warn("Not a valid cell! ", cell);
  }
  return cell.value;
}
function setValue(storage, value) {
  if (!(storage instanceof Cell)) {
    throw new TypeError(
      "setValue must be passed a tracked store created with `createStorage`."
    );
  }
  storage.value = storage._lastValue = value;
}
function createCell(initialValue, isEqual = tripleEq) {
  return new Cell(initialValue, isEqual);
}
function createCache(fn) {
  assertIsFunction(
    fn,
    "the first parameter to `createCache` must be a function"
  );
  return new TrackingCache(fn);
}

// src/autotrackMemoize/tracking.ts
var neverEq = (a, b) => false;
function createTag() {
  return createCell(null, neverEq);
}
function dirtyTag(tag, value) {
  setValue(tag, value);
}
var consumeCollection = (node) => {
  let tag = node.collectionTag;
  if (tag === null) {
    tag = node.collectionTag = createTag();
  }
  reselect_getValue(tag);
};
var dirtyCollection = (node) => {
  const tag = node.collectionTag;
  if (tag !== null) {
    dirtyTag(tag, null);
  }
};

// src/autotrackMemoize/proxy.ts
var REDUX_PROXY_LABEL = Symbol();
var nextId = 0;
var proto = Object.getPrototypeOf({});
var ObjectTreeNode = class {
  constructor(value) {
    this.value = value;
    this.value = value;
    this.tag.value = value;
  }
  proxy = new Proxy(this, objectProxyHandler);
  tag = createTag();
  tags = {};
  children = {};
  collectionTag = null;
  id = nextId++;
};
var objectProxyHandler = {
  get(node, key) {
    function calculateResult() {
      const { value } = node;
      const childValue = Reflect.get(value, key);
      if (typeof key === "symbol") {
        return childValue;
      }
      if (key in proto) {
        return childValue;
      }
      if (typeof childValue === "object" && childValue !== null) {
        let childNode = node.children[key];
        if (childNode === void 0) {
          childNode = node.children[key] = createNode(childValue);
        }
        if (childNode.tag) {
          reselect_getValue(childNode.tag);
        }
        return childNode.proxy;
      } else {
        let tag = node.tags[key];
        if (tag === void 0) {
          tag = node.tags[key] = createTag();
          tag.value = childValue;
        }
        reselect_getValue(tag);
        return childValue;
      }
    }
    const res = calculateResult();
    return res;
  },
  ownKeys(node) {
    consumeCollection(node);
    return Reflect.ownKeys(node.value);
  },
  getOwnPropertyDescriptor(node, prop) {
    return Reflect.getOwnPropertyDescriptor(node.value, prop);
  },
  has(node, prop) {
    return Reflect.has(node.value, prop);
  }
};
var ArrayTreeNode = class {
  constructor(value) {
    this.value = value;
    this.value = value;
    this.tag.value = value;
  }
  proxy = new Proxy([this], arrayProxyHandler);
  tag = createTag();
  tags = {};
  children = {};
  collectionTag = null;
  id = nextId++;
};
var arrayProxyHandler = {
  get([node], key) {
    if (key === "length") {
      consumeCollection(node);
    }
    return objectProxyHandler.get(node, key);
  },
  ownKeys([node]) {
    return objectProxyHandler.ownKeys(node);
  },
  getOwnPropertyDescriptor([node], prop) {
    return objectProxyHandler.getOwnPropertyDescriptor(node, prop);
  },
  has([node], prop) {
    return objectProxyHandler.has(node, prop);
  }
};
function createNode(value) {
  if (Array.isArray(value)) {
    return new ArrayTreeNode(value);
  }
  return new ObjectTreeNode(value);
}
function updateNode(node, newValue) {
  const { value, tags, children } = node;
  node.value = newValue;
  if (Array.isArray(value) && Array.isArray(newValue) && value.length !== newValue.length) {
    dirtyCollection(node);
  } else {
    if (value !== newValue) {
      let oldKeysSize = 0;
      let newKeysSize = 0;
      let anyKeysAdded = false;
      for (const _key in value) {
        oldKeysSize++;
      }
      for (const key in newValue) {
        newKeysSize++;
        if (!(key in value)) {
          anyKeysAdded = true;
          break;
        }
      }
      const isDifferent = anyKeysAdded || oldKeysSize !== newKeysSize;
      if (isDifferent) {
        dirtyCollection(node);
      }
    }
  }
  for (const key in tags) {
    const childValue = value[key];
    const newChildValue = newValue[key];
    if (childValue !== newChildValue) {
      dirtyCollection(node);
      dirtyTag(tags[key], newChildValue);
    }
    if (typeof newChildValue === "object" && newChildValue !== null) {
      delete tags[key];
    }
  }
  for (const key in children) {
    const childNode = children[key];
    const newChildValue = newValue[key];
    const childValue = childNode.value;
    if (childValue === newChildValue) {
      continue;
    } else if (typeof newChildValue === "object" && newChildValue !== null) {
      updateNode(childNode, newChildValue);
    } else {
      deleteNode(childNode);
      delete children[key];
    }
  }
}
function deleteNode(node) {
  if (node.tag) {
    dirtyTag(node.tag, null);
  }
  dirtyCollection(node);
  for (const key in node.tags) {
    dirtyTag(node.tags[key], null);
  }
  for (const key in node.children) {
    deleteNode(node.children[key]);
  }
}

// src/lruMemoize.ts
function createSingletonCache(equals) {
  let entry;
  return {
    get(key) {
      if (entry && equals(entry.key, key)) {
        return entry.value;
      }
      return NOT_FOUND;
    },
    put(key, value) {
      entry = { key, value };
    },
    getEntries() {
      return entry ? [entry] : [];
    },
    clear() {
      entry = void 0;
    }
  };
}
function createLruCache(maxSize, equals) {
  let entries = [];
  function get(key) {
    const cacheIndex = entries.findIndex((entry) => equals(key, entry.key));
    if (cacheIndex > -1) {
      const entry = entries[cacheIndex];
      if (cacheIndex > 0) {
        entries.splice(cacheIndex, 1);
        entries.unshift(entry);
      }
      return entry.value;
    }
    return NOT_FOUND;
  }
  function put(key, value) {
    if (get(key) === NOT_FOUND) {
      entries.unshift({ key, value });
      if (entries.length > maxSize) {
        entries.pop();
      }
    }
  }
  function getEntries() {
    return entries;
  }
  function clear() {
    entries = [];
  }
  return { get, put, getEntries, clear };
}
var referenceEqualityCheck = (a, b) => a === b;
function createCacheKeyComparator(equalityCheck) {
  return function areArgumentsShallowlyEqual(prev, next) {
    if (prev === null || next === null || prev.length !== next.length) {
      return false;
    }
    const { length } = prev;
    for (let i = 0; i < length; i++) {
      if (!equalityCheck(prev[i], next[i])) {
        return false;
      }
    }
    return true;
  };
}
function lruMemoize(func, equalityCheckOrOptions) {
  const providedOptions = typeof equalityCheckOrOptions === "object" ? equalityCheckOrOptions : { equalityCheck: equalityCheckOrOptions };
  const {
    equalityCheck = referenceEqualityCheck,
    maxSize = 1,
    resultEqualityCheck
  } = providedOptions;
  const comparator = createCacheKeyComparator(equalityCheck);
  let resultsCount = 0;
  const cache = maxSize <= 1 ? createSingletonCache(comparator) : createLruCache(maxSize, comparator);
  function memoized() {
    let value = cache.get(arguments);
    if (value === NOT_FOUND) {
      value = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const entries = cache.getEntries();
        const matchingEntry = entries.find(
          (entry) => resultEqualityCheck(entry.value, value)
        );
        if (matchingEntry) {
          value = matchingEntry.value;
          resultsCount !== 0 && resultsCount--;
        }
      }
      cache.put(arguments, value);
    }
    return value;
  }
  memoized.clearCache = () => {
    cache.clear();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}

// src/autotrackMemoize/autotrackMemoize.ts
function autotrackMemoize(func) {
  const node = createNode(
    []
  );
  let lastArgs = null;
  const shallowEqual = createCacheKeyComparator(referenceEqualityCheck);
  const cache = createCache(() => {
    const res = func.apply(null, node.proxy);
    return res;
  });
  function memoized() {
    if (!shallowEqual(lastArgs, arguments)) {
      updateNode(node, arguments);
      lastArgs = arguments;
    }
    return cache.value;
  }
  memoized.clearCache = () => {
    return cache.clear();
  };
  return memoized;
}

// src/weakMapMemoize.ts
var StrongRef = class {
  constructor(value) {
    this.value = value;
  }
  deref() {
    return this.value;
  }
};
var Ref = typeof WeakRef !== "undefined" ? WeakRef : StrongRef;
var UNTERMINATED = 0;
var TERMINATED = 1;
function createCacheNode() {
  return {
    s: UNTERMINATED,
    v: void 0,
    o: null,
    p: null
  };
}
function weakMapMemoize(func, options = {}) {
  let fnNode = createCacheNode();
  const { resultEqualityCheck } = options;
  let lastResult;
  let resultsCount = 0;
  function memoized() {
    let cacheNode = fnNode;
    const { length } = arguments;
    for (let i = 0, l = length; i < l; i++) {
      const arg = arguments[i];
      if (typeof arg === "function" || typeof arg === "object" && arg !== null) {
        let objectCache = cacheNode.o;
        if (objectCache === null) {
          cacheNode.o = objectCache = /* @__PURE__ */ new WeakMap();
        }
        const objectNode = objectCache.get(arg);
        if (objectNode === void 0) {
          cacheNode = createCacheNode();
          objectCache.set(arg, cacheNode);
        } else {
          cacheNode = objectNode;
        }
      } else {
        let primitiveCache = cacheNode.p;
        if (primitiveCache === null) {
          cacheNode.p = primitiveCache = /* @__PURE__ */ new Map();
        }
        const primitiveNode = primitiveCache.get(arg);
        if (primitiveNode === void 0) {
          cacheNode = createCacheNode();
          primitiveCache.set(arg, cacheNode);
        } else {
          cacheNode = primitiveNode;
        }
      }
    }
    const terminatedNode = cacheNode;
    let result;
    if (cacheNode.s === TERMINATED) {
      result = cacheNode.v;
    } else {
      result = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const lastResultValue = lastResult?.deref?.() ?? lastResult;
        if (lastResultValue != null && resultEqualityCheck(lastResultValue, result)) {
          result = lastResultValue;
          resultsCount !== 0 && resultsCount--;
        }
        const needsWeakRef = typeof result === "object" && result !== null || typeof result === "function";
        lastResult = needsWeakRef ? new Ref(result) : result;
      }
    }
    terminatedNode.s = TERMINATED;
    terminatedNode.v = result;
    return result;
  }
  memoized.clearCache = () => {
    fnNode = createCacheNode();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}

// src/createSelectorCreator.ts
function createSelectorCreator(memoizeOrOptions, ...memoizeOptionsFromArgs) {
  const createSelectorCreatorOptions = typeof memoizeOrOptions === "function" ? {
    memoize: memoizeOrOptions,
    memoizeOptions: memoizeOptionsFromArgs
  } : memoizeOrOptions;
  const createSelector2 = (...createSelectorArgs) => {
    let recomputations = 0;
    let dependencyRecomputations = 0;
    let lastResult;
    let directlyPassedOptions = {};
    let resultFunc = createSelectorArgs.pop();
    if (typeof resultFunc === "object") {
      directlyPassedOptions = resultFunc;
      resultFunc = createSelectorArgs.pop();
    }
    assertIsFunction(
      resultFunc,
      `createSelector expects an output function after the inputs, but received: [${typeof resultFunc}]`
    );
    const combinedOptions = {
      ...createSelectorCreatorOptions,
      ...directlyPassedOptions
    };
    const {
      memoize,
      memoizeOptions = [],
      argsMemoize = weakMapMemoize,
      argsMemoizeOptions = [],
      devModeChecks = {}
    } = combinedOptions;
    const finalMemoizeOptions = ensureIsArray(memoizeOptions);
    const finalArgsMemoizeOptions = ensureIsArray(argsMemoizeOptions);
    const dependencies = getDependencies(createSelectorArgs);
    const memoizedResultFunc = memoize(function recomputationWrapper() {
      recomputations++;
      return resultFunc.apply(
        null,
        arguments
      );
    }, ...finalMemoizeOptions);
    let firstRun = true;
    const selector = argsMemoize(function dependenciesChecker() {
      dependencyRecomputations++;
      const inputSelectorResults = collectInputSelectorResults(
        dependencies,
        arguments
      );
      lastResult = memoizedResultFunc.apply(null, inputSelectorResults);
      if (false) // removed by dead control flow
{}
      return lastResult;
    }, ...finalArgsMemoizeOptions);
    return Object.assign(selector, {
      resultFunc,
      memoizedResultFunc,
      dependencies,
      dependencyRecomputations: () => dependencyRecomputations,
      resetDependencyRecomputations: () => {
        dependencyRecomputations = 0;
      },
      lastResult: () => lastResult,
      recomputations: () => recomputations,
      resetRecomputations: () => {
        recomputations = 0;
      },
      memoize,
      argsMemoize
    });
  };
  Object.assign(createSelector2, {
    withTypes: () => createSelector2
  });
  return createSelector2;
}
var createSelector = /* @__PURE__ */ createSelectorCreator(weakMapMemoize);

// src/createStructuredSelector.ts
var createStructuredSelector = Object.assign(
  (inputSelectorsObject, selectorCreator = createSelector) => {
    assertIsObject(
      inputSelectorsObject,
      `createStructuredSelector expects first argument to be an object where each property is a selector, instead received a ${typeof inputSelectorsObject}`
    );
    const inputSelectorKeys = Object.keys(inputSelectorsObject);
    const dependencies = inputSelectorKeys.map(
      (key) => inputSelectorsObject[key]
    );
    const structuredSelector = selectorCreator(
      dependencies,
      (...inputSelectorResults) => {
        return inputSelectorResults.reduce((composition, value, index) => {
          composition[inputSelectorKeys[index]] = value;
          return composition;
        }, {});
      }
    );
    return structuredSelector;
  },
  { withTypes: () => createStructuredSelector }
);

//# sourceMappingURL=reselect.mjs.map
;// ./node_modules/redux-thunk/dist/redux-thunk.mjs
// src/index.ts
function createThunkMiddleware(extraArgument) {
  const middleware = ({ dispatch, getState }) => (next) => (action) => {
    if (typeof action === "function") {
      return action(dispatch, getState, extraArgument);
    }
    return next(action);
  };
  return middleware;
}
var redux_thunk_thunk = createThunkMiddleware();
var withExtraArgument = createThunkMiddleware;


;// ./node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs
// src/index.ts



// src/immerImports.ts


// src/index.ts


// src/reselectImports.ts


// src/createDraftSafeSelector.ts
var createDraftSafeSelectorCreator = (...args) => {
  const createSelector2 = createSelectorCreator(...args);
  const createDraftSafeSelector2 = Object.assign((...args2) => {
    const selector = createSelector2(...args2);
    const wrappedSelector = (value, ...rest) => selector(isDraft(value) ? current(value) : value, ...rest);
    Object.assign(wrappedSelector, selector);
    return wrappedSelector;
  }, {
    withTypes: () => createDraftSafeSelector2
  });
  return createDraftSafeSelector2;
};
var createDraftSafeSelector = /* @__PURE__ */ createDraftSafeSelectorCreator(weakMapMemoize);

// src/reduxImports.ts


// src/devtoolsExtension.ts
var composeWithDevTools = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ : function() {
  if (arguments.length === 0) return void 0;
  if (typeof arguments[0] === "object") return compose;
  return compose.apply(null, arguments);
};
var devToolsEnhancer = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION__ ? window.__REDUX_DEVTOOLS_EXTENSION__ : function() {
  return function(noop3) {
    return noop3;
  };
};

// src/getDefaultMiddleware.ts


// src/tsHelpers.ts
var hasMatchFunction = (v) => {
  return v && typeof v.match === "function";
};

// src/createAction.ts
function createAction(type, prepareAction) {
  function actionCreator(...args) {
    if (prepareAction) {
      let prepared = prepareAction(...args);
      if (!prepared) {
        throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(0) : 0);
      }
      return {
        type,
        payload: prepared.payload,
        ..."meta" in prepared && {
          meta: prepared.meta
        },
        ..."error" in prepared && {
          error: prepared.error
        }
      };
    }
    return {
      type,
      payload: args[0]
    };
  }
  actionCreator.toString = () => `${type}`;
  actionCreator.type = type;
  actionCreator.match = (action) => isAction(action) && action.type === type;
  return actionCreator;
}
function isActionCreator(action) {
  return typeof action === "function" && "type" in action && // hasMatchFunction only wants Matchers but I don't see the point in rewriting it
  hasMatchFunction(action);
}
function isFSA(action) {
  return isAction(action) && Object.keys(action).every(isValidKey);
}
function isValidKey(key) {
  return ["type", "payload", "error", "meta"].indexOf(key) > -1;
}

// src/actionCreatorInvariantMiddleware.ts
function getMessage(type) {
  const splitType = type ? `${type}`.split("/") : [];
  const actionName = splitType[splitType.length - 1] || "actionCreator";
  return `Detected an action creator with type "${type || "unknown"}" being dispatched. 
Make sure you're calling the action creator before dispatching, i.e. \`dispatch(${actionName}())\` instead of \`dispatch(${actionName})\`. This is necessary even if the action has no payload.`;
}
function createActionCreatorInvariantMiddleware(options = {}) {
  if (true) {
    return () => (next) => (action) => next(action);
  }
  // removed by dead control flow

  // removed by dead control flow

}

// src/utils.ts
function getTimeMeasureUtils(maxDelay, fnName) {
  let elapsed = 0;
  return {
    measureTime(fn) {
      const started = Date.now();
      try {
        return fn();
      } finally {
        const finished = Date.now();
        elapsed += finished - started;
      }
    },
    warnIfExceeded() {
      if (elapsed > maxDelay) {
        console.warn(`${fnName} took ${elapsed}ms, which is more than the warning threshold of ${maxDelay}ms. 
If your state or actions are very large, you may want to disable the middleware as it might cause too much of a slowdown in development mode. See https://redux-toolkit.js.org/api/getDefaultMiddleware for instructions.
It is disabled in production builds, so you don't need to worry about that.`);
      }
    }
  };
}
var Tuple = class _Tuple extends Array {
  constructor(...items) {
    super(...items);
    Object.setPrototypeOf(this, _Tuple.prototype);
  }
  static get [Symbol.species]() {
    return _Tuple;
  }
  concat(...arr) {
    return super.concat.apply(this, arr);
  }
  prepend(...arr) {
    if (arr.length === 1 && Array.isArray(arr[0])) {
      return new _Tuple(...arr[0].concat(this));
    }
    return new _Tuple(...arr.concat(this));
  }
};
function freezeDraftable(val) {
  return isDraftable(val) ? produce(val, () => {
  }) : val;
}
function getOrInsertComputed(map, key, compute) {
  if (map.has(key)) return map.get(key);
  return map.set(key, compute(key)).get(key);
}

// src/immutableStateInvariantMiddleware.ts
function isImmutableDefault(value) {
  return typeof value !== "object" || value == null || Object.isFrozen(value);
}
function trackForMutations(isImmutable, ignoredPaths, obj) {
  const trackedProperties = trackProperties(isImmutable, ignoredPaths, obj);
  return {
    detectMutations() {
      return detectMutations(isImmutable, ignoredPaths, trackedProperties, obj);
    }
  };
}
function trackProperties(isImmutable, ignoredPaths = [], obj, path = "", checkedObjects = /* @__PURE__ */ new Set()) {
  const tracked = {
    value: obj
  };
  if (!isImmutable(obj) && !checkedObjects.has(obj)) {
    checkedObjects.add(obj);
    tracked.children = {};
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for (const key in obj) {
      const nestedPath = path ? path + "." + key : key;
      if (hasIgnoredPaths) {
        const hasMatches = ignoredPaths.some((ignored) => {
          if (ignored instanceof RegExp) {
            return ignored.test(nestedPath);
          }
          return nestedPath === ignored;
        });
        if (hasMatches) {
          continue;
        }
      }
      tracked.children[key] = trackProperties(isImmutable, ignoredPaths, obj[key], nestedPath);
    }
  }
  return tracked;
}
function detectMutations(isImmutable, ignoredPaths = [], trackedProperty, obj, sameParentRef = false, path = "") {
  const prevObj = trackedProperty ? trackedProperty.value : void 0;
  const sameRef = prevObj === obj;
  if (sameParentRef && !sameRef && !Number.isNaN(obj)) {
    return {
      wasMutated: true,
      path
    };
  }
  if (isImmutable(prevObj) || isImmutable(obj)) {
    return {
      wasMutated: false
    };
  }
  const keysToDetect = {};
  for (let key in trackedProperty.children) {
    keysToDetect[key] = true;
  }
  for (let key in obj) {
    keysToDetect[key] = true;
  }
  const hasIgnoredPaths = ignoredPaths.length > 0;
  for (let key in keysToDetect) {
    const nestedPath = path ? path + "." + key : key;
    if (hasIgnoredPaths) {
      const hasMatches = ignoredPaths.some((ignored) => {
        if (ignored instanceof RegExp) {
          return ignored.test(nestedPath);
        }
        return nestedPath === ignored;
      });
      if (hasMatches) {
        continue;
      }
    }
    const result = detectMutations(isImmutable, ignoredPaths, trackedProperty.children[key], obj[key], sameRef, nestedPath);
    if (result.wasMutated) {
      return result;
    }
  }
  return {
    wasMutated: false
  };
}
function createImmutableStateInvariantMiddleware(options = {}) {
  if (true) {
    return () => (next) => (action) => next(action);
  } else // removed by dead control flow
{ var stringify, getSerialize; }
}

// src/serializableStateInvariantMiddleware.ts
function isPlain(val) {
  const type = typeof val;
  return val == null || type === "string" || type === "boolean" || type === "number" || Array.isArray(val) || isPlainObject(val);
}
function findNonSerializableValue(value, path = "", isSerializable = isPlain, getEntries, ignoredPaths = [], cache) {
  let foundNestedSerializable;
  if (!isSerializable(value)) {
    return {
      keyPath: path || "<root>",
      value
    };
  }
  if (typeof value !== "object" || value === null) {
    return false;
  }
  if (cache?.has(value)) return false;
  const entries = getEntries != null ? getEntries(value) : Object.entries(value);
  const hasIgnoredPaths = ignoredPaths.length > 0;
  for (const [key, nestedValue] of entries) {
    const nestedPath = path ? path + "." + key : key;
    if (hasIgnoredPaths) {
      const hasMatches = ignoredPaths.some((ignored) => {
        if (ignored instanceof RegExp) {
          return ignored.test(nestedPath);
        }
        return nestedPath === ignored;
      });
      if (hasMatches) {
        continue;
      }
    }
    if (!isSerializable(nestedValue)) {
      return {
        keyPath: nestedPath,
        value: nestedValue
      };
    }
    if (typeof nestedValue === "object") {
      foundNestedSerializable = findNonSerializableValue(nestedValue, nestedPath, isSerializable, getEntries, ignoredPaths, cache);
      if (foundNestedSerializable) {
        return foundNestedSerializable;
      }
    }
  }
  if (cache && isNestedFrozen(value)) cache.add(value);
  return false;
}
function isNestedFrozen(value) {
  if (!Object.isFrozen(value)) return false;
  for (const nestedValue of Object.values(value)) {
    if (typeof nestedValue !== "object" || nestedValue === null) continue;
    if (!isNestedFrozen(nestedValue)) return false;
  }
  return true;
}
function createSerializableStateInvariantMiddleware(options = {}) {
  if (true) {
    return () => (next) => (action) => next(action);
  } else // removed by dead control flow
{}
}

// src/getDefaultMiddleware.ts
function redux_toolkit_modern_isBoolean(x) {
  return typeof x === "boolean";
}
var buildGetDefaultMiddleware = () => function getDefaultMiddleware(options) {
  const {
    thunk = true,
    immutableCheck = true,
    serializableCheck = true,
    actionCreatorCheck = true
  } = options ?? {};
  let middlewareArray = new Tuple();
  if (thunk) {
    if (redux_toolkit_modern_isBoolean(thunk)) {
      middlewareArray.push(redux_thunk_thunk);
    } else {
      middlewareArray.push(withExtraArgument(thunk.extraArgument));
    }
  }
  if (false) // removed by dead control flow
{}
  return middlewareArray;
};

// src/autoBatchEnhancer.ts
var SHOULD_AUTOBATCH = "RTK_autoBatch";
var prepareAutoBatched = () => (payload) => ({
  payload,
  meta: {
    [SHOULD_AUTOBATCH]: true
  }
});
var createQueueWithTimer = (timeout) => {
  return (notify) => {
    setTimeout(notify, timeout);
  };
};
var autoBatchEnhancer = (options = {
  type: "raf"
}) => (next) => (...args) => {
  const store = next(...args);
  let notifying = true;
  let shouldNotifyAtEndOfTick = false;
  let notificationQueued = false;
  const listeners = /* @__PURE__ */ new Set();
  const queueCallback = options.type === "tick" ? queueMicrotask : options.type === "raf" ? (
    // requestAnimationFrame won't exist in SSR environments. Fall back to a vague approximation just to keep from erroring.
    typeof window !== "undefined" && window.requestAnimationFrame ? window.requestAnimationFrame : createQueueWithTimer(10)
  ) : options.type === "callback" ? options.queueNotification : createQueueWithTimer(options.timeout);
  const notifyListeners = () => {
    notificationQueued = false;
    if (shouldNotifyAtEndOfTick) {
      shouldNotifyAtEndOfTick = false;
      listeners.forEach((l) => l());
    }
  };
  return Object.assign({}, store, {
    // Override the base `store.subscribe` method to keep original listeners
    // from running if we're delaying notifications
    subscribe(listener2) {
      const wrappedListener = () => notifying && listener2();
      const unsubscribe = store.subscribe(wrappedListener);
      listeners.add(listener2);
      return () => {
        unsubscribe();
        listeners.delete(listener2);
      };
    },
    // Override the base `store.dispatch` method so that we can check actions
    // for the `shouldAutoBatch` flag and determine if batching is active
    dispatch(action) {
      try {
        notifying = !action?.meta?.[SHOULD_AUTOBATCH];
        shouldNotifyAtEndOfTick = !notifying;
        if (shouldNotifyAtEndOfTick) {
          if (!notificationQueued) {
            notificationQueued = true;
            queueCallback(notifyListeners);
          }
        }
        return store.dispatch(action);
      } finally {
        notifying = true;
      }
    }
  });
};

// src/getDefaultEnhancers.ts
var buildGetDefaultEnhancers = (middlewareEnhancer) => function getDefaultEnhancers(options) {
  const {
    autoBatch = true
  } = options ?? {};
  let enhancerArray = new Tuple(middlewareEnhancer);
  if (autoBatch) {
    enhancerArray.push(autoBatchEnhancer(typeof autoBatch === "object" ? autoBatch : void 0));
  }
  return enhancerArray;
};

// src/configureStore.ts
function configureStore(options) {
  const getDefaultMiddleware = buildGetDefaultMiddleware();
  const {
    reducer = void 0,
    middleware,
    devTools = true,
    duplicateMiddlewareCheck = true,
    preloadedState = void 0,
    enhancers = void 0
  } = options || {};
  let rootReducer;
  if (typeof reducer === "function") {
    rootReducer = reducer;
  } else if (isPlainObject(reducer)) {
    rootReducer = combineReducers(reducer);
  } else {
    throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(1) : 0);
  }
  if (false) // removed by dead control flow
{}
  let finalMiddleware;
  if (typeof middleware === "function") {
    finalMiddleware = middleware(getDefaultMiddleware);
    if (false) // removed by dead control flow
{}
  } else {
    finalMiddleware = getDefaultMiddleware();
  }
  if (false) // removed by dead control flow
{}
  if (false) // removed by dead control flow
{}
  let finalCompose = compose;
  if (devTools) {
    finalCompose = composeWithDevTools({
      // Enable capture of stack traces for dispatched Redux actions
      trace: "production" !== "production",
      ...typeof devTools === "object" && devTools
    });
  }
  const middlewareEnhancer = applyMiddleware(...finalMiddleware);
  const getDefaultEnhancers = buildGetDefaultEnhancers(middlewareEnhancer);
  if (false) // removed by dead control flow
{}
  let storeEnhancers = typeof enhancers === "function" ? enhancers(getDefaultEnhancers) : getDefaultEnhancers();
  if (false) // removed by dead control flow
{}
  if (false) // removed by dead control flow
{}
  if (false) // removed by dead control flow
{}
  const composedEnhancer = finalCompose(...storeEnhancers);
  return createStore(rootReducer, preloadedState, composedEnhancer);
}

// src/mapBuilders.ts
function executeReducerBuilderCallback(builderCallback) {
  const actionsMap = {};
  const actionMatchers = [];
  let defaultCaseReducer;
  const builder = {
    addCase(typeOrActionCreator, reducer) {
      if (false) // removed by dead control flow
{}
      const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
      if (!type) {
        throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(28) : 0);
      }
      if (type in actionsMap) {
        throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(29) : 0);
      }
      actionsMap[type] = reducer;
      return builder;
    },
    addAsyncThunk(asyncThunk, reducers) {
      if (false) // removed by dead control flow
{}
      if (reducers.pending) actionsMap[asyncThunk.pending.type] = reducers.pending;
      if (reducers.rejected) actionsMap[asyncThunk.rejected.type] = reducers.rejected;
      if (reducers.fulfilled) actionsMap[asyncThunk.fulfilled.type] = reducers.fulfilled;
      if (reducers.settled) actionMatchers.push({
        matcher: asyncThunk.settled,
        reducer: reducers.settled
      });
      return builder;
    },
    addMatcher(matcher, reducer) {
      if (false) // removed by dead control flow
{}
      actionMatchers.push({
        matcher,
        reducer
      });
      return builder;
    },
    addDefaultCase(reducer) {
      if (false) // removed by dead control flow
{}
      defaultCaseReducer = reducer;
      return builder;
    }
  };
  builderCallback(builder);
  return [actionsMap, actionMatchers, defaultCaseReducer];
}

// src/createReducer.ts
function isStateFunction(x) {
  return typeof x === "function";
}
function createReducer(initialState, mapOrBuilderCallback) {
  if (false) // removed by dead control flow
{}
  let [actionsMap, finalActionMatchers, finalDefaultCaseReducer] = executeReducerBuilderCallback(mapOrBuilderCallback);
  let getInitialState;
  if (isStateFunction(initialState)) {
    getInitialState = () => freezeDraftable(initialState());
  } else {
    const frozenInitialState = freezeDraftable(initialState);
    getInitialState = () => frozenInitialState;
  }
  function reducer(state = getInitialState(), action) {
    let caseReducers = [actionsMap[action.type], ...finalActionMatchers.filter(({
      matcher
    }) => matcher(action)).map(({
      reducer: reducer2
    }) => reducer2)];
    if (caseReducers.filter((cr) => !!cr).length === 0) {
      caseReducers = [finalDefaultCaseReducer];
    }
    return caseReducers.reduce((previousState, caseReducer) => {
      if (caseReducer) {
        if (isDraft(previousState)) {
          const draft = previousState;
          const result = caseReducer(draft, action);
          if (result === void 0) {
            return previousState;
          }
          return result;
        } else if (!isDraftable(previousState)) {
          const result = caseReducer(previousState, action);
          if (result === void 0) {
            if (previousState === null) {
              return previousState;
            }
            throw Error("A case reducer on a non-draftable value must not return undefined");
          }
          return result;
        } else {
          return produce(previousState, (draft) => {
            return caseReducer(draft, action);
          });
        }
      }
      return previousState;
    }, state);
  }
  reducer.getInitialState = getInitialState;
  return reducer;
}

// src/matchers.ts
var matches = (matcher, action) => {
  if (hasMatchFunction(matcher)) {
    return matcher.match(action);
  } else {
    return matcher(action);
  }
};
function isAnyOf(...matchers) {
  return (action) => {
    return matchers.some((matcher) => matches(matcher, action));
  };
}
function isAllOf(...matchers) {
  return (action) => {
    return matchers.every((matcher) => matches(matcher, action));
  };
}
function hasExpectedRequestMetadata(action, validStatus) {
  if (!action || !action.meta) return false;
  const hasValidRequestId = typeof action.meta.requestId === "string";
  const hasValidRequestStatus = validStatus.indexOf(action.meta.requestStatus) > -1;
  return hasValidRequestId && hasValidRequestStatus;
}
function isAsyncThunkArray(a) {
  return typeof a[0] === "function" && "pending" in a[0] && "fulfilled" in a[0] && "rejected" in a[0];
}
function isPending(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["pending"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isPending()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.pending));
}
function isRejected(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["rejected"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isRejected()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.rejected));
}
function isRejectedWithValue(...asyncThunks) {
  const hasFlag = (action) => {
    return action && action.meta && action.meta.rejectedWithValue;
  };
  if (asyncThunks.length === 0) {
    return isAllOf(isRejected(...asyncThunks), hasFlag);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isRejectedWithValue()(asyncThunks[0]);
  }
  return isAllOf(isRejected(...asyncThunks), hasFlag);
}
function isFulfilled(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["fulfilled"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isFulfilled()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.map((asyncThunk) => asyncThunk.fulfilled));
}
function isAsyncThunkAction(...asyncThunks) {
  if (asyncThunks.length === 0) {
    return (action) => hasExpectedRequestMetadata(action, ["pending", "fulfilled", "rejected"]);
  }
  if (!isAsyncThunkArray(asyncThunks)) {
    return isAsyncThunkAction()(asyncThunks[0]);
  }
  return isAnyOf(...asyncThunks.flatMap((asyncThunk) => [asyncThunk.pending, asyncThunk.rejected, asyncThunk.fulfilled]));
}

// src/nanoid.ts
var urlAlphabet = "ModuleSymbhasOwnPr-0123456789ABCDEFGHNRVfgctiUvz_KqYTJkLxpZXIjQW";
var nanoid = (size = 21) => {
  let id = "";
  let i = size;
  while (i--) {
    id += urlAlphabet[Math.random() * 64 | 0];
  }
  return id;
};

// src/createAsyncThunk.ts
var commonProperties = ["name", "message", "stack", "code"];
var RejectWithValue = class {
  constructor(payload, meta) {
    this.payload = payload;
    this.meta = meta;
  }
  /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */
  _type;
};
var FulfillWithMeta = class {
  constructor(payload, meta) {
    this.payload = payload;
    this.meta = meta;
  }
  /*
  type-only property to distinguish between RejectWithValue and FulfillWithMeta
  does not exist at runtime
  */
  _type;
};
var miniSerializeError = (value) => {
  if (typeof value === "object" && value !== null) {
    const simpleError = {};
    for (const property of commonProperties) {
      if (typeof value[property] === "string") {
        simpleError[property] = value[property];
      }
    }
    return simpleError;
  }
  return {
    message: String(value)
  };
};
var externalAbortMessage = "External signal was aborted";
var createAsyncThunk = /* @__PURE__ */ (() => {
  function createAsyncThunk2(typePrefix, payloadCreator, options) {
    const fulfilled = createAction(typePrefix + "/fulfilled", (payload, requestId, arg, meta) => ({
      payload,
      meta: {
        ...meta || {},
        arg,
        requestId,
        requestStatus: "fulfilled"
      }
    }));
    const pending = createAction(typePrefix + "/pending", (requestId, arg, meta) => ({
      payload: void 0,
      meta: {
        ...meta || {},
        arg,
        requestId,
        requestStatus: "pending"
      }
    }));
    const rejected = createAction(typePrefix + "/rejected", (error, requestId, arg, payload, meta) => ({
      payload,
      error: (options && options.serializeError || miniSerializeError)(error || "Rejected"),
      meta: {
        ...meta || {},
        arg,
        requestId,
        rejectedWithValue: !!payload,
        requestStatus: "rejected",
        aborted: error?.name === "AbortError",
        condition: error?.name === "ConditionError"
      }
    }));
    function actionCreator(arg, {
      signal
    } = {}) {
      return (dispatch, getState, extra) => {
        const requestId = options?.idGenerator ? options.idGenerator(arg) : nanoid();
        const abortController = new AbortController();
        let abortHandler;
        let abortReason;
        function abort(reason) {
          abortReason = reason;
          abortController.abort();
        }
        if (signal) {
          if (signal.aborted) {
            abort(externalAbortMessage);
          } else {
            signal.addEventListener("abort", () => abort(externalAbortMessage), {
              once: true
            });
          }
        }
        const promise = async function() {
          let finalAction;
          try {
            let conditionResult = options?.condition?.(arg, {
              getState,
              extra
            });
            if (isThenable(conditionResult)) {
              conditionResult = await conditionResult;
            }
            if (conditionResult === false || abortController.signal.aborted) {
              throw {
                name: "ConditionError",
                message: "Aborted due to condition callback returning false."
              };
            }
            const abortedPromise = new Promise((_, reject) => {
              abortHandler = () => {
                reject({
                  name: "AbortError",
                  message: abortReason || "Aborted"
                });
              };
              abortController.signal.addEventListener("abort", abortHandler, {
                once: true
              });
            });
            dispatch(pending(requestId, arg, options?.getPendingMeta?.({
              requestId,
              arg
            }, {
              getState,
              extra
            })));
            finalAction = await Promise.race([abortedPromise, Promise.resolve(payloadCreator(arg, {
              dispatch,
              getState,
              extra,
              requestId,
              signal: abortController.signal,
              abort,
              rejectWithValue: (value, meta) => {
                return new RejectWithValue(value, meta);
              },
              fulfillWithValue: (value, meta) => {
                return new FulfillWithMeta(value, meta);
              }
            })).then((result) => {
              if (result instanceof RejectWithValue) {
                throw result;
              }
              if (result instanceof FulfillWithMeta) {
                return fulfilled(result.payload, requestId, arg, result.meta);
              }
              return fulfilled(result, requestId, arg);
            })]);
          } catch (err) {
            finalAction = err instanceof RejectWithValue ? rejected(null, requestId, arg, err.payload, err.meta) : rejected(err, requestId, arg);
          } finally {
            if (abortHandler) {
              abortController.signal.removeEventListener("abort", abortHandler);
            }
          }
          const skipDispatch = options && !options.dispatchConditionRejection && rejected.match(finalAction) && finalAction.meta.condition;
          if (!skipDispatch) {
            dispatch(finalAction);
          }
          return finalAction;
        }();
        return Object.assign(promise, {
          abort,
          requestId,
          arg,
          unwrap() {
            return promise.then(unwrapResult);
          }
        });
      };
    }
    return Object.assign(actionCreator, {
      pending,
      rejected,
      fulfilled,
      settled: isAnyOf(rejected, fulfilled),
      typePrefix
    });
  }
  createAsyncThunk2.withTypes = () => createAsyncThunk2;
  return createAsyncThunk2;
})();
function unwrapResult(action) {
  if (action.meta && action.meta.rejectedWithValue) {
    throw action.payload;
  }
  if (action.error) {
    throw action.error;
  }
  return action.payload;
}
function isThenable(value) {
  return value !== null && typeof value === "object" && typeof value.then === "function";
}

// src/createSlice.ts
var asyncThunkSymbol = /* @__PURE__ */ Symbol.for("rtk-slice-createasyncthunk");
var asyncThunkCreator = {
  [asyncThunkSymbol]: createAsyncThunk
};
var ReducerType = /* @__PURE__ */ ((ReducerType2) => {
  ReducerType2["reducer"] = "reducer";
  ReducerType2["reducerWithPrepare"] = "reducerWithPrepare";
  ReducerType2["asyncThunk"] = "asyncThunk";
  return ReducerType2;
})(ReducerType || {});
function getType(slice, actionKey) {
  return `${slice}/${actionKey}`;
}
function buildCreateSlice({
  creators
} = {}) {
  const cAT = creators?.asyncThunk?.[asyncThunkSymbol];
  return function createSlice2(options) {
    const {
      name,
      reducerPath = name
    } = options;
    if (!name) {
      throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(11) : 0);
    }
    if (typeof process !== "undefined" && "production" === "development") // removed by dead control flow
{}
    const reducers = (typeof options.reducers === "function" ? options.reducers(buildReducerCreators()) : options.reducers) || {};
    const reducerNames = Object.keys(reducers);
    const context = {
      sliceCaseReducersByName: {},
      sliceCaseReducersByType: {},
      actionCreators: {},
      sliceMatchers: []
    };
    const contextMethods = {
      addCase(typeOrActionCreator, reducer2) {
        const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
        if (!type) {
          throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(12) : 0);
        }
        if (type in context.sliceCaseReducersByType) {
          throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(13) : 0);
        }
        context.sliceCaseReducersByType[type] = reducer2;
        return contextMethods;
      },
      addMatcher(matcher, reducer2) {
        context.sliceMatchers.push({
          matcher,
          reducer: reducer2
        });
        return contextMethods;
      },
      exposeAction(name2, actionCreator) {
        context.actionCreators[name2] = actionCreator;
        return contextMethods;
      },
      exposeCaseReducer(name2, reducer2) {
        context.sliceCaseReducersByName[name2] = reducer2;
        return contextMethods;
      }
    };
    reducerNames.forEach((reducerName) => {
      const reducerDefinition = reducers[reducerName];
      const reducerDetails = {
        reducerName,
        type: getType(name, reducerName),
        createNotation: typeof options.reducers === "function"
      };
      if (isAsyncThunkSliceReducerDefinition(reducerDefinition)) {
        handleThunkCaseReducerDefinition(reducerDetails, reducerDefinition, contextMethods, cAT);
      } else {
        handleNormalReducerDefinition(reducerDetails, reducerDefinition, contextMethods);
      }
    });
    function buildReducer() {
      if (false) // removed by dead control flow
{}
      const [extraReducers = {}, actionMatchers = [], defaultCaseReducer = void 0] = typeof options.extraReducers === "function" ? executeReducerBuilderCallback(options.extraReducers) : [options.extraReducers];
      const finalCaseReducers = {
        ...extraReducers,
        ...context.sliceCaseReducersByType
      };
      return createReducer(options.initialState, (builder) => {
        for (let key in finalCaseReducers) {
          builder.addCase(key, finalCaseReducers[key]);
        }
        for (let sM of context.sliceMatchers) {
          builder.addMatcher(sM.matcher, sM.reducer);
        }
        for (let m of actionMatchers) {
          builder.addMatcher(m.matcher, m.reducer);
        }
        if (defaultCaseReducer) {
          builder.addDefaultCase(defaultCaseReducer);
        }
      });
    }
    const selectSelf = (state) => state;
    const injectedSelectorCache = /* @__PURE__ */ new Map();
    const injectedStateCache = /* @__PURE__ */ new WeakMap();
    let _reducer;
    function reducer(state, action) {
      if (!_reducer) _reducer = buildReducer();
      return _reducer(state, action);
    }
    function getInitialState() {
      if (!_reducer) _reducer = buildReducer();
      return _reducer.getInitialState();
    }
    function makeSelectorProps(reducerPath2, injected = false) {
      function selectSlice(state) {
        let sliceState = state[reducerPath2];
        if (typeof sliceState === "undefined") {
          if (injected) {
            sliceState = getOrInsertComputed(injectedStateCache, selectSlice, getInitialState);
          } else if (false) // removed by dead control flow
{}
        }
        return sliceState;
      }
      function getSelectors(selectState = selectSelf) {
        const selectorCache = getOrInsertComputed(injectedSelectorCache, injected, () => /* @__PURE__ */ new WeakMap());
        return getOrInsertComputed(selectorCache, selectState, () => {
          const map = {};
          for (const [name2, selector] of Object.entries(options.selectors ?? {})) {
            map[name2] = wrapSelector(selector, selectState, () => getOrInsertComputed(injectedStateCache, selectState, getInitialState), injected);
          }
          return map;
        });
      }
      return {
        reducerPath: reducerPath2,
        getSelectors,
        get selectors() {
          return getSelectors(selectSlice);
        },
        selectSlice
      };
    }
    const slice = {
      name,
      reducer,
      actions: context.actionCreators,
      caseReducers: context.sliceCaseReducersByName,
      getInitialState,
      ...makeSelectorProps(reducerPath),
      injectInto(injectable, {
        reducerPath: pathOpt,
        ...config
      } = {}) {
        const newReducerPath = pathOpt ?? reducerPath;
        injectable.inject({
          reducerPath: newReducerPath,
          reducer
        }, config);
        return {
          ...slice,
          ...makeSelectorProps(newReducerPath, true)
        };
      }
    };
    return slice;
  };
}
function wrapSelector(selector, selectState, getInitialState, injected) {
  function wrapper(rootState, ...args) {
    let sliceState = selectState(rootState);
    if (typeof sliceState === "undefined") {
      if (injected) {
        sliceState = getInitialState();
      } else if (false) // removed by dead control flow
{}
    }
    return selector(sliceState, ...args);
  }
  wrapper.unwrapped = selector;
  return wrapper;
}
var createSlice = /* @__PURE__ */ buildCreateSlice();
function buildReducerCreators() {
  function asyncThunk(payloadCreator, config) {
    return {
      _reducerDefinitionType: "asyncThunk" /* asyncThunk */,
      payloadCreator,
      ...config
    };
  }
  asyncThunk.withTypes = () => asyncThunk;
  return {
    reducer(caseReducer) {
      return Object.assign({
        // hack so the wrapping function has the same name as the original
        // we need to create a wrapper so the `reducerDefinitionType` is not assigned to the original
        [caseReducer.name](...args) {
          return caseReducer(...args);
        }
      }[caseReducer.name], {
        _reducerDefinitionType: "reducer" /* reducer */
      });
    },
    preparedReducer(prepare, reducer) {
      return {
        _reducerDefinitionType: "reducerWithPrepare" /* reducerWithPrepare */,
        prepare,
        reducer
      };
    },
    asyncThunk
  };
}
function handleNormalReducerDefinition({
  type,
  reducerName,
  createNotation
}, maybeReducerWithPrepare, context) {
  let caseReducer;
  let prepareCallback;
  if ("reducer" in maybeReducerWithPrepare) {
    if (createNotation && !isCaseReducerWithPrepareDefinition(maybeReducerWithPrepare)) {
      throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(17) : 0);
    }
    caseReducer = maybeReducerWithPrepare.reducer;
    prepareCallback = maybeReducerWithPrepare.prepare;
  } else {
    caseReducer = maybeReducerWithPrepare;
  }
  context.addCase(type, caseReducer).exposeCaseReducer(reducerName, caseReducer).exposeAction(reducerName, prepareCallback ? createAction(type, prepareCallback) : createAction(type));
}
function isAsyncThunkSliceReducerDefinition(reducerDefinition) {
  return reducerDefinition._reducerDefinitionType === "asyncThunk" /* asyncThunk */;
}
function isCaseReducerWithPrepareDefinition(reducerDefinition) {
  return reducerDefinition._reducerDefinitionType === "reducerWithPrepare" /* reducerWithPrepare */;
}
function handleThunkCaseReducerDefinition({
  type,
  reducerName
}, reducerDefinition, context, cAT) {
  if (!cAT) {
    throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(18) : 0);
  }
  const {
    payloadCreator,
    fulfilled,
    pending,
    rejected,
    settled,
    options
  } = reducerDefinition;
  const thunk = cAT(type, payloadCreator, options);
  context.exposeAction(reducerName, thunk);
  if (fulfilled) {
    context.addCase(thunk.fulfilled, fulfilled);
  }
  if (pending) {
    context.addCase(thunk.pending, pending);
  }
  if (rejected) {
    context.addCase(thunk.rejected, rejected);
  }
  if (settled) {
    context.addMatcher(thunk.settled, settled);
  }
  context.exposeCaseReducer(reducerName, {
    fulfilled: fulfilled || redux_toolkit_modern_noop,
    pending: pending || redux_toolkit_modern_noop,
    rejected: rejected || redux_toolkit_modern_noop,
    settled: settled || redux_toolkit_modern_noop
  });
}
function redux_toolkit_modern_noop() {
}

// src/entities/entity_state.ts
function getInitialEntityState() {
  return {
    ids: [],
    entities: {}
  };
}
function createInitialStateFactory(stateAdapter) {
  function getInitialState(additionalState = {}, entities) {
    const state = Object.assign(getInitialEntityState(), additionalState);
    return entities ? stateAdapter.setAll(state, entities) : state;
  }
  return {
    getInitialState
  };
}

// src/entities/state_selectors.ts
function createSelectorsFactory() {
  function getSelectors(selectState, options = {}) {
    const {
      createSelector: createSelector2 = createDraftSafeSelector
    } = options;
    const selectIds = (state) => state.ids;
    const selectEntities = (state) => state.entities;
    const selectAll = createSelector2(selectIds, selectEntities, (ids, entities) => ids.map((id) => entities[id]));
    const selectId = (_, id) => id;
    const selectById = (entities, id) => entities[id];
    const selectTotal = createSelector2(selectIds, (ids) => ids.length);
    if (!selectState) {
      return {
        selectIds,
        selectEntities,
        selectAll,
        selectTotal,
        selectById: createSelector2(selectEntities, selectId, selectById)
      };
    }
    const selectGlobalizedEntities = createSelector2(selectState, selectEntities);
    return {
      selectIds: createSelector2(selectState, selectIds),
      selectEntities: selectGlobalizedEntities,
      selectAll: createSelector2(selectState, selectAll),
      selectTotal: createSelector2(selectState, selectTotal),
      selectById: createSelector2(selectGlobalizedEntities, selectId, selectById)
    };
  }
  return {
    getSelectors
  };
}

// src/entities/state_adapter.ts
var isDraftTyped = isDraft;
function createSingleArgumentStateOperator(mutator) {
  const operator = createStateOperator((_, state) => mutator(state));
  return function operation(state) {
    return operator(state, void 0);
  };
}
function createStateOperator(mutator) {
  return function operation(state, arg) {
    function isPayloadActionArgument(arg2) {
      return isFSA(arg2);
    }
    const runMutator = (draft) => {
      if (isPayloadActionArgument(arg)) {
        mutator(arg.payload, draft);
      } else {
        mutator(arg, draft);
      }
    };
    if (isDraftTyped(state)) {
      runMutator(state);
      return state;
    }
    return produce(state, runMutator);
  };
}

// src/entities/utils.ts
function selectIdValue(entity, selectId) {
  const key = selectId(entity);
  if (false) // removed by dead control flow
{}
  return key;
}
function ensureEntitiesArray(entities) {
  if (!Array.isArray(entities)) {
    entities = Object.values(entities);
  }
  return entities;
}
function getCurrent(value) {
  return isDraft(value) ? current(value) : value;
}
function splitAddedUpdatedEntities(newEntities, selectId, state) {
  newEntities = ensureEntitiesArray(newEntities);
  const existingIdsArray = getCurrent(state.ids);
  const existingIds = new Set(existingIdsArray);
  const added = [];
  const addedIds = /* @__PURE__ */ new Set([]);
  const updated = [];
  for (const entity of newEntities) {
    const id = selectIdValue(entity, selectId);
    if (existingIds.has(id) || addedIds.has(id)) {
      updated.push({
        id,
        changes: entity
      });
    } else {
      addedIds.add(id);
      added.push(entity);
    }
  }
  return [added, updated, existingIdsArray];
}

// src/entities/unsorted_state_adapter.ts
function createUnsortedStateAdapter(selectId) {
  function addOneMutably(entity, state) {
    const key = selectIdValue(entity, selectId);
    if (key in state.entities) {
      return;
    }
    state.ids.push(key);
    state.entities[key] = entity;
  }
  function addManyMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    for (const entity of newEntities) {
      addOneMutably(entity, state);
    }
  }
  function setOneMutably(entity, state) {
    const key = selectIdValue(entity, selectId);
    if (!(key in state.entities)) {
      state.ids.push(key);
    }
    ;
    state.entities[key] = entity;
  }
  function setManyMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    for (const entity of newEntities) {
      setOneMutably(entity, state);
    }
  }
  function setAllMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    state.ids = [];
    state.entities = {};
    addManyMutably(newEntities, state);
  }
  function removeOneMutably(key, state) {
    return removeManyMutably([key], state);
  }
  function removeManyMutably(keys, state) {
    let didMutate = false;
    keys.forEach((key) => {
      if (key in state.entities) {
        delete state.entities[key];
        didMutate = true;
      }
    });
    if (didMutate) {
      state.ids = state.ids.filter((id) => id in state.entities);
    }
  }
  function removeAllMutably(state) {
    Object.assign(state, {
      ids: [],
      entities: {}
    });
  }
  function takeNewKey(keys, update, state) {
    const original3 = state.entities[update.id];
    if (original3 === void 0) {
      return false;
    }
    const updated = Object.assign({}, original3, update.changes);
    const newKey = selectIdValue(updated, selectId);
    const hasNewKey = newKey !== update.id;
    if (hasNewKey) {
      keys[update.id] = newKey;
      delete state.entities[update.id];
    }
    ;
    state.entities[newKey] = updated;
    return hasNewKey;
  }
  function updateOneMutably(update, state) {
    return updateManyMutably([update], state);
  }
  function updateManyMutably(updates, state) {
    const newKeys = {};
    const updatesPerEntity = {};
    updates.forEach((update) => {
      if (update.id in state.entities) {
        updatesPerEntity[update.id] = {
          id: update.id,
          // Spreads ignore falsy values, so this works even if there isn't
          // an existing update already at this key
          changes: {
            ...updatesPerEntity[update.id]?.changes,
            ...update.changes
          }
        };
      }
    });
    updates = Object.values(updatesPerEntity);
    const didMutateEntities = updates.length > 0;
    if (didMutateEntities) {
      const didMutateIds = updates.filter((update) => takeNewKey(newKeys, update, state)).length > 0;
      if (didMutateIds) {
        state.ids = Object.values(state.entities).map((e) => selectIdValue(e, selectId));
      }
    }
  }
  function upsertOneMutably(entity, state) {
    return upsertManyMutably([entity], state);
  }
  function upsertManyMutably(newEntities, state) {
    const [added, updated] = splitAddedUpdatedEntities(newEntities, selectId, state);
    addManyMutably(added, state);
    updateManyMutably(updated, state);
  }
  return {
    removeAll: createSingleArgumentStateOperator(removeAllMutably),
    addOne: createStateOperator(addOneMutably),
    addMany: createStateOperator(addManyMutably),
    setOne: createStateOperator(setOneMutably),
    setMany: createStateOperator(setManyMutably),
    setAll: createStateOperator(setAllMutably),
    updateOne: createStateOperator(updateOneMutably),
    updateMany: createStateOperator(updateManyMutably),
    upsertOne: createStateOperator(upsertOneMutably),
    upsertMany: createStateOperator(upsertManyMutably),
    removeOne: createStateOperator(removeOneMutably),
    removeMany: createStateOperator(removeManyMutably)
  };
}

// src/entities/sorted_state_adapter.ts
function findInsertIndex(sortedItems, item, comparisonFunction) {
  let lowIndex = 0;
  let highIndex = sortedItems.length;
  while (lowIndex < highIndex) {
    let middleIndex = lowIndex + highIndex >>> 1;
    const currentItem = sortedItems[middleIndex];
    const res = comparisonFunction(item, currentItem);
    if (res >= 0) {
      lowIndex = middleIndex + 1;
    } else {
      highIndex = middleIndex;
    }
  }
  return lowIndex;
}
function insert(sortedItems, item, comparisonFunction) {
  const insertAtIndex = findInsertIndex(sortedItems, item, comparisonFunction);
  sortedItems.splice(insertAtIndex, 0, item);
  return sortedItems;
}
function createSortedStateAdapter(selectId, comparer) {
  const {
    removeOne,
    removeMany,
    removeAll
  } = createUnsortedStateAdapter(selectId);
  function addOneMutably(entity, state) {
    return addManyMutably([entity], state);
  }
  function addManyMutably(newEntities, state, existingIds) {
    newEntities = ensureEntitiesArray(newEntities);
    const existingKeys = new Set(existingIds ?? getCurrent(state.ids));
    const addedKeys = /* @__PURE__ */ new Set();
    const models = newEntities.filter((model) => {
      const modelId = selectIdValue(model, selectId);
      const notAdded = !addedKeys.has(modelId);
      if (notAdded) addedKeys.add(modelId);
      return !existingKeys.has(modelId) && notAdded;
    });
    if (models.length !== 0) {
      mergeFunction(state, models);
    }
  }
  function setOneMutably(entity, state) {
    return setManyMutably([entity], state);
  }
  function setManyMutably(newEntities, state) {
    let deduplicatedEntities = {};
    newEntities = ensureEntitiesArray(newEntities);
    if (newEntities.length !== 0) {
      for (const item of newEntities) {
        const entityId = selectId(item);
        deduplicatedEntities[entityId] = item;
        delete state.entities[entityId];
      }
      newEntities = ensureEntitiesArray(deduplicatedEntities);
      mergeFunction(state, newEntities);
    }
  }
  function setAllMutably(newEntities, state) {
    newEntities = ensureEntitiesArray(newEntities);
    state.entities = {};
    state.ids = [];
    addManyMutably(newEntities, state, []);
  }
  function updateOneMutably(update, state) {
    return updateManyMutably([update], state);
  }
  function updateManyMutably(updates, state) {
    let appliedUpdates = false;
    let replacedIds = false;
    for (let update of updates) {
      const entity = state.entities[update.id];
      if (!entity) {
        continue;
      }
      appliedUpdates = true;
      Object.assign(entity, update.changes);
      const newId = selectId(entity);
      if (update.id !== newId) {
        replacedIds = true;
        delete state.entities[update.id];
        const oldIndex = state.ids.indexOf(update.id);
        state.ids[oldIndex] = newId;
        state.entities[newId] = entity;
      }
    }
    if (appliedUpdates) {
      mergeFunction(state, [], appliedUpdates, replacedIds);
    }
  }
  function upsertOneMutably(entity, state) {
    return upsertManyMutably([entity], state);
  }
  function upsertManyMutably(newEntities, state) {
    const [added, updated, existingIdsArray] = splitAddedUpdatedEntities(newEntities, selectId, state);
    if (added.length) {
      addManyMutably(added, state, existingIdsArray);
    }
    if (updated.length) {
      updateManyMutably(updated, state);
    }
  }
  function areArraysEqual(a, b) {
    if (a.length !== b.length) {
      return false;
    }
    for (let i = 0; i < a.length; i++) {
      if (a[i] === b[i]) {
        continue;
      }
      return false;
    }
    return true;
  }
  const mergeFunction = (state, addedItems, appliedUpdates, replacedIds) => {
    const currentEntities = getCurrent(state.entities);
    const currentIds = getCurrent(state.ids);
    const stateEntities = state.entities;
    let ids = currentIds;
    if (replacedIds) {
      ids = new Set(currentIds);
    }
    let sortedEntities = [];
    for (const id of ids) {
      const entity = currentEntities[id];
      if (entity) {
        sortedEntities.push(entity);
      }
    }
    const wasPreviouslyEmpty = sortedEntities.length === 0;
    for (const item of addedItems) {
      stateEntities[selectId(item)] = item;
      if (!wasPreviouslyEmpty) {
        insert(sortedEntities, item, comparer);
      }
    }
    if (wasPreviouslyEmpty) {
      sortedEntities = addedItems.slice().sort(comparer);
    } else if (appliedUpdates) {
      sortedEntities.sort(comparer);
    }
    const newSortedIds = sortedEntities.map(selectId);
    if (!areArraysEqual(currentIds, newSortedIds)) {
      state.ids = newSortedIds;
    }
  };
  return {
    removeOne,
    removeMany,
    removeAll,
    addOne: createStateOperator(addOneMutably),
    updateOne: createStateOperator(updateOneMutably),
    upsertOne: createStateOperator(upsertOneMutably),
    setOne: createStateOperator(setOneMutably),
    setMany: createStateOperator(setManyMutably),
    setAll: createStateOperator(setAllMutably),
    addMany: createStateOperator(addManyMutably),
    updateMany: createStateOperator(updateManyMutably),
    upsertMany: createStateOperator(upsertManyMutably)
  };
}

// src/entities/create_adapter.ts
function createEntityAdapter(options = {}) {
  const {
    selectId,
    sortComparer
  } = {
    sortComparer: false,
    selectId: (instance) => instance.id,
    ...options
  };
  const stateAdapter = sortComparer ? createSortedStateAdapter(selectId, sortComparer) : createUnsortedStateAdapter(selectId);
  const stateFactory = createInitialStateFactory(stateAdapter);
  const selectorsFactory = createSelectorsFactory();
  return {
    selectId,
    sortComparer,
    ...stateFactory,
    ...selectorsFactory,
    ...stateAdapter
  };
}

// src/listenerMiddleware/exceptions.ts
var task = "task";
var listener = "listener";
var completed = "completed";
var cancelled = "cancelled";
var taskCancelled = `task-${cancelled}`;
var taskCompleted = `task-${completed}`;
var listenerCancelled = `${listener}-${cancelled}`;
var listenerCompleted = `${listener}-${completed}`;
var TaskAbortError = class {
  constructor(code) {
    this.code = code;
    this.message = `${task} ${cancelled} (reason: ${code})`;
  }
  name = "TaskAbortError";
  message;
};

// src/listenerMiddleware/utils.ts
var assertFunction = (func, expected) => {
  if (typeof func !== "function") {
    throw new TypeError( true ? redux_toolkit_modern_formatProdErrorMessage(32) : 0);
  }
};
var noop2 = () => {
};
var catchRejection = (promise, onError = noop2) => {
  promise.catch(onError);
  return promise;
};
var addAbortSignalListener = (abortSignal, callback) => {
  abortSignal.addEventListener("abort", callback, {
    once: true
  });
  return () => abortSignal.removeEventListener("abort", callback);
};

// src/listenerMiddleware/task.ts
var validateActive = (signal) => {
  if (signal.aborted) {
    throw new TaskAbortError(signal.reason);
  }
};
function raceWithSignal(signal, promise) {
  let cleanup = noop2;
  return new Promise((resolve, reject) => {
    const notifyRejection = () => reject(new TaskAbortError(signal.reason));
    if (signal.aborted) {
      notifyRejection();
      return;
    }
    cleanup = addAbortSignalListener(signal, notifyRejection);
    promise.finally(() => cleanup()).then(resolve, reject);
  }).finally(() => {
    cleanup = noop2;
  });
}
var runTask = async (task2, cleanUp) => {
  try {
    await Promise.resolve();
    const value = await task2();
    return {
      status: "ok",
      value
    };
  } catch (error) {
    return {
      status: error instanceof TaskAbortError ? "cancelled" : "rejected",
      error
    };
  } finally {
    cleanUp?.();
  }
};
var createPause = (signal) => {
  return (promise) => {
    return catchRejection(raceWithSignal(signal, promise).then((output) => {
      validateActive(signal);
      return output;
    }));
  };
};
var createDelay = (signal) => {
  const pause = createPause(signal);
  return (timeoutMs) => {
    return pause(new Promise((resolve) => setTimeout(resolve, timeoutMs)));
  };
};

// src/listenerMiddleware/index.ts
var {
  assign: redux_toolkit_modern_assign
} = Object;
var INTERNAL_NIL_TOKEN = {};
var alm = "listenerMiddleware";
var createFork = (parentAbortSignal, parentBlockingPromises) => {
  const linkControllers = (controller) => addAbortSignalListener(parentAbortSignal, () => controller.abort(parentAbortSignal.reason));
  return (taskExecutor, opts) => {
    assertFunction(taskExecutor, "taskExecutor");
    const childAbortController = new AbortController();
    linkControllers(childAbortController);
    const result = runTask(async () => {
      validateActive(parentAbortSignal);
      validateActive(childAbortController.signal);
      const result2 = await taskExecutor({
        pause: createPause(childAbortController.signal),
        delay: createDelay(childAbortController.signal),
        signal: childAbortController.signal
      });
      validateActive(childAbortController.signal);
      return result2;
    }, () => childAbortController.abort(taskCompleted));
    if (opts?.autoJoin) {
      parentBlockingPromises.push(result.catch(noop2));
    }
    return {
      result: createPause(parentAbortSignal)(result),
      cancel() {
        childAbortController.abort(taskCancelled);
      }
    };
  };
};
var createTakePattern = (startListening, signal) => {
  const take = async (predicate, timeout) => {
    validateActive(signal);
    let unsubscribe = () => {
    };
    const tuplePromise = new Promise((resolve, reject) => {
      let stopListening = startListening({
        predicate,
        effect: (action, listenerApi) => {
          listenerApi.unsubscribe();
          resolve([action, listenerApi.getState(), listenerApi.getOriginalState()]);
        }
      });
      unsubscribe = () => {
        stopListening();
        reject();
      };
    });
    const promises = [tuplePromise];
    if (timeout != null) {
      promises.push(new Promise((resolve) => setTimeout(resolve, timeout, null)));
    }
    try {
      const output = await raceWithSignal(signal, Promise.race(promises));
      validateActive(signal);
      return output;
    } finally {
      unsubscribe();
    }
  };
  return (predicate, timeout) => catchRejection(take(predicate, timeout));
};
var getListenerEntryPropsFrom = (options) => {
  let {
    type,
    actionCreator,
    matcher,
    predicate,
    effect
  } = options;
  if (type) {
    predicate = createAction(type).match;
  } else if (actionCreator) {
    type = actionCreator.type;
    predicate = actionCreator.match;
  } else if (matcher) {
    predicate = matcher;
  } else if (predicate) {
  } else {
    throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(21) : 0);
  }
  assertFunction(effect, "options.listener");
  return {
    predicate,
    type,
    effect
  };
};
var createListenerEntry = /* @__PURE__ */ redux_toolkit_modern_assign((options) => {
  const {
    type,
    predicate,
    effect
  } = getListenerEntryPropsFrom(options);
  const entry = {
    id: nanoid(),
    effect,
    type,
    predicate,
    pending: /* @__PURE__ */ new Set(),
    unsubscribe: () => {
      throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(22) : 0);
    }
  };
  return entry;
}, {
  withTypes: () => createListenerEntry
});
var findListenerEntry = (listenerMap, options) => {
  const {
    type,
    effect,
    predicate
  } = getListenerEntryPropsFrom(options);
  return Array.from(listenerMap.values()).find((entry) => {
    const matchPredicateOrType = typeof type === "string" ? entry.type === type : entry.predicate === predicate;
    return matchPredicateOrType && entry.effect === effect;
  });
};
var cancelActiveListeners = (entry) => {
  entry.pending.forEach((controller) => {
    controller.abort(listenerCancelled);
  });
};
var createClearListenerMiddleware = (listenerMap, executingListeners) => {
  return () => {
    for (const listener2 of executingListeners.keys()) {
      cancelActiveListeners(listener2);
    }
    listenerMap.clear();
  };
};
var safelyNotifyError = (errorHandler, errorToNotify, errorInfo) => {
  try {
    errorHandler(errorToNotify, errorInfo);
  } catch (errorHandlerError) {
    setTimeout(() => {
      throw errorHandlerError;
    }, 0);
  }
};
var addListener = /* @__PURE__ */ redux_toolkit_modern_assign(/* @__PURE__ */ createAction(`${alm}/add`), {
  withTypes: () => addListener
});
var clearAllListeners = /* @__PURE__ */ createAction(`${alm}/removeAll`);
var removeListener = /* @__PURE__ */ redux_toolkit_modern_assign(/* @__PURE__ */ createAction(`${alm}/remove`), {
  withTypes: () => removeListener
});
var defaultErrorHandler = (...args) => {
  console.error(`${alm}/error`, ...args);
};
var createListenerMiddleware = (middlewareOptions = {}) => {
  const listenerMap = /* @__PURE__ */ new Map();
  const executingListeners = /* @__PURE__ */ new Map();
  const trackExecutingListener = (entry) => {
    const count = executingListeners.get(entry) ?? 0;
    executingListeners.set(entry, count + 1);
  };
  const untrackExecutingListener = (entry) => {
    const count = executingListeners.get(entry) ?? 1;
    if (count === 1) {
      executingListeners.delete(entry);
    } else {
      executingListeners.set(entry, count - 1);
    }
  };
  const {
    extra,
    onError = defaultErrorHandler
  } = middlewareOptions;
  assertFunction(onError, "onError");
  const insertEntry = (entry) => {
    entry.unsubscribe = () => listenerMap.delete(entry.id);
    listenerMap.set(entry.id, entry);
    return (cancelOptions) => {
      entry.unsubscribe();
      if (cancelOptions?.cancelActive) {
        cancelActiveListeners(entry);
      }
    };
  };
  const startListening = (options) => {
    const entry = findListenerEntry(listenerMap, options) ?? createListenerEntry(options);
    return insertEntry(entry);
  };
  redux_toolkit_modern_assign(startListening, {
    withTypes: () => startListening
  });
  const stopListening = (options) => {
    const entry = findListenerEntry(listenerMap, options);
    if (entry) {
      entry.unsubscribe();
      if (options.cancelActive) {
        cancelActiveListeners(entry);
      }
    }
    return !!entry;
  };
  redux_toolkit_modern_assign(stopListening, {
    withTypes: () => stopListening
  });
  const notifyListener = async (entry, action, api, getOriginalState) => {
    const internalTaskController = new AbortController();
    const take = createTakePattern(startListening, internalTaskController.signal);
    const autoJoinPromises = [];
    try {
      entry.pending.add(internalTaskController);
      trackExecutingListener(entry);
      await Promise.resolve(entry.effect(
        action,
        // Use assign() rather than ... to avoid extra helper functions added to bundle
        redux_toolkit_modern_assign({}, api, {
          getOriginalState,
          condition: (predicate, timeout) => take(predicate, timeout).then(Boolean),
          take,
          delay: createDelay(internalTaskController.signal),
          pause: createPause(internalTaskController.signal),
          extra,
          signal: internalTaskController.signal,
          fork: createFork(internalTaskController.signal, autoJoinPromises),
          unsubscribe: entry.unsubscribe,
          subscribe: () => {
            listenerMap.set(entry.id, entry);
          },
          cancelActiveListeners: () => {
            entry.pending.forEach((controller, _, set) => {
              if (controller !== internalTaskController) {
                controller.abort(listenerCancelled);
                set.delete(controller);
              }
            });
          },
          cancel: () => {
            internalTaskController.abort(listenerCancelled);
            entry.pending.delete(internalTaskController);
          },
          throwIfCancelled: () => {
            validateActive(internalTaskController.signal);
          }
        })
      ));
    } catch (listenerError) {
      if (!(listenerError instanceof TaskAbortError)) {
        safelyNotifyError(onError, listenerError, {
          raisedBy: "effect"
        });
      }
    } finally {
      await Promise.all(autoJoinPromises);
      internalTaskController.abort(listenerCompleted);
      untrackExecutingListener(entry);
      entry.pending.delete(internalTaskController);
    }
  };
  const clearListenerMiddleware = createClearListenerMiddleware(listenerMap, executingListeners);
  const middleware = (api) => (next) => (action) => {
    if (!isAction(action)) {
      return next(action);
    }
    if (addListener.match(action)) {
      return startListening(action.payload);
    }
    if (clearAllListeners.match(action)) {
      clearListenerMiddleware();
      return;
    }
    if (removeListener.match(action)) {
      return stopListening(action.payload);
    }
    let originalState = api.getState();
    const getOriginalState = () => {
      if (originalState === INTERNAL_NIL_TOKEN) {
        throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(23) : 0);
      }
      return originalState;
    };
    let result;
    try {
      result = next(action);
      if (listenerMap.size > 0) {
        const currentState = api.getState();
        const listenerEntries = Array.from(listenerMap.values());
        for (const entry of listenerEntries) {
          let runListener = false;
          try {
            runListener = entry.predicate(action, currentState, originalState);
          } catch (predicateError) {
            runListener = false;
            safelyNotifyError(onError, predicateError, {
              raisedBy: "predicate"
            });
          }
          if (!runListener) {
            continue;
          }
          notifyListener(entry, action, api, getOriginalState);
        }
      }
    } finally {
      originalState = INTERNAL_NIL_TOKEN;
    }
    return result;
  };
  return {
    middleware,
    startListening,
    stopListening,
    clearListeners: clearListenerMiddleware
  };
};

// src/dynamicMiddleware/index.ts
var createMiddlewareEntry = (middleware) => ({
  middleware,
  applied: /* @__PURE__ */ new Map()
});
var matchInstance = (instanceId) => (action) => action?.meta?.instanceId === instanceId;
var createDynamicMiddleware = () => {
  const instanceId = nanoid();
  const middlewareMap = /* @__PURE__ */ new Map();
  const withMiddleware = Object.assign(createAction("dynamicMiddleware/add", (...middlewares) => ({
    payload: middlewares,
    meta: {
      instanceId
    }
  })), {
    withTypes: () => withMiddleware
  });
  const addMiddleware = Object.assign(function addMiddleware2(...middlewares) {
    middlewares.forEach((middleware2) => {
      getOrInsertComputed(middlewareMap, middleware2, createMiddlewareEntry);
    });
  }, {
    withTypes: () => addMiddleware
  });
  const getFinalMiddleware = (api) => {
    const appliedMiddleware = Array.from(middlewareMap.values()).map((entry) => getOrInsertComputed(entry.applied, api, entry.middleware));
    return compose(...appliedMiddleware);
  };
  const isWithMiddleware = isAllOf(withMiddleware, matchInstance(instanceId));
  const middleware = (api) => (next) => (action) => {
    if (isWithMiddleware(action)) {
      addMiddleware(...action.payload);
      return api.dispatch;
    }
    return getFinalMiddleware(api)(next)(action);
  };
  return {
    middleware,
    addMiddleware,
    withMiddleware,
    instanceId
  };
};

// src/combineSlices.ts

var isSliceLike = (maybeSliceLike) => "reducerPath" in maybeSliceLike && typeof maybeSliceLike.reducerPath === "string";
var getReducers = (slices) => slices.flatMap((sliceOrMap) => isSliceLike(sliceOrMap) ? [[sliceOrMap.reducerPath, sliceOrMap.reducer]] : Object.entries(sliceOrMap));
var ORIGINAL_STATE = Symbol.for("rtk-state-proxy-original");
var isStateProxy = (value) => !!value && !!value[ORIGINAL_STATE];
var stateProxyMap = /* @__PURE__ */ new WeakMap();
var createStateProxy = (state, reducerMap, initialStateCache) => getOrInsertComputed(stateProxyMap, state, () => new Proxy(state, {
  get: (target, prop, receiver) => {
    if (prop === ORIGINAL_STATE) return target;
    const result = Reflect.get(target, prop, receiver);
    if (typeof result === "undefined") {
      const cached = initialStateCache[prop];
      if (typeof cached !== "undefined") return cached;
      const reducer = reducerMap[prop];
      if (reducer) {
        const reducerResult = reducer(void 0, {
          type: nanoid()
        });
        if (typeof reducerResult === "undefined") {
          throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(24) : 0);
        }
        initialStateCache[prop] = reducerResult;
        return reducerResult;
      }
    }
    return result;
  }
}));
var redux_toolkit_modern_original = (state) => {
  if (!isStateProxy(state)) {
    throw new Error( true ? redux_toolkit_modern_formatProdErrorMessage(25) : 0);
  }
  return state[ORIGINAL_STATE];
};
var emptyObject = {};
var noopReducer = (state = emptyObject) => state;
function combineSlices(...slices) {
  const reducerMap = Object.fromEntries(getReducers(slices));
  const getReducer = () => Object.keys(reducerMap).length ? combineReducers(reducerMap) : noopReducer;
  let reducer = getReducer();
  function combinedReducer(state, action) {
    return reducer(state, action);
  }
  combinedReducer.withLazyLoadedSlices = () => combinedReducer;
  const initialStateCache = {};
  const inject = (slice, config = {}) => {
    const {
      reducerPath,
      reducer: reducerToInject
    } = slice;
    const currentReducer = reducerMap[reducerPath];
    if (!config.overrideExisting && currentReducer && currentReducer !== reducerToInject) {
      if (typeof process !== "undefined" && "production" === "development") // removed by dead control flow
{}
      return combinedReducer;
    }
    if (config.overrideExisting && currentReducer !== reducerToInject) {
      delete initialStateCache[reducerPath];
    }
    reducerMap[reducerPath] = reducerToInject;
    reducer = getReducer();
    return combinedReducer;
  };
  const selector = Object.assign(function makeSelector(selectorFn, selectState) {
    return function selector2(state, ...args) {
      return selectorFn(createStateProxy(selectState ? selectState(state, ...args) : state, reducerMap, initialStateCache), ...args);
    };
  }, {
    original: redux_toolkit_modern_original
  });
  return Object.assign(combinedReducer, {
    inject,
    selector
  });
}

// src/formatProdErrorMessage.ts
function redux_toolkit_modern_formatProdErrorMessage(code) {
  return `Minified Redux Toolkit error #${code}; visit https://redux-toolkit.js.org/Errors?code=${code} for the full message or use the non-minified dev environment for full errors. `;
}

//# sourceMappingURL=redux-toolkit.modern.mjs.map
;// ./node_modules/recharts/es6/state/optionsSlice.js



/**
 * These chart options are decided internally, by Recharts,
 * and will not change during the lifetime of the chart.
 *
 * Changing these options can be done by swapping the root element
 * which will make a brand-new Redux store.
 *
 * If you want to store options that can be changed by the user,
 * use UpdatableChartOptions in rootPropsSlice.ts.
 */

var arrayTooltipSearcher = (data, strIndex) => {
  if (!strIndex) return undefined;
  if (!Array.isArray(data)) return undefined;
  var numIndex = Number.parseInt(strIndex, 10);
  if (isNan(numIndex)) {
    return undefined;
  }
  return data[numIndex];
};
var initialState = {
  chartName: '',
  tooltipPayloadSearcher: () => undefined,
  eventEmitter: undefined,
  defaultTooltipEventType: 'axis'
};
var optionsSlice = createSlice({
  name: 'options',
  initialState,
  reducers: {
    createEventEmitter: state => {
      if (state.eventEmitter == null) {
        state.eventEmitter = Symbol('rechartsEventEmitter');
      }
    }
  }
});
var optionsReducer = optionsSlice.reducer;
var {
  createEventEmitter
} = optionsSlice.actions;
;// ./node_modules/recharts/es6/util/resolveDefaultProps.js
function resolveDefaultProps_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function resolveDefaultProps_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? resolveDefaultProps_ownKeys(Object(t), !0).forEach(function (r) { resolveDefaultProps_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : resolveDefaultProps_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function resolveDefaultProps_defineProperty(e, r, t) { return (r = resolveDefaultProps_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function resolveDefaultProps_toPropertyKey(t) { var i = resolveDefaultProps_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function resolveDefaultProps_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * This function mimics the behavior of the `defaultProps` static property in React.
 * Functional components do not have a defaultProps property, so this function is useful to resolve default props.
 *
 * The common recommendation is to use ES6 destructuring with default values in the function signature,
 * but you need to be careful there and make sure you destructure all the individual properties
 * and not the whole object. See the test file for example.
 *
 * And because destructuring all properties one by one is a faff, and it's easy to miss one property,
 * this function exists.
 *
 * @param realProps - the props object passed to the component by the user
 * @param defaultProps - the default props object defined in the component by Recharts
 * @returns - the props object with all the default props resolved. All `undefined` values are replaced with the default value.
 */
function resolveDefaultProps(realProps, defaultProps) {
  /*
   * To avoid mutating the original `realProps` object passed to the function, create a shallow copy of it.
   * `resolvedProps` will be modified directly with the defaults.
   */
  var resolvedProps = resolveDefaultProps_objectSpread({}, realProps);
  /*
   * Since the function guarantees `D extends Partial<T>`, this assignment is safe.
   * It allows TypeScript to work with the well-defined `Partial<T>` type inside the loop,
   * making subsequent type inference (especially for `dp[key]`) much more straightforward for the compiler.
   * This is a key step to improve type safety *without* value assertions later.
   */
  var dp = defaultProps;
  /*
   * `Object.keys` doesn't preserve strong key types - it always returns Array<string>.
   * However, due to the `D extends Partial<T>` constraint,
   * we know these keys *must* also be valid keys of `T`.
   * This assertion informs TypeScript of this relationship, avoiding type errors when using `key` to index `acc` (type T).
   *
   * Type assertions are not sound but in this case it's necessary
   * as `Object.keys` does not do what we want it to do.
   */
  var keys = Object.keys(defaultProps);
  var withDefaults = keys.reduce((acc, key) => {
    if (acc[key] === undefined && dp[key] !== undefined) {
      acc[key] = dp[key];
    }
    return acc;
  }, resolvedProps);
  /*
   * And again type assertions are not safe but here we have done the runtime work
   * so let's bypass the lack of static type safety and tell the compiler what happened.
   */
  return withDefaults;
}

/**
 * Helper type to extract the keys of T that are required.
 * It iterates through each key K in T. If Pick<T, K> cannot be assigned an empty object {},
 * it means K is required, so we keep K; otherwise, we discard it (never).
 * [keyof T] at the end creates a union of the kept keys.
 */

/**
 * Helper type to extract the keys of T that are optional.
 * It iterates through each key K in T. If Pick<T, K> can be assigned an empty object {},
 * it means K is optional (or potentially missing), so we keep K; otherwise, we discard it (never).
 * [keyof T] at the end creates a union of the kept keys.
 */

/**
 * Helper type to ensure keys of D exist in T.
 * For each key K in D, if K is also a key of T, keep the type D[K].
 * If K is NOT a key of T, map it to type `never`.
 * An object cannot have a property of type `never`, effectively disallowing extra keys.
 */

/**
 * This type will take a source type `Props` and a default type `Defaults` and will return a new type
 * where all properties that are optional in `Props` but required in `Defaults` are made required in the result.
 * Properties that are required in `Props` and optional in `Defaults` will remain required.
 * Properties that are optional in both `Props` and `Defaults` will remain optional.
 *
 * This is useful for creating a type that represents the resolved props of a component with default props.
 */
// EXTERNAL MODULE: ./node_modules/use-sync-external-store/with-selector.js
var with_selector = __webpack_require__(8418);
;// ./node_modules/react-redux/dist/react-redux.mjs
// src/utils/react.ts


// src/utils/react-is.ts
var IS_REACT_19 = /* @__PURE__ */ external_React_.version.startsWith("19");
var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for(
  IS_REACT_19 ? "react.transitional.element" : "react.element"
);
var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
var REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode");
var REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler");
var REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer");
var REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context");
var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
var REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense");
var REACT_SUSPENSE_LIST_TYPE = /* @__PURE__ */ Symbol.for(
  "react.suspense_list"
);
var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
var REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
var REACT_OFFSCREEN_TYPE = /* @__PURE__ */ (/* unused pure expression or super */ null && (Symbol.for("react.offscreen")));
var REACT_CLIENT_REFERENCE = /* @__PURE__ */ (/* unused pure expression or super */ null && (Symbol.for(
  "react.client.reference"
)));
var ForwardRef = REACT_FORWARD_REF_TYPE;
var Memo = REACT_MEMO_TYPE;
function isValidElementType(type) {
  return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || type === REACT_OFFSCREEN_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_CONSUMER_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_CLIENT_REFERENCE || type.getModuleId !== void 0) ? true : false;
}
function typeOf(object) {
  if (typeof object === "object" && object !== null) {
    const { $$typeof } = object;
    switch ($$typeof) {
      case REACT_ELEMENT_TYPE:
        switch (object = object.type, object) {
          case REACT_FRAGMENT_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_SUSPENSE_TYPE:
          case REACT_SUSPENSE_LIST_TYPE:
            return object;
          default:
            switch (object = object && object.$$typeof, object) {
              case REACT_CONTEXT_TYPE:
              case REACT_FORWARD_REF_TYPE:
              case REACT_LAZY_TYPE:
              case REACT_MEMO_TYPE:
                return object;
              case REACT_CONSUMER_TYPE:
                return object;
              default:
                return $$typeof;
            }
        }
      case REACT_PORTAL_TYPE:
        return $$typeof;
    }
  }
}
function isContextConsumer(object) {
  return IS_REACT_19 ? typeOf(object) === REACT_CONSUMER_TYPE : typeOf(object) === REACT_CONTEXT_TYPE;
}
function isMemo(object) {
  return typeOf(object) === REACT_MEMO_TYPE;
}

// src/utils/warning.ts
function react_redux_warning(message) {
  if (typeof console !== "undefined" && typeof console.error === "function") {
    console.error(message);
  }
  try {
    throw new Error(message);
  } catch (e) {
  }
}

// src/connect/verifySubselectors.ts
function verify(selector, methodName) {
  if (!selector) {
    throw new Error(`Unexpected value for ${methodName} in connect.`);
  } else if (methodName === "mapStateToProps" || methodName === "mapDispatchToProps") {
    if (!Object.prototype.hasOwnProperty.call(selector, "dependsOnOwnProps")) {
      react_redux_warning(
        `The selector for ${methodName} of connect did not specify a value for dependsOnOwnProps.`
      );
    }
  }
}
function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps) {
  verify(mapStateToProps, "mapStateToProps");
  verify(mapDispatchToProps, "mapDispatchToProps");
  verify(mergeProps, "mergeProps");
}

// src/connect/selectorFactory.ts
function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, {
  areStatesEqual,
  areOwnPropsEqual,
  areStatePropsEqual
}) {
  let hasRunAtLeastOnce = false;
  let state;
  let ownProps;
  let stateProps;
  let dispatchProps;
  let mergedProps;
  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }
  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps)
      dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps)
      stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps)
      dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleNewState() {
    const nextStateProps = mapStateToProps(state, ownProps);
    const statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;
    if (statePropsChanged)
      mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }
  function handleSubsequentCalls(nextState, nextOwnProps) {
    const propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    const stateChanged = !areStatesEqual(
      nextState,
      state,
      nextOwnProps,
      ownProps
    );
    state = nextState;
    ownProps = nextOwnProps;
    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }
  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
}
function finalPropsSelectorFactory(dispatch, {
  initMapStateToProps,
  initMapDispatchToProps,
  initMergeProps,
  ...options
}) {
  const mapStateToProps = initMapStateToProps(dispatch, options);
  const mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  const mergeProps = initMergeProps(dispatch, options);
  if (false) // removed by dead control flow
{}
  return pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}

// src/utils/bindActionCreators.ts
function react_redux_bindActionCreators(actionCreators, dispatch) {
  const boundActionCreators = {};
  for (const key in actionCreators) {
    const actionCreator = actionCreators[key];
    if (typeof actionCreator === "function") {
      boundActionCreators[key] = (...args) => dispatch(actionCreator(...args));
    }
  }
  return boundActionCreators;
}

// src/utils/isPlainObject.ts
function react_redux_isPlainObject(obj) {
  if (typeof obj !== "object" || obj === null) return false;
  const proto = Object.getPrototypeOf(obj);
  if (proto === null) return true;
  let baseProto = proto;
  while (Object.getPrototypeOf(baseProto) !== null) {
    baseProto = Object.getPrototypeOf(baseProto);
  }
  return proto === baseProto;
}

// src/utils/verifyPlainObject.ts
function verifyPlainObject(value, displayName, methodName) {
  if (!react_redux_isPlainObject(value)) {
    react_redux_warning(
      `${methodName}() in ${displayName} must return a plain object. Instead received ${value}.`
    );
  }
}

// src/connect/wrapMapToProps.ts
function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch) {
    const constant = getConstant(dispatch);
    function constantSelector() {
      return constant;
    }
    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
}
function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
}
function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, { displayName }) {
    const proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch, void 0);
    };
    proxy.dependsOnOwnProps = true;
    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      let props = proxy(stateOrDispatch, ownProps);
      if (typeof props === "function") {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }
      if (false)
        // removed by dead control flow
{}
      return props;
    };
    return proxy;
  };
}

// src/connect/invalidArgFactory.ts
function createInvalidArgFactory(arg, name) {
  return (dispatch, options) => {
    throw new Error(
      `Invalid value of type ${typeof arg} for ${name} argument when connecting component ${options.wrappedComponentName}.`
    );
  };
}

// src/connect/mapDispatchToProps.ts
function mapDispatchToPropsFactory(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === "object" ? wrapMapToPropsConstant(
    (dispatch) => (
      // @ts-ignore
      react_redux_bindActionCreators(mapDispatchToProps, dispatch)
    )
  ) : !mapDispatchToProps ? wrapMapToPropsConstant((dispatch) => ({
    dispatch
  })) : typeof mapDispatchToProps === "function" ? (
    // @ts-ignore
    wrapMapToPropsFunc(mapDispatchToProps, "mapDispatchToProps")
  ) : createInvalidArgFactory(mapDispatchToProps, "mapDispatchToProps");
}

// src/connect/mapStateToProps.ts
function mapStateToPropsFactory(mapStateToProps) {
  return !mapStateToProps ? wrapMapToPropsConstant(() => ({})) : typeof mapStateToProps === "function" ? (
    // @ts-ignore
    wrapMapToPropsFunc(mapStateToProps, "mapStateToProps")
  ) : createInvalidArgFactory(mapStateToProps, "mapStateToProps");
}

// src/connect/mergeProps.ts
function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return { ...ownProps, ...stateProps, ...dispatchProps };
}
function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, { displayName, areMergedPropsEqual }) {
    let hasRunOnce = false;
    let mergedProps;
    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      const nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);
      if (hasRunOnce) {
        if (!areMergedPropsEqual(nextMergedProps, mergedProps))
          mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;
        if (false)
          // removed by dead control flow
{}
      }
      return mergedProps;
    };
  };
}
function mergePropsFactory(mergeProps) {
  return !mergeProps ? () => defaultMergeProps : typeof mergeProps === "function" ? wrapMergePropsFunc(mergeProps) : createInvalidArgFactory(mergeProps, "mergeProps");
}

// src/utils/batch.ts
function defaultNoopBatch(callback) {
  callback();
}

// src/utils/Subscription.ts
function createListenerCollection() {
  let first = null;
  let last = null;
  return {
    clear() {
      first = null;
      last = null;
    },
    notify() {
      defaultNoopBatch(() => {
        let listener = first;
        while (listener) {
          listener.callback();
          listener = listener.next;
        }
      });
    },
    get() {
      const listeners = [];
      let listener = first;
      while (listener) {
        listeners.push(listener);
        listener = listener.next;
      }
      return listeners;
    },
    subscribe(callback) {
      let isSubscribed = true;
      const listener = last = {
        callback,
        next: null,
        prev: last
      };
      if (listener.prev) {
        listener.prev.next = listener;
      } else {
        first = listener;
      }
      return function unsubscribe() {
        if (!isSubscribed || first === null) return;
        isSubscribed = false;
        if (listener.next) {
          listener.next.prev = listener.prev;
        } else {
          last = listener.prev;
        }
        if (listener.prev) {
          listener.prev.next = listener.next;
        } else {
          first = listener.next;
        }
      };
    }
  };
}
var nullListeners = {
  notify() {
  },
  get: () => []
};
function createSubscription(store, parentSub) {
  let unsubscribe;
  let listeners = nullListeners;
  let subscriptionsAmount = 0;
  let selfSubscribed = false;
  function addNestedSub(listener) {
    trySubscribe();
    const cleanupListener = listeners.subscribe(listener);
    let removed = false;
    return () => {
      if (!removed) {
        removed = true;
        cleanupListener();
        tryUnsubscribe();
      }
    };
  }
  function notifyNestedSubs() {
    listeners.notify();
  }
  function handleChangeWrapper() {
    if (subscription.onStateChange) {
      subscription.onStateChange();
    }
  }
  function isSubscribed() {
    return selfSubscribed;
  }
  function trySubscribe() {
    subscriptionsAmount++;
    if (!unsubscribe) {
      unsubscribe = parentSub ? parentSub.addNestedSub(handleChangeWrapper) : store.subscribe(handleChangeWrapper);
      listeners = createListenerCollection();
    }
  }
  function tryUnsubscribe() {
    subscriptionsAmount--;
    if (unsubscribe && subscriptionsAmount === 0) {
      unsubscribe();
      unsubscribe = void 0;
      listeners.clear();
      listeners = nullListeners;
    }
  }
  function trySubscribeSelf() {
    if (!selfSubscribed) {
      selfSubscribed = true;
      trySubscribe();
    }
  }
  function tryUnsubscribeSelf() {
    if (selfSubscribed) {
      selfSubscribed = false;
      tryUnsubscribe();
    }
  }
  const subscription = {
    addNestedSub,
    notifyNestedSubs,
    handleChangeWrapper,
    isSubscribed,
    trySubscribe: trySubscribeSelf,
    tryUnsubscribe: tryUnsubscribeSelf,
    getListeners: () => listeners
  };
  return subscription;
}

// src/utils/useIsomorphicLayoutEffect.ts
var canUseDOM = () => !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
var isDOM = /* @__PURE__ */ canUseDOM();
var isRunningInReactNative = () => typeof navigator !== "undefined" && navigator.product === "ReactNative";
var isReactNative = /* @__PURE__ */ isRunningInReactNative();
var getUseIsomorphicLayoutEffect = () => isDOM || isReactNative ? external_React_.useLayoutEffect : external_React_.useEffect;
var useIsomorphicLayoutEffect = /* @__PURE__ */ getUseIsomorphicLayoutEffect();

// src/utils/shallowEqual.ts
function react_redux_is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
function shallowEqual(objA, objB) {
  if (react_redux_is(objA, objB)) return true;
  if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
    return false;
  }
  const keysA = Object.keys(objA);
  const keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) return false;
  for (let i = 0; i < keysA.length; i++) {
    if (!Object.prototype.hasOwnProperty.call(objB, keysA[i]) || !react_redux_is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }
  return true;
}

// src/utils/hoistStatics.ts
var REACT_STATICS = {
  childContextTypes: true,
  contextType: true,
  contextTypes: true,
  defaultProps: true,
  displayName: true,
  getDefaultProps: true,
  getDerivedStateFromError: true,
  getDerivedStateFromProps: true,
  mixins: true,
  propTypes: true,
  type: true
};
var KNOWN_STATICS = {
  name: true,
  length: true,
  prototype: true,
  caller: true,
  callee: true,
  arguments: true,
  arity: true
};
var FORWARD_REF_STATICS = {
  $$typeof: true,
  render: true,
  defaultProps: true,
  displayName: true,
  propTypes: true
};
var MEMO_STATICS = {
  $$typeof: true,
  compare: true,
  defaultProps: true,
  displayName: true,
  propTypes: true,
  type: true
};
var TYPE_STATICS = {
  [ForwardRef]: FORWARD_REF_STATICS,
  [Memo]: MEMO_STATICS
};
function getStatics(component) {
  if (isMemo(component)) {
    return MEMO_STATICS;
  }
  return TYPE_STATICS[component["$$typeof"]] || REACT_STATICS;
}
var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var react_redux_getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;
function hoistNonReactStatics(targetComponent, sourceComponent) {
  if (typeof sourceComponent !== "string") {
    if (objectPrototype) {
      const inheritedComponent = react_redux_getPrototypeOf(sourceComponent);
      if (inheritedComponent && inheritedComponent !== objectPrototype) {
        hoistNonReactStatics(targetComponent, inheritedComponent);
      }
    }
    let keys = getOwnPropertyNames(sourceComponent);
    if (getOwnPropertySymbols) {
      keys = keys.concat(getOwnPropertySymbols(sourceComponent));
    }
    const targetStatics = getStatics(targetComponent);
    const sourceStatics = getStatics(sourceComponent);
    for (let i = 0; i < keys.length; ++i) {
      const key = keys[i];
      if (!KNOWN_STATICS[key] && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
        const descriptor = getOwnPropertyDescriptor(sourceComponent, key);
        try {
          defineProperty(targetComponent, key, descriptor);
        } catch (e) {
        }
      }
    }
  }
  return targetComponent;
}

// src/components/Context.ts
var ContextKey = /* @__PURE__ */ Symbol.for(`react-redux-context`);
var gT = typeof globalThis !== "undefined" ? globalThis : (
  /* fall back to a per-module scope (pre-8.1 behaviour) if `globalThis` is not available */
  {}
);
function getContext() {
  if (!external_React_.createContext) return {};
  const contextMap = gT[ContextKey] ??= /* @__PURE__ */ new Map();
  let realContext = contextMap.get(external_React_.createContext);
  if (!realContext) {
    realContext = external_React_.createContext(
      null
    );
    if (false) // removed by dead control flow
{}
    contextMap.set(external_React_.createContext, realContext);
  }
  return realContext;
}
var ReactReduxContext = /* @__PURE__ */ getContext();

// src/components/connect.tsx
var NO_SUBSCRIPTION_ARRAY = [null, null];
var stringifyComponent = (Comp) => {
  try {
    return JSON.stringify(Comp);
  } catch (err) {
    return String(Comp);
  }
};
function useIsomorphicLayoutEffectWithArgs(effectFunc, effectArgs, dependencies) {
  useIsomorphicLayoutEffect(() => effectFunc(...effectArgs), dependencies);
}
function captureWrapperProps(lastWrapperProps, lastChildProps, renderIsScheduled, wrapperProps, childPropsFromStoreUpdate, notifyNestedSubs) {
  lastWrapperProps.current = wrapperProps;
  renderIsScheduled.current = false;
  if (childPropsFromStoreUpdate.current) {
    childPropsFromStoreUpdate.current = null;
    notifyNestedSubs();
  }
}
function subscribeUpdates(shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, isMounted, childPropsFromStoreUpdate, notifyNestedSubs, additionalSubscribeListener) {
  if (!shouldHandleStateChanges) return () => {
  };
  let didUnsubscribe = false;
  let lastThrownError = null;
  const checkForUpdates = () => {
    if (didUnsubscribe || !isMounted.current) {
      return;
    }
    const latestStoreState = store.getState();
    let newChildProps, error;
    try {
      newChildProps = childPropsSelector(
        latestStoreState,
        lastWrapperProps.current
      );
    } catch (e) {
      error = e;
      lastThrownError = e;
    }
    if (!error) {
      lastThrownError = null;
    }
    if (newChildProps === lastChildProps.current) {
      if (!renderIsScheduled.current) {
        notifyNestedSubs();
      }
    } else {
      lastChildProps.current = newChildProps;
      childPropsFromStoreUpdate.current = newChildProps;
      renderIsScheduled.current = true;
      additionalSubscribeListener();
    }
  };
  subscription.onStateChange = checkForUpdates;
  subscription.trySubscribe();
  checkForUpdates();
  const unsubscribeWrapper = () => {
    didUnsubscribe = true;
    subscription.tryUnsubscribe();
    subscription.onStateChange = null;
    if (lastThrownError) {
      throw lastThrownError;
    }
  };
  return unsubscribeWrapper;
}
function strictEqual(a, b) {
  return a === b;
}
var hasWarnedAboutDeprecatedPureOption = false;
function connect(mapStateToProps, mapDispatchToProps, mergeProps, {
  // The `pure` option has been removed, so TS doesn't like us destructuring this to check its existence.
  // @ts-ignore
  pure,
  areStatesEqual = strictEqual,
  areOwnPropsEqual = shallowEqual,
  areStatePropsEqual = shallowEqual,
  areMergedPropsEqual = shallowEqual,
  // use React's forwardRef to expose a ref of the wrapped component
  forwardRef = false,
  // the context consumer to use
  context = ReactReduxContext
} = {}) {
  if (false) // removed by dead control flow
{}
  const Context = context;
  const initMapStateToProps = mapStateToPropsFactory(mapStateToProps);
  const initMapDispatchToProps = mapDispatchToPropsFactory(mapDispatchToProps);
  const initMergeProps = mergePropsFactory(mergeProps);
  const shouldHandleStateChanges = Boolean(mapStateToProps);
  const wrapWithConnect = (WrappedComponent) => {
    if (false) // removed by dead control flow
{}
    const wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || "Component";
    const displayName = `Connect(${wrappedComponentName})`;
    const selectorFactoryOptions = {
      shouldHandleStateChanges,
      displayName,
      wrappedComponentName,
      WrappedComponent,
      // @ts-ignore
      initMapStateToProps,
      initMapDispatchToProps,
      initMergeProps,
      areStatesEqual,
      areStatePropsEqual,
      areOwnPropsEqual,
      areMergedPropsEqual
    };
    function ConnectFunction(props) {
      const [propsContext, reactReduxForwardedRef, wrapperProps] = external_React_.useMemo(() => {
        const { reactReduxForwardedRef: reactReduxForwardedRef2, ...wrapperProps2 } = props;
        return [props.context, reactReduxForwardedRef2, wrapperProps2];
      }, [props]);
      const ContextToUse = external_React_.useMemo(() => {
        let ResultContext = Context;
        if (propsContext?.Consumer) {
          if (false) // removed by dead control flow
{}
        }
        return ResultContext;
      }, [propsContext, Context]);
      const contextValue = external_React_.useContext(ContextToUse);
      const didStoreComeFromProps = Boolean(props.store) && Boolean(props.store.getState) && Boolean(props.store.dispatch);
      const didStoreComeFromContext = Boolean(contextValue) && Boolean(contextValue.store);
      if (false) // removed by dead control flow
{}
      const store = didStoreComeFromProps ? props.store : contextValue.store;
      const getServerState = didStoreComeFromContext ? contextValue.getServerState : store.getState;
      const childPropsSelector = external_React_.useMemo(() => {
        return finalPropsSelectorFactory(store.dispatch, selectorFactoryOptions);
      }, [store]);
      const [subscription, notifyNestedSubs] = external_React_.useMemo(() => {
        if (!shouldHandleStateChanges) return NO_SUBSCRIPTION_ARRAY;
        const subscription2 = createSubscription(
          store,
          didStoreComeFromProps ? void 0 : contextValue.subscription
        );
        const notifyNestedSubs2 = subscription2.notifyNestedSubs.bind(subscription2);
        return [subscription2, notifyNestedSubs2];
      }, [store, didStoreComeFromProps, contextValue]);
      const overriddenContextValue = external_React_.useMemo(() => {
        if (didStoreComeFromProps) {
          return contextValue;
        }
        return {
          ...contextValue,
          subscription
        };
      }, [didStoreComeFromProps, contextValue, subscription]);
      const lastChildProps = external_React_.useRef(void 0);
      const lastWrapperProps = external_React_.useRef(wrapperProps);
      const childPropsFromStoreUpdate = external_React_.useRef(void 0);
      const renderIsScheduled = external_React_.useRef(false);
      const isMounted = external_React_.useRef(false);
      const latestSubscriptionCallbackError = external_React_.useRef(
        void 0
      );
      useIsomorphicLayoutEffect(() => {
        isMounted.current = true;
        return () => {
          isMounted.current = false;
        };
      }, []);
      const actualChildPropsSelector = external_React_.useMemo(() => {
        const selector = () => {
          if (childPropsFromStoreUpdate.current && wrapperProps === lastWrapperProps.current) {
            return childPropsFromStoreUpdate.current;
          }
          return childPropsSelector(store.getState(), wrapperProps);
        };
        return selector;
      }, [store, wrapperProps]);
      const subscribeForReact = external_React_.useMemo(() => {
        const subscribe = (reactListener) => {
          if (!subscription) {
            return () => {
            };
          }
          return subscribeUpdates(
            shouldHandleStateChanges,
            store,
            subscription,
            // @ts-ignore
            childPropsSelector,
            lastWrapperProps,
            lastChildProps,
            renderIsScheduled,
            isMounted,
            childPropsFromStoreUpdate,
            notifyNestedSubs,
            reactListener
          );
        };
        return subscribe;
      }, [subscription]);
      useIsomorphicLayoutEffectWithArgs(captureWrapperProps, [
        lastWrapperProps,
        lastChildProps,
        renderIsScheduled,
        wrapperProps,
        childPropsFromStoreUpdate,
        notifyNestedSubs
      ]);
      let actualChildProps;
      try {
        actualChildProps = external_React_.useSyncExternalStore(
          // TODO We're passing through a big wrapper that does a bunch of extra side effects besides subscribing
          subscribeForReact,
          // TODO This is incredibly hacky. We've already processed the store update and calculated new child props,
          // TODO and we're just passing that through so it triggers a re-render for us rather than relying on `uSES`.
          actualChildPropsSelector,
          getServerState ? () => childPropsSelector(getServerState(), wrapperProps) : actualChildPropsSelector
        );
      } catch (err) {
        if (latestSubscriptionCallbackError.current) {
          ;
          err.message += `
The error may be correlated with this previous error:
${latestSubscriptionCallbackError.current.stack}

`;
        }
        throw err;
      }
      useIsomorphicLayoutEffect(() => {
        latestSubscriptionCallbackError.current = void 0;
        childPropsFromStoreUpdate.current = void 0;
        lastChildProps.current = actualChildProps;
      });
      const renderedWrappedComponent = external_React_.useMemo(() => {
        return (
          // @ts-ignore
          /* @__PURE__ */ external_React_.createElement(
            WrappedComponent,
            {
              ...actualChildProps,
              ref: reactReduxForwardedRef
            }
          )
        );
      }, [reactReduxForwardedRef, WrappedComponent, actualChildProps]);
      const renderedChild = external_React_.useMemo(() => {
        if (shouldHandleStateChanges) {
          return /* @__PURE__ */ external_React_.createElement(ContextToUse.Provider, { value: overriddenContextValue }, renderedWrappedComponent);
        }
        return renderedWrappedComponent;
      }, [ContextToUse, renderedWrappedComponent, overriddenContextValue]);
      return renderedChild;
    }
    const _Connect = external_React_.memo(ConnectFunction);
    const Connect = _Connect;
    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = ConnectFunction.displayName = displayName;
    if (forwardRef) {
      const _forwarded = external_React_.forwardRef(
        function forwardConnectRef(props, ref) {
          return /* @__PURE__ */ external_React_.createElement(Connect, { ...props, reactReduxForwardedRef: ref });
        }
      );
      const forwarded = _forwarded;
      forwarded.displayName = displayName;
      forwarded.WrappedComponent = WrappedComponent;
      return /* @__PURE__ */ hoistNonReactStatics(forwarded, WrappedComponent);
    }
    return /* @__PURE__ */ hoistNonReactStatics(Connect, WrappedComponent);
  };
  return wrapWithConnect;
}
var connect_default = connect;

// src/components/Provider.tsx
function Provider(providerProps) {
  const { children, context, serverState, store } = providerProps;
  const contextValue = external_React_.useMemo(() => {
    const subscription = createSubscription(store);
    const baseContextValue = {
      store,
      subscription,
      getServerState: serverState ? () => serverState : void 0
    };
    if (true) {
      return baseContextValue;
    } else // removed by dead control flow
{}
  }, [store, serverState]);
  const previousState = external_React_.useMemo(() => store.getState(), [store]);
  useIsomorphicLayoutEffect(() => {
    const { subscription } = contextValue;
    subscription.onStateChange = subscription.notifyNestedSubs;
    subscription.trySubscribe();
    if (previousState !== store.getState()) {
      subscription.notifyNestedSubs();
    }
    return () => {
      subscription.tryUnsubscribe();
      subscription.onStateChange = void 0;
    };
  }, [contextValue, previousState]);
  const Context = context || ReactReduxContext;
  return /* @__PURE__ */ external_React_.createElement(Context.Provider, { value: contextValue }, children);
}
var Provider_default = Provider;

// src/hooks/useReduxContext.ts
function createReduxContextHook(context = ReactReduxContext) {
  return function useReduxContext2() {
    const contextValue = external_React_.useContext(context);
    if (false) // removed by dead control flow
{}
    return contextValue;
  };
}
var useReduxContext = /* @__PURE__ */ createReduxContextHook();

// src/hooks/useStore.ts
function createStoreHook(context = ReactReduxContext) {
  const useReduxContext2 = context === ReactReduxContext ? useReduxContext : (
    // @ts-ignore
    createReduxContextHook(context)
  );
  const useStore2 = () => {
    const { store } = useReduxContext2();
    return store;
  };
  Object.assign(useStore2, {
    withTypes: () => useStore2
  });
  return useStore2;
}
var useStore = /* @__PURE__ */ createStoreHook();

// src/hooks/useDispatch.ts
function createDispatchHook(context = ReactReduxContext) {
  const useStore2 = context === ReactReduxContext ? useStore : createStoreHook(context);
  const useDispatch2 = () => {
    const store = useStore2();
    return store.dispatch;
  };
  Object.assign(useDispatch2, {
    withTypes: () => useDispatch2
  });
  return useDispatch2;
}
var useDispatch = /* @__PURE__ */ createDispatchHook();

// src/hooks/useSelector.ts

var refEquality = (a, b) => a === b;
function createSelectorHook(context = ReactReduxContext) {
  const useReduxContext2 = context === ReactReduxContext ? useReduxContext : createReduxContextHook(context);
  const useSelector2 = (selector, equalityFnOrOptions = {}) => {
    const { equalityFn = refEquality } = typeof equalityFnOrOptions === "function" ? { equalityFn: equalityFnOrOptions } : equalityFnOrOptions;
    if (false) // removed by dead control flow
{}
    const reduxContext = useReduxContext2();
    const { store, subscription, getServerState } = reduxContext;
    const firstRun = external_React_.useRef(true);
    const wrappedSelector = external_React_.useCallback(
      {
        [selector.name](state) {
          const selected = selector(state);
          if (false) // removed by dead control flow
{}
          return selected;
        }
      }[selector.name],
      [selector]
    );
    const selectedState = (0,with_selector.useSyncExternalStoreWithSelector)(
      subscription.addNestedSub,
      store.getState,
      getServerState || store.getState,
      wrappedSelector,
      equalityFn
    );
    external_React_.useDebugValue(selectedState);
    return selectedState;
  };
  Object.assign(useSelector2, {
    withTypes: () => useSelector2
  });
  return useSelector2;
}
var useSelector = /* @__PURE__ */ createSelectorHook();

// src/exports.ts
var batch = defaultNoopBatch;

//# sourceMappingURL=react-redux.mjs.map
;// ./node_modules/immer/dist/immer.mjs
// src/utils/env.ts
var immer_NOTHING = Symbol.for("immer-nothing");
var immer_DRAFTABLE = Symbol.for("immer-draftable");
var immer_DRAFT_STATE = Symbol.for("immer-state");

// src/utils/errors.ts
var immer_errors =  false ? 0 : [];
function immer_die(error, ...args) {
  if (false) // removed by dead control flow
{}
  throw new Error(
    `[Immer] minified error nr: ${error}. Full error at: https://bit.ly/3cXEKWf`
  );
}

// src/utils/common.ts
var immer_getPrototypeOf = Object.getPrototypeOf;
function immer_isDraft(value) {
  return !!value && !!value[immer_DRAFT_STATE];
}
function immer_isDraftable(value) {
  if (!value)
    return false;
  return dist_immer_isPlainObject(value) || Array.isArray(value) || !!value[immer_DRAFTABLE] || !!value.constructor?.[immer_DRAFTABLE] || immer_isMap(value) || immer_isSet(value);
}
var immer_objectCtorString = Object.prototype.constructor.toString();
var immer_cachedCtorStrings = /* @__PURE__ */ new WeakMap();
function dist_immer_isPlainObject(value) {
  if (!value || typeof value !== "object")
    return false;
  const proto = Object.getPrototypeOf(value);
  if (proto === null || proto === Object.prototype)
    return true;
  const Ctor = Object.hasOwnProperty.call(proto, "constructor") && proto.constructor;
  if (Ctor === Object)
    return true;
  if (typeof Ctor !== "function")
    return false;
  let ctorString = immer_cachedCtorStrings.get(Ctor);
  if (ctorString === void 0) {
    ctorString = Function.toString.call(Ctor);
    immer_cachedCtorStrings.set(Ctor, ctorString);
  }
  return ctorString === immer_objectCtorString;
}
function immer_original(value) {
  if (!immer_isDraft(value))
    immer_die(15, value);
  return value[immer_DRAFT_STATE].base_;
}
function immer_each(obj, iter, strict = true) {
  if (immer_getArchtype(obj) === 0 /* Object */) {
    const keys = strict ? Reflect.ownKeys(obj) : Object.keys(obj);
    keys.forEach((key) => {
      iter(key, obj[key], obj);
    });
  } else {
    obj.forEach((entry, index) => iter(index, entry, obj));
  }
}
function immer_getArchtype(thing) {
  const state = thing[immer_DRAFT_STATE];
  return state ? state.type_ : Array.isArray(thing) ? 1 /* Array */ : immer_isMap(thing) ? 2 /* Map */ : immer_isSet(thing) ? 3 /* Set */ : 0 /* Object */;
}
function immer_has(thing, prop) {
  return immer_getArchtype(thing) === 2 /* Map */ ? thing.has(prop) : Object.prototype.hasOwnProperty.call(thing, prop);
}
function dist_immer_get(thing, prop) {
  return immer_getArchtype(thing) === 2 /* Map */ ? thing.get(prop) : thing[prop];
}
function immer_set(thing, propOrOldValue, value) {
  const t = immer_getArchtype(thing);
  if (t === 2 /* Map */)
    thing.set(propOrOldValue, value);
  else if (t === 3 /* Set */) {
    thing.add(value);
  } else
    thing[propOrOldValue] = value;
}
function immer_is(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
function immer_isMap(target) {
  return target instanceof Map;
}
function immer_isSet(target) {
  return target instanceof Set;
}
function immer_latest(state) {
  return state.copy_ || state.base_;
}
function immer_shallowCopy(base, strict) {
  if (immer_isMap(base)) {
    return new Map(base);
  }
  if (immer_isSet(base)) {
    return new Set(base);
  }
  if (Array.isArray(base))
    return Array.prototype.slice.call(base);
  const isPlain = dist_immer_isPlainObject(base);
  if (strict === true || strict === "class_only" && !isPlain) {
    const descriptors = Object.getOwnPropertyDescriptors(base);
    delete descriptors[immer_DRAFT_STATE];
    let keys = Reflect.ownKeys(descriptors);
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const desc = descriptors[key];
      if (desc.writable === false) {
        desc.writable = true;
        desc.configurable = true;
      }
      if (desc.get || desc.set)
        descriptors[key] = {
          configurable: true,
          writable: true,
          // could live with !!desc.set as well here...
          enumerable: desc.enumerable,
          value: base[key]
        };
    }
    return Object.create(immer_getPrototypeOf(base), descriptors);
  } else {
    const proto = immer_getPrototypeOf(base);
    if (proto !== null && isPlain) {
      return { ...base };
    }
    const obj = Object.create(proto);
    return Object.assign(obj, base);
  }
}
function immer_freeze(obj, deep = false) {
  if (immer_isFrozen(obj) || immer_isDraft(obj) || !immer_isDraftable(obj))
    return obj;
  if (immer_getArchtype(obj) > 1) {
    Object.defineProperties(obj, {
      set: immer_dontMutateMethodOverride,
      add: immer_dontMutateMethodOverride,
      clear: immer_dontMutateMethodOverride,
      delete: immer_dontMutateMethodOverride
    });
  }
  Object.freeze(obj);
  if (deep)
    Object.values(obj).forEach((value) => immer_freeze(value, true));
  return obj;
}
function immer_dontMutateFrozenCollections() {
  immer_die(2);
}
var immer_dontMutateMethodOverride = {
  value: immer_dontMutateFrozenCollections
};
function immer_isFrozen(obj) {
  if (obj === null || typeof obj !== "object")
    return true;
  return Object.isFrozen(obj);
}

// src/utils/plugins.ts
var immer_plugins = {};
function immer_getPlugin(pluginKey) {
  const plugin = immer_plugins[pluginKey];
  if (!plugin) {
    immer_die(0, pluginKey);
  }
  return plugin;
}
function immer_loadPlugin(pluginKey, implementation) {
  if (!immer_plugins[pluginKey])
    immer_plugins[pluginKey] = implementation;
}

// src/core/scope.ts
var immer_currentScope;
function immer_getCurrentScope() {
  return immer_currentScope;
}
function immer_createScope(parent_, immer_) {
  return {
    drafts_: [],
    parent_,
    immer_,
    // Whenever the modified draft contains a draft from another scope, we
    // need to prevent auto-freezing so the unowned draft can be finalized.
    canAutoFreeze_: true,
    unfinalizedDrafts_: 0
  };
}
function immer_usePatchesInScope(scope, patchListener) {
  if (patchListener) {
    immer_getPlugin("Patches");
    scope.patches_ = [];
    scope.inversePatches_ = [];
    scope.patchListener_ = patchListener;
  }
}
function immer_revokeScope(scope) {
  immer_leaveScope(scope);
  scope.drafts_.forEach(immer_revokeDraft);
  scope.drafts_ = null;
}
function immer_leaveScope(scope) {
  if (scope === immer_currentScope) {
    immer_currentScope = scope.parent_;
  }
}
function immer_enterScope(immer2) {
  return immer_currentScope = immer_createScope(immer_currentScope, immer2);
}
function immer_revokeDraft(draft) {
  const state = draft[immer_DRAFT_STATE];
  if (state.type_ === 0 /* Object */ || state.type_ === 1 /* Array */)
    state.revoke_();
  else
    state.revoked_ = true;
}

// src/core/finalize.ts
function immer_processResult(result, scope) {
  scope.unfinalizedDrafts_ = scope.drafts_.length;
  const baseDraft = scope.drafts_[0];
  const isReplaced = result !== void 0 && result !== baseDraft;
  if (isReplaced) {
    if (baseDraft[immer_DRAFT_STATE].modified_) {
      immer_revokeScope(scope);
      immer_die(4);
    }
    if (immer_isDraftable(result)) {
      result = immer_finalize(scope, result);
      if (!scope.parent_)
        immer_maybeFreeze(scope, result);
    }
    if (scope.patches_) {
      immer_getPlugin("Patches").generateReplacementPatches_(
        baseDraft[immer_DRAFT_STATE].base_,
        result,
        scope.patches_,
        scope.inversePatches_
      );
    }
  } else {
    result = immer_finalize(scope, baseDraft, []);
  }
  immer_revokeScope(scope);
  if (scope.patches_) {
    scope.patchListener_(scope.patches_, scope.inversePatches_);
  }
  return result !== immer_NOTHING ? result : void 0;
}
function immer_finalize(rootScope, value, path) {
  if (immer_isFrozen(value))
    return value;
  const useStrictIteration = rootScope.immer_.shouldUseStrictIteration();
  const state = value[immer_DRAFT_STATE];
  if (!state) {
    immer_each(
      value,
      (key, childValue) => finalizeProperty(rootScope, state, value, key, childValue, path),
      useStrictIteration
    );
    return value;
  }
  if (state.scope_ !== rootScope)
    return value;
  if (!state.modified_) {
    immer_maybeFreeze(rootScope, state.base_, true);
    return state.base_;
  }
  if (!state.finalized_) {
    state.finalized_ = true;
    state.scope_.unfinalizedDrafts_--;
    const result = state.copy_;
    let resultEach = result;
    let isSet2 = false;
    if (state.type_ === 3 /* Set */) {
      resultEach = new Set(result);
      result.clear();
      isSet2 = true;
    }
    immer_each(
      resultEach,
      (key, childValue) => finalizeProperty(
        rootScope,
        state,
        result,
        key,
        childValue,
        path,
        isSet2
      ),
      useStrictIteration
    );
    immer_maybeFreeze(rootScope, result, false);
    if (path && rootScope.patches_) {
      immer_getPlugin("Patches").generatePatches_(
        state,
        path,
        rootScope.patches_,
        rootScope.inversePatches_
      );
    }
  }
  return state.copy_;
}
function finalizeProperty(rootScope, parentState, targetObject, prop, childValue, rootPath, targetIsSet) {
  if (childValue == null) {
    return;
  }
  if (typeof childValue !== "object" && !targetIsSet) {
    return;
  }
  const childIsFrozen = immer_isFrozen(childValue);
  if (childIsFrozen && !targetIsSet) {
    return;
  }
  if (false)
    // removed by dead control flow
{}
  if (immer_isDraft(childValue)) {
    const path = rootPath && parentState && parentState.type_ !== 3 /* Set */ && // Set objects are atomic since they have no keys.
    !immer_has(parentState.assigned_, prop) ? rootPath.concat(prop) : void 0;
    const res = immer_finalize(rootScope, childValue, path);
    immer_set(targetObject, prop, res);
    if (immer_isDraft(res)) {
      rootScope.canAutoFreeze_ = false;
    } else
      return;
  } else if (targetIsSet) {
    targetObject.add(childValue);
  }
  if (immer_isDraftable(childValue) && !childIsFrozen) {
    if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
      return;
    }
    if (parentState && parentState.base_ && parentState.base_[prop] === childValue && childIsFrozen) {
      return;
    }
    immer_finalize(rootScope, childValue);
    if ((!parentState || !parentState.scope_.parent_) && typeof prop !== "symbol" && (immer_isMap(targetObject) ? targetObject.has(prop) : Object.prototype.propertyIsEnumerable.call(targetObject, prop)))
      immer_maybeFreeze(rootScope, childValue);
  }
}
function immer_maybeFreeze(scope, value, deep = false) {
  if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
    immer_freeze(value, deep);
  }
}

// src/core/proxy.ts
function immer_createProxyProxy(base, parent) {
  const isArray = Array.isArray(base);
  const state = {
    type_: isArray ? 1 /* Array */ : 0 /* Object */,
    // Track which produce call this is associated with.
    scope_: parent ? parent.scope_ : immer_getCurrentScope(),
    // True for both shallow and deep changes.
    modified_: false,
    // Used during finalization.
    finalized_: false,
    // Track which properties have been assigned (true) or deleted (false).
    assigned_: {},
    // The parent draft state.
    parent_: parent,
    // The base state.
    base_: base,
    // The base proxy.
    draft_: null,
    // set below
    // The base copy with any updated values.
    copy_: null,
    // Called by the `produce` function.
    revoke_: null,
    isManual_: false
  };
  let target = state;
  let traps = immer_objectTraps;
  if (isArray) {
    target = [state];
    traps = immer_arrayTraps;
  }
  const { revoke, proxy } = Proxy.revocable(target, traps);
  state.draft_ = proxy;
  state.revoke_ = revoke;
  return proxy;
}
var immer_objectTraps = {
  get(state, prop) {
    if (prop === immer_DRAFT_STATE)
      return state;
    const source = immer_latest(state);
    if (!immer_has(source, prop)) {
      return immer_readPropFromProto(state, source, prop);
    }
    const value = source[prop];
    if (state.finalized_ || !immer_isDraftable(value)) {
      return value;
    }
    if (value === immer_peek(state.base_, prop)) {
      immer_prepareCopy(state);
      return state.copy_[prop] = immer_createProxy(value, state);
    }
    return value;
  },
  has(state, prop) {
    return prop in immer_latest(state);
  },
  ownKeys(state) {
    return Reflect.ownKeys(immer_latest(state));
  },
  set(state, prop, value) {
    const desc = immer_getDescriptorFromProto(immer_latest(state), prop);
    if (desc?.set) {
      desc.set.call(state.draft_, value);
      return true;
    }
    if (!state.modified_) {
      const current2 = immer_peek(immer_latest(state), prop);
      const currentState = current2?.[immer_DRAFT_STATE];
      if (currentState && currentState.base_ === value) {
        state.copy_[prop] = value;
        state.assigned_[prop] = false;
        return true;
      }
      if (immer_is(value, current2) && (value !== void 0 || immer_has(state.base_, prop)))
        return true;
      immer_prepareCopy(state);
      immer_markChanged(state);
    }
    if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
    (value !== void 0 || prop in state.copy_) || // special case: NaN
    Number.isNaN(value) && Number.isNaN(state.copy_[prop]))
      return true;
    state.copy_[prop] = value;
    state.assigned_[prop] = true;
    return true;
  },
  deleteProperty(state, prop) {
    if (immer_peek(state.base_, prop) !== void 0 || prop in state.base_) {
      state.assigned_[prop] = false;
      immer_prepareCopy(state);
      immer_markChanged(state);
    } else {
      delete state.assigned_[prop];
    }
    if (state.copy_) {
      delete state.copy_[prop];
    }
    return true;
  },
  // Note: We never coerce `desc.value` into an Immer draft, because we can't make
  // the same guarantee in ES5 mode.
  getOwnPropertyDescriptor(state, prop) {
    const owner = immer_latest(state);
    const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
    if (!desc)
      return desc;
    return {
      writable: true,
      configurable: state.type_ !== 1 /* Array */ || prop !== "length",
      enumerable: desc.enumerable,
      value: owner[prop]
    };
  },
  defineProperty() {
    immer_die(11);
  },
  getPrototypeOf(state) {
    return immer_getPrototypeOf(state.base_);
  },
  setPrototypeOf() {
    immer_die(12);
  }
};
var immer_arrayTraps = {};
immer_each(immer_objectTraps, (key, fn) => {
  immer_arrayTraps[key] = function() {
    arguments[0] = arguments[0][0];
    return fn.apply(this, arguments);
  };
});
immer_arrayTraps.deleteProperty = function(state, prop) {
  if (false)
    // removed by dead control flow
{}
  return immer_arrayTraps.set.call(this, state, prop, void 0);
};
immer_arrayTraps.set = function(state, prop, value) {
  if (false)
    // removed by dead control flow
{}
  return immer_objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function immer_peek(draft, prop) {
  const state = draft[immer_DRAFT_STATE];
  const source = state ? immer_latest(state) : draft;
  return source[prop];
}
function immer_readPropFromProto(state, source, prop) {
  const desc = immer_getDescriptorFromProto(source, prop);
  return desc ? `value` in desc ? desc.value : (
    // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_)
  ) : void 0;
}
function immer_getDescriptorFromProto(source, prop) {
  if (!(prop in source))
    return void 0;
  let proto = immer_getPrototypeOf(source);
  while (proto) {
    const desc = Object.getOwnPropertyDescriptor(proto, prop);
    if (desc)
      return desc;
    proto = immer_getPrototypeOf(proto);
  }
  return void 0;
}
function immer_markChanged(state) {
  if (!state.modified_) {
    state.modified_ = true;
    if (state.parent_) {
      immer_markChanged(state.parent_);
    }
  }
}
function immer_prepareCopy(state) {
  if (!state.copy_) {
    state.copy_ = immer_shallowCopy(
      state.base_,
      state.scope_.immer_.useStrictShallowCopy_
    );
  }
}

// src/core/immerClass.ts
var immer_Immer2 = class {
  constructor(config) {
    this.autoFreeze_ = true;
    this.useStrictShallowCopy_ = false;
    this.useStrictIteration_ = true;
    /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */
    this.produce = (base, recipe, patchListener) => {
      if (typeof base === "function" && typeof recipe !== "function") {
        const defaultBase = recipe;
        recipe = base;
        const self = this;
        return function curriedProduce(base2 = defaultBase, ...args) {
          return self.produce(base2, (draft) => recipe.call(this, draft, ...args));
        };
      }
      if (typeof recipe !== "function")
        immer_die(6);
      if (patchListener !== void 0 && typeof patchListener !== "function")
        immer_die(7);
      let result;
      if (immer_isDraftable(base)) {
        const scope = immer_enterScope(this);
        const proxy = immer_createProxy(base, void 0);
        let hasError = true;
        try {
          result = recipe(proxy);
          hasError = false;
        } finally {
          if (hasError)
            immer_revokeScope(scope);
          else
            immer_leaveScope(scope);
        }
        immer_usePatchesInScope(scope, patchListener);
        return immer_processResult(result, scope);
      } else if (!base || typeof base !== "object") {
        result = recipe(base);
        if (result === void 0)
          result = base;
        if (result === immer_NOTHING)
          result = void 0;
        if (this.autoFreeze_)
          immer_freeze(result, true);
        if (patchListener) {
          const p = [];
          const ip = [];
          immer_getPlugin("Patches").generateReplacementPatches_(base, result, p, ip);
          patchListener(p, ip);
        }
        return result;
      } else
        immer_die(1, base);
    };
    this.produceWithPatches = (base, recipe) => {
      if (typeof base === "function") {
        return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
      }
      let patches, inversePatches;
      const result = this.produce(base, recipe, (p, ip) => {
        patches = p;
        inversePatches = ip;
      });
      return [result, patches, inversePatches];
    };
    if (typeof config?.autoFreeze === "boolean")
      this.setAutoFreeze(config.autoFreeze);
    if (typeof config?.useStrictShallowCopy === "boolean")
      this.setUseStrictShallowCopy(config.useStrictShallowCopy);
    if (typeof config?.useStrictIteration === "boolean")
      this.setUseStrictIteration(config.useStrictIteration);
  }
  createDraft(base) {
    if (!immer_isDraftable(base))
      immer_die(8);
    if (immer_isDraft(base))
      base = immer_current(base);
    const scope = immer_enterScope(this);
    const proxy = immer_createProxy(base, void 0);
    proxy[immer_DRAFT_STATE].isManual_ = true;
    immer_leaveScope(scope);
    return proxy;
  }
  finishDraft(draft, patchListener) {
    const state = draft && draft[immer_DRAFT_STATE];
    if (!state || !state.isManual_)
      immer_die(9);
    const { scope_: scope } = state;
    immer_usePatchesInScope(scope, patchListener);
    return immer_processResult(void 0, scope);
  }
  /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */
  setAutoFreeze(value) {
    this.autoFreeze_ = value;
  }
  /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */
  setUseStrictShallowCopy(value) {
    this.useStrictShallowCopy_ = value;
  }
  /**
   * Pass false to use faster iteration that skips non-enumerable properties
   * but still handles symbols for compatibility.
   *
   * By default, strict iteration is enabled (includes all own properties).
   */
  setUseStrictIteration(value) {
    this.useStrictIteration_ = value;
  }
  shouldUseStrictIteration() {
    return this.useStrictIteration_;
  }
  applyPatches(base, patches) {
    let i;
    for (i = patches.length - 1; i >= 0; i--) {
      const patch = patches[i];
      if (patch.path.length === 0 && patch.op === "replace") {
        base = patch.value;
        break;
      }
    }
    if (i > -1) {
      patches = patches.slice(i + 1);
    }
    const applyPatchesImpl = immer_getPlugin("Patches").applyPatches_;
    if (immer_isDraft(base)) {
      return applyPatchesImpl(base, patches);
    }
    return this.produce(
      base,
      (draft) => applyPatchesImpl(draft, patches)
    );
  }
};
function immer_createProxy(value, parent) {
  const draft = immer_isMap(value) ? immer_getPlugin("MapSet").proxyMap_(value, parent) : immer_isSet(value) ? immer_getPlugin("MapSet").proxySet_(value, parent) : immer_createProxyProxy(value, parent);
  const scope = parent ? parent.scope_ : immer_getCurrentScope();
  scope.drafts_.push(draft);
  return draft;
}

// src/core/current.ts
function immer_current(value) {
  if (!immer_isDraft(value))
    immer_die(10, value);
  return immer_currentImpl(value);
}
function immer_currentImpl(value) {
  if (!immer_isDraftable(value) || immer_isFrozen(value))
    return value;
  const state = value[immer_DRAFT_STATE];
  let copy;
  let strict = true;
  if (state) {
    if (!state.modified_)
      return state.base_;
    state.finalized_ = true;
    copy = immer_shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
    strict = state.scope_.immer_.shouldUseStrictIteration();
  } else {
    copy = immer_shallowCopy(value, true);
  }
  immer_each(
    copy,
    (key, childValue) => {
      immer_set(copy, key, immer_currentImpl(childValue));
    },
    strict
  );
  if (state) {
    state.finalized_ = false;
  }
  return copy;
}

// src/plugins/patches.ts
function immer_enablePatches() {
  const errorOffset = 16;
  if (false) // removed by dead control flow
{}
  const REPLACE = "replace";
  const ADD = "add";
  const REMOVE = "remove";
  function generatePatches_(state, basePath, patches, inversePatches) {
    switch (state.type_) {
      case 0 /* Object */:
      case 2 /* Map */:
        return generatePatchesFromAssigned(
          state,
          basePath,
          patches,
          inversePatches
        );
      case 1 /* Array */:
        return generateArrayPatches(state, basePath, patches, inversePatches);
      case 3 /* Set */:
        return generateSetPatches(
          state,
          basePath,
          patches,
          inversePatches
        );
    }
  }
  function generateArrayPatches(state, basePath, patches, inversePatches) {
    let { base_, assigned_ } = state;
    let copy_ = state.copy_;
    if (copy_.length < base_.length) {
      ;
      [base_, copy_] = [copy_, base_];
      [patches, inversePatches] = [inversePatches, patches];
    }
    for (let i = 0; i < base_.length; i++) {
      if (assigned_[i] && copy_[i] !== base_[i]) {
        const path = basePath.concat([i]);
        patches.push({
          op: REPLACE,
          path,
          // Need to maybe clone it, as it can in fact be the original value
          // due to the base/copy inversion at the start of this function
          value: clonePatchValueIfNeeded(copy_[i])
        });
        inversePatches.push({
          op: REPLACE,
          path,
          value: clonePatchValueIfNeeded(base_[i])
        });
      }
    }
    for (let i = base_.length; i < copy_.length; i++) {
      const path = basePath.concat([i]);
      patches.push({
        op: ADD,
        path,
        // Need to maybe clone it, as it can in fact be the original value
        // due to the base/copy inversion at the start of this function
        value: clonePatchValueIfNeeded(copy_[i])
      });
    }
    for (let i = copy_.length - 1; base_.length <= i; --i) {
      const path = basePath.concat([i]);
      inversePatches.push({
        op: REMOVE,
        path
      });
    }
  }
  function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
    const { base_, copy_ } = state;
    immer_each(state.assigned_, (key, assignedValue) => {
      const origValue = dist_immer_get(base_, key);
      const value = dist_immer_get(copy_, key);
      const op = !assignedValue ? REMOVE : immer_has(base_, key) ? REPLACE : ADD;
      if (origValue === value && op === REPLACE)
        return;
      const path = basePath.concat(key);
      patches.push(op === REMOVE ? { op, path } : { op, path, value });
      inversePatches.push(
        op === ADD ? { op: REMOVE, path } : op === REMOVE ? { op: ADD, path, value: clonePatchValueIfNeeded(origValue) } : { op: REPLACE, path, value: clonePatchValueIfNeeded(origValue) }
      );
    });
  }
  function generateSetPatches(state, basePath, patches, inversePatches) {
    let { base_, copy_ } = state;
    let i = 0;
    base_.forEach((value) => {
      if (!copy_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: REMOVE,
          path,
          value
        });
        inversePatches.unshift({
          op: ADD,
          path,
          value
        });
      }
      i++;
    });
    i = 0;
    copy_.forEach((value) => {
      if (!base_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: ADD,
          path,
          value
        });
        inversePatches.unshift({
          op: REMOVE,
          path,
          value
        });
      }
      i++;
    });
  }
  function generateReplacementPatches_(baseValue, replacement, patches, inversePatches) {
    patches.push({
      op: REPLACE,
      path: [],
      value: replacement === immer_NOTHING ? void 0 : replacement
    });
    inversePatches.push({
      op: REPLACE,
      path: [],
      value: baseValue
    });
  }
  function applyPatches_(draft, patches) {
    patches.forEach((patch) => {
      const { path, op } = patch;
      let base = draft;
      for (let i = 0; i < path.length - 1; i++) {
        const parentType = immer_getArchtype(base);
        let p = path[i];
        if (typeof p !== "string" && typeof p !== "number") {
          p = "" + p;
        }
        if ((parentType === 0 /* Object */ || parentType === 1 /* Array */) && (p === "__proto__" || p === "constructor"))
          immer_die(errorOffset + 3);
        if (typeof base === "function" && p === "prototype")
          immer_die(errorOffset + 3);
        base = dist_immer_get(base, p);
        if (typeof base !== "object")
          immer_die(errorOffset + 2, path.join("/"));
      }
      const type = immer_getArchtype(base);
      const value = deepClonePatchValue(patch.value);
      const key = path[path.length - 1];
      switch (op) {
        case REPLACE:
          switch (type) {
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              immer_die(errorOffset);
            default:
              return base[key] = value;
          }
        case ADD:
          switch (type) {
            case 1 /* Array */:
              return key === "-" ? base.push(value) : base.splice(key, 0, value);
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              return base.add(value);
            default:
              return base[key] = value;
          }
        case REMOVE:
          switch (type) {
            case 1 /* Array */:
              return base.splice(key, 1);
            case 2 /* Map */:
              return base.delete(key);
            case 3 /* Set */:
              return base.delete(patch.value);
            default:
              return delete base[key];
          }
        default:
          immer_die(errorOffset + 1, op);
      }
    });
    return draft;
  }
  function deepClonePatchValue(obj) {
    if (!immer_isDraftable(obj))
      return obj;
    if (Array.isArray(obj))
      return obj.map(deepClonePatchValue);
    if (immer_isMap(obj))
      return new Map(
        Array.from(obj.entries()).map(([k, v]) => [k, deepClonePatchValue(v)])
      );
    if (immer_isSet(obj))
      return new Set(Array.from(obj).map(deepClonePatchValue));
    const cloned = Object.create(immer_getPrototypeOf(obj));
    for (const key in obj)
      cloned[key] = deepClonePatchValue(obj[key]);
    if (immer_has(obj, immer_DRAFTABLE))
      cloned[immer_DRAFTABLE] = obj[immer_DRAFTABLE];
    return cloned;
  }
  function clonePatchValueIfNeeded(obj) {
    if (immer_isDraft(obj)) {
      return deepClonePatchValue(obj);
    } else
      return obj;
  }
  immer_loadPlugin("Patches", {
    applyPatches_,
    generatePatches_,
    generateReplacementPatches_
  });
}

// src/plugins/mapset.ts
function immer_enableMapSet() {
  class DraftMap extends Map {
    constructor(target, parent) {
      super();
      this[immer_DRAFT_STATE] = {
        type_: 2 /* Map */,
        parent_: parent,
        scope_: parent ? parent.scope_ : immer_getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        assigned_: void 0,
        base_: target,
        draft_: this,
        isManual_: false,
        revoked_: false
      };
    }
    get size() {
      return immer_latest(this[immer_DRAFT_STATE]).size;
    }
    has(key) {
      return immer_latest(this[immer_DRAFT_STATE]).has(key);
    }
    set(key, value) {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      if (!immer_latest(state).has(key) || immer_latest(state).get(key) !== value) {
        prepareMapCopy(state);
        immer_markChanged(state);
        state.assigned_.set(key, true);
        state.copy_.set(key, value);
        state.assigned_.set(key, true);
      }
      return this;
    }
    delete(key) {
      if (!this.has(key)) {
        return false;
      }
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      prepareMapCopy(state);
      immer_markChanged(state);
      if (state.base_.has(key)) {
        state.assigned_.set(key, false);
      } else {
        state.assigned_.delete(key);
      }
      state.copy_.delete(key);
      return true;
    }
    clear() {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      if (immer_latest(state).size) {
        prepareMapCopy(state);
        immer_markChanged(state);
        state.assigned_ = /* @__PURE__ */ new Map();
        immer_each(state.base_, (key) => {
          state.assigned_.set(key, false);
        });
        state.copy_.clear();
      }
    }
    forEach(cb, thisArg) {
      const state = this[immer_DRAFT_STATE];
      immer_latest(state).forEach((_value, key, _map) => {
        cb.call(thisArg, this.get(key), key, this);
      });
    }
    get(key) {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      const value = immer_latest(state).get(key);
      if (state.finalized_ || !immer_isDraftable(value)) {
        return value;
      }
      if (value !== state.base_.get(key)) {
        return value;
      }
      const draft = immer_createProxy(value, state);
      prepareMapCopy(state);
      state.copy_.set(key, draft);
      return draft;
    }
    keys() {
      return immer_latest(this[immer_DRAFT_STATE]).keys();
    }
    values() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.values(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value
          };
        }
      };
    }
    entries() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.entries(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value: [r.value, value]
          };
        }
      };
    }
    [(immer_DRAFT_STATE, Symbol.iterator)]() {
      return this.entries();
    }
  }
  function proxyMap_(target, parent) {
    return new DraftMap(target, parent);
  }
  function prepareMapCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = new Map(state.base_);
    }
  }
  class DraftSet extends Set {
    constructor(target, parent) {
      super();
      this[immer_DRAFT_STATE] = {
        type_: 3 /* Set */,
        parent_: parent,
        scope_: parent ? parent.scope_ : immer_getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        base_: target,
        draft_: this,
        drafts_: /* @__PURE__ */ new Map(),
        revoked_: false,
        isManual_: false
      };
    }
    get size() {
      return immer_latest(this[immer_DRAFT_STATE]).size;
    }
    has(value) {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      if (!state.copy_) {
        return state.base_.has(value);
      }
      if (state.copy_.has(value))
        return true;
      if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value)))
        return true;
      return false;
    }
    add(value) {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      if (!this.has(value)) {
        prepareSetCopy(state);
        immer_markChanged(state);
        state.copy_.add(value);
      }
      return this;
    }
    delete(value) {
      if (!this.has(value)) {
        return false;
      }
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      immer_markChanged(state);
      return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : (
        /* istanbul ignore next */
        false
      ));
    }
    clear() {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      if (immer_latest(state).size) {
        prepareSetCopy(state);
        immer_markChanged(state);
        state.copy_.clear();
      }
    }
    values() {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.values();
    }
    entries() {
      const state = this[immer_DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.entries();
    }
    keys() {
      return this.values();
    }
    [(immer_DRAFT_STATE, Symbol.iterator)]() {
      return this.values();
    }
    forEach(cb, thisArg) {
      const iterator = this.values();
      let result = iterator.next();
      while (!result.done) {
        cb.call(thisArg, result.value, result.value, this);
        result = iterator.next();
      }
    }
  }
  function proxySet_(target, parent) {
    return new DraftSet(target, parent);
  }
  function prepareSetCopy(state) {
    if (!state.copy_) {
      state.copy_ = /* @__PURE__ */ new Set();
      state.base_.forEach((value) => {
        if (immer_isDraftable(value)) {
          const draft = immer_createProxy(value, state);
          state.drafts_.set(value, draft);
          state.copy_.add(draft);
        } else {
          state.copy_.add(value);
        }
      });
    }
  }
  function assertUnrevoked(state) {
    if (state.revoked_)
      immer_die(3, JSON.stringify(immer_latest(state)));
  }
  immer_loadPlugin("MapSet", { proxyMap_, proxySet_ });
}

// src/immer.ts
var immer_immer = new immer_Immer2();
var immer_produce = immer_immer.produce;
var immer_produceWithPatches = /* @__PURE__ */ immer_immer.produceWithPatches.bind(
  immer_immer
);
var immer_setAutoFreeze = /* @__PURE__ */ immer_immer.setAutoFreeze.bind(immer_immer);
var immer_setUseStrictShallowCopy = /* @__PURE__ */ immer_immer.setUseStrictShallowCopy.bind(
  immer_immer
);
var immer_setUseStrictIteration = /* @__PURE__ */ immer_immer.setUseStrictIteration.bind(
  immer_immer
);
var immer_applyPatches = /* @__PURE__ */ immer_immer.applyPatches.bind(immer_immer);
var immer_createDraft = /* @__PURE__ */ immer_immer.createDraft.bind(immer_immer);
var immer_finishDraft = /* @__PURE__ */ immer_immer.finishDraft.bind(immer_immer);
function immer_castDraft(value) {
  return value;
}
function immer_castImmutable(value) {
  return value;
}

//# sourceMappingURL=immer.mjs.map
;// ./node_modules/recharts/es6/state/tooltipSlice.js



/**
 * One Tooltip can display multiple TooltipPayloadEntries at a time.
 */

/**
 * So what happens is that the tooltip payload is decided based on the available data, and the dataKey.
 * The dataKey can either be defined on the graphical element (like Line, or Bar)
 * or on the tooltip itself.
 *
 * The data can be defined in the chart element, or in the graphical item.
 *
 * So this type is all the settings, other than the data + dataKey complications.
 */

/**
 * This is what Tooltip renders.
 */

/**
 * null means no active index
 * string means: whichever index from the chart data it is.
 * Different charts have different requirements on data shapes,
 * and are also responsible for providing a function that will accept this index
 * and return data.
 */

/**
 * Different items have different data shapes so the state has no opinion on what the data shape should be;
 * the only requirement is that the chart also provides a searcher function
 * that accepts the data, and a key, and returns whatever the payload in Tooltip should be.
 */

/**
 * So this informs the "tooltip event type". Tooltip event type can be either "axis" or "item"
 * and it is used for two things:
 * 1. Sets the active area
 * 2. Sets the background and cursor highlights
 *
 * Some charts only allow to have one type of tooltip event type, some allow both.
 * Those charts that allow both will have one default, and the "shared" prop will be used to switch between them.
 * Undefined means "use the chart default".
 *
 * Charts that only allow one tooltip event type, will ignore the shared prop.
 */

/**
 * A generic state for user interaction with the chart.
 * User interaction can come through multiple channels: mouse events, keyboard events, or hardcoded in props, or synchronised from other charts.
 *
 * Each of the interaction states is represented as TooltipInteractionState,
 * and then the selectors and Tooltip will decide which of the interaction states to use.
 */

var noInteraction = {
  active: false,
  index: null,
  dataKey: undefined,
  graphicalItemId: undefined,
  coordinate: undefined
};

/**
 * The tooltip interaction state stores:
 *
 * - Which graphical item is user interacting with at the moment,
 * - which axis (or, which part of chart background) is user interacting with at the moment
 * - The data that individual graphical items wish to be displayed in case the tooltip gets activated
 */

var tooltipSlice_initialState = {
  itemInteraction: {
    click: noInteraction,
    hover: noInteraction
  },
  axisInteraction: {
    click: noInteraction,
    hover: noInteraction
  },
  keyboardInteraction: noInteraction,
  syncInteraction: {
    active: false,
    index: null,
    dataKey: undefined,
    label: undefined,
    coordinate: undefined,
    sourceViewBox: undefined,
    graphicalItemId: undefined
  },
  tooltipItemPayloads: [],
  settings: {
    shared: undefined,
    trigger: 'hover',
    axisId: 0,
    active: false,
    defaultIndex: undefined
  }
};

/**
 * This is the event we get when user is interacting with a specific graphical item.
 */

/**
 * Keyboard interaction payload has no graphical item ID,
 * and no dataKey, because keyboard interaction is always
 * with the whole chart, not with a specific graphical item.
 */

var tooltipSlice = createSlice({
  name: 'tooltip',
  initialState: tooltipSlice_initialState,
  reducers: {
    addTooltipEntrySettings: {
      reducer(state, action) {
        state.tooltipItemPayloads.push(immer_castDraft(action.payload));
      },
      prepare: prepareAutoBatched()
    },
    replaceTooltipEntrySettings: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        var index = current(state).tooltipItemPayloads.indexOf(immer_castDraft(prev));
        if (index > -1) {
          state.tooltipItemPayloads[index] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeTooltipEntrySettings: {
      reducer(state, action) {
        var index = current(state).tooltipItemPayloads.indexOf(immer_castDraft(action.payload));
        if (index > -1) {
          state.tooltipItemPayloads.splice(index, 1);
        }
      },
      prepare: prepareAutoBatched()
    },
    setTooltipSettingsState(state, action) {
      state.settings = action.payload;
    },
    setActiveMouseOverItemIndex(state, action) {
      state.syncInteraction.active = false;
      state.keyboardInteraction.active = false;
      state.itemInteraction.hover.active = true;
      state.itemInteraction.hover.index = action.payload.activeIndex;
      state.itemInteraction.hover.dataKey = action.payload.activeDataKey;
      state.itemInteraction.hover.graphicalItemId = action.payload.activeGraphicalItemId;
      state.itemInteraction.hover.coordinate = action.payload.activeCoordinate;
    },
    mouseLeaveChart(state) {
      /*
       * Clear only the active flags. Why?
       * 1. Keep Coordinate to preserve animation - next time the Tooltip appears, we want to render it from
       * the last place where it was when it disappeared.
       * 2. We want to keep all the properties anyway just in case the tooltip has `active=true` prop
       * and continues being visible even after the mouse has left the chart.
       */
      state.itemInteraction.hover.active = false;
      state.axisInteraction.hover.active = false;
    },
    mouseLeaveItem(state) {
      state.itemInteraction.hover.active = false;
    },
    setActiveClickItemIndex(state, action) {
      state.syncInteraction.active = false;
      state.itemInteraction.click.active = true;
      state.keyboardInteraction.active = false;
      state.itemInteraction.click.index = action.payload.activeIndex;
      state.itemInteraction.click.dataKey = action.payload.activeDataKey;
      state.itemInteraction.click.graphicalItemId = action.payload.activeGraphicalItemId;
      state.itemInteraction.click.coordinate = action.payload.activeCoordinate;
    },
    setMouseOverAxisIndex(state, action) {
      state.syncInteraction.active = false;
      state.axisInteraction.hover.active = true;
      state.keyboardInteraction.active = false;
      state.axisInteraction.hover.index = action.payload.activeIndex;
      state.axisInteraction.hover.dataKey = action.payload.activeDataKey;
      state.axisInteraction.hover.coordinate = action.payload.activeCoordinate;
    },
    setMouseClickAxisIndex(state, action) {
      state.syncInteraction.active = false;
      state.keyboardInteraction.active = false;
      state.axisInteraction.click.active = true;
      state.axisInteraction.click.index = action.payload.activeIndex;
      state.axisInteraction.click.dataKey = action.payload.activeDataKey;
      state.axisInteraction.click.coordinate = action.payload.activeCoordinate;
    },
    setSyncInteraction(state, action) {
      state.syncInteraction = action.payload;
    },
    setKeyboardInteraction(state, action) {
      state.keyboardInteraction.active = action.payload.active;
      state.keyboardInteraction.index = action.payload.activeIndex;
      state.keyboardInteraction.coordinate = action.payload.activeCoordinate;
    }
  }
});
var {
  addTooltipEntrySettings,
  replaceTooltipEntrySettings,
  removeTooltipEntrySettings,
  setTooltipSettingsState,
  setActiveMouseOverItemIndex,
  mouseLeaveItem,
  mouseLeaveChart,
  setActiveClickItemIndex,
  setMouseOverAxisIndex,
  setMouseClickAxisIndex,
  setSyncInteraction,
  setKeyboardInteraction
} = tooltipSlice.actions;
var tooltipReducer = tooltipSlice.reducer;
;// ./node_modules/recharts/es6/state/chartDataSlice.js



/**
 * This is the data that's coming through main chart `data` prop
 * Recharts is very flexible in what it accepts so the type is very flexible too.
 * This will typically be an object, and various components will provide various `dataKey`
 * that dictates how to pull data from that object.
 *
 * TL;DR: before dataKey
 *
 * @inline
 */

/**
 * So this is the same unknown type as ChartData but this is after the dataKey has been applied.
 * We still don't know what the type is - that depends on what exactly it was before the dataKey application,
 * and the dataKey can return whatever anyway - but let's keep it separate as a form of documentation.
 *
 * TL;DR: ChartData after dataKey.
 */

var initialChartDataState = {
  chartData: undefined,
  computedData: undefined,
  dataStartIndex: 0,
  dataEndIndex: 0
};
var chartDataSlice = createSlice({
  name: 'chartData',
  initialState: initialChartDataState,
  reducers: {
    setChartData(state, action) {
      state.chartData = immer_castDraft(action.payload);
      if (action.payload == null) {
        state.dataStartIndex = 0;
        state.dataEndIndex = 0;
        return;
      }
      if (action.payload.length > 0 && state.dataEndIndex !== action.payload.length - 1) {
        state.dataEndIndex = action.payload.length - 1;
      }
    },
    setComputedData(state, action) {
      state.computedData = action.payload;
    },
    setDataStartEndIndexes(state, action) {
      var {
        startIndex,
        endIndex
      } = action.payload;
      if (startIndex != null) {
        state.dataStartIndex = startIndex;
      }
      if (endIndex != null) {
        state.dataEndIndex = endIndex;
      }
    }
  }
});
var {
  setChartData,
  setDataStartEndIndexes,
  setComputedData
} = chartDataSlice.actions;
var chartDataReducer = chartDataSlice.reducer;
;// ./node_modules/recharts/es6/state/layoutSlice.js

var layoutSlice_initialState = {
  layoutType: 'horizontal',
  width: 0,
  height: 0,
  margin: {
    top: 5,
    right: 5,
    bottom: 5,
    left: 5
  },
  scale: 1
};
var chartLayoutSlice = createSlice({
  name: 'chartLayout',
  initialState: layoutSlice_initialState,
  reducers: {
    setLayout(state, action) {
      state.layoutType = action.payload;
    },
    setChartSize(state, action) {
      state.width = action.payload.width;
      state.height = action.payload.height;
    },
    setMargin(state, action) {
      var _action$payload$top, _action$payload$right, _action$payload$botto, _action$payload$left;
      state.margin.top = (_action$payload$top = action.payload.top) !== null && _action$payload$top !== void 0 ? _action$payload$top : 0;
      state.margin.right = (_action$payload$right = action.payload.right) !== null && _action$payload$right !== void 0 ? _action$payload$right : 0;
      state.margin.bottom = (_action$payload$botto = action.payload.bottom) !== null && _action$payload$botto !== void 0 ? _action$payload$botto : 0;
      state.margin.left = (_action$payload$left = action.payload.left) !== null && _action$payload$left !== void 0 ? _action$payload$left : 0;
    },
    setScale(state, action) {
      state.scale = action.payload;
    }
  }
});
var {
  setMargin,
  setLayout,
  setChartSize,
  setScale
} = chartLayoutSlice.actions;
var chartLayoutReducer = chartLayoutSlice.reducer;
// EXTERNAL MODULE: ./node_modules/use-sync-external-store/shim/with-selector.js
var shim_with_selector = __webpack_require__(9242);
;// ./node_modules/recharts/es6/state/RechartsReduxContext.js


/*
 * This is a copy of the React-Redux context type, but with our own store type.
 * We could import directly from react-redux like this:
 * import { ReactReduxContextValue } from 'react-redux/src/components/Context';
 * but that makes typescript angry with some errors I am not sure how to resolve
 * so copy it is.
 */

/**
 * We need to use our own independent Redux context because we need to avoid interfering with other people's Redux stores
 * in case they decide to install and use Recharts in another Redux app which is likely to happen.
 *
 * https://react-redux.js.org/using-react-redux/accessing-store#providing-custom-context
 */
var RechartsReduxContext = /*#__PURE__*/(0,external_React_.createContext)(null);
;// ./node_modules/recharts/es6/state/hooks.js



var noopDispatch = a => a;
var useAppDispatch = () => {
  var context = (0,external_React_.useContext)(RechartsReduxContext);
  if (context) {
    return context.store.dispatch;
  }
  return noopDispatch;
};
var hooks_noop = () => {};
var addNestedSubNoop = () => hooks_noop;
var hooks_refEquality = (a, b) => a === b;

/**
 * This is a recharts variant of `useSelector` from 'react-redux' package.
 *
 * The difference is that react-redux version will throw an Error when used outside of Redux context.
 *
 * This, recharts version, will return undefined instead.
 *
 * This is because we want to allow using our components outside the Chart wrapper,
 * and have people provide all props explicitly.
 *
 * If however they use the component inside a chart wrapper then those props become optional,
 * and we read them from Redux state instead.
 *
 * @param selector for pulling things out of Redux store; will not be called if the store is not accessible
 * @return whatever the selector returned; or undefined when outside of Redux store
 */
function useAppSelector(selector) {
  var context = (0,external_React_.useContext)(RechartsReduxContext);
  var outOfContextSelector = (0,external_React_.useMemo)(() => {
    if (!context) {
      return hooks_noop;
    }
    return state => {
      if (state == null) {
        return undefined;
      }
      return selector(state);
    };
  }, [context, selector]);
  return (0,shim_with_selector.useSyncExternalStoreWithSelector)(context ? context.subscription.addNestedSub : addNestedSubNoop, context ? context.store.getState : hooks_noop, context ? context.store.getState : hooks_noop, outOfContextSelector, hooks_refEquality);
}
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/sortBy.js
var sortBy = __webpack_require__(184);
var sortBy_default = /*#__PURE__*/__webpack_require__.n(sortBy);
;// ./node_modules/recharts/es6/state/selectors/legendSelectors.js


var selectLegendSettings = state => state.legend.settings;
var selectLegendSize = state => state.legend.size;
var selectAllLegendPayload2DArray = state => state.legend.payload;
var selectLegendPayload = createSelector([selectAllLegendPayload2DArray, selectLegendSettings], (payloads, _ref) => {
  var {
    itemSorter
  } = _ref;
  var flat = payloads.flat(1);
  return itemSorter ? sortBy_default()(flat, itemSorter) : flat;
});
;// ./node_modules/d3-shape/src/offset/none.js
/* harmony default export */ function none(series, order) {
  if (!((n = series.length) > 1)) return;
  for (var i = 1, j, s0, s1 = series[order[0]], n, m = s1.length; i < n; ++i) {
    s0 = s1, s1 = series[order[i]];
    for (j = 0; j < m; ++j) {
      s1[j][1] += s1[j][0] = isNaN(s0[j][1]) ? s0[j][0] : s0[j][1];
    }
  }
}

;// ./node_modules/d3-shape/src/offset/expand.js


/* harmony default export */ function expand(series, order) {
  if (!((n = series.length) > 0)) return;
  for (var i, n, j = 0, m = series[0].length, y; j < m; ++j) {
    for (y = i = 0; i < n; ++i) y += series[i][j][1] || 0;
    if (y) for (i = 0; i < n; ++i) series[i][j][1] /= y;
  }
  none(series, order);
}

;// ./node_modules/d3-shape/src/offset/silhouette.js


/* harmony default export */ function silhouette(series, order) {
  if (!((n = series.length) > 0)) return;
  for (var j = 0, s0 = series[order[0]], n, m = s0.length; j < m; ++j) {
    for (var i = 0, y = 0; i < n; ++i) y += series[i][j][1] || 0;
    s0[j][1] += s0[j][0] = -y / 2;
  }
  none(series, order);
}

;// ./node_modules/d3-shape/src/offset/wiggle.js


/* harmony default export */ function wiggle(series, order) {
  if (!((n = series.length) > 0) || !((m = (s0 = series[order[0]]).length) > 0)) return;
  for (var y = 0, j = 1, s0, m, n; j < m; ++j) {
    for (var i = 0, s1 = 0, s2 = 0; i < n; ++i) {
      var si = series[order[i]],
          sij0 = si[j][1] || 0,
          sij1 = si[j - 1][1] || 0,
          s3 = (sij0 - sij1) / 2;
      for (var k = 0; k < i; ++k) {
        var sk = series[order[k]],
            skj0 = sk[j][1] || 0,
            skj1 = sk[j - 1][1] || 0;
        s3 += skj0 - skj1;
      }
      s1 += sij0, s2 += s3 * sij0;
    }
    s0[j - 1][1] += s0[j - 1][0] = y;
    if (s1) y -= s2 / s1;
  }
  s0[j - 1][1] += s0[j - 1][0] = y;
  none(series, order);
}

;// ./node_modules/d3-shape/src/array.js
var slice = Array.prototype.slice;

/* harmony default export */ function array(x) {
  return typeof x === "object" && "length" in x
    ? x // Array, TypedArray, NodeList, array-like
    : Array.from(x); // Map, Set, iterable, string, or anything else
}

;// ./node_modules/d3-shape/src/constant.js
/* harmony default export */ function constant(x) {
  return function constant() {
    return x;
  };
}

;// ./node_modules/d3-shape/src/order/none.js
/* harmony default export */ function order_none(series) {
  var n = series.length, o = new Array(n);
  while (--n >= 0) o[n] = n;
  return o;
}

;// ./node_modules/d3-shape/src/stack.js





function stackValue(d, key) {
  return d[key];
}

function stackSeries(key) {
  const series = [];
  series.key = key;
  return series;
}

/* harmony default export */ function src_stack() {
  var keys = constant([]),
      order = order_none,
      offset = none,
      value = stackValue;

  function stack(data) {
    var sz = Array.from(keys.apply(this, arguments), stackSeries),
        i, n = sz.length, j = -1,
        oz;

    for (const d of data) {
      for (i = 0, ++j; i < n; ++i) {
        (sz[i][j] = [0, +value(d, sz[i].key, j, data)]).data = d;
      }
    }

    for (i = 0, oz = array(order(sz)); i < n; ++i) {
      sz[oz[i]].index = i;
    }

    offset(sz, oz);
    return sz;
  }

  stack.keys = function(_) {
    return arguments.length ? (keys = typeof _ === "function" ? _ : constant(Array.from(_)), stack) : keys;
  };

  stack.value = function(_) {
    return arguments.length ? (value = typeof _ === "function" ? _ : constant(+_), stack) : value;
  };

  stack.order = function(_) {
    return arguments.length ? (order = _ == null ? order_none : typeof _ === "function" ? _ : constant(Array.from(_)), stack) : order;
  };

  stack.offset = function(_) {
    return arguments.length ? (offset = _ == null ? none : _, stack) : offset;
  };

  return stack;
}

;// ./node_modules/recharts/es6/util/getSliced.js
function getSliced(arr, startIndex, endIndex) {
  if (!Array.isArray(arr)) {
    return arr;
  }
  if (arr && startIndex + endIndex !== 0) {
    return arr.slice(startIndex, endIndex + 1);
  }
  return arr;
}
;// ./node_modules/recharts/es6/util/ChartUtils.js
function ChartUtils_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function ChartUtils_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ChartUtils_ownKeys(Object(t), !0).forEach(function (r) { ChartUtils_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ChartUtils_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function ChartUtils_defineProperty(e, r, t) { return (r = ChartUtils_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function ChartUtils_toPropertyKey(t) { var i = ChartUtils_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function ChartUtils_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }






function getValueByDataKey(obj, dataKey, defaultValue) {
  if (isNullish(obj) || isNullish(dataKey)) {
    return defaultValue;
  }
  if (isNumOrStr(dataKey)) {
    return get_default()(obj, dataKey, defaultValue);
  }
  if (typeof dataKey === 'function') {
    return dataKey(obj);
  }
  return defaultValue;
}
var appendOffsetOfLegend = (offset, legendSettings, legendSize) => {
  if (legendSettings && legendSize) {
    var {
      width: boxWidth,
      height: boxHeight
    } = legendSize;
    var {
      align,
      verticalAlign,
      layout
    } = legendSettings;
    if ((layout === 'vertical' || layout === 'horizontal' && verticalAlign === 'middle') && align !== 'center' && isNumber(offset[align])) {
      return ChartUtils_objectSpread(ChartUtils_objectSpread({}, offset), {}, {
        [align]: offset[align] + (boxWidth || 0)
      });
    }
    if ((layout === 'horizontal' || layout === 'vertical' && align === 'center') && verticalAlign !== 'middle' && isNumber(offset[verticalAlign])) {
      return ChartUtils_objectSpread(ChartUtils_objectSpread({}, offset), {}, {
        [verticalAlign]: offset[verticalAlign] + (boxHeight || 0)
      });
    }
  }
  return offset;
};
var isCategoricalAxis = (layout, axisType) => layout === 'horizontal' && axisType === 'xAxis' || layout === 'vertical' && axisType === 'yAxis' || layout === 'centric' && axisType === 'angleAxis' || layout === 'radial' && axisType === 'radiusAxis';

/**
 * Calculate the Coordinates of grid
 * @param  {Array} ticks           The ticks in axis
 * @param {Number} minValue        The minimum value of axis
 * @param {Number} maxValue        The maximum value of axis
 * @param {boolean} syncWithTicks  Synchronize grid lines with ticks or not
 * @return {Array}                 Coordinates
 */
var getCoordinatesOfGrid = (ticks, minValue, maxValue, syncWithTicks) => {
  if (syncWithTicks) {
    return ticks.map(entry => entry.coordinate);
  }
  var hasMin, hasMax;
  var values = ticks.map(entry => {
    if (entry.coordinate === minValue) {
      hasMin = true;
    }
    if (entry.coordinate === maxValue) {
      hasMax = true;
    }
    return entry.coordinate;
  });
  if (!hasMin) {
    values.push(minValue);
  }
  if (!hasMax) {
    values.push(maxValue);
  }
  return values;
};
/**
 * Of on four almost identical implementations of tick generation.
 * The four horsemen of tick generation are:
 * - {@link selectTooltipAxisTicks}
 * - {@link combineAxisTicks}
 * - {@link getTicksOfAxis}.
 * - {@link combineGraphicalItemTicks}
 */
var getTicksOfAxis = (axis, isGrid, isAll) => {
  if (!axis) {
    return null;
  }
  var {
    duplicateDomain,
    type,
    range,
    scale,
    realScaleType,
    isCategorical,
    categoricalDomain,
    tickCount,
    ticks,
    niceTicks,
    axisType
  } = axis;
  if (!scale) {
    return null;
  }
  var offsetForBand = realScaleType === 'scaleBand' && scale.bandwidth ? scale.bandwidth() / 2 : 2;
  var offset = (isGrid || isAll) && type === 'category' && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
  offset = axisType === 'angleAxis' && range && range.length >= 2 ? mathSign(range[0] - range[1]) * 2 * offset : offset;

  // The ticks set by user should only affect the ticks adjacent to axis line
  if (isGrid && (ticks || niceTicks)) {
    var result = (ticks || niceTicks || []).map((entry, index) => {
      var scaleContent = duplicateDomain ? duplicateDomain.indexOf(entry) : entry;
      var scaled = scale.map(scaleContent);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        // If the scaleContent is not a number, the coordinate will be NaN.
        // That could be the case for example with a PointScale and a string as domain.
        coordinate: scaled + offset,
        value: entry,
        offset,
        index
      };
    }).filter(isNotNil);
    return result;
  }

  // When axis is a categorical axis, but the type of axis is number or the scale of axis is not "auto"
  if (isCategorical && categoricalDomain) {
    return categoricalDomain.map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }
  if (scale.ticks && !isAll && tickCount != null) {
    return scale.ticks(tickCount).map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }

  // When axis has duplicated text, serial numbers are used to generate scale
  return scale.domain().map((entry, index) => {
    var scaled = scale.map(entry);
    if (!isWellBehavedNumber(scaled)) {
      return null;
    }
    return {
      coordinate: scaled + offset,
      // @ts-expect-error can't use Date as an index
      value: duplicateDomain ? duplicateDomain[entry] : entry,
      index,
      offset
    };
  }).filter(isNotNil);
};

/**
 * Both value and domain are tuples of two numbers
 * - but the type stays as array of numbers until we have better support in rest of the app
 * @param value input that will be truncated
 * @param domain boundaries
 * @returns tuple of two numbers
 */
var truncateByDomain = (value, domain) => {
  if (!domain || domain.length !== 2 || !isNumber(domain[0]) || !isNumber(domain[1])) {
    return value;
  }
  var minValue = Math.min(domain[0], domain[1]);
  var maxValue = Math.max(domain[0], domain[1]);
  var result = [value[0], value[1]];
  if (!isNumber(value[0]) || value[0] < minValue) {
    result[0] = minValue;
  }
  if (!isNumber(value[1]) || value[1] > maxValue) {
    result[1] = maxValue;
  }
  if (result[0] > maxValue) {
    result[0] = maxValue;
  }
  if (result[1] < minValue) {
    result[1] = minValue;
  }
  return result;
};

/**
 * Stacks all positive numbers above zero and all negative numbers below zero.
 *
 * If all values in the series are positive then this behaves the same as 'none' stacker.
 *
 * @param {Array} series from d3-shape Stack
 * @return {Array} series with applied offset
 */
var offsetSign = series => {
  var _series$;
  var n = series.length;
  if (n <= 0) {
    return;
  }
  var m = (_series$ = series[0]) === null || _series$ === void 0 ? void 0 : _series$.length;
  if (m == null || m <= 0) {
    return;
  }
  for (var j = 0; j < m; ++j) {
    var positive = 0;
    var negative = 0;
    for (var i = 0; i < n; ++i) {
      var row = series[i];
      var col = row === null || row === void 0 ? void 0 : row[j];
      if (col == null) {
        continue;
      }
      var series1 = col[1];
      var series0 = col[0];
      var value = isNan(series1) ? series0 : series1;
      if (value >= 0) {
        col[0] = positive;
        positive += value;
        col[1] = positive;
      } else {
        col[0] = negative;
        negative += value;
        col[1] = negative;
      }
    }
  }
};

/**
 * Replaces all negative values with zero when stacking data.
 *
 * If all values in the series are positive then this behaves the same as 'none' stacker.
 *
 * @param {Array} series from d3-shape Stack
 * @return {Array} series with applied offset
 */
var offsetPositive = series => {
  var _series$2;
  var n = series.length;
  if (n <= 0) {
    return;
  }
  var m = (_series$2 = series[0]) === null || _series$2 === void 0 ? void 0 : _series$2.length;
  if (m == null || m <= 0) {
    return;
  }
  for (var j = 0; j < m; ++j) {
    var positive = 0;
    for (var i = 0; i < n; ++i) {
      var row = series[i];
      var col = row === null || row === void 0 ? void 0 : row[j];
      if (col == null) {
        continue;
      }
      var value = isNan(col[1]) ? col[0] : col[1];
      if (value >= 0) {
        col[0] = positive;
        positive += value;
        col[1] = positive;
      } else {
        col[0] = 0;
        col[1] = 0;
      }
    }
  }
};

/**
 * Function type to compute offset for stacked data.
 *
 * d3-shape has something fishy going on with its types.
 * In @definitelytyped/d3-shape, this function (the offset accessor) is typed as Series<> => void.
 * However! When I actually open the storybook I can see that the offset accessor actually receives Array<Series<>>.
 * The same I can see in the source code itself:
 * https://github.com/DefinitelyTyped/DefinitelyTyped/discussions/66042
 * That one unfortunately has no types but we can tell it passes three-dimensional array.
 *
 * Which leads me to believe that definitelytyped is wrong on this one.
 * There's open discussion on this topic without much attention:
 * https://github.com/DefinitelyTyped/DefinitelyTyped/discussions/66042
 */

var STACK_OFFSET_MAP = {
  sign: offsetSign,
  // @ts-expect-error definitelytyped types are incorrect
  expand: expand,
  // @ts-expect-error definitelytyped types are incorrect
  none: none,
  // @ts-expect-error definitelytyped types are incorrect
  silhouette: silhouette,
  // @ts-expect-error definitelytyped types are incorrect
  wiggle: wiggle,
  positive: offsetPositive
};
var getStackedData = (data, dataKeys, offsetType) => {
  var _STACK_OFFSET_MAP$off;
  var offsetAccessor = (_STACK_OFFSET_MAP$off = STACK_OFFSET_MAP[offsetType]) !== null && _STACK_OFFSET_MAP$off !== void 0 ? _STACK_OFFSET_MAP$off : none;
  var stack = src_stack().keys(dataKeys).value((d, key) => Number(getValueByDataKey(d, key, 0))).order(order_none)
  // @ts-expect-error definitelytyped types are incorrect
  .offset(offsetAccessor);
  var result = stack(data);

  // Post-process ranged data: if value is an array of two numbers, use them directly without stacking
  result.forEach((series, seriesIndex) => {
    series.forEach((point, pointIndex) => {
      var value = getValueByDataKey(data[pointIndex], dataKeys[seriesIndex], 0);
      if (Array.isArray(value) && value.length === 2 && isNumber(value[0]) && isNumber(value[1])) {
        // eslint-disable-next-line prefer-destructuring,no-param-reassign
        point[0] = value[0];
        // eslint-disable-next-line prefer-destructuring,no-param-reassign
        point[1] = value[1];
      }
    });
  });
  return result;
};

/**
 * Externally, we accept both strings and numbers as stack IDs
 * @inline
 */

/**
 * Stack IDs in the external props allow numbers; but internally we use it as an object key
 * and object keys are always strings. Also, it would be kinda confusing if stackId=8 and stackId='8' were different stacks
 * so let's just force a string.
 */

function getNormalizedStackId(publicStackId) {
  return publicStackId == null ? undefined : String(publicStackId);
}
function getCateCoordinateOfLine(_ref) {
  var {
    axis,
    ticks,
    bandSize,
    entry,
    index,
    dataKey
  } = _ref;
  if (axis.type === 'category') {
    // find coordinate of category axis by the value of category
    // @ts-expect-error why does this use direct object access instead of getValueByDataKey?
    if (!axis.allowDuplicatedCategory && axis.dataKey && !isNullish(entry[axis.dataKey])) {
      // @ts-expect-error why does this use direct object access instead of getValueByDataKey?
      var matchedTick = findEntryInArray(ticks, 'value', entry[axis.dataKey]);
      if (matchedTick) {
        return matchedTick.coordinate + bandSize / 2;
      }
    }
    return ticks !== null && ticks !== void 0 && ticks[index] ? ticks[index].coordinate + bandSize / 2 : null;
  }
  var value = getValueByDataKey(entry, !isNullish(dataKey) ? dataKey : axis.dataKey);
  var scaled = axis.scale.map(value);
  if (!isNumber(scaled)) {
    return null;
  }
  return scaled;
}
var getCateCoordinateOfBar = _ref2 => {
  var {
    axis,
    ticks,
    offset,
    bandSize,
    entry,
    index
  } = _ref2;
  if (axis.type === 'category') {
    return ticks[index] ? ticks[index].coordinate + offset : null;
  }
  // @ts-expect-error getValueByDataKey does not validate the output type
  var value = getValueByDataKey(entry, axis.dataKey, axis.scale.domain()[index]);
  if (isNullish(value)) {
    return null;
  }
  var scaled = axis.scale.map(value);
  if (!isNumber(scaled)) {
    return null;
  }
  return scaled - bandSize / 2 + offset;
};
var getBaseValueOfBar = _ref3 => {
  var {
    numericAxis
  } = _ref3;
  var domain = numericAxis.scale.domain();
  if (numericAxis.type === 'number') {
    // @ts-expect-error type number means the domain has numbers in it but this relationship is not known to typescript
    var minValue = Math.min(domain[0], domain[1]);
    // @ts-expect-error type number means the domain has numbers in it but this relationship is not known to typescript
    var maxValue = Math.max(domain[0], domain[1]);
    if (minValue <= 0 && maxValue >= 0) {
      return 0;
    }
    if (maxValue < 0) {
      return maxValue;
    }
    return minValue;
  }
  return domain[0];
};
var getDomainOfSingle = data => {
  var flat = data.flat(2).filter(isNumber);
  return [Math.min(...flat), Math.max(...flat)];
};
var makeDomainFinite = domain => {
  return [domain[0] === Infinity ? 0 : domain[0], domain[1] === -Infinity ? 0 : domain[1]];
};
var getDomainOfStackGroups = (stackGroups, startIndex, endIndex) => {
  if (stackGroups == null) {
    return undefined;
  }
  return makeDomainFinite(Object.keys(stackGroups).reduce((result, stackId) => {
    var group = stackGroups[stackId];
    if (!group) {
      return result;
    }
    var {
      stackedData
    } = group;
    var domain = stackedData.reduce((res, entry) => {
      var sliced = getSliced(entry, startIndex, endIndex);
      var s = getDomainOfSingle(sliced);
      if (!isWellBehavedNumber(s[0]) || !isWellBehavedNumber(s[1])) {
        return res;
      }
      return [Math.min(res[0], s[0]), Math.max(res[1], s[1])];
    }, [Infinity, -Infinity]);
    return [Math.min(domain[0], result[0]), Math.max(domain[1], result[1])];
  }, [Infinity, -Infinity]));
};
var MIN_VALUE_REG = /^dataMin[\s]*-[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/;
var MAX_VALUE_REG = /^dataMax[\s]*\+[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/;

/**
 * Calculate the size between two category
 * @param  {Object} axis  The options of axis
 * @param  {Array}  ticks The ticks of axis
 * @param  {Boolean} isBar if items in axis are bars
 * @return {Number} Size
 */
var getBandSizeOfAxis = (axis, ticks, isBar) => {
  if (axis && axis.scale && axis.scale.bandwidth) {
    var bandWidth = axis.scale.bandwidth();
    if (!isBar || bandWidth > 0) {
      return bandWidth;
    }
  }
  if (axis && ticks && ticks.length >= 2) {
    var orderedTicks = sortBy_default()(ticks, o => o.coordinate);
    var bandSize = Infinity;
    for (var i = 1, len = orderedTicks.length; i < len; i++) {
      var cur = orderedTicks[i];
      var prev = orderedTicks[i - 1];
      bandSize = Math.min(((cur === null || cur === void 0 ? void 0 : cur.coordinate) || 0) - ((prev === null || prev === void 0 ? void 0 : prev.coordinate) || 0), bandSize);
    }
    return bandSize === Infinity ? 0 : bandSize;
  }
  return isBar ? undefined : 0;
};
function getTooltipEntry(_ref4) {
  var {
    tooltipEntrySettings,
    dataKey,
    payload,
    value,
    name
  } = _ref4;
  return ChartUtils_objectSpread(ChartUtils_objectSpread({}, tooltipEntrySettings), {}, {
    dataKey,
    payload,
    value,
    name
  });
}
function getTooltipNameProp(nameFromItem, dataKey) {
  if (nameFromItem) {
    return String(nameFromItem);
  }
  if (typeof dataKey === 'string') {
    return dataKey;
  }
  return undefined;
}
var calculateCartesianTooltipPos = (coordinate, layout) => {
  if (layout === 'horizontal') {
    return coordinate.chartX;
  }
  if (layout === 'vertical') {
    return coordinate.chartY;
  }
  return undefined;
};
var calculatePolarTooltipPos = (rangeObj, layout) => {
  if (layout === 'centric') {
    return rangeObj.angle;
  }
  return rangeObj.radius;
};
;// ./node_modules/recharts/es6/state/selectors/containerSelectors.js
var selectChartWidth = state => state.layout.width;
var selectChartHeight = state => state.layout.height;
var selectContainerScale = state => state.layout.scale;
var selectMargin = state => state.layout.margin;
;// ./node_modules/recharts/es6/state/selectors/selectAllAxes.js

var selectAllXAxes = createSelector(state => state.cartesianAxis.xAxis, xAxisMap => {
  return Object.values(xAxisMap);
});
var selectAllYAxes = createSelector(state => state.cartesianAxis.yAxis, yAxisMap => {
  return Object.values(yAxisMap);
});
;// ./node_modules/recharts/es6/util/Constants.js
var COLOR_PANEL = ['#1890FF', '#66B5FF', '#41D9C7', '#2FC25B', '#6EDB8F', '#9AE65C', '#FACC14', '#E6965C', '#57AD71', '#223273', '#738AE6', '#7564CC', '#8543E0', '#A877ED', '#5C8EE6', '#13C2C2', '#70E0E0', '#5CA3E6', '#3436C7', '#8082FF', '#DD81E6', '#F04864', '#FA7D92', '#D598D9'];

/**
 * We use this attribute to identify which element is the one that the user is touching.
 * The index is the position of the element in the data array.
 * This can be either a number (for array-based charts) or a string (for the charts that have a matrix-shaped data).
 */
var DATA_ITEM_INDEX_ATTRIBUTE_NAME = 'data-recharts-item-index';

/**
 * We use this attribute to identify which element is the one that the user is touching.
 * Unlike dataKey, or name, it is always unique.
 */
var DATA_ITEM_GRAPHICAL_ITEM_ID_ATTRIBUTE_NAME = 'data-recharts-item-id';
var DEFAULT_Y_AXIS_WIDTH = 60;
;// ./node_modules/recharts/es6/state/selectors/selectChartOffsetInternal.js
function selectChartOffsetInternal_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function selectChartOffsetInternal_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? selectChartOffsetInternal_ownKeys(Object(t), !0).forEach(function (r) { selectChartOffsetInternal_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : selectChartOffsetInternal_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function selectChartOffsetInternal_defineProperty(e, r, t) { return (r = selectChartOffsetInternal_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function selectChartOffsetInternal_toPropertyKey(t) { var i = selectChartOffsetInternal_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function selectChartOffsetInternal_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }






var selectBrushHeight = state => state.brush.height;
function selectLeftAxesOffset(state) {
  var yAxes = selectAllYAxes(state);
  return yAxes.reduce((result, entry) => {
    if (entry.orientation === 'left' && !entry.mirror && !entry.hide) {
      var width = typeof entry.width === 'number' ? entry.width : DEFAULT_Y_AXIS_WIDTH;
      return result + width;
    }
    return result;
  }, 0);
}
function selectRightAxesOffset(state) {
  var yAxes = selectAllYAxes(state);
  return yAxes.reduce((result, entry) => {
    if (entry.orientation === 'right' && !entry.mirror && !entry.hide) {
      var width = typeof entry.width === 'number' ? entry.width : DEFAULT_Y_AXIS_WIDTH;
      return result + width;
    }
    return result;
  }, 0);
}
function selectTopAxesOffset(state) {
  var xAxes = selectAllXAxes(state);
  return xAxes.reduce((result, entry) => {
    if (entry.orientation === 'top' && !entry.mirror && !entry.hide) {
      return result + entry.height;
    }
    return result;
  }, 0);
}
function selectBottomAxesOffset(state) {
  var xAxes = selectAllXAxes(state);
  return xAxes.reduce((result, entry) => {
    if (entry.orientation === 'bottom' && !entry.mirror && !entry.hide) {
      return result + entry.height;
    }
    return result;
  }, 0);
}

/**
 * For internal use only.
 *
 * @param root state
 * @return ChartOffsetInternal
 */
var selectChartOffsetInternal = createSelector([selectChartWidth, selectChartHeight, selectMargin, selectBrushHeight, selectLeftAxesOffset, selectRightAxesOffset, selectTopAxesOffset, selectBottomAxesOffset, selectLegendSettings, selectLegendSize], (chartWidth, chartHeight, margin, brushHeight, leftAxesOffset, rightAxesOffset, topAxesOffset, bottomAxesOffset, legendSettings, legendSize) => {
  var offsetH = {
    left: (margin.left || 0) + leftAxesOffset,
    right: (margin.right || 0) + rightAxesOffset
  };
  var offsetV = {
    top: (margin.top || 0) + topAxesOffset,
    bottom: (margin.bottom || 0) + bottomAxesOffset
  };
  var offset = selectChartOffsetInternal_objectSpread(selectChartOffsetInternal_objectSpread({}, offsetV), offsetH);
  var brushBottom = offset.bottom;
  offset.bottom += brushHeight;
  offset = appendOffsetOfLegend(offset, legendSettings, legendSize);
  var offsetWidth = chartWidth - offset.left - offset.right;
  var offsetHeight = chartHeight - offset.top - offset.bottom;
  return selectChartOffsetInternal_objectSpread(selectChartOffsetInternal_objectSpread({
    brushBottom
  }, offset), {}, {
    // never return negative values for height and width
    width: Math.max(offsetWidth, 0),
    height: Math.max(offsetHeight, 0)
  });
});
var selectChartViewBox = createSelector(selectChartOffsetInternal, offset => ({
  x: offset.left,
  y: offset.top,
  width: offset.width,
  height: offset.height
}));
var selectAxisViewBox = createSelector(selectChartWidth, selectChartHeight, (width, height) => ({
  x: 0,
  y: 0,
  width,
  height
}));
;// ./node_modules/recharts/es6/context/PanoramaContext.js


var PanoramaContext = /*#__PURE__*/(0,external_React_.createContext)(null);
var useIsPanorama = () => (0,external_React_.useContext)(PanoramaContext) != null;
var PanoramaContextProvider = _ref => {
  var {
    children
  } = _ref;
  return /*#__PURE__*/external_React_.createElement(PanoramaContext.Provider, {
    value: true
  }, children);
};
;// ./node_modules/recharts/es6/state/selectors/brushSelectors.js




var selectBrushSettings = state => state.brush;
var selectBrushDimensions = createSelector([selectBrushSettings, selectChartOffsetInternal, selectMargin], (brushSettings, offset, margin) => ({
  height: brushSettings.height,
  x: isNumber(brushSettings.x) ? brushSettings.x : offset.left,
  y: isNumber(brushSettings.y) ? brushSettings.y : offset.top + offset.height + offset.brushBottom - ((margin === null || margin === void 0 ? void 0 : margin.bottom) || 0),
  width: isNumber(brushSettings.width) ? brushSettings.width : offset.width
}));
;// ./node_modules/recharts/es6/context/chartLayoutContext.js









function cartesianViewBoxToTrapezoid(box) {
  if (!box) {
    return undefined;
  }
  return {
    x: box.x,
    y: box.y,
    upperWidth: 'upperWidth' in box ? box.upperWidth : box.width,
    lowerWidth: 'lowerWidth' in box ? box.lowerWidth : box.width,
    width: box.width,
    height: box.height
  };
}
var useViewBox = () => {
  var _useAppSelector;
  var panorama = useIsPanorama();
  var rootViewBox = useAppSelector(selectChartViewBox);
  var brushDimensions = useAppSelector(selectBrushDimensions);
  var brushPadding = (_useAppSelector = useAppSelector(selectBrushSettings)) === null || _useAppSelector === void 0 ? void 0 : _useAppSelector.padding;
  if (!panorama || !brushDimensions || !brushPadding) {
    return rootViewBox;
  }
  return {
    width: brushDimensions.width - brushPadding.left - brushPadding.right,
    height: brushDimensions.height - brushPadding.top - brushPadding.bottom,
    x: brushPadding.left,
    y: brushPadding.top
  };
};
var manyComponentsThrowErrorsIfOffsetIsUndefined = {
  top: 0,
  bottom: 0,
  left: 0,
  right: 0,
  width: 0,
  height: 0,
  brushBottom: 0
};
/**
 * For internal use only. If you want this information, `import { useOffset } from 'recharts'` instead.
 *
 * Returns the offset of the chart in pixels.
 *
 * @returns {ChartOffsetInternal} The offset of the chart in pixels, or a default value if not in a chart context.
 */
var useOffsetInternal = () => {
  var _useAppSelector2;
  return (_useAppSelector2 = useAppSelector(selectChartOffsetInternal)) !== null && _useAppSelector2 !== void 0 ? _useAppSelector2 : manyComponentsThrowErrorsIfOffsetIsUndefined;
};

/**
 * Returns the width of the chart in pixels.
 *
 * If you are using chart with hardcoded `width` prop, then the width returned will be the same
 * as the `width` prop on the main chart element.
 *
 * If you are using a chart with a `ResponsiveContainer`, the width will be the size of the chart
 * as the ResponsiveContainer has decided it would be.
 *
 * If the chart has any axes or legend, the `width` will be the size of the chart
 * including the axes and legend. Meaning: adding axes and legend will not change the width.
 *
 * The dimensions do not scale, meaning as user zoom in and out, the width number will not change
 * as the chart gets visually larger or smaller.
 *
 * Returns `undefined` if used outside a chart context.
 *
 * @returns {number | undefined} The width of the chart in pixels, or `undefined` if not in a chart context.
 */
var useChartWidth = () => {
  return useAppSelector(selectChartWidth);
};

/**
 * Returns the height of the chart in pixels.
 *
 * If you are using chart with hardcoded `height` props, then the height returned will be the same
 * as the `height` prop on the main chart element.
 *
 * If you are using a chart with a `ResponsiveContainer`, the height will be the size of the chart
 * as the ResponsiveContainer has decided it would be.
 *
 * If the chart has any axes or legend, the `height` will be the size of the chart
 * including the axes and legend. Meaning: adding axes and legend will not change the height.
 *
 * The dimensions do not scale, meaning as user zoom in and out, the height number will not change
 * as the chart gets visually larger or smaller.
 *
 * Returns `undefined` if used outside a chart context.
 *
 * @returns {number | undefined} The height of the chart in pixels, or `undefined` if not in a chart context.
 */
var useChartHeight = () => {
  return useAppSelector(selectChartHeight);
};

/**
 * Margin is the empty space around the chart. Excludes axes and legend and brushes and the like.
 * This is declared by the user in the chart props.
 * If you are interested in the space occupied by axes, legend, or brushes,
 * use {@link useOffset} instead, which also includes calculated widths and heights of axes and legends.
 *
 * Returns `undefined` if used outside a chart context.
 *
 * @returns {Margin | undefined} The margin of the chart in pixels, or `undefined` if not in a chart context.
 */
var useMargin = () => {
  return useAppSelector(state => state.layout.margin);
};
var selectChartLayout = state => state.layout.layoutType;
var useChartLayout = () => useAppSelector(selectChartLayout);
var useCartesianChartLayout = () => {
  var layout = useChartLayout();
  if (layout === 'horizontal' || layout === 'vertical') {
    return layout;
  }
  return undefined;
};
var selectPolarChartLayout = state => {
  var layout = state.layout.layoutType;
  if (layout === 'centric' || layout === 'radial') {
    return layout;
  }
  return undefined;
};
var usePolarChartLayout = () => {
  return useAppSelector(selectPolarChartLayout);
};

/**
 * Returns true if the component is rendered inside a chart context.
 * Some components may be used both inside and outside of charts,
 * and this hook allows them to determine if they are in a chart context or not.
 *
 * Other selectors may return undefined when used outside a chart context,
 * or undefined when inside a chart, but without relevant data.
 * This hook provides a more explicit way to check for chart context.
 *
 * @returns {boolean} True if in chart context, false otherwise.
 */
var useIsInChartContext = () => {
  /*
   * All charts provide a layout type in the chart context.
   * If we have a layout type, we are in a chart context.
   */
  var layout = useChartLayout();
  return layout !== undefined;
};
var ReportChartSize = props => {
  var dispatch = useAppDispatch();

  /*
   * Skip dispatching properties in panorama chart for two reasons:
   * 1. The root chart should be deciding on these properties, and
   * 2. Brush reads these properties from redux store, and so they must remain stable
   *      to avoid circular dependency and infinite re-rendering.
   */
  var isPanorama = useIsPanorama();
  var {
    width: widthFromProps,
    height: heightFromProps
  } = props;
  var responsiveContainerCalculations = useResponsiveContainerContext();
  var width = widthFromProps;
  var height = heightFromProps;
  if (responsiveContainerCalculations) {
    /*
     * In case we receive width and height from ResponsiveContainer,
     * we will always prefer those.
     * Only in case ResponsiveContainer does not provide width or height,
     * we will fall back to the explicitly provided width and height.
     *
     * This to me feels backwards - we should allow override by the more specific props on individual charts, right?
     * But this is 3.x behaviour, so let's keep it for backwards compatibility.
     *
     * We can change this in 4.x if we want to.
     */
    width = responsiveContainerCalculations.width > 0 ? responsiveContainerCalculations.width : widthFromProps;
    height = responsiveContainerCalculations.height > 0 ? responsiveContainerCalculations.height : heightFromProps;
  }
  (0,external_React_.useEffect)(() => {
    if (!isPanorama && isPositiveNumber(width) && isPositiveNumber(height)) {
      dispatch(setChartSize({
        width,
        height
      }));
    }
  }, [dispatch, isPanorama, width, height]);
  return null;
};
var ReportChartMargin = _ref => {
  var {
    margin
  } = _ref;
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(setMargin(margin));
  }, [dispatch, margin]);
  return null;
};
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/range.js
var range = __webpack_require__(3412);
var range_default = /*#__PURE__*/__webpack_require__.n(range);
;// ./node_modules/d3-array/src/range.js
function range_range(start, stop, step) {
  start = +start, stop = +stop, step = (n = arguments.length) < 2 ? (stop = start, start = 0, 1) : n < 3 ? 1 : +step;

  var i = -1,
      n = Math.max(0, Math.ceil((stop - start) / step)) | 0,
      range = new Array(n);

  while (++i < n) {
    range[i] = start + i * step;
  }

  return range;
}

;// ./node_modules/d3-scale/src/init.js
function initRange(domain, range) {
  switch (arguments.length) {
    case 0: break;
    case 1: this.range(domain); break;
    default: this.range(range).domain(domain); break;
  }
  return this;
}

function initInterpolator(domain, interpolator) {
  switch (arguments.length) {
    case 0: break;
    case 1: {
      if (typeof domain === "function") this.interpolator(domain);
      else this.range(domain);
      break;
    }
    default: {
      this.domain(domain);
      if (typeof interpolator === "function") this.interpolator(interpolator);
      else this.range(interpolator);
      break;
    }
  }
  return this;
}

;// ./node_modules/internmap/src/index.js
class InternMap extends Map {
  constructor(entries, key = keyof) {
    super();
    Object.defineProperties(this, {_intern: {value: new Map()}, _key: {value: key}});
    if (entries != null) for (const [key, value] of entries) this.set(key, value);
  }
  get(key) {
    return super.get(intern_get(this, key));
  }
  has(key) {
    return super.has(intern_get(this, key));
  }
  set(key, value) {
    return super.set(intern_set(this, key), value);
  }
  delete(key) {
    return super.delete(intern_delete(this, key));
  }
}

class InternSet extends Set {
  constructor(values, key = keyof) {
    super();
    Object.defineProperties(this, {_intern: {value: new Map()}, _key: {value: key}});
    if (values != null) for (const value of values) this.add(value);
  }
  has(value) {
    return super.has(intern_get(this, value));
  }
  add(value) {
    return super.add(intern_set(this, value));
  }
  delete(value) {
    return super.delete(intern_delete(this, value));
  }
}

function intern_get({_intern, _key}, value) {
  const key = _key(value);
  return _intern.has(key) ? _intern.get(key) : value;
}

function intern_set({_intern, _key}, value) {
  const key = _key(value);
  if (_intern.has(key)) return _intern.get(key);
  _intern.set(key, value);
  return value;
}

function intern_delete({_intern, _key}, value) {
  const key = _key(value);
  if (_intern.has(key)) {
    value = _intern.get(key);
    _intern.delete(key);
  }
  return value;
}

function keyof(value) {
  return value !== null && typeof value === "object" ? value.valueOf() : value;
}

;// ./node_modules/d3-scale/src/ordinal.js



const implicit = Symbol("implicit");

function ordinal() {
  var index = new InternMap(),
      domain = [],
      range = [],
      unknown = implicit;

  function scale(d) {
    let i = index.get(d);
    if (i === undefined) {
      if (unknown !== implicit) return unknown;
      index.set(d, i = domain.push(d) - 1);
    }
    return range[i % range.length];
  }

  scale.domain = function(_) {
    if (!arguments.length) return domain.slice();
    domain = [], index = new InternMap();
    for (const value of _) {
      if (index.has(value)) continue;
      index.set(value, domain.push(value) - 1);
    }
    return scale;
  };

  scale.range = function(_) {
    return arguments.length ? (range = Array.from(_), scale) : range.slice();
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  scale.copy = function() {
    return ordinal(domain, range).unknown(unknown);
  };

  initRange.apply(scale, arguments);

  return scale;
}

;// ./node_modules/d3-scale/src/band.js




function band() {
  var scale = ordinal().unknown(undefined),
      domain = scale.domain,
      ordinalRange = scale.range,
      r0 = 0,
      r1 = 1,
      step,
      bandwidth,
      round = false,
      paddingInner = 0,
      paddingOuter = 0,
      align = 0.5;

  delete scale.unknown;

  function rescale() {
    var n = domain().length,
        reverse = r1 < r0,
        start = reverse ? r1 : r0,
        stop = reverse ? r0 : r1;
    step = (stop - start) / Math.max(1, n - paddingInner + paddingOuter * 2);
    if (round) step = Math.floor(step);
    start += (stop - start - step * (n - paddingInner)) * align;
    bandwidth = step * (1 - paddingInner);
    if (round) start = Math.round(start), bandwidth = Math.round(bandwidth);
    var values = range_range(n).map(function(i) { return start + step * i; });
    return ordinalRange(reverse ? values.reverse() : values);
  }

  scale.domain = function(_) {
    return arguments.length ? (domain(_), rescale()) : domain();
  };

  scale.range = function(_) {
    return arguments.length ? ([r0, r1] = _, r0 = +r0, r1 = +r1, rescale()) : [r0, r1];
  };

  scale.rangeRound = function(_) {
    return [r0, r1] = _, r0 = +r0, r1 = +r1, round = true, rescale();
  };

  scale.bandwidth = function() {
    return bandwidth;
  };

  scale.step = function() {
    return step;
  };

  scale.round = function(_) {
    return arguments.length ? (round = !!_, rescale()) : round;
  };

  scale.padding = function(_) {
    return arguments.length ? (paddingInner = Math.min(1, paddingOuter = +_), rescale()) : paddingInner;
  };

  scale.paddingInner = function(_) {
    return arguments.length ? (paddingInner = Math.min(1, _), rescale()) : paddingInner;
  };

  scale.paddingOuter = function(_) {
    return arguments.length ? (paddingOuter = +_, rescale()) : paddingOuter;
  };

  scale.align = function(_) {
    return arguments.length ? (align = Math.max(0, Math.min(1, _)), rescale()) : align;
  };

  scale.copy = function() {
    return band(domain(), [r0, r1])
        .round(round)
        .paddingInner(paddingInner)
        .paddingOuter(paddingOuter)
        .align(align);
  };

  return initRange.apply(rescale(), arguments);
}

function pointish(scale) {
  var copy = scale.copy;

  scale.padding = scale.paddingOuter;
  delete scale.paddingInner;
  delete scale.paddingOuter;

  scale.copy = function() {
    return pointish(copy());
  };

  return scale;
}

function point() {
  return pointish(band.apply(null, arguments).paddingInner(1));
}

;// ./node_modules/d3-array/src/ticks.js
const e10 = Math.sqrt(50),
    e5 = Math.sqrt(10),
    e2 = Math.sqrt(2);

function tickSpec(start, stop, count) {
  const step = (stop - start) / Math.max(0, count),
      power = Math.floor(Math.log10(step)),
      error = step / Math.pow(10, power),
      factor = error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1;
  let i1, i2, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start * inc);
    i2 = Math.round(stop * inc);
    if (i1 / inc < start) ++i1;
    if (i2 / inc > stop) --i2;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start / inc);
    i2 = Math.round(stop / inc);
    if (i1 * inc < start) ++i1;
    if (i2 * inc > stop) --i2;
  }
  if (i2 < i1 && 0.5 <= count && count < 2) return tickSpec(start, stop, count * 2);
  return [i1, i2, inc];
}

function ticks(start, stop, count) {
  stop = +stop, start = +start, count = +count;
  if (!(count > 0)) return [];
  if (start === stop) return [start];
  const reverse = stop < start, [i1, i2, inc] = reverse ? tickSpec(stop, start, count) : tickSpec(start, stop, count);
  if (!(i2 >= i1)) return [];
  const n = i2 - i1 + 1, ticks = new Array(n);
  if (reverse) {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks[i] = (i2 - i) / -inc;
    else for (let i = 0; i < n; ++i) ticks[i] = (i2 - i) * inc;
  } else {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks[i] = (i1 + i) / -inc;
    else for (let i = 0; i < n; ++i) ticks[i] = (i1 + i) * inc;
  }
  return ticks;
}

function tickIncrement(start, stop, count) {
  stop = +stop, start = +start, count = +count;
  return tickSpec(start, stop, count)[2];
}

function tickStep(start, stop, count) {
  stop = +stop, start = +start, count = +count;
  const reverse = stop < start, inc = reverse ? tickIncrement(stop, start, count) : tickIncrement(start, stop, count);
  return (reverse ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

;// ./node_modules/d3-array/src/ascending.js
function ascending(a, b) {
  return a == null || b == null ? NaN : a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
}

;// ./node_modules/d3-array/src/descending.js
function descending(a, b) {
  return a == null || b == null ? NaN
    : b < a ? -1
    : b > a ? 1
    : b >= a ? 0
    : NaN;
}

;// ./node_modules/d3-array/src/bisector.js



function bisector(f) {
  let compare1, compare2, delta;

  // If an accessor is specified, promote it to a comparator. In this case we
  // can test whether the search value is (self-) comparable. We can’t do this
  // for a comparator (except for specific, known comparators) because we can’t
  // tell if the comparator is symmetric, and an asymmetric comparator can’t be
  // used to test whether a single value is comparable.
  if (f.length !== 2) {
    compare1 = ascending;
    compare2 = (d, x) => ascending(f(d), x);
    delta = (d, x) => f(d) - x;
  } else {
    compare1 = f === ascending || f === descending ? f : zero;
    compare2 = f;
    delta = f;
  }

  function left(a, x, lo = 0, hi = a.length) {
    if (lo < hi) {
      if (compare1(x, x) !== 0) return hi;
      do {
        const mid = (lo + hi) >>> 1;
        if (compare2(a[mid], x) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }

  function right(a, x, lo = 0, hi = a.length) {
    if (lo < hi) {
      if (compare1(x, x) !== 0) return hi;
      do {
        const mid = (lo + hi) >>> 1;
        if (compare2(a[mid], x) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }

  function center(a, x, lo = 0, hi = a.length) {
    const i = left(a, x, lo, hi - 1);
    return i > lo && delta(a[i - 1], x) > -delta(a[i], x) ? i - 1 : i;
  }

  return {left, center, right};
}

function zero() {
  return 0;
}

;// ./node_modules/d3-array/src/number.js
function number(x) {
  return x === null ? NaN : +x;
}

function* numbers(values, valueof) {
  if (valueof === undefined) {
    for (let value of values) {
      if (value != null && (value = +value) >= value) {
        yield value;
      }
    }
  } else {
    let index = -1;
    for (let value of values) {
      if ((value = valueof(value, ++index, values)) != null && (value = +value) >= value) {
        yield value;
      }
    }
  }
}

;// ./node_modules/d3-array/src/bisect.js




const ascendingBisect = bisector(ascending);
const bisectRight = ascendingBisect.right;
const bisectLeft = ascendingBisect.left;
const bisectCenter = bisector(number).center;
/* harmony default export */ const bisect = (bisectRight);

;// ./node_modules/d3-color/src/define.js
/* harmony default export */ function src_define(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}

function extend(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

;// ./node_modules/d3-color/src/color.js


function Color() {}

var darker = 0.7;
var brighter = 1 / darker;

var reI = "\\s*([+-]?\\d+)\\s*",
    reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*",
    reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*",
    reHex = /^#([0-9a-f]{3,8})$/,
    reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`),
    reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`),
    reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`),
    reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`),
    reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`),
    reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);

var named = {
  aliceblue: 0xf0f8ff,
  antiquewhite: 0xfaebd7,
  aqua: 0x00ffff,
  aquamarine: 0x7fffd4,
  azure: 0xf0ffff,
  beige: 0xf5f5dc,
  bisque: 0xffe4c4,
  black: 0x000000,
  blanchedalmond: 0xffebcd,
  blue: 0x0000ff,
  blueviolet: 0x8a2be2,
  brown: 0xa52a2a,
  burlywood: 0xdeb887,
  cadetblue: 0x5f9ea0,
  chartreuse: 0x7fff00,
  chocolate: 0xd2691e,
  coral: 0xff7f50,
  cornflowerblue: 0x6495ed,
  cornsilk: 0xfff8dc,
  crimson: 0xdc143c,
  cyan: 0x00ffff,
  darkblue: 0x00008b,
  darkcyan: 0x008b8b,
  darkgoldenrod: 0xb8860b,
  darkgray: 0xa9a9a9,
  darkgreen: 0x006400,
  darkgrey: 0xa9a9a9,
  darkkhaki: 0xbdb76b,
  darkmagenta: 0x8b008b,
  darkolivegreen: 0x556b2f,
  darkorange: 0xff8c00,
  darkorchid: 0x9932cc,
  darkred: 0x8b0000,
  darksalmon: 0xe9967a,
  darkseagreen: 0x8fbc8f,
  darkslateblue: 0x483d8b,
  darkslategray: 0x2f4f4f,
  darkslategrey: 0x2f4f4f,
  darkturquoise: 0x00ced1,
  darkviolet: 0x9400d3,
  deeppink: 0xff1493,
  deepskyblue: 0x00bfff,
  dimgray: 0x696969,
  dimgrey: 0x696969,
  dodgerblue: 0x1e90ff,
  firebrick: 0xb22222,
  floralwhite: 0xfffaf0,
  forestgreen: 0x228b22,
  fuchsia: 0xff00ff,
  gainsboro: 0xdcdcdc,
  ghostwhite: 0xf8f8ff,
  gold: 0xffd700,
  goldenrod: 0xdaa520,
  gray: 0x808080,
  green: 0x008000,
  greenyellow: 0xadff2f,
  grey: 0x808080,
  honeydew: 0xf0fff0,
  hotpink: 0xff69b4,
  indianred: 0xcd5c5c,
  indigo: 0x4b0082,
  ivory: 0xfffff0,
  khaki: 0xf0e68c,
  lavender: 0xe6e6fa,
  lavenderblush: 0xfff0f5,
  lawngreen: 0x7cfc00,
  lemonchiffon: 0xfffacd,
  lightblue: 0xadd8e6,
  lightcoral: 0xf08080,
  lightcyan: 0xe0ffff,
  lightgoldenrodyellow: 0xfafad2,
  lightgray: 0xd3d3d3,
  lightgreen: 0x90ee90,
  lightgrey: 0xd3d3d3,
  lightpink: 0xffb6c1,
  lightsalmon: 0xffa07a,
  lightseagreen: 0x20b2aa,
  lightskyblue: 0x87cefa,
  lightslategray: 0x778899,
  lightslategrey: 0x778899,
  lightsteelblue: 0xb0c4de,
  lightyellow: 0xffffe0,
  lime: 0x00ff00,
  limegreen: 0x32cd32,
  linen: 0xfaf0e6,
  magenta: 0xff00ff,
  maroon: 0x800000,
  mediumaquamarine: 0x66cdaa,
  mediumblue: 0x0000cd,
  mediumorchid: 0xba55d3,
  mediumpurple: 0x9370db,
  mediumseagreen: 0x3cb371,
  mediumslateblue: 0x7b68ee,
  mediumspringgreen: 0x00fa9a,
  mediumturquoise: 0x48d1cc,
  mediumvioletred: 0xc71585,
  midnightblue: 0x191970,
  mintcream: 0xf5fffa,
  mistyrose: 0xffe4e1,
  moccasin: 0xffe4b5,
  navajowhite: 0xffdead,
  navy: 0x000080,
  oldlace: 0xfdf5e6,
  olive: 0x808000,
  olivedrab: 0x6b8e23,
  orange: 0xffa500,
  orangered: 0xff4500,
  orchid: 0xda70d6,
  palegoldenrod: 0xeee8aa,
  palegreen: 0x98fb98,
  paleturquoise: 0xafeeee,
  palevioletred: 0xdb7093,
  papayawhip: 0xffefd5,
  peachpuff: 0xffdab9,
  peru: 0xcd853f,
  pink: 0xffc0cb,
  plum: 0xdda0dd,
  powderblue: 0xb0e0e6,
  purple: 0x800080,
  rebeccapurple: 0x663399,
  red: 0xff0000,
  rosybrown: 0xbc8f8f,
  royalblue: 0x4169e1,
  saddlebrown: 0x8b4513,
  salmon: 0xfa8072,
  sandybrown: 0xf4a460,
  seagreen: 0x2e8b57,
  seashell: 0xfff5ee,
  sienna: 0xa0522d,
  silver: 0xc0c0c0,
  skyblue: 0x87ceeb,
  slateblue: 0x6a5acd,
  slategray: 0x708090,
  slategrey: 0x708090,
  snow: 0xfffafa,
  springgreen: 0x00ff7f,
  steelblue: 0x4682b4,
  tan: 0xd2b48c,
  teal: 0x008080,
  thistle: 0xd8bfd8,
  tomato: 0xff6347,
  turquoise: 0x40e0d0,
  violet: 0xee82ee,
  wheat: 0xf5deb3,
  white: 0xffffff,
  whitesmoke: 0xf5f5f5,
  yellow: 0xffff00,
  yellowgreen: 0x9acd32
};

src_define(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor, this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex, // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});

function color_formatHex() {
  return this.rgb().formatHex();
}

function color_formatHex8() {
  return this.rgb().formatHex8();
}

function color_formatHsl() {
  return hslConvert(this).formatHsl();
}

function color_formatRgb() {
  return this.rgb().formatRgb();
}

function color(format) {
  var m, l;
  format = (format + "").trim().toLowerCase();
  return (m = reHex.exec(format)) ? (l = m[1].length, m = parseInt(m[1], 16), l === 6 ? rgbn(m) // #ff0000
      : l === 3 ? new Rgb((m >> 8 & 0xf) | (m >> 4 & 0xf0), (m >> 4 & 0xf) | (m & 0xf0), ((m & 0xf) << 4) | (m & 0xf), 1) // #f00
      : l === 8 ? rgba(m >> 24 & 0xff, m >> 16 & 0xff, m >> 8 & 0xff, (m & 0xff) / 0xff) // #ff000000
      : l === 4 ? rgba((m >> 12 & 0xf) | (m >> 8 & 0xf0), (m >> 8 & 0xf) | (m >> 4 & 0xf0), (m >> 4 & 0xf) | (m & 0xf0), (((m & 0xf) << 4) | (m & 0xf)) / 0xff) // #f000
      : null) // invalid hex
      : (m = reRgbInteger.exec(format)) ? new Rgb(m[1], m[2], m[3], 1) // rgb(255, 0, 0)
      : (m = reRgbPercent.exec(format)) ? new Rgb(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, 1) // rgb(100%, 0%, 0%)
      : (m = reRgbaInteger.exec(format)) ? rgba(m[1], m[2], m[3], m[4]) // rgba(255, 0, 0, 1)
      : (m = reRgbaPercent.exec(format)) ? rgba(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, m[4]) // rgb(100%, 0%, 0%, 1)
      : (m = reHslPercent.exec(format)) ? hsla(m[1], m[2] / 100, m[3] / 100, 1) // hsl(120, 50%, 50%)
      : (m = reHslaPercent.exec(format)) ? hsla(m[1], m[2] / 100, m[3] / 100, m[4]) // hsla(120, 50%, 50%, 1)
      : named.hasOwnProperty(format) ? rgbn(named[format]) // eslint-disable-line no-prototype-builtins
      : format === "transparent" ? new Rgb(NaN, NaN, NaN, 0)
      : null;
}

function rgbn(n) {
  return new Rgb(n >> 16 & 0xff, n >> 8 & 0xff, n & 0xff, 1);
}

function rgba(r, g, b, a) {
  if (a <= 0) r = g = b = NaN;
  return new Rgb(r, g, b, a);
}

function rgbConvert(o) {
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Rgb;
  o = o.rgb();
  return new Rgb(o.r, o.g, o.b, o.opacity);
}

function color_rgb(r, g, b, opacity) {
  return arguments.length === 1 ? rgbConvert(r) : new Rgb(r, g, b, opacity == null ? 1 : opacity);
}

function Rgb(r, g, b, opacity) {
  this.r = +r;
  this.g = +g;
  this.b = +b;
  this.opacity = +opacity;
}

src_define(Rgb, color_rgb, extend(Color, {
  brighter(k) {
    k = k == null ? brighter : Math.pow(brighter, k);
    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
  },
  darker(k) {
    k = k == null ? darker : Math.pow(darker, k);
    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return (-0.5 <= this.r && this.r < 255.5)
        && (-0.5 <= this.g && this.g < 255.5)
        && (-0.5 <= this.b && this.b < 255.5)
        && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex, // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));

function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}

function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}

function rgb_formatRgb() {
  const a = clampa(this.opacity);
  return `${a === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a === 1 ? ")" : `, ${a})`}`;
}

function clampa(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}

function clampi(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}

function hex(value) {
  value = clampi(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}

function hsla(h, s, l, a) {
  if (a <= 0) h = s = l = NaN;
  else if (l <= 0 || l >= 1) h = s = NaN;
  else if (s <= 0) h = NaN;
  return new Hsl(h, s, l, a);
}

function hslConvert(o) {
  if (o instanceof Hsl) return new Hsl(o.h, o.s, o.l, o.opacity);
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Hsl;
  if (o instanceof Hsl) return o;
  o = o.rgb();
  var r = o.r / 255,
      g = o.g / 255,
      b = o.b / 255,
      min = Math.min(r, g, b),
      max = Math.max(r, g, b),
      h = NaN,
      s = max - min,
      l = (max + min) / 2;
  if (s) {
    if (r === max) h = (g - b) / s + (g < b) * 6;
    else if (g === max) h = (b - r) / s + 2;
    else h = (r - g) / s + 4;
    s /= l < 0.5 ? max + min : 2 - max - min;
    h *= 60;
  } else {
    s = l > 0 && l < 1 ? 0 : h;
  }
  return new Hsl(h, s, l, o.opacity);
}

function hsl(h, s, l, opacity) {
  return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s, l, opacity == null ? 1 : opacity);
}

function Hsl(h, s, l, opacity) {
  this.h = +h;
  this.s = +s;
  this.l = +l;
  this.opacity = +opacity;
}

src_define(Hsl, hsl, extend(Color, {
  brighter(k) {
    k = k == null ? brighter : Math.pow(brighter, k);
    return new Hsl(this.h, this.s, this.l * k, this.opacity);
  },
  darker(k) {
    k = k == null ? darker : Math.pow(darker, k);
    return new Hsl(this.h, this.s, this.l * k, this.opacity);
  },
  rgb() {
    var h = this.h % 360 + (this.h < 0) * 360,
        s = isNaN(h) || isNaN(this.s) ? 0 : this.s,
        l = this.l,
        m2 = l + (l < 0.5 ? l : 1 - l) * s,
        m1 = 2 * l - m2;
    return new Rgb(
      hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m2),
      hsl2rgb(h, m1, m2),
      hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m2),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s))
        && (0 <= this.l && this.l <= 1)
        && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a = clampa(this.opacity);
    return `${a === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a === 1 ? ")" : `, ${a})`}`;
  }
}));

function clamph(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}

function clampt(value) {
  return Math.max(0, Math.min(1, value || 0));
}

/* From FvD 13.37, CSS Color Module Level 3 */
function hsl2rgb(h, m1, m2) {
  return (h < 60 ? m1 + (m2 - m1) * h / 60
      : h < 180 ? m2
      : h < 240 ? m1 + (m2 - m1) * (240 - h) / 60
      : m1) * 255;
}

;// ./node_modules/d3-interpolate/src/basis.js
function basis(t1, v0, v1, v2, v3) {
  var t2 = t1 * t1, t3 = t2 * t1;
  return ((1 - 3 * t1 + 3 * t2 - t3) * v0
      + (4 - 6 * t2 + 3 * t3) * v1
      + (1 + 3 * t1 + 3 * t2 - 3 * t3) * v2
      + t3 * v3) / 6;
}

/* harmony default export */ function src_basis(values) {
  var n = values.length - 1;
  return function(t) {
    var i = t <= 0 ? (t = 0) : t >= 1 ? (t = 1, n - 1) : Math.floor(t * n),
        v1 = values[i],
        v2 = values[i + 1],
        v0 = i > 0 ? values[i - 1] : 2 * v1 - v2,
        v3 = i < n - 1 ? values[i + 2] : 2 * v2 - v1;
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

;// ./node_modules/d3-interpolate/src/basisClosed.js


/* harmony default export */ function basisClosed(values) {
  var n = values.length;
  return function(t) {
    var i = Math.floor(((t %= 1) < 0 ? ++t : t) * n),
        v0 = values[(i + n - 1) % n],
        v1 = values[i % n],
        v2 = values[(i + 1) % n],
        v3 = values[(i + 2) % n];
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

;// ./node_modules/d3-interpolate/src/constant.js
/* harmony default export */ const src_constant = (x => () => x);

;// ./node_modules/d3-interpolate/src/color.js


function linear(a, d) {
  return function(t) {
    return a + t * d;
  };
}

function exponential(a, b, y) {
  return a = Math.pow(a, y), b = Math.pow(b, y) - a, y = 1 / y, function(t) {
    return Math.pow(a + t * b, y);
  };
}

function hue(a, b) {
  var d = b - a;
  return d ? linear(a, d > 180 || d < -180 ? d - 360 * Math.round(d / 360) : d) : src_constant(isNaN(a) ? b : a);
}

function gamma(y) {
  return (y = +y) === 1 ? nogamma : function(a, b) {
    return b - a ? exponential(a, b, y) : src_constant(isNaN(a) ? b : a);
  };
}

function nogamma(a, b) {
  var d = b - a;
  return d ? linear(a, d) : src_constant(isNaN(a) ? b : a);
}

;// ./node_modules/d3-interpolate/src/rgb.js





/* harmony default export */ const rgb = ((function rgbGamma(y) {
  var color = gamma(y);

  function rgb(start, end) {
    var r = color((start = color_rgb(start)).r, (end = color_rgb(end)).r),
        g = color(start.g, end.g),
        b = color(start.b, end.b),
        opacity = nogamma(start.opacity, end.opacity);
    return function(t) {
      start.r = r(t);
      start.g = g(t);
      start.b = b(t);
      start.opacity = opacity(t);
      return start + "";
    };
  }

  rgb.gamma = rgbGamma;

  return rgb;
})(1));

function rgbSpline(spline) {
  return function(colors) {
    var n = colors.length,
        r = new Array(n),
        g = new Array(n),
        b = new Array(n),
        i, color;
    for (i = 0; i < n; ++i) {
      color = color_rgb(colors[i]);
      r[i] = color.r || 0;
      g[i] = color.g || 0;
      b[i] = color.b || 0;
    }
    r = spline(r);
    g = spline(g);
    b = spline(b);
    color.opacity = 1;
    return function(t) {
      color.r = r(t);
      color.g = g(t);
      color.b = b(t);
      return color + "";
    };
  };
}

var rgbBasis = rgbSpline(src_basis);
var rgbBasisClosed = rgbSpline(basisClosed);

;// ./node_modules/d3-interpolate/src/numberArray.js
/* harmony default export */ function numberArray(a, b) {
  if (!b) b = [];
  var n = a ? Math.min(b.length, a.length) : 0,
      c = b.slice(),
      i;
  return function(t) {
    for (i = 0; i < n; ++i) c[i] = a[i] * (1 - t) + b[i] * t;
    return c;
  };
}

function isNumberArray(x) {
  return ArrayBuffer.isView(x) && !(x instanceof DataView);
}

;// ./node_modules/d3-interpolate/src/array.js



/* harmony default export */ function src_array(a, b) {
  return (isNumberArray(b) ? numberArray : genericArray)(a, b);
}

function genericArray(a, b) {
  var nb = b ? b.length : 0,
      na = a ? Math.min(nb, a.length) : 0,
      x = new Array(na),
      c = new Array(nb),
      i;

  for (i = 0; i < na; ++i) x[i] = value(a[i], b[i]);
  for (; i < nb; ++i) c[i] = b[i];

  return function(t) {
    for (i = 0; i < na; ++i) c[i] = x[i](t);
    return c;
  };
}

;// ./node_modules/d3-interpolate/src/date.js
/* harmony default export */ function date(a, b) {
  var d = new Date;
  return a = +a, b = +b, function(t) {
    return d.setTime(a * (1 - t) + b * t), d;
  };
}

;// ./node_modules/d3-interpolate/src/number.js
/* harmony default export */ function src_number(a, b) {
  return a = +a, b = +b, function(t) {
    return a * (1 - t) + b * t;
  };
}

;// ./node_modules/d3-interpolate/src/object.js


/* harmony default export */ function object(a, b) {
  var i = {},
      c = {},
      k;

  if (a === null || typeof a !== "object") a = {};
  if (b === null || typeof b !== "object") b = {};

  for (k in b) {
    if (k in a) {
      i[k] = value(a[k], b[k]);
    } else {
      c[k] = b[k];
    }
  }

  return function(t) {
    for (k in i) c[k] = i[k](t);
    return c;
  };
}

;// ./node_modules/d3-interpolate/src/string.js


var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g,
    reB = new RegExp(reA.source, "g");

function string_zero(b) {
  return function() {
    return b;
  };
}

function one(b) {
  return function(t) {
    return b(t) + "";
  };
}

/* harmony default export */ function string(a, b) {
  var bi = reA.lastIndex = reB.lastIndex = 0, // scan index for next number in b
      am, // current match in a
      bm, // current match in b
      bs, // string preceding current number in b, if any
      i = -1, // index in s
      s = [], // string constants and placeholders
      q = []; // number interpolators

  // Coerce inputs to strings.
  a = a + "", b = b + "";

  // Interpolate pairs of numbers in a & b.
  while ((am = reA.exec(a))
      && (bm = reB.exec(b))) {
    if ((bs = bm.index) > bi) { // a string precedes the next number in b
      bs = b.slice(bi, bs);
      if (s[i]) s[i] += bs; // coalesce with previous string
      else s[++i] = bs;
    }
    if ((am = am[0]) === (bm = bm[0])) { // numbers in a & b match
      if (s[i]) s[i] += bm; // coalesce with previous string
      else s[++i] = bm;
    } else { // interpolate non-matching numbers
      s[++i] = null;
      q.push({i: i, x: src_number(am, bm)});
    }
    bi = reB.lastIndex;
  }

  // Add remains of b.
  if (bi < b.length) {
    bs = b.slice(bi);
    if (s[i]) s[i] += bs; // coalesce with previous string
    else s[++i] = bs;
  }

  // Special optimization for only a single match.
  // Otherwise, interpolate each of the numbers and rejoin the string.
  return s.length < 2 ? (q[0]
      ? one(q[0].x)
      : string_zero(b))
      : (b = q.length, function(t) {
          for (var i = 0, o; i < b; ++i) s[(o = q[i]).i] = o.x(t);
          return s.join("");
        });
}

;// ./node_modules/d3-interpolate/src/value.js










/* harmony default export */ function value(a, b) {
  var t = typeof b, c;
  return b == null || t === "boolean" ? src_constant(b)
      : (t === "number" ? src_number
      : t === "string" ? ((c = color(b)) ? (b = c, rgb) : string)
      : b instanceof color ? rgb
      : b instanceof Date ? date
      : isNumberArray(b) ? numberArray
      : Array.isArray(b) ? genericArray
      : typeof b.valueOf !== "function" && typeof b.toString !== "function" || isNaN(b) ? object
      : src_number)(a, b);
}

;// ./node_modules/d3-interpolate/src/round.js
/* harmony default export */ function src_round(a, b) {
  return a = +a, b = +b, function(t) {
    return Math.round(a * (1 - t) + b * t);
  };
}

;// ./node_modules/d3-scale/src/constant.js
function constants(x) {
  return function() {
    return x;
  };
}

;// ./node_modules/d3-scale/src/number.js
function number_number(x) {
  return +x;
}

;// ./node_modules/d3-scale/src/continuous.js





var unit = [0, 1];

function identity(x) {
  return x;
}

function normalize(a, b) {
  return (b -= (a = +a))
      ? function(x) { return (x - a) / b; }
      : constants(isNaN(b) ? NaN : 0.5);
}

function clamper(a, b) {
  var t;
  if (a > b) t = a, a = b, b = t;
  return function(x) { return Math.max(a, Math.min(b, x)); };
}

// normalize(a, b)(x) takes a domain value x in [a,b] and returns the corresponding parameter t in [0,1].
// interpolate(a, b)(t) takes a parameter t in [0,1] and returns the corresponding range value x in [a,b].
function bimap(domain, range, interpolate) {
  var d0 = domain[0], d1 = domain[1], r0 = range[0], r1 = range[1];
  if (d1 < d0) d0 = normalize(d1, d0), r0 = interpolate(r1, r0);
  else d0 = normalize(d0, d1), r0 = interpolate(r0, r1);
  return function(x) { return r0(d0(x)); };
}

function polymap(domain, range, interpolate) {
  var j = Math.min(domain.length, range.length) - 1,
      d = new Array(j),
      r = new Array(j),
      i = -1;

  // Reverse descending domains.
  if (domain[j] < domain[0]) {
    domain = domain.slice().reverse();
    range = range.slice().reverse();
  }

  while (++i < j) {
    d[i] = normalize(domain[i], domain[i + 1]);
    r[i] = interpolate(range[i], range[i + 1]);
  }

  return function(x) {
    var i = bisect(domain, x, 1, j) - 1;
    return r[i](d[i](x));
  };
}

function copy(source, target) {
  return target
      .domain(source.domain())
      .range(source.range())
      .interpolate(source.interpolate())
      .clamp(source.clamp())
      .unknown(source.unknown());
}

function transformer() {
  var domain = unit,
      range = unit,
      interpolate = value,
      transform,
      untransform,
      unknown,
      clamp = identity,
      piecewise,
      output,
      input;

  function rescale() {
    var n = Math.min(domain.length, range.length);
    if (clamp !== identity) clamp = clamper(domain[0], domain[n - 1]);
    piecewise = n > 2 ? polymap : bimap;
    output = input = null;
    return scale;
  }

  function scale(x) {
    return x == null || isNaN(x = +x) ? unknown : (output || (output = piecewise(domain.map(transform), range, interpolate)))(transform(clamp(x)));
  }

  scale.invert = function(y) {
    return clamp(untransform((input || (input = piecewise(range, domain.map(transform), src_number)))(y)));
  };

  scale.domain = function(_) {
    return arguments.length ? (domain = Array.from(_, number_number), rescale()) : domain.slice();
  };

  scale.range = function(_) {
    return arguments.length ? (range = Array.from(_), rescale()) : range.slice();
  };

  scale.rangeRound = function(_) {
    return range = Array.from(_), interpolate = src_round, rescale();
  };

  scale.clamp = function(_) {
    return arguments.length ? (clamp = _ ? true : identity, rescale()) : clamp !== identity;
  };

  scale.interpolate = function(_) {
    return arguments.length ? (interpolate = _, rescale()) : interpolate;
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  return function(t, u) {
    transform = t, untransform = u;
    return rescale();
  };
}

function continuous() {
  return transformer()(identity, identity);
}

;// ./node_modules/d3-format/src/formatSpecifier.js
// [[fill]align][sign][symbol][0][width][,][.precision][~][type]
var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;

function formatSpecifier(specifier) {
  if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match;
  return new FormatSpecifier({
    fill: match[1],
    align: match[2],
    sign: match[3],
    symbol: match[4],
    zero: match[5],
    width: match[6],
    comma: match[7],
    precision: match[8] && match[8].slice(1),
    trim: match[9],
    type: match[10]
  });
}

formatSpecifier.prototype = FormatSpecifier.prototype; // instanceof

function FormatSpecifier(specifier) {
  this.fill = specifier.fill === undefined ? " " : specifier.fill + "";
  this.align = specifier.align === undefined ? ">" : specifier.align + "";
  this.sign = specifier.sign === undefined ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === undefined ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === undefined ? undefined : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === undefined ? undefined : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === undefined ? "" : specifier.type + "";
}

FormatSpecifier.prototype.toString = function() {
  return this.fill
      + this.align
      + this.sign
      + this.symbol
      + (this.zero ? "0" : "")
      + (this.width === undefined ? "" : Math.max(1, this.width | 0))
      + (this.comma ? "," : "")
      + (this.precision === undefined ? "" : "." + Math.max(0, this.precision | 0))
      + (this.trim ? "~" : "")
      + this.type;
};

;// ./node_modules/d3-format/src/formatDecimal.js
/* harmony default export */ function formatDecimal(x) {
  return Math.abs(x = Math.round(x)) >= 1e21
      ? x.toLocaleString("en").replace(/,/g, "")
      : x.toString(10);
}

// Computes the decimal coefficient and exponent of the specified number x with
// significant digits p, where x is positive and p is in [1, 21] or undefined.
// For example, formatDecimalParts(1.23) returns ["123", 0].
function formatDecimalParts(x, p) {
  if (!isFinite(x) || x === 0) return null; // NaN, ±Infinity, ±0
  var i = (x = p ? x.toExponential(p - 1) : x.toExponential()).indexOf("e"), coefficient = x.slice(0, i);

  // The string returned by toExponential either has the form \d\.\d+e[-+]\d+
  // (e.g., 1.2e+3) or the form \de[-+]\d+ (e.g., 1e+3).
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x.slice(i + 1)
  ];
}

;// ./node_modules/d3-format/src/exponent.js


/* harmony default export */ function exponent(x) {
  return x = formatDecimalParts(Math.abs(x)), x ? x[1] : NaN;
}

;// ./node_modules/d3-format/src/precisionPrefix.js


/* harmony default export */ function precisionPrefix(step, value) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent(value) / 3))) * 3 - exponent(Math.abs(step)));
}

;// ./node_modules/d3-format/src/formatGroup.js
/* harmony default export */ function formatGroup(grouping, thousands) {
  return function(value, width) {
    var i = value.length,
        t = [],
        j = 0,
        g = grouping[0],
        length = 0;

    while (i > 0 && g > 0) {
      if (length + g + 1 > width) g = Math.max(1, width - length);
      t.push(value.substring(i -= g, i + g));
      if ((length += g + 1) > width) break;
      g = grouping[j = (j + 1) % grouping.length];
    }

    return t.reverse().join(thousands);
  };
}

;// ./node_modules/d3-format/src/formatNumerals.js
/* harmony default export */ function formatNumerals(numerals) {
  return function(value) {
    return value.replace(/[0-9]/g, function(i) {
      return numerals[+i];
    });
  };
}

;// ./node_modules/d3-format/src/formatTrim.js
// Trims insignificant zeros, e.g., replaces 1.2000k with 1.2k.
/* harmony default export */ function formatTrim(s) {
  out: for (var n = s.length, i = 1, i0 = -1, i1; i < n; ++i) {
    switch (s[i]) {
      case ".": i0 = i1 = i; break;
      case "0": if (i0 === 0) i0 = i; i1 = i; break;
      default: if (!+s[i]) break out; if (i0 > 0) i0 = 0; break;
    }
  }
  return i0 > 0 ? s.slice(0, i0) + s.slice(i1 + 1) : s;
}

;// ./node_modules/d3-format/src/formatPrefixAuto.js


var prefixExponent;

/* harmony default export */ function formatPrefixAuto(x, p) {
  var d = formatDecimalParts(x, p);
  if (!d) return prefixExponent = undefined, x.toPrecision(p);
  var coefficient = d[0],
      exponent = d[1],
      i = exponent - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1,
      n = coefficient.length;
  return i === n ? coefficient
      : i > n ? coefficient + new Array(i - n + 1).join("0")
      : i > 0 ? coefficient.slice(0, i) + "." + coefficient.slice(i)
      : "0." + new Array(1 - i).join("0") + formatDecimalParts(x, Math.max(0, p + i - 1))[0]; // less than 1y!
}

;// ./node_modules/d3-format/src/formatRounded.js


/* harmony default export */ function formatRounded(x, p) {
  var d = formatDecimalParts(x, p);
  if (!d) return x + "";
  var coefficient = d[0],
      exponent = d[1];
  return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient
      : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1)
      : coefficient + new Array(exponent - coefficient.length + 2).join("0");
}

;// ./node_modules/d3-format/src/formatTypes.js




/* harmony default export */ const formatTypes = ({
  "%": (x, p) => (x * 100).toFixed(p),
  "b": (x) => Math.round(x).toString(2),
  "c": (x) => x + "",
  "d": formatDecimal,
  "e": (x, p) => x.toExponential(p),
  "f": (x, p) => x.toFixed(p),
  "g": (x, p) => x.toPrecision(p),
  "o": (x) => Math.round(x).toString(8),
  "p": (x, p) => formatRounded(x * 100, p),
  "r": formatRounded,
  "s": formatPrefixAuto,
  "X": (x) => Math.round(x).toString(16).toUpperCase(),
  "x": (x) => Math.round(x).toString(16)
});

;// ./node_modules/d3-format/src/identity.js
/* harmony default export */ function src_identity(x) {
  return x;
}

;// ./node_modules/d3-format/src/locale.js









var map = Array.prototype.map,
    prefixes = ["y","z","a","f","p","n","µ","m","","k","M","G","T","P","E","Z","Y"];

/* harmony default export */ function locale(locale) {
  var group = locale.grouping === undefined || locale.thousands === undefined ? src_identity : formatGroup(map.call(locale.grouping, Number), locale.thousands + ""),
      currencyPrefix = locale.currency === undefined ? "" : locale.currency[0] + "",
      currencySuffix = locale.currency === undefined ? "" : locale.currency[1] + "",
      decimal = locale.decimal === undefined ? "." : locale.decimal + "",
      numerals = locale.numerals === undefined ? src_identity : formatNumerals(map.call(locale.numerals, String)),
      percent = locale.percent === undefined ? "%" : locale.percent + "",
      minus = locale.minus === undefined ? "−" : locale.minus + "",
      nan = locale.nan === undefined ? "NaN" : locale.nan + "";

  function newFormat(specifier, options) {
    specifier = formatSpecifier(specifier);

    var fill = specifier.fill,
        align = specifier.align,
        sign = specifier.sign,
        symbol = specifier.symbol,
        zero = specifier.zero,
        width = specifier.width,
        comma = specifier.comma,
        precision = specifier.precision,
        trim = specifier.trim,
        type = specifier.type;

    // The "n" type is an alias for ",g".
    if (type === "n") comma = true, type = "g";

    // The "" type, and any invalid type, is an alias for ".12~g".
    else if (!formatTypes[type]) precision === undefined && (precision = 12), trim = true, type = "g";

    // If zero fill is specified, padding goes after sign and before digits.
    if (zero || (fill === "0" && align === "=")) zero = true, fill = "0", align = "=";

    // Compute the prefix and suffix.
    // For SI-prefix, the suffix is lazily computed.
    var prefix = (options && options.prefix !== undefined ? options.prefix : "") + (symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : ""),
        suffix = (symbol === "$" ? currencySuffix : /[%p]/.test(type) ? percent : "") + (options && options.suffix !== undefined ? options.suffix : "");

    // What format function should we use?
    // Is this an integer type?
    // Can this type generate exponential notation?
    var formatType = formatTypes[type],
        maybeSuffix = /[defgprs%]/.test(type);

    // Set the default precision if not specified,
    // or clamp the specified precision to the supported range.
    // For significant precision, it must be in [1, 21].
    // For fixed precision, it must be in [0, 20].
    precision = precision === undefined ? 6
        : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision))
        : Math.max(0, Math.min(20, precision));

    function format(value) {
      var valuePrefix = prefix,
          valueSuffix = suffix,
          i, n, c;

      if (type === "c") {
        valueSuffix = formatType(value) + valueSuffix;
        value = "";
      } else {
        value = +value;

        // Determine the sign. -0 is not less than 0, but 1 / -0 is!
        var valueNegative = value < 0 || 1 / value < 0;

        // Perform the initial formatting.
        value = isNaN(value) ? nan : formatType(Math.abs(value), precision);

        // Trim insignificant zeros.
        if (trim) value = formatTrim(value);

        // If a negative value rounds to zero after formatting, and no explicit positive sign is requested, hide the sign.
        if (valueNegative && +value === 0 && sign !== "+") valueNegative = false;

        // Compute the prefix and suffix.
        valuePrefix = (valueNegative ? (sign === "(" ? sign : minus) : sign === "-" || sign === "(" ? "" : sign) + valuePrefix;
        valueSuffix = (type === "s" && !isNaN(value) && prefixExponent !== undefined ? prefixes[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign === "(" ? ")" : "");

        // Break the formatted value into the integer “value” part that can be
        // grouped, and fractional or exponential “suffix” part that is not.
        if (maybeSuffix) {
          i = -1, n = value.length;
          while (++i < n) {
            if (c = value.charCodeAt(i), 48 > c || c > 57) {
              valueSuffix = (c === 46 ? decimal + value.slice(i + 1) : value.slice(i)) + valueSuffix;
              value = value.slice(0, i);
              break;
            }
          }
        }
      }

      // If the fill character is not "0", grouping is applied before padding.
      if (comma && !zero) value = group(value, Infinity);

      // Compute the padding.
      var length = valuePrefix.length + value.length + valueSuffix.length,
          padding = length < width ? new Array(width - length + 1).join(fill) : "";

      // If the fill character is "0", grouping is applied after padding.
      if (comma && zero) value = group(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";

      // Reconstruct the final output based on the desired alignment.
      switch (align) {
        case "<": value = valuePrefix + value + valueSuffix + padding; break;
        case "=": value = valuePrefix + padding + value + valueSuffix; break;
        case "^": value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length); break;
        default: value = padding + valuePrefix + value + valueSuffix; break;
      }

      return numerals(value);
    }

    format.toString = function() {
      return specifier + "";
    };

    return format;
  }

  function formatPrefix(specifier, value) {
    var e = Math.max(-8, Math.min(8, Math.floor(exponent(value) / 3))) * 3,
        k = Math.pow(10, -e),
        f = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier), {suffix: prefixes[8 + e / 3]});
    return function(value) {
      return f(k * value);
    };
  }

  return {
    format: newFormat,
    formatPrefix: formatPrefix
  };
}

;// ./node_modules/d3-format/src/defaultLocale.js


var defaultLocale_locale;
var format;
var formatPrefix;

defaultLocale({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});

function defaultLocale(definition) {
  defaultLocale_locale = locale(definition);
  format = defaultLocale_locale.format;
  formatPrefix = defaultLocale_locale.formatPrefix;
  return defaultLocale_locale;
}

;// ./node_modules/d3-format/src/precisionRound.js


/* harmony default export */ function precisionRound(step, max) {
  step = Math.abs(step), max = Math.abs(max) - step;
  return Math.max(0, exponent(max) - exponent(step)) + 1;
}

;// ./node_modules/d3-format/src/precisionFixed.js


/* harmony default export */ function precisionFixed(step) {
  return Math.max(0, -exponent(Math.abs(step)));
}

;// ./node_modules/d3-scale/src/tickFormat.js



function tickFormat(start, stop, count, specifier) {
  var step = tickStep(start, stop, count),
      precision;
  specifier = formatSpecifier(specifier == null ? ",f" : specifier);
  switch (specifier.type) {
    case "s": {
      var value = Math.max(Math.abs(start), Math.abs(stop));
      if (specifier.precision == null && !isNaN(precision = precisionPrefix(step, value))) specifier.precision = precision;
      return formatPrefix(specifier, value);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      if (specifier.precision == null && !isNaN(precision = precisionRound(step, Math.max(Math.abs(start), Math.abs(stop))))) specifier.precision = precision - (specifier.type === "e");
      break;
    }
    case "f":
    case "%": {
      if (specifier.precision == null && !isNaN(precision = precisionFixed(step))) specifier.precision = precision - (specifier.type === "%") * 2;
      break;
    }
  }
  return format(specifier);
}

;// ./node_modules/d3-scale/src/linear.js





function linearish(scale) {
  var domain = scale.domain;

  scale.ticks = function(count) {
    var d = domain();
    return ticks(d[0], d[d.length - 1], count == null ? 10 : count);
  };

  scale.tickFormat = function(count, specifier) {
    var d = domain();
    return tickFormat(d[0], d[d.length - 1], count == null ? 10 : count, specifier);
  };

  scale.nice = function(count) {
    if (count == null) count = 10;

    var d = domain();
    var i0 = 0;
    var i1 = d.length - 1;
    var start = d[i0];
    var stop = d[i1];
    var prestep;
    var step;
    var maxIter = 10;

    if (stop < start) {
      step = start, start = stop, stop = step;
      step = i0, i0 = i1, i1 = step;
    }
    
    while (maxIter-- > 0) {
      step = tickIncrement(start, stop, count);
      if (step === prestep) {
        d[i0] = start
        d[i1] = stop
        return domain(d);
      } else if (step > 0) {
        start = Math.floor(start / step) * step;
        stop = Math.ceil(stop / step) * step;
      } else if (step < 0) {
        start = Math.ceil(start * step) / step;
        stop = Math.floor(stop * step) / step;
      } else {
        break;
      }
      prestep = step;
    }

    return scale;
  };

  return scale;
}

function linear_linear() {
  var scale = continuous();

  scale.copy = function() {
    return copy(scale, linear_linear());
  };

  initRange.apply(scale, arguments);

  return linearish(scale);
}

;// ./node_modules/d3-scale/src/identity.js



function identity_identity(domain) {
  var unknown;

  function scale(x) {
    return x == null || isNaN(x = +x) ? unknown : x;
  }

  scale.invert = scale;

  scale.domain = scale.range = function(_) {
    return arguments.length ? (domain = Array.from(_, number_number), scale) : domain.slice();
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  scale.copy = function() {
    return identity_identity(domain).unknown(unknown);
  };

  domain = arguments.length ? Array.from(domain, number_number) : [0, 1];

  return linearish(scale);
}

;// ./node_modules/d3-scale/src/nice.js
function nice(domain, interval) {
  domain = domain.slice();

  var i0 = 0,
      i1 = domain.length - 1,
      x0 = domain[i0],
      x1 = domain[i1],
      t;

  if (x1 < x0) {
    t = i0, i0 = i1, i1 = t;
    t = x0, x0 = x1, x1 = t;
  }

  domain[i0] = interval.floor(x0);
  domain[i1] = interval.ceil(x1);
  return domain;
}

;// ./node_modules/d3-scale/src/log.js






function transformLog(x) {
  return Math.log(x);
}

function transformExp(x) {
  return Math.exp(x);
}

function transformLogn(x) {
  return -Math.log(-x);
}

function transformExpn(x) {
  return -Math.exp(-x);
}

function pow10(x) {
  return isFinite(x) ? +("1e" + x) : x < 0 ? 0 : x;
}

function powp(base) {
  return base === 10 ? pow10
      : base === Math.E ? Math.exp
      : x => Math.pow(base, x);
}

function logp(base) {
  return base === Math.E ? Math.log
      : base === 10 && Math.log10
      || base === 2 && Math.log2
      || (base = Math.log(base), x => Math.log(x) / base);
}

function reflect(f) {
  return (x, k) => -f(-x, k);
}

function loggish(transform) {
  const scale = transform(transformLog, transformExp);
  const domain = scale.domain;
  let base = 10;
  let logs;
  let pows;

  function rescale() {
    logs = logp(base), pows = powp(base);
    if (domain()[0] < 0) {
      logs = reflect(logs), pows = reflect(pows);
      transform(transformLogn, transformExpn);
    } else {
      transform(transformLog, transformExp);
    }
    return scale;
  }

  scale.base = function(_) {
    return arguments.length ? (base = +_, rescale()) : base;
  };

  scale.domain = function(_) {
    return arguments.length ? (domain(_), rescale()) : domain();
  };

  scale.ticks = count => {
    const d = domain();
    let u = d[0];
    let v = d[d.length - 1];
    const r = v < u;

    if (r) ([u, v] = [v, u]);

    let i = logs(u);
    let j = logs(v);
    let k;
    let t;
    const n = count == null ? 10 : +count;
    let z = [];

    if (!(base % 1) && j - i < n) {
      i = Math.floor(i), j = Math.ceil(j);
      if (u > 0) for (; i <= j; ++i) {
        for (k = 1; k < base; ++k) {
          t = i < 0 ? k / pows(-i) : k * pows(i);
          if (t < u) continue;
          if (t > v) break;
          z.push(t);
        }
      } else for (; i <= j; ++i) {
        for (k = base - 1; k >= 1; --k) {
          t = i > 0 ? k / pows(-i) : k * pows(i);
          if (t < u) continue;
          if (t > v) break;
          z.push(t);
        }
      }
      if (z.length * 2 < n) z = ticks(u, v, n);
    } else {
      z = ticks(i, j, Math.min(j - i, n)).map(pows);
    }
    return r ? z.reverse() : z;
  };

  scale.tickFormat = (count, specifier) => {
    if (count == null) count = 10;
    if (specifier == null) specifier = base === 10 ? "s" : ",";
    if (typeof specifier !== "function") {
      if (!(base % 1) && (specifier = formatSpecifier(specifier)).precision == null) specifier.trim = true;
      specifier = format(specifier);
    }
    if (count === Infinity) return specifier;
    const k = Math.max(1, base * count / scale.ticks().length); // TODO fast estimate?
    return d => {
      let i = d / pows(Math.round(logs(d)));
      if (i * base < base - 0.5) i *= base;
      return i <= k ? specifier(d) : "";
    };
  };

  scale.nice = () => {
    return domain(nice(domain(), {
      floor: x => pows(Math.floor(logs(x))),
      ceil: x => pows(Math.ceil(logs(x)))
    }));
  };

  return scale;
}

function log() {
  const scale = loggish(transformer()).domain([1, 10]);
  scale.copy = () => copy(scale, log()).base(scale.base());
  initRange.apply(scale, arguments);
  return scale;
}

;// ./node_modules/d3-scale/src/symlog.js




function transformSymlog(c) {
  return function(x) {
    return Math.sign(x) * Math.log1p(Math.abs(x / c));
  };
}

function transformSymexp(c) {
  return function(x) {
    return Math.sign(x) * Math.expm1(Math.abs(x)) * c;
  };
}

function symlogish(transform) {
  var c = 1, scale = transform(transformSymlog(c), transformSymexp(c));

  scale.constant = function(_) {
    return arguments.length ? transform(transformSymlog(c = +_), transformSymexp(c)) : c;
  };

  return linearish(scale);
}

function symlog() {
  var scale = symlogish(transformer());

  scale.copy = function() {
    return copy(scale, symlog()).constant(scale.constant());
  };

  return initRange.apply(scale, arguments);
}

;// ./node_modules/d3-scale/src/pow.js




function transformPow(exponent) {
  return function(x) {
    return x < 0 ? -Math.pow(-x, exponent) : Math.pow(x, exponent);
  };
}

function transformSqrt(x) {
  return x < 0 ? -Math.sqrt(-x) : Math.sqrt(x);
}

function transformSquare(x) {
  return x < 0 ? -x * x : x * x;
}

function powish(transform) {
  var scale = transform(identity, identity),
      exponent = 1;

  function rescale() {
    return exponent === 1 ? transform(identity, identity)
        : exponent === 0.5 ? transform(transformSqrt, transformSquare)
        : transform(transformPow(exponent), transformPow(1 / exponent));
  }

  scale.exponent = function(_) {
    return arguments.length ? (exponent = +_, rescale()) : exponent;
  };

  return linearish(scale);
}

function pow() {
  var scale = powish(transformer());

  scale.copy = function() {
    return copy(scale, pow()).exponent(scale.exponent());
  };

  initRange.apply(scale, arguments);

  return scale;
}

function sqrt() {
  return pow.apply(null, arguments).exponent(0.5);
}

;// ./node_modules/d3-scale/src/radial.js





function square(x) {
  return Math.sign(x) * x * x;
}

function unsquare(x) {
  return Math.sign(x) * Math.sqrt(Math.abs(x));
}

function radial() {
  var squared = continuous(),
      range = [0, 1],
      round = false,
      unknown;

  function scale(x) {
    var y = unsquare(squared(x));
    return isNaN(y) ? unknown : round ? Math.round(y) : y;
  }

  scale.invert = function(y) {
    return squared.invert(square(y));
  };

  scale.domain = function(_) {
    return arguments.length ? (squared.domain(_), scale) : squared.domain();
  };

  scale.range = function(_) {
    return arguments.length ? (squared.range((range = Array.from(_, number_number)).map(square)), scale) : range.slice();
  };

  scale.rangeRound = function(_) {
    return scale.range(_).round(true);
  };

  scale.round = function(_) {
    return arguments.length ? (round = !!_, scale) : round;
  };

  scale.clamp = function(_) {
    return arguments.length ? (squared.clamp(_), scale) : squared.clamp();
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  scale.copy = function() {
    return radial(squared.domain(), range)
        .round(round)
        .clamp(squared.clamp())
        .unknown(unknown);
  };

  initRange.apply(scale, arguments);

  return linearish(scale);
}

;// ./node_modules/d3-array/src/max.js
function max(values, valueof) {
  let max;
  if (valueof === undefined) {
    for (const value of values) {
      if (value != null
          && (max < value || (max === undefined && value >= value))) {
        max = value;
      }
    }
  } else {
    let index = -1;
    for (let value of values) {
      if ((value = valueof(value, ++index, values)) != null
          && (max < value || (max === undefined && value >= value))) {
        max = value;
      }
    }
  }
  return max;
}

;// ./node_modules/d3-array/src/maxIndex.js
function maxIndex(values, valueof) {
  let max;
  let maxIndex = -1;
  let index = -1;
  if (valueof === undefined) {
    for (const value of values) {
      ++index;
      if (value != null
          && (max < value || (max === undefined && value >= value))) {
        max = value, maxIndex = index;
      }
    }
  } else {
    for (let value of values) {
      if ((value = valueof(value, ++index, values)) != null
          && (max < value || (max === undefined && value >= value))) {
        max = value, maxIndex = index;
      }
    }
  }
  return maxIndex;
}

;// ./node_modules/d3-array/src/min.js
function min(values, valueof) {
  let min;
  if (valueof === undefined) {
    for (const value of values) {
      if (value != null
          && (min > value || (min === undefined && value >= value))) {
        min = value;
      }
    }
  } else {
    let index = -1;
    for (let value of values) {
      if ((value = valueof(value, ++index, values)) != null
          && (min > value || (min === undefined && value >= value))) {
        min = value;
      }
    }
  }
  return min;
}

;// ./node_modules/d3-array/src/minIndex.js
function minIndex(values, valueof) {
  let min;
  let minIndex = -1;
  let index = -1;
  if (valueof === undefined) {
    for (const value of values) {
      ++index;
      if (value != null
          && (min > value || (min === undefined && value >= value))) {
        min = value, minIndex = index;
      }
    }
  } else {
    for (let value of values) {
      if ((value = valueof(value, ++index, values)) != null
          && (min > value || (min === undefined && value >= value))) {
        min = value, minIndex = index;
      }
    }
  }
  return minIndex;
}

;// ./node_modules/d3-array/src/permute.js
function permute(source, keys) {
  return Array.from(keys, key => source[key]);
}

;// ./node_modules/d3-array/src/sort.js



function sort(values, ...F) {
  if (typeof values[Symbol.iterator] !== "function") throw new TypeError("values is not iterable");
  values = Array.from(values);
  let [f] = F;
  if ((f && f.length !== 2) || F.length > 1) {
    const index = Uint32Array.from(values, (d, i) => i);
    if (F.length > 1) {
      F = F.map(f => values.map(f));
      index.sort((i, j) => {
        for (const f of F) {
          const c = ascendingDefined(f[i], f[j]);
          if (c) return c;
        }
      });
    } else {
      f = values.map(f);
      index.sort((i, j) => ascendingDefined(f[i], f[j]));
    }
    return permute(values, index);
  }
  return values.sort(compareDefined(f));
}

function compareDefined(compare = ascending) {
  if (compare === ascending) return ascendingDefined;
  if (typeof compare !== "function") throw new TypeError("compare is not a function");
  return (a, b) => {
    const x = compare(a, b);
    if (x || x === 0) return x;
    return (compare(b, b) === 0) - (compare(a, a) === 0);
  };
}

function ascendingDefined(a, b) {
  return (a == null || !(a >= a)) - (b == null || !(b >= b)) || (a < b ? -1 : a > b ? 1 : 0);
}

;// ./node_modules/d3-array/src/quickselect.js


// Based on https://github.com/mourner/quickselect
// ISC license, Copyright 2018 Vladimir Agafonkin.
function quickselect(array, k, left = 0, right = Infinity, compare) {
  k = Math.floor(k);
  left = Math.floor(Math.max(0, left));
  right = Math.floor(Math.min(array.length - 1, right));

  if (!(left <= k && k <= right)) return array;

  compare = compare === undefined ? ascendingDefined : compareDefined(compare);

  while (right > left) {
    if (right - left > 600) {
      const n = right - left + 1;
      const m = k - left + 1;
      const z = Math.log(n);
      const s = 0.5 * Math.exp(2 * z / 3);
      const sd = 0.5 * Math.sqrt(z * s * (n - s) / n) * (m - n / 2 < 0 ? -1 : 1);
      const newLeft = Math.max(left, Math.floor(k - m * s / n + sd));
      const newRight = Math.min(right, Math.floor(k + (n - m) * s / n + sd));
      quickselect(array, k, newLeft, newRight, compare);
    }

    const t = array[k];
    let i = left;
    let j = right;

    swap(array, left, k);
    if (compare(array[right], t) > 0) swap(array, left, right);

    while (i < j) {
      swap(array, i, j), ++i, --j;
      while (compare(array[i], t) < 0) ++i;
      while (compare(array[j], t) > 0) --j;
    }

    if (compare(array[left], t) === 0) swap(array, left, j);
    else ++j, swap(array, j, right);

    if (j <= k) left = j + 1;
    if (k <= j) right = j - 1;
  }

  return array;
}

function swap(array, i, j) {
  const t = array[i];
  array[i] = array[j];
  array[j] = t;
}

;// ./node_modules/d3-array/src/greatest.js


function greatest(values, compare = ascending) {
  let max;
  let defined = false;
  if (compare.length === 1) {
    let maxValue;
    for (const element of values) {
      const value = compare(element);
      if (defined
          ? ascending(value, maxValue) > 0
          : ascending(value, value) === 0) {
        max = element;
        maxValue = value;
        defined = true;
      }
    }
  } else {
    for (const value of values) {
      if (defined
          ? compare(value, max) > 0
          : compare(value, value) === 0) {
        max = value;
        defined = true;
      }
    }
  }
  return max;
}

;// ./node_modules/d3-array/src/quantile.js









function quantile(values, p, valueof) {
  values = Float64Array.from(numbers(values, valueof));
  if (!(n = values.length) || isNaN(p = +p)) return;
  if (p <= 0 || n < 2) return min(values);
  if (p >= 1) return max(values);
  var n,
      i = (n - 1) * p,
      i0 = Math.floor(i),
      value0 = max(quickselect(values, i0).subarray(0, i0 + 1)),
      value1 = min(values.subarray(i0 + 1));
  return value0 + (value1 - value0) * (i - i0);
}

function quantileSorted(values, p, valueof = number) {
  if (!(n = values.length) || isNaN(p = +p)) return;
  if (p <= 0 || n < 2) return +valueof(values[0], 0, values);
  if (p >= 1) return +valueof(values[n - 1], n - 1, values);
  var n,
      i = (n - 1) * p,
      i0 = Math.floor(i),
      value0 = +valueof(values[i0], i0, values),
      value1 = +valueof(values[i0 + 1], i0 + 1, values);
  return value0 + (value1 - value0) * (i - i0);
}

function quantileIndex(values, p, valueof = number) {
  if (isNaN(p = +p)) return;
  numbers = Float64Array.from(values, (_, i) => number(valueof(values[i], i, values)));
  if (p <= 0) return minIndex(numbers);
  if (p >= 1) return maxIndex(numbers);
  var numbers,
      index = Uint32Array.from(values, (_, i) => i),
      j = numbers.length - 1,
      i = Math.floor(j * p);
  quickselect(index, i, 0, j, (i, j) => ascendingDefined(numbers[i], numbers[j]));
  i = greatest(index.subarray(0, i + 1), (i) => numbers[i]);
  return i >= 0 ? i : -1;
}

;// ./node_modules/d3-scale/src/quantile.js



function quantile_quantile() {
  var domain = [],
      range = [],
      thresholds = [],
      unknown;

  function rescale() {
    var i = 0, n = Math.max(1, range.length);
    thresholds = new Array(n - 1);
    while (++i < n) thresholds[i - 1] = quantileSorted(domain, i / n);
    return scale;
  }

  function scale(x) {
    return x == null || isNaN(x = +x) ? unknown : range[bisect(thresholds, x)];
  }

  scale.invertExtent = function(y) {
    var i = range.indexOf(y);
    return i < 0 ? [NaN, NaN] : [
      i > 0 ? thresholds[i - 1] : domain[0],
      i < thresholds.length ? thresholds[i] : domain[domain.length - 1]
    ];
  };

  scale.domain = function(_) {
    if (!arguments.length) return domain.slice();
    domain = [];
    for (let d of _) if (d != null && !isNaN(d = +d)) domain.push(d);
    domain.sort(ascending);
    return rescale();
  };

  scale.range = function(_) {
    return arguments.length ? (range = Array.from(_), rescale()) : range.slice();
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  scale.quantiles = function() {
    return thresholds.slice();
  };

  scale.copy = function() {
    return quantile_quantile()
        .domain(domain)
        .range(range)
        .unknown(unknown);
  };

  return initRange.apply(scale, arguments);
}

;// ./node_modules/d3-scale/src/quantize.js




function quantize() {
  var x0 = 0,
      x1 = 1,
      n = 1,
      domain = [0.5],
      range = [0, 1],
      unknown;

  function scale(x) {
    return x != null && x <= x ? range[bisect(domain, x, 0, n)] : unknown;
  }

  function rescale() {
    var i = -1;
    domain = new Array(n);
    while (++i < n) domain[i] = ((i + 1) * x1 - (i - n) * x0) / (n + 1);
    return scale;
  }

  scale.domain = function(_) {
    return arguments.length ? ([x0, x1] = _, x0 = +x0, x1 = +x1, rescale()) : [x0, x1];
  };

  scale.range = function(_) {
    return arguments.length ? (n = (range = Array.from(_)).length - 1, rescale()) : range.slice();
  };

  scale.invertExtent = function(y) {
    var i = range.indexOf(y);
    return i < 0 ? [NaN, NaN]
        : i < 1 ? [x0, domain[0]]
        : i >= n ? [domain[n - 1], x1]
        : [domain[i - 1], domain[i]];
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : scale;
  };

  scale.thresholds = function() {
    return domain.slice();
  };

  scale.copy = function() {
    return quantize()
        .domain([x0, x1])
        .range(range)
        .unknown(unknown);
  };

  return initRange.apply(linearish(scale), arguments);
}

;// ./node_modules/d3-scale/src/threshold.js



function threshold() {
  var domain = [0.5],
      range = [0, 1],
      unknown,
      n = 1;

  function scale(x) {
    return x != null && x <= x ? range[bisect(domain, x, 0, n)] : unknown;
  }

  scale.domain = function(_) {
    return arguments.length ? (domain = Array.from(_), n = Math.min(domain.length, range.length - 1), scale) : domain.slice();
  };

  scale.range = function(_) {
    return arguments.length ? (range = Array.from(_), n = Math.min(domain.length, range.length - 1), scale) : range.slice();
  };

  scale.invertExtent = function(y) {
    var i = range.indexOf(y);
    return [domain[i - 1], domain[i]];
  };

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  scale.copy = function() {
    return threshold()
        .domain(domain)
        .range(range)
        .unknown(unknown);
  };

  return initRange.apply(scale, arguments);
}

;// ./node_modules/d3-time/src/duration.js
const durationSecond = 1000;
const durationMinute = durationSecond * 60;
const durationHour = durationMinute * 60;
const durationDay = durationHour * 24;
const durationWeek = durationDay * 7;
const durationMonth = durationDay * 30;
const durationYear = durationDay * 365;

;// ./node_modules/d3-time/src/interval.js
const t0 = new Date, t1 = new Date;

function timeInterval(floori, offseti, count, field) {

  function interval(date) {
    return floori(date = arguments.length === 0 ? new Date : new Date(+date)), date;
  }

  interval.floor = (date) => {
    return floori(date = new Date(+date)), date;
  };

  interval.ceil = (date) => {
    return floori(date = new Date(date - 1)), offseti(date, 1), floori(date), date;
  };

  interval.round = (date) => {
    const d0 = interval(date), d1 = interval.ceil(date);
    return date - d0 < d1 - date ? d0 : d1;
  };

  interval.offset = (date, step) => {
    return offseti(date = new Date(+date), step == null ? 1 : Math.floor(step)), date;
  };

  interval.range = (start, stop, step) => {
    const range = [];
    start = interval.ceil(start);
    step = step == null ? 1 : Math.floor(step);
    if (!(start < stop) || !(step > 0)) return range; // also handles Invalid Date
    let previous;
    do range.push(previous = new Date(+start)), offseti(start, step), floori(start);
    while (previous < start && start < stop);
    return range;
  };

  interval.filter = (test) => {
    return timeInterval((date) => {
      if (date >= date) while (floori(date), !test(date)) date.setTime(date - 1);
    }, (date, step) => {
      if (date >= date) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date, -1), !test(date)) {} // eslint-disable-line no-empty
        } else while (--step >= 0) {
          while (offseti(date, +1), !test(date)) {} // eslint-disable-line no-empty
        }
      }
    });
  };

  if (count) {
    interval.count = (start, end) => {
      t0.setTime(+start), t1.setTime(+end);
      floori(t0), floori(t1);
      return Math.floor(count(t0, t1));
    };

    interval.every = (step) => {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null
          : !(step > 1) ? interval
          : interval.filter(field
              ? (d) => field(d) % step === 0
              : (d) => interval.count(0, d) % step === 0);
    };
  }

  return interval;
}

;// ./node_modules/d3-time/src/millisecond.js


const millisecond = timeInterval(() => {
  // noop
}, (date, step) => {
  date.setTime(+date + step);
}, (start, end) => {
  return end - start;
});

// An optimized implementation for this simple case.
millisecond.every = (k) => {
  k = Math.floor(k);
  if (!isFinite(k) || !(k > 0)) return null;
  if (!(k > 1)) return millisecond;
  return timeInterval((date) => {
    date.setTime(Math.floor(date / k) * k);
  }, (date, step) => {
    date.setTime(+date + step * k);
  }, (start, end) => {
    return (end - start) / k;
  });
};

const milliseconds = millisecond.range;

;// ./node_modules/d3-time/src/second.js



const second = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds());
}, (date, step) => {
  date.setTime(+date + step * durationSecond);
}, (start, end) => {
  return (end - start) / durationSecond;
}, (date) => {
  return date.getUTCSeconds();
});

const seconds = second.range;

;// ./node_modules/d3-time/src/minute.js



const timeMinute = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond);
}, (date, step) => {
  date.setTime(+date + step * durationMinute);
}, (start, end) => {
  return (end - start) / durationMinute;
}, (date) => {
  return date.getMinutes();
});

const timeMinutes = timeMinute.range;

const utcMinute = timeInterval((date) => {
  date.setUTCSeconds(0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationMinute);
}, (start, end) => {
  return (end - start) / durationMinute;
}, (date) => {
  return date.getUTCMinutes();
});

const utcMinutes = utcMinute.range;

;// ./node_modules/d3-time/src/hour.js



const timeHour = timeInterval((date) => {
  date.setTime(date - date.getMilliseconds() - date.getSeconds() * durationSecond - date.getMinutes() * durationMinute);
}, (date, step) => {
  date.setTime(+date + step * durationHour);
}, (start, end) => {
  return (end - start) / durationHour;
}, (date) => {
  return date.getHours();
});

const timeHours = timeHour.range;

const utcHour = timeInterval((date) => {
  date.setUTCMinutes(0, 0, 0);
}, (date, step) => {
  date.setTime(+date + step * durationHour);
}, (start, end) => {
  return (end - start) / durationHour;
}, (date) => {
  return date.getUTCHours();
});

const utcHours = utcHour.range;

;// ./node_modules/d3-time/src/day.js



const timeDay = timeInterval(
  date => date.setHours(0, 0, 0, 0),
  (date, step) => date.setDate(date.getDate() + step),
  (start, end) => (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationDay,
  date => date.getDate() - 1
);

const timeDays = timeDay.range;

const utcDay = timeInterval((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start, end) => {
  return (end - start) / durationDay;
}, (date) => {
  return date.getUTCDate() - 1;
});

const utcDays = utcDay.range;

const unixDay = timeInterval((date) => {
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCDate(date.getUTCDate() + step);
}, (start, end) => {
  return (end - start) / durationDay;
}, (date) => {
  return Math.floor(date / durationDay);
});

const unixDays = unixDay.range;

;// ./node_modules/d3-time/src/week.js



function timeWeekday(i) {
  return timeInterval((date) => {
    date.setDate(date.getDate() - (date.getDay() + 7 - i) % 7);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setDate(date.getDate() + step * 7);
  }, (start, end) => {
    return (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationWeek;
  });
}

const timeSunday = timeWeekday(0);
const timeMonday = timeWeekday(1);
const timeTuesday = timeWeekday(2);
const timeWednesday = timeWeekday(3);
const timeThursday = timeWeekday(4);
const timeFriday = timeWeekday(5);
const timeSaturday = timeWeekday(6);

const timeSundays = timeSunday.range;
const timeMondays = timeMonday.range;
const timeTuesdays = timeTuesday.range;
const timeWednesdays = timeWednesday.range;
const timeThursdays = timeThursday.range;
const timeFridays = timeFriday.range;
const timeSaturdays = timeSaturday.range;

function utcWeekday(i) {
  return timeInterval((date) => {
    date.setUTCDate(date.getUTCDate() - (date.getUTCDay() + 7 - i) % 7);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCDate(date.getUTCDate() + step * 7);
  }, (start, end) => {
    return (end - start) / durationWeek;
  });
}

const utcSunday = utcWeekday(0);
const utcMonday = utcWeekday(1);
const utcTuesday = utcWeekday(2);
const utcWednesday = utcWeekday(3);
const utcThursday = utcWeekday(4);
const utcFriday = utcWeekday(5);
const utcSaturday = utcWeekday(6);

const utcSundays = utcSunday.range;
const utcMondays = utcMonday.range;
const utcTuesdays = utcTuesday.range;
const utcWednesdays = utcWednesday.range;
const utcThursdays = utcThursday.range;
const utcFridays = utcFriday.range;
const utcSaturdays = utcSaturday.range;

;// ./node_modules/d3-time/src/month.js


const timeMonth = timeInterval((date) => {
  date.setDate(1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setMonth(date.getMonth() + step);
}, (start, end) => {
  return end.getMonth() - start.getMonth() + (end.getFullYear() - start.getFullYear()) * 12;
}, (date) => {
  return date.getMonth();
});

const timeMonths = timeMonth.range;

const utcMonth = timeInterval((date) => {
  date.setUTCDate(1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCMonth(date.getUTCMonth() + step);
}, (start, end) => {
  return end.getUTCMonth() - start.getUTCMonth() + (end.getUTCFullYear() - start.getUTCFullYear()) * 12;
}, (date) => {
  return date.getUTCMonth();
});

const utcMonths = utcMonth.range;

;// ./node_modules/d3-time/src/year.js


const timeYear = timeInterval((date) => {
  date.setMonth(0, 1);
  date.setHours(0, 0, 0, 0);
}, (date, step) => {
  date.setFullYear(date.getFullYear() + step);
}, (start, end) => {
  return end.getFullYear() - start.getFullYear();
}, (date) => {
  return date.getFullYear();
});

// An optimized implementation for this simple case.
timeYear.every = (k) => {
  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date) => {
    date.setFullYear(Math.floor(date.getFullYear() / k) * k);
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setFullYear(date.getFullYear() + step * k);
  });
};

const timeYears = timeYear.range;

const utcYear = timeInterval((date) => {
  date.setUTCMonth(0, 1);
  date.setUTCHours(0, 0, 0, 0);
}, (date, step) => {
  date.setUTCFullYear(date.getUTCFullYear() + step);
}, (start, end) => {
  return end.getUTCFullYear() - start.getUTCFullYear();
}, (date) => {
  return date.getUTCFullYear();
});

// An optimized implementation for this simple case.
utcYear.every = (k) => {
  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date) => {
    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k) * k);
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCFullYear(date.getUTCFullYear() + step * k);
  });
};

const utcYears = utcYear.range;

;// ./node_modules/d3-time/src/ticks.js











function ticker(year, month, week, day, hour, minute) {

  const tickIntervals = [
    [second,  1,      durationSecond],
    [second,  5,  5 * durationSecond],
    [second, 15, 15 * durationSecond],
    [second, 30, 30 * durationSecond],
    [minute,  1,      durationMinute],
    [minute,  5,  5 * durationMinute],
    [minute, 15, 15 * durationMinute],
    [minute, 30, 30 * durationMinute],
    [  hour,  1,      durationHour  ],
    [  hour,  3,  3 * durationHour  ],
    [  hour,  6,  6 * durationHour  ],
    [  hour, 12, 12 * durationHour  ],
    [   day,  1,      durationDay   ],
    [   day,  2,  2 * durationDay   ],
    [  week,  1,      durationWeek  ],
    [ month,  1,      durationMonth ],
    [ month,  3,  3 * durationMonth ],
    [  year,  1,      durationYear  ]
  ];

  function ticks(start, stop, count) {
    const reverse = stop < start;
    if (reverse) [start, stop] = [stop, start];
    const interval = count && typeof count.range === "function" ? count : tickInterval(start, stop, count);
    const ticks = interval ? interval.range(start, +stop + 1) : []; // inclusive stop
    return reverse ? ticks.reverse() : ticks;
  }

  function tickInterval(start, stop, count) {
    const target = Math.abs(stop - start) / count;
    const i = bisector(([,, step]) => step).right(tickIntervals, target);
    if (i === tickIntervals.length) return year.every(tickStep(start / durationYear, stop / durationYear, count));
    if (i === 0) return millisecond.every(Math.max(tickStep(start, stop, count), 1));
    const [t, step] = tickIntervals[target / tickIntervals[i - 1][2] < tickIntervals[i][2] / target ? i - 1 : i];
    return t.every(step);
  }

  return [ticks, tickInterval];
}

const [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
const [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);



;// ./node_modules/d3-time-format/src/locale.js


function localDate(d) {
  if (0 <= d.y && d.y < 100) {
    var date = new Date(-1, d.m, d.d, d.H, d.M, d.S, d.L);
    date.setFullYear(d.y);
    return date;
  }
  return new Date(d.y, d.m, d.d, d.H, d.M, d.S, d.L);
}

function utcDate(d) {
  if (0 <= d.y && d.y < 100) {
    var date = new Date(Date.UTC(-1, d.m, d.d, d.H, d.M, d.S, d.L));
    date.setUTCFullYear(d.y);
    return date;
  }
  return new Date(Date.UTC(d.y, d.m, d.d, d.H, d.M, d.S, d.L));
}

function newDate(y, m, d) {
  return {y: y, m: m, d: d, H: 0, M: 0, S: 0, L: 0};
}

function formatLocale(locale) {
  var locale_dateTime = locale.dateTime,
      locale_date = locale.date,
      locale_time = locale.time,
      locale_periods = locale.periods,
      locale_weekdays = locale.days,
      locale_shortWeekdays = locale.shortDays,
      locale_months = locale.months,
      locale_shortMonths = locale.shortMonths;

  var periodRe = formatRe(locale_periods),
      periodLookup = formatLookup(locale_periods),
      weekdayRe = formatRe(locale_weekdays),
      weekdayLookup = formatLookup(locale_weekdays),
      shortWeekdayRe = formatRe(locale_shortWeekdays),
      shortWeekdayLookup = formatLookup(locale_shortWeekdays),
      monthRe = formatRe(locale_months),
      monthLookup = formatLookup(locale_months),
      shortMonthRe = formatRe(locale_shortMonths),
      shortMonthLookup = formatLookup(locale_shortMonths);

  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth,
    "e": formatDayOfMonth,
    "f": formatMicroseconds,
    "g": formatYearISO,
    "G": formatFullYearISO,
    "H": formatHour24,
    "I": formatHour12,
    "j": formatDayOfYear,
    "L": formatMilliseconds,
    "m": formatMonthNumber,
    "M": formatMinutes,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatSeconds,
    "u": formatWeekdayNumberMonday,
    "U": formatWeekNumberSunday,
    "V": formatWeekNumberISO,
    "w": formatWeekdayNumberSunday,
    "W": formatWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatYear,
    "Y": formatFullYear,
    "Z": formatZone,
    "%": formatLiteralPercent
  };

  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth,
    "e": formatUTCDayOfMonth,
    "f": formatUTCMicroseconds,
    "g": formatUTCYearISO,
    "G": formatUTCFullYearISO,
    "H": formatUTCHour24,
    "I": formatUTCHour12,
    "j": formatUTCDayOfYear,
    "L": formatUTCMilliseconds,
    "m": formatUTCMonthNumber,
    "M": formatUTCMinutes,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatUTCSeconds,
    "u": formatUTCWeekdayNumberMonday,
    "U": formatUTCWeekNumberSunday,
    "V": formatUTCWeekNumberISO,
    "w": formatUTCWeekdayNumberSunday,
    "W": formatUTCWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatUTCYear,
    "Y": formatUTCFullYear,
    "Z": formatUTCZone,
    "%": formatLiteralPercent
  };

  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth,
    "e": parseDayOfMonth,
    "f": parseMicroseconds,
    "g": parseYear,
    "G": parseFullYear,
    "H": parseHour24,
    "I": parseHour24,
    "j": parseDayOfYear,
    "L": parseMilliseconds,
    "m": parseMonthNumber,
    "M": parseMinutes,
    "p": parsePeriod,
    "q": parseQuarter,
    "Q": parseUnixTimestamp,
    "s": parseUnixTimestampSeconds,
    "S": parseSeconds,
    "u": parseWeekdayNumberMonday,
    "U": parseWeekNumberSunday,
    "V": parseWeekNumberISO,
    "w": parseWeekdayNumberSunday,
    "W": parseWeekNumberMonday,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear,
    "Y": parseFullYear,
    "Z": parseZone,
    "%": parseLiteralPercent
  };

  // These recursive directive definitions must be deferred.
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);

  function newFormat(specifier, formats) {
    return function(date) {
      var string = [],
          i = -1,
          j = 0,
          n = specifier.length,
          c,
          pad,
          format;

      if (!(date instanceof Date)) date = new Date(+date);

      while (++i < n) {
        if (specifier.charCodeAt(i) === 37) {
          string.push(specifier.slice(j, i));
          if ((pad = pads[c = specifier.charAt(++i)]) != null) c = specifier.charAt(++i);
          else pad = c === "e" ? " " : "0";
          if (format = formats[c]) c = format(date, pad);
          string.push(c);
          j = i + 1;
        }
      }

      string.push(specifier.slice(j, i));
      return string.join("");
    };
  }

  function newParse(specifier, Z) {
    return function(string) {
      var d = newDate(1900, undefined, 1),
          i = parseSpecifier(d, specifier, string += "", 0),
          week, day;
      if (i != string.length) return null;

      // If a UNIX timestamp is specified, return it.
      if ("Q" in d) return new Date(d.Q);
      if ("s" in d) return new Date(d.s * 1000 + ("L" in d ? d.L : 0));

      // If this is utcParse, never use the local timezone.
      if (Z && !("Z" in d)) d.Z = 0;

      // The am-pm flag is 0 for AM, and 1 for PM.
      if ("p" in d) d.H = d.H % 12 + d.p * 12;

      // If the month was not specified, inherit from the quarter.
      if (d.m === undefined) d.m = "q" in d ? d.q : 0;

      // Convert day-of-week and week-of-year to day-of-year.
      if ("V" in d) {
        if (d.V < 1 || d.V > 53) return null;
        if (!("w" in d)) d.w = 1;
        if ("Z" in d) {
          week = utcDate(newDate(d.y, 0, 1)), day = week.getUTCDay();
          week = day > 4 || day === 0 ? utcMonday.ceil(week) : utcMonday(week);
          week = utcDay.offset(week, (d.V - 1) * 7);
          d.y = week.getUTCFullYear();
          d.m = week.getUTCMonth();
          d.d = week.getUTCDate() + (d.w + 6) % 7;
        } else {
          week = localDate(newDate(d.y, 0, 1)), day = week.getDay();
          week = day > 4 || day === 0 ? timeMonday.ceil(week) : timeMonday(week);
          week = timeDay.offset(week, (d.V - 1) * 7);
          d.y = week.getFullYear();
          d.m = week.getMonth();
          d.d = week.getDate() + (d.w + 6) % 7;
        }
      } else if ("W" in d || "U" in d) {
        if (!("w" in d)) d.w = "u" in d ? d.u % 7 : "W" in d ? 1 : 0;
        day = "Z" in d ? utcDate(newDate(d.y, 0, 1)).getUTCDay() : localDate(newDate(d.y, 0, 1)).getDay();
        d.m = 0;
        d.d = "W" in d ? (d.w + 6) % 7 + d.W * 7 - (day + 5) % 7 : d.w + d.U * 7 - (day + 6) % 7;
      }

      // If a time zone is specified, all fields are interpreted as UTC and then
      // offset according to the specified time zone.
      if ("Z" in d) {
        d.H += d.Z / 100 | 0;
        d.M += d.Z % 100;
        return utcDate(d);
      }

      // Otherwise, all fields are in local time.
      return localDate(d);
    };
  }

  function parseSpecifier(d, specifier, string, j) {
    var i = 0,
        n = specifier.length,
        m = string.length,
        c,
        parse;

    while (i < n) {
      if (j >= m) return -1;
      c = specifier.charCodeAt(i++);
      if (c === 37) {
        c = specifier.charAt(i++);
        parse = parses[c in pads ? specifier.charAt(i++) : c];
        if (!parse || ((j = parse(d, string, j)) < 0)) return -1;
      } else if (c != string.charCodeAt(j++)) {
        return -1;
      }
    }

    return j;
  }

  function parsePeriod(d, string, i) {
    var n = periodRe.exec(string.slice(i));
    return n ? (d.p = periodLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }

  function parseShortWeekday(d, string, i) {
    var n = shortWeekdayRe.exec(string.slice(i));
    return n ? (d.w = shortWeekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }

  function parseWeekday(d, string, i) {
    var n = weekdayRe.exec(string.slice(i));
    return n ? (d.w = weekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }

  function parseShortMonth(d, string, i) {
    var n = shortMonthRe.exec(string.slice(i));
    return n ? (d.m = shortMonthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }

  function parseMonth(d, string, i) {
    var n = monthRe.exec(string.slice(i));
    return n ? (d.m = monthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }

  function parseLocaleDateTime(d, string, i) {
    return parseSpecifier(d, locale_dateTime, string, i);
  }

  function parseLocaleDate(d, string, i) {
    return parseSpecifier(d, locale_date, string, i);
  }

  function parseLocaleTime(d, string, i) {
    return parseSpecifier(d, locale_time, string, i);
  }

  function formatShortWeekday(d) {
    return locale_shortWeekdays[d.getDay()];
  }

  function formatWeekday(d) {
    return locale_weekdays[d.getDay()];
  }

  function formatShortMonth(d) {
    return locale_shortMonths[d.getMonth()];
  }

  function formatMonth(d) {
    return locale_months[d.getMonth()];
  }

  function formatPeriod(d) {
    return locale_periods[+(d.getHours() >= 12)];
  }

  function formatQuarter(d) {
    return 1 + ~~(d.getMonth() / 3);
  }

  function formatUTCShortWeekday(d) {
    return locale_shortWeekdays[d.getUTCDay()];
  }

  function formatUTCWeekday(d) {
    return locale_weekdays[d.getUTCDay()];
  }

  function formatUTCShortMonth(d) {
    return locale_shortMonths[d.getUTCMonth()];
  }

  function formatUTCMonth(d) {
    return locale_months[d.getUTCMonth()];
  }

  function formatUTCPeriod(d) {
    return locale_periods[+(d.getUTCHours() >= 12)];
  }

  function formatUTCQuarter(d) {
    return 1 + ~~(d.getUTCMonth() / 3);
  }

  return {
    format: function(specifier) {
      var f = newFormat(specifier += "", formats);
      f.toString = function() { return specifier; };
      return f;
    },
    parse: function(specifier) {
      var p = newParse(specifier += "", false);
      p.toString = function() { return specifier; };
      return p;
    },
    utcFormat: function(specifier) {
      var f = newFormat(specifier += "", utcFormats);
      f.toString = function() { return specifier; };
      return f;
    },
    utcParse: function(specifier) {
      var p = newParse(specifier += "", true);
      p.toString = function() { return specifier; };
      return p;
    }
  };
}

var pads = {"-": "", "_": " ", "0": "0"},
    numberRe = /^\s*\d+/, // note: ignores next directive
    percentRe = /^%/,
    requoteRe = /[\\^$*+?|[\]().{}]/g;

function pad(value, fill, width) {
  var sign = value < 0 ? "-" : "",
      string = (sign ? -value : value) + "",
      length = string.length;
  return sign + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}

function requote(s) {
  return s.replace(requoteRe, "\\$&");
}

function formatRe(names) {
  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
}

function formatLookup(names) {
  return new Map(names.map((name, i) => [name.toLowerCase(), i]));
}

function parseWeekdayNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.w = +n[0], i + n[0].length) : -1;
}

function parseWeekdayNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.u = +n[0], i + n[0].length) : -1;
}

function parseWeekNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.U = +n[0], i + n[0].length) : -1;
}

function parseWeekNumberISO(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.V = +n[0], i + n[0].length) : -1;
}

function parseWeekNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.W = +n[0], i + n[0].length) : -1;
}

function parseFullYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 4));
  return n ? (d.y = +n[0], i + n[0].length) : -1;
}

function parseYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.y = +n[0] + (+n[0] > 68 ? 1900 : 2000), i + n[0].length) : -1;
}

function parseZone(d, string, i) {
  var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i, i + 6));
  return n ? (d.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), i + n[0].length) : -1;
}

function parseQuarter(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.q = n[0] * 3 - 3, i + n[0].length) : -1;
}

function parseMonthNumber(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.m = n[0] - 1, i + n[0].length) : -1;
}

function parseDayOfMonth(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.d = +n[0], i + n[0].length) : -1;
}

function parseDayOfYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.m = 0, d.d = +n[0], i + n[0].length) : -1;
}

function parseHour24(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.H = +n[0], i + n[0].length) : -1;
}

function parseMinutes(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.M = +n[0], i + n[0].length) : -1;
}

function parseSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.S = +n[0], i + n[0].length) : -1;
}

function parseMilliseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.L = +n[0], i + n[0].length) : -1;
}

function parseMicroseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 6));
  return n ? (d.L = Math.floor(n[0] / 1000), i + n[0].length) : -1;
}

function parseLiteralPercent(d, string, i) {
  var n = percentRe.exec(string.slice(i, i + 1));
  return n ? i + n[0].length : -1;
}

function parseUnixTimestamp(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.Q = +n[0], i + n[0].length) : -1;
}

function parseUnixTimestampSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.s = +n[0], i + n[0].length) : -1;
}

function formatDayOfMonth(d, p) {
  return pad(d.getDate(), p, 2);
}

function formatHour24(d, p) {
  return pad(d.getHours(), p, 2);
}

function formatHour12(d, p) {
  return pad(d.getHours() % 12 || 12, p, 2);
}

function formatDayOfYear(d, p) {
  return pad(1 + timeDay.count(timeYear(d), d), p, 3);
}

function formatMilliseconds(d, p) {
  return pad(d.getMilliseconds(), p, 3);
}

function formatMicroseconds(d, p) {
  return formatMilliseconds(d, p) + "000";
}

function formatMonthNumber(d, p) {
  return pad(d.getMonth() + 1, p, 2);
}

function formatMinutes(d, p) {
  return pad(d.getMinutes(), p, 2);
}

function formatSeconds(d, p) {
  return pad(d.getSeconds(), p, 2);
}

function formatWeekdayNumberMonday(d) {
  var day = d.getDay();
  return day === 0 ? 7 : day;
}

function formatWeekNumberSunday(d, p) {
  return pad(timeSunday.count(timeYear(d) - 1, d), p, 2);
}

function dISO(d) {
  var day = d.getDay();
  return (day >= 4 || day === 0) ? timeThursday(d) : timeThursday.ceil(d);
}

function formatWeekNumberISO(d, p) {
  d = dISO(d);
  return pad(timeThursday.count(timeYear(d), d) + (timeYear(d).getDay() === 4), p, 2);
}

function formatWeekdayNumberSunday(d) {
  return d.getDay();
}

function formatWeekNumberMonday(d, p) {
  return pad(timeMonday.count(timeYear(d) - 1, d), p, 2);
}

function formatYear(d, p) {
  return pad(d.getFullYear() % 100, p, 2);
}

function formatYearISO(d, p) {
  d = dISO(d);
  return pad(d.getFullYear() % 100, p, 2);
}

function formatFullYear(d, p) {
  return pad(d.getFullYear() % 10000, p, 4);
}

function formatFullYearISO(d, p) {
  var day = d.getDay();
  d = (day >= 4 || day === 0) ? timeThursday(d) : timeThursday.ceil(d);
  return pad(d.getFullYear() % 10000, p, 4);
}

function formatZone(d) {
  var z = d.getTimezoneOffset();
  return (z > 0 ? "-" : (z *= -1, "+"))
      + pad(z / 60 | 0, "0", 2)
      + pad(z % 60, "0", 2);
}

function formatUTCDayOfMonth(d, p) {
  return pad(d.getUTCDate(), p, 2);
}

function formatUTCHour24(d, p) {
  return pad(d.getUTCHours(), p, 2);
}

function formatUTCHour12(d, p) {
  return pad(d.getUTCHours() % 12 || 12, p, 2);
}

function formatUTCDayOfYear(d, p) {
  return pad(1 + utcDay.count(utcYear(d), d), p, 3);
}

function formatUTCMilliseconds(d, p) {
  return pad(d.getUTCMilliseconds(), p, 3);
}

function formatUTCMicroseconds(d, p) {
  return formatUTCMilliseconds(d, p) + "000";
}

function formatUTCMonthNumber(d, p) {
  return pad(d.getUTCMonth() + 1, p, 2);
}

function formatUTCMinutes(d, p) {
  return pad(d.getUTCMinutes(), p, 2);
}

function formatUTCSeconds(d, p) {
  return pad(d.getUTCSeconds(), p, 2);
}

function formatUTCWeekdayNumberMonday(d) {
  var dow = d.getUTCDay();
  return dow === 0 ? 7 : dow;
}

function formatUTCWeekNumberSunday(d, p) {
  return pad(utcSunday.count(utcYear(d) - 1, d), p, 2);
}

function UTCdISO(d) {
  var day = d.getUTCDay();
  return (day >= 4 || day === 0) ? utcThursday(d) : utcThursday.ceil(d);
}

function formatUTCWeekNumberISO(d, p) {
  d = UTCdISO(d);
  return pad(utcThursday.count(utcYear(d), d) + (utcYear(d).getUTCDay() === 4), p, 2);
}

function formatUTCWeekdayNumberSunday(d) {
  return d.getUTCDay();
}

function formatUTCWeekNumberMonday(d, p) {
  return pad(utcMonday.count(utcYear(d) - 1, d), p, 2);
}

function formatUTCYear(d, p) {
  return pad(d.getUTCFullYear() % 100, p, 2);
}

function formatUTCYearISO(d, p) {
  d = UTCdISO(d);
  return pad(d.getUTCFullYear() % 100, p, 2);
}

function formatUTCFullYear(d, p) {
  return pad(d.getUTCFullYear() % 10000, p, 4);
}

function formatUTCFullYearISO(d, p) {
  var day = d.getUTCDay();
  d = (day >= 4 || day === 0) ? utcThursday(d) : utcThursday.ceil(d);
  return pad(d.getUTCFullYear() % 10000, p, 4);
}

function formatUTCZone() {
  return "+0000";
}

function formatLiteralPercent() {
  return "%";
}

function formatUnixTimestamp(d) {
  return +d;
}

function formatUnixTimestampSeconds(d) {
  return Math.floor(+d / 1000);
}

;// ./node_modules/d3-time-format/src/defaultLocale.js


var src_defaultLocale_locale;
var timeFormat;
var timeParse;
var utcFormat;
var utcParse;

defaultLocale_defaultLocale({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});

function defaultLocale_defaultLocale(definition) {
  src_defaultLocale_locale = formatLocale(definition);
  timeFormat = src_defaultLocale_locale.format;
  timeParse = src_defaultLocale_locale.parse;
  utcFormat = src_defaultLocale_locale.utcFormat;
  utcParse = src_defaultLocale_locale.utcParse;
  return src_defaultLocale_locale;
}

;// ./node_modules/d3-scale/src/time.js






function time_date(t) {
  return new Date(t);
}

function time_number(t) {
  return t instanceof Date ? +t : +new Date(+t);
}

function calendar(ticks, tickInterval, year, month, week, day, hour, minute, second, format) {
  var scale = continuous(),
      invert = scale.invert,
      domain = scale.domain;

  var formatMillisecond = format(".%L"),
      formatSecond = format(":%S"),
      formatMinute = format("%I:%M"),
      formatHour = format("%I %p"),
      formatDay = format("%a %d"),
      formatWeek = format("%b %d"),
      formatMonth = format("%B"),
      formatYear = format("%Y");

  function tickFormat(date) {
    return (second(date) < date ? formatMillisecond
        : minute(date) < date ? formatSecond
        : hour(date) < date ? formatMinute
        : day(date) < date ? formatHour
        : month(date) < date ? (week(date) < date ? formatDay : formatWeek)
        : year(date) < date ? formatMonth
        : formatYear)(date);
  }

  scale.invert = function(y) {
    return new Date(invert(y));
  };

  scale.domain = function(_) {
    return arguments.length ? domain(Array.from(_, time_number)) : domain().map(time_date);
  };

  scale.ticks = function(interval) {
    var d = domain();
    return ticks(d[0], d[d.length - 1], interval == null ? 10 : interval);
  };

  scale.tickFormat = function(count, specifier) {
    return specifier == null ? tickFormat : format(specifier);
  };

  scale.nice = function(interval) {
    var d = domain();
    if (!interval || typeof interval.range !== "function") interval = tickInterval(d[0], d[d.length - 1], interval == null ? 10 : interval);
    return interval ? domain(nice(d, interval)) : scale;
  };

  scale.copy = function() {
    return copy(scale, calendar(ticks, tickInterval, year, month, week, day, hour, minute, second, format));
  };

  return scale;
}

function time() {
  return initRange.apply(calendar(timeTicks, timeTickInterval, timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute, second, timeFormat).domain([new Date(2000, 0, 1), new Date(2000, 0, 2)]), arguments);
}

;// ./node_modules/d3-scale/src/utcTime.js





function utcTime() {
  return initRange.apply(calendar(utcTicks, utcTickInterval, utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, utcFormat).domain([Date.UTC(2000, 0, 1), Date.UTC(2000, 0, 2)]), arguments);
}

;// ./node_modules/d3-scale/src/sequential.js








function sequential_transformer() {
  var x0 = 0,
      x1 = 1,
      t0,
      t1,
      k10,
      transform,
      interpolator = identity,
      clamp = false,
      unknown;

  function scale(x) {
    return x == null || isNaN(x = +x) ? unknown : interpolator(k10 === 0 ? 0.5 : (x = (transform(x) - t0) * k10, clamp ? Math.max(0, Math.min(1, x)) : x));
  }

  scale.domain = function(_) {
    return arguments.length ? ([x0, x1] = _, t0 = transform(x0 = +x0), t1 = transform(x1 = +x1), k10 = t0 === t1 ? 0 : 1 / (t1 - t0), scale) : [x0, x1];
  };

  scale.clamp = function(_) {
    return arguments.length ? (clamp = !!_, scale) : clamp;
  };

  scale.interpolator = function(_) {
    return arguments.length ? (interpolator = _, scale) : interpolator;
  };

  function range(interpolate) {
    return function(_) {
      var r0, r1;
      return arguments.length ? ([r0, r1] = _, interpolator = interpolate(r0, r1), scale) : [interpolator(0), interpolator(1)];
    };
  }

  scale.range = range(value);

  scale.rangeRound = range(src_round);

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  return function(t) {
    transform = t, t0 = t(x0), t1 = t(x1), k10 = t0 === t1 ? 0 : 1 / (t1 - t0);
    return scale;
  };
}

function sequential_copy(source, target) {
  return target
      .domain(source.domain())
      .interpolator(source.interpolator())
      .clamp(source.clamp())
      .unknown(source.unknown());
}

function sequential() {
  var scale = linearish(sequential_transformer()(identity));

  scale.copy = function() {
    return sequential_copy(scale, sequential());
  };

  return initInterpolator.apply(scale, arguments);
}

function sequentialLog() {
  var scale = loggish(sequential_transformer()).domain([1, 10]);

  scale.copy = function() {
    return sequential_copy(scale, sequentialLog()).base(scale.base());
  };

  return initInterpolator.apply(scale, arguments);
}

function sequentialSymlog() {
  var scale = symlogish(sequential_transformer());

  scale.copy = function() {
    return sequential_copy(scale, sequentialSymlog()).constant(scale.constant());
  };

  return initInterpolator.apply(scale, arguments);
}

function sequentialPow() {
  var scale = powish(sequential_transformer());

  scale.copy = function() {
    return sequential_copy(scale, sequentialPow()).exponent(scale.exponent());
  };

  return initInterpolator.apply(scale, arguments);
}

function sequentialSqrt() {
  return sequentialPow.apply(null, arguments).exponent(0.5);
}

;// ./node_modules/d3-scale/src/sequentialQuantile.js




function sequentialQuantile() {
  var domain = [],
      interpolator = identity;

  function scale(x) {
    if (x != null && !isNaN(x = +x)) return interpolator((bisect(domain, x, 1) - 1) / (domain.length - 1));
  }

  scale.domain = function(_) {
    if (!arguments.length) return domain.slice();
    domain = [];
    for (let d of _) if (d != null && !isNaN(d = +d)) domain.push(d);
    domain.sort(ascending);
    return scale;
  };

  scale.interpolator = function(_) {
    return arguments.length ? (interpolator = _, scale) : interpolator;
  };

  scale.range = function() {
    return domain.map((d, i) => interpolator(i / (domain.length - 1)));
  };

  scale.quantiles = function(n) {
    return Array.from({length: n + 1}, (_, i) => quantile(domain, i / n));
  };

  scale.copy = function() {
    return sequentialQuantile(interpolator).domain(domain);
  };

  return initInterpolator.apply(scale, arguments);
}

;// ./node_modules/d3-interpolate/src/piecewise.js


function piecewise(interpolate, values) {
  if (values === undefined) values = interpolate, interpolate = value;
  var i = 0, n = values.length - 1, v = values[0], I = new Array(n < 0 ? 0 : n);
  while (i < n) I[i] = interpolate(v, v = values[++i]);
  return function(t) {
    var i = Math.max(0, Math.min(n - 1, Math.floor(t *= n)));
    return I[i](t - i);
  };
}

;// ./node_modules/d3-scale/src/diverging.js









function diverging_transformer() {
  var x0 = 0,
      x1 = 0.5,
      x2 = 1,
      s = 1,
      t0,
      t1,
      t2,
      k10,
      k21,
      interpolator = identity,
      transform,
      clamp = false,
      unknown;

  function scale(x) {
    return isNaN(x = +x) ? unknown : (x = 0.5 + ((x = +transform(x)) - t1) * (s * x < s * t1 ? k10 : k21), interpolator(clamp ? Math.max(0, Math.min(1, x)) : x));
  }

  scale.domain = function(_) {
    return arguments.length ? ([x0, x1, x2] = _, t0 = transform(x0 = +x0), t1 = transform(x1 = +x1), t2 = transform(x2 = +x2), k10 = t0 === t1 ? 0 : 0.5 / (t1 - t0), k21 = t1 === t2 ? 0 : 0.5 / (t2 - t1), s = t1 < t0 ? -1 : 1, scale) : [x0, x1, x2];
  };

  scale.clamp = function(_) {
    return arguments.length ? (clamp = !!_, scale) : clamp;
  };

  scale.interpolator = function(_) {
    return arguments.length ? (interpolator = _, scale) : interpolator;
  };

  function range(interpolate) {
    return function(_) {
      var r0, r1, r2;
      return arguments.length ? ([r0, r1, r2] = _, interpolator = piecewise(interpolate, [r0, r1, r2]), scale) : [interpolator(0), interpolator(0.5), interpolator(1)];
    };
  }

  scale.range = range(value);

  scale.rangeRound = range(src_round);

  scale.unknown = function(_) {
    return arguments.length ? (unknown = _, scale) : unknown;
  };

  return function(t) {
    transform = t, t0 = t(x0), t1 = t(x1), t2 = t(x2), k10 = t0 === t1 ? 0 : 0.5 / (t1 - t0), k21 = t1 === t2 ? 0 : 0.5 / (t2 - t1), s = t1 < t0 ? -1 : 1;
    return scale;
  };
}

function diverging() {
  var scale = linearish(diverging_transformer()(identity));

  scale.copy = function() {
    return sequential_copy(scale, diverging());
  };

  return initInterpolator.apply(scale, arguments);
}

function divergingLog() {
  var scale = loggish(diverging_transformer()).domain([0.1, 1, 10]);

  scale.copy = function() {
    return sequential_copy(scale, divergingLog()).base(scale.base());
  };

  return initInterpolator.apply(scale, arguments);
}

function divergingSymlog() {
  var scale = symlogish(diverging_transformer());

  scale.copy = function() {
    return sequential_copy(scale, divergingSymlog()).constant(scale.constant());
  };

  return initInterpolator.apply(scale, arguments);
}

function divergingPow() {
  var scale = powish(diverging_transformer());

  scale.copy = function() {
    return sequential_copy(scale, divergingPow()).exponent(scale.exponent());
  };

  return initInterpolator.apply(scale, arguments);
}

function divergingSqrt() {
  return divergingPow.apply(null, arguments).exponent(0.5);
}

;// ./node_modules/d3-scale/src/index.js


































;// ./node_modules/victory-vendor/es/d3-scale.js

// `victory-vendor/d3-scale` (ESM)
// See upstream license: https://github.com/d3/d3-scale/blob/main/LICENSE
//
// Our ESM package uses the underlying installed dependencies of `node_modules/d3-scale`


;// ./node_modules/recharts/es6/state/selectors/dataSelectors.js

/**
 * This selector always returns the data with the indexes set by a Brush.
 * Trouble is, that might or might not be what you want.
 *
 * In charts with Brush, you will sometimes want to select the full range of data, and sometimes the one decided by the Brush
 * - even if the Brush is active, the panorama inside the Brush should show the full range of data.
 *
 * So instead of this selector, consider using either selectChartDataAndAlwaysIgnoreIndexes or selectChartDataWithIndexesIfNotInPanorama
 *
 * @param state RechartsRootState
 * @returns data defined on the chart root element, such as BarChart or ScatterChart
 */
var selectChartDataWithIndexes = state => state.chartData;

/**
 * This selector will always return the full range of data, ignoring the indexes set by a Brush.
 * Useful for when you want to render the full range of data, even if a Brush is active.
 * For example: in the Brush panorama, in Legend, in Tooltip.
 */
var selectChartDataAndAlwaysIgnoreIndexes = createSelector([selectChartDataWithIndexes], dataState => {
  var dataEndIndex = dataState.chartData != null ? dataState.chartData.length - 1 : 0;
  return {
    chartData: dataState.chartData,
    computedData: dataState.computedData,
    dataEndIndex,
    dataStartIndex: 0
  };
});
var selectChartDataWithIndexesIfNotInPanoramaPosition4 = (state, _unused1, _unused2, isPanorama) => {
  if (isPanorama) {
    return selectChartDataAndAlwaysIgnoreIndexes(state);
  }
  return selectChartDataWithIndexes(state);
};
var selectChartDataWithIndexesIfNotInPanoramaPosition3 = (state, _unused1, isPanorama) => {
  if (isPanorama) {
    return selectChartDataAndAlwaysIgnoreIndexes(state);
  }
  return selectChartDataWithIndexes(state);
};
;// ./node_modules/recharts/es6/util/isDomainSpecifiedByUser.js



function isWellFormedNumberDomain(v) {
  if (Array.isArray(v) && v.length === 2) {
    var [min, max] = v;
    if (isWellBehavedNumber(min) && isWellBehavedNumber(max)) {
      return true;
    }
  }
  return false;
}
function extendDomain(providedDomain, boundaryDomain, allowDataOverflow) {
  if (allowDataOverflow) {
    // If the data are allowed to overflow - we're fine with whatever user provided
    return providedDomain;
  }
  /*
   * If the data are not allowed to overflow - we need to extend the domain.
   * Means that effectively the user is allowed to make the domain larger
   * but not smaller.
   */
  return [Math.min(providedDomain[0], boundaryDomain[0]), Math.max(providedDomain[1], boundaryDomain[1])];
}

/**
 * So Recharts allows users to provide their own domains,
 * but it also places some expectations on what the domain is.
 * We can improve on the typescript typing, but we also need a runtime test
 to observe that the user-provided domain is well-formed,
 * that is: an array with exactly two numbers.
 *
 * This function does not accept data as an argument.
 * This is to enable a performance optimization - if the domain is there,
 * and we know what it is without traversing all the data,
 * then we don't have to traverse all the data!
 *
 * If the user-provided domain is not well-formed,
 * this function will return undefined - in which case we should traverse the data to calculate the real domain.
 *
 * This function is for parsing the numerical domain only.
 *
 * @param userDomain external prop, user provided, before validation. Can have various shapes: array, function, special magical strings inside too.
 * @param allowDataOverflow boolean, provided by users. If true then the data domain wins
 *
 * @return [min, max] domain if it's well-formed; undefined if the domain is invalid
 */
function numericalDomainSpecifiedWithoutRequiringData(userDomain, allowDataOverflow) {
  if (!allowDataOverflow) {
    // Cannot compute data overflow if the data is not provided
    return undefined;
  }
  if (typeof userDomain === 'function') {
    // The user function expects the data to be provided as an argument
    return undefined;
  }
  if (Array.isArray(userDomain) && userDomain.length === 2) {
    var [providedMin, providedMax] = userDomain;
    var finalMin, finalMax;
    if (isWellBehavedNumber(providedMin)) {
      finalMin = providedMin;
    } else if (typeof providedMin === 'function') {
      // The user function expects the data to be provided as an argument
      return undefined;
    }
    if (isWellBehavedNumber(providedMax)) {
      finalMax = providedMax;
    } else if (typeof providedMax === 'function') {
      // The user function expects the data to be provided as an argument
      return undefined;
    }
    var candidate = [finalMin, finalMax];
    if (isWellFormedNumberDomain(candidate)) {
      return candidate;
    }
  }
  return undefined;
}

/**
 * So Recharts allows users to provide their own domains,
 * but it also places some expectations on what the domain is.
 * We can improve on the typescript typing, but we also need a runtime test
 * to observe that the user-provided domain is well-formed,
 * that is: an array with exactly two numbers.
 * If the user-provided domain is not well-formed,
 * this function will return undefined - in which case we should traverse the data to calculate the real domain.
 *
 * This function is for parsing the numerical domain only.
 *
 * You are probably thinking, why does domain need tick count?
 * Well it adjusts the domain based on where the "nice ticks" land, and nice ticks depend on the tick count.
 *
 * @param userDomain external prop, user provided, before validation. Can have various shapes: array, function, special magical strings inside too.
 * @param dataDomain calculated from data. Can be undefined, as an option for performance optimization
 * @param allowDataOverflow provided by users. If true then the data domain wins
 *
 * @return [min, max] domain if it's well-formed; undefined if the domain is invalid
 */
function parseNumericalUserDomain(userDomain, dataDomain, allowDataOverflow) {
  if (!allowDataOverflow && dataDomain == null) {
    // Cannot compute data overflow if the data is not provided
    return undefined;
  }
  if (typeof userDomain === 'function' && dataDomain != null) {
    try {
      var result = userDomain(dataDomain, allowDataOverflow);
      if (isWellFormedNumberDomain(result)) {
        return extendDomain(result, dataDomain, allowDataOverflow);
      }
    } catch (_unused) {
      /* ignore the exception and compute domain from data later */
    }
  }
  if (Array.isArray(userDomain) && userDomain.length === 2) {
    var [providedMin, providedMax] = userDomain;
    var finalMin, finalMax;
    if (providedMin === 'auto') {
      if (dataDomain != null) {
        finalMin = Math.min(...dataDomain);
      }
    } else if (isNumber(providedMin)) {
      finalMin = providedMin;
    } else if (typeof providedMin === 'function') {
      try {
        if (dataDomain != null) {
          finalMin = providedMin(dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[0]);
        }
      } catch (_unused2) {
        /* ignore the exception and compute domain from data later */
      }
    } else if (typeof providedMin === 'string' && MIN_VALUE_REG.test(providedMin)) {
      var match = MIN_VALUE_REG.exec(providedMin);
      if (match == null || match[1] == null || dataDomain == null) {
        finalMin = undefined;
      } else {
        var value = +match[1];
        finalMin = dataDomain[0] - value;
      }
    } else {
      finalMin = dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[0];
    }
    if (providedMax === 'auto') {
      if (dataDomain != null) {
        finalMax = Math.max(...dataDomain);
      }
    } else if (isNumber(providedMax)) {
      finalMax = providedMax;
    } else if (typeof providedMax === 'function') {
      try {
        if (dataDomain != null) {
          finalMax = providedMax(dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[1]);
        }
      } catch (_unused3) {
        /* ignore the exception and compute domain from data later */
      }
    } else if (typeof providedMax === 'string' && MAX_VALUE_REG.test(providedMax)) {
      var _match = MAX_VALUE_REG.exec(providedMax);
      if (_match == null || _match[1] == null || dataDomain == null) {
        finalMax = undefined;
      } else {
        var _value = +_match[1];
        finalMax = dataDomain[1] + _value;
      }
    } else {
      finalMax = dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[1];
    }
    var candidate = [finalMin, finalMax];
    if (isWellFormedNumberDomain(candidate)) {
      if (dataDomain == null) {
        return candidate;
      }
      return extendDomain(candidate, dataDomain, allowDataOverflow);
    }
  }
  return undefined;
}
// EXTERNAL MODULE: ./node_modules/decimal.js-light/decimal.js
var decimal = __webpack_require__(8351);
var decimal_default = /*#__PURE__*/__webpack_require__.n(decimal);
;// ./node_modules/recharts/es6/util/scale/util/arithmetic.js
/**
 * @fileOverview Some common arithmetic methods
 * @author xile611
 * @date 2015-09-17
 */


/**
 * Get the digit count of a number.
 * If the absolute value is in the interval [0.1, 1), the result is 0.
 * If the absolute value is in the interval [0.01, 0.1), the digit count is -1.
 * If the absolute value is in the interval [0.001, 0.01), the digit count is -2.
 *
 * @param  {Number} value The number
 * @return {Integer}      Digit count
 */
function getDigitCount(value) {
  var result;
  if (value === 0) {
    result = 1;
  } else {
    result = Math.floor(new (decimal_default())(value).abs().log(10).toNumber()) + 1;
  }
  return result;
}

/**
 * Get the data in the interval [start, end) with a fixed step.
 * Also handles JS calculation precision issues.
 *
 * @param  {Decimal} start Start point
 * @param  {Decimal} end   End point, not included
 * @param  {Decimal} step  Step size
 * @return {Array}         Array of numbers
 */
function rangeStep(start, end, step) {
  var num = new (decimal_default())(start);
  var i = 0;
  var result = [];

  // magic number to prevent infinite loop
  while (num.lt(end) && i < 100000) {
    result.push(num.toNumber());
    num = num.add(step);
    i++;
  }
  return result;
}

;// ./node_modules/recharts/es6/util/scale/getNiceTickValues.js
/**
 * @fileOverview calculate tick values of scale
 * @author xile611, arcthur
 * @date 2015-09-17
 */


/**
 * Calculate a interval of a minimum value and a maximum value
 *
 * @param  {Number} min       The minimum value
 * @param  {Number} max       The maximum value
 * @return {Array} An interval
 */
var getValidInterval = _ref => {
  var [min, max] = _ref;
  var [validMin, validMax] = [min, max];

  // exchange
  if (min > max) {
    [validMin, validMax] = [max, min];
  }
  return [validMin, validMax];
};

/**
 * Calculate the step which is easy to understand between ticks, like 10, 20, 25
 *
 * @param  roughStep        The rough step calculated by dividing the difference by the tickCount
 * @param  allowDecimals    Allow the ticks to be decimals or not
 * @param  correctionFactor A correction factor
 * @return The step which is easy to understand between two ticks
 */
var getFormatStep = (roughStep, allowDecimals, correctionFactor) => {
  if (roughStep.lte(0)) {
    return new (decimal_default())(0);
  }
  var digitCount = getDigitCount(roughStep.toNumber());
  // The ratio between the rough step and the smallest number which has a bigger
  // order of magnitudes than the rough step
  var digitCountValue = new (decimal_default())(10).pow(digitCount);
  var stepRatio = roughStep.div(digitCountValue);
  // When an integer and a float multiplied, the accuracy of result may be wrong
  var stepRatioScale = digitCount !== 1 ? 0.05 : 0.1;
  var amendStepRatio = new (decimal_default())(Math.ceil(stepRatio.div(stepRatioScale).toNumber())).add(correctionFactor).mul(stepRatioScale);
  var formatStep = amendStepRatio.mul(digitCountValue);
  return allowDecimals ? new (decimal_default())(formatStep.toNumber()) : new (decimal_default())(Math.ceil(formatStep.toNumber()));
};

/**
 * calculate the ticks when the minimum value equals to the maximum value
 *
 * @param  value         The minimum value which is also the maximum value
 * @param  tickCount     The count of ticks
 * @param  allowDecimals Allow the ticks to be decimals or not
 * @return array of ticks
 */
var getTickOfSingleValue = (value, tickCount, allowDecimals) => {
  var step = new (decimal_default())(1);
  // calculate the middle value of ticks
  var middle = new (decimal_default())(value);
  if (!middle.isint() && allowDecimals) {
    var absVal = Math.abs(value);
    if (absVal < 1) {
      // The step should be a float number when the difference is smaller than 1
      step = new (decimal_default())(10).pow(getDigitCount(value) - 1);
      middle = new (decimal_default())(Math.floor(middle.div(step).toNumber())).mul(step);
    } else if (absVal > 1) {
      // Return the maximum integer which is smaller than 'value' when 'value' is greater than 1
      middle = new (decimal_default())(Math.floor(value));
    }
  } else if (value === 0) {
    middle = new (decimal_default())(Math.floor((tickCount - 1) / 2));
  } else if (!allowDecimals) {
    middle = new (decimal_default())(Math.floor(value));
  }
  var middleIndex = Math.floor((tickCount - 1) / 2);
  var ticks = [];
  for (var i = 0; i < tickCount; i++) {
    ticks.push(middle.add(new (decimal_default())(i - middleIndex).mul(step)).toNumber());
  }
  return ticks;
};

/**
 * Calculate the step
 *
 * @param  min              The minimum value of an interval
 * @param  max              The maximum value of an interval
 * @param  tickCount        The count of ticks
 * @param  allowDecimals    Allow the ticks to be decimals or not
 * @param  correctionFactor A correction factor
 * @return The step, minimum value of ticks, maximum value of ticks
 */
var _calculateStep = function calculateStep(min, max, tickCount, allowDecimals) {
  var correctionFactor = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : 0;
  // dirty hack (for recharts' test)
  if (!Number.isFinite((max - min) / (tickCount - 1))) {
    return {
      step: new (decimal_default())(0),
      tickMin: new (decimal_default())(0),
      tickMax: new (decimal_default())(0)
    };
  }

  // The step which is easy to understand between two ticks
  var step = getFormatStep(new (decimal_default())(max).sub(min).div(tickCount - 1), allowDecimals, correctionFactor);

  // A medial value of ticks
  var middle;

  // When 0 is inside the interval, 0 should be a tick
  if (min <= 0 && max >= 0) {
    middle = new (decimal_default())(0);
  } else {
    // calculate the middle value
    middle = new (decimal_default())(min).add(max).div(2);
    // minus modulo value
    middle = middle.sub(new (decimal_default())(middle).mod(step));
  }
  var belowCount = Math.ceil(middle.sub(min).div(step).toNumber());
  var upCount = Math.ceil(new (decimal_default())(max).sub(middle).div(step).toNumber());
  var scaleCount = belowCount + upCount + 1;
  if (scaleCount > tickCount) {
    // When more ticks need to cover the interval, step should be bigger.
    return _calculateStep(min, max, tickCount, allowDecimals, correctionFactor + 1);
  }
  if (scaleCount < tickCount) {
    // When less ticks can cover the interval, we should add some additional ticks
    upCount = max > 0 ? upCount + (tickCount - scaleCount) : upCount;
    belowCount = max > 0 ? belowCount : belowCount + (tickCount - scaleCount);
  }
  return {
    step,
    tickMin: middle.sub(new (decimal_default())(belowCount).mul(step)),
    tickMax: middle.add(new (decimal_default())(upCount).mul(step))
  };
};

/**
 * Calculate the ticks of an interval. Ticks can appear outside the interval
 * if it makes them more rounded and nice.
 *
 * @param tuple of [min,max] min: The minimum value, max: The maximum value
 * @param tickCount     The count of ticks
 * @param allowDecimals Allow the ticks to be decimals or not
 * @return array of ticks
 */

var getNiceTickValues = function getNiceTickValues(_ref2) {
  var [min, max] = _ref2;
  var tickCount = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 6;
  var allowDecimals = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  // More than two ticks should be return
  var count = Math.max(tickCount, 2);
  var [cormin, cormax] = getValidInterval([min, max]);
  if (cormin === -Infinity || cormax === Infinity) {
    var _values = cormax === Infinity ? [cormin, ...Array(tickCount - 1).fill(Infinity)] : [...Array(tickCount - 1).fill(-Infinity), cormax];
    return min > max ? _values.reverse() : _values;
  }
  if (cormin === cormax) {
    return getTickOfSingleValue(cormin, tickCount, allowDecimals);
  }

  // Get the step between two ticks
  var {
    step,
    tickMin,
    tickMax
  } = _calculateStep(cormin, cormax, count, allowDecimals, 0);
  var values = rangeStep(tickMin, tickMax.add(new (decimal_default())(0.1).mul(step)), step);
  return min > max ? values.reverse() : values;
};

/**
 * Calculate the ticks of an interval.
 * Ticks will be constrained to the interval [min, max] even if it makes them less rounded and nice.
 *
 * @param tuple of [min,max] min: The minimum value, max: The maximum value
 * @param tickCount     The count of ticks. This function may return less than tickCount ticks if the interval is too small.
 * @param allowDecimals Allow the ticks to be decimals or not
 * @return array of ticks
 */
var getTickValuesFixedDomain = function getTickValuesFixedDomain(_ref3, tickCount) {
  var [min, max] = _ref3;
  var allowDecimals = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  // More than two ticks should be return
  var [cormin, cormax] = getValidInterval([min, max]);
  if (cormin === -Infinity || cormax === Infinity) {
    return [min, max];
  }
  if (cormin === cormax) {
    return [cormin];
  }
  var count = Math.max(tickCount, 2);
  var step = getFormatStep(new (decimal_default())(cormax).sub(cormin).div(count - 1), allowDecimals, 0);
  var values = [...rangeStep(new (decimal_default())(cormin), new (decimal_default())(cormax), step), cormax];
  if (allowDecimals === false) {
    /*
     * allowDecimals is false means that we want to have integer ticks.
     * The step is guaranteed to be an integer in the code above which is great start
     * but when the first step is not an integer, it will start stepping from a decimal value anyway.
     * So we need to round all the values to integers after the fact.
     */
    values = values.map(value => Math.round(value));
  }
  return min > max ? values.reverse() : values;
};
;// ./node_modules/recharts/es6/state/selectors/rootPropsSelectors.js
var selectRootMaxBarSize = state => state.rootProps.maxBarSize;
var selectBarGap = state => state.rootProps.barGap;
var selectBarCategoryGap = state => state.rootProps.barCategoryGap;
var selectRootBarSize = state => state.rootProps.barSize;
var selectStackOffsetType = state => state.rootProps.stackOffset;
var selectReverseStackOrder = state => state.rootProps.reverseStackOrder;
var selectChartName = state => state.options.chartName;
var selectSyncId = state => state.rootProps.syncId;
var selectSyncMethod = state => state.rootProps.syncMethod;
var selectEventEmitter = state => state.options.eventEmitter;
var selectChartBaseValue = state => state.rootProps.baseValue;
;// ./node_modules/recharts/es6/util/PolarUtils.js
function PolarUtils_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function PolarUtils_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? PolarUtils_ownKeys(Object(t), !0).forEach(function (r) { PolarUtils_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : PolarUtils_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function PolarUtils_defineProperty(e, r, t) { return (r = PolarUtils_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function PolarUtils_toPropertyKey(t) { var i = PolarUtils_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function PolarUtils_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var RADIAN = Math.PI / 180;
var degreeToRadian = angle => angle * Math.PI / 180;
var radianToDegree = angleInRadian => angleInRadian * 180 / Math.PI;
var polarToCartesian = (cx, cy, radius, angle) => ({
  x: cx + Math.cos(-RADIAN * angle) * radius,
  y: cy + Math.sin(-RADIAN * angle) * radius
});
var getMaxRadius = function getMaxRadius(width, height) {
  var offset = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    width: 0,
    height: 0,
    brushBottom: 0
  };
  return Math.min(Math.abs(width - (offset.left || 0) - (offset.right || 0)), Math.abs(height - (offset.top || 0) - (offset.bottom || 0))) / 2;
};
var distanceBetweenPoints = (point, anotherPoint) => {
  var {
    x: x1,
    y: y1
  } = point;
  var {
    x: x2,
    y: y2
  } = anotherPoint;
  return Math.sqrt((x1 - x2) ** 2 + (y1 - y2) ** 2);
};
var getAngleOfPoint = (_ref, _ref2) => {
  var {
    x,
    y
  } = _ref;
  var {
    cx,
    cy
  } = _ref2;
  var radius = distanceBetweenPoints({
    x,
    y
  }, {
    x: cx,
    y: cy
  });
  if (radius <= 0) {
    return {
      radius,
      angle: 0
    };
  }
  var cos = (x - cx) / radius;
  var angleInRadian = Math.acos(cos);
  if (y > cy) {
    angleInRadian = 2 * Math.PI - angleInRadian;
  }
  return {
    radius,
    angle: radianToDegree(angleInRadian),
    angleInRadian
  };
};
var formatAngleOfSector = _ref3 => {
  var {
    startAngle,
    endAngle
  } = _ref3;
  var startCnt = Math.floor(startAngle / 360);
  var endCnt = Math.floor(endAngle / 360);
  var min = Math.min(startCnt, endCnt);
  return {
    startAngle: startAngle - min * 360,
    endAngle: endAngle - min * 360
  };
};
var reverseFormatAngleOfSector = (angle, _ref4) => {
  var {
    startAngle,
    endAngle
  } = _ref4;
  var startCnt = Math.floor(startAngle / 360);
  var endCnt = Math.floor(endAngle / 360);
  var min = Math.min(startCnt, endCnt);
  return angle + min * 360;
};
var inRangeOfSector = (_ref5, viewBox) => {
  var {
    chartX: x,
    chartY: y
  } = _ref5;
  var {
    radius,
    angle
  } = getAngleOfPoint({
    x,
    y
  }, viewBox);
  var {
    innerRadius,
    outerRadius
  } = viewBox;
  if (radius < innerRadius || radius > outerRadius) {
    return null;
  }
  if (radius === 0) {
    return null;
  }
  var {
    startAngle,
    endAngle
  } = formatAngleOfSector(viewBox);
  var formatAngle = angle;
  var inRange;
  if (startAngle <= endAngle) {
    while (formatAngle > endAngle) {
      formatAngle -= 360;
    }
    while (formatAngle < startAngle) {
      formatAngle += 360;
    }
    inRange = formatAngle >= startAngle && formatAngle <= endAngle;
  } else {
    while (formatAngle > startAngle) {
      formatAngle -= 360;
    }
    while (formatAngle < endAngle) {
      formatAngle += 360;
    }
    inRange = formatAngle >= endAngle && formatAngle <= startAngle;
  }
  if (inRange) {
    return PolarUtils_objectSpread(PolarUtils_objectSpread({}, viewBox), {}, {
      radius,
      angle: reverseFormatAngleOfSector(formatAngle, viewBox)
    });
  }
  return null;
};
;// ./node_modules/recharts/es6/zIndex/DefaultZIndexes.js
/**
 * A collection of all default zIndex values used by Recharts.
 *
 * You can reuse these, or you can define your own.
 */
var DefaultZIndexes = {
  /**
   * CartesianGrid and PolarGrid
   */
  grid: -100,
  /**
   * Background of Bar and RadialBar.
   * This is not visible by default but can be enabled by setting background={true} on Bar or RadialBar.
   */
  barBackground: -50,
  /*
   * other chart elements or custom elements without specific zIndex
   * render in here, at zIndex 0
   */

  /**
   * Area, Pie, Radar, and ReferenceArea
   */
  area: 100,
  /**
   * Cursor is embedded inside Tooltip and controlled by it.
   * The Tooltip itself has a separate portal and is not included in the zIndex system;
   * Cursor is the decoration inside the chart area. CursorRectangle is a rectangle box.
   * It renders below bar so that in a stacked bar chart the cursor rectangle does not hide the other bars.
   */
  cursorRectangle: 200,
  /**
   * Bar and RadialBar
   */
  bar: 300,
  /**
   * Line and ReferenceLine, and ErrorBor
   */
  line: 400,
  /**
   * XAxis and YAxis and PolarAngleAxis and PolarRadiusAxis ticks and lines and children
   */
  axis: 500,
  /**
   * Scatter and ReferenceDot,
   * and Dots of Line and Area and Radar if they have dot=true
   */
  scatter: 600,
  /**
   * Hovering over a Bar or RadialBar renders a highlight rectangle
   */
  activeBar: 1000,
  /**
   * Cursor is embedded inside Tooltip and controlled by it.
   * The Tooltip itself has a separate portal and is not included in the zIndex system;
   * Cursor is the decoration inside the chart area, usually a cross or a box.
   * CursorLine is a line cursor rendered in Line, Area, Scatter, Radar charts.
   * It renders above the Line and Scatter so that it is always visible.
   * It renders below active dot so that the dot is always visible and shows the current point.
   * We're also assuming that the active dot is small enough that it does not fully cover the cursor line.
   *
   * This also applies to the radial cursor in RadialBarChart.
   */
  cursorLine: 1100,
  /**
   * Hovering over a Point in Line, Area, Scatter, Radar renders a highlight dot
   */
  activeDot: 1200,
  /**
   * LabelList and Label, including Axis labels
   */
  label: 2000
};
;// ./node_modules/recharts/es6/polar/defaultPolarAngleAxisProps.js

var defaultPolarAngleAxisProps = {
  allowDecimals: false,
  allowDuplicatedCategory: true,
  // if I set this to false then Tooltip synchronisation stops working in Radar, wtf
  allowDataOverflow: false,
  angle: 0,
  angleAxisId: 0,
  axisLine: true,
  axisLineType: 'polygon',
  cx: 0,
  cy: 0,
  hide: false,
  includeHidden: false,
  label: false,
  orientation: 'outer',
  reversed: false,
  scale: 'auto',
  tick: true,
  tickLine: true,
  tickSize: 8,
  type: 'auto',
  zIndex: DefaultZIndexes.axis
};
;// ./node_modules/recharts/es6/polar/defaultPolarRadiusAxisProps.js

var defaultPolarRadiusAxisProps = {
  allowDataOverflow: false,
  allowDecimals: false,
  allowDuplicatedCategory: true,
  angle: 0,
  axisLine: true,
  includeHidden: false,
  hide: false,
  label: false,
  orientation: 'right',
  radiusAxisId: 0,
  reversed: false,
  scale: 'auto',
  stroke: '#ccc',
  tick: true,
  tickCount: 5,
  tickLine: true,
  type: 'auto',
  zIndex: DefaultZIndexes.axis
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineAxisRangeWithReverse.js
var combineAxisRangeWithReverse = (axisSettings, axisRange) => {
  if (!axisSettings || !axisRange) {
    return undefined;
  }
  if (axisSettings !== null && axisSettings !== void 0 && axisSettings.reversed) {
    return [axisRange[1], axisRange[0]];
  }
  return axisRange;
};
;// ./node_modules/recharts/es6/util/getAxisTypeBasedOnLayout.js


/**
 * This function evaluates the "auto" axis domain type based on the chart layout and axis type.
 * It outputs a definitive axis domain type that can be used for further processing.
 */
function getAxisTypeBasedOnLayout(layout, axisType, axisDomainType) {
  if (axisDomainType !== 'auto') {
    return axisDomainType;
  }
  if (layout == null) {
    return undefined;
  }
  return isCategoricalAxis(layout, axisType) ? 'category' : 'number';
}
;// ./node_modules/recharts/es6/state/selectors/polarAxisSelectors.js
function polarAxisSelectors_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function polarAxisSelectors_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? polarAxisSelectors_ownKeys(Object(t), !0).forEach(function (r) { polarAxisSelectors_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : polarAxisSelectors_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function polarAxisSelectors_defineProperty(e, r, t) { return (r = polarAxisSelectors_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function polarAxisSelectors_toPropertyKey(t) { var i = polarAxisSelectors_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function polarAxisSelectors_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }










var implicitAngleAxis = {
  allowDataOverflow: defaultPolarAngleAxisProps.allowDataOverflow,
  allowDecimals: defaultPolarAngleAxisProps.allowDecimals,
  allowDuplicatedCategory: false,
  // defaultPolarAngleAxisProps.allowDuplicatedCategory has it set to true but the actual axis rendering ignores the prop because reasons,
  dataKey: undefined,
  domain: undefined,
  id: defaultPolarAngleAxisProps.angleAxisId,
  includeHidden: false,
  name: undefined,
  reversed: defaultPolarAngleAxisProps.reversed,
  scale: defaultPolarAngleAxisProps.scale,
  tick: defaultPolarAngleAxisProps.tick,
  tickCount: undefined,
  ticks: undefined,
  type: defaultPolarAngleAxisProps.type,
  unit: undefined
};
var implicitRadiusAxis = {
  allowDataOverflow: defaultPolarRadiusAxisProps.allowDataOverflow,
  allowDecimals: defaultPolarRadiusAxisProps.allowDecimals,
  allowDuplicatedCategory: defaultPolarRadiusAxisProps.allowDuplicatedCategory,
  dataKey: undefined,
  domain: undefined,
  id: defaultPolarRadiusAxisProps.radiusAxisId,
  includeHidden: defaultPolarRadiusAxisProps.includeHidden,
  name: undefined,
  reversed: defaultPolarRadiusAxisProps.reversed,
  scale: defaultPolarRadiusAxisProps.scale,
  tick: defaultPolarRadiusAxisProps.tick,
  tickCount: defaultPolarRadiusAxisProps.tickCount,
  ticks: undefined,
  type: defaultPolarRadiusAxisProps.type,
  unit: undefined
};
var selectAngleAxisNoDefaults = (state, angleAxisId) => {
  if (angleAxisId == null) {
    return undefined;
  }
  return state.polarAxis.angleAxis[angleAxisId];
};
var selectAngleAxis = createSelector([selectAngleAxisNoDefaults, selectPolarChartLayout], (angleAxisSettings, layout) => {
  var _getAxisTypeBasedOnLa;
  if (angleAxisSettings != null) {
    return angleAxisSettings;
  }
  var evaluatedType = (_getAxisTypeBasedOnLa = getAxisTypeBasedOnLayout(layout, 'angleAxis', implicitAngleAxis.type)) !== null && _getAxisTypeBasedOnLa !== void 0 ? _getAxisTypeBasedOnLa : 'category';
  return polarAxisSelectors_objectSpread(polarAxisSelectors_objectSpread({}, implicitAngleAxis), {}, {
    type: evaluatedType
  });
});
var selectRadiusAxisNoDefaults = (state, radiusAxisId) => {
  return state.polarAxis.radiusAxis[radiusAxisId];
};
var selectRadiusAxis = createSelector([selectRadiusAxisNoDefaults, selectPolarChartLayout], (radiusAxisSettings, layout) => {
  var _getAxisTypeBasedOnLa2;
  if (radiusAxisSettings != null) {
    return radiusAxisSettings;
  }
  var evaluatedType = (_getAxisTypeBasedOnLa2 = getAxisTypeBasedOnLayout(layout, 'radiusAxis', implicitRadiusAxis.type)) !== null && _getAxisTypeBasedOnLa2 !== void 0 ? _getAxisTypeBasedOnLa2 : 'category';
  return polarAxisSelectors_objectSpread(polarAxisSelectors_objectSpread({}, implicitRadiusAxis), {}, {
    type: evaluatedType
  });
});
var selectPolarOptions = state => state.polarOptions;
var selectMaxRadius = createSelector([selectChartWidth, selectChartHeight, selectChartOffsetInternal], getMaxRadius);
var selectInnerRadius = createSelector([selectPolarOptions, selectMaxRadius], (polarChartOptions, maxRadius) => {
  if (polarChartOptions == null) {
    return undefined;
  }
  return getPercentValue(polarChartOptions.innerRadius, maxRadius, 0);
});
var selectOuterRadius = createSelector([selectPolarOptions, selectMaxRadius], (polarChartOptions, maxRadius) => {
  if (polarChartOptions == null) {
    return undefined;
  }
  return getPercentValue(polarChartOptions.outerRadius, maxRadius, maxRadius * 0.8);
});
var combineAngleAxisRange = polarOptions => {
  if (polarOptions == null) {
    return [0, 0];
  }
  var {
    startAngle,
    endAngle
  } = polarOptions;
  return [startAngle, endAngle];
};
var selectAngleAxisRange = createSelector([selectPolarOptions], combineAngleAxisRange);
var selectAngleAxisRangeWithReversed = createSelector([selectAngleAxis, selectAngleAxisRange], combineAxisRangeWithReverse);
var selectRadiusAxisRange = createSelector([selectMaxRadius, selectInnerRadius, selectOuterRadius], (maxRadius, innerRadius, outerRadius) => {
  if (maxRadius == null || innerRadius == null || outerRadius == null) {
    return undefined;
  }
  return [innerRadius, outerRadius];
});
var selectRadiusAxisRangeWithReversed = createSelector([selectRadiusAxis, selectRadiusAxisRange], combineAxisRangeWithReverse);
var selectPolarViewBox = createSelector([selectChartLayout, selectPolarOptions, selectInnerRadius, selectOuterRadius, selectChartWidth, selectChartHeight], (layout, polarOptions, innerRadius, outerRadius, width, height) => {
  if (layout !== 'centric' && layout !== 'radial' || polarOptions == null || innerRadius == null || outerRadius == null) {
    return undefined;
  }
  var {
    cx,
    cy,
    startAngle,
    endAngle
  } = polarOptions;
  return {
    cx: getPercentValue(cx, width, width / 2),
    cy: getPercentValue(cy, height, height / 2),
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    clockWise: false // this property look useful, why not use it?
  };
});
;// ./node_modules/recharts/es6/state/selectors/pickAxisType.js
var pickAxisType = (_state, axisType) => axisType;
;// ./node_modules/recharts/es6/state/selectors/pickAxisId.js
var pickAxisId = (_state, _axisType, axisId) => axisId;
;// ./node_modules/recharts/es6/util/stacks/getStackSeriesIdentifier.js
/**
 * Returns identifier for stack series which is one individual graphical item in the stack.
 * @param graphicalItem - The graphical item representing the series in the stack.
 * @return The identifier for the series in the stack
 */
function getStackSeriesIdentifier(graphicalItem) {
  return graphicalItem === null || graphicalItem === void 0 ? void 0 : graphicalItem.id;
}
;// ./node_modules/recharts/es6/state/selectors/combiners/combineDisplayedStackedData.js



/**
 * In a stacked chart, each graphical item has its own data. That data could be either:
 * - defined on the chart root, in which case the item gets a unique dataKey
 * - or defined on the item itself, in which case multiple items can share the same dataKey
 *
 * That means we cannot use the dataKey as a unique identifier for the item.
 *
 * This type represents a single data point in a stacked chart, where each key is a series identifier
 * and the value is the numeric value for that series using the numerical axis dataKey.
 */

function combineDisplayedStackedData(stackedGraphicalItems, _ref, tooltipAxisSettings) {
  var {
    chartData = []
  } = _ref;
  var {
    allowDuplicatedCategory,
    dataKey: tooltipDataKey
  } = tooltipAxisSettings;

  // A map of tooltip data keys to the stacked data points
  var knownItemsByDataKey = new Map();
  stackedGraphicalItems.forEach(item => {
    var _item$data;
    // If there is no data on the individual item then we use the root chart data
    var resolvedData = (_item$data = item.data) !== null && _item$data !== void 0 ? _item$data : chartData;
    if (resolvedData == null || resolvedData.length === 0) {
      // if that doesn't work then we skip this item
      return;
    }
    var stackIdentifier = getStackSeriesIdentifier(item);
    resolvedData.forEach((entry, index) => {
      var tooltipValue = tooltipDataKey == null || allowDuplicatedCategory ? index : String(getValueByDataKey(entry, tooltipDataKey, null));
      var numericValue = getValueByDataKey(entry, item.dataKey, 0);
      var curr;
      if (knownItemsByDataKey.has(tooltipValue)) {
        curr = knownItemsByDataKey.get(tooltipValue);
      } else {
        curr = {};
      }
      Object.assign(curr, {
        [stackIdentifier]: numericValue
      });
      knownItemsByDataKey.set(tooltipValue, curr);
    });
  });
  return Array.from(knownItemsByDataKey.values());
}
;// ./node_modules/recharts/es6/state/types/StackedGraphicalItem.js
/**
 * Some graphical items allow data stacking. The stacks are optional,
 * so all props here are optional too.
 */

/**
 * Some graphical items allow data stacking.
 * This interface is used to represent the items that are stacked
 * because the user has provided the stackId and dataKey properties.
 */

function isStacked(graphicalItem) {
  return 'stackId' in graphicalItem && graphicalItem.stackId != null && graphicalItem.dataKey != null;
}
;// ./node_modules/recharts/es6/state/selectors/numberDomainEqualityCheck.js
var numberDomainEqualityCheck = (a, b) => {
  if (a === b) {
    return true;
  }
  if (a == null || b == null) {
    return false;
  }
  return a[0] === b[0] && a[1] === b[1];
};
;// ./node_modules/recharts/es6/state/selectors/arrayEqualityCheck.js
/**
 * Checks if two arrays are equal, treating empty arrays as equal regardless of reference.
 * If both arrays are non-empty, it checks for reference equality.
 * @param a
 * @param b
 */
function emptyArraysAreEqualCheck(a, b) {
  if (Array.isArray(a) && Array.isArray(b) && a.length === 0 && b.length === 0) {
    // empty arrays are always equal, regardless of reference
    return true;
  }
  return a === b;
}

/**
 * Checks if two arrays have the same contents in the same order.
 * @param a
 * @param b
 */
function arrayContentsAreEqualCheck(a, b) {
  if (a.length === b.length) {
    for (var i = 0; i < a.length; i++) {
      if (a[i] !== b[i]) {
        return false;
      }
    }
    return true;
  }
  return false;
}
;// ./node_modules/recharts/es6/state/selectors/selectTooltipAxisType.js


/**
 * angle, radius, X, Y, and Z axes all have domain and range and scale and associated settings
 */

/**
 * Z axis is never displayed and so it lacks ticks and tick settings.
 */

var selectTooltipAxisType = state => {
  var layout = selectChartLayout(state);
  if (layout === 'horizontal') {
    return 'xAxis';
  }
  if (layout === 'vertical') {
    return 'yAxis';
  }
  if (layout === 'centric') {
    return 'angleAxis';
  }
  return 'radiusAxis';
};
;// ./node_modules/recharts/es6/state/selectors/selectTooltipAxisId.js
var selectTooltipAxisId = state => state.tooltip.settings.axisId;
;// ./node_modules/recharts/es6/util/scale/RechartsScale.js



/**
 * This is internal representation of scale used in Recharts.
 * Users will provide CustomScaleDefinition or a string, which we will parse into RechartsScale.
 * Most importantly, RechartsScale is fully immutable - there are no setters that mutate the scale in place.
 * This is important for React integration - if the scale changes, we want to trigger re-renders.
 * Mutating the scale in place would not trigger re-renders, leading to stale UI.
 */

/**
 * Position within a band for banded scales.
 * In scales that are not banded, this parameter is ignored.
 *
 * @inline
 */

function getD3ScaleFromType(realScaleType) {
  if (realScaleType in d3_scale_namespaceObject) {
    // @ts-expect-error we should do better type verification here
    return d3_scale_namespaceObject[realScaleType]();
  }
  var name = "scale".concat(upperFirst(realScaleType));
  if (name in d3_scale_namespaceObject) {
    // @ts-expect-error we should do better type verification here
    return d3_scale_namespaceObject[name]();
  }
  return undefined;
}
function d3ScaleToRechartsScale(d3Scale) {
  var ticksFn = d3Scale.ticks;
  var bandwidthFn = d3Scale.bandwidth;
  var d3Range = d3Scale.range();
  var range = [Math.min(...d3Range), Math.max(...d3Range)];
  return {
    domain: () => d3Scale.domain(),
    range: function (_range) {
      function range() {
        return _range.apply(this, arguments);
      }
      range.toString = function () {
        return _range.toString();
      };
      return range;
    }(() => range),
    rangeMin: () => range[0],
    rangeMax: () => range[1],
    isInRange(value) {
      var first = range[0];
      var last = range[1];
      return first <= last ? value >= first && value <= last : value >= last && value <= first;
    },
    bandwidth: bandwidthFn ? () => bandwidthFn.call(d3Scale) : undefined,
    ticks: ticksFn ? count => ticksFn.call(d3Scale, count) : undefined,
    map: (input, options) => {
      var baseValue = d3Scale(input);
      if (baseValue == null) {
        return undefined;
      }
      if (d3Scale.bandwidth && options !== null && options !== void 0 && options.position) {
        var bandWidth = d3Scale.bandwidth();
        switch (options.position) {
          case 'middle':
            baseValue += bandWidth / 2;
            break;
          case 'end':
            baseValue += bandWidth;
            break;
          default:
            // 'start' requires no adjustment
            break;
        }
      }
      return baseValue;
    }
  };
}

/**
 * Converts external scale definition into internal RechartsScale definition.
 * @param scale custom function scale - if you have the string, use `combineRealScaleType` first
 * @param axisDomain
 * @param axisRange
 */

function rechartsScaleFactory(scale, axisDomain, axisRange) {
  if (typeof scale === 'function') {
    return d3ScaleToRechartsScale(scale.copy().domain(axisDomain).range(axisRange));
  }
  if (scale == null) {
    return undefined;
  }
  var d3ScaleFunction = getD3ScaleFromType(scale);
  if (d3ScaleFunction == null) {
    return undefined;
  }
  d3ScaleFunction.domain(axisDomain).range(axisRange);
  return d3ScaleToRechartsScale(d3ScaleFunction);
}
;// ./node_modules/recharts/es6/state/selectors/combiners/combineCheckedDomain.js



/**
 * This function validates and transforms the axis domain so that it is safe to use in the provided scale.
 */
var combineCheckedDomain = (realScaleType, axisDomain) => {
  if (axisDomain == null) {
    return undefined;
  }
  switch (realScaleType) {
    case 'linear':
      {
        /*
         * linear scale only reads the first two numbers in the domain, and ignores everything else.
         * So if it happens that someone somehow gave us a bigger domain,
         * let's pick the min and max from it.
         */
        if (!isWellFormedNumberDomain(axisDomain)) {
          var min, max;
          for (var i = 0; i < axisDomain.length; i++) {
            var value = axisDomain[i];
            if (!isWellBehavedNumber(value)) {
              continue;
            }
            if (min === undefined || value < min) {
              min = value;
            }
            if (max === undefined || value > max) {
              max = value;
            }
          }
          if (min !== undefined && max !== undefined) {
            return [min, max];
          }
          return undefined;
        }
        return axisDomain;
      }
    default:
      return axisDomain;
  }
};
;// ./node_modules/recharts/es6/state/selectors/axisSelectors.js
function axisSelectors_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function axisSelectors_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? axisSelectors_ownKeys(Object(t), !0).forEach(function (r) { axisSelectors_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : axisSelectors_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function axisSelectors_defineProperty(e, r, t) { return (r = axisSelectors_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function axisSelectors_toPropertyKey(t) { var i = axisSelectors_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function axisSelectors_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }





























var defaultNumericDomain = [0, 'auto'];
/**
 * If an axis is not explicitly defined as an element,
 * we still need to render something in the chart and we need
 * some object to hold the domain and default settings.
 */
var implicitXAxis = {
  allowDataOverflow: false,
  allowDecimals: true,
  allowDuplicatedCategory: true,
  angle: 0,
  dataKey: undefined,
  domain: undefined,
  height: 30,
  hide: true,
  id: 0,
  includeHidden: false,
  interval: 'preserveEnd',
  minTickGap: 5,
  mirror: false,
  name: undefined,
  orientation: 'bottom',
  padding: {
    left: 0,
    right: 0
  },
  reversed: false,
  scale: 'auto',
  tick: true,
  tickCount: 5,
  tickFormatter: undefined,
  ticks: undefined,
  type: 'category',
  unit: undefined
};
var selectXAxisSettingsNoDefaults = (state, axisId) => {
  return state.cartesianAxis.xAxis[axisId];
};
var selectXAxisSettings = (state, axisId) => {
  var axis = selectXAxisSettingsNoDefaults(state, axisId);
  if (axis == null) {
    return implicitXAxis;
  }
  return axis;
};

/**
 * If an axis is not explicitly defined as an element,
 * we still need to render something in the chart and we need
 * some object to hold the domain and default settings.
 */
var implicitYAxis = {
  allowDataOverflow: false,
  allowDecimals: true,
  allowDuplicatedCategory: true,
  angle: 0,
  dataKey: undefined,
  domain: defaultNumericDomain,
  hide: true,
  id: 0,
  includeHidden: false,
  interval: 'preserveEnd',
  minTickGap: 5,
  mirror: false,
  name: undefined,
  orientation: 'left',
  padding: {
    top: 0,
    bottom: 0
  },
  reversed: false,
  scale: 'auto',
  tick: true,
  tickCount: 5,
  tickFormatter: undefined,
  ticks: undefined,
  type: 'number',
  unit: undefined,
  width: DEFAULT_Y_AXIS_WIDTH
};
var selectYAxisSettingsNoDefaults = (state, axisId) => {
  return state.cartesianAxis.yAxis[axisId];
};
var selectYAxisSettings = (state, axisId) => {
  var axis = selectYAxisSettingsNoDefaults(state, axisId);
  if (axis == null) {
    return implicitYAxis;
  }
  return axis;
};
var implicitZAxis = {
  domain: [0, 'auto'],
  includeHidden: false,
  reversed: false,
  allowDataOverflow: false,
  allowDuplicatedCategory: false,
  dataKey: undefined,
  id: 0,
  name: '',
  range: [64, 64],
  scale: 'auto',
  type: 'number',
  unit: ''
};
var selectZAxisSettings = (state, axisId) => {
  var axis = state.cartesianAxis.zAxis[axisId];
  if (axis == null) {
    return implicitZAxis;
  }
  return axis;
};
var selectBaseAxis = (state, axisType, axisId) => {
  switch (axisType) {
    case 'xAxis':
      {
        return selectXAxisSettings(state, axisId);
      }
    case 'yAxis':
      {
        return selectYAxisSettings(state, axisId);
      }
    case 'zAxis':
      {
        return selectZAxisSettings(state, axisId);
      }
    case 'angleAxis':
      {
        return selectAngleAxis(state, axisId);
      }
    case 'radiusAxis':
      {
        return selectRadiusAxis(state, axisId);
      }
    default:
      throw new Error("Unexpected axis type: ".concat(axisType));
  }
};
var selectCartesianAxisSettings = (state, axisType, axisId) => {
  switch (axisType) {
    case 'xAxis':
      {
        return selectXAxisSettings(state, axisId);
      }
    case 'yAxis':
      {
        return selectYAxisSettings(state, axisId);
      }
    default:
      throw new Error("Unexpected axis type: ".concat(axisType));
  }
};

/**
 * Selects either an X or Y axis. Doesn't work with Z axis - for that, instead use selectBaseAxis.
 * @param state Root state
 * @param axisType xAxis | yAxis
 * @param axisId xAxisId | yAxisId
 * @returns axis settings object
 */
var selectRenderableAxisSettings = (state, axisType, axisId) => {
  switch (axisType) {
    case 'xAxis':
      {
        return selectXAxisSettings(state, axisId);
      }
    case 'yAxis':
      {
        return selectYAxisSettings(state, axisId);
      }
    case 'angleAxis':
      {
        return selectAngleAxis(state, axisId);
      }
    case 'radiusAxis':
      {
        return selectRadiusAxis(state, axisId);
      }
    default:
      throw new Error("Unexpected axis type: ".concat(axisType));
  }
};

/**
 * @param state RechartsRootState
 * @return boolean true if there is at least one Bar or RadialBar
 */
var selectHasBar = state => state.graphicalItems.cartesianItems.some(item => item.type === 'bar') || state.graphicalItems.polarItems.some(item => item.type === 'radialBar');

/**
 * Filters CartesianGraphicalItemSettings by the relevant axis ID
 * @param axisType 'xAxis' | 'yAxis' | 'zAxis' | 'radiusAxis' | 'angleAxis'
 * @param axisId from props, defaults to 0
 *
 * @returns Predicate function that return true for CartesianGraphicalItemSettings that are relevant to the specified axis
 */
function itemAxisPredicate(axisType, axisId) {
  return item => {
    switch (axisType) {
      case 'xAxis':
        // This is sensitive to the data type, as 0 !== '0'. I wonder if we should be more flexible. How does 2.x branch behave? TODO write test for that
        return 'xAxisId' in item && item.xAxisId === axisId;
      case 'yAxis':
        return 'yAxisId' in item && item.yAxisId === axisId;
      case 'zAxis':
        return 'zAxisId' in item && item.zAxisId === axisId;
      case 'angleAxis':
        return 'angleAxisId' in item && item.angleAxisId === axisId;
      case 'radiusAxis':
        return 'radiusAxisId' in item && item.radiusAxisId === axisId;
      default:
        return false;
    }
  };
}

// TODO appears there is a bug where this selector is called from polar context, find and fix it.
var selectUnfilteredCartesianItems = state => state.graphicalItems.cartesianItems;
var selectAxisPredicate = createSelector([pickAxisType, pickAxisId], itemAxisPredicate);
var combineGraphicalItemsSettings = (graphicalItems, axisSettings, axisPredicate) => graphicalItems.filter(axisPredicate).filter(item => {
  if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.includeHidden) === true) {
    return true;
  }
  return !item.hide;
});
var selectCartesianItemsSettings = createSelector([selectUnfilteredCartesianItems, selectBaseAxis, selectAxisPredicate], combineGraphicalItemsSettings, {
  memoizeOptions: {
    resultEqualityCheck: emptyArraysAreEqualCheck
  }
});
var selectStackedCartesianItemsSettings = createSelector([selectCartesianItemsSettings], cartesianItems => {
  return cartesianItems.filter(item => item.type === 'area' || item.type === 'bar').filter(isStacked);
});
var filterGraphicalNotStackedItems = cartesianItems => cartesianItems.filter(item => !('stackId' in item) || item.stackId === undefined);
var selectCartesianItemsSettingsExceptStacked = createSelector([selectCartesianItemsSettings], filterGraphicalNotStackedItems);
var combineGraphicalItemsData = cartesianItems => cartesianItems.map(item => item.data).filter(Boolean).flat(1);

/**
 * This is a "cheap" selector - it returns the data but doesn't iterate them, so it is not sensitive on the array length.
 * Also does not apply dataKey yet.
 * @param state RechartsRootState
 * @returns data defined on the chart graphical items, such as Line or Scatter or Pie, and filtered with appropriate dataKey
 */
var selectCartesianGraphicalItemsData = createSelector([selectCartesianItemsSettings], combineGraphicalItemsData, {
  memoizeOptions: {
    resultEqualityCheck: emptyArraysAreEqualCheck
  }
});
var combineDisplayedData = (graphicalItemsData, _ref) => {
  var {
    chartData = [],
    dataStartIndex,
    dataEndIndex
  } = _ref;
  if (graphicalItemsData.length > 0) {
    /*
     * There is no slicing when data is defined on graphical items. Why?
     * Because Brush ignores data defined on graphical items,
     * and does not render.
     * So Brush will never show up in a Scatter chart for example.
     * This is something we will need to fix.
     *
     * Now, when the root chart data is not defined, the dataEndIndex is 0,
     * which means the itemsData will be sliced to an empty array anyway.
     * But that's an implementation detail, and we can fix that too.
     *
     * Also, in absence of Axis dataKey, we use the dataKey from each item, respectively.
     * This is the usual pattern for numerical axis, that is the one where bars go up:
     * users don't specify any dataKey by default and expect the axis to "just match the data".
     */
    return graphicalItemsData;
  }
  return chartData.slice(dataStartIndex, dataEndIndex + 1);
};

/**
 * This selector will return all data there is in the chart: graphical items, chart root, all together.
 * Useful for figuring out an axis domain (because that needs to know of everything),
 * not useful for rendering individual graphical elements (because they need to know which data is theirs and which is not).
 *
 * This function will discard the original indexes, so it is also not useful for anything that depends on ordering.
 */
var selectDisplayedData = createSelector([selectCartesianGraphicalItemsData, selectChartDataWithIndexesIfNotInPanoramaPosition4], combineDisplayedData);
var combineAppliedValues = (data, axisSettings, items) => {
  if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null) {
    return data.map(item => ({
      value: getValueByDataKey(item, axisSettings.dataKey)
    }));
  }
  if (items.length > 0) {
    return items.map(item => item.dataKey).flatMap(dataKey => data.map(entry => ({
      value: getValueByDataKey(entry, dataKey)
    })));
  }
  return data.map(entry => ({
    value: entry
  }));
};

/**
 * This selector will return all values with the appropriate dataKey applied on them.
 * Which dataKey is appropriate depends on where it is defined.
 *
 * This is an expensive selector - it will iterate all data and compute their value using the provided dataKey.
 */
var selectAllAppliedValues = createSelector([selectDisplayedData, selectBaseAxis, selectCartesianItemsSettings], combineAppliedValues);
function isErrorBarRelevantForAxisType(axisType, errorBar) {
  switch (axisType) {
    case 'xAxis':
      return errorBar.direction === 'x';
    case 'yAxis':
      return errorBar.direction === 'y';
    default:
      return false;
  }
}
function makeNumber(val) {
  if (isNumOrStr(val) || val instanceof Date) {
    var n = Number(val);
    if (isWellBehavedNumber(n)) {
      return n;
    }
  }
  return undefined;
}
function makeDomain(val) {
  if (Array.isArray(val)) {
    var attempt = [makeNumber(val[0]), makeNumber(val[1])];
    if (isWellFormedNumberDomain(attempt)) {
      return attempt;
    }
    return undefined;
  }
  var n = makeNumber(val);
  if (n == null) {
    return undefined;
  }
  return [n, n];
}
function onlyAllowNumbers(data) {
  return data.map(makeNumber).filter(isNotNil);
}

/**
 * @param entry One item in the 'data' array. Could be anything really - this is defined externally. This is the raw, before dataKey application
 * @param appliedValue This is the result of applying the 'main' dataKey on the `entry`.
 * @param relevantErrorBars Error bars that are relevant for the current axis and layout and all that.
 * @return either undefined or an array of ErrorValue
 */
function getErrorDomainByDataKey(entry, appliedValue, relevantErrorBars) {
  if (!relevantErrorBars || typeof appliedValue !== 'number' || isNan(appliedValue)) {
    return [];
  }
  if (!relevantErrorBars.length) {
    return [];
  }
  return onlyAllowNumbers(relevantErrorBars.flatMap(eb => {
    var errorValue = getValueByDataKey(entry, eb.dataKey);
    var lowBound, highBound;
    if (Array.isArray(errorValue)) {
      [lowBound, highBound] = errorValue;
    } else {
      lowBound = highBound = errorValue;
    }
    if (!isWellBehavedNumber(lowBound) || !isWellBehavedNumber(highBound)) {
      return undefined;
    }
    return [appliedValue - lowBound, appliedValue + highBound];
  }));
}
var selectTooltipAxis = state => {
  var axisType = selectTooltipAxisType(state);
  var axisId = selectTooltipAxisId(state);
  return selectRenderableAxisSettings(state, axisType, axisId);
};
var selectTooltipAxisDataKey = createSelector([selectTooltipAxis], axis => axis === null || axis === void 0 ? void 0 : axis.dataKey);
var selectDisplayedStackedData = createSelector([selectStackedCartesianItemsSettings, selectChartDataWithIndexesIfNotInPanoramaPosition4, selectTooltipAxis], combineDisplayedStackedData);
var combineStackGroups = (displayedData, items, stackOffsetType, reverseStackOrder) => {
  var initialItemsGroups = {};
  var itemsGroup = items.reduce((acc, item) => {
    if (item.stackId == null) {
      return acc;
    }
    var stack = acc[item.stackId];
    if (stack == null) {
      stack = [];
    }
    stack.push(item);
    acc[item.stackId] = stack;
    return acc;
  }, initialItemsGroups);
  return Object.fromEntries(Object.entries(itemsGroup).map(_ref2 => {
    var [stackId, graphicalItems] = _ref2;
    var orderedGraphicalItems = reverseStackOrder ? [...graphicalItems].reverse() : graphicalItems;
    var dataKeys = orderedGraphicalItems.map(getStackSeriesIdentifier);
    return [stackId, {
      // @ts-expect-error getStackedData requires that the input is array of objects, Recharts does not test for that
      stackedData: getStackedData(displayedData, dataKeys, stackOffsetType),
      graphicalItems: orderedGraphicalItems
    }];
  }));
};

/**
 * Stack groups are groups of graphical items that stack on each other.
 * Stack is a function of axis type (X, Y), axis ID, and stack ID.
 * Graphical items that do not have a stack ID are not going to be present in stack groups.
 */
var selectStackGroups = createSelector([selectDisplayedStackedData, selectStackedCartesianItemsSettings, selectStackOffsetType, selectReverseStackOrder], combineStackGroups);
var combineDomainOfStackGroups = (stackGroups, _ref3, axisType, domainFromUserPreference) => {
  var {
    dataStartIndex,
    dataEndIndex
  } = _ref3;
  if (domainFromUserPreference != null) {
    // User has specified a domain, so we respect that and we can skip computing anything else
    return undefined;
  }
  if (axisType === 'zAxis') {
    // ZAxis ignores stacks
    return undefined;
  }
  var domainOfStackGroups = getDomainOfStackGroups(stackGroups, dataStartIndex, dataEndIndex);
  if (domainOfStackGroups != null && domainOfStackGroups[0] === 0 && domainOfStackGroups[1] === 0) {
    return undefined;
  }
  return domainOfStackGroups;
};
var selectAllowsDataOverflow = createSelector([selectBaseAxis], axisSettings => axisSettings.allowDataOverflow);
var getDomainDefinition = axisSettings => {
  var _axisSettings$domain;
  if (axisSettings == null || !('domain' in axisSettings)) {
    return defaultNumericDomain;
  }
  if (axisSettings.domain != null) {
    return axisSettings.domain;
  }
  if ('ticks' in axisSettings && axisSettings.ticks != null) {
    if (axisSettings.type === 'number') {
      var allValues = onlyAllowNumbers(axisSettings.ticks);
      return [Math.min(...allValues), Math.max(...allValues)];
    }
    if (axisSettings.type === 'category') {
      return axisSettings.ticks.map(String);
    }
  }
  return (_axisSettings$domain = axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.domain) !== null && _axisSettings$domain !== void 0 ? _axisSettings$domain : defaultNumericDomain;
};
var selectDomainDefinition = createSelector([selectBaseAxis], getDomainDefinition);

/**
 * Under certain circumstances, we can determine the domain without looking at the data at all.
 * This is the case when the domain is explicitly specified as numbers, or when it is specified
 * as 'auto' or 'dataMin'/'dataMax' and data overflow is not allowed.
 *
 * In that case, this function will return the domain, otherwise it returns undefined.
 *
 * This is an optimization to avoid unnecessary data processing.
 * @param state
 * @param axisType
 * @param axisId
 * @param isPanorama
 */
var selectDomainFromUserPreference = createSelector([selectDomainDefinition, selectAllowsDataOverflow], numericalDomainSpecifiedWithoutRequiringData);
var selectDomainOfStackGroups = createSelector([selectStackGroups, selectChartDataWithIndexes, pickAxisType, selectDomainFromUserPreference], combineDomainOfStackGroups, {
  memoizeOptions: {
    resultEqualityCheck: numberDomainEqualityCheck
  }
});
var selectAllErrorBarSettings = state => state.errorBars;
var combineRelevantErrorBarSettings = (cartesianItemsSettings, allErrorBarSettings, axisType) => {
  return cartesianItemsSettings.flatMap(item => {
    return allErrorBarSettings[item.id];
  }).filter(Boolean).filter(e => {
    return isErrorBarRelevantForAxisType(axisType, e);
  });
};
var mergeDomains = function mergeDomains() {
  for (var _len = arguments.length, domains = new Array(_len), _key = 0; _key < _len; _key++) {
    domains[_key] = arguments[_key];
  }
  var allDomains = domains.filter(Boolean);
  if (allDomains.length === 0) {
    return undefined;
  }
  var allValues = allDomains.flat();
  var min = Math.min(...allValues);
  var max = Math.max(...allValues);
  return [min, max];
};
var combineDomainOfAllAppliedNumericalValuesIncludingErrorValues = (data, axisSettings, items, errorBars, axisType) => {
  var lowerEnd, upperEnd;
  if (items.length > 0) {
    data.forEach(entry => {
      items.forEach(item => {
        var _errorBars$item$id, _axisSettings$dataKey;
        var relevantErrorBars = (_errorBars$item$id = errorBars[item.id]) === null || _errorBars$item$id === void 0 ? void 0 : _errorBars$item$id.filter(errorBar => isErrorBarRelevantForAxisType(axisType, errorBar));
        var valueByDataKey = getValueByDataKey(entry, (_axisSettings$dataKey = axisSettings.dataKey) !== null && _axisSettings$dataKey !== void 0 ? _axisSettings$dataKey : item.dataKey);
        var errorDomain = getErrorDomainByDataKey(entry, valueByDataKey, relevantErrorBars);
        if (errorDomain.length >= 2) {
          var localLower = Math.min(...errorDomain);
          var localUpper = Math.max(...errorDomain);
          if (lowerEnd == null || localLower < lowerEnd) {
            lowerEnd = localLower;
          }
          if (upperEnd == null || localUpper > upperEnd) {
            upperEnd = localUpper;
          }
        }
        var dataValueDomain = makeDomain(valueByDataKey);
        if (dataValueDomain != null) {
          lowerEnd = lowerEnd == null ? dataValueDomain[0] : Math.min(lowerEnd, dataValueDomain[0]);
          upperEnd = upperEnd == null ? dataValueDomain[1] : Math.max(upperEnd, dataValueDomain[1]);
        }
      });
    });
  }
  if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null) {
    data.forEach(item => {
      var dataValueDomain = makeDomain(getValueByDataKey(item, axisSettings.dataKey));
      if (dataValueDomain != null) {
        lowerEnd = lowerEnd == null ? dataValueDomain[0] : Math.min(lowerEnd, dataValueDomain[0]);
        upperEnd = upperEnd == null ? dataValueDomain[1] : Math.max(upperEnd, dataValueDomain[1]);
      }
    });
  }
  if (isWellBehavedNumber(lowerEnd) && isWellBehavedNumber(upperEnd)) {
    return [lowerEnd, upperEnd];
  }
  return undefined;
};
var selectDomainOfAllAppliedNumericalValuesIncludingErrorValues = createSelector([selectDisplayedData, selectBaseAxis, selectCartesianItemsSettingsExceptStacked, selectAllErrorBarSettings, pickAxisType], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues, {
  memoizeOptions: {
    resultEqualityCheck: numberDomainEqualityCheck
  }
});
function onlyAllowNumbersAndStringsAndDates(item) {
  var {
    value
  } = item;
  if (isNumOrStr(value) || value instanceof Date) {
    return value;
  }
  return undefined;
}
var computeDomainOfTypeCategory = (allDataSquished, axisSettings, isCategorical) => {
  var categoricalDomain = allDataSquished.map(onlyAllowNumbersAndStringsAndDates).filter(v => v != null);
  if (isCategorical && (axisSettings.dataKey == null || axisSettings.allowDuplicatedCategory && hasDuplicate(categoricalDomain))) {
    /*
     * 1. In an absence of dataKey, Recharts will use array indexes as its categorical domain
     * 2. When category axis has duplicated text, serial numbers are used to generate scale
     */
    return range_default()(0, allDataSquished.length);
  }
  if (axisSettings.allowDuplicatedCategory) {
    return categoricalDomain;
  }
  return Array.from(new Set(categoricalDomain));
};
var selectReferenceDots = state => state.referenceElements.dots;
var filterReferenceElements = (elements, axisType, axisId) => {
  return elements.filter(el => el.ifOverflow === 'extendDomain').filter(el => {
    if (axisType === 'xAxis') {
      return el.xAxisId === axisId;
    }
    return el.yAxisId === axisId;
  });
};
var selectReferenceDotsByAxis = createSelector([selectReferenceDots, pickAxisType, pickAxisId], filterReferenceElements);
var selectReferenceAreas = state => state.referenceElements.areas;
var selectReferenceAreasByAxis = createSelector([selectReferenceAreas, pickAxisType, pickAxisId], filterReferenceElements);
var selectReferenceLines = state => state.referenceElements.lines;
var selectReferenceLinesByAxis = createSelector([selectReferenceLines, pickAxisType, pickAxisId], filterReferenceElements);
var combineDotsDomain = (dots, axisType) => {
  if (dots == null) {
    return undefined;
  }
  var allCoords = onlyAllowNumbers(dots.map(dot => axisType === 'xAxis' ? dot.x : dot.y));
  if (allCoords.length === 0) {
    return undefined;
  }
  return [Math.min(...allCoords), Math.max(...allCoords)];
};
var selectReferenceDotsDomain = createSelector(selectReferenceDotsByAxis, pickAxisType, combineDotsDomain);
var combineAreasDomain = (areas, axisType) => {
  if (areas == null) {
    return undefined;
  }
  var allCoords = onlyAllowNumbers(areas.flatMap(area => [axisType === 'xAxis' ? area.x1 : area.y1, axisType === 'xAxis' ? area.x2 : area.y2]));
  if (allCoords.length === 0) {
    return undefined;
  }
  return [Math.min(...allCoords), Math.max(...allCoords)];
};
var selectReferenceAreasDomain = createSelector([selectReferenceAreasByAxis, pickAxisType], combineAreasDomain);
function extractXCoordinates(line) {
  var _line$segment;
  if (line.x != null) {
    return onlyAllowNumbers([line.x]);
  }
  var segmentCoordinates = (_line$segment = line.segment) === null || _line$segment === void 0 ? void 0 : _line$segment.map(s => s.x);
  if (segmentCoordinates == null || segmentCoordinates.length === 0) {
    return [];
  }
  return onlyAllowNumbers(segmentCoordinates);
}
function extractYCoordinates(line) {
  var _line$segment2;
  if (line.y != null) {
    return onlyAllowNumbers([line.y]);
  }
  var segmentCoordinates = (_line$segment2 = line.segment) === null || _line$segment2 === void 0 ? void 0 : _line$segment2.map(s => s.y);
  if (segmentCoordinates == null || segmentCoordinates.length === 0) {
    return [];
  }
  return onlyAllowNumbers(segmentCoordinates);
}
var combineLinesDomain = (lines, axisType) => {
  if (lines == null) {
    return undefined;
  }
  var allCoords = lines.flatMap(line => axisType === 'xAxis' ? extractXCoordinates(line) : extractYCoordinates(line));
  if (allCoords.length === 0) {
    return undefined;
  }
  return [Math.min(...allCoords), Math.max(...allCoords)];
};
var selectReferenceLinesDomain = createSelector([selectReferenceLinesByAxis, pickAxisType], combineLinesDomain);
var selectReferenceElementsDomain = createSelector(selectReferenceDotsDomain, selectReferenceLinesDomain, selectReferenceAreasDomain, (dotsDomain, linesDomain, areasDomain) => {
  return mergeDomains(dotsDomain, areasDomain, linesDomain);
});
var combineNumericalDomain = (axisSettings, domainDefinition, domainFromUserPreference, domainOfStackGroups, dataAndErrorBarsDomain, referenceElementsDomain, layout, axisType) => {
  if (domainFromUserPreference != null) {
    // We're done! No need to compute anything else.
    return domainFromUserPreference;
  }
  var shouldIncludeDomainOfStackGroups = layout === 'vertical' && axisType === 'xAxis' || layout === 'horizontal' && axisType === 'yAxis';
  var mergedDomains = shouldIncludeDomainOfStackGroups ? mergeDomains(domainOfStackGroups, referenceElementsDomain, dataAndErrorBarsDomain) : mergeDomains(referenceElementsDomain, dataAndErrorBarsDomain);
  return parseNumericalUserDomain(domainDefinition, mergedDomains, axisSettings.allowDataOverflow);
};
var selectNumericalDomain = createSelector([selectBaseAxis, selectDomainDefinition, selectDomainFromUserPreference, selectDomainOfStackGroups, selectDomainOfAllAppliedNumericalValuesIncludingErrorValues, selectReferenceElementsDomain, selectChartLayout, pickAxisType], combineNumericalDomain, {
  memoizeOptions: {
    resultEqualityCheck: numberDomainEqualityCheck
  }
});

/**
 * Expand by design maps everything between 0 and 1,
 * there is nothing to compute.
 * See https://d3js.org/d3-shape/stack#stack-offsets
 */
var expandDomain = [0, 1];
var combineAxisDomain = (axisSettings, layout, displayedData, allAppliedValues, stackOffsetType, axisType, numericalDomain) => {
  if ((axisSettings == null || displayedData == null || displayedData.length === 0) && numericalDomain === undefined) {
    return undefined;
  }
  var {
    dataKey,
    type
  } = axisSettings;
  var isCategorical = isCategoricalAxis(layout, axisType);
  if (isCategorical && dataKey == null) {
    var _displayedData$length;
    return range_default()(0, (_displayedData$length = displayedData === null || displayedData === void 0 ? void 0 : displayedData.length) !== null && _displayedData$length !== void 0 ? _displayedData$length : 0);
  }
  if (type === 'category') {
    return computeDomainOfTypeCategory(allAppliedValues, axisSettings, isCategorical);
  }
  if (stackOffsetType === 'expand') {
    return expandDomain;
  }
  return numericalDomain;
};
var selectAxisDomain = createSelector([selectBaseAxis, selectChartLayout, selectDisplayedData, selectAllAppliedValues, selectStackOffsetType, pickAxisType, selectNumericalDomain], combineAxisDomain);
function isSupportedScaleName(name) {
  return name in d3_scale_namespaceObject;
}
var combineRealScaleType = (axisConfig, hasBar, chartType) => {
  if (axisConfig == null) {
    return undefined;
  }
  var {
    scale,
    type
  } = axisConfig;
  if (scale === 'auto') {
    if (type === 'category' && chartType && (chartType.indexOf('LineChart') >= 0 || chartType.indexOf('AreaChart') >= 0 || chartType.indexOf('ComposedChart') >= 0 && !hasBar)) {
      return 'point';
    }
    if (type === 'category') {
      return 'band';
    }
    return 'linear';
  }
  if (typeof scale === 'string') {
    var name = "scale".concat(upperFirst(scale));
    return isSupportedScaleName(name) ? name : 'point';
  }
  return undefined;
};
var selectRealScaleType = createSelector([selectBaseAxis, selectHasBar, selectChartName], combineRealScaleType);
function combineScaleFunction(axis, realScaleType, axisDomain, axisRange) {
  if (axisDomain == null || axisRange == null) {
    return undefined;
  }
  if (typeof axis.scale === 'function') {
    return rechartsScaleFactory(axis.scale, axisDomain, axisRange);
  }
  return rechartsScaleFactory(realScaleType, axisDomain, axisRange);
}
var combineNiceTicks = (axisDomain, axisSettings, realScaleType) => {
  var domainDefinition = getDomainDefinition(axisSettings);
  if (realScaleType !== 'auto' && realScaleType !== 'linear') {
    return undefined;
  }
  if (axisSettings != null && axisSettings.tickCount && Array.isArray(domainDefinition) && (domainDefinition[0] === 'auto' || domainDefinition[1] === 'auto') && isWellFormedNumberDomain(axisDomain)) {
    return getNiceTickValues(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals);
  }
  if (axisSettings != null && axisSettings.tickCount && axisSettings.type === 'number' && isWellFormedNumberDomain(axisDomain)) {
    return getTickValuesFixedDomain(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals);
  }
  return undefined;
};
var selectNiceTicks = createSelector([selectAxisDomain, selectRenderableAxisSettings, selectRealScaleType], combineNiceTicks);
var combineAxisDomainWithNiceTicks = (axisSettings, domain, niceTicks, axisType) => {
  if (
  /*
   * Angle axis for some reason uses nice ticks when rendering axis tick labels,
   * but doesn't use nice ticks for extending domain like all the other axes do.
   * Not really sure why? Is there a good reason,
   * or is it just because someone added support for nice ticks to the other axes and forgot this one?
   */
  axisType !== 'angleAxis' && (axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.type) === 'number' && isWellFormedNumberDomain(domain) && Array.isArray(niceTicks) && niceTicks.length > 0) {
    var _niceTicks$, _niceTicks;
    var minFromDomain = domain[0];
    var minFromTicks = (_niceTicks$ = niceTicks[0]) !== null && _niceTicks$ !== void 0 ? _niceTicks$ : 0;
    var maxFromDomain = domain[1];
    var maxFromTicks = (_niceTicks = niceTicks[niceTicks.length - 1]) !== null && _niceTicks !== void 0 ? _niceTicks : 0;
    return [Math.min(minFromDomain, minFromTicks), Math.max(maxFromDomain, maxFromTicks)];
  }
  return domain;
};
var selectAxisDomainIncludingNiceTicks = createSelector([selectBaseAxis, selectAxisDomain, selectNiceTicks, pickAxisType], combineAxisDomainWithNiceTicks);

/**
 * Returns the smallest gap, between two numbers in the data, as a ratio of the whole range (max - min).
 * Ignores domain provided by user and only considers domain from data.
 *
 * The result is a number between 0 and 1.
 */
var selectSmallestDistanceBetweenValues = createSelector(selectAllAppliedValues, selectBaseAxis, (allDataSquished, axisSettings) => {
  if (!axisSettings || axisSettings.type !== 'number') {
    return undefined;
  }
  var smallestDistanceBetweenValues = Infinity;
  var sortedValues = Array.from(onlyAllowNumbers(allDataSquished.map(d => d.value))).sort((a, b) => a - b);
  var first = sortedValues[0];
  var last = sortedValues[sortedValues.length - 1];
  if (first == null || last == null) {
    return Infinity;
  }
  var diff = last - first;
  if (diff === 0) {
    return Infinity;
  }
  // Only do n - 1 distance calculations because there's only n - 1 distances between n values.
  for (var i = 0; i < sortedValues.length - 1; i++) {
    var curr = sortedValues[i];
    var next = sortedValues[i + 1];
    if (curr == null || next == null) {
      continue;
    }
    var distance = next - curr;
    smallestDistanceBetweenValues = Math.min(smallestDistanceBetweenValues, distance);
  }
  return smallestDistanceBetweenValues / diff;
});
var selectCalculatedPadding = createSelector(selectSmallestDistanceBetweenValues, selectChartLayout, selectBarCategoryGap, selectChartOffsetInternal, (_1, _2, _3, _4, padding) => padding, (smallestDistanceInPercent, layout, barCategoryGap, offset, padding) => {
  if (!isWellBehavedNumber(smallestDistanceInPercent)) {
    return 0;
  }
  var rangeWidth = layout === 'vertical' ? offset.height : offset.width;
  if (padding === 'gap') {
    return smallestDistanceInPercent * rangeWidth / 2;
  }
  if (padding === 'no-gap') {
    var gap = getPercentValue(barCategoryGap, smallestDistanceInPercent * rangeWidth);
    var halfBand = smallestDistanceInPercent * rangeWidth / 2;
    return halfBand - gap - (halfBand - gap) / rangeWidth * gap;
  }
  return 0;
});
var selectCalculatedXAxisPadding = (state, axisId, isPanorama) => {
  var xAxisSettings = selectXAxisSettings(state, axisId);
  if (xAxisSettings == null || typeof xAxisSettings.padding !== 'string') {
    return 0;
  }
  return selectCalculatedPadding(state, 'xAxis', axisId, isPanorama, xAxisSettings.padding);
};
var selectCalculatedYAxisPadding = (state, axisId, isPanorama) => {
  var yAxisSettings = selectYAxisSettings(state, axisId);
  if (yAxisSettings == null || typeof yAxisSettings.padding !== 'string') {
    return 0;
  }
  return selectCalculatedPadding(state, 'yAxis', axisId, isPanorama, yAxisSettings.padding);
};
var selectXAxisPadding = createSelector(selectXAxisSettings, selectCalculatedXAxisPadding, (xAxisSettings, calculated) => {
  var _padding$left, _padding$right;
  if (xAxisSettings == null) {
    return {
      left: 0,
      right: 0
    };
  }
  var {
    padding
  } = xAxisSettings;
  if (typeof padding === 'string') {
    return {
      left: calculated,
      right: calculated
    };
  }
  return {
    left: ((_padding$left = padding.left) !== null && _padding$left !== void 0 ? _padding$left : 0) + calculated,
    right: ((_padding$right = padding.right) !== null && _padding$right !== void 0 ? _padding$right : 0) + calculated
  };
});
var selectYAxisPadding = createSelector(selectYAxisSettings, selectCalculatedYAxisPadding, (yAxisSettings, calculated) => {
  var _padding$top, _padding$bottom;
  if (yAxisSettings == null) {
    return {
      top: 0,
      bottom: 0
    };
  }
  var {
    padding
  } = yAxisSettings;
  if (typeof padding === 'string') {
    return {
      top: calculated,
      bottom: calculated
    };
  }
  return {
    top: ((_padding$top = padding.top) !== null && _padding$top !== void 0 ? _padding$top : 0) + calculated,
    bottom: ((_padding$bottom = padding.bottom) !== null && _padding$bottom !== void 0 ? _padding$bottom : 0) + calculated
  };
});
var combineXAxisRange = createSelector([selectChartOffsetInternal, selectXAxisPadding, selectBrushDimensions, selectBrushSettings, (_state, _axisId, isPanorama) => isPanorama], (offset, padding, brushDimensions, _ref4, isPanorama) => {
  var {
    padding: brushPadding
  } = _ref4;
  if (isPanorama) {
    return [brushPadding.left, brushDimensions.width - brushPadding.right];
  }
  return [offset.left + padding.left, offset.left + offset.width - padding.right];
});
var combineYAxisRange = createSelector([selectChartOffsetInternal, selectChartLayout, selectYAxisPadding, selectBrushDimensions, selectBrushSettings, (_state, _axisId, isPanorama) => isPanorama], (offset, layout, padding, brushDimensions, _ref5, isPanorama) => {
  var {
    padding: brushPadding
  } = _ref5;
  if (isPanorama) {
    return [brushDimensions.height - brushPadding.bottom, brushPadding.top];
  }
  if (layout === 'horizontal') {
    return [offset.top + offset.height - padding.bottom, offset.top + padding.top];
  }
  return [offset.top + padding.top, offset.top + offset.height - padding.bottom];
});
var selectAxisRange = (state, axisType, axisId, isPanorama) => {
  var _selectZAxisSettings;
  switch (axisType) {
    case 'xAxis':
      return combineXAxisRange(state, axisId, isPanorama);
    case 'yAxis':
      return combineYAxisRange(state, axisId, isPanorama);
    case 'zAxis':
      return (_selectZAxisSettings = selectZAxisSettings(state, axisId)) === null || _selectZAxisSettings === void 0 ? void 0 : _selectZAxisSettings.range;
    case 'angleAxis':
      return selectAngleAxisRange(state);
    case 'radiusAxis':
      return selectRadiusAxisRange(state, axisId);
    default:
      return undefined;
  }
};
var selectAxisRangeWithReverse = createSelector([selectBaseAxis, selectAxisRange], combineAxisRangeWithReverse);
var selectCheckedAxisDomain = createSelector([selectRealScaleType, selectAxisDomainIncludingNiceTicks], combineCheckedDomain);
var selectAxisScale = createSelector([selectBaseAxis, selectRealScaleType, selectCheckedAxisDomain, selectAxisRangeWithReverse], combineScaleFunction);
var selectErrorBarsSettings = createSelector([selectCartesianItemsSettings, selectAllErrorBarSettings, pickAxisType], combineRelevantErrorBarSettings);
function compareIds(a, b) {
  if (a.id < b.id) {
    return -1;
  }
  if (a.id > b.id) {
    return 1;
  }
  return 0;
}
var pickAxisOrientation = (_state, orientation) => orientation;
var pickMirror = (_state, _orientation, mirror) => mirror;
var selectAllXAxesWithOffsetType = createSelector(selectAllXAxes, pickAxisOrientation, pickMirror, (allAxes, orientation, mirror) => allAxes.filter(axis => axis.orientation === orientation).filter(axis => axis.mirror === mirror).sort(compareIds));
var selectAllYAxesWithOffsetType = createSelector(selectAllYAxes, pickAxisOrientation, pickMirror, (allAxes, orientation, mirror) => allAxes.filter(axis => axis.orientation === orientation).filter(axis => axis.mirror === mirror).sort(compareIds));
var getXAxisSize = (offset, axisSettings) => {
  return {
    width: offset.width,
    height: axisSettings.height
  };
};
var getYAxisSize = (offset, axisSettings) => {
  var width = typeof axisSettings.width === 'number' ? axisSettings.width : DEFAULT_Y_AXIS_WIDTH;
  return {
    width,
    height: offset.height
  };
};
var selectXAxisSize = createSelector(selectChartOffsetInternal, selectXAxisSettings, getXAxisSize);
var combineXAxisPositionStartingPoint = (offset, orientation, chartHeight) => {
  switch (orientation) {
    case 'top':
      return offset.top;
    case 'bottom':
      return chartHeight - offset.bottom;
    default:
      return 0;
  }
};
var combineYAxisPositionStartingPoint = (offset, orientation, chartWidth) => {
  switch (orientation) {
    case 'left':
      return offset.left;
    case 'right':
      return chartWidth - offset.right;
    default:
      return 0;
  }
};
var selectAllXAxesOffsetSteps = createSelector(selectChartHeight, selectChartOffsetInternal, selectAllXAxesWithOffsetType, pickAxisOrientation, pickMirror, (chartHeight, offset, allAxesWithSameOffsetType, orientation, mirror) => {
  var steps = {};
  var position;
  allAxesWithSameOffsetType.forEach(axis => {
    var axisSize = getXAxisSize(offset, axis);
    if (position == null) {
      position = combineXAxisPositionStartingPoint(offset, orientation, chartHeight);
    }
    var needSpace = orientation === 'top' && !mirror || orientation === 'bottom' && mirror;
    steps[axis.id] = position - Number(needSpace) * axisSize.height;
    position += (needSpace ? -1 : 1) * axisSize.height;
  });
  return steps;
});
var selectAllYAxesOffsetSteps = createSelector(selectChartWidth, selectChartOffsetInternal, selectAllYAxesWithOffsetType, pickAxisOrientation, pickMirror, (chartWidth, offset, allAxesWithSameOffsetType, orientation, mirror) => {
  var steps = {};
  var position;
  allAxesWithSameOffsetType.forEach(axis => {
    var axisSize = getYAxisSize(offset, axis);
    if (position == null) {
      position = combineYAxisPositionStartingPoint(offset, orientation, chartWidth);
    }
    var needSpace = orientation === 'left' && !mirror || orientation === 'right' && mirror;
    steps[axis.id] = position - Number(needSpace) * axisSize.width;
    position += (needSpace ? -1 : 1) * axisSize.width;
  });
  return steps;
});
var selectXAxisOffsetSteps = (state, axisId) => {
  var axisSettings = selectXAxisSettings(state, axisId);
  if (axisSettings == null) {
    return undefined;
  }
  return selectAllXAxesOffsetSteps(state, axisSettings.orientation, axisSettings.mirror);
};
var selectXAxisPosition = createSelector([selectChartOffsetInternal, selectXAxisSettings, selectXAxisOffsetSteps, (_, axisId) => axisId], (offset, axisSettings, allSteps, axisId) => {
  if (axisSettings == null) {
    return undefined;
  }
  var stepOfThisAxis = allSteps === null || allSteps === void 0 ? void 0 : allSteps[axisId];
  if (stepOfThisAxis == null) {
    return {
      x: offset.left,
      y: 0
    };
  }
  return {
    x: offset.left,
    y: stepOfThisAxis
  };
});
var selectYAxisOffsetSteps = (state, axisId) => {
  var axisSettings = selectYAxisSettings(state, axisId);
  if (axisSettings == null) {
    return undefined;
  }
  return selectAllYAxesOffsetSteps(state, axisSettings.orientation, axisSettings.mirror);
};
var selectYAxisPosition = createSelector([selectChartOffsetInternal, selectYAxisSettings, selectYAxisOffsetSteps, (_, axisId) => axisId], (offset, axisSettings, allSteps, axisId) => {
  if (axisSettings == null) {
    return undefined;
  }
  var stepOfThisAxis = allSteps === null || allSteps === void 0 ? void 0 : allSteps[axisId];
  if (stepOfThisAxis == null) {
    return {
      x: 0,
      y: offset.top
    };
  }
  return {
    x: stepOfThisAxis,
    y: offset.top
  };
});
var selectYAxisSize = createSelector(selectChartOffsetInternal, selectYAxisSettings, (offset, axisSettings) => {
  var width = typeof axisSettings.width === 'number' ? axisSettings.width : DEFAULT_Y_AXIS_WIDTH;
  return {
    width,
    height: offset.height
  };
});
var selectCartesianAxisSize = (state, axisType, axisId) => {
  switch (axisType) {
    case 'xAxis':
      {
        return selectXAxisSize(state, axisId).width;
      }
    case 'yAxis':
      {
        return selectYAxisSize(state, axisId).height;
      }
    default:
      {
        return undefined;
      }
  }
};
var combineDuplicateDomain = (chartLayout, appliedValues, axis, axisType) => {
  if (axis == null) {
    return undefined;
  }
  var {
    allowDuplicatedCategory,
    type,
    dataKey
  } = axis;
  var isCategorical = isCategoricalAxis(chartLayout, axisType);
  var allData = appliedValues.map(av => av.value);
  if (dataKey && isCategorical && type === 'category' && allowDuplicatedCategory && hasDuplicate(allData)) {
    return allData;
  }
  return undefined;
};
var selectDuplicateDomain = createSelector([selectChartLayout, selectAllAppliedValues, selectBaseAxis, pickAxisType], combineDuplicateDomain);
var combineCategoricalDomain = (layout, appliedValues, axis, axisType) => {
  if (axis == null || axis.dataKey == null) {
    return undefined;
  }
  var {
    type,
    scale
  } = axis;
  var isCategorical = isCategoricalAxis(layout, axisType);
  if (isCategorical && (type === 'number' || scale !== 'auto')) {
    return appliedValues.map(d => d.value);
  }
  return undefined;
};
var selectCategoricalDomain = createSelector([selectChartLayout, selectAllAppliedValues, selectRenderableAxisSettings, pickAxisType], combineCategoricalDomain);
var selectAxisPropsNeededForCartesianGridTicksGenerator = createSelector([selectChartLayout, selectCartesianAxisSettings, selectRealScaleType, selectAxisScale, selectDuplicateDomain, selectCategoricalDomain, selectAxisRange, selectNiceTicks, pickAxisType], (layout, axis, realScaleType, scale, duplicateDomain, categoricalDomain, axisRange, niceTicks, axisType) => {
  if (axis == null) {
    return undefined;
  }
  var isCategorical = isCategoricalAxis(layout, axisType);
  return {
    angle: axis.angle,
    interval: axis.interval,
    minTickGap: axis.minTickGap,
    orientation: axis.orientation,
    tick: axis.tick,
    tickCount: axis.tickCount,
    tickFormatter: axis.tickFormatter,
    ticks: axis.ticks,
    type: axis.type,
    unit: axis.unit,
    axisType,
    categoricalDomain,
    duplicateDomain,
    isCategorical,
    niceTicks,
    range: axisRange,
    realScaleType,
    scale
  };
});

/**
 * Of on four almost identical implementations of tick generation.
 * The four horsemen of tick generation are:
 * - {@link selectTooltipAxisTicks}
 * - {@link combineAxisTicks}
 * - {@link getTicksOfAxis}.
 * - {@link combineGraphicalItemTicks}
 */
var combineAxisTicks = (layout, axis, realScaleType, scale, niceTicks, axisRange, duplicateDomain, categoricalDomain, axisType) => {
  if (axis == null || scale == null) {
    return undefined;
  }
  var isCategorical = isCategoricalAxis(layout, axisType);
  var {
    type,
    ticks,
    tickCount
  } = axis;
  var offsetForBand =
  // @ts-expect-error This is testing for `scaleBand` but for band axis the type is reported as `band` so this looks like a dead code with a workaround elsewhere?
  realScaleType === 'scaleBand' && typeof scale.bandwidth === 'function' ? scale.bandwidth() / 2 : 2;
  var offset = type === 'category' && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
  offset = axisType === 'angleAxis' && axisRange != null && axisRange.length >= 2 ? mathSign(axisRange[0] - axisRange[1]) * 2 * offset : offset;

  // The ticks set by user should only affect the ticks adjacent to axis line
  var ticksOrNiceTicks = ticks || niceTicks;
  if (ticksOrNiceTicks) {
    return ticksOrNiceTicks.map((entry, index) => {
      var scaleContent = duplicateDomain ? duplicateDomain.indexOf(entry) : entry;
      var scaled = scale.map(scaleContent);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        index,
        coordinate: scaled + offset,
        value: entry,
        offset
      };
    }).filter(isNotNil);
  }

  // When axis is a categorical axis, but the type of axis is number or the scale of axis is not "auto"
  if (isCategorical && categoricalDomain) {
    return categoricalDomain.map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }
  if (scale.ticks) {
    return scale.ticks(tickCount).map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }

  // When axis has duplicated text, serial numbers are used to generate scale
  return scale.domain().map((entry, index) => {
    var scaled = scale.map(entry);
    if (!isWellBehavedNumber(scaled)) {
      return null;
    }
    return {
      coordinate: scaled + offset,
      // @ts-expect-error can't use Date as index
      value: duplicateDomain ? duplicateDomain[entry] : entry,
      index,
      offset
    };
  }).filter(isNotNil);
};
var selectTicksOfAxis = createSelector([selectChartLayout, selectRenderableAxisSettings, selectRealScaleType, selectAxisScale, selectNiceTicks, selectAxisRange, selectDuplicateDomain, selectCategoricalDomain, pickAxisType], combineAxisTicks);

/**
 * Of on four almost identical implementations of tick generation.
 * The four horsemen of tick generation are:
 * - {@link selectTooltipAxisTicks}
 * - {@link combineAxisTicks}
 * - {@link getTicksOfAxis}.
 * - {@link combineGraphicalItemTicks}
 */
var combineGraphicalItemTicks = (layout, axis, scale, axisRange, duplicateDomain, categoricalDomain, axisType) => {
  if (axis == null || scale == null || axisRange == null || axisRange[0] === axisRange[1]) {
    return undefined;
  }
  var isCategorical = isCategoricalAxis(layout, axisType);
  var {
    tickCount
  } = axis;
  var offset = 0;
  offset = axisType === 'angleAxis' && (axisRange === null || axisRange === void 0 ? void 0 : axisRange.length) >= 2 ? mathSign(axisRange[0] - axisRange[1]) * 2 * offset : offset;

  // When axis is a categorical axis, but the type of axis is number or the scale of axis is not "auto"
  if (isCategorical && categoricalDomain) {
    return categoricalDomain.map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }
  if (scale.ticks) {
    return scale.ticks(tickCount).map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }

  // When axis has duplicated text, serial numbers are used to generate scale
  return scale.domain().map((entry, index) => {
    var scaled = scale.map(entry);
    if (!isWellBehavedNumber(scaled)) {
      return null;
    }
    return {
      coordinate: scaled + offset,
      // @ts-expect-error can't use unknown as index
      value: duplicateDomain ? duplicateDomain[entry] : entry,
      index,
      offset
    };
  }).filter(isNotNil);
};
var selectTicksOfGraphicalItem = createSelector([selectChartLayout, selectRenderableAxisSettings, selectAxisScale, selectAxisRange, selectDuplicateDomain, selectCategoricalDomain, pickAxisType], combineGraphicalItemTicks);

/**
 * This is the internal representation of an axis along with its scale function.
 * Here we have already computed the scale function for the axis,
 * and replaced the union type of scale (string | function) with just the function type.
 */

var selectAxisWithScale = createSelector(selectBaseAxis, selectAxisScale, (axis, scale) => {
  if (axis == null || scale == null) {
    return undefined;
  }
  return axisSelectors_objectSpread(axisSelectors_objectSpread({}, axis), {}, {
    scale
  });
});
var selectZAxisScale = createSelector([selectBaseAxis, selectRealScaleType, selectAxisDomain, selectAxisRangeWithReverse], combineScaleFunction);
var selectZAxisWithScale = createSelector((state, _axisType, axisId) => selectZAxisSettings(state, axisId), selectZAxisScale, (axis, scale) => {
  if (axis == null || scale == null) {
    return undefined;
  }
  return axisSelectors_objectSpread(axisSelectors_objectSpread({}, axis), {}, {
    scale
  });
});

/**
 * We are also going to need to implement polar chart directions if we want to support keyboard controls for those.
 */

var selectChartDirection = createSelector([selectChartLayout, selectAllXAxes, selectAllYAxes], (layout, allXAxes, allYAxes) => {
  switch (layout) {
    case 'horizontal':
      {
        return allXAxes.some(axis => axis.reversed) ? 'right-to-left' : 'left-to-right';
      }
    case 'vertical':
      {
        return allYAxes.some(axis => axis.reversed) ? 'bottom-to-top' : 'top-to-bottom';
      }
    // TODO: make this better. For now, right arrow triggers "forward", left arrow "back"
    // however, the tooltip moves an unintuitive direction because of how the indices are rendered
    case 'centric':
    case 'radial':
      {
        return 'left-to-right';
      }
    default:
      {
        return undefined;
      }
  }
});
;// ./node_modules/recharts/es6/state/selectors/selectTooltipEventType.js

var selectDefaultTooltipEventType = state => state.options.defaultTooltipEventType;
var selectValidateTooltipEventTypes = state => state.options.validateTooltipEventTypes;
function combineTooltipEventType(shared, defaultTooltipEventType, validateTooltipEventTypes) {
  if (shared == null) {
    return defaultTooltipEventType;
  }
  var eventType = shared ? 'axis' : 'item';
  if (validateTooltipEventTypes == null) {
    return defaultTooltipEventType;
  }
  return validateTooltipEventTypes.includes(eventType) ? eventType : defaultTooltipEventType;
}
function selectTooltipEventType(state, shared) {
  var defaultTooltipEventType = selectDefaultTooltipEventType(state);
  var validateTooltipEventTypes = selectValidateTooltipEventTypes(state);
  return combineTooltipEventType(shared, defaultTooltipEventType, validateTooltipEventTypes);
}
function useTooltipEventType(shared) {
  return useAppSelector(state => selectTooltipEventType(state, shared));
}
;// ./node_modules/recharts/es6/state/selectors/combiners/combineActiveLabel.js

var combineActiveLabel = (tooltipTicks, activeIndex) => {
  var _tooltipTicks$n;
  var n = Number(activeIndex);
  if (isNan(n) || activeIndex == null) {
    return undefined;
  }
  return n >= 0 ? tooltipTicks === null || tooltipTicks === void 0 || (_tooltipTicks$n = tooltipTicks[n]) === null || _tooltipTicks$n === void 0 ? void 0 : _tooltipTicks$n.value : undefined;
};
;// ./node_modules/recharts/es6/state/selectors/selectTooltipSettings.js
var selectTooltipSettings = state => state.tooltip.settings;
;// ./node_modules/recharts/es6/state/selectors/combiners/combineTooltipInteractionState.js
function combineTooltipInteractionState_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function combineTooltipInteractionState_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? combineTooltipInteractionState_ownKeys(Object(t), !0).forEach(function (r) { combineTooltipInteractionState_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : combineTooltipInteractionState_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function combineTooltipInteractionState_defineProperty(e, r, t) { return (r = combineTooltipInteractionState_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function combineTooltipInteractionState_toPropertyKey(t) { var i = combineTooltipInteractionState_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function combineTooltipInteractionState_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }

function chooseAppropriateMouseInteraction(tooltipState, tooltipEventType, trigger) {
  if (tooltipEventType === 'axis') {
    if (trigger === 'click') {
      return tooltipState.axisInteraction.click;
    }
    return tooltipState.axisInteraction.hover;
  }
  if (trigger === 'click') {
    return tooltipState.itemInteraction.click;
  }
  return tooltipState.itemInteraction.hover;
}
function hasBeenActivePreviously(tooltipInteractionState) {
  return tooltipInteractionState.index != null;
}
var combineTooltipInteractionState = (tooltipState, tooltipEventType, trigger, defaultIndex) => {
  if (tooltipEventType == null) {
    return noInteraction;
  }
  var appropriateMouseInteraction = chooseAppropriateMouseInteraction(tooltipState, tooltipEventType, trigger);
  if (appropriateMouseInteraction == null) {
    return noInteraction;
  }
  if (appropriateMouseInteraction.active) {
    return appropriateMouseInteraction;
  }
  if (tooltipState.keyboardInteraction.active) {
    return tooltipState.keyboardInteraction;
  }
  if (tooltipState.syncInteraction.active && tooltipState.syncInteraction.index != null) {
    return tooltipState.syncInteraction;
  }
  var activeFromProps = tooltipState.settings.active === true;
  if (hasBeenActivePreviously(appropriateMouseInteraction)) {
    if (activeFromProps) {
      return combineTooltipInteractionState_objectSpread(combineTooltipInteractionState_objectSpread({}, appropriateMouseInteraction), {}, {
        active: true
      });
    }
  } else if (defaultIndex != null) {
    return {
      active: true,
      coordinate: undefined,
      dataKey: undefined,
      index: defaultIndex,
      graphicalItemId: undefined
    };
  }
  return combineTooltipInteractionState_objectSpread(combineTooltipInteractionState_objectSpread({}, noInteraction), {}, {
    coordinate: appropriateMouseInteraction.coordinate
  });
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineActiveTooltipIndex.js



function toFiniteNumber(value) {
  if (typeof value === 'number') {
    return Number.isFinite(value) ? value : undefined;
  }
  if (value instanceof Date) {
    var numericValue = value.valueOf();
    return Number.isFinite(numericValue) ? numericValue : undefined;
  }
  var parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : undefined;
}
function isValueWithinNumberDomain(value, domain) {
  var numericValue = toFiniteNumber(value);
  var lowerBound = domain[0];
  var upperBound = domain[1];
  if (numericValue === undefined) {
    return false;
  }
  var min = Math.min(lowerBound, upperBound);
  var max = Math.max(lowerBound, upperBound);
  return numericValue >= min && numericValue <= max;
}
function isValueWithinDomain(entry, axisDataKey, domain) {
  if (domain == null || axisDataKey == null) {
    return true;
  }
  var value = getValueByDataKey(entry, axisDataKey);
  if (value == null) {
    return true;
  }
  if (!isWellFormedNumberDomain(domain)) {
    return true;
  }
  return isValueWithinNumberDomain(value, domain);
}
var combineActiveTooltipIndex = (tooltipInteraction, chartData, axisDataKey, domain) => {
  var desiredIndex = tooltipInteraction === null || tooltipInteraction === void 0 ? void 0 : tooltipInteraction.index;
  if (desiredIndex == null) {
    return null;
  }
  var indexAsNumber = Number(desiredIndex);
  if (!isWellBehavedNumber(indexAsNumber)) {
    // this is for charts like Sankey and Treemap that do not support numerical indexes. We need a proper solution for this before we can start supporting keyboard events on these charts.
    return desiredIndex;
  }

  /*
   * Zero is a trivial limit for single-dimensional charts like Line and Area,
   * but this also needs a support for multidimensional charts like Sankey and Treemap! TODO
   */
  var lowerLimit = 0;
  var upperLimit = +Infinity;
  if (chartData.length > 0) {
    upperLimit = chartData.length - 1;
  }

  // now let's clamp the desiredIndex between the limits
  var clampedIndex = Math.max(lowerLimit, Math.min(indexAsNumber, upperLimit));
  var entry = chartData[clampedIndex];
  if (entry == null) {
    return String(clampedIndex);
  }
  if (!isValueWithinDomain(entry, axisDataKey, domain)) {
    return null;
  }
  return String(clampedIndex);
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineCoordinateForDefaultIndex.js
var combineCoordinateForDefaultIndex = (width, height, layout, offset, tooltipTicks, defaultIndex, tooltipConfigurations) => {
  if (defaultIndex == null) {
    return undefined;
  }
  /*
   * With defaultIndex alone, we don't have enough information to decide _which_ of the multiple tooltips to display.
   * Maybe one day we could add new prop `activeGraphicalItemId` to the chart to help with that.
   * Until then, we choose the first one.
   */
  var firstConfiguration = tooltipConfigurations[0];
  var maybePosition = firstConfiguration === null || firstConfiguration === void 0 ? void 0 : firstConfiguration.getPosition(defaultIndex);
  if (maybePosition != null) {
    return maybePosition;
  }
  var tick = tooltipTicks === null || tooltipTicks === void 0 ? void 0 : tooltipTicks[Number(defaultIndex)];
  if (!tick) {
    return undefined;
  }
  switch (layout) {
    case 'horizontal':
      {
        return {
          x: tick.coordinate,
          y: (offset.top + height) / 2
        };
      }
    default:
      {
        // This logic is not super sound - it conflates vertical, radial, centric layouts into just one. TODO improve!
        return {
          x: (offset.left + width) / 2,
          y: tick.coordinate
        };
      }
  }
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineTooltipPayloadConfigurations.js
var combineTooltipPayloadConfigurations = (tooltipState, tooltipEventType, trigger, defaultIndex) => {
  // if tooltip reacts to axis interaction, then we display all items at the same time.
  if (tooltipEventType === 'axis') {
    return tooltipState.tooltipItemPayloads;
  }
  /*
   * By now we already know that tooltipEventType is 'item', so we can only search in itemInteractions.
   * item means that only the hovered or clicked item will be present in the tooltip.
   */
  if (tooltipState.tooltipItemPayloads.length === 0) {
    // No point filtering if the payload is empty
    return [];
  }
  var filterByGraphicalItemId;
  if (trigger === 'hover') {
    filterByGraphicalItemId = tooltipState.itemInteraction.hover.graphicalItemId;
  } else {
    filterByGraphicalItemId = tooltipState.itemInteraction.click.graphicalItemId;
  }
  if (filterByGraphicalItemId == null && defaultIndex != null) {
    /*
     * So when we use `defaultIndex` - we don't have a dataKey to filter by because user did not hover over anything yet.
     * In that case let's display the first item in the tooltip; after all, this is `item` interaction case,
     * so we should display only one item at a time instead of all.
     */
    var firstItemPayload = tooltipState.tooltipItemPayloads[0];
    if (firstItemPayload != null) {
      return [firstItemPayload];
    }
    return [];
  }
  return tooltipState.tooltipItemPayloads.filter(tpc => {
    var _tpc$settings;
    return ((_tpc$settings = tpc.settings) === null || _tpc$settings === void 0 ? void 0 : _tpc$settings.graphicalItemId) === filterByGraphicalItemId;
  });
};
;// ./node_modules/recharts/es6/state/selectors/selectTooltipPayloadSearcher.js
var selectTooltipPayloadSearcher = state => state.options.tooltipPayloadSearcher;
;// ./node_modules/recharts/es6/state/selectors/selectTooltipState.js
var selectTooltipState = state => state.tooltip;
;// ./node_modules/recharts/es6/state/selectors/combiners/combineTooltipPayload.js
function combineTooltipPayload_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function combineTooltipPayload_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? combineTooltipPayload_ownKeys(Object(t), !0).forEach(function (r) { combineTooltipPayload_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : combineTooltipPayload_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function combineTooltipPayload_defineProperty(e, r, t) { return (r = combineTooltipPayload_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function combineTooltipPayload_toPropertyKey(t) { var i = combineTooltipPayload_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function combineTooltipPayload_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }



function selectFinalData(dataDefinedOnItem, dataDefinedOnChart) {
  /*
   * If a payload has data specified directly from the graphical item, prefer that.
   * Otherwise, fill in data from the chart level, using the same index.
   */
  if (dataDefinedOnItem != null) {
    return dataDefinedOnItem;
  }
  return dataDefinedOnChart;
}
var combineTooltipPayload = (tooltipPayloadConfigurations, activeIndex, chartDataState, tooltipAxisDataKey, activeLabel, tooltipPayloadSearcher, tooltipEventType) => {
  if (activeIndex == null || tooltipPayloadSearcher == null) {
    return undefined;
  }
  var {
    chartData,
    computedData,
    dataStartIndex,
    dataEndIndex
  } = chartDataState;
  var init = [];
  return tooltipPayloadConfigurations.reduce((agg, _ref) => {
    var _settings$dataKey;
    var {
      dataDefinedOnItem,
      settings
    } = _ref;
    var finalData = selectFinalData(dataDefinedOnItem, chartData);
    var sliced = Array.isArray(finalData) ? getSliced(finalData, dataStartIndex, dataEndIndex) : finalData;
    var finalDataKey = (_settings$dataKey = settings === null || settings === void 0 ? void 0 : settings.dataKey) !== null && _settings$dataKey !== void 0 ? _settings$dataKey : tooltipAxisDataKey;
    // BaseAxisProps does not support nameKey but it could!
    var finalNameKey = settings === null || settings === void 0 ? void 0 : settings.nameKey; // ?? tooltipAxis?.nameKey;
    var tooltipPayload;
    if (tooltipAxisDataKey && Array.isArray(sliced) &&
    /*
     * findEntryInArray won't work for Scatter because Scatter provides an array of arrays
     * as tooltip payloads and findEntryInArray is not prepared to handle that.
     * Sad but also ScatterChart only allows 'item' tooltipEventType
     * and also this is only a problem if there are multiple Scatters and each has its own data array
     * so let's fix that some other time.
     */
    !Array.isArray(sliced[0]) &&
    /*
     * If the tooltipEventType is 'axis', we should search for the dataKey in the sliced data
     * because thanks to allowDuplicatedCategory=false, the order of elements in the array
     * no longer matches the order of elements in the original data
     * and so we need to search by the active dataKey + label rather than by index.
     *
     * The same happens if multiple graphical items are present in the chart
     * and each of them has its own data array. Those arrays get concatenated
     * and again the tooltip index no longer matches the original data.
     *
     * On the other hand the tooltipEventType 'item' should always search by index
     * because we get the index from interacting over the individual elements
     * which is always accurate, irrespective of the allowDuplicatedCategory setting.
     */
    tooltipEventType === 'axis') {
      tooltipPayload = findEntryInArray(sliced, tooltipAxisDataKey, activeLabel);
    } else {
      /*
       * This is a problem because it assumes that the index is pointing to the displayed data
       * which it isn't because the index is pointing to the tooltip ticks array.
       * The above approach (with findEntryInArray) is the correct one, but it only works
       * if the axis dataKey is defined explicitly, and if the data is an array of objects.
       */
      tooltipPayload = tooltipPayloadSearcher(sliced, activeIndex, computedData, finalNameKey);
    }
    if (Array.isArray(tooltipPayload)) {
      tooltipPayload.forEach(item => {
        var newSettings = combineTooltipPayload_objectSpread(combineTooltipPayload_objectSpread({}, settings), {}, {
          // @ts-expect-error we're assuming that item has name and unit properties
          name: item.name,
          // @ts-expect-error we're assuming that item has name and unit properties
          unit: item.unit,
          // color and fill are erased to keep 100% the identical behaviour to recharts 2.x - but there's nothing stopping us from returning them here. It's technically a breaking change.
          color: undefined,
          // color and fill are erased to keep 100% the identical behaviour to recharts 2.x - but there's nothing stopping us from returning them here. It's technically a breaking change.
          fill: undefined
        });
        agg.push(getTooltipEntry({
          tooltipEntrySettings: newSettings,
          // @ts-expect-error we're assuming that item has name and unit properties
          dataKey: item.dataKey,
          // @ts-expect-error we're assuming that item has name and unit properties
          payload: item.payload,
          // @ts-expect-error getValueByDataKey does not validate the output type
          value: getValueByDataKey(item.payload, item.dataKey),
          // @ts-expect-error we're assuming that item has name and unit properties
          name: item.name
        }));
      });
    } else {
      var _getValueByDataKey;
      // I am not quite sure why these two branches (Array vs Array of Arrays) have to behave differently - I imagine we should unify these. 3.x breaking change?
      agg.push(getTooltipEntry({
        tooltipEntrySettings: settings,
        dataKey: finalDataKey,
        payload: tooltipPayload,
        // @ts-expect-error getValueByDataKey does not validate the output type
        value: getValueByDataKey(tooltipPayload, finalDataKey),
        // @ts-expect-error getValueByDataKey does not validate the output type
        name: (_getValueByDataKey = getValueByDataKey(tooltipPayload, finalNameKey)) !== null && _getValueByDataKey !== void 0 ? _getValueByDataKey : settings === null || settings === void 0 ? void 0 : settings.name
      }));
    }
    return agg;
  }, init);
};
;// ./node_modules/recharts/es6/state/selectors/tooltipSelectors.js




























var selectTooltipAxisRealScaleType = createSelector([selectTooltipAxis, selectHasBar, selectChartName], combineRealScaleType);
var selectAllUnfilteredGraphicalItems = createSelector([state => state.graphicalItems.cartesianItems, state => state.graphicalItems.polarItems], (cartesianItems, polarItems) => [...cartesianItems, ...polarItems]);
var selectTooltipAxisPredicate = createSelector([selectTooltipAxisType, selectTooltipAxisId], itemAxisPredicate);
var selectAllGraphicalItemsSettings = createSelector([selectAllUnfilteredGraphicalItems, selectTooltipAxis, selectTooltipAxisPredicate], combineGraphicalItemsSettings, {
  memoizeOptions: {
    resultEqualityCheck: emptyArraysAreEqualCheck
  }
});
var selectAllStackedGraphicalItemsSettings = createSelector([selectAllGraphicalItemsSettings], graphicalItems => graphicalItems.filter(isStacked));
var selectTooltipGraphicalItemsData = createSelector([selectAllGraphicalItemsSettings], combineGraphicalItemsData, {
  memoizeOptions: {
    resultEqualityCheck: emptyArraysAreEqualCheck
  }
});

/**
 * Data for tooltip always use the data with indexes set by a Brush,
 * and never accept the isPanorama flag:
 * because Tooltip never displays inside the panorama anyway
 * so we don't need to worry what would happen there.
 */
var selectTooltipDisplayedData = createSelector([selectTooltipGraphicalItemsData, selectChartDataWithIndexes], combineDisplayedData);
var selectTooltipStackedData = createSelector([selectAllStackedGraphicalItemsSettings, selectChartDataWithIndexes, selectTooltipAxis], combineDisplayedStackedData);
var selectAllTooltipAppliedValues = createSelector([selectTooltipDisplayedData, selectTooltipAxis, selectAllGraphicalItemsSettings], combineAppliedValues);
var selectTooltipAxisDomainDefinition = createSelector([selectTooltipAxis], getDomainDefinition);
var selectTooltipDataOverflow = createSelector([selectTooltipAxis], axisSettings => axisSettings.allowDataOverflow);
var selectTooltipDomainFromUserPreferences = createSelector([selectTooltipAxisDomainDefinition, selectTooltipDataOverflow], numericalDomainSpecifiedWithoutRequiringData);
var selectAllStackedGraphicalItems = createSelector([selectAllGraphicalItemsSettings], graphicalItems => graphicalItems.filter(isStacked));
var selectTooltipStackGroups = createSelector([selectTooltipStackedData, selectAllStackedGraphicalItems, selectStackOffsetType, selectReverseStackOrder], combineStackGroups);
var selectTooltipDomainOfStackGroups = createSelector([selectTooltipStackGroups, selectChartDataWithIndexes, selectTooltipAxisType, selectTooltipDomainFromUserPreferences], combineDomainOfStackGroups);
var selectTooltipItemsSettingsExceptStacked = createSelector([selectAllGraphicalItemsSettings], filterGraphicalNotStackedItems);
var tooltipSelectors_selectDomainOfAllAppliedNumericalValuesIncludingErrorValues = createSelector([selectTooltipDisplayedData, selectTooltipAxis, selectTooltipItemsSettingsExceptStacked, selectAllErrorBarSettings, selectTooltipAxisType], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues, {
  memoizeOptions: {
    resultEqualityCheck: numberDomainEqualityCheck
  }
});
var selectReferenceDotsByTooltipAxis = createSelector([selectReferenceDots, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
var selectTooltipReferenceDotsDomain = createSelector([selectReferenceDotsByTooltipAxis, selectTooltipAxisType], combineDotsDomain);
var selectReferenceAreasByTooltipAxis = createSelector([selectReferenceAreas, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
var selectTooltipReferenceAreasDomain = createSelector([selectReferenceAreasByTooltipAxis, selectTooltipAxisType], combineAreasDomain);
var selectReferenceLinesByTooltipAxis = createSelector([selectReferenceLines, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
var selectTooltipReferenceLinesDomain = createSelector([selectReferenceLinesByTooltipAxis, selectTooltipAxisType], combineLinesDomain);
var selectTooltipReferenceElementsDomain = createSelector([selectTooltipReferenceDotsDomain, selectTooltipReferenceLinesDomain, selectTooltipReferenceAreasDomain], mergeDomains);
var selectTooltipNumericalDomain = createSelector([selectTooltipAxis, selectTooltipAxisDomainDefinition, selectTooltipDomainFromUserPreferences, selectTooltipDomainOfStackGroups, tooltipSelectors_selectDomainOfAllAppliedNumericalValuesIncludingErrorValues, selectTooltipReferenceElementsDomain, selectChartLayout, selectTooltipAxisType], combineNumericalDomain);
var selectTooltipAxisDomain = createSelector([selectTooltipAxis, selectChartLayout, selectTooltipDisplayedData, selectAllTooltipAppliedValues, selectStackOffsetType, selectTooltipAxisType, selectTooltipNumericalDomain], combineAxisDomain);
var selectTooltipNiceTicks = createSelector([selectTooltipAxisDomain, selectTooltipAxis, selectTooltipAxisRealScaleType], combineNiceTicks);
var selectTooltipAxisDomainIncludingNiceTicks = createSelector([selectTooltipAxis, selectTooltipAxisDomain, selectTooltipNiceTicks, selectTooltipAxisType], combineAxisDomainWithNiceTicks);
var selectTooltipAxisRange = state => {
  var axisType = selectTooltipAxisType(state);
  var axisId = selectTooltipAxisId(state);
  var isPanorama = false; // Tooltip never displays in panorama so this is safe to assume
  return selectAxisRange(state, axisType, axisId, isPanorama);
};
var selectTooltipAxisRangeWithReverse = createSelector([selectTooltipAxis, selectTooltipAxisRange], combineAxisRangeWithReverse);
var selectTooltipAxisScale = createSelector([selectTooltipAxis, selectTooltipAxisRealScaleType, selectTooltipAxisDomainIncludingNiceTicks, selectTooltipAxisRangeWithReverse], combineScaleFunction);
var selectTooltipDuplicateDomain = createSelector([selectChartLayout, selectAllTooltipAppliedValues, selectTooltipAxis, selectTooltipAxisType], combineDuplicateDomain);
var selectTooltipCategoricalDomain = createSelector([selectChartLayout, selectAllTooltipAppliedValues, selectTooltipAxis, selectTooltipAxisType], combineCategoricalDomain);
var combineTicksOfTooltipAxis = (layout, axis, realScaleType, scale, range, duplicateDomain, categoricalDomain, axisType) => {
  if (!axis) {
    return undefined;
  }
  var {
    type
  } = axis;
  var isCategorical = isCategoricalAxis(layout, axisType);
  if (!scale) {
    return undefined;
  }
  var offsetForBand = realScaleType === 'scaleBand' && scale.bandwidth ? scale.bandwidth() / 2 : 2;
  var offset = type === 'category' && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
  offset = axisType === 'angleAxis' && range != null && (range === null || range === void 0 ? void 0 : range.length) >= 2 ? mathSign(range[0] - range[1]) * 2 * offset : offset;

  // When axis is a categorical axis, but the type of axis is number or the scale of axis is not "auto"
  if (isCategorical && categoricalDomain) {
    return categoricalDomain.map((entry, index) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        value: entry,
        index,
        offset
      };
    }).filter(isNotNil);
  }

  // When axis has duplicated text, serial numbers are used to generate scale
  return scale.domain().map((entry, index) => {
    var scaled = scale.map(entry);
    if (!isWellBehavedNumber(scaled)) {
      return null;
    }
    return {
      coordinate: scaled + offset,
      // @ts-expect-error can't use Date as an index
      value: duplicateDomain ? duplicateDomain[entry] : entry,
      index,
      offset
    };
  }).filter(isNotNil);
};

/**
 * Of on four almost identical implementations of tick generation.
 * The four horsemen of tick generation are:
 * - {@link selectTooltipAxisTicks}
 * - {@link combineAxisTicks}
 * - {@link getTicksOfAxis}.
 * - {@link combineGraphicalItemTicks}
 */
var selectTooltipAxisTicks = createSelector([selectChartLayout, selectTooltipAxis, selectTooltipAxisRealScaleType, selectTooltipAxisScale, selectTooltipAxisRange, selectTooltipDuplicateDomain, selectTooltipCategoricalDomain, selectTooltipAxisType], combineTicksOfTooltipAxis);
var tooltipSelectors_selectTooltipEventType = createSelector([selectDefaultTooltipEventType, selectValidateTooltipEventTypes, selectTooltipSettings], (defaultTooltipEventType, validateTooltipEventType, settings) => combineTooltipEventType(settings.shared, defaultTooltipEventType, validateTooltipEventType));
var selectTooltipTrigger = state => state.tooltip.settings.trigger;
var selectDefaultIndex = state => state.tooltip.settings.defaultIndex;
var selectTooltipInteractionState = createSelector([selectTooltipState, tooltipSelectors_selectTooltipEventType, selectTooltipTrigger, selectDefaultIndex], combineTooltipInteractionState);
var selectActiveTooltipIndex = createSelector([selectTooltipInteractionState, selectTooltipDisplayedData, selectTooltipAxisDataKey, selectTooltipAxisDomain], combineActiveTooltipIndex);
var selectActiveLabel = createSelector([selectTooltipAxisTicks, selectActiveTooltipIndex], combineActiveLabel);
var selectActiveTooltipDataKey = createSelector([selectTooltipInteractionState], tooltipInteraction => {
  if (!tooltipInteraction) {
    return undefined;
  }
  return tooltipInteraction.dataKey;
});
var selectActiveTooltipGraphicalItemId = createSelector([selectTooltipInteractionState], tooltipInteraction => {
  if (!tooltipInteraction) {
    return undefined;
  }
  return tooltipInteraction.graphicalItemId;
});
var selectTooltipPayloadConfigurations = createSelector([selectTooltipState, tooltipSelectors_selectTooltipEventType, selectTooltipTrigger, selectDefaultIndex], combineTooltipPayloadConfigurations);
var selectTooltipCoordinateForDefaultIndex = createSelector([selectChartWidth, selectChartHeight, selectChartLayout, selectChartOffsetInternal, selectTooltipAxisTicks, selectDefaultIndex, selectTooltipPayloadConfigurations], combineCoordinateForDefaultIndex);
var selectActiveTooltipCoordinate = createSelector([selectTooltipInteractionState, selectTooltipCoordinateForDefaultIndex], (tooltipInteractionState, defaultIndexCoordinate) => {
  if (tooltipInteractionState !== null && tooltipInteractionState !== void 0 && tooltipInteractionState.coordinate) {
    return tooltipInteractionState.coordinate;
  }
  return defaultIndexCoordinate;
});
var selectIsTooltipActive = createSelector([selectTooltipInteractionState], tooltipInteractionState => {
  var _tooltipInteractionSt;
  return (_tooltipInteractionSt = tooltipInteractionState === null || tooltipInteractionState === void 0 ? void 0 : tooltipInteractionState.active) !== null && _tooltipInteractionSt !== void 0 ? _tooltipInteractionSt : false;
});
var selectActiveTooltipPayload = createSelector([selectTooltipPayloadConfigurations, selectActiveTooltipIndex, selectChartDataWithIndexes, selectTooltipAxisDataKey, selectActiveLabel, selectTooltipPayloadSearcher, tooltipSelectors_selectTooltipEventType], combineTooltipPayload);
var selectActiveTooltipDataPoints = createSelector([selectActiveTooltipPayload], payload => {
  if (payload == null) {
    return undefined;
  }
  var dataPoints = payload.map(p => p.payload).filter(p => p != null);
  return Array.from(new Set(dataPoints));
});
;// ./node_modules/recharts/es6/util/getActiveCoordinate.js
function getActiveCoordinate_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function getActiveCoordinate_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? getActiveCoordinate_ownKeys(Object(t), !0).forEach(function (r) { getActiveCoordinate_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : getActiveCoordinate_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function getActiveCoordinate_defineProperty(e, r, t) { return (r = getActiveCoordinate_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function getActiveCoordinate_toPropertyKey(t) { var i = getActiveCoordinate_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function getActiveCoordinate_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


var getActiveCartesianCoordinate = (layout, tooltipTicks, activeIndex, pointer) => {
  var entry = tooltipTicks.find(tick => tick && tick.index === activeIndex);
  if (entry) {
    if (layout === 'horizontal') {
      return {
        x: entry.coordinate,
        y: pointer.chartY
      };
    }
    if (layout === 'vertical') {
      return {
        x: pointer.chartX,
        y: entry.coordinate
      };
    }
  }
  return {
    x: 0,
    y: 0
  };
};

/**
 * Get the active coordinate in polar coordinate system.
 * Internally we only really use x and y, but this returned object is part of public API
 * (because it goes straight to the tooltip content) so we keep all the other properties
 * for backwards compatibility.
 *
 * @param layout - The polar layout type ('centric' or 'radial').
 * @param tooltipTicks - Array of tick items used for tooltips.
 * @param activeIndex - The index of the active tick.
 * @param rangeObj - The range object containing polar chart properties.
 * @returns The active coordinate object with polar properties.
 */
var getActivePolarCoordinate = (layout, tooltipTicks, activeIndex, rangeObj) => {
  var entry = tooltipTicks.find(tick => tick && tick.index === activeIndex);
  if (entry) {
    if (layout === 'centric') {
      var _angle = entry.coordinate;
      var {
        radius: _radius
      } = rangeObj;
      return getActiveCoordinate_objectSpread(getActiveCoordinate_objectSpread(getActiveCoordinate_objectSpread({}, rangeObj), polarToCartesian(rangeObj.cx, rangeObj.cy, _radius, _angle)), {}, {
        angle: _angle,
        radius: _radius
      });
    }
    var radius = entry.coordinate;
    var {
      angle
    } = rangeObj;
    return getActiveCoordinate_objectSpread(getActiveCoordinate_objectSpread(getActiveCoordinate_objectSpread({}, rangeObj), polarToCartesian(rangeObj.cx, rangeObj.cy, radius, angle)), {}, {
      angle,
      radius
    });
  }
  return {
    angle: 0,
    clockWise: false,
    cx: 0,
    cy: 0,
    endAngle: 0,
    innerRadius: 0,
    outerRadius: 0,
    radius: 0,
    startAngle: 0,
    x: 0,
    y: 0
  };
};
function isInCartesianRange(pointer, offset) {
  var {
    chartX: x,
    chartY: y
  } = pointer;
  return x >= offset.left && x <= offset.left + offset.width && y >= offset.top && y <= offset.top + offset.height;
}
var calculateActiveTickIndex = (coordinate, ticks, unsortedTicks, axisType, range) => {
  var _ticks$length;
  var len = (_ticks$length = ticks === null || ticks === void 0 ? void 0 : ticks.length) !== null && _ticks$length !== void 0 ? _ticks$length : 0;

  // if there are 1 or fewer ticks or if there is no coordinate then the active tick is at index 0
  if (len <= 1 || coordinate == null) {
    return 0;
  }
  if (axisType === 'angleAxis' && range != null && Math.abs(Math.abs(range[1] - range[0]) - 360) <= 1e-6) {
    // ticks are distributed in a circle
    for (var i = 0; i < len; i++) {
      var _unsortedTicks, _unsortedTicks2, _unsortedTicks$i, _unsortedTicks$, _unsortedTicks3;
      var before = i > 0 ? (_unsortedTicks = unsortedTicks[i - 1]) === null || _unsortedTicks === void 0 ? void 0 : _unsortedTicks.coordinate : (_unsortedTicks2 = unsortedTicks[len - 1]) === null || _unsortedTicks2 === void 0 ? void 0 : _unsortedTicks2.coordinate;
      var cur = (_unsortedTicks$i = unsortedTicks[i]) === null || _unsortedTicks$i === void 0 ? void 0 : _unsortedTicks$i.coordinate;
      var after = i >= len - 1 ? (_unsortedTicks$ = unsortedTicks[0]) === null || _unsortedTicks$ === void 0 ? void 0 : _unsortedTicks$.coordinate : (_unsortedTicks3 = unsortedTicks[i + 1]) === null || _unsortedTicks3 === void 0 ? void 0 : _unsortedTicks3.coordinate;
      var sameDirectionCoord = void 0;
      if (before == null || cur == null || after == null) {
        continue;
      }
      if (mathSign(cur - before) !== mathSign(after - cur)) {
        var diffInterval = [];
        if (mathSign(after - cur) === mathSign(range[1] - range[0])) {
          sameDirectionCoord = after;
          var curInRange = cur + range[1] - range[0];
          diffInterval[0] = Math.min(curInRange, (curInRange + before) / 2);
          diffInterval[1] = Math.max(curInRange, (curInRange + before) / 2);
        } else {
          sameDirectionCoord = before;
          var afterInRange = after + range[1] - range[0];
          diffInterval[0] = Math.min(cur, (afterInRange + cur) / 2);
          diffInterval[1] = Math.max(cur, (afterInRange + cur) / 2);
        }
        var sameInterval = [Math.min(cur, (sameDirectionCoord + cur) / 2), Math.max(cur, (sameDirectionCoord + cur) / 2)];
        if (coordinate > sameInterval[0] && coordinate <= sameInterval[1] || coordinate >= diffInterval[0] && coordinate <= diffInterval[1]) {
          var _unsortedTicks$i2;
          return (_unsortedTicks$i2 = unsortedTicks[i]) === null || _unsortedTicks$i2 === void 0 ? void 0 : _unsortedTicks$i2.index;
        }
      } else {
        var minValue = Math.min(before, after);
        var maxValue = Math.max(before, after);
        if (coordinate > (minValue + cur) / 2 && coordinate <= (maxValue + cur) / 2) {
          var _unsortedTicks$i3;
          return (_unsortedTicks$i3 = unsortedTicks[i]) === null || _unsortedTicks$i3 === void 0 ? void 0 : _unsortedTicks$i3.index;
        }
      }
    }
  } else if (ticks) {
    // ticks are distributed in a single direction
    for (var _i = 0; _i < len; _i++) {
      var curr = ticks[_i];
      if (curr == null) {
        continue;
      }
      var next = ticks[_i + 1];
      var prev = ticks[_i - 1];
      if (_i === 0 && next != null && coordinate <= (curr.coordinate + next.coordinate) / 2) {
        return curr.index;
      }
      if (_i === len - 1 && prev != null && coordinate > (curr.coordinate + prev.coordinate) / 2) {
        return curr.index;
      }
      if (_i > 0 && _i < len - 1 && prev != null && next != null && coordinate > (curr.coordinate + prev.coordinate) / 2 && coordinate <= (curr.coordinate + next.coordinate) / 2) {
        return curr.index;
      }
    }
  }
  return -1;
};
;// ./node_modules/recharts/es6/state/selectors/selectors.js





















var useChartName = () => {
  return useAppSelector(selectChartName);
};
var pickTooltipEventType = (_state, tooltipEventType) => tooltipEventType;
var pickTrigger = (_state, _tooltipEventType, trigger) => trigger;
var pickDefaultIndex = (_state, _tooltipEventType, _trigger, defaultIndex) => defaultIndex;
var selectOrderedTooltipTicks = createSelector(selectTooltipAxisTicks, ticks => sortBy_default()(ticks, o => o.coordinate));
var selectors_selectTooltipInteractionState = createSelector([selectTooltipState, pickTooltipEventType, pickTrigger, pickDefaultIndex], combineTooltipInteractionState);
var selectActiveIndex = createSelector([selectors_selectTooltipInteractionState, selectTooltipDisplayedData, selectTooltipAxisDataKey, selectTooltipAxisDomain], combineActiveTooltipIndex);
var selectTooltipDataKey = (state, tooltipEventType, trigger) => {
  if (tooltipEventType == null) {
    return undefined;
  }
  var tooltipState = selectTooltipState(state);
  if (tooltipEventType === 'axis') {
    if (trigger === 'hover') {
      return tooltipState.axisInteraction.hover.dataKey;
    }
    return tooltipState.axisInteraction.click.dataKey;
  }
  if (trigger === 'hover') {
    return tooltipState.itemInteraction.hover.dataKey;
  }
  return tooltipState.itemInteraction.click.dataKey;
};
var selectors_selectTooltipPayloadConfigurations = createSelector([selectTooltipState, pickTooltipEventType, pickTrigger, pickDefaultIndex], combineTooltipPayloadConfigurations);
var selectCoordinateForDefaultIndex = createSelector([selectChartWidth, selectChartHeight, selectChartLayout, selectChartOffsetInternal, selectTooltipAxisTicks, pickDefaultIndex, selectors_selectTooltipPayloadConfigurations], combineCoordinateForDefaultIndex);
var selectActiveCoordinate = createSelector([selectors_selectTooltipInteractionState, selectCoordinateForDefaultIndex], (tooltipInteractionState, defaultIndexCoordinate) => {
  var _tooltipInteractionSt;
  return (_tooltipInteractionSt = tooltipInteractionState.coordinate) !== null && _tooltipInteractionSt !== void 0 ? _tooltipInteractionSt : defaultIndexCoordinate;
});
var selectors_selectActiveLabel = createSelector([selectTooltipAxisTicks, selectActiveIndex], combineActiveLabel);
var selectTooltipPayload = createSelector([selectors_selectTooltipPayloadConfigurations, selectActiveIndex, selectChartDataWithIndexes, selectTooltipAxisDataKey, selectors_selectActiveLabel, selectTooltipPayloadSearcher, pickTooltipEventType], combineTooltipPayload);
var selectors_selectIsTooltipActive = createSelector([selectors_selectTooltipInteractionState, selectActiveIndex], (tooltipInteractionState, activeIndex) => {
  return {
    isActive: tooltipInteractionState.active && activeIndex != null,
    activeIndex
  };
});
var combineActiveCartesianProps = (chartEvent, layout, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset) => {
  if (!chartEvent || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks) {
    return undefined;
  }
  if (!isInCartesianRange(chartEvent, offset)) {
    return undefined;
  }
  var pos = calculateCartesianTooltipPos(chartEvent, layout);
  var activeIndex = calculateActiveTickIndex(pos, orderedTooltipTicks, tooltipTicks, tooltipAxisType, tooltipAxisRange);
  var activeCoordinate = getActiveCartesianCoordinate(layout, tooltipTicks, activeIndex, chartEvent);
  return {
    activeIndex: String(activeIndex),
    activeCoordinate
  };
};
var combineActivePolarProps = (chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks) => {
  if (!chartEvent || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks || !polarViewBox) {
    return undefined;
  }
  var rangeObj = inRangeOfSector(chartEvent, polarViewBox);
  if (!rangeObj) {
    return undefined;
  }
  var pos = calculatePolarTooltipPos(rangeObj, layout);
  var activeIndex = calculateActiveTickIndex(pos, orderedTooltipTicks, tooltipTicks, tooltipAxisType, tooltipAxisRange);
  var activeCoordinate = getActivePolarCoordinate(layout, tooltipTicks, activeIndex, rangeObj);
  return {
    activeIndex: String(activeIndex),
    activeCoordinate
  };
};
var combineActiveProps = (chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset) => {
  if (!chartEvent || !layout || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks) {
    return undefined;
  }
  if (layout === 'horizontal' || layout === 'vertical') {
    return combineActiveCartesianProps(chartEvent, layout, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset);
  }
  return combineActivePolarProps(chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks);
};
;// ./node_modules/recharts/es6/state/selectors/selectActivePropsFromChartPointer.js







var pickChartPointer = (_state, chartPointer) => chartPointer;
var selectActivePropsFromChartPointer = createSelector([pickChartPointer, selectChartLayout, selectPolarViewBox, selectTooltipAxisType, selectTooltipAxisRangeWithReverse, selectTooltipAxisTicks, selectOrderedTooltipTicks, selectChartOffsetInternal], combineActiveProps);
;// ./node_modules/recharts/es6/util/getChartPointer.js
/**
 * Computes the chart coordinates from the mouse event.
 *
 * The coordinates are relative to the top-left corner of the chart,
 * where the top-left corner of the chart is (0, 0).
 * Moving right, the x-coordinate increases, and moving down, the y-coordinate increases.
 *
 * The coordinates are rounded to the nearest integer and are including a CSS transform scale.
 * So a chart that's scaled will return the same coordinates as a chart that's not scaled.
 *
 * @param event The mouse event from React event handlers
 * @return chartPointer The chart coordinates relative to the top-left corner of the chart
 */
var getChartPointer = event => {
  var rect = event.currentTarget.getBoundingClientRect();
  var scaleX = rect.width / event.currentTarget.offsetWidth;
  var scaleY = rect.height / event.currentTarget.offsetHeight;
  return {
    /*
     * Here it's important to use:
     * - event.clientX and event.clientY to get the mouse position relative to the viewport, including scroll.
     * - pageX and pageY are not used because they are relative to the whole document, and ignore scroll.
     * - rect.left and rect.top are used to get the position of the chart relative to the viewport.
     * - offsetX and offsetY are not used because they are relative to the offset parent
     *  which may or may not be the same as the clientX and clientY, depending on the position of the chart in the DOM
     *  and surrounding element styles. CSS position: relative, absolute, fixed, will change the offset parent.
     * - scaleX and scaleY are necessary for when the chart element is scaled using CSS `transform: scale(N)`.
     */
    chartX: Math.round((event.clientX - rect.left) / scaleX),
    chartY: Math.round((event.clientY - rect.top) / scaleY)
  };
};
;// ./node_modules/recharts/es6/state/mouseEventsMiddleware.js





var mouseClickAction = createAction('mouseClick');
var mouseClickMiddleware = createListenerMiddleware();

// TODO: there's a bug here when you click the chart the activeIndex resets to zero
mouseClickMiddleware.startListening({
  actionCreator: mouseClickAction,
  effect: (action, listenerApi) => {
    var mousePointer = action.payload;
    var activeProps = selectActivePropsFromChartPointer(listenerApi.getState(), getChartPointer(mousePointer));
    if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
      listenerApi.dispatch(setMouseClickAxisIndex({
        activeIndex: activeProps.activeIndex,
        activeDataKey: undefined,
        activeCoordinate: activeProps.activeCoordinate
      }));
    }
  }
});
var mouseMoveAction = createAction('mouseMove');
var mouseMoveMiddleware = createListenerMiddleware();

/*
 * This single rafId is safe because:
 * 1. Each chart has its own Redux store instance with its own middleware
 * 2. mouseMoveAction only fires from one DOM element (the chart wrapper)
 * 3. Rapid mousemove events from the same element SHOULD debounce - we only care about the latest position
 * This is different from externalEventsMiddleware which handles multiple event types
 * (click, mouseenter, mouseleave, etc.) that should NOT cancel each other.
 */
var rafId = null;
mouseMoveMiddleware.startListening({
  actionCreator: mouseMoveAction,
  effect: (action, listenerApi) => {
    var mousePointer = action.payload;

    // Cancel any pending animation frame
    if (rafId !== null) {
      cancelAnimationFrame(rafId);
    }
    var chartPointer = getChartPointer(mousePointer);

    // Schedule the dispatch for the next animation frame
    rafId = requestAnimationFrame(() => {
      var state = listenerApi.getState();
      var tooltipEventType = selectTooltipEventType(state, state.tooltip.settings.shared);
      // this functionality only applies to charts that have axes
      if (tooltipEventType === 'axis') {
        var activeProps = selectActivePropsFromChartPointer(state, chartPointer);
        if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
          listenerApi.dispatch(setMouseOverAxisIndex({
            activeIndex: activeProps.activeIndex,
            activeDataKey: undefined,
            activeCoordinate: activeProps.activeCoordinate
          }));
        } else {
          // this is needed to clear tooltip state when the mouse moves out of the inRange (svg - offset) function, but not yet out of the svg
          listenerApi.dispatch(mouseLeaveChart());
        }
      }
      rafId = null;
    });
  }
});
;// ./node_modules/recharts/es6/state/reduxDevtoolsJsonStringifyReplacer.js
function reduxDevtoolsJsonStringifyReplacer(key, value) {
  if (value instanceof HTMLElement) {
    return "HTMLElement <".concat(value.tagName, " class=\"").concat(value.className, "\">");
  }
  if (value === window) {
    return 'global.window';
  }
  if (key === 'children' && typeof value === 'object' && value !== null) {
    return '<<CHILDREN>>';
  }
  return value;
}
;// ./node_modules/recharts/es6/state/cartesianAxisSlice.js
function cartesianAxisSlice_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function cartesianAxisSlice_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? cartesianAxisSlice_ownKeys(Object(t), !0).forEach(function (r) { cartesianAxisSlice_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : cartesianAxisSlice_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function cartesianAxisSlice_defineProperty(e, r, t) { return (r = cartesianAxisSlice_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function cartesianAxisSlice_toPropertyKey(t) { var i = cartesianAxisSlice_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function cartesianAxisSlice_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }



/**
 * @inline
 */

var defaultAxisId = 0;

/**
 * Properties shared in X, Y, and Z axes.
 * User defined axis settings, coming from props.
 */

/**
 * These are the external props, visible for users as they set them using our public API.
 * There is all sorts of internal computed things based on these, but they will come through selectors.
 *
 * Properties shared between X and Y axes
 */

/**
 * Z axis is special because it's never displayed. It controls the size of Scatter dots,
 * but it never displays ticks anywhere.
 */

var cartesianAxisSlice_initialState = {
  xAxis: {},
  yAxis: {},
  zAxis: {}
};

/**
 * This is the slice where each individual Axis element pushes its own configuration.
 * Prefer to use this one instead of axisSlice.
 */
var cartesianAxisSlice = createSlice({
  name: 'cartesianAxis',
  initialState: cartesianAxisSlice_initialState,
  reducers: {
    addXAxis: {
      reducer(state, action) {
        state.xAxis[action.payload.id] = immer_castDraft(action.payload);
      },
      prepare: prepareAutoBatched()
    },
    replaceXAxis: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        if (state.xAxis[prev.id] !== undefined) {
          if (prev.id !== next.id) {
            delete state.xAxis[prev.id];
          }
          state.xAxis[next.id] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeXAxis: {
      reducer(state, action) {
        delete state.xAxis[action.payload.id];
      },
      prepare: prepareAutoBatched()
    },
    addYAxis: {
      reducer(state, action) {
        state.yAxis[action.payload.id] = immer_castDraft(action.payload);
      },
      prepare: prepareAutoBatched()
    },
    replaceYAxis: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        if (state.yAxis[prev.id] !== undefined) {
          if (prev.id !== next.id) {
            delete state.yAxis[prev.id];
          }
          state.yAxis[next.id] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeYAxis: {
      reducer(state, action) {
        delete state.yAxis[action.payload.id];
      },
      prepare: prepareAutoBatched()
    },
    addZAxis: {
      reducer(state, action) {
        state.zAxis[action.payload.id] = immer_castDraft(action.payload);
      },
      prepare: prepareAutoBatched()
    },
    replaceZAxis: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        if (state.zAxis[prev.id] !== undefined) {
          if (prev.id !== next.id) {
            delete state.zAxis[prev.id];
          }
          state.zAxis[next.id] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeZAxis: {
      reducer(state, action) {
        delete state.zAxis[action.payload.id];
      },
      prepare: prepareAutoBatched()
    },
    updateYAxisWidth(state, action) {
      var {
        id,
        width
      } = action.payload;
      var axis = state.yAxis[id];
      if (axis) {
        var _history$;
        var history = axis.widthHistory || [];
        // An oscillation is detected when the new width is the same as the width before the last one.
        // This is a simple A -> B -> A pattern. If the next width is B, and the difference is less than 1 pixel, we ignore it.
        if (history.length === 3 && history[0] === history[2] && width === history[1] && width !== axis.width && Math.abs(width - ((_history$ = history[0]) !== null && _history$ !== void 0 ? _history$ : 0)) <= 1) {
          return;
        }
        var newHistory = [...history, width].slice(-3);
        state.yAxis[id] = cartesianAxisSlice_objectSpread(cartesianAxisSlice_objectSpread({}, axis), {}, {
          width,
          widthHistory: newHistory
        });
      }
    }
  }
});
var {
  addXAxis,
  replaceXAxis,
  removeXAxis,
  addYAxis,
  replaceYAxis,
  removeYAxis,
  addZAxis,
  replaceZAxis,
  removeZAxis,
  updateYAxisWidth
} = cartesianAxisSlice.actions;
var cartesianAxisReducer = cartesianAxisSlice.reducer;
;// ./node_modules/recharts/es6/state/graphicalItemsSlice.js



/**
 * Unique ID of the graphical item.
 * This is used to identify the graphical item in the state and in the React tree.
 * This is required for every graphical item - it's either provided by the user or generated automatically.
 */

var graphicalItemsSlice_initialState = {
  cartesianItems: [],
  polarItems: []
};
var graphicalItemsSlice = createSlice({
  name: 'graphicalItems',
  initialState: graphicalItemsSlice_initialState,
  reducers: {
    addCartesianGraphicalItem: {
      reducer(state, action) {
        state.cartesianItems.push(immer_castDraft(action.payload));
      },
      prepare: prepareAutoBatched()
    },
    replaceCartesianGraphicalItem: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        var index = current(state).cartesianItems.indexOf(immer_castDraft(prev));
        if (index > -1) {
          state.cartesianItems[index] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeCartesianGraphicalItem: {
      reducer(state, action) {
        var index = current(state).cartesianItems.indexOf(immer_castDraft(action.payload));
        if (index > -1) {
          state.cartesianItems.splice(index, 1);
        }
      },
      prepare: prepareAutoBatched()
    },
    addPolarGraphicalItem: {
      reducer(state, action) {
        state.polarItems.push(immer_castDraft(action.payload));
      },
      prepare: prepareAutoBatched()
    },
    removePolarGraphicalItem: {
      reducer(state, action) {
        var index = current(state).polarItems.indexOf(immer_castDraft(action.payload));
        if (index > -1) {
          state.polarItems.splice(index, 1);
        }
      },
      prepare: prepareAutoBatched()
    }
  }
});
var {
  addCartesianGraphicalItem,
  replaceCartesianGraphicalItem,
  removeCartesianGraphicalItem,
  addPolarGraphicalItem,
  removePolarGraphicalItem
} = graphicalItemsSlice.actions;
var graphicalItemsReducer = graphicalItemsSlice.reducer;
;// ./node_modules/recharts/es6/state/referenceElementsSlice.js


var referenceElementsSlice_initialState = {
  dots: [],
  areas: [],
  lines: []
};
var referenceElementsSlice = createSlice({
  name: 'referenceElements',
  initialState: referenceElementsSlice_initialState,
  reducers: {
    addDot: (state, action) => {
      state.dots.push(action.payload);
    },
    removeDot: (state, action) => {
      var index = current(state).dots.findIndex(dot => dot === action.payload);
      if (index !== -1) {
        state.dots.splice(index, 1);
      }
    },
    addArea: (state, action) => {
      state.areas.push(action.payload);
    },
    removeArea: (state, action) => {
      var index = current(state).areas.findIndex(area => area === action.payload);
      if (index !== -1) {
        state.areas.splice(index, 1);
      }
    },
    addLine: (state, action) => {
      state.lines.push(immer_castDraft(action.payload));
    },
    removeLine: (state, action) => {
      var index = current(state).lines.findIndex(line => line === action.payload);
      if (index !== -1) {
        state.lines.splice(index, 1);
      }
    }
  }
});
var {
  addDot,
  removeDot,
  addArea,
  removeArea,
  addLine,
  removeLine
} = referenceElementsSlice.actions;
var referenceElementsReducer = referenceElementsSlice.reducer;
;// ./node_modules/recharts/es6/state/brushSlice.js


/**
 * From all Brush properties, only height has a default value and will always be defined.
 * Other properties are nullable and will be computed from offsets and margins if they are not set.
 */

var brushSlice_initialState = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  padding: {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  }
};
var brushSlice = createSlice({
  name: 'brush',
  initialState: brushSlice_initialState,
  reducers: {
    setBrushSettings(_state, action) {
      if (action.payload == null) {
        return brushSlice_initialState;
      }
      return action.payload;
    }
  }
});
var {
  setBrushSettings
} = brushSlice.actions;
var brushReducer = brushSlice.reducer;
;// ./node_modules/recharts/es6/state/legendSlice.js



/**
 * The properties inside this state update independently of each other and quite often.
 * When selecting, never select the whole state because you are going to get
 * unnecessary re-renders. Select only the properties you need.
 *
 * This is why this state type is not exported - don't use it directly.
 */

var legendSlice_initialState = {
  settings: {
    layout: 'horizontal',
    align: 'center',
    verticalAlign: 'middle',
    itemSorter: 'value'
  },
  size: {
    width: 0,
    height: 0
  },
  payload: []
};
var legendSlice = createSlice({
  name: 'legend',
  initialState: legendSlice_initialState,
  reducers: {
    setLegendSize(state, action) {
      state.size.width = action.payload.width;
      state.size.height = action.payload.height;
    },
    setLegendSettings(state, action) {
      state.settings.align = action.payload.align;
      state.settings.layout = action.payload.layout;
      state.settings.verticalAlign = action.payload.verticalAlign;
      state.settings.itemSorter = action.payload.itemSorter;
    },
    addLegendPayload: {
      reducer(state, action) {
        state.payload.push(immer_castDraft(action.payload));
      },
      prepare: prepareAutoBatched()
    },
    replaceLegendPayload: {
      reducer(state, action) {
        var {
          prev,
          next
        } = action.payload;
        var index = current(state).payload.indexOf(immer_castDraft(prev));
        if (index > -1) {
          state.payload[index] = immer_castDraft(next);
        }
      },
      prepare: prepareAutoBatched()
    },
    removeLegendPayload: {
      reducer(state, action) {
        var index = current(state).payload.indexOf(immer_castDraft(action.payload));
        if (index > -1) {
          state.payload.splice(index, 1);
        }
      },
      prepare: prepareAutoBatched()
    }
  }
});
var {
  setLegendSize,
  setLegendSettings,
  addLegendPayload,
  replaceLegendPayload,
  removeLegendPayload
} = legendSlice.actions;
var legendReducer = legendSlice.reducer;
;// ./node_modules/recharts/es6/state/rootPropsSlice.js


/**
 * These are chart options that users can choose - which means they can also
 * choose to change them which should trigger a re-render.
 */

var rootPropsSlice_initialState = {
  accessibilityLayer: true,
  barCategoryGap: '10%',
  barGap: 4,
  barSize: undefined,
  className: undefined,
  maxBarSize: undefined,
  stackOffset: 'none',
  syncId: undefined,
  syncMethod: 'index',
  baseValue: undefined,
  reverseStackOrder: false
};
var rootPropsSlice = createSlice({
  name: 'rootProps',
  initialState: rootPropsSlice_initialState,
  reducers: {
    updateOptions: (state, action) => {
      var _action$payload$barGa;
      state.accessibilityLayer = action.payload.accessibilityLayer;
      state.barCategoryGap = action.payload.barCategoryGap;
      state.barGap = (_action$payload$barGa = action.payload.barGap) !== null && _action$payload$barGa !== void 0 ? _action$payload$barGa : rootPropsSlice_initialState.barGap;
      state.barSize = action.payload.barSize;
      state.maxBarSize = action.payload.maxBarSize;
      state.stackOffset = action.payload.stackOffset;
      state.syncId = action.payload.syncId;
      state.syncMethod = action.payload.syncMethod;
      state.className = action.payload.className;
      state.baseValue = action.payload.baseValue;
      state.reverseStackOrder = action.payload.reverseStackOrder;
    }
  }
});
var rootPropsReducer = rootPropsSlice.reducer;
var {
  updateOptions
} = rootPropsSlice.actions;
;// ./node_modules/recharts/es6/state/polarAxisSlice.js


var polarAxisSlice_initialState = {
  radiusAxis: {},
  angleAxis: {}
};
var polarAxisSlice = createSlice({
  name: 'polarAxis',
  initialState: polarAxisSlice_initialState,
  reducers: {
    addRadiusAxis(state, action) {
      state.radiusAxis[action.payload.id] = immer_castDraft(action.payload);
    },
    removeRadiusAxis(state, action) {
      delete state.radiusAxis[action.payload.id];
    },
    addAngleAxis(state, action) {
      state.angleAxis[action.payload.id] = immer_castDraft(action.payload);
    },
    removeAngleAxis(state, action) {
      delete state.angleAxis[action.payload.id];
    }
  }
});
var {
  addRadiusAxis,
  removeRadiusAxis,
  addAngleAxis,
  removeAngleAxis
} = polarAxisSlice.actions;
var polarAxisReducer = polarAxisSlice.reducer;
;// ./node_modules/recharts/es6/state/polarOptionsSlice.js

var polarOptionsSlice_initialState = null;
var reducers = {
  updatePolarOptions: (_state, action) => {
    return action.payload;
  }
};
var polarOptionsSlice = createSlice({
  name: 'polarOptions',
  initialState: polarOptionsSlice_initialState,
  reducers
});
var {
  updatePolarOptions
} = polarOptionsSlice.actions;
var polarOptionsReducer = polarOptionsSlice.reducer;
;// ./node_modules/recharts/es6/state/keyboardEventsMiddleware.js






var keyDownAction = createAction('keyDown');
var focusAction = createAction('focus');
var keyboardEventsMiddleware = createListenerMiddleware();
keyboardEventsMiddleware.startListening({
  actionCreator: keyDownAction,
  effect: (action, listenerApi) => {
    var state = listenerApi.getState();
    var accessibilityLayerIsActive = state.rootProps.accessibilityLayer !== false;
    if (!accessibilityLayerIsActive) {
      return;
    }
    var {
      keyboardInteraction
    } = state.tooltip;
    var key = action.payload;
    if (key !== 'ArrowRight' && key !== 'ArrowLeft' && key !== 'Enter') {
      return;
    }

    // TODO this is lacking index for charts that do not support numeric indexes
    var resolvedIndex = combineActiveTooltipIndex(keyboardInteraction, selectTooltipDisplayedData(state), selectTooltipAxisDataKey(state), selectTooltipAxisDomain(state));
    var currentIndex = resolvedIndex == null ? -1 : Number(resolvedIndex);
    if (!Number.isFinite(currentIndex) || currentIndex < 0) {
      return;
    }
    var tooltipTicks = selectTooltipAxisTicks(state);
    if (key === 'Enter') {
      var _coordinate = selectCoordinateForDefaultIndex(state, 'axis', 'hover', String(keyboardInteraction.index));
      listenerApi.dispatch(setKeyboardInteraction({
        active: !keyboardInteraction.active,
        activeIndex: keyboardInteraction.index,
        activeCoordinate: _coordinate
      }));
      return;
    }
    var direction = selectChartDirection(state);
    var directionMultiplier = direction === 'left-to-right' ? 1 : -1;
    var movement = key === 'ArrowRight' ? 1 : -1;
    var nextIndex = currentIndex + movement * directionMultiplier;
    if (tooltipTicks == null || nextIndex >= tooltipTicks.length || nextIndex < 0) {
      return;
    }
    var coordinate = selectCoordinateForDefaultIndex(state, 'axis', 'hover', String(nextIndex));
    listenerApi.dispatch(setKeyboardInteraction({
      active: true,
      activeIndex: nextIndex.toString(),
      activeCoordinate: coordinate
    }));
  }
});
keyboardEventsMiddleware.startListening({
  actionCreator: focusAction,
  effect: (_action, listenerApi) => {
    var state = listenerApi.getState();
    var accessibilityLayerIsActive = state.rootProps.accessibilityLayer !== false;
    if (!accessibilityLayerIsActive) {
      return;
    }
    var {
      keyboardInteraction
    } = state.tooltip;
    if (keyboardInteraction.active) {
      return;
    }
    if (keyboardInteraction.index == null) {
      var nextIndex = '0';
      var coordinate = selectCoordinateForDefaultIndex(state, 'axis', 'hover', String(nextIndex));
      listenerApi.dispatch(setKeyboardInteraction({
        active: true,
        activeIndex: nextIndex,
        activeCoordinate: coordinate
      }));
    }
  }
});
;// ./node_modules/recharts/es6/state/externalEventsMiddleware.js


var externalEventAction = createAction('externalEvent');
var externalEventsMiddleware = createListenerMiddleware();

/*
 * We need a Map keyed by event type because this middleware handles MULTIPLE different event types
 * (click, mouseenter, mouseleave, mousedown, mouseup, contextmenu, dblclick, touchstart, touchmove, touchend)
 * from the same DOM element. Different event types should NOT cancel each other's animation frames.
 * For example, a click event and a mousemove event can happen in quick succession and both should be processed.
 * This is different from mouseMoveMiddleware which only handles one event type and uses a single rafId.
 */
var rafIdMap = new Map();
externalEventsMiddleware.startListening({
  actionCreator: externalEventAction,
  effect: (action, listenerApi) => {
    var {
      handler,
      reactEvent
    } = action.payload;
    if (handler == null) {
      return;
    }
    reactEvent.persist();
    var eventType = reactEvent.type;

    // Cancel any pending animation frame for this event type
    var existingRafId = rafIdMap.get(eventType);
    if (existingRafId !== undefined) {
      cancelAnimationFrame(existingRafId);
    }
    var rafId = requestAnimationFrame(() => {
      try {
        /*
         * Here it is important that we get the latest state inside the animation frame callback,
         * not from the outer scope, because there may have been other actions dispatched
         * between the time the event was fired and the animation frame callback is executed.
         * One of those actions is the one that actually sets the active tooltip state!
         */
        var state = listenerApi.getState();
        var nextState = {
          activeCoordinate: selectActiveTooltipCoordinate(state),
          activeDataKey: selectActiveTooltipDataKey(state),
          activeIndex: selectActiveTooltipIndex(state),
          activeLabel: selectActiveLabel(state),
          activeTooltipIndex: selectActiveTooltipIndex(state),
          isTooltipActive: selectIsTooltipActive(state)
        };
        handler(nextState, reactEvent);
      } finally {
        rafIdMap.delete(eventType);
      }
    });
    rafIdMap.set(eventType, rafId);
  }
});
;// ./node_modules/recharts/es6/state/selectors/touchSelectors.js


var selectAllTooltipPayloadConfiguration = createSelector([selectTooltipState], tooltipState => tooltipState.tooltipItemPayloads);
var selectTooltipCoordinate = createSelector([selectAllTooltipPayloadConfiguration, (_state, tooltipIndex) => tooltipIndex, (_state, _tooltipIndex, graphicalItemId) => graphicalItemId], (allTooltipConfigurations, tooltipIndex, graphicalItemId) => {
  if (tooltipIndex == null) {
    return undefined;
  }
  var mostRelevantTooltipConfiguration = allTooltipConfigurations.find(tooltipConfiguration => {
    return tooltipConfiguration.settings.graphicalItemId === graphicalItemId;
  });
  if (mostRelevantTooltipConfiguration == null) {
    return undefined;
  }
  var {
    getPosition
  } = mostRelevantTooltipConfiguration;
  if (getPosition == null) {
    return undefined;
  }
  return getPosition(tooltipIndex);
});
;// ./node_modules/recharts/es6/state/touchEventsMiddleware.js








var touchEventAction = createAction('touchMove');
var touchEventMiddleware = createListenerMiddleware();
touchEventMiddleware.startListening({
  actionCreator: touchEventAction,
  effect: (action, listenerApi) => {
    var touchEvent = action.payload;
    if (touchEvent.touches == null || touchEvent.touches.length === 0) {
      return;
    }
    var state = listenerApi.getState();
    var tooltipEventType = selectTooltipEventType(state, state.tooltip.settings.shared);
    if (tooltipEventType === 'axis') {
      var touch = touchEvent.touches[0];
      if (touch == null) {
        return;
      }
      var activeProps = selectActivePropsFromChartPointer(state, getChartPointer({
        clientX: touch.clientX,
        clientY: touch.clientY,
        currentTarget: touchEvent.currentTarget
      }));
      if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
        listenerApi.dispatch(setMouseOverAxisIndex({
          activeIndex: activeProps.activeIndex,
          activeDataKey: undefined,
          activeCoordinate: activeProps.activeCoordinate
        }));
      }
    } else if (tooltipEventType === 'item') {
      var _target$getAttribute;
      var _touch = touchEvent.touches[0];
      if (document.elementFromPoint == null || _touch == null) {
        return;
      }
      var target = document.elementFromPoint(_touch.clientX, _touch.clientY);
      if (!target || !target.getAttribute) {
        return;
      }
      var itemIndex = target.getAttribute(DATA_ITEM_INDEX_ATTRIBUTE_NAME);
      var graphicalItemId = (_target$getAttribute = target.getAttribute(DATA_ITEM_GRAPHICAL_ITEM_ID_ATTRIBUTE_NAME)) !== null && _target$getAttribute !== void 0 ? _target$getAttribute : undefined;
      var settings = selectAllGraphicalItemsSettings(state).find(item => item.id === graphicalItemId);
      if (itemIndex == null || settings == null || graphicalItemId == null) {
        return;
      }
      var {
        dataKey
      } = settings;
      var coordinate = selectTooltipCoordinate(state, itemIndex, graphicalItemId);
      listenerApi.dispatch(setActiveMouseOverItemIndex({
        activeDataKey: dataKey,
        activeIndex: itemIndex,
        activeCoordinate: coordinate,
        activeGraphicalItemId: graphicalItemId
      }));
    }
  }
});
;// ./node_modules/recharts/es6/state/errorBarSlice.js


/**
 * ErrorBars have lot more settings but all the others are scoped to the component itself.
 * Only some of them required to be reported to the global store because XAxis and YAxis need to know
 * if the error bar is contributing to extending the axis domain.
 */

var errorBarSlice_initialState = {};
var errorBarSlice = createSlice({
  name: 'errorBars',
  initialState: errorBarSlice_initialState,
  reducers: {
    addErrorBar: (state, action) => {
      var {
        itemId,
        errorBar
      } = action.payload;
      if (!state[itemId]) {
        state[itemId] = [];
      }
      state[itemId].push(errorBar);
    },
    replaceErrorBar: (state, action) => {
      var {
        itemId,
        prev,
        next
      } = action.payload;
      if (state[itemId]) {
        state[itemId] = state[itemId].map(e => e.dataKey === prev.dataKey && e.direction === prev.direction ? next : e);
      }
    },
    removeErrorBar: (state, action) => {
      var {
        itemId,
        errorBar
      } = action.payload;
      if (state[itemId]) {
        state[itemId] = state[itemId].filter(e => e.dataKey !== errorBar.dataKey || e.direction !== errorBar.direction);
      }
    }
  }
});
var {
  addErrorBar,
  replaceErrorBar,
  removeErrorBar
} = errorBarSlice.actions;
var errorBarReducer = errorBarSlice.reducer;
;// ./node_modules/recharts/es6/util/Global.js
var parseIsSsrByDefault = () => !(typeof window !== 'undefined' && window.document && Boolean(window.document.createElement) && window.setTimeout);
var Global = {
  devToolsEnabled: true,
  isSsr: parseIsSsrByDefault()
};
;// ./node_modules/recharts/es6/state/zIndexSlice.js
function zIndexSlice_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function zIndexSlice_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? zIndexSlice_ownKeys(Object(t), !0).forEach(function (r) { zIndexSlice_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : zIndexSlice_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function zIndexSlice_defineProperty(e, r, t) { return (r = zIndexSlice_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function zIndexSlice_toPropertyKey(t) { var i = zIndexSlice_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function zIndexSlice_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * This slice contains a registry of z-index values for various components.
 * The state is a map from z-index numbers to element references.
 */



var seed = {};
var zIndexSlice_initialState = {
  zIndexMap: Object.values(DefaultZIndexes).reduce((acc, current) => zIndexSlice_objectSpread(zIndexSlice_objectSpread({}, acc), {}, {
    [current]: {
      element: undefined,
      panoramaElement: undefined,
      consumers: 0
    }
  }), seed)
};
var defaultZIndexSet = new Set(Object.values(DefaultZIndexes));
function isDefaultZIndex(zIndex) {
  return defaultZIndexSet.has(zIndex);
}
var zIndexSlice = createSlice({
  name: 'zIndex',
  initialState: zIndexSlice_initialState,
  reducers: {
    registerZIndexPortal: {
      reducer: (state, action) => {
        var {
          zIndex
        } = action.payload;
        if (state.zIndexMap[zIndex]) {
          state.zIndexMap[zIndex].consumers += 1;
        } else {
          state.zIndexMap[zIndex] = {
            consumers: 1,
            element: undefined,
            panoramaElement: undefined
          };
        }
      },
      prepare: prepareAutoBatched()
    },
    unregisterZIndexPortal: {
      reducer: (state, action) => {
        var {
          zIndex
        } = action.payload;
        if (state.zIndexMap[zIndex]) {
          state.zIndexMap[zIndex].consumers -= 1;
          /*
           * Garbage collect unused z-index entries, except for default z-indexes.
           * Default z-indexes are always rendered, regardless of whether there are consumers or not.
           * And because of that, even if we delete this entry, the ZIndexPortal provider will still be rendered
           * and React is not going to re-create it, and it won't re-register the element ID.
           * So let's not delete default z-index entries.
           */
          if (state.zIndexMap[zIndex].consumers <= 0 && !isDefaultZIndex(zIndex)) {
            delete state.zIndexMap[zIndex];
          }
        }
      },
      prepare: prepareAutoBatched()
    },
    registerZIndexPortalElement: {
      reducer: (state, action) => {
        var {
          zIndex,
          element,
          isPanorama
        } = action.payload;
        if (state.zIndexMap[zIndex]) {
          if (isPanorama) {
            state.zIndexMap[zIndex].panoramaElement = immer_castDraft(element);
          } else {
            state.zIndexMap[zIndex].element = immer_castDraft(element);
          }
        } else {
          state.zIndexMap[zIndex] = {
            consumers: 0,
            element: isPanorama ? undefined : immer_castDraft(element),
            panoramaElement: isPanorama ? immer_castDraft(element) : undefined
          };
        }
      },
      prepare: prepareAutoBatched()
    },
    unregisterZIndexPortalElement: {
      reducer: (state, action) => {
        var {
          zIndex
        } = action.payload;
        if (state.zIndexMap[zIndex]) {
          if (action.payload.isPanorama) {
            state.zIndexMap[zIndex].panoramaElement = undefined;
          } else {
            state.zIndexMap[zIndex].element = undefined;
          }
        }
      },
      prepare: prepareAutoBatched()
    }
  }
});
var {
  registerZIndexPortal,
  unregisterZIndexPortal,
  registerZIndexPortalElement,
  unregisterZIndexPortalElement
} = zIndexSlice.actions;
var zIndexReducer = zIndexSlice.reducer;
;// ./node_modules/recharts/es6/state/store.js





















var rootReducer = combineReducers({
  brush: brushReducer,
  cartesianAxis: cartesianAxisReducer,
  chartData: chartDataReducer,
  errorBars: errorBarReducer,
  graphicalItems: graphicalItemsReducer,
  layout: chartLayoutReducer,
  legend: legendReducer,
  options: optionsReducer,
  polarAxis: polarAxisReducer,
  polarOptions: polarOptionsReducer,
  referenceElements: referenceElementsReducer,
  rootProps: rootPropsReducer,
  tooltip: tooltipReducer,
  zIndex: zIndexReducer
});
var createRechartsStore = function createRechartsStore(preloadedState) {
  var chartName = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'Chart';
  return configureStore({
    reducer: rootReducer,
    // redux-toolkit v1 types are unhappy with the preloadedState type. Remove the `as any` when bumping to v2
    preloadedState: preloadedState,
    // @ts-expect-error redux-toolkit v1 types are unhappy with the middleware array. Remove this comment when bumping to v2
    middleware: getDefaultMiddleware => {
      var _process$env$NODE_ENV;
      return getDefaultMiddleware({
        serializableCheck: false,
        immutableCheck: !['commonjs', 'es6', 'production'].includes((_process$env$NODE_ENV = "es6") !== null && _process$env$NODE_ENV !== void 0 ? _process$env$NODE_ENV : '')
      }).concat([mouseClickMiddleware.middleware, mouseMoveMiddleware.middleware, keyboardEventsMiddleware.middleware, externalEventsMiddleware.middleware, touchEventMiddleware.middleware]);
    },
    /*
     * I can't find out how to satisfy typescript here.
     * We return `EnhancerArray<[StoreEnhancer<{}, {}>, StoreEnhancer]>` from this function,
     * but the types say we should return `EnhancerArray<StoreEnhancer<{}, {}>`.
     * Looks like it's badly inferred generics, but it won't allow me to provide the correct type manually either.
     * So let's just ignore the error for now.
     */
    // @ts-expect-error mismatched generics
    enhancers: getDefaultEnhancers => {
      var enhancers = getDefaultEnhancers;
      if (typeof getDefaultEnhancers === 'function') {
        /*
         * In RTK v2 this is always a function, but in v1 it is an array.
         * Because we have @types/redux-toolkit v1 as a dependency, typescript is going to flag this as an error.
         * We support both RTK v1 and v2, so we need to do this check.
         * https://redux-toolkit.js.org/usage/migrating-rtk-2#configurestoreenhancers-must-be-a-callback
         */
        // @ts-expect-error RTK v2 behaviour on RTK v1 types
        enhancers = getDefaultEnhancers();
      }
      return enhancers.concat(autoBatchEnhancer({
        type: 'raf'
      }));
    },
    devTools: Global.devToolsEnabled && {
      serialize: {
        replacer: reduxDevtoolsJsonStringifyReplacer
      },
      name: "recharts-".concat(chartName)
    }
  });
};
;// ./node_modules/recharts/es6/state/RechartsStoreProvider.js






function RechartsStoreProvider(_ref) {
  var {
    preloadedState,
    children,
    reduxStoreName
  } = _ref;
  var isPanorama = useIsPanorama();
  /*
   * Why the ref? Redux official documentation recommends to use store as a singleton,
   * and reuse that everywhere: https://redux-toolkit.js.org/api/configureStore#basic-example
   *
   * Which is correct! Except that is considering deploying Redux in an app.
   * Recharts as a library supports multiple charts on the same page.
   * And each of these charts needs its own store independent of others!
   *
   * The alternative is to have everything in the store keyed by the chart id.
   * Which would make working with everything a little bit more painful because we need the chart id everywhere.
   */
  var storeRef = (0,external_React_.useRef)(null);

  /*
   * Panorama means that this chart is not its own chart, it's only a "preview"
   * being rendered as a child of Brush.
   * In such case, it should not have a store on its own - it should implicitly inherit
   * whatever data is in the "parent" or "root" chart.
   * Which here is represented by not having a Provider at all. All selectors will use the root store by default.
   */
  if (isPanorama) {
    return children;
  }
  if (storeRef.current == null) {
    storeRef.current = createRechartsStore(preloadedState, reduxStoreName);
  }

  // @ts-expect-error React-Redux types demand that the context internal value is not null, but we have that as default.
  var nonNullContext = RechartsReduxContext;
  return /*#__PURE__*/external_React_.createElement(Provider_default, {
    context: nonNullContext,
    store: storeRef.current
  }, children);
}
;// ./node_modules/recharts/es6/context/chartDataContext.js




var ChartDataContextProvider = props => {
  var {
    chartData
  } = props;
  var dispatch = useAppDispatch();
  var isPanorama = useIsPanorama();
  (0,external_React_.useEffect)(() => {
    if (isPanorama) {
      // Panorama mode reuses data from the main chart, so we must not overwrite it here.
      return () => {
        // there is nothing to clean up
      };
    }
    dispatch(setChartData(chartData));
    return () => {
      dispatch(setChartData(undefined));
    };
  }, [chartData, dispatch, isPanorama]);
  return null;
};
var SetComputedData = props => {
  var {
    computedData
  } = props;
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(setComputedData(computedData));
    return () => {
      dispatch(setChartData(undefined));
    };
  }, [computedData, dispatch]);
  return null;
};
var selectChartData = state => state.chartData.chartData;

/**
 * "data" is the data of the chart - it has no type because this part of recharts is very flexible.
 * Basically it's an array of "something" and then there's the dataKey property in various places
 * that's meant to pull other things away from the data.
 *
 * Some charts have `data` defined on the chart root, and they will return the array through this hook.
 * For example: <ComposedChart data={data} />.
 *
 * Other charts, such as Pie, have data defined on individual graphical elements.
 * These charts will return `undefined` through this hook, and you need to read the data from children.
 * For example: <PieChart><Pie data={data} />
 *
 * Some charts also allow setting both - data on the parent, and data on the children at the same time!
 * However, this particular selector will only return the ones defined on the parent.
 *
 * @deprecated use one of the other selectors instead - which one, depends on how do you identify the applicable graphical items.
 *
 * @return data array for some charts and undefined for other
 */
var useChartData = () => useAppSelector(selectChartData);
var selectDataIndex = state => {
  var {
    dataStartIndex,
    dataEndIndex
  } = state.chartData;
  return {
    startIndex: dataStartIndex,
    endIndex: dataEndIndex
  };
};

/**
 * startIndex and endIndex are data boundaries, set through Brush.
 *
 * @return object with startIndex and endIndex
 */
var useDataIndex = () => {
  return useAppSelector(selectDataIndex);
};
;// ./node_modules/recharts/es6/util/propsAreEqual.js

var propsToShallowCompare = new Set(['axisLine', 'tickLine', 'activeBar', 'activeDot', 'activeLabel', 'activeShape', 'allowEscapeViewBox', 'background', 'cursor', 'dot', 'label', 'line', 'margin', 'padding', 'position', 'shape', 'style', 'tick', 'wrapperStyle',
// radius can be an array of 4 numbers, easy to compare shallowly
'radius']);

/**
 * When comparing two values, returns true if they are the same value or
 * are both NaN.
 *
 * If we used just a simple triple equals, we would get false negatives for two NaNs
 * which could cause extra re-renders so let's have this instead.
 *
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Equality_comparisons_and_sameness#same-value-zero_equality
 *
 * @param x first value to compare
 * @param y second value to compare
 * return true if the same, false if different
 */
function sameValueZero(x, y) {
  if (x == null && y == null) {
    /*
     * treat null and undefined as equal. Internally in Recharts we make no difference between these two
     * so there is no need to re-render.
     */
    return true;
  }
  if (typeof x === 'number' && typeof y === 'number') {
    // x and y are equal (this is true for -0 and 0) or they are both NaN
    // eslint-disable-next-line no-self-compare
    return x === y || x !== x && y !== y;
  }
  return x === y;
}

/**
 * So usually React would compare only the first level of props using Object.is.
 * However, in our case many props are objects or arrays, and our own docs recommend to do that!
 * Therefore, we need a custom comparison function that does a shallow comparison of each prop value.
 *
 * Because charts can and do receive large props (typically the data array),
 * we only limit this to a subset of known props that are likely to be objects/arrays.
 *
 * @param prevProps
 * @param nextProps
 */
function propsAreEqual(prevProps, nextProps) {
  var allKeys = new Set([...Object.keys(prevProps), ...Object.keys(nextProps)]);
  for (var key of allKeys) {
    /*
     * If a key is on a special allowlist, go one level deeper
     * and do a shallow comparison of the values.
     */
    if (propsToShallowCompare.has(key)) {
      if (prevProps[key] == null && nextProps[key] == null) {
        /*
         * treat null and undefined as equal. Internally in Recharts we make no difference between these two
         * so there is no need to re-render.
         */
        continue;
      }
      if (!shallowEqual(prevProps[key], nextProps[key])) {
        return false;
      }
      /*
       * Otherwise do a simple same-value comparison (with NaN support).
       */
    } else if (!sameValueZero(prevProps[key], nextProps[key])) {
      return false;
    }
  }
  return true;
}
;// ./node_modules/recharts/es6/state/ReportMainChartProps.js






/**
 * "Main" props are props that are only accepted on the main chart,
 * as opposed to the small panorama chart inside a Brush.
 */

function ReportMainChartPropsImpl(_ref) {
  var {
    layout,
    margin
  } = _ref;
  var dispatch = useAppDispatch();

  /*
   * Skip dispatching properties in panorama chart for two reasons:
   * 1. The root chart should be deciding on these properties, and
   * 2. Brush reads these properties from redux store, and so they must remain stable
   *      to avoid circular dependency and infinite re-rendering.
   */
  var isPanorama = useIsPanorama();
  /*
   * useEffect here is required to avoid the "Cannot update a component while rendering a different component" error.
   * https://github.com/facebook/react/issues/18178
   *
   * Reported in https://github.com/recharts/recharts/issues/5514
   */
  (0,external_React_.useEffect)(() => {
    if (!isPanorama) {
      dispatch(setLayout(layout));
      dispatch(setMargin(margin));
    }
  }, [dispatch, isPanorama, layout, margin]);
  return null;
}
var ReportMainChartProps = /*#__PURE__*/(0,external_React_.memo)(ReportMainChartPropsImpl, propsAreEqual);
;// ./node_modules/recharts/es6/state/ReportChartProps.js



function ReportChartProps(props) {
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(updateOptions(props));
  }, [dispatch, props]);
  return null;
}
;// ./node_modules/recharts/es6/state/ReportPolarOptions.js



function ReportPolarOptions(props) {
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(updatePolarOptions(props));
  }, [dispatch, props]);
  return null;
}
;// ./node_modules/recharts/es6/context/accessibilityContext.js

var useAccessibilityLayer = () => {
  var _useAppSelector;
  return (_useAppSelector = useAppSelector(state => state.rootProps.accessibilityLayer)) !== null && _useAppSelector !== void 0 ? _useAppSelector : true;
};
;// ./node_modules/recharts/es6/util/excludeEventProps.js
var EventKeys = ['dangerouslySetInnerHTML', 'onCopy', 'onCopyCapture', 'onCut', 'onCutCapture', 'onPaste', 'onPasteCapture', 'onCompositionEnd', 'onCompositionEndCapture', 'onCompositionStart', 'onCompositionStartCapture', 'onCompositionUpdate', 'onCompositionUpdateCapture', 'onFocus', 'onFocusCapture', 'onBlur', 'onBlurCapture', 'onChange', 'onChangeCapture', 'onBeforeInput', 'onBeforeInputCapture', 'onInput', 'onInputCapture', 'onReset', 'onResetCapture', 'onSubmit', 'onSubmitCapture', 'onInvalid', 'onInvalidCapture', 'onLoad', 'onLoadCapture', 'onError', 'onErrorCapture', 'onKeyDown', 'onKeyDownCapture', 'onKeyPress', 'onKeyPressCapture', 'onKeyUp', 'onKeyUpCapture', 'onAbort', 'onAbortCapture', 'onCanPlay', 'onCanPlayCapture', 'onCanPlayThrough', 'onCanPlayThroughCapture', 'onDurationChange', 'onDurationChangeCapture', 'onEmptied', 'onEmptiedCapture', 'onEncrypted', 'onEncryptedCapture', 'onEnded', 'onEndedCapture', 'onLoadedData', 'onLoadedDataCapture', 'onLoadedMetadata', 'onLoadedMetadataCapture', 'onLoadStart', 'onLoadStartCapture', 'onPause', 'onPauseCapture', 'onPlay', 'onPlayCapture', 'onPlaying', 'onPlayingCapture', 'onProgress', 'onProgressCapture', 'onRateChange', 'onRateChangeCapture', 'onSeeked', 'onSeekedCapture', 'onSeeking', 'onSeekingCapture', 'onStalled', 'onStalledCapture', 'onSuspend', 'onSuspendCapture', 'onTimeUpdate', 'onTimeUpdateCapture', 'onVolumeChange', 'onVolumeChangeCapture', 'onWaiting', 'onWaitingCapture', 'onAuxClick', 'onAuxClickCapture', 'onClick', 'onClickCapture', 'onContextMenu', 'onContextMenuCapture', 'onDoubleClick', 'onDoubleClickCapture', 'onDrag', 'onDragCapture', 'onDragEnd', 'onDragEndCapture', 'onDragEnter', 'onDragEnterCapture', 'onDragExit', 'onDragExitCapture', 'onDragLeave', 'onDragLeaveCapture', 'onDragOver', 'onDragOverCapture', 'onDragStart', 'onDragStartCapture', 'onDrop', 'onDropCapture', 'onMouseDown', 'onMouseDownCapture', 'onMouseEnter', 'onMouseLeave', 'onMouseMove', 'onMouseMoveCapture', 'onMouseOut', 'onMouseOutCapture', 'onMouseOver', 'onMouseOverCapture', 'onMouseUp', 'onMouseUpCapture', 'onSelect', 'onSelectCapture', 'onTouchCancel', 'onTouchCancelCapture', 'onTouchEnd', 'onTouchEndCapture', 'onTouchMove', 'onTouchMoveCapture', 'onTouchStart', 'onTouchStartCapture', 'onPointerDown', 'onPointerDownCapture', 'onPointerMove', 'onPointerMoveCapture', 'onPointerUp', 'onPointerUpCapture', 'onPointerCancel', 'onPointerCancelCapture', 'onPointerEnter', 'onPointerEnterCapture', 'onPointerLeave', 'onPointerLeaveCapture', 'onPointerOver', 'onPointerOverCapture', 'onPointerOut', 'onPointerOutCapture', 'onGotPointerCapture', 'onGotPointerCaptureCapture', 'onLostPointerCapture', 'onLostPointerCaptureCapture', 'onScroll', 'onScrollCapture', 'onWheel', 'onWheelCapture', 'onAnimationStart', 'onAnimationStartCapture', 'onAnimationEnd', 'onAnimationEndCapture', 'onAnimationIteration', 'onAnimationIterationCapture', 'onTransitionEnd', 'onTransitionEndCapture'];
function isEventKey(key) {
  if (typeof key !== 'string') {
    return false;
  }
  var allowedEventKeys = EventKeys;
  return allowedEventKeys.includes(key);
}
;// ./node_modules/recharts/es6/util/svgPropertiesNoEvents.js

var SVGElementPropKeys = ['aria-activedescendant', 'aria-atomic', 'aria-autocomplete', 'aria-busy', 'aria-checked', 'aria-colcount', 'aria-colindex', 'aria-colspan', 'aria-controls', 'aria-current', 'aria-describedby', 'aria-details', 'aria-disabled', 'aria-errormessage', 'aria-expanded', 'aria-flowto', 'aria-haspopup', 'aria-hidden', 'aria-invalid', 'aria-keyshortcuts', 'aria-label', 'aria-labelledby', 'aria-level', 'aria-live', 'aria-modal', 'aria-multiline', 'aria-multiselectable', 'aria-orientation', 'aria-owns', 'aria-placeholder', 'aria-posinset', 'aria-pressed', 'aria-readonly', 'aria-relevant', 'aria-required', 'aria-roledescription', 'aria-rowcount', 'aria-rowindex', 'aria-rowspan', 'aria-selected', 'aria-setsize', 'aria-sort', 'aria-valuemax', 'aria-valuemin', 'aria-valuenow', 'aria-valuetext', 'className', 'color', 'height', 'id', 'lang', 'max', 'media', 'method', 'min', 'name', 'style',
/*
 * removed 'type' SVGElementPropKey because we do not currently use any SVG elements
 * that can use it, and it conflicts with the recharts prop 'type'
 * https://github.com/recharts/recharts/pull/3327
 * https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/type
 */
// 'type',
'target', 'width', 'role', 'tabIndex', 'accentHeight', 'accumulate', 'additive', 'alignmentBaseline', 'allowReorder', 'alphabetic', 'amplitude', 'arabicForm', 'ascent', 'attributeName', 'attributeType', 'autoReverse', 'azimuth', 'baseFrequency', 'baselineShift', 'baseProfile', 'bbox', 'begin', 'bias', 'by', 'calcMode', 'capHeight', 'clip', 'clipPath', 'clipPathUnits', 'clipRule', 'colorInterpolation', 'colorInterpolationFilters', 'colorProfile', 'colorRendering', 'contentScriptType', 'contentStyleType', 'cursor', 'cx', 'cy', 'd', 'decelerate', 'descent', 'diffuseConstant', 'direction', 'display', 'divisor', 'dominantBaseline', 'dur', 'dx', 'dy', 'edgeMode', 'elevation', 'enableBackground', 'end', 'exponent', 'externalResourcesRequired', 'fill', 'fillOpacity', 'fillRule', 'filter', 'filterRes', 'filterUnits', 'floodColor', 'floodOpacity', 'focusable', 'fontFamily', 'fontSize', 'fontSizeAdjust', 'fontStretch', 'fontStyle', 'fontVariant', 'fontWeight', 'format', 'from', 'fx', 'fy', 'g1', 'g2', 'glyphName', 'glyphOrientationHorizontal', 'glyphOrientationVertical', 'glyphRef', 'gradientTransform', 'gradientUnits', 'hanging', 'horizAdvX', 'horizOriginX', 'href', 'ideographic', 'imageRendering', 'in2', 'in', 'intercept', 'k1', 'k2', 'k3', 'k4', 'k', 'kernelMatrix', 'kernelUnitLength', 'kerning', 'keyPoints', 'keySplines', 'keyTimes', 'lengthAdjust', 'letterSpacing', 'lightingColor', 'limitingConeAngle', 'local', 'markerEnd', 'markerHeight', 'markerMid', 'markerStart', 'markerUnits', 'markerWidth', 'mask', 'maskContentUnits', 'maskUnits', 'mathematical', 'mode', 'numOctaves', 'offset', 'opacity', 'operator', 'order', 'orient', 'orientation', 'origin', 'overflow', 'overlinePosition', 'overlineThickness', 'paintOrder', 'panose1', 'pathLength', 'patternContentUnits', 'patternTransform', 'patternUnits', 'pointerEvents', 'pointsAtX', 'pointsAtY', 'pointsAtZ', 'preserveAlpha', 'preserveAspectRatio', 'primitiveUnits', 'r', 'radius', 'refX', 'refY', 'renderingIntent', 'repeatCount', 'repeatDur', 'requiredExtensions', 'requiredFeatures', 'restart', 'result', 'rotate', 'rx', 'ry', 'seed', 'shapeRendering', 'slope', 'spacing', 'specularConstant', 'specularExponent', 'speed', 'spreadMethod', 'startOffset', 'stdDeviation', 'stemh', 'stemv', 'stitchTiles', 'stopColor', 'stopOpacity', 'strikethroughPosition', 'strikethroughThickness', 'string', 'stroke', 'strokeDasharray', 'strokeDashoffset', 'strokeLinecap', 'strokeLinejoin', 'strokeMiterlimit', 'strokeOpacity', 'strokeWidth', 'surfaceScale', 'systemLanguage', 'tableValues', 'targetX', 'targetY', 'textAnchor', 'textDecoration', 'textLength', 'textRendering', 'to', 'transform', 'u1', 'u2', 'underlinePosition', 'underlineThickness', 'unicode', 'unicodeBidi', 'unicodeRange', 'unitsPerEm', 'vAlphabetic', 'values', 'vectorEffect', 'version', 'vertAdvY', 'vertOriginX', 'vertOriginY', 'vHanging', 'vIdeographic', 'viewTarget', 'visibility', 'vMathematical', 'widths', 'wordSpacing', 'writingMode', 'x1', 'x2', 'x', 'xChannelSelector', 'xHeight', 'xlinkActuate', 'xlinkArcrole', 'xlinkHref', 'xlinkRole', 'xlinkShow', 'xlinkTitle', 'xlinkType', 'xmlBase', 'xmlLang', 'xmlns', 'xmlnsXlink', 'xmlSpace', 'y1', 'y2', 'y', 'yChannelSelector', 'z', 'zoomAndPan', 'ref', 'key', 'angle'];
var SVGElementPropKeySet = new Set(SVGElementPropKeys);
function isSvgElementPropKey(key) {
  if (typeof key !== 'string') {
    return false;
  }
  return SVGElementPropKeySet.has(key);
}
/**
 * Checks if the property is a data attribute.
 * @param key The property key.
 * @returns True if the key starts with 'data-', false otherwise.
 */
function isDataAttribute(key) {
  return typeof key === 'string' && key.startsWith('data-');
}

/**
 * Filters an object to only include SVG properties. Removes all event handlers too.
 * @param obj - The object to filter
 * @returns A new object containing only valid SVG properties, excluding event handlers.
 */
function svgPropertiesNoEvents(obj) {
  if (typeof obj !== 'object' || obj === null) {
    return {};
  }
  var result = {};
  for (var key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      if (isSvgElementPropKey(key) || isDataAttribute(key)) {
        result[key] = obj[key];
      }
    }
  }
  return result;
}

/**
 * Function to filter SVG properties from various input types.
 * The input types can be:
 * - A record of string keys to any values, in which case it returns a record of only SVG properties
 * - A React element, in which case it returns the props of the element filtered to only SVG properties
 * - Anything else, in which case it returns null
 *
 * This function has a wide-open return type, because it will read and filter the props of an arbitrary React element.
 * This can be SVG, HTML, whatnot, with arbitrary values, so we can't type it more specifically.
 *
 * If you wish to have a type-safe version, use svgPropertiesNoEvents directly with a typed object.
 *
 * @param input - The input to filter, which can be a record, a React element, or other types.
 * @returns A record of SVG properties if the input is a record or React element, otherwise null.
 */
function svgPropertiesNoEventsFromUnknown(input) {
  if (input == null) {
    return null;
  }
  if (/*#__PURE__*/(0,external_React_.isValidElement)(input) && typeof input.props === 'object' && input.props !== null) {
    var p = input.props;
    return svgPropertiesNoEvents(p);
  }
  if (typeof input === 'object' && !Array.isArray(input)) {
    return svgPropertiesNoEvents(input);
  }
  return null;
}
;// ./node_modules/recharts/es6/util/svgPropertiesAndEvents.js



/**
 * Filters an object to only include SVG properties, data attributes, and event handlers.
 * @param obj - The object to filter.
 * @returns A new object containing only valid SVG properties, data attributes, and event handlers.
 */
function svgPropertiesAndEvents(obj) {
  var result = {};
  // for ... in loop is 10x faster than Object.entries + filter + Object.fromEntries in Chrome

  for (var key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      if (isSvgElementPropKey(key) || isDataAttribute(key) || isEventKey(key)) {
        result[key] = obj[key];
      }
    }
  }
  return result;
}

/**
 * Function to filter SVG properties from various input types.
 * The input types can be:
 * - A record of string keys to any values, in which case it returns a record of only SVG properties
 * - A React element, in which case it returns the props of the element filtered to only SVG properties
 * - Anything else, in which case it returns null
 *
 * This function has a wide-open return type, because it will read and filter the props of an arbitrary React element.
 * This can be SVG, HTML, whatnot, with arbitrary values, so we can't type it more specifically.
 *
 * If you wish to have a type-safe version, use svgPropertiesNoEvents directly with a typed object.
 *
 * @param input - The input to filter, which can be a record, a React element, or other types.
 * @returns A record of SVG properties if the input is a record or React element, otherwise null.
 */
function svgPropertiesAndEventsFromUnknown(input) {
  if (input == null) {
    return null;
  }
  if (/*#__PURE__*/(0,external_React_.isValidElement)(input)) {
    // @ts-expect-error we can't type this better because input can be any React element
    return svgPropertiesAndEvents(input.props);
  }
  if (typeof input === 'object' && !Array.isArray(input)) {
    return svgPropertiesAndEvents(input);
  }
  return null;
}
;// ./node_modules/recharts/es6/container/Surface.js
var _excluded = ["children", "width", "height", "viewBox", "className", "style", "title", "desc"];
function Surface_extends() { return Surface_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Surface_extends.apply(null, arguments); }
function _objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = _objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function _objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }




/**
 * Renders an SVG element.
 *
 * All charts already include a Surface component, so you would not normally use this directly.
 *
 * @link https://developer.mozilla.org/en-US/docs/Web/SVG/Element/svg
 */
var Surface = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
      children,
      width,
      height,
      viewBox,
      className,
      style,
      title,
      desc
    } = props,
    others = _objectWithoutProperties(props, _excluded);
  var svgView = viewBox || {
    width,
    height,
    x: 0,
    y: 0
  };
  var layerClass = clsx('recharts-surface', className);
  return /*#__PURE__*/external_React_.createElement("svg", Surface_extends({}, svgPropertiesAndEvents(others), {
    className: layerClass,
    width: width,
    height: height,
    style: style,
    viewBox: "".concat(svgView.x, " ").concat(svgView.y, " ").concat(svgView.width, " ").concat(svgView.height),
    ref: ref
  }), /*#__PURE__*/external_React_.createElement("title", null, title), /*#__PURE__*/external_React_.createElement("desc", null, desc), children);
});
;// ./node_modules/recharts/es6/zIndex/zIndexSelectors.js




/**
 * Given a zIndex, returns the corresponding portal element reference.
 * If no zIndex is provided or if the zIndex is not registered, returns undefined.
 *
 * It also returns undefined in case the z-index portal has not been rendered yet.
 */
var selectZIndexPortalElement = createSelector(state => state.zIndex.zIndexMap, (_, zIndex) => zIndex, (_, _zIndex, isPanorama) => isPanorama, (zIndexMap, zIndex, isPanorama) => {
  if (zIndex == null) {
    return undefined;
  }
  var entry = zIndexMap[zIndex];
  if (entry == null) {
    return undefined;
  }
  if (isPanorama) {
    return entry.panoramaElement;
  }
  return entry.element;
});
var selectAllRegisteredZIndexes = createSelector(state => state.zIndex.zIndexMap, zIndexMap => {
  var allNumbers = Object.keys(zIndexMap).map(zIndexStr => parseInt(zIndexStr, 10)).concat(Object.values(DefaultZIndexes));
  var uniqueNumbers = Array.from(new Set(allNumbers));
  return uniqueNumbers.sort((a, b) => a - b);
}, {
  memoizeOptions: {
    resultEqualityCheck: arrayContentsAreEqualCheck
  }
});
;// ./node_modules/recharts/es6/zIndex/ZIndexPortal.js





function ZIndexSvgPortal(_ref) {
  var {
    zIndex,
    isPanorama
  } = _ref;
  var ref = (0,external_React_.useRef)(null);
  var dispatch = useAppDispatch();
  (0,external_React_.useLayoutEffect)(() => {
    if (ref.current) {
      dispatch(registerZIndexPortalElement({
        zIndex,
        element: ref.current,
        isPanorama
      }));
    }
    return () => {
      dispatch(unregisterZIndexPortalElement({
        zIndex,
        isPanorama
      }));
    };
  }, [dispatch, zIndex, isPanorama]);
  // these g elements should not be tabbable
  return /*#__PURE__*/external_React_.createElement("g", {
    tabIndex: -1,
    ref: ref
  });
}
function AllZIndexPortals(_ref2) {
  var {
    children,
    isPanorama
  } = _ref2;
  var allRegisteredZIndexes = useAppSelector(selectAllRegisteredZIndexes);
  if (!allRegisteredZIndexes || allRegisteredZIndexes.length === 0) {
    return children;
  }
  var allNegativeZIndexes = allRegisteredZIndexes.filter(zIndex => zIndex < 0);
  // We exclude zero on purpose - that is the default layer, and it doesn't need a portal.
  var allPositiveZIndexes = allRegisteredZIndexes.filter(zIndex => zIndex > 0);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, allNegativeZIndexes.map(zIndex => /*#__PURE__*/external_React_.createElement(ZIndexSvgPortal, {
    key: zIndex,
    zIndex: zIndex,
    isPanorama: isPanorama
  })), children, allPositiveZIndexes.map(zIndex => /*#__PURE__*/external_React_.createElement(ZIndexSvgPortal, {
    key: zIndex,
    zIndex: zIndex,
    isPanorama: isPanorama
  })));
}
;// ./node_modules/recharts/es6/container/RootSurface.js
var RootSurface_excluded = ["children"];
function RootSurface_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = RootSurface_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function RootSurface_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function RootSurface_extends() { return RootSurface_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, RootSurface_extends.apply(null, arguments); }










var FULL_WIDTH_AND_HEIGHT = {
  width: '100%',
  height: '100%',
  /*
   * display: block is necessary here because the default for an SVG is display: inline,
   * which in some browsers (Chrome) adds a little bit of extra space above and below the SVG
   * to make space for the descender of letters like "g" and "y". This throws off the height calculation
   * and causes the container to grow indefinitely on each render with responsive=true.
   * Display: block removes that extra space.
   *
   * Interestingly, Firefox does not have this problem, but it doesn't hurt to add the style anyway.
   */
  display: 'block'
};
var MainChartSurface = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var width = useChartWidth();
  var height = useChartHeight();
  var hasAccessibilityLayer = useAccessibilityLayer();
  if (!isPositiveNumber(width) || !isPositiveNumber(height)) {
    return null;
  }
  var {
    children,
    otherAttributes,
    title,
    desc
  } = props;
  var tabIndex, role;
  if (otherAttributes != null) {
    if (typeof otherAttributes.tabIndex === 'number') {
      tabIndex = otherAttributes.tabIndex;
    } else {
      tabIndex = hasAccessibilityLayer ? 0 : undefined;
    }
    if (typeof otherAttributes.role === 'string') {
      role = otherAttributes.role;
    } else {
      role = hasAccessibilityLayer ? 'application' : undefined;
    }
  }
  return /*#__PURE__*/external_React_.createElement(Surface, RootSurface_extends({}, otherAttributes, {
    title: title,
    desc: desc,
    role: role,
    tabIndex: tabIndex,
    width: width,
    height: height,
    style: FULL_WIDTH_AND_HEIGHT,
    ref: ref
  }), children);
});
var BrushPanoramaSurface = _ref => {
  var {
    children
  } = _ref;
  var brushDimensions = useAppSelector(selectBrushDimensions);
  if (!brushDimensions) {
    return null;
  }
  var {
    width,
    height,
    y,
    x
  } = brushDimensions;
  return /*#__PURE__*/external_React_.createElement(Surface, {
    width: width,
    height: height,
    x: x,
    y: y
  }, children);
};
var RootSurface = /*#__PURE__*/(0,external_React_.forwardRef)((_ref2, ref) => {
  var {
      children
    } = _ref2,
    rest = RootSurface_objectWithoutProperties(_ref2, RootSurface_excluded);
  var isPanorama = useIsPanorama();
  if (isPanorama) {
    return /*#__PURE__*/external_React_.createElement(BrushPanoramaSurface, null, /*#__PURE__*/external_React_.createElement(AllZIndexPortals, {
      isPanorama: true
    }, children));
  }
  return /*#__PURE__*/external_React_.createElement(MainChartSurface, RootSurface_extends({
    ref: ref
  }, rest), /*#__PURE__*/external_React_.createElement(AllZIndexPortals, {
    isPanorama: false
  }, children));
});
// EXTERNAL MODULE: ./node_modules/eventemitter3/index.js
var eventemitter3 = __webpack_require__(228);
;// ./node_modules/eventemitter3/index.mjs



/* harmony default export */ const node_modules_eventemitter3 = (eventemitter3);

;// ./node_modules/recharts/es6/util/Events.js

var eventCenter = new node_modules_eventemitter3();

var TOOLTIP_SYNC_EVENT = 'recharts.syncEvent.tooltip';
var BRUSH_SYNC_EVENT = 'recharts.syncEvent.brush';
;// ./node_modules/recharts/es6/synchronisation/syncSelectors.js
function selectSynchronisedTooltipState(state) {
  return state.tooltip.syncInteraction;
}
;// ./node_modules/recharts/es6/synchronisation/useChartSynchronisation.js
var useChartSynchronisation_excluded = ["x", "y"];
function useChartSynchronisation_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function useChartSynchronisation_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? useChartSynchronisation_ownKeys(Object(t), !0).forEach(function (r) { useChartSynchronisation_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : useChartSynchronisation_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function useChartSynchronisation_defineProperty(e, r, t) { return (r = useChartSynchronisation_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function useChartSynchronisation_toPropertyKey(t) { var i = useChartSynchronisation_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function useChartSynchronisation_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function useChartSynchronisation_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = useChartSynchronisation_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function useChartSynchronisation_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }












function useTooltipSyncEventsListener() {
  var mySyncId = useAppSelector(selectSyncId);
  var myEventEmitter = useAppSelector(selectEventEmitter);
  var dispatch = useAppDispatch();
  var syncMethod = useAppSelector(selectSyncMethod);
  var tooltipTicks = useAppSelector(selectTooltipAxisTicks);
  var layout = useChartLayout();
  var viewBox = useViewBox();
  var className = useAppSelector(state => state.rootProps.className);
  (0,external_React_.useEffect)(() => {
    if (mySyncId == null) {
      // This chart is not synchronised with any other chart so we don't need to listen for any events.
      return noop;
    }
    var listener = (incomingSyncId, action, emitter) => {
      if (myEventEmitter === emitter) {
        // We don't want to dispatch actions that we sent ourselves.
        return;
      }
      if (mySyncId !== incomingSyncId) {
        // This event is not for this chart
        return;
      }
      if (syncMethod === 'index') {
        var _action$payload;
        if (viewBox && action !== null && action !== void 0 && (_action$payload = action.payload) !== null && _action$payload !== void 0 && _action$payload.coordinate && action.payload.sourceViewBox) {
          var _action$payload$coord = action.payload.coordinate,
            {
              x: _x,
              y: _y
            } = _action$payload$coord,
            otherCoordinateProps = useChartSynchronisation_objectWithoutProperties(_action$payload$coord, useChartSynchronisation_excluded);
          var {
            x: sourceX,
            y: sourceY,
            width: sourceWidth,
            height: sourceHeight
          } = action.payload.sourceViewBox;
          var scaledCoordinate = useChartSynchronisation_objectSpread(useChartSynchronisation_objectSpread({}, otherCoordinateProps), {}, {
            x: viewBox.x + (sourceWidth ? (_x - sourceX) / sourceWidth : 0) * viewBox.width,
            y: viewBox.y + (sourceHeight ? (_y - sourceY) / sourceHeight : 0) * viewBox.height
          });
          dispatch(useChartSynchronisation_objectSpread(useChartSynchronisation_objectSpread({}, action), {}, {
            payload: useChartSynchronisation_objectSpread(useChartSynchronisation_objectSpread({}, action.payload), {}, {
              coordinate: scaledCoordinate
            })
          }));
        } else {
          dispatch(action);
        }
        return;
      }
      if (tooltipTicks == null) {
        // for the other two sync methods, we need the ticks to be available
        return;
      }
      var activeTick;
      if (typeof syncMethod === 'function') {
        /*
         * This is what the data shape in 2.x CategoricalChartState used to look like.
         * In 3.x we store things differently but let's try to keep the old shape for compatibility.
         */
        var syncMethodParam = {
          activeTooltipIndex: action.payload.index == null ? undefined : Number(action.payload.index),
          isTooltipActive: action.payload.active,
          activeIndex: action.payload.index == null ? undefined : Number(action.payload.index),
          activeLabel: action.payload.label,
          activeDataKey: action.payload.dataKey,
          activeCoordinate: action.payload.coordinate
        };
        // Call a callback function. If there is an application specific algorithm
        var activeTooltipIndex = syncMethod(tooltipTicks, syncMethodParam);
        activeTick = tooltipTicks[activeTooltipIndex];
      } else if (syncMethod === 'value') {
        // labels are always strings, tick.value might be a string or a number, depending on axis type
        activeTick = tooltipTicks.find(tick => String(tick.value) === action.payload.label);
      }
      var {
        coordinate
      } = action.payload;
      if (activeTick == null || action.payload.active === false || coordinate == null || viewBox == null) {
        dispatch(setSyncInteraction({
          active: false,
          coordinate: undefined,
          dataKey: undefined,
          index: null,
          label: undefined,
          sourceViewBox: undefined,
          graphicalItemId: undefined
        }));
        return;
      }
      var {
        x,
        y
      } = coordinate;
      var validateChartX = Math.min(x, viewBox.x + viewBox.width);
      var validateChartY = Math.min(y, viewBox.y + viewBox.height);
      var activeCoordinate = {
        x: layout === 'horizontal' ? activeTick.coordinate : validateChartX,
        y: layout === 'horizontal' ? validateChartY : activeTick.coordinate
      };
      var syncAction = setSyncInteraction({
        active: action.payload.active,
        coordinate: activeCoordinate,
        dataKey: action.payload.dataKey,
        index: String(activeTick.index),
        label: action.payload.label,
        sourceViewBox: action.payload.sourceViewBox,
        graphicalItemId: action.payload.graphicalItemId
      });
      dispatch(syncAction);
    };
    eventCenter.on(TOOLTIP_SYNC_EVENT, listener);
    return () => {
      eventCenter.off(TOOLTIP_SYNC_EVENT, listener);
    };
  }, [className, dispatch, myEventEmitter, mySyncId, syncMethod, tooltipTicks, layout, viewBox]);
}
function useBrushSyncEventsListener() {
  var mySyncId = useAppSelector(selectSyncId);
  var myEventEmitter = useAppSelector(selectEventEmitter);
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    if (mySyncId == null) {
      // This chart is not synchronised with any other chart so we don't need to listen for any events.
      return noop;
    }
    var listener = (incomingSyncId, action, emitter) => {
      if (myEventEmitter === emitter) {
        // We don't want to dispatch actions that we sent ourselves.
        return;
      }
      if (mySyncId === incomingSyncId) {
        dispatch(setDataStartEndIndexes(action));
      }
    };
    eventCenter.on(BRUSH_SYNC_EVENT, listener);
    return () => {
      eventCenter.off(BRUSH_SYNC_EVENT, listener);
    };
  }, [dispatch, myEventEmitter, mySyncId]);
}

/**
 * Will receive synchronisation events from other charts.
 *
 * Reads syncMethod from state and decides how to synchronise the tooltip based on that.
 *
 * @returns void
 */
function useSynchronisedEventsFromOtherCharts() {
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(createEventEmitter());
  }, [dispatch]);
  useTooltipSyncEventsListener();
  useBrushSyncEventsListener();
}

/**
 * Will send events to other charts.
 * If syncId is undefined, no events will be sent.
 *
 * This ignores the syncMethod, because that is set and computed on the receiving end.
 *
 * @param tooltipEventType from Tooltip
 * @param trigger from Tooltip
 * @param activeCoordinate from state
 * @param activeLabel from state
 * @param activeIndex from state
 * @param isTooltipActive from state
 * @returns void
 */
function useTooltipChartSynchronisation(tooltipEventType, trigger, activeCoordinate, activeLabel, activeIndex, isTooltipActive) {
  var activeDataKey = useAppSelector(state => selectTooltipDataKey(state, tooltipEventType, trigger));
  var eventEmitterSymbol = useAppSelector(selectEventEmitter);
  var syncId = useAppSelector(selectSyncId);
  var syncMethod = useAppSelector(selectSyncMethod);
  var tooltipState = useAppSelector(selectSynchronisedTooltipState);
  var isReceivingSynchronisation = tooltipState === null || tooltipState === void 0 ? void 0 : tooltipState.active;
  var viewBox = useViewBox();
  (0,external_React_.useEffect)(() => {
    if (isReceivingSynchronisation) {
      /*
       * This chart currently has active tooltip, synchronised from another chart.
       * Let's not send any outgoing synchronisation events while that's happening
       * to avoid infinite loops.
       */
      return;
    }
    if (syncId == null) {
      /*
       * syncId is not set, means that this chart is not synchronised with any other chart,
       * means we don't need to send synchronisation events
       */
      return;
    }
    if (eventEmitterSymbol == null) {
      /*
       * When using Recharts internal hooks and selectors outside charts context,
       * these properties will be undefined. Let's return silently instead of throwing an error.
       */
      return;
    }
    var syncAction = setSyncInteraction({
      active: isTooltipActive,
      coordinate: activeCoordinate,
      dataKey: activeDataKey,
      index: activeIndex,
      label: typeof activeLabel === 'number' ? String(activeLabel) : activeLabel,
      sourceViewBox: viewBox,
      graphicalItemId: undefined
    });
    eventCenter.emit(TOOLTIP_SYNC_EVENT, syncId, syncAction, eventEmitterSymbol);
  }, [isReceivingSynchronisation, activeCoordinate, activeDataKey, activeIndex, activeLabel, eventEmitterSymbol, syncId, syncMethod, isTooltipActive, viewBox]);
}
function useBrushChartSynchronisation() {
  var syncId = useAppSelector(selectSyncId);
  var eventEmitterSymbol = useAppSelector(selectEventEmitter);
  var brushStartIndex = useAppSelector(state => state.chartData.dataStartIndex);
  var brushEndIndex = useAppSelector(state => state.chartData.dataEndIndex);
  (0,external_React_.useEffect)(() => {
    if (syncId == null || brushStartIndex == null || brushEndIndex == null || eventEmitterSymbol == null) {
      return;
    }
    var syncAction = {
      startIndex: brushStartIndex,
      endIndex: brushEndIndex
    };
    eventCenter.emit(BRUSH_SYNC_EVENT, syncId, syncAction, eventEmitterSymbol);
  }, [brushEndIndex, brushStartIndex, eventEmitterSymbol, syncId]);
}
;// ./node_modules/recharts/es6/util/useReportScale.js





function useReportScale() {
  var dispatch = useAppDispatch();
  var [ref, setRef] = (0,external_React_.useState)(null);
  var scale = useAppSelector(selectContainerScale);
  (0,external_React_.useEffect)(() => {
    if (ref == null) {
      return;
    }
    var rect = ref.getBoundingClientRect();
    var newScale = rect.width / ref.offsetWidth;
    if (isWellBehavedNumber(newScale) && newScale !== scale) {
      dispatch(setScale(newScale));
    }
  }, [ref, dispatch, scale]);
  return setRef;
}
;// ./node_modules/recharts/es6/context/tooltipPortalContext.js

var TooltipPortalContext = /*#__PURE__*/(0,external_React_.createContext)(null);
var useTooltipPortal = () => (0,external_React_.useContext)(TooltipPortalContext);
;// ./node_modules/recharts/es6/context/legendPortalContext.js

var LegendPortalContext = /*#__PURE__*/(0,external_React_.createContext)(null);
var useLegendPortal = () => (0,external_React_.useContext)(LegendPortalContext);
;// ./node_modules/recharts/es6/chart/RechartsWrapper.js
function RechartsWrapper_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function RechartsWrapper_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? RechartsWrapper_ownKeys(Object(t), !0).forEach(function (r) { RechartsWrapper_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : RechartsWrapper_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function RechartsWrapper_defineProperty(e, r, t) { return (r = RechartsWrapper_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function RechartsWrapper_toPropertyKey(t) { var i = RechartsWrapper_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function RechartsWrapper_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function RechartsWrapper_extends() { return RechartsWrapper_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, RechartsWrapper_extends.apply(null, arguments); }















var EventSynchronizer = () => {
  useSynchronisedEventsFromOtherCharts();
  return null;
};
function getNumberOrZero(value) {
  if (typeof value === 'number') {
    return value;
  }
  if (typeof value === 'string') {
    var parsed = parseFloat(value);
    if (!Number.isNaN(parsed)) {
      return parsed;
    }
  }
  return 0;
}
var ResponsiveDiv = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var _props$style, _props$style2;
  var observerRef = (0,external_React_.useRef)(null);
  var [sizes, setSizes] = (0,external_React_.useState)({
    containerWidth: getNumberOrZero((_props$style = props.style) === null || _props$style === void 0 ? void 0 : _props$style.width),
    containerHeight: getNumberOrZero((_props$style2 = props.style) === null || _props$style2 === void 0 ? void 0 : _props$style2.height)
  });
  var setContainerSize = (0,external_React_.useCallback)((newWidth, newHeight) => {
    setSizes(prevState => {
      var roundedWidth = Math.round(newWidth);
      var roundedHeight = Math.round(newHeight);
      if (prevState.containerWidth === roundedWidth && prevState.containerHeight === roundedHeight) {
        return prevState;
      }
      return {
        containerWidth: roundedWidth,
        containerHeight: roundedHeight
      };
    });
  }, []);
  var innerRef = (0,external_React_.useCallback)(node => {
    if (typeof ref === 'function') {
      ref(node);
    }
    if (node != null && typeof ResizeObserver !== 'undefined') {
      var {
        width: containerWidth,
        height: containerHeight
      } = node.getBoundingClientRect();
      setContainerSize(containerWidth, containerHeight);
      var callback = entries => {
        var entry = entries[0];
        if (entry == null) {
          return;
        }
        var {
          width,
          height
        } = entry.contentRect;
        setContainerSize(width, height);
      };
      var observer = new ResizeObserver(callback);
      observer.observe(node);
      observerRef.current = observer;
    }
  }, [ref, setContainerSize]);
  (0,external_React_.useEffect)(() => {
    return () => {
      var observer = observerRef.current;
      if (observer != null) {
        observer.disconnect();
      }
    };
  }, [setContainerSize]);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(ReportChartSize, {
    width: sizes.containerWidth,
    height: sizes.containerHeight
  }), /*#__PURE__*/external_React_.createElement("div", RechartsWrapper_extends({
    ref: innerRef
  }, props)));
});
var ReadSizeOnceDiv = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
    width,
    height
  } = props;
  var [sizes, setSizes] = (0,external_React_.useState)({
    containerWidth: getNumberOrZero(width),
    containerHeight: getNumberOrZero(height)
  });
  var setContainerSize = (0,external_React_.useCallback)((newWidth, newHeight) => {
    setSizes(prevState => {
      var roundedWidth = Math.round(newWidth);
      var roundedHeight = Math.round(newHeight);
      if (prevState.containerWidth === roundedWidth && prevState.containerHeight === roundedHeight) {
        return prevState;
      }
      return {
        containerWidth: roundedWidth,
        containerHeight: roundedHeight
      };
    });
  }, []);
  var innerRef = (0,external_React_.useCallback)(node => {
    if (typeof ref === 'function') {
      ref(node);
    }
    if (node != null) {
      var {
        width: containerWidth,
        height: containerHeight
      } = node.getBoundingClientRect();
      setContainerSize(containerWidth, containerHeight);
    }
  }, [ref, setContainerSize]);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(ReportChartSize, {
    width: sizes.containerWidth,
    height: sizes.containerHeight
  }), /*#__PURE__*/external_React_.createElement("div", RechartsWrapper_extends({
    ref: innerRef
  }, props)));
});
var StaticDiv = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
    width,
    height
  } = props;
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(ReportChartSize, {
    width: width,
    height: height
  }), /*#__PURE__*/external_React_.createElement("div", RechartsWrapper_extends({
    ref: ref
  }, props)));
});
var NonResponsiveDiv = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
    width,
    height
  } = props;
  // When width or height are percentages or CSS short names, read size from DOM once
  if (typeof width === 'string' || typeof height === 'string') {
    return /*#__PURE__*/external_React_.createElement(ReadSizeOnceDiv, RechartsWrapper_extends({}, props, {
      ref: ref
    }));
  }
  // When both are numbers, use them directly
  if (typeof width === 'number' && typeof height === 'number') {
    return /*#__PURE__*/external_React_.createElement(StaticDiv, RechartsWrapper_extends({}, props, {
      width: width,
      height: height,
      ref: ref
    }));
  }
  // When width/height are undefined, render wrapper div without reporting size
  // This results in no SVG being rendered (intentional for backwards compatibility)
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(ReportChartSize, {
    width: width,
    height: height
  }), /*#__PURE__*/external_React_.createElement("div", RechartsWrapper_extends({
    ref: ref
  }, props)));
});
function getWrapperDivComponent(responsive) {
  return responsive ? ResponsiveDiv : NonResponsiveDiv;
}
var RechartsWrapper = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
    children,
    className,
    height: heightFromProps,
    onClick,
    onContextMenu,
    onDoubleClick,
    onMouseDown,
    onMouseEnter,
    onMouseLeave,
    onMouseMove,
    onMouseUp,
    onTouchEnd,
    onTouchMove,
    onTouchStart,
    style,
    width: widthFromProps,
    responsive,
    dispatchTouchEvents = true
  } = props;
  var containerRef = (0,external_React_.useRef)(null);
  var dispatch = useAppDispatch();
  var [tooltipPortal, setTooltipPortal] = (0,external_React_.useState)(null);
  var [legendPortal, setLegendPortal] = (0,external_React_.useState)(null);
  var setScaleRef = useReportScale();
  var responsiveContainerCalculations = useResponsiveContainerContext();
  var width = (responsiveContainerCalculations === null || responsiveContainerCalculations === void 0 ? void 0 : responsiveContainerCalculations.width) > 0 ? responsiveContainerCalculations.width : widthFromProps;
  var height = (responsiveContainerCalculations === null || responsiveContainerCalculations === void 0 ? void 0 : responsiveContainerCalculations.height) > 0 ? responsiveContainerCalculations.height : heightFromProps;
  var innerRef = (0,external_React_.useCallback)(node => {
    setScaleRef(node);
    if (typeof ref === 'function') {
      ref(node);
    }
    setTooltipPortal(node);
    setLegendPortal(node);
    if (node != null) {
      containerRef.current = node;
    }
  }, [setScaleRef, ref, setTooltipPortal, setLegendPortal]);
  var myOnClick = (0,external_React_.useCallback)(e => {
    dispatch(mouseClickAction(e));
    dispatch(externalEventAction({
      handler: onClick,
      reactEvent: e
    }));
  }, [dispatch, onClick]);
  var myOnMouseEnter = (0,external_React_.useCallback)(e => {
    dispatch(mouseMoveAction(e));
    dispatch(externalEventAction({
      handler: onMouseEnter,
      reactEvent: e
    }));
  }, [dispatch, onMouseEnter]);
  var myOnMouseLeave = (0,external_React_.useCallback)(e => {
    dispatch(mouseLeaveChart());
    dispatch(externalEventAction({
      handler: onMouseLeave,
      reactEvent: e
    }));
  }, [dispatch, onMouseLeave]);
  var myOnMouseMove = (0,external_React_.useCallback)(e => {
    dispatch(mouseMoveAction(e));
    dispatch(externalEventAction({
      handler: onMouseMove,
      reactEvent: e
    }));
  }, [dispatch, onMouseMove]);
  var onFocus = (0,external_React_.useCallback)(() => {
    dispatch(focusAction());
  }, [dispatch]);
  var onKeyDown = (0,external_React_.useCallback)(e => {
    dispatch(keyDownAction(e.key));
  }, [dispatch]);
  var myOnContextMenu = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onContextMenu,
      reactEvent: e
    }));
  }, [dispatch, onContextMenu]);
  var myOnDoubleClick = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onDoubleClick,
      reactEvent: e
    }));
  }, [dispatch, onDoubleClick]);
  var myOnMouseDown = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onMouseDown,
      reactEvent: e
    }));
  }, [dispatch, onMouseDown]);
  var myOnMouseUp = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onMouseUp,
      reactEvent: e
    }));
  }, [dispatch, onMouseUp]);
  var myOnTouchStart = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onTouchStart,
      reactEvent: e
    }));
  }, [dispatch, onTouchStart]);

  /*
   * onTouchMove is special because it behaves different from mouse events.
   * Mouse events have 'enter' + 'leave' combo that notify us when the mouse is over
   * a certain element. Touch events don't have that; touch only gives us
   * start (finger down), end (finger up) and move (finger moving).
   * So we need to figure out which element the user is touching
   * ourselves. Fortunately, there's a convenient method for that:
   * https://developer.mozilla.org/en-US/docs/Web/API/Document/elementFromPoint
   */
  var myOnTouchMove = (0,external_React_.useCallback)(e => {
    if (dispatchTouchEvents) {
      dispatch(touchEventAction(e));
    }
    dispatch(externalEventAction({
      handler: onTouchMove,
      reactEvent: e
    }));
  }, [dispatch, dispatchTouchEvents, onTouchMove]);
  var myOnTouchEnd = (0,external_React_.useCallback)(e => {
    dispatch(externalEventAction({
      handler: onTouchEnd,
      reactEvent: e
    }));
  }, [dispatch, onTouchEnd]);
  var WrapperDiv = getWrapperDivComponent(responsive);
  return /*#__PURE__*/external_React_.createElement(TooltipPortalContext.Provider, {
    value: tooltipPortal
  }, /*#__PURE__*/external_React_.createElement(LegendPortalContext.Provider, {
    value: legendPortal
  }, /*#__PURE__*/external_React_.createElement(WrapperDiv, {
    width: width !== null && width !== void 0 ? width : style === null || style === void 0 ? void 0 : style.width,
    height: height !== null && height !== void 0 ? height : style === null || style === void 0 ? void 0 : style.height,
    className: clsx('recharts-wrapper', className),
    style: RechartsWrapper_objectSpread({
      position: 'relative',
      cursor: 'default',
      width,
      height
    }, style),
    onClick: myOnClick,
    onContextMenu: myOnContextMenu,
    onDoubleClick: myOnDoubleClick,
    onFocus: onFocus,
    onKeyDown: onKeyDown,
    onMouseDown: myOnMouseDown,
    onMouseEnter: myOnMouseEnter,
    onMouseLeave: myOnMouseLeave,
    onMouseMove: myOnMouseMove,
    onMouseUp: myOnMouseUp,
    onTouchEnd: myOnTouchEnd,
    onTouchMove: myOnTouchMove,
    onTouchStart: myOnTouchStart,
    ref: innerRef
  }, /*#__PURE__*/external_React_.createElement(EventSynchronizer, null), children)));
});
;// ./node_modules/recharts/es6/state/selectors/selectChartOffset.js


var selectChartOffset = createSelector([selectChartOffsetInternal], offsetInternal => {
  return {
    top: offsetInternal.top,
    bottom: offsetInternal.bottom,
    left: offsetInternal.left,
    right: offsetInternal.right
  };
});
;// ./node_modules/recharts/es6/state/selectors/selectPlotArea.js



var selectPlotArea = createSelector([selectChartOffset, selectChartWidth, selectChartHeight], (offset, chartWidth, chartHeight) => {
  if (!offset || chartWidth == null || chartHeight == null) {
    return undefined;
  }
  return {
    x: offset.left,
    y: offset.top,
    width: Math.max(0, chartWidth - offset.left - offset.right),
    height: Math.max(0, chartHeight - offset.top - offset.bottom)
  };
});
;// ./node_modules/recharts/es6/hooks.js







var useXAxis = xAxisId => {
  var isPanorama = useIsPanorama();
  return useAppSelector(state => selectAxisWithScale(state, 'xAxis', xAxisId, isPanorama));
};
var useYAxis = yAxisId => {
  var isPanorama = useIsPanorama();
  return useAppSelector(state => selectAxisWithScale(state, 'yAxis', yAxisId, isPanorama));
};

/**
 * Returns the active tooltip label. The label is one of the values from the chart data,
 * and is used to display in the tooltip content.
 *
 * Returns undefined if there is no active user interaction or if used outside a chart context
 *
 * @returns ActiveLabel
 */
var useActiveTooltipLabel = () => {
  return useAppSelector(selectActiveLabel);
};

/**
 * Returns the offset of the chart in pixels.
 *
 * Offset defines the blank space between the chart and the plot area.
 * This blank space is occupied by supporting elements like axes, legends, and brushes.
 *
 * The offset includes:
 *
 * - Margins
 * - Width and height of the axes
 * - Width and height of the legend
 * - Brush height
 *
 * If you are interested in the margin alone, use {@link useMargin} instead.
 *
 * The offset is independent of charts position on the page, meaning it does not change as the chart is scrolled or resized.
 *
 * It is also independent of the scale and zoom, meaning that as the user zooms in and out,
 * the numbers will not change as the chart gets visually larger or smaller.
 *
 * This hook must be used within a chart context (inside a `<LineChart>`, `<BarChart>`, etc.).
 * This hook returns `undefined` if used outside a chart context.
 *
 * @returns Offset of the chart in pixels, or undefined if used outside a chart context.
 */
var useOffset = () => {
  return useAppSelector(selectChartOffset);
};

/**
 * Plot area is the area where the actual chart data is rendered.
 * This means: bars, lines, scatter points, etc.
 *
 * The plot area is calculated based on the chart dimensions and the offset.
 *
 * Plot area `width` and `height` are the dimensions in pixels;
 * `x` and `y` are the coordinates of the top-left corner of the plot area relative to the chart container.
 *
 * They are also independent of the scale and zoom, meaning that as the user zooms in and out,
 * the plot area dimensions will not change as the chart gets visually larger or smaller.
 *
 * This hook must be used within a chart context (inside a `<LineChart>`, `<BarChart>`, etc.).
 * This hook returns `undefined` if used outside a chart context.
 *
 * @returns Plot area of the chart in pixels, or undefined if used outside a chart context.
 */
var usePlotArea = () => {
  return useAppSelector(selectPlotArea);
};

/**
 * Returns the currently active data points being displayed in the Tooltip.
 * Active means that it is currently visible; this hook will return `undefined` if there is no current interaction.
 *
 * This follows the `<Tooltip />` props, if the Tooltip element is present in the chart.
 * If there is no `<Tooltip />` then this hook will follow the default Tooltip props.
 *
 * Data point is whatever you pass as an input to the chart using the `data={}` prop.
 *
 * This returns an array because a chart can have multiple graphical items in it (multiple Lines for example)
 * and tooltip with `shared={true}` will display all items at the same time.
 *
 * Returns undefined when used outside a chart context.
 *
 * @returns Data points that are currently visible in a Tooltip
 */
var useActiveTooltipDataPoints = () => {
  return useAppSelector(selectActiveTooltipDataPoints);
};

/**
 * Returns the calculated domain of an X-axis.
 *
 * The domain can be numerical: `[min, max]`, or categorical: `['a', 'b', 'c']`.
 *
 * The type of the domain is defined by the `type` prop of the XAxis.
 *
 * The values of the domain are calculated based on the data and the `dataKey` of the axis.
 *
 * If the chart has a Brush, the domain will be filtered to the brushed indexes if the hook is used outside a Brush context,
 * and the full domain will be returned if the hook is used inside a Brush context.
 *
 * @param xAxisId The `xAxisId` of the X-axis. Defaults to `0` if not provided.
 * @returns The domain of the X-axis, or `undefined` if it cannot be calculated or if used outside a chart context.
 */
var useXAxisDomain = function useXAxisDomain() {
  var xAxisId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : defaultAxisId;
  var isPanorama = useIsPanorama();
  return useAppSelector(state => selectAxisDomain(state, 'xAxis', xAxisId, isPanorama));
};

/**
 * Returns the calculated domain of a Y-axis.
 *
 * The domain can be numerical: `[min, max]`, or categorical: `['a', 'b', 'c']`.
 *
 * The type of the domain is defined by the `type` prop of the YAxis.
 *
 * The values of the domain are calculated based on the data and the `dataKey` of the axis.
 *
 * Does not interact with Brushes, as Y-axes do not support brushing.
 *
 * @param yAxisId The `yAxisId` of the Y-axis. Defaults to `0` if not provided.
 * @returns The domain of the Y-axis, or `undefined` if it cannot be calculated or if used outside a chart context.
 */
var useYAxisDomain = function useYAxisDomain() {
  var yAxisId = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : defaultAxisId;
  var isPanorama = useIsPanorama();
  return useAppSelector(state => selectAxisDomain(state, 'yAxis', yAxisId, isPanorama));
};

/**
 * Returns true if the {@link Tooltip} is currently active (visible).
 *
 * Returns false if the Tooltip is not active or if used outside a chart context.
 *
 * Recharts only allows one Tooltip per chart, so this hook does not take any parameters.
 * Weird things may happen if you have multiple Tooltip components in the same chart so please don't do that.
 *
 * @returns {boolean} True if the Tooltip is active, false otherwise.
 * @since 3.7
 */
var useIsTooltipActive = () => {
  var _useAppSelector;
  return (_useAppSelector = useAppSelector(selectIsTooltipActive)) !== null && _useAppSelector !== void 0 ? _useAppSelector : false;
};

/**
 * Returns the Cartesian `x` + `y` coordinates of the active {@link Tooltip}.
 *
 * Returns undefined if there is no active user interaction or if used outside a chart context.
 *
 * Recharts only allows one Tooltip per chart, so this hook does not take any parameters.
 * Weird things may happen if you have multiple Tooltip components in the same chart so please don't do that.
 *
 * @returns {Coordinate | undefined} The coordinate of the active Tooltip, or undefined.
 * @since 3.7
 */
var useActiveTooltipCoordinate = () => {
  var coordinate = useAppSelector(selectActiveTooltipCoordinate);
  if (coordinate == null) {
    return undefined;
  }
  return {
    x: coordinate.x,
    y: coordinate.y
  };
};
;// ./node_modules/recharts/es6/container/ClipPathProvider.js




var ClipPathIdContext = /*#__PURE__*/(0,external_React_.createContext)(undefined);

/**
 * Generates a unique clip path ID for use in SVG elements,
 * and puts it in a context provider.
 *
 * To read the clip path ID, use the `useClipPathId` hook,
 * or render `<ClipPath>` component which will automatically use the ID from this context.
 *
 * @param props children - React children to be wrapped by the provider
 * @returns React Context Provider
 */
var ClipPathProvider = _ref => {
  var {
    children
  } = _ref;
  var [clipPathId] = (0,external_React_.useState)("".concat(uniqueId('recharts'), "-clip"));
  var plotArea = usePlotArea();
  if (plotArea == null) {
    return null;
  }
  var {
    x,
    y,
    width,
    height
  } = plotArea;
  return /*#__PURE__*/external_React_.createElement(ClipPathIdContext.Provider, {
    value: clipPathId
  }, /*#__PURE__*/external_React_.createElement("defs", null, /*#__PURE__*/external_React_.createElement("clipPath", {
    id: clipPathId
  }, /*#__PURE__*/external_React_.createElement("rect", {
    x: x,
    y: y,
    height: height,
    width: width
  }))), children);
};
var useClipPathId = () => {
  return (0,external_React_.useContext)(ClipPathIdContext);
};
;// ./node_modules/recharts/es6/chart/CategoricalChart.js
var CategoricalChart_excluded = ["width", "height", "responsive", "children", "className", "style", "compact", "title", "desc"];
function CategoricalChart_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = CategoricalChart_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function CategoricalChart_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }







var CategoricalChart = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
      width,
      height,
      responsive,
      children,
      className,
      style,
      compact,
      title,
      desc
    } = props,
    others = CategoricalChart_objectWithoutProperties(props, CategoricalChart_excluded);
  var attrs = svgPropertiesNoEvents(others);

  /*
   * The "compact" mode is used as the panorama within Brush.
   * However because `compact` is a public prop, let's assume that it can render outside of Brush too.
   */
  if (compact) {
    return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(ReportChartSize, {
      width: width,
      height: height
    }), /*#__PURE__*/external_React_.createElement(RootSurface, {
      otherAttributes: attrs,
      title: title,
      desc: desc
    }, children));
  }
  return /*#__PURE__*/external_React_.createElement(RechartsWrapper, {
    className: className,
    style: style,
    width: width,
    height: height,
    responsive: responsive !== null && responsive !== void 0 ? responsive : false,
    onClick: props.onClick,
    onMouseLeave: props.onMouseLeave,
    onMouseEnter: props.onMouseEnter,
    onMouseMove: props.onMouseMove,
    onMouseDown: props.onMouseDown,
    onMouseUp: props.onMouseUp,
    onContextMenu: props.onContextMenu,
    onDoubleClick: props.onDoubleClick,
    onTouchStart: props.onTouchStart,
    onTouchMove: props.onTouchMove,
    onTouchEnd: props.onTouchEnd
  }, /*#__PURE__*/external_React_.createElement(RootSurface, {
    otherAttributes: attrs,
    title: title,
    desc: desc,
    ref: ref
  }, /*#__PURE__*/external_React_.createElement(ClipPathProvider, null, children)));
});
;// ./node_modules/recharts/es6/chart/PolarChart.js
var PolarChart_excluded = ["layout"];
function PolarChart_extends() { return PolarChart_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, PolarChart_extends.apply(null, arguments); }
function PolarChart_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = PolarChart_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function PolarChart_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }









var defaultMargin = {
  top: 5,
  right: 5,
  bottom: 5,
  left: 5
};

/**
 * These default props are the same for all PolarChart components.
 */
var defaultPolarChartProps = {
  accessibilityLayer: true,
  stackOffset: 'none',
  barCategoryGap: '10%',
  barGap: 4,
  margin: defaultMargin,
  reverseStackOrder: false,
  syncMethod: 'index',
  layout: 'radial',
  responsive: false,
  cx: '50%',
  cy: '50%',
  innerRadius: 0,
  outerRadius: '80%'
};

/**
 * These props are required for the PolarChart to function correctly.
 * Users usually would not need to specify these explicitly,
 * because the convenience components like PieChart, RadarChart, etc.
 * will provide these defaults.
 * We can't have the defaults in this file because each of those convenience components
 * have their own opinions about what they should be.
 */

/**
 * These are one-time, immutable options that decide the chart's behavior.
 * Users who wish to call CartesianChart may decide to pass these options explicitly,
 * but usually we would expect that they use one of the convenience components like PieChart, RadarChart, etc.
 */

var PolarChart = /*#__PURE__*/(0,external_React_.forwardRef)(function PolarChart(props, ref) {
  var _polarChartProps$id;
  var polarChartProps = resolveDefaultProps(props.categoricalChartProps, defaultPolarChartProps);
  var {
      layout
    } = polarChartProps,
    otherCategoricalProps = PolarChart_objectWithoutProperties(polarChartProps, PolarChart_excluded);
  var {
    chartName,
    defaultTooltipEventType,
    validateTooltipEventTypes,
    tooltipPayloadSearcher
  } = props;
  var options = {
    chartName,
    defaultTooltipEventType,
    validateTooltipEventTypes,
    tooltipPayloadSearcher,
    eventEmitter: undefined
  };
  return /*#__PURE__*/external_React_.createElement(RechartsStoreProvider, {
    preloadedState: {
      options
    },
    reduxStoreName: (_polarChartProps$id = polarChartProps.id) !== null && _polarChartProps$id !== void 0 ? _polarChartProps$id : chartName
  }, /*#__PURE__*/external_React_.createElement(ChartDataContextProvider, {
    chartData: polarChartProps.data
  }), /*#__PURE__*/external_React_.createElement(ReportMainChartProps, {
    layout: layout,
    margin: polarChartProps.margin
  }), /*#__PURE__*/external_React_.createElement(ReportChartProps, {
    baseValue: undefined,
    accessibilityLayer: polarChartProps.accessibilityLayer,
    barCategoryGap: polarChartProps.barCategoryGap,
    maxBarSize: polarChartProps.maxBarSize,
    stackOffset: polarChartProps.stackOffset,
    barGap: polarChartProps.barGap,
    barSize: polarChartProps.barSize,
    syncId: polarChartProps.syncId,
    syncMethod: polarChartProps.syncMethod,
    className: polarChartProps.className,
    reverseStackOrder: polarChartProps.reverseStackOrder
  }), /*#__PURE__*/external_React_.createElement(ReportPolarOptions, {
    cx: polarChartProps.cx,
    cy: polarChartProps.cy,
    startAngle: polarChartProps.startAngle,
    endAngle: polarChartProps.endAngle,
    innerRadius: polarChartProps.innerRadius,
    outerRadius: polarChartProps.outerRadius
  }), /*#__PURE__*/external_React_.createElement(CategoricalChart, PolarChart_extends({}, otherCategoricalProps, {
    ref: ref
  })));
});
;// ./node_modules/recharts/es6/chart/RadarChart.js
function RadarChart_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function RadarChart_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? RadarChart_ownKeys(Object(t), !0).forEach(function (r) { RadarChart_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : RadarChart_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function RadarChart_defineProperty(e, r, t) { return (r = RadarChart_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function RadarChart_toPropertyKey(t) { var i = RadarChart_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function RadarChart_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }





var allowedTooltipTypes = ['axis'];
var defaultRadarChartProps = RadarChart_objectSpread(RadarChart_objectSpread({}, defaultPolarChartProps), {}, {
  layout: 'centric',
  startAngle: 90,
  endAngle: -270
});
/**
 * @consumes ResponsiveContainerContext
 * @provides PolarViewBoxContext
 * @provides PolarChartContext
 */
var RadarChart = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var propsWithDefaults = resolveDefaultProps(props, defaultRadarChartProps);
  return /*#__PURE__*/external_React_.createElement(PolarChart, {
    chartName: "RadarChart",
    defaultTooltipEventType: "axis",
    validateTooltipEventTypes: allowedTooltipTypes,
    tooltipPayloadSearcher: arrayTooltipSearcher,
    categoricalChartProps: propsWithDefaults,
    ref: ref
  });
});
;// ./node_modules/recharts/es6/state/selectors/polarSelectors.js









var selectUnfilteredPolarItems = state => state.graphicalItems.polarItems;
var polarSelectors_selectAxisPredicate = createSelector([pickAxisType, pickAxisId], itemAxisPredicate);
var selectPolarItemsSettings = createSelector([selectUnfilteredPolarItems, selectBaseAxis, polarSelectors_selectAxisPredicate], combineGraphicalItemsSettings);
var selectPolarGraphicalItemsData = createSelector([selectPolarItemsSettings], combineGraphicalItemsData);
var selectPolarDisplayedData = createSelector([selectPolarGraphicalItemsData, selectChartDataAndAlwaysIgnoreIndexes], combineDisplayedData);
var selectPolarAppliedValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings], combineAppliedValues);
var selectAllPolarAppliedNumericalValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings], (data, axisSettings, items) => {
  if (items.length > 0) {
    return data.flatMap(entry => {
      return items.flatMap(item => {
        var _axisSettings$dataKey;
        var valueByDataKey = getValueByDataKey(entry, (_axisSettings$dataKey = axisSettings.dataKey) !== null && _axisSettings$dataKey !== void 0 ? _axisSettings$dataKey : item.dataKey);
        return {
          value: valueByDataKey,
          errorDomain: [] // polar charts do not have error bars
        };
      });
    }).filter(Boolean);
  }
  if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null) {
    return data.map(item => ({
      value: getValueByDataKey(item, axisSettings.dataKey),
      errorDomain: []
    }));
  }
  return data.map(entry => ({
    value: entry,
    errorDomain: []
  }));
});
var unsupportedInPolarChart = () => undefined;
var selectDomainOfAllPolarAppliedNumericalValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings, selectAllErrorBarSettings, pickAxisType], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues);
var selectPolarNumericalDomain = createSelector([selectBaseAxis, selectDomainDefinition, selectDomainFromUserPreference, unsupportedInPolarChart, selectDomainOfAllPolarAppliedNumericalValues, unsupportedInPolarChart, selectChartLayout, pickAxisType], combineNumericalDomain);
var selectPolarAxisDomain = createSelector([selectBaseAxis, selectChartLayout, selectPolarDisplayedData, selectPolarAppliedValues, selectStackOffsetType, pickAxisType, selectPolarNumericalDomain], combineAxisDomain);
var selectPolarNiceTicks = createSelector([selectPolarAxisDomain, selectRenderableAxisSettings, selectRealScaleType], combineNiceTicks);
var selectPolarAxisDomainIncludingNiceTicks = createSelector([selectBaseAxis, selectPolarAxisDomain, selectPolarNiceTicks, pickAxisType], combineAxisDomainWithNiceTicks);
var selectPolarAxisCheckedDomain = createSelector([selectRealScaleType, selectPolarAxisDomainIncludingNiceTicks], combineCheckedDomain);
;// ./node_modules/recharts/es6/state/selectors/polarScaleSelectors.js






var selectPolarAxis = (state, axisType, axisId) => {
  switch (axisType) {
    case 'angleAxis':
      {
        return selectAngleAxis(state, axisId);
      }
    case 'radiusAxis':
      {
        return selectRadiusAxis(state, axisId);
      }
    default:
      {
        throw new Error("Unexpected axis type: ".concat(axisType));
      }
  }
};
var selectPolarAxisRangeWithReversed = (state, axisType, axisId) => {
  switch (axisType) {
    case 'angleAxis':
      {
        return selectAngleAxisRangeWithReversed(state, axisId);
      }
    case 'radiusAxis':
      {
        return selectRadiusAxisRangeWithReversed(state, axisId);
      }
    default:
      {
        throw new Error("Unexpected axis type: ".concat(axisType));
      }
  }
};
var selectPolarAxisScale = createSelector([selectPolarAxis, selectRealScaleType, selectPolarAxisCheckedDomain, selectPolarAxisRangeWithReversed], combineScaleFunction);
var selectPolarCategoricalDomain = createSelector([selectChartLayout, selectPolarAppliedValues, selectRenderableAxisSettings, pickAxisType], combineCategoricalDomain);
var selectPolarAxisTicks = createSelector([selectChartLayout, selectPolarAxis, selectRealScaleType, selectPolarAxisScale, selectPolarNiceTicks, selectPolarAxisRangeWithReversed, selectDuplicateDomain, selectPolarCategoricalDomain, pickAxisType], combineAxisTicks);
var selectPolarAngleAxisTicks = createSelector([selectPolarAxisTicks], ticks => {
  /*
   * Angle axis is circular; so here we need to look for ticks that overlap (i.e., 0 and 360 degrees)
   * and remove the duplicate tick to avoid rendering issues.
   */
  if (!ticks) {
    return undefined;
  }
  var uniqueTicksMap = new Map();
  ticks.forEach(tick => {
    var normalizedCoordinate = (tick.coordinate + 360) % 360;
    if (!uniqueTicksMap.has(normalizedCoordinate)) {
      uniqueTicksMap.set(normalizedCoordinate, tick);
    }
  });
  return Array.from(uniqueTicksMap.values());
});
var selectPolarGraphicalItemAxisTicks = createSelector([selectChartLayout, selectPolarAxis, selectPolarAxisScale, selectPolarAxisRangeWithReversed, selectDuplicateDomain, selectPolarCategoricalDomain, pickAxisType], combineGraphicalItemTicks);
;// ./node_modules/recharts/es6/state/selectors/polarGridSelectors.js


var selectAngleAxisTicks = (state, anglexisId) => selectPolarAxisTicks(state, 'angleAxis', anglexisId, false);
var selectPolarGridAngles = createSelector([selectAngleAxisTicks], ticks => {
  if (!ticks) {
    return undefined;
  }
  return ticks.map(tick => tick.coordinate);
});
var selectRadiusAxisTicks = (state, radiusAxisId) => selectPolarAxisTicks(state, 'radiusAxis', radiusAxisId, false);
var selectPolarGridRadii = createSelector([selectRadiusAxisTicks], ticks => {
  if (!ticks) {
    return undefined;
  }
  return ticks.map(tick => tick.coordinate);
});
;// external "ReactDOM"
const external_ReactDOM_namespaceObject = ReactDOM;
;// ./node_modules/recharts/es6/zIndex/ZIndexLayer.js









/**
 * @since 3.4
 */

/**
 * A layer that renders its children into a portal corresponding to the given zIndex.
 * We can't use regular CSS `z-index` because SVG does not support it.
 * So instead, we create separate DOM nodes for each zIndex layer
 * and render the children into the corresponding DOM node using React portals.
 *
 * This component must be used inside a Chart component.
 *
 * @param zIndex numeric zIndex value, higher values are rendered on top of lower values
 * @param children the content to render inside this zIndex layer
 *
 * @since 3.4
 */
function ZIndexLayer(_ref) {
  var {
    zIndex,
    children
  } = _ref;
  /*
   * If we are outside of chart, then we can't rely on the zIndex portal state,
   * so we just render normally.
   */
  var isInChartContext = useIsInChartContext();
  /*
   * If zIndex is undefined then we render normally without portals.
   * Also, if zIndex is 0, we render normally without portals,
   * because 0 is the default layer that does not need a portal.
   */
  var shouldRenderInPortal = isInChartContext && zIndex !== undefined && zIndex !== 0;
  var isPanorama = useIsPanorama();
  var dispatch = useAppDispatch();
  (0,external_React_.useLayoutEffect)(() => {
    if (!shouldRenderInPortal) {
      // Nothing to do. We have to call the hook because of the rules of hooks.
      return noop;
    }
    /*
     * Because zIndexes are dynamic (meaning, we're not working with a predefined set of layers,
     * but we allow users to define any zIndex at any time), we need to register
     * the requested zIndex in the global store. This way, the ZIndexPortals component
     * can render the corresponding portals and only the requested ones.
     */
    dispatch(registerZIndexPortal({
      zIndex
    }));
    return () => {
      dispatch(unregisterZIndexPortal({
        zIndex
      }));
    };
  }, [dispatch, zIndex, shouldRenderInPortal]);
  var portalElement = useAppSelector(state => selectZIndexPortalElement(state, zIndex, isPanorama));
  if (!shouldRenderInPortal) {
    // If no zIndex is provided or zIndex is 0, render normally without portals
    return children;
  }
  if (!portalElement) {
    /*
     * If we don't have a portal element yet, this means that the registration
     * has not been processed yet by the ZIndexPortals component.
     * So here we render null and wait for the next render cycle.
     */
    return null;
  }
  return /*#__PURE__*/(0,external_ReactDOM_namespaceObject.createPortal)(children, portalElement);
}
;// ./node_modules/recharts/es6/polar/PolarGrid.js
var PolarGrid_excluded = ["gridType", "radialLines", "angleAxisId", "radiusAxisId", "cx", "cy", "innerRadius", "outerRadius", "polarAngles", "polarRadius", "zIndex"];
function PolarGrid_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = PolarGrid_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function PolarGrid_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function PolarGrid_extends() { return PolarGrid_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, PolarGrid_extends.apply(null, arguments); }
function PolarGrid_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function PolarGrid_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? PolarGrid_ownKeys(Object(t), !0).forEach(function (r) { PolarGrid_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : PolarGrid_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function PolarGrid_defineProperty(e, r, t) { return (r = PolarGrid_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function PolarGrid_toPropertyKey(t) { var i = PolarGrid_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function PolarGrid_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }










var getPolygonPath = (radius, cx, cy, polarAngles) => {
  var path = '';
  polarAngles.forEach((angle, i) => {
    var point = polarToCartesian(cx, cy, radius, angle);
    if (i) {
      path += "L ".concat(point.x, ",").concat(point.y);
    } else {
      path += "M ".concat(point.x, ",").concat(point.y);
    }
  });
  path += 'Z';
  return path;
};

// Draw axis of radial line
var PolarAngles = props => {
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    polarAngles,
    radialLines
  } = props;
  if (!polarAngles || !polarAngles.length || !radialLines) {
    return null;
  }
  var polarAnglesProps = PolarGrid_objectSpread({
    stroke: '#ccc'
  }, svgPropertiesNoEvents(props));
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-polar-grid-angle"
  }, polarAngles.map(entry => {
    var start = polarToCartesian(cx, cy, innerRadius, entry);
    var end = polarToCartesian(cx, cy, outerRadius, entry);
    return /*#__PURE__*/external_React_.createElement("line", PolarGrid_extends({
      key: "line-".concat(entry)
    }, polarAnglesProps, {
      x1: start.x,
      y1: start.y,
      x2: end.x,
      y2: end.y
    }));
  }));
};

// Draw concentric circles
var ConcentricCircle = props => {
  var {
    cx,
    cy,
    radius
  } = props;
  var concentricCircleProps = PolarGrid_objectSpread({
    stroke: '#ccc',
    fill: 'none'
  }, svgPropertiesNoEvents(props));
  return (
    /*#__PURE__*/
    // @ts-expect-error wrong SVG element type
    external_React_.createElement("circle", PolarGrid_extends({}, concentricCircleProps, {
      className: clsx('recharts-polar-grid-concentric-circle', props.className),
      cx: cx,
      cy: cy,
      r: radius
    }))
  );
};

// Draw concentric polygons
var ConcentricPolygon = props => {
  var {
    radius
  } = props;
  var concentricPolygonProps = PolarGrid_objectSpread({
    stroke: '#ccc',
    fill: 'none'
  }, svgPropertiesNoEvents(props));
  return /*#__PURE__*/external_React_.createElement("path", PolarGrid_extends({}, concentricPolygonProps, {
    className: clsx('recharts-polar-grid-concentric-polygon', props.className),
    d: getPolygonPath(radius, props.cx, props.cy, props.polarAngles)
  }));
};

// Draw concentric axis
var ConcentricGridPath = props => {
  var {
    polarRadius,
    gridType
  } = props;
  if (!polarRadius || !polarRadius.length) {
    return null;
  }
  var maxPolarRadius = Math.max(...polarRadius);
  var renderBackground = props.fill && props.fill !== 'none';
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-polar-grid-concentric"
  }, renderBackground && gridType === 'circle' && /*#__PURE__*/external_React_.createElement(ConcentricCircle, PolarGrid_extends({}, props, {
    radius: maxPolarRadius
  })), renderBackground && gridType !== 'circle' && /*#__PURE__*/external_React_.createElement(ConcentricPolygon, PolarGrid_extends({}, props, {
    radius: maxPolarRadius
  })), polarRadius.map((entry, i) => {
    var key = i;
    if (gridType === 'circle') {
      return /*#__PURE__*/external_React_.createElement(ConcentricCircle, PolarGrid_extends({
        key: key
      }, props, {
        fill: "none",
        radius: entry
      }));
    }
    return /*#__PURE__*/external_React_.createElement(ConcentricPolygon, PolarGrid_extends({
      key: key
    }, props, {
      fill: "none",
      radius: entry
    }));
  }));
};
var defaultPolarGridProps = {
  angleAxisId: 0,
  radiusAxisId: 0,
  gridType: 'polygon',
  radialLines: true,
  zIndex: DefaultZIndexes.grid
};

/**
 * @consumes PolarViewBoxContext
 */
var PolarGrid = outsideProps => {
  var _ref, _polarViewBox$cx, _ref2, _polarViewBox$cy, _ref3, _polarViewBox$innerRa, _ref4, _polarViewBox$outerRa;
  var _resolveDefaultProps = resolveDefaultProps(outsideProps, defaultPolarGridProps),
    {
      gridType,
      radialLines,
      angleAxisId,
      radiusAxisId,
      cx: cxFromOutside,
      cy: cyFromOutside,
      innerRadius: innerRadiusFromOutside,
      outerRadius: outerRadiusFromOutside,
      polarAngles: polarAnglesInput,
      polarRadius: polarRadiusInput,
      zIndex
    } = _resolveDefaultProps,
    inputs = PolarGrid_objectWithoutProperties(_resolveDefaultProps, PolarGrid_excluded);
  var polarViewBox = useAppSelector(selectPolarViewBox);
  var polarAnglesFromRedux = useAppSelector(state => selectPolarGridAngles(state, angleAxisId));
  var polarRadiiFromRedux = useAppSelector(state => selectPolarGridRadii(state, radiusAxisId));
  var polarAngles = Array.isArray(polarAnglesInput) ? polarAnglesInput : polarAnglesFromRedux;
  var polarRadius = Array.isArray(polarRadiusInput) ? polarRadiusInput : polarRadiiFromRedux;
  if (polarAngles == null || polarRadius == null) {
    return null;
  }
  var props = PolarGrid_objectSpread({
    cx: (_ref = (_polarViewBox$cx = polarViewBox === null || polarViewBox === void 0 ? void 0 : polarViewBox.cx) !== null && _polarViewBox$cx !== void 0 ? _polarViewBox$cx : cxFromOutside) !== null && _ref !== void 0 ? _ref : 0,
    cy: (_ref2 = (_polarViewBox$cy = polarViewBox === null || polarViewBox === void 0 ? void 0 : polarViewBox.cy) !== null && _polarViewBox$cy !== void 0 ? _polarViewBox$cy : cyFromOutside) !== null && _ref2 !== void 0 ? _ref2 : 0,
    innerRadius: (_ref3 = (_polarViewBox$innerRa = polarViewBox === null || polarViewBox === void 0 ? void 0 : polarViewBox.innerRadius) !== null && _polarViewBox$innerRa !== void 0 ? _polarViewBox$innerRa : innerRadiusFromOutside) !== null && _ref3 !== void 0 ? _ref3 : 0,
    outerRadius: (_ref4 = (_polarViewBox$outerRa = polarViewBox === null || polarViewBox === void 0 ? void 0 : polarViewBox.outerRadius) !== null && _polarViewBox$outerRa !== void 0 ? _polarViewBox$outerRa : outerRadiusFromOutside) !== null && _ref4 !== void 0 ? _ref4 : 0,
    polarAngles,
    polarRadius,
    zIndex
  }, inputs);
  var {
    outerRadius
  } = props;
  if (outerRadius <= 0) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-polar-grid"
  }, /*#__PURE__*/external_React_.createElement(ConcentricGridPath, PolarGrid_extends({
    gridType: gridType,
    radialLines: radialLines
  }, props, {
    polarAngles: polarAngles,
    polarRadius: polarRadius
  })), /*#__PURE__*/external_React_.createElement(PolarAngles, PolarGrid_extends({
    gridType: gridType,
    radialLines: radialLines
  }, props, {
    polarAngles: polarAngles,
    polarRadius: polarRadius
  }))));
};
PolarGrid.displayName = 'PolarGrid';
;// ./node_modules/recharts/es6/container/Layer.js
var Layer_excluded = ["children", "className"];
function Layer_extends() { return Layer_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Layer_extends.apply(null, arguments); }
function Layer_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Layer_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Layer_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }



/**
 * Creates an SVG group element to group other SVG elements.
 *
 * Useful if you want to apply transformations or styles to a set of elements
 * without affecting other elements in the SVG.
 *
 * @link https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/g
 */
var Layer = /*#__PURE__*/external_React_.forwardRef((props, ref) => {
  var {
      children,
      className
    } = props,
    others = Layer_objectWithoutProperties(props, Layer_excluded);
  var layerClass = clsx('recharts-layer', className);
  return /*#__PURE__*/external_React_.createElement("g", Layer_extends({
    className: layerClass
  }, svgPropertiesAndEvents(others), {
    ref: ref
  }), children);
});
;// ./node_modules/recharts/es6/util/types.js



/**
 * Determines how values are stacked:
 *
 * - `none` is the default, it adds values on top of each other. No smarts. Negative values will overlap.
 * - `expand` make it so that the values always add up to 1 - so the chart will look like a rectangle.
 * - `wiggle` and `silhouette` tries to keep the chart centered.
 * - `sign` stacks positive values above zero and negative values below zero. Similar to `none` but handles negatives.
 * - `positive` ignores all negative values, and then behaves like \`none\`.
 *
 * @see {@link https://d3js.org/d3-shape/stack#stack-offsets}
 * (note that the `diverging` offset in d3 is named `sign` in recharts)
 *
 * @inline
 */

/**
 * @deprecated use either `CartesianLayout` or `PolarLayout` instead.
 * Mixing both charts families leads to ambiguity in the type system.
 * These two layouts share very few properties, so it is best to keep them separate.
 */

/**
 * The type of axis.
 *
 * `category`: Treats data as distinct values.
 * Each value is in the same distance from its neighbors, regardless of their actual numeric difference.
 *
 * `number`: Treats data as continuous range.
 * Values that are numerically closer are placed closer together on the axis.
 *
 * `auto`: the type is inferred based on the chart layout.
 *
 * This is external type - users will provide this type in props.
 * Internally we will evaluate it to either 'category' or 'number' based on the layout,
 * before sending it to the store.
 *
 * @inline
 */

/**
 * Individual axes are responsible for resolving the 'auto' type to either 'number' or 'category',
 * based on the chart layout and axis kind. Then they can start using this type.
 */

/**
 * Extracts values from data objects.
 *
 * @inline
 */

/**
 * @inline
 */

/**
 * @inline
 */

/**
 * @deprecated do not use: too many properties, mixing too many concepts, cartesian and polar together, everything optional.
 * Instead, use either `Coordinate` or `PolarCoordinate`.
 */

var isPolarCoordinate = c => {
  return 'radius' in c && 'startAngle' in c && 'endAngle' in c;
};

/**
 * String shortcuts for scale types.
 * In case none of these does what you want you can also provide your own scale function
 * @see {@link CustomScaleDefinition}
 */

//
// Event Handler Types -- Copied from @types/react/index.d.ts and adapted for Props.
//

/**
 * The type of easing function to use for animations
 *
 * @inline
 */

/** Specifies the duration of animation, the unit of this option is ms. */

/**
 * This object defines the offset of the chart area and width and height and brush and ... it's a bit too much information all in one.
 * We use it internally but let's not expose it to the outside world.
 * If you are looking for this information, instead import `ChartOffset` or `PlotArea` from `recharts`.
 */

/**
 * The domain of axis.
 * This is the definition
 *
 * Numeric domain is always defined by an array of exactly two values, for the min and the max of the axis.
 * Categorical domain is defined as array of all possible values.
 *
 * Can be specified in many ways:
 * - array of numbers
 * - with special strings like 'dataMin' and 'dataMax'
 * - with special string math like 'dataMin - 100'
 * - with keyword 'auto'
 * - or a function
 * - array of functions
 * - or a combination of the above
 */

/**
 * NumberDomain is an evaluated {@link AxisDomain}.
 * Unlike {@link AxisDomain}, it has no variety - it's a tuple of two number.
 * This is after all the keywords and functions were evaluated and what is left is [min, max].
 *
 * Know that the min, max values are not guaranteed to be nice numbers - values like -Infinity or NaN are possible.
 *
 * There are also `category` axes that have different things than numbers in their domain.
 */

/**
 * Props shared in all renderable axes - meaning the ones that are drawn on the chart,
 * can have ticks, axis line, etc.
 */

/** Defines how ticks are placed and whether / how tick collisions are handled.
 * 'preserveStart' keeps the left tick on collision and ensures that the first tick is always shown.
 * 'preserveEnd' keeps the right tick on collision and ensures that the last tick is always shown.
 * 'preserveStartEnd' keeps the left tick on collision and ensures that the first and last ticks always show.
 * 'equidistantPreserveStart' selects a number N such that every nTh tick will be shown without collision.
 * 'equidistantPreserveEnd' selects a number N such that every nTh tick will be shown, ensuring the last tick is always visible.
 */

/**
 * Ticks can be any type when the axis is the type of category.
 *
 * Ticks must be numbers when the axis is the type of number.
 */

/**
 * @inline
 */

/**
 * @inline
 */

var adaptEventHandlers = (props, newHandler) => {
  if (!props || typeof props === 'function' || typeof props === 'boolean') {
    return null;
  }
  var inputProps = props;
  if (/*#__PURE__*/(0,external_React_.isValidElement)(props)) {
    inputProps = props.props;
  }
  if (typeof inputProps !== 'object' && typeof inputProps !== 'function') {
    return null;
  }
  var out = {};
  Object.keys(inputProps).forEach(key => {
    if (isEventKey(key)) {
      out[key] = newHandler || (e => inputProps[key](inputProps, e));
    }
  });
  return out;
};
var getEventHandlerOfChild = (originalHandler, data, index) => e => {
  originalHandler(data, index, e);
  return null;
};
var adaptEventsOfChild = (props, data, index) => {
  if (props === null || typeof props !== 'object' && typeof props !== 'function') {
    return null;
  }
  var out = null;
  Object.keys(props).forEach(key => {
    var item = props[key];
    if (isEventKey(key) && typeof item === 'function') {
      if (!out) out = {};
      out[key] = getEventHandlerOfChild(item, data, index);
    }
  });
  return out;
};

/**
 * 'axis' means that all graphical items belonging to this axis tick will be highlighted,
 * and all will be present in the tooltip.
 * Tooltip with 'axis' will display when hovering on the chart background.
 *
 * 'item' means only the one graphical item being hovered will show in the tooltip.
 * Tooltip with 'item' will display when hovering over individual graphical items.
 *
 * This is calculated internally;
 * charts have a `defaultTooltipEventType` and `validateTooltipEventTypes` options.
 *
 * Users then use <Tooltip shared={true} /> or <Tooltip shared={false} /> to control their preference,
 * and charts will then see what is allowed and what is not.
 */

/**
 * These are the props we are going to pass to an `activeDot` or `dot` if it is a function or a custom Component
 */

/**
 * This is the type of `activeDot` prop on:
 * - Area
 * - Line
 * - Radar
 *
 * @inline
 */

/**
 * Inside the dot event handlers we provide extra information about the dot point
 * that the Dot component itself does not need but users might find useful.
 */

/**
 * This is the type of `dot` prop on:
 * - Area
 * - Line
 * - Radar
 *
 * @inline
 */

/**
 * Simplified version of the MouseEvent so that we don't have to mock the whole thing in tests.
 *
 * This is meant to represent the React.MouseEvent
 * which is a wrapper on top of https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent
 */

/**
 * Coordinates relative to the top-left corner of the chart.
 * Also include scale which means that a chart that's scaled will return the same coordinates as a chart that's not scaled.
 */

/**
 * Props shared with all charts.
 */

var isNonEmptyArray = arr => {
  return Array.isArray(arr) && arr.length > 0;
};
;// ./node_modules/recharts/es6/shape/Dot.js
function Dot_extends() { return Dot_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Dot_extends.apply(null, arguments); }





/**
 * Renders a dot in the chart.
 *
 * This component accepts X and Y coordinates in pixels.
 * If you need to position the rectangle based on your chart's data,
 * consider using the {@link ReferenceDot} component instead.
 *
 * @param props
 * @constructor
 */
var Dot = props => {
  var {
    cx,
    cy,
    r,
    className
  } = props;
  var layerClass = clsx('recharts-dot', className);
  if (isNumber(cx) && isNumber(cy) && isNumber(r)) {
    return /*#__PURE__*/external_React_.createElement("circle", Dot_extends({}, svgPropertiesNoEvents(props), adaptEventHandlers(props), {
      className: layerClass,
      cx: cx,
      cy: cy,
      r: r
    }));
  }
  return null;
};
;// ./node_modules/recharts/es6/shape/Polygon.js
var Polygon_excluded = ["points", "className", "baseLinePoints", "connectNulls"];
var _templateObject;
function Polygon_extends() { return Polygon_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Polygon_extends.apply(null, arguments); }
function Polygon_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Polygon_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Polygon_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function _taggedTemplateLiteral(e, t) { return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } })); }
/**
 * @fileOverview Polygon
 */




var isValidatePoint = point => {
  return point != null && point.x === +point.x && point.y === +point.y;
};
var getParsedPoints = function getParsedPoints() {
  var points = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var segmentPoints = [[]];
  points.forEach(entry => {
    var lastLink = segmentPoints[segmentPoints.length - 1];
    if (isValidatePoint(entry)) {
      if (lastLink) {
        lastLink.push(entry);
      }
    } else if (lastLink && lastLink.length > 0) {
      // add another path
      segmentPoints.push([]);
    }
  });
  var firstPoint = points[0];
  var lastLink = segmentPoints[segmentPoints.length - 1];
  if (isValidatePoint(firstPoint) && lastLink) {
    lastLink.push(firstPoint);
  }
  var finalLink = segmentPoints[segmentPoints.length - 1];
  if (finalLink && finalLink.length <= 0) {
    segmentPoints = segmentPoints.slice(0, -1);
  }
  return segmentPoints;
};
var getSinglePolygonPath = (points, connectNulls) => {
  var segmentPoints = getParsedPoints(points);
  if (connectNulls) {
    segmentPoints = [segmentPoints.reduce((res, segPoints) => {
      return [...res, ...segPoints];
    }, [])];
  }
  var polygonPath = segmentPoints.map(segPoints => {
    return segPoints.reduce((path, point, index) => {
      return roundTemplateLiteral(_templateObject || (_templateObject = _taggedTemplateLiteral(["", "", "", ",", ""])), path, index === 0 ? 'M' : 'L', point.x, point.y);
    }, '');
  }).join('');
  return segmentPoints.length === 1 ? "".concat(polygonPath, "Z") : polygonPath;
};
var getRanglePath = (points, baseLinePoints, connectNulls) => {
  var outerPath = getSinglePolygonPath(points, connectNulls);
  return "".concat(outerPath.slice(-1) === 'Z' ? outerPath.slice(0, -1) : outerPath, "L").concat(getSinglePolygonPath(Array.from(baseLinePoints).reverse(), connectNulls).slice(1));
};
var Polygon = props => {
  var {
      points,
      className,
      baseLinePoints,
      connectNulls
    } = props,
    others = Polygon_objectWithoutProperties(props, Polygon_excluded);
  if (!points || !points.length) {
    return null;
  }
  var layerClass = clsx('recharts-polygon', className);
  if (baseLinePoints && baseLinePoints.length) {
    var hasStroke = others.stroke && others.stroke !== 'none';
    var rangePath = getRanglePath(points, baseLinePoints, connectNulls);
    return /*#__PURE__*/external_React_.createElement("g", {
      className: layerClass
    }, /*#__PURE__*/external_React_.createElement("path", Polygon_extends({}, svgPropertiesAndEvents(others), {
      fill: rangePath.slice(-1) === 'Z' ? others.fill : 'none',
      stroke: "none",
      d: rangePath
    })), hasStroke ? /*#__PURE__*/external_React_.createElement("path", Polygon_extends({}, svgPropertiesAndEvents(others), {
      fill: "none",
      d: getSinglePolygonPath(points, connectNulls)
    })) : null, hasStroke ? /*#__PURE__*/external_React_.createElement("path", Polygon_extends({}, svgPropertiesAndEvents(others), {
      fill: "none",
      d: getSinglePolygonPath(baseLinePoints, connectNulls)
    })) : null);
  }
  var singlePath = getSinglePolygonPath(points, connectNulls);
  return /*#__PURE__*/external_React_.createElement("path", Polygon_extends({}, svgPropertiesAndEvents(others), {
    fill: singlePath.slice(-1) === 'Z' ? others.fill : 'none',
    className: layerClass,
    d: singlePath
  }));
};
;// ./node_modules/recharts/es6/util/LRUCache.js
function LRUCache_defineProperty(e, r, t) { return (r = LRUCache_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function LRUCache_toPropertyKey(t) { var i = LRUCache_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function LRUCache_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * Simple LRU (Least Recently Used) cache implementation
 */
class LRUCache {
  constructor(maxSize) {
    LRUCache_defineProperty(this, "cache", new Map());
    this.maxSize = maxSize;
  }
  get(key) {
    var value = this.cache.get(key);
    if (value !== undefined) {
      this.cache.delete(key);
      this.cache.set(key, value);
    }
    return value;
  }
  set(key, value) {
    if (this.cache.has(key)) {
      this.cache.delete(key);
    } else if (this.cache.size >= this.maxSize) {
      var firstKey = this.cache.keys().next().value;
      if (firstKey != null) {
        this.cache.delete(firstKey);
      }
    }
    this.cache.set(key, value);
  }
  clear() {
    this.cache.clear();
  }
  size() {
    return this.cache.size;
  }
}
;// ./node_modules/recharts/es6/util/DOMUtils.js
function DOMUtils_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function DOMUtils_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? DOMUtils_ownKeys(Object(t), !0).forEach(function (r) { DOMUtils_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : DOMUtils_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function DOMUtils_defineProperty(e, r, t) { return (r = DOMUtils_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function DOMUtils_toPropertyKey(t) { var i = DOMUtils_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function DOMUtils_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


var defaultConfig = {
  cacheSize: 2000,
  enableCache: true
};
var currentConfig = DOMUtils_objectSpread({}, defaultConfig);
var stringCache = new LRUCache(currentConfig.cacheSize);
var SPAN_STYLE = {
  position: 'absolute',
  top: '-20000px',
  left: 0,
  padding: 0,
  margin: 0,
  border: 'none',
  whiteSpace: 'pre'
};
var MEASUREMENT_SPAN_ID = 'recharts_measurement_span';
function createCacheKey(text, style) {
  // Simple string concatenation for better performance than JSON.stringify
  var fontSize = style.fontSize || '';
  var fontFamily = style.fontFamily || '';
  var fontWeight = style.fontWeight || '';
  var fontStyle = style.fontStyle || '';
  var letterSpacing = style.letterSpacing || '';
  var textTransform = style.textTransform || '';
  return "".concat(text, "|").concat(fontSize, "|").concat(fontFamily, "|").concat(fontWeight, "|").concat(fontStyle, "|").concat(letterSpacing, "|").concat(textTransform);
}

/**
 * Measure text using DOM (accurate but slower)
 * @param text - The text to measure
 * @param style - CSS style properties to apply
 * @returns The size of the text
 */
var measureTextWithDOM = (text, style) => {
  try {
    var measurementSpan = document.getElementById(MEASUREMENT_SPAN_ID);
    if (!measurementSpan) {
      measurementSpan = document.createElement('span');
      measurementSpan.setAttribute('id', MEASUREMENT_SPAN_ID);
      measurementSpan.setAttribute('aria-hidden', 'true');
      document.body.appendChild(measurementSpan);
    }

    // Apply styles directly without unnecessary object creation
    Object.assign(measurementSpan.style, SPAN_STYLE, style);
    measurementSpan.textContent = "".concat(text);
    var rect = measurementSpan.getBoundingClientRect();
    return {
      width: rect.width,
      height: rect.height
    };
  } catch (_unused) {
    return {
      width: 0,
      height: 0
    };
  }
};
var getStringSize = function getStringSize(text) {
  var style = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  if (text === undefined || text === null || Global.isSsr) {
    return {
      width: 0,
      height: 0
    };
  }

  // If caching is disabled, measure directly
  if (!currentConfig.enableCache) {
    return measureTextWithDOM(text, style);
  }
  var cacheKey = createCacheKey(text, style);
  var cachedResult = stringCache.get(cacheKey);
  if (cachedResult) {
    return cachedResult;
  }

  // Measure using DOM
  var result = measureTextWithDOM(text, style);

  // Store in LRU cache
  stringCache.set(cacheKey, result);
  return result;
};

/**
 * Configure text measurement behavior
 * @param config - Partial configuration to apply
 * @returns void
 */
var configureTextMeasurement = config => {
  var newConfig = DOMUtils_objectSpread(DOMUtils_objectSpread({}, currentConfig), config);
  if (newConfig.cacheSize !== currentConfig.cacheSize) {
    stringCache = new LRUCache(newConfig.cacheSize);
  }
  currentConfig = newConfig;
};

/**
 * Get current text measurement configuration
 * @returns Current configuration
 */
var getTextMeasurementConfig = () => DOMUtils_objectSpread({}, currentConfig);

/**
 * Clear the string size cache. Useful for testing or memory management.
 * @returns void
 */
var clearStringCache = () => {
  stringCache.clear();
};

/**
 * Get cache statistics for debugging purposes.
 * @returns Cache statistics including size and max size
 */
var getStringCacheStats = () => ({
  size: stringCache.size(),
  maxSize: currentConfig.cacheSize
});
;// ./node_modules/recharts/es6/util/ReduceCSSCalc.js
var _DecimalCSS;
function ReduceCSSCalc_defineProperty(e, r, t) { return (r = ReduceCSSCalc_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function ReduceCSSCalc_toPropertyKey(t) { var i = ReduceCSSCalc_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function ReduceCSSCalc_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }

var MULTIPLY_OR_DIVIDE_REGEX = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([*/])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/;
var ADD_OR_SUBTRACT_REGEX = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([+-])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/;
var CSS_LENGTH_UNIT_REGEX = /^px|cm|vh|vw|em|rem|%|mm|in|pt|pc|ex|ch|vmin|vmax|Q$/;
var NUM_SPLIT_REGEX = /(-?\d+(?:\.\d+)?)([a-zA-Z%]+)?/;
var CONVERSION_RATES = {
  cm: 96 / 2.54,
  mm: 96 / 25.4,
  pt: 96 / 72,
  pc: 96 / 6,
  in: 96,
  Q: 96 / (2.54 * 40),
  px: 1
};
var FIXED_CSS_LENGTH_UNITS = ['cm', 'mm', 'pt', 'pc', 'in', 'Q', 'px'];
function isSupportedUnit(unit) {
  return FIXED_CSS_LENGTH_UNITS.includes(unit);
}
var STR_NAN = 'NaN';
function convertToPx(value, unit) {
  return value * CONVERSION_RATES[unit];
}
class DecimalCSS {
  static parse(str) {
    var _NUM_SPLIT_REGEX$exec;
    var [, numStr, unit] = (_NUM_SPLIT_REGEX$exec = NUM_SPLIT_REGEX.exec(str)) !== null && _NUM_SPLIT_REGEX$exec !== void 0 ? _NUM_SPLIT_REGEX$exec : [];
    if (numStr == null) {
      return DecimalCSS.NaN;
    }
    return new DecimalCSS(parseFloat(numStr), unit !== null && unit !== void 0 ? unit : '');
  }
  constructor(num, unit) {
    this.num = num;
    this.unit = unit;
    this.num = num;
    this.unit = unit;
    if (isNan(num)) {
      this.unit = '';
    }
    if (unit !== '' && !CSS_LENGTH_UNIT_REGEX.test(unit)) {
      this.num = NaN;
      this.unit = '';
    }
    if (isSupportedUnit(unit)) {
      this.num = convertToPx(num, unit);
      this.unit = 'px';
    }
  }
  add(other) {
    if (this.unit !== other.unit) {
      return new DecimalCSS(NaN, '');
    }
    return new DecimalCSS(this.num + other.num, this.unit);
  }
  subtract(other) {
    if (this.unit !== other.unit) {
      return new DecimalCSS(NaN, '');
    }
    return new DecimalCSS(this.num - other.num, this.unit);
  }
  multiply(other) {
    if (this.unit !== '' && other.unit !== '' && this.unit !== other.unit) {
      return new DecimalCSS(NaN, '');
    }
    return new DecimalCSS(this.num * other.num, this.unit || other.unit);
  }
  divide(other) {
    if (this.unit !== '' && other.unit !== '' && this.unit !== other.unit) {
      return new DecimalCSS(NaN, '');
    }
    return new DecimalCSS(this.num / other.num, this.unit || other.unit);
  }
  toString() {
    return "".concat(this.num).concat(this.unit);
  }
  isNaN() {
    return isNan(this.num);
  }
}
_DecimalCSS = DecimalCSS;
ReduceCSSCalc_defineProperty(DecimalCSS, "NaN", new _DecimalCSS(NaN, ''));
function calculateArithmetic(expr) {
  if (expr == null || expr.includes(STR_NAN)) {
    return STR_NAN;
  }
  var newExpr = expr;
  while (newExpr.includes('*') || newExpr.includes('/')) {
    var _MULTIPLY_OR_DIVIDE_R;
    var [, leftOperand, operator, rightOperand] = (_MULTIPLY_OR_DIVIDE_R = MULTIPLY_OR_DIVIDE_REGEX.exec(newExpr)) !== null && _MULTIPLY_OR_DIVIDE_R !== void 0 ? _MULTIPLY_OR_DIVIDE_R : [];
    var lTs = DecimalCSS.parse(leftOperand !== null && leftOperand !== void 0 ? leftOperand : '');
    var rTs = DecimalCSS.parse(rightOperand !== null && rightOperand !== void 0 ? rightOperand : '');
    var result = operator === '*' ? lTs.multiply(rTs) : lTs.divide(rTs);
    if (result.isNaN()) {
      return STR_NAN;
    }
    newExpr = newExpr.replace(MULTIPLY_OR_DIVIDE_REGEX, result.toString());
  }
  while (newExpr.includes('+') || /.-\d+(?:\.\d+)?/.test(newExpr)) {
    var _ADD_OR_SUBTRACT_REGE;
    var [, _leftOperand, _operator, _rightOperand] = (_ADD_OR_SUBTRACT_REGE = ADD_OR_SUBTRACT_REGEX.exec(newExpr)) !== null && _ADD_OR_SUBTRACT_REGE !== void 0 ? _ADD_OR_SUBTRACT_REGE : [];
    var _lTs = DecimalCSS.parse(_leftOperand !== null && _leftOperand !== void 0 ? _leftOperand : '');
    var _rTs = DecimalCSS.parse(_rightOperand !== null && _rightOperand !== void 0 ? _rightOperand : '');
    var _result = _operator === '+' ? _lTs.add(_rTs) : _lTs.subtract(_rTs);
    if (_result.isNaN()) {
      return STR_NAN;
    }
    newExpr = newExpr.replace(ADD_OR_SUBTRACT_REGEX, _result.toString());
  }
  return newExpr;
}
var PARENTHESES_REGEX = /\(([^()]*)\)/;
function calculateParentheses(expr) {
  var newExpr = expr;
  var match;
  // eslint-disable-next-line no-cond-assign
  while ((match = PARENTHESES_REGEX.exec(newExpr)) != null) {
    var [, parentheticalExpression] = match;
    newExpr = newExpr.replace(PARENTHESES_REGEX, calculateArithmetic(parentheticalExpression));
  }
  return newExpr;
}
function evaluateExpression(expression) {
  var newExpr = expression.replace(/\s+/g, '');
  newExpr = calculateParentheses(newExpr);
  newExpr = calculateArithmetic(newExpr);
  return newExpr;
}
function safeEvaluateExpression(expression) {
  try {
    return evaluateExpression(expression);
  } catch (_unused) {
    return STR_NAN;
  }
}
function reduceCSSCalc(expression) {
  var result = safeEvaluateExpression(expression.slice(5, -1));
  if (result === STR_NAN) {
    return '';
  }
  return result;
}
;// ./node_modules/recharts/es6/component/Text.js
var Text_excluded = ["x", "y", "lineHeight", "capHeight", "fill", "scaleToFit", "textAnchor", "verticalAnchor"],
  _excluded2 = ["dx", "dy", "angle", "className", "breakAll"];
function Text_extends() { return Text_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Text_extends.apply(null, arguments); }
function Text_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Text_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Text_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }










var BREAKING_SPACES = /[ \f\n\r\t\v\u2028\u2029]+/;
var calculateWordWidths = _ref => {
  var {
    children,
    breakAll,
    style
  } = _ref;
  try {
    var words = [];
    if (!isNullish(children)) {
      if (breakAll) {
        words = children.toString().split('');
      } else {
        words = children.toString().split(BREAKING_SPACES);
      }
    }
    var wordsWithComputedWidth = words.map(word => ({
      word,
      width: getStringSize(word, style).width
    }));
    var spaceWidth = breakAll ? 0 : getStringSize('\u00A0', style).width;
    return {
      wordsWithComputedWidth,
      spaceWidth
    };
  } catch (_unused) {
    return null;
  }
};

/**
 * @inline
 */

function isValidTextAnchor(value) {
  return value === 'start' || value === 'middle' || value === 'end' || value === 'inherit';
}

/**
 * @inline
 */

/**
 * @inline
 */

var calculate = (words, lineWidth, spaceWidth, scaleToFit) => words.reduce((result, _ref2) => {
  var {
    word,
    width
  } = _ref2;
  var currentLine = result[result.length - 1];
  if (currentLine && width != null && (lineWidth == null || scaleToFit || currentLine.width + width + spaceWidth < Number(lineWidth))) {
    // Word can be added to an existing line
    currentLine.words.push(word);
    currentLine.width += width + spaceWidth;
  } else {
    // Add first word to line or word is too long to scaleToFit on existing line
    var newLine = {
      words: [word],
      width
    };
    result.push(newLine);
  }
  return result;
}, []);
var findLongestLine = words => words.reduce((a, b) => a.width > b.width ? a : b);
var suffix = '…';
var checkOverflow = (text, index, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit) => {
  var tempText = text.slice(0, index);
  var words = calculateWordWidths({
    breakAll,
    style,
    children: tempText + suffix
  });
  if (!words) {
    return [false, []];
  }
  var result = calculate(words.wordsWithComputedWidth, lineWidth, spaceWidth, scaleToFit);
  var doesOverflow = result.length > maxLines || findLongestLine(result).width > Number(lineWidth);
  return [doesOverflow, result];
};
var calculateWordsByLines = (_ref3, initialWordsWithComputedWith, spaceWidth, lineWidth, scaleToFit) => {
  var {
    maxLines,
    children,
    style,
    breakAll
  } = _ref3;
  var shouldLimitLines = isNumber(maxLines);
  var text = String(children);
  var originalResult = calculate(initialWordsWithComputedWith, lineWidth, spaceWidth, scaleToFit);
  if (!shouldLimitLines || scaleToFit) {
    return originalResult;
  }
  var overflows = originalResult.length > maxLines || findLongestLine(originalResult).width > Number(lineWidth);
  if (!overflows) {
    return originalResult;
  }
  var start = 0;
  var end = text.length - 1;
  var iterations = 0;
  var trimmedResult;
  while (start <= end && iterations <= text.length - 1) {
    var middle = Math.floor((start + end) / 2);
    var prev = middle - 1;
    var [doesPrevOverflow, result] = checkOverflow(text, prev, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit);
    var [doesMiddleOverflow] = checkOverflow(text, middle, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit);
    if (!doesPrevOverflow && !doesMiddleOverflow) {
      start = middle + 1;
    }
    if (doesPrevOverflow && doesMiddleOverflow) {
      end = middle - 1;
    }
    if (!doesPrevOverflow && doesMiddleOverflow) {
      trimmedResult = result;
      break;
    }
    iterations++;
  }

  // Fallback to originalResult (result without trimming) if we cannot find the
  // where to trim.  This should not happen :tm:
  return trimmedResult || originalResult;
};
var getWordsWithoutCalculate = children => {
  var words = !isNullish(children) ? children.toString().split(BREAKING_SPACES) : [];
  return [{
    words,
    width: undefined
  }];
};
var getWordsByLines = _ref4 => {
  var {
    width,
    scaleToFit,
    children,
    style,
    breakAll,
    maxLines
  } = _ref4;
  // Only perform calculations if using features that require them (multiline, scaleToFit)
  if ((width || scaleToFit) && !Global.isSsr) {
    var wordsWithComputedWidth, spaceWidth;
    var wordWidths = calculateWordWidths({
      breakAll,
      children,
      style
    });
    if (wordWidths) {
      var {
        wordsWithComputedWidth: wcw,
        spaceWidth: sw
      } = wordWidths;
      wordsWithComputedWidth = wcw;
      spaceWidth = sw;
    } else {
      return getWordsWithoutCalculate(children);
    }
    return calculateWordsByLines({
      breakAll,
      children,
      maxLines,
      style
    }, wordsWithComputedWidth, spaceWidth, width, Boolean(scaleToFit));
  }
  return getWordsWithoutCalculate(children);
};
var DEFAULT_FILL = '#808080';
var textDefaultProps = {
  angle: 0,
  breakAll: false,
  // Magic number from d3
  capHeight: '0.71em',
  fill: DEFAULT_FILL,
  lineHeight: '1em',
  scaleToFit: false,
  textAnchor: 'start',
  // Maintain compat with existing charts / default SVG behavior
  verticalAnchor: 'end',
  x: 0,
  y: 0
};
var Text = /*#__PURE__*/(0,external_React_.forwardRef)((outsideProps, ref) => {
  var _resolveDefaultProps = resolveDefaultProps(outsideProps, textDefaultProps),
    {
      x: propsX,
      y: propsY,
      lineHeight,
      capHeight,
      fill,
      scaleToFit,
      textAnchor,
      verticalAnchor
    } = _resolveDefaultProps,
    props = Text_objectWithoutProperties(_resolveDefaultProps, Text_excluded);
  var wordsByLines = (0,external_React_.useMemo)(() => {
    return getWordsByLines({
      breakAll: props.breakAll,
      children: props.children,
      maxLines: props.maxLines,
      scaleToFit,
      style: props.style,
      width: props.width
    });
  }, [props.breakAll, props.children, props.maxLines, scaleToFit, props.style, props.width]);
  var {
      dx,
      dy,
      angle,
      className,
      breakAll
    } = props,
    textProps = Text_objectWithoutProperties(props, _excluded2);
  if (!isNumOrStr(propsX) || !isNumOrStr(propsY) || wordsByLines.length === 0) {
    return null;
  }
  var x = Number(propsX) + (isNumber(dx) ? dx : 0);
  var y = Number(propsY) + (isNumber(dy) ? dy : 0);
  if (!isWellBehavedNumber(x) || !isWellBehavedNumber(y)) {
    return null;
  }
  var startDy;
  switch (verticalAnchor) {
    case 'start':
      startDy = reduceCSSCalc("calc(".concat(capHeight, ")"));
      break;
    case 'middle':
      startDy = reduceCSSCalc("calc(".concat((wordsByLines.length - 1) / 2, " * -").concat(lineHeight, " + (").concat(capHeight, " / 2))"));
      break;
    default:
      startDy = reduceCSSCalc("calc(".concat(wordsByLines.length - 1, " * -").concat(lineHeight, ")"));
      break;
  }
  var transforms = [];
  var firstLine = wordsByLines[0];
  if (scaleToFit && firstLine != null) {
    var lineWidth = firstLine.width;
    var {
      width
    } = props;
    transforms.push("scale(".concat(isNumber(width) && isNumber(lineWidth) ? width / lineWidth : 1, ")"));
  }
  if (angle) {
    transforms.push("rotate(".concat(angle, ", ").concat(x, ", ").concat(y, ")"));
  }
  if (transforms.length) {
    textProps.transform = transforms.join(' ');
  }
  return /*#__PURE__*/external_React_.createElement("text", Text_extends({}, svgPropertiesAndEvents(textProps), {
    ref: ref,
    x: x,
    y: y,
    className: clsx('recharts-text', className),
    textAnchor: textAnchor,
    fill: fill.includes('url') ? DEFAULT_FILL : fill
  }), wordsByLines.map((line, index) => {
    var words = line.words.join(breakAll ? '' : ' ');
    return (
      /*#__PURE__*/
      // duplicate words will cause duplicate keys which is why we add the array index here
      external_React_.createElement("tspan", {
        x: x,
        dy: index === 0 ? startDy : lineHeight,
        key: "".concat(words, "-").concat(index)
      }, words)
    );
  }));
});
Text.displayName = 'Text';
;// ./node_modules/recharts/es6/util/getClassNameFromUnknown.js
function getClassNameFromUnknown(u) {
  if (u && typeof u === 'object' && 'className' in u && typeof u.className === 'string') {
    return u.className;
  }
  return '';
}
;// ./node_modules/recharts/es6/polar/PolarAngleAxis.js
var PolarAngleAxis_excluded = ["children", "type"];
function PolarAngleAxis_extends() { return PolarAngleAxis_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, PolarAngleAxis_extends.apply(null, arguments); }
function PolarAngleAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function PolarAngleAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? PolarAngleAxis_ownKeys(Object(t), !0).forEach(function (r) { PolarAngleAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : PolarAngleAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function PolarAngleAxis_defineProperty(e, r, t) { return (r = PolarAngleAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function PolarAngleAxis_toPropertyKey(t) { var i = PolarAngleAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function PolarAngleAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function PolarAngleAxis_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = PolarAngleAxis_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function PolarAngleAxis_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }






















var eps = 1e-5;
var COS_45 = Math.cos(degreeToRadian(45));
var AXIS_TYPE = 'angleAxis';
function SetAngleAxisSettings(props) {
  var dispatch = useAppDispatch();
  var layout = usePolarChartLayout();
  var settings = (0,external_React_.useMemo)(() => {
    var {
        children,
        type: typeFromProps
      } = props,
      rest = PolarAngleAxis_objectWithoutProperties(props, PolarAngleAxis_excluded);
    var evaluatedType = getAxisTypeBasedOnLayout(layout, 'angleAxis', typeFromProps);
    if (evaluatedType == null) {
      return undefined;
    }
    return PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread({}, rest), {}, {
      type: evaluatedType
    });
  }, [props, layout]);
  var synchronizedSettings = useAppSelector(state => selectAngleAxis(state, settings === null || settings === void 0 ? void 0 : settings.id));
  var settingsAreSynchronized = settings === synchronizedSettings;
  (0,external_React_.useEffect)(() => {
    if (settings == null) {
      return noop;
    }
    dispatch(addAngleAxis(settings));
    return () => {
      dispatch(removeAngleAxis(settings));
    };
  }, [dispatch, settings]);
  if (settingsAreSynchronized) {
    return props.children;
  }
  return null;
}

/**
 * Calculate the coordinate of line endpoint
 * @param data The data if there are ticks
 * @param props axis settings
 * @return (x1, y1): The point close to text,
 *         (x2, y2): The point close to axis
 */
var getTickLineCoord = (data, props) => {
  var {
    cx,
    cy,
    radius,
    orientation,
    tickSize
  } = props;
  var tickLineSize = tickSize || 8;
  var p1 = polarToCartesian(cx, cy, radius, data.coordinate);
  var p2 = polarToCartesian(cx, cy, radius + (orientation === 'inner' ? -1 : 1) * tickLineSize, data.coordinate);
  return {
    x1: p1.x,
    y1: p1.y,
    x2: p2.x,
    y2: p2.y
  };
};

/**
 * Get the text-anchor of each tick
 * @param data Data of ticks
 * @param orientation of the axis ticks
 * @return text-anchor
 */
var getTickTextAnchor = (data, orientation) => {
  var cos = Math.cos(degreeToRadian(-data.coordinate));
  if (cos > eps) {
    return orientation === 'outer' ? 'start' : 'end';
  }
  if (cos < -eps) {
    return orientation === 'outer' ? 'end' : 'start';
  }
  return 'middle';
};

/**
 * Get the text vertical anchor of each tick
 * @param data Data of a tick
 * @return text vertical anchor
 */
var getTickTextVerticalAnchor = data => {
  var cos = Math.cos(degreeToRadian(-data.coordinate));
  var sin = Math.sin(degreeToRadian(-data.coordinate));

  // handle top and bottom sectors: 90±45deg and 270±45deg
  if (Math.abs(cos) <= COS_45) {
    // sin > 0: top sector, sin < 0: bottom sector
    return sin > 0 ? 'start' : 'end';
  }
  return 'middle';
};
var AxisLine = props => {
  var {
    cx,
    cy,
    radius,
    axisLineType,
    axisLine,
    ticks
  } = props;
  if (!axisLine) {
    return null;
  }
  var axisLineProps = PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread({}, svgPropertiesNoEvents(props)), {}, {
    fill: 'none'
  }, svgPropertiesNoEvents(axisLine));
  if (axisLineType === 'circle') {
    // @ts-expect-error wrong SVG element type
    return /*#__PURE__*/external_React_.createElement(Dot, PolarAngleAxis_extends({
      className: "recharts-polar-angle-axis-line"
    }, axisLineProps, {
      cx: cx,
      cy: cy,
      r: radius
    }));
  }
  var points = ticks.map(entry => polarToCartesian(cx, cy, radius, entry.coordinate));

  // @ts-expect-error wrong SVG element type
  return /*#__PURE__*/external_React_.createElement(Polygon, PolarAngleAxis_extends({
    className: "recharts-polar-angle-axis-line"
  }, axisLineProps, {
    points: points
  }));
};
var TickItemText = _ref => {
  var {
    tick,
    tickProps,
    value
  } = _ref;
  if (!tick) {
    return null;
  }
  if (/*#__PURE__*/external_React_.isValidElement(tick)) {
    return /*#__PURE__*/external_React_.cloneElement(tick, tickProps);
  }
  if (typeof tick === 'function') {
    return tick(tickProps);
  }
  return /*#__PURE__*/external_React_.createElement(Text, PolarAngleAxis_extends({}, tickProps, {
    className: "recharts-polar-angle-axis-tick-value"
  }), value);
};
var Ticks = props => {
  var {
    tick,
    tickLine,
    tickFormatter,
    stroke,
    ticks
  } = props;
  var axisProps = svgPropertiesNoEvents(props);
  var customTickProps = svgPropertiesNoEventsFromUnknown(tick);
  var tickLineProps = PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread({}, axisProps), {}, {
    fill: 'none'
  }, svgPropertiesNoEvents(tickLine));
  var items = ticks.map((entry, i) => {
    var lineCoord = getTickLineCoord(entry, props);
    var textAnchor = getTickTextAnchor(entry, props.orientation);
    var verticalAnchor = getTickTextVerticalAnchor(entry);
    var tickProps = PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread({}, axisProps), {}, {
      // @ts-expect-error customTickProps is contributing unknown props
      textAnchor,
      verticalAnchor,
      // @ts-expect-error customTickProps is contributing unknown props
      stroke: 'none',
      // @ts-expect-error customTickProps is contributing unknown props
      fill: stroke
    }, customTickProps), {}, {
      index: i,
      payload: entry,
      x: lineCoord.x2,
      y: lineCoord.y2
    });
    return /*#__PURE__*/external_React_.createElement(Layer, PolarAngleAxis_extends({
      className: clsx('recharts-polar-angle-axis-tick', getClassNameFromUnknown(tick)),
      key: "tick-".concat(entry.coordinate)
    }, adaptEventsOfChild(props, entry, i)), tickLine && /*#__PURE__*/external_React_.createElement("line", PolarAngleAxis_extends({
      className: "recharts-polar-angle-axis-tick-line"
    }, tickLineProps, lineCoord)), /*#__PURE__*/external_React_.createElement(TickItemText, {
      tick: tick,
      tickProps: tickProps,
      value: tickFormatter ? tickFormatter(entry.value, i) : entry.value
    }));
  });
  return /*#__PURE__*/external_React_.createElement(Layer, {
    className: "recharts-polar-angle-axis-ticks"
  }, items);
};
var PolarAngleAxisWrapper = defaultsAndInputs => {
  var {
    angleAxisId
  } = defaultsAndInputs;
  var viewBox = useAppSelector(selectPolarViewBox);
  var scale = useAppSelector(state => selectPolarAxisScale(state, 'angleAxis', angleAxisId));
  var isPanorama = useIsPanorama();
  var ticks = useAppSelector(state => selectPolarAngleAxisTicks(state, 'angleAxis', angleAxisId, isPanorama));
  if (viewBox == null || !ticks || !ticks.length || scale == null) {
    return null;
  }
  var props = PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread(PolarAngleAxis_objectSpread({}, defaultsAndInputs), {}, {
    scale
  }, viewBox), {}, {
    radius: viewBox.outerRadius,
    ticks
  });
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(Layer, {
    className: clsx('recharts-polar-angle-axis', AXIS_TYPE, props.className)
  }, /*#__PURE__*/external_React_.createElement(AxisLine, props), /*#__PURE__*/external_React_.createElement(Ticks, props)));
};

/**
 * @provides PolarLabelContext
 * @consumes PolarViewBoxContext
 */
function PolarAngleAxis(outsideProps) {
  var props = resolveDefaultProps(outsideProps, defaultPolarAngleAxisProps);
  return /*#__PURE__*/external_React_.createElement(SetAngleAxisSettings, {
    id: props.angleAxisId,
    scale: props.scale,
    type: props.type,
    dataKey: props.dataKey,
    unit: undefined,
    name: props.name,
    allowDuplicatedCategory: false // Ignoring the prop on purpose because axis calculation behaves as if it was false and Tooltip requires it to be true.
    ,
    allowDataOverflow: false,
    reversed: props.reversed,
    includeHidden: false,
    allowDecimals: props.allowDecimals,
    tickCount: props.tickCount
    // @ts-expect-error the type does not match. Is RadiusAxis really expecting what it says?
    ,
    ticks: props.ticks,
    tick: props.tick,
    domain: props.domain
  }, /*#__PURE__*/external_React_.createElement(PolarAngleAxisWrapper, props));
}
PolarAngleAxis.displayName = 'PolarAngleAxis';
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/maxBy.js
var maxBy = __webpack_require__(4338);
var maxBy_default = /*#__PURE__*/__webpack_require__.n(maxBy);
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/minBy.js
var minBy = __webpack_require__(2972);
var minBy_default = /*#__PURE__*/__webpack_require__.n(minBy);
;// ./node_modules/recharts/es6/cartesian/getCartesianPosition.js
function getCartesianPosition_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function getCartesianPosition_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? getCartesianPosition_ownKeys(Object(t), !0).forEach(function (r) { getCartesianPosition_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : getCartesianPosition_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function getCartesianPosition_defineProperty(e, r, t) { return (r = getCartesianPosition_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function getCartesianPosition_toPropertyKey(t) { var i = getCartesianPosition_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function getCartesianPosition_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


/**
 * Calculates the position and alignment for a generic element in a Cartesian coordinate system.
 *
 * @param options - The options including viewBox, position, and offset.
 * @returns The calculated x, y, alignment and size.
 */
var getCartesianPosition = options => {
  var {
    viewBox,
    position,
    offset = 0,
    parentViewBox: parentViewBoxFromOptions,
    clamp
  } = options;
  var {
    x,
    y,
    height,
    upperWidth,
    lowerWidth
  } = cartesianViewBoxToTrapezoid(viewBox);

  // Funnel.tsx provides a viewBox where `x` is the top-left of the trapezoid shape.
  var upperX = x;
  // The trapezoid is centered, so we can calculate the other corners from the top-left.
  var lowerX = x + (upperWidth - lowerWidth) / 2;
  // middleX is the x-coordinate of the left edge at the vertical midpoint of the trapezoid.
  var middleX = (upperX + lowerX) / 2;
  // The width of the trapezoid at its vertical midpoint.
  var midHeightWidth = (upperWidth + lowerWidth) / 2;
  // The center x-coordinate is constant for the entire height of the trapezoid.
  var centerX = upperX + upperWidth / 2;

  // Define vertical offsets and position inverts based on the value being positive or negative.
  // This allows labels to be positioned correctly for bars with negative height.
  var verticalSign = height >= 0 ? 1 : -1;
  var verticalOffset = verticalSign * offset;
  var verticalEnd = verticalSign > 0 ? 'end' : 'start';
  var verticalStart = verticalSign > 0 ? 'start' : 'end';

  // Define horizontal offsets and position inverts based on the value being positive or negative.
  // This allows labels to be positioned correctly for bars with negative width.
  var horizontalSign = upperWidth >= 0 ? 1 : -1;
  var horizontalOffset = horizontalSign * offset;
  var horizontalEnd = horizontalSign > 0 ? 'end' : 'start';
  var horizontalStart = horizontalSign > 0 ? 'start' : 'end';

  // We assume parentViewBox is generic if provided.
  // The user has asserted that parentViewBox will be CartesianViewBoxRequired if present.
  var parentViewBox = parentViewBoxFromOptions;
  if (position === 'top') {
    var result = {
      x: upperX + upperWidth / 2,
      y: y - verticalOffset,
      horizontalAnchor: 'middle',
      verticalAnchor: verticalEnd
    };
    if (clamp && parentViewBox) {
      result.height = Math.max(y - parentViewBox.y, 0);
      result.width = upperWidth;
    }
    return result;
  }
  if (position === 'bottom') {
    var _result = {
      x: lowerX + lowerWidth / 2,
      y: y + height + verticalOffset,
      horizontalAnchor: 'middle',
      verticalAnchor: verticalStart
    };
    if (clamp && parentViewBox) {
      _result.height = Math.max(parentViewBox.y + parentViewBox.height - (y + height), 0);
      _result.width = lowerWidth;
    }
    return _result;
  }
  if (position === 'left') {
    var _result2 = {
      x: middleX - horizontalOffset,
      y: y + height / 2,
      horizontalAnchor: horizontalEnd,
      verticalAnchor: 'middle'
    };
    if (clamp && parentViewBox) {
      _result2.width = Math.max(_result2.x - parentViewBox.x, 0);
      _result2.height = height;
    }
    return _result2;
  }
  if (position === 'right') {
    var _result3 = {
      x: middleX + midHeightWidth + horizontalOffset,
      y: y + height / 2,
      horizontalAnchor: horizontalStart,
      verticalAnchor: 'middle'
    };
    if (clamp && parentViewBox) {
      _result3.width = Math.max(parentViewBox.x + parentViewBox.width - _result3.x, 0);
      _result3.height = height;
    }
    return _result3;
  }
  var sizeAttrs = clamp && parentViewBox ? {
    width: midHeightWidth,
    height
  } : {};
  if (position === 'insideLeft') {
    return getCartesianPosition_objectSpread({
      x: middleX + horizontalOffset,
      y: y + height / 2,
      horizontalAnchor: horizontalStart,
      verticalAnchor: 'middle'
    }, sizeAttrs);
  }
  if (position === 'insideRight') {
    return getCartesianPosition_objectSpread({
      x: middleX + midHeightWidth - horizontalOffset,
      y: y + height / 2,
      horizontalAnchor: horizontalEnd,
      verticalAnchor: 'middle'
    }, sizeAttrs);
  }
  if (position === 'insideTop') {
    return getCartesianPosition_objectSpread({
      x: upperX + upperWidth / 2,
      y: y + verticalOffset,
      horizontalAnchor: 'middle',
      verticalAnchor: verticalStart
    }, sizeAttrs);
  }
  if (position === 'insideBottom') {
    return getCartesianPosition_objectSpread({
      x: lowerX + lowerWidth / 2,
      y: y + height - verticalOffset,
      horizontalAnchor: 'middle',
      verticalAnchor: verticalEnd
    }, sizeAttrs);
  }
  if (position === 'insideTopLeft') {
    return getCartesianPosition_objectSpread({
      x: upperX + horizontalOffset,
      y: y + verticalOffset,
      horizontalAnchor: horizontalStart,
      verticalAnchor: verticalStart
    }, sizeAttrs);
  }
  if (position === 'insideTopRight') {
    return getCartesianPosition_objectSpread({
      x: upperX + upperWidth - horizontalOffset,
      y: y + verticalOffset,
      horizontalAnchor: horizontalEnd,
      verticalAnchor: verticalStart
    }, sizeAttrs);
  }
  if (position === 'insideBottomLeft') {
    return getCartesianPosition_objectSpread({
      x: lowerX + horizontalOffset,
      y: y + height - verticalOffset,
      horizontalAnchor: horizontalStart,
      verticalAnchor: verticalEnd
    }, sizeAttrs);
  }
  if (position === 'insideBottomRight') {
    return getCartesianPosition_objectSpread({
      x: lowerX + lowerWidth - horizontalOffset,
      y: y + height - verticalOffset,
      horizontalAnchor: horizontalEnd,
      verticalAnchor: verticalEnd
    }, sizeAttrs);
  }
  if (!!position && typeof position === 'object' && (isNumber(position.x) || isPercent(position.x)) && (isNumber(position.y) || isPercent(position.y))) {
    // TODO: This is not quite right. The width of the trapezoid changes with y.
    // A percentage-based x should be relative to the width at that y.
    // For now, we use the mid-height width as a reasonable approximation.
    return getCartesianPosition_objectSpread({
      x: x + getPercentValue(position.x, midHeightWidth),
      y: y + getPercentValue(position.y, height),
      horizontalAnchor: 'end',
      verticalAnchor: 'end'
    }, sizeAttrs);
  }
  return getCartesianPosition_objectSpread({
    x: centerX,
    y: y + height / 2,
    horizontalAnchor: 'middle',
    verticalAnchor: 'middle'
  }, sizeAttrs);
};
;// ./node_modules/recharts/es6/component/Label.js
var Label_excluded = ["labelRef"],
  Label_excluded2 = ["content"];
function Label_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Label_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Label_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function Label_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Label_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Label_ownKeys(Object(t), !0).forEach(function (r) { Label_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Label_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Label_defineProperty(e, r, t) { return (r = Label_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Label_toPropertyKey(t) { var i = Label_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Label_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Label_extends() { return Label_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Label_extends.apply(null, arguments); }















/**
 * @inline
 */

/**
 * @inline
 */

/**
 * @inline
 */

var CartesianLabelContext = /*#__PURE__*/(0,external_React_.createContext)(null);
var CartesianLabelContextProvider = _ref => {
  var {
    x,
    y,
    upperWidth,
    lowerWidth,
    width,
    height,
    children
  } = _ref;
  var viewBox = (0,external_React_.useMemo)(() => ({
    x,
    y,
    upperWidth,
    lowerWidth,
    width,
    height
  }), [x, y, upperWidth, lowerWidth, width, height]);
  return /*#__PURE__*/external_React_.createElement(CartesianLabelContext.Provider, {
    value: viewBox
  }, children);
};
var useCartesianLabelContext = () => {
  var labelChildContext = (0,external_React_.useContext)(CartesianLabelContext);
  var chartContext = useViewBox();
  return labelChildContext || (chartContext ? cartesianViewBoxToTrapezoid(chartContext) : undefined);
};
var PolarLabelContext = /*#__PURE__*/(0,external_React_.createContext)(null);
var PolarLabelContextProvider = _ref2 => {
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    clockWise,
    children
  } = _ref2;
  var viewBox = (0,external_React_.useMemo)(() => ({
    cx,
    cy,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    clockWise
  }), [cx, cy, innerRadius, outerRadius, startAngle, endAngle, clockWise]);
  return /*#__PURE__*/external_React_.createElement(PolarLabelContext.Provider, {
    value: viewBox
  }, children);
};
var usePolarLabelContext = () => {
  var labelChildContext = (0,external_React_.useContext)(PolarLabelContext);
  var chartContext = useAppSelector(selectPolarViewBox);
  return labelChildContext || chartContext;
};
var getLabel = props => {
  var {
    value,
    formatter
  } = props;
  var label = isNullish(props.children) ? value : props.children;
  if (typeof formatter === 'function') {
    return formatter(label);
  }
  return label;
};
var isLabelContentAFunction = content => {
  return content != null && typeof content === 'function';
};
var getDeltaAngle = (startAngle, endAngle) => {
  var sign = mathSign(endAngle - startAngle);
  var deltaAngle = Math.min(Math.abs(endAngle - startAngle), 360);
  return sign * deltaAngle;
};
var renderRadialLabel = (labelProps, position, label, attrs, viewBox) => {
  var {
    offset,
    className
  } = labelProps;
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle,
    clockWise
  } = viewBox;
  var radius = (innerRadius + outerRadius) / 2;
  var deltaAngle = getDeltaAngle(startAngle, endAngle);
  var sign = deltaAngle >= 0 ? 1 : -1;
  var labelAngle, direction;
  switch (position) {
    case 'insideStart':
      labelAngle = startAngle + sign * offset;
      direction = clockWise;
      break;
    case 'insideEnd':
      labelAngle = endAngle - sign * offset;
      direction = !clockWise;
      break;
    case 'end':
      labelAngle = endAngle + sign * offset;
      direction = clockWise;
      break;
    default:
      throw new Error("Unsupported position ".concat(position));
  }
  direction = deltaAngle <= 0 ? direction : !direction;
  var startPoint = polarToCartesian(cx, cy, radius, labelAngle);
  var endPoint = polarToCartesian(cx, cy, radius, labelAngle + (direction ? 1 : -1) * 359);
  var path = "M".concat(startPoint.x, ",").concat(startPoint.y, "\n    A").concat(radius, ",").concat(radius, ",0,1,").concat(direction ? 0 : 1, ",\n    ").concat(endPoint.x, ",").concat(endPoint.y);
  var id = isNullish(labelProps.id) ? uniqueId('recharts-radial-line-') : labelProps.id;
  return /*#__PURE__*/external_React_.createElement("text", Label_extends({}, attrs, {
    dominantBaseline: "central",
    className: clsx('recharts-radial-bar-label', className)
  }), /*#__PURE__*/external_React_.createElement("defs", null, /*#__PURE__*/external_React_.createElement("path", {
    id: id,
    d: path
  })), /*#__PURE__*/external_React_.createElement("textPath", {
    xlinkHref: "#".concat(id)
  }, label));
};
var getAttrsOfPolarLabel = (viewBox, offset, position) => {
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle
  } = viewBox;
  var midAngle = (startAngle + endAngle) / 2;
  if (position === 'outside') {
    var {
      x: _x,
      y: _y
    } = polarToCartesian(cx, cy, outerRadius + offset, midAngle);
    return {
      x: _x,
      y: _y,
      textAnchor: _x >= cx ? 'start' : 'end',
      verticalAnchor: 'middle'
    };
  }
  if (position === 'center') {
    return {
      x: cx,
      y: cy,
      textAnchor: 'middle',
      verticalAnchor: 'middle'
    };
  }
  if (position === 'centerTop') {
    return {
      x: cx,
      y: cy,
      textAnchor: 'middle',
      verticalAnchor: 'start'
    };
  }
  if (position === 'centerBottom') {
    return {
      x: cx,
      y: cy,
      textAnchor: 'middle',
      verticalAnchor: 'end'
    };
  }
  var r = (innerRadius + outerRadius) / 2;
  var {
    x,
    y
  } = polarToCartesian(cx, cy, r, midAngle);
  return {
    x,
    y,
    textAnchor: 'middle',
    verticalAnchor: 'middle'
  };
};
var isPolar = viewBox => viewBox != null && 'cx' in viewBox && isNumber(viewBox.cx);
var defaultLabelProps = {
  angle: 0,
  offset: 5,
  zIndex: DefaultZIndexes.label,
  position: 'middle',
  textBreakAll: false
};
function polarViewBoxToTrapezoid(viewBox) {
  if (!isPolar(viewBox)) {
    return viewBox;
  }
  var {
    cx,
    cy,
    outerRadius
  } = viewBox;
  var diameter = outerRadius * 2;
  return {
    x: cx - outerRadius,
    y: cy - outerRadius,
    width: diameter,
    upperWidth: diameter,
    lowerWidth: diameter,
    height: diameter
  };
}

/**
 * @consumes CartesianViewBoxContext
 * @consumes PolarViewBoxContext
 * @consumes CartesianLabelContext
 * @consumes PolarLabelContext
 */
function Label(outerProps) {
  var props = resolveDefaultProps(outerProps, defaultLabelProps);
  var {
    viewBox: viewBoxFromProps,
    parentViewBox,
    position,
    value,
    children,
    content,
    className = '',
    textBreakAll,
    labelRef
  } = props;
  var polarViewBox = usePolarLabelContext();
  var cartesianViewBox = useCartesianLabelContext();

  /*
   * I am not proud about this solution, but it's a quick fix for https://github.com/recharts/recharts/issues/6030#issuecomment-3155352460.
   * What we should really do is split Label into two components: CartesianLabel and PolarLabel and then handle their respective viewBoxes separately.
   * Also other components should set its own viewBox in a context so that we can fix https://github.com/recharts/recharts/issues/6156
   */
  var resolvedViewBox = position === 'center' ? cartesianViewBox : polarViewBox !== null && polarViewBox !== void 0 ? polarViewBox : cartesianViewBox;
  var viewBox, label, positionAttrs;
  if (viewBoxFromProps == null) {
    viewBox = resolvedViewBox;
  } else if (isPolar(viewBoxFromProps)) {
    viewBox = viewBoxFromProps;
  } else {
    viewBox = cartesianViewBoxToTrapezoid(viewBoxFromProps);
  }
  var cartesianBox = polarViewBoxToTrapezoid(viewBox);
  if (!viewBox || isNullish(value) && isNullish(children) && ! /*#__PURE__*/(0,external_React_.isValidElement)(content) && typeof content !== 'function') {
    return null;
  }
  var propsWithViewBox = Label_objectSpread(Label_objectSpread({}, props), {}, {
    viewBox
  });
  if (/*#__PURE__*/(0,external_React_.isValidElement)(content)) {
    var {
        labelRef: _
      } = propsWithViewBox,
      propsWithoutLabelRef = Label_objectWithoutProperties(propsWithViewBox, Label_excluded);
    return /*#__PURE__*/(0,external_React_.cloneElement)(content, propsWithoutLabelRef);
  }
  if (typeof content === 'function') {
    var {
        content: _2
      } = propsWithViewBox,
      propsForContent = Label_objectWithoutProperties(propsWithViewBox, Label_excluded2);
    // @ts-expect-error we're not checking if the content component returns something that Text is able to render
    label = /*#__PURE__*/(0,external_React_.createElement)(content, propsForContent);
    if (/*#__PURE__*/(0,external_React_.isValidElement)(label)) {
      return label;
    }
  } else {
    label = getLabel(props);
  }
  var attrs = svgPropertiesAndEvents(props);
  if (isPolar(viewBox)) {
    // TODO: Generic Polar Hook
    if (position === 'insideStart' || position === 'insideEnd' || position === 'end') {
      return renderRadialLabel(props, position, label, attrs, viewBox);
    }
    positionAttrs = getAttrsOfPolarLabel(viewBox, props.offset, props.position);
  } else {
    if (!cartesianBox) {
      return null;
    }
    var cartesianResult = getCartesianPosition({
      viewBox: cartesianBox,
      position,
      offset: props.offset,
      parentViewBox: isPolar(parentViewBox) ? undefined : parentViewBox,
      clamp: true
    });
    positionAttrs = Label_objectSpread(Label_objectSpread({
      x: cartesianResult.x,
      y: cartesianResult.y,
      textAnchor: cartesianResult.horizontalAnchor,
      verticalAnchor: cartesianResult.verticalAnchor
    }, cartesianResult.width !== undefined ? {
      width: cartesianResult.width
    } : {}), cartesianResult.height !== undefined ? {
      height: cartesianResult.height
    } : {});
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(Text, Label_extends({
    ref: labelRef,
    className: clsx('recharts-label', className)
  }, attrs, positionAttrs, {
    /*
     * textAnchor is decided by default based on the `position`
     * but we allow overriding via props for precise control.
     */
    textAnchor: isValidTextAnchor(attrs.textAnchor) ? attrs.textAnchor : positionAttrs.textAnchor,
    breakAll: textBreakAll
  }), label));
}
Label.displayName = 'Label';
var parseLabel = (label, viewBox, labelRef) => {
  if (!label) {
    return null;
  }
  var commonProps = {
    viewBox,
    labelRef
  };
  if (label === true) {
    return /*#__PURE__*/external_React_.createElement(Label, Label_extends({
      key: "label-implicit"
    }, commonProps));
  }
  if (isNumOrStr(label)) {
    return /*#__PURE__*/external_React_.createElement(Label, Label_extends({
      key: "label-implicit",
      value: label
    }, commonProps));
  }
  if (/*#__PURE__*/(0,external_React_.isValidElement)(label)) {
    if (label.type === Label) {
      return /*#__PURE__*/(0,external_React_.cloneElement)(label, Label_objectSpread({
        key: 'label-implicit'
      }, commonProps));
    }
    return /*#__PURE__*/external_React_.createElement(Label, Label_extends({
      key: "label-implicit",
      content: label
    }, commonProps));
  }
  if (isLabelContentAFunction(label)) {
    return /*#__PURE__*/external_React_.createElement(Label, Label_extends({
      key: "label-implicit",
      content: label
    }, commonProps));
  }
  if (label && typeof label === 'object') {
    return /*#__PURE__*/external_React_.createElement(Label, Label_extends({}, label, {
      key: "label-implicit"
    }, commonProps));
  }
  return null;
};
function CartesianLabelFromLabelProp(_ref3) {
  var {
    label,
    labelRef
  } = _ref3;
  var viewBox = useCartesianLabelContext();
  return parseLabel(label, viewBox, labelRef) || null;
}
function PolarLabelFromLabelProp(_ref4) {
  var {
    label
  } = _ref4;
  var viewBox = usePolarLabelContext();
  return parseLabel(label, viewBox) || null;
}
;// ./node_modules/recharts/es6/polar/PolarRadiusAxis.js
var PolarRadiusAxis_excluded = ["type"],
  PolarRadiusAxis_excluded2 = ["cx", "cy", "angle", "axisLine"],
  _excluded3 = ["angle", "tickFormatter", "stroke", "tick"];
function PolarRadiusAxis_extends() { return PolarRadiusAxis_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, PolarRadiusAxis_extends.apply(null, arguments); }
function PolarRadiusAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function PolarRadiusAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? PolarRadiusAxis_ownKeys(Object(t), !0).forEach(function (r) { PolarRadiusAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : PolarRadiusAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function PolarRadiusAxis_defineProperty(e, r, t) { return (r = PolarRadiusAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function PolarRadiusAxis_toPropertyKey(t) { var i = PolarRadiusAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function PolarRadiusAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function PolarRadiusAxis_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = PolarRadiusAxis_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function PolarRadiusAxis_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }






















var PolarRadiusAxis_AXIS_TYPE = 'radiusAxis';
function SetRadiusAxisSettings(props) {
  var dispatch = useAppDispatch();
  var layout = usePolarChartLayout();
  var settings = (0,external_React_.useMemo)(() => {
    var {
        type: typeFromProps
      } = props,
      rest = PolarRadiusAxis_objectWithoutProperties(props, PolarRadiusAxis_excluded);
    var evaluatedType = getAxisTypeBasedOnLayout(layout, 'radiusAxis', typeFromProps);
    if (evaluatedType == null) {
      return undefined;
    }
    return PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread({}, rest), {}, {
      type: evaluatedType
    });
  }, [props, layout]);
  (0,external_React_.useEffect)(() => {
    if (settings == null) {
      return noop;
    }
    dispatch(addRadiusAxis(settings));
    return () => {
      dispatch(removeRadiusAxis(settings));
    };
  }, [dispatch, settings]);
  return null;
}

/**
 * Calculate the coordinate of tick
 * @param coordinate The radius of tick
 * @param angle from props
 * @param cx from chart
 * @param cy from chart
 * @return (x, y)
 */
var getTickValueCoord = (_ref, angle, cx, cy) => {
  var {
    coordinate
  } = _ref;
  return polarToCartesian(cx, cy, coordinate, angle);
};
var PolarRadiusAxis_getTickTextAnchor = orientation => {
  var textAnchor;
  switch (orientation) {
    case 'left':
      textAnchor = 'end';
      break;
    case 'right':
      textAnchor = 'start';
      break;
    default:
      textAnchor = 'middle';
      break;
  }
  return textAnchor;
};
var getViewBox = (angle, cx, cy, ticks) => {
  var maxRadiusTick = maxBy_default()(ticks, entry => entry.coordinate || 0);
  var minRadiusTick = minBy_default()(ticks, entry => entry.coordinate || 0);
  return {
    cx,
    cy,
    startAngle: angle,
    endAngle: angle,
    innerRadius: (minRadiusTick === null || minRadiusTick === void 0 ? void 0 : minRadiusTick.coordinate) || 0,
    outerRadius: (maxRadiusTick === null || maxRadiusTick === void 0 ? void 0 : maxRadiusTick.coordinate) || 0,
    clockWise: false
  };
};
var renderAxisLine = (props, ticks) => {
  var {
      cx,
      cy,
      angle,
      axisLine
    } = props,
    others = PolarRadiusAxis_objectWithoutProperties(props, PolarRadiusAxis_excluded2);
  var extent = ticks.reduce((result, entry) => [Math.min(result[0], entry.coordinate), Math.max(result[1], entry.coordinate)], [Infinity, -Infinity]);
  var point0 = polarToCartesian(cx, cy, extent[0], angle);
  var point1 = polarToCartesian(cx, cy, extent[1], angle);
  var axisLineProps = PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread({}, svgPropertiesNoEvents(others)), {}, {
    fill: 'none'
  }, svgPropertiesNoEvents(axisLine)), {}, {
    x1: point0.x,
    y1: point0.y,
    x2: point1.x,
    y2: point1.y
  });

  // @ts-expect-error wrong SVG element type
  return /*#__PURE__*/external_React_.createElement("line", PolarRadiusAxis_extends({
    className: "recharts-polar-radius-axis-line"
  }, axisLineProps));
};
var renderTickItem = (option, tickProps, value) => {
  var tickItem;
  if (/*#__PURE__*/external_React_.isValidElement(option)) {
    tickItem = /*#__PURE__*/external_React_.cloneElement(option, tickProps);
  } else if (typeof option === 'function') {
    tickItem = option(tickProps);
  } else {
    tickItem = /*#__PURE__*/external_React_.createElement(Text, PolarRadiusAxis_extends({}, tickProps, {
      className: "recharts-polar-radius-axis-tick-value"
    }), value);
  }
  return tickItem;
};
var renderTicks = (props, ticks) => {
  var {
      angle,
      tickFormatter,
      stroke,
      tick
    } = props,
    others = PolarRadiusAxis_objectWithoutProperties(props, _excluded3);
  var textAnchor = PolarRadiusAxis_getTickTextAnchor(props.orientation);
  var axisProps = svgPropertiesNoEvents(others);
  var customTickProps = svgPropertiesNoEventsFromUnknown(tick);
  var items = ticks.map((entry, i) => {
    var coord = getTickValueCoord(entry, props.angle, props.cx, props.cy);
    var tickProps = PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread({
      textAnchor,
      transform: "rotate(".concat(90 - angle, ", ").concat(coord.x, ", ").concat(coord.y, ")")
    }, axisProps), {}, {
      stroke: 'none',
      fill: stroke
    }, customTickProps), {}, {
      index: i
    }, coord), {}, {
      payload: entry
    });
    return /*#__PURE__*/external_React_.createElement(Layer, PolarRadiusAxis_extends({
      className: clsx('recharts-polar-radius-axis-tick', getClassNameFromUnknown(tick)),
      key: "tick-".concat(entry.coordinate)
    }, adaptEventsOfChild(props, entry, i)), renderTickItem(tick, tickProps, tickFormatter ? tickFormatter(entry.value, i) : entry.value));
  });
  return /*#__PURE__*/external_React_.createElement(Layer, {
    className: "recharts-polar-radius-axis-ticks"
  }, items);
};
var PolarRadiusAxisWrapper = defaultsAndInputs => {
  var {
    radiusAxisId
  } = defaultsAndInputs;
  var viewBox = useAppSelector(selectPolarViewBox);
  var scale = useAppSelector(state => selectPolarAxisScale(state, 'radiusAxis', radiusAxisId));
  var ticks = useAppSelector(state => selectPolarAxisTicks(state, 'radiusAxis', radiusAxisId, false));
  if (viewBox == null || !ticks || !ticks.length || scale == null) {
    return null;
  }
  var props = PolarRadiusAxis_objectSpread(PolarRadiusAxis_objectSpread({}, defaultsAndInputs), {}, {
    scale
  }, viewBox);
  var {
    tick,
    axisLine
  } = props;
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(Layer, {
    className: clsx('recharts-polar-radius-axis', PolarRadiusAxis_AXIS_TYPE, props.className)
  }, axisLine && renderAxisLine(props, ticks), tick && renderTicks(props, ticks), /*#__PURE__*/external_React_.createElement(PolarLabelContextProvider, getViewBox(props.angle, props.cx, props.cy, ticks), /*#__PURE__*/external_React_.createElement(PolarLabelFromLabelProp, {
    label: props.label
  }), props.children)));
};

/**
 * @provides PolarLabelContext
 * @consumes PolarViewBoxContext
 */
function PolarRadiusAxis(outsideProps) {
  var props = resolveDefaultProps(outsideProps, defaultPolarRadiusAxisProps);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(SetRadiusAxisSettings, {
    domain: props.domain,
    id: props.radiusAxisId,
    scale: props.scale,
    type: props.type,
    dataKey: props.dataKey,
    unit: undefined,
    name: props.name,
    allowDuplicatedCategory: props.allowDuplicatedCategory,
    allowDataOverflow: props.allowDataOverflow,
    reversed: props.reversed,
    includeHidden: props.includeHidden,
    allowDecimals: props.allowDecimals
    // @ts-expect-error the type does not match. Is RadiusAxis really expecting what it says?
    ,
    ticks: props.ticks,
    tickCount: props.tickCount,
    tick: props.tick
  }), /*#__PURE__*/external_React_.createElement(PolarRadiusAxisWrapper, props));
}
PolarRadiusAxis.displayName = 'PolarRadiusAxis';
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/last.js
var last = __webpack_require__(25);
var last_default = /*#__PURE__*/__webpack_require__.n(last);
;// ./node_modules/recharts/es6/component/LabelList.js
var LabelList_excluded = ["valueAccessor"],
  LabelList_excluded2 = ["dataKey", "clockWise", "id", "textBreakAll", "zIndex"];
function LabelList_extends() { return LabelList_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, LabelList_extends.apply(null, arguments); }
function LabelList_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = LabelList_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function LabelList_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }











/**
 * This is public API because we expose it as the valueAccessor parameter.
 *
 * The properties of "viewBox" are repeated as the root props of the entry object.
 * So it doesn't matter if you read entry.x or entry.viewBox.x, they are the same.
 *
 * It's not necessary to pass redundant data, but we keep it for backward compatibility.
 */

/**
 * LabelList props do not allow refs because the same props are reused in multiple elements so we don't have a good single place to ref to.
 */

/**
 * This is the type accepted for the `label` prop on various graphical items.
 * It accepts:
 *
 * boolean:
 *    true = labels show,
 *    false = labels don't show
 * React element:
 *    will be cloned with extra props
 * function:
 *    is used as <Label content={function} />, so this will be called once for each individual label (so typically once for each data point)
 * object:
 *    the props to be passed to a LabelList component
 *
 * @inline
 */

var defaultAccessor = entry => Array.isArray(entry.value) ? last_default()(entry.value) : entry.value;
var CartesianLabelListContext = /*#__PURE__*/(0,external_React_.createContext)(undefined);
var CartesianLabelListContextProvider = CartesianLabelListContext.Provider;
var PolarLabelListContext = /*#__PURE__*/(0,external_React_.createContext)(undefined);
var PolarLabelListContextProvider = PolarLabelListContext.Provider;
function useCartesianLabelListContext() {
  return (0,external_React_.useContext)(CartesianLabelListContext);
}
function usePolarLabelListContext() {
  return (0,external_React_.useContext)(PolarLabelListContext);
}

/**
 * @consumes LabelListContext
 */
function LabelList(_ref) {
  var {
      valueAccessor = defaultAccessor
    } = _ref,
    restProps = LabelList_objectWithoutProperties(_ref, LabelList_excluded);
  var {
      dataKey,
      clockWise,
      id,
      textBreakAll,
      zIndex
    } = restProps,
    others = LabelList_objectWithoutProperties(restProps, LabelList_excluded2);
  var cartesianData = useCartesianLabelListContext();
  var polarData = usePolarLabelListContext();
  var data = cartesianData || polarData;
  if (!data || !data.length) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: zIndex !== null && zIndex !== void 0 ? zIndex : DefaultZIndexes.label
  }, /*#__PURE__*/external_React_.createElement(Layer, {
    className: "recharts-label-list"
  }, data.map((entry, index) => {
    var _restProps$fill;
    var value = isNullish(dataKey) ? valueAccessor(entry, index) : getValueByDataKey(entry.payload, dataKey);
    var idProps = isNullish(id) ? {} : {
      id: "".concat(id, "-").concat(index)
    };
    return /*#__PURE__*/external_React_.createElement(Label, LabelList_extends({
      key: "label-".concat(index)
    }, svgPropertiesAndEvents(entry), others, idProps, {
      /*
       * Prefer to use the explicit fill from LabelList props.
       * Only in an absence of that, fall back to the fill of the entry.
       * The entry fill can be quite difficult to see especially in Bar, Pie, RadialBar in inside positions.
       * On the other hand it's quite convenient in Scatter, Line, or when the position is outside the Bar, Pie filled shapes.
       */
      fill: (_restProps$fill = restProps.fill) !== null && _restProps$fill !== void 0 ? _restProps$fill : entry.fill,
      parentViewBox: entry.parentViewBox,
      value: value,
      textBreakAll: textBreakAll,
      viewBox: entry.viewBox,
      index: index
      /*
       * Here we don't want to use the default Label zIndex,
       * we want it to inherit the zIndex of the LabelList itself
       * which means just rendering as a regular child, without portaling anywhere.
       */,
      zIndex: 0
    }));
  })));
}
LabelList.displayName = 'LabelList';
function LabelListFromLabelProp(_ref2) {
  var {
    label
  } = _ref2;
  if (!label) {
    return null;
  }
  if (label === true) {
    return /*#__PURE__*/external_React_.createElement(LabelList, {
      key: "labelList-implicit"
    });
  }
  if (/*#__PURE__*/external_React_.isValidElement(label) || isLabelContentAFunction(label)) {
    return /*#__PURE__*/external_React_.createElement(LabelList, {
      key: "labelList-implicit",
      content: label
    });
  }
  if (typeof label === 'object') {
    return /*#__PURE__*/external_React_.createElement(LabelList, LabelList_extends({
      key: "labelList-implicit"
    }, label, {
      type: String(label.type)
    }));
  }
  return null;
}
// EXTERNAL MODULE: ./node_modules/react-is/cjs/react-is.production.js
var react_is_production = __webpack_require__(4405);
;// ./node_modules/recharts/es6/util/ReactUtils.js




var SCALE_TYPES = ['auto', 'linear', 'pow', 'sqrt', 'log', 'identity', 'time', 'band', 'point', 'ordinal', 'quantile', 'quantize', 'utc', 'sequential', 'threshold'];

/**
 * @deprecated instead find another approach that does not depend on displayName.
 * Get the display name of a component
 * @param  {Object} Comp Specified Component
 * @return {String}      Display name of Component
 */
var getDisplayName = Comp => {
  if (typeof Comp === 'string') {
    return Comp;
  }
  if (!Comp) {
    return '';
  }
  return Comp.displayName || Comp.name || 'Component';
};

// `toArray` gets called multiple times during the render
// so we can memoize last invocation (since reference to `children` is the same)
var lastChildren = null;
var lastResult = null;

/**
 * @deprecated instead find another approach that does not require reading React Elements from DOM.
 *
 * @param children do not use
 * @return deprecated do not use
 */
var toArray = children => {
  if (children === lastChildren && Array.isArray(lastResult)) {
    return lastResult;
  }
  var result = [];
  external_React_.Children.forEach(children, child => {
    if (isNullish(child)) return;
    if ((0,react_is_production.isFragment)(child)) {
      result = result.concat(toArray(child.props.children));
    } else {
      // @ts-expect-error this could still be Iterable<ReactNode> and TS does not like that
      result.push(child);
    }
  });
  lastResult = result;
  lastChildren = children;
  return result;
};

/**
 * @deprecated instead find another approach that does not require reading React Elements from DOM.
 *
 * Find and return all matched children by type.
 * `type` must be a React.ComponentType
 *
 * @param children do not use
 * @param type do not use
 * @return deprecated do not use
 */
function findAllByType(children, type) {
  var result = [];
  var types = [];
  if (Array.isArray(type)) {
    types = type.map(t => getDisplayName(t));
  } else {
    types = [getDisplayName(type)];
  }
  toArray(children).forEach(child => {
    // @ts-expect-error toArray and lodash.get are not compatible. Let's get rid of the whole findAllByType function
    var childType = get_default()(child, 'type.displayName') || get_default()(child, 'type.name');
    if (childType && types.indexOf(childType) !== -1) {
      result.push(child);
    }
  });
  return result;
}
var isClipDot = dot => {
  if (dot && typeof dot === 'object' && 'clipDot' in dot) {
    return Boolean(dot.clipDot);
  }
  return true;
};
;// ./node_modules/recharts/es6/component/Dots.js
var Dots_excluded = ["points"];
function Dots_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Dots_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Dots_ownKeys(Object(t), !0).forEach(function (r) { Dots_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Dots_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Dots_defineProperty(e, r, t) { return (r = Dots_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Dots_toPropertyKey(t) { var i = Dots_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Dots_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Dots_extends() { return Dots_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Dots_extends.apply(null, arguments); }
function Dots_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Dots_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Dots_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }









function DotItem(_ref) {
  var {
    option,
    dotProps,
    className
  } = _ref;
  if (/*#__PURE__*/(0,external_React_.isValidElement)(option)) {
    // @ts-expect-error we can't type check element cloning properly
    return /*#__PURE__*/(0,external_React_.cloneElement)(option, dotProps);
  }
  if (typeof option === 'function') {
    return option(dotProps);
  }
  var finalClassName = clsx(className, typeof option !== 'boolean' ? option.className : '');
  var _ref2 = dotProps !== null && dotProps !== void 0 ? dotProps : {},
    {
      points
    } = _ref2,
    props = Dots_objectWithoutProperties(_ref2, Dots_excluded);
  return /*#__PURE__*/external_React_.createElement(Dot, Dots_extends({}, props, {
    className: finalClassName
  }));
}
function shouldRenderDots(points, dot) {
  if (points == null) {
    return false;
  }
  if (dot) {
    return true;
  }
  return points.length === 1;
}
function Dots(_ref3) {
  var {
    points,
    dot,
    className,
    dotClassName,
    dataKey,
    baseProps,
    needClip,
    clipPathId,
    zIndex = DefaultZIndexes.scatter
  } = _ref3;
  if (!shouldRenderDots(points, dot)) {
    return null;
  }
  var clipDot = isClipDot(dot);
  var customDotProps = svgPropertiesAndEventsFromUnknown(dot);
  var dots = points.map((entry, i) => {
    var _entry$x, _entry$y;
    var dotProps = Dots_objectSpread(Dots_objectSpread(Dots_objectSpread({
      r: 3
    }, baseProps), customDotProps), {}, {
      index: i,
      cx: (_entry$x = entry.x) !== null && _entry$x !== void 0 ? _entry$x : undefined,
      cy: (_entry$y = entry.y) !== null && _entry$y !== void 0 ? _entry$y : undefined,
      dataKey,
      value: entry.value,
      payload: entry.payload,
      points
    });
    return /*#__PURE__*/external_React_.createElement(DotItem, {
      key: "dot-".concat(i),
      option: dot,
      dotProps: dotProps,
      className: dotClassName
    });
  });
  var layerProps = {};
  if (needClip && clipPathId != null) {
    layerProps.clipPath = "url(#clipPath-".concat(clipDot ? '' : 'dots-').concat(clipPathId, ")");
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: zIndex
  }, /*#__PURE__*/external_React_.createElement(Layer, Dots_extends({
    className: className
  }, layerProps), dots));
}
;// ./node_modules/recharts/es6/component/ActivePoints.js
function ActivePoints_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function ActivePoints_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ActivePoints_ownKeys(Object(t), !0).forEach(function (r) { ActivePoints_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ActivePoints_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function ActivePoints_defineProperty(e, r, t) { return (r = ActivePoints_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function ActivePoints_toPropertyKey(t) { var i = ActivePoints_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function ActivePoints_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }












var ActivePoint = _ref => {
  var {
    point,
    childIndex,
    mainColor,
    activeDot,
    dataKey,
    clipPath
  } = _ref;
  if (activeDot === false || point.x == null || point.y == null) {
    return null;
  }
  var dotPropsTyped = {
    index: childIndex,
    dataKey,
    cx: point.x,
    cy: point.y,
    r: 4,
    fill: mainColor !== null && mainColor !== void 0 ? mainColor : 'none',
    strokeWidth: 2,
    stroke: '#fff',
    payload: point.payload,
    value: point.value
  };

  // @ts-expect-error svgPropertiesNoEventsFromUnknown(activeDot) is contributing unknown props
  var dotProps = ActivePoints_objectSpread(ActivePoints_objectSpread(ActivePoints_objectSpread({}, dotPropsTyped), svgPropertiesNoEventsFromUnknown(activeDot)), adaptEventHandlers(activeDot));
  var dot;
  if (/*#__PURE__*/(0,external_React_.isValidElement)(activeDot)) {
    // @ts-expect-error we're improperly typing events
    dot = /*#__PURE__*/(0,external_React_.cloneElement)(activeDot, dotProps);
  } else if (typeof activeDot === 'function') {
    dot = activeDot(dotProps);
  } else {
    dot = /*#__PURE__*/external_React_.createElement(Dot, dotProps);
  }
  return /*#__PURE__*/external_React_.createElement(Layer, {
    className: "recharts-active-dot",
    clipPath: clipPath
  }, dot);
};
function ActivePoints(_ref2) {
  var {
    points,
    mainColor,
    activeDot,
    itemDataKey,
    clipPath,
    zIndex = DefaultZIndexes.activeDot
  } = _ref2;
  var activeTooltipIndex = useAppSelector(selectActiveTooltipIndex);
  var activeDataPoints = useActiveTooltipDataPoints();
  if (points == null || activeDataPoints == null) {
    return null;
  }
  var activePoint = points.find(p => activeDataPoints.includes(p.payload));
  if (isNullish(activePoint)) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: zIndex
  }, /*#__PURE__*/external_React_.createElement(ActivePoint, {
    point: activePoint,
    childIndex: Number(activeTooltipIndex),
    mainColor: mainColor,
    dataKey: itemDataKey,
    activeDot: activeDot,
    clipPath: clipPath
  }));
}
;// ./node_modules/recharts/es6/state/SetTooltipEntrySettings.js




function SetTooltipEntrySettings(_ref) {
  var {
    tooltipEntrySettings
  } = _ref;
  var dispatch = useAppDispatch();
  var isPanorama = useIsPanorama();
  var prevSettingsRef = (0,external_React_.useRef)(null);
  (0,external_React_.useLayoutEffect)(() => {
    if (isPanorama) {
      // Panorama graphical items should never contribute to Tooltip payload.
      return;
    }
    if (prevSettingsRef.current === null) {
      dispatch(addTooltipEntrySettings(tooltipEntrySettings));
    } else if (prevSettingsRef.current !== tooltipEntrySettings) {
      dispatch(replaceTooltipEntrySettings({
        prev: prevSettingsRef.current,
        next: tooltipEntrySettings
      }));
    }
    prevSettingsRef.current = tooltipEntrySettings;
  }, [tooltipEntrySettings, dispatch, isPanorama]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevSettingsRef.current) {
        dispatch(removeTooltipEntrySettings(prevSettingsRef.current));
        prevSettingsRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
}
;// ./node_modules/recharts/es6/state/selectors/radarSelectors.js
function radarSelectors_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function radarSelectors_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? radarSelectors_ownKeys(Object(t), !0).forEach(function (r) { radarSelectors_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : radarSelectors_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function radarSelectors_defineProperty(e, r, t) { return (r = radarSelectors_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function radarSelectors_toPropertyKey(t) { var i = radarSelectors_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function radarSelectors_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }








var selectRadiusAxisScale = (state, radiusAxisId) => selectPolarAxisScale(state, 'radiusAxis', radiusAxisId);
var selectRadiusAxisForRadar = createSelector([selectRadiusAxisScale], scale => {
  if (scale == null) {
    return undefined;
  }
  return {
    scale
  };
});
var selectRadiusAxisForBandSize = createSelector([selectRadiusAxis, selectRadiusAxisScale], (axisSettings, scale) => {
  if (axisSettings == null || scale == null) {
    return undefined;
  }
  return radarSelectors_objectSpread(radarSelectors_objectSpread({}, axisSettings), {}, {
    scale
  });
});
var radarSelectors_selectRadiusAxisTicks = (state, radiusAxisId, _angleAxisId, isPanorama) => {
  return selectPolarAxisTicks(state, 'radiusAxis', radiusAxisId, isPanorama);
};
var selectAngleAxisForRadar = (state, _radiusAxisId, angleAxisId) => selectAngleAxis(state, angleAxisId);
var selectPolarAxisScaleForRadar = (state, _radiusAxisId, angleAxisId) => selectPolarAxisScale(state, 'angleAxis', angleAxisId);
var selectAngleAxisForBandSize = createSelector([selectAngleAxisForRadar, selectPolarAxisScaleForRadar], (axisSettings, scale) => {
  if (axisSettings == null || scale == null) {
    return undefined;
  }
  return radarSelectors_objectSpread(radarSelectors_objectSpread({}, axisSettings), {}, {
    scale
  });
});
var radarSelectors_selectAngleAxisTicks = (state, _radiusAxisId, angleAxisId, isPanorama) => {
  return selectPolarAxisTicks(state, 'angleAxis', angleAxisId, isPanorama);
};
var selectAngleAxisWithScaleAndViewport = createSelector([selectAngleAxisForRadar, selectPolarAxisScaleForRadar, selectPolarViewBox], (axisOptions, scale, polarViewBox) => {
  if (polarViewBox == null || scale == null) {
    return undefined;
  }
  return {
    scale,
    type: axisOptions.type,
    dataKey: axisOptions.dataKey,
    cx: polarViewBox.cx,
    cy: polarViewBox.cy
  };
});
var pickId = (_state, _radiusAxisId, _angleAxisId, _isPanorama, radarId) => radarId;
var selectBandSizeOfAxis = createSelector([selectChartLayout, selectRadiusAxisForBandSize, radarSelectors_selectRadiusAxisTicks, selectAngleAxisForBandSize, radarSelectors_selectAngleAxisTicks], (layout, radiusAxis, radiusAxisTicks, angleAxis, angleAxisTicks) => {
  if (isCategoricalAxis(layout, 'radiusAxis')) {
    return getBandSizeOfAxis(radiusAxis, radiusAxisTicks, false);
  }
  return getBandSizeOfAxis(angleAxis, angleAxisTicks, false);
});
var selectSynchronisedRadarDataKey = createSelector([selectUnfilteredPolarItems, pickId], (graphicalItems, radarId) => {
  if (graphicalItems == null) {
    return undefined;
  }
  // Find the radar item with the given radarId
  var pgis = graphicalItems.find(item => item.type === 'radar' && radarId === item.id);
  // If found, return its dataKey
  return pgis === null || pgis === void 0 ? void 0 : pgis.dataKey;
});
var selectRadarPoints = createSelector([selectRadiusAxisForRadar, selectAngleAxisWithScaleAndViewport, selectChartDataAndAlwaysIgnoreIndexes, selectSynchronisedRadarDataKey, selectBandSizeOfAxis], (radiusAxis, angleAxis, _ref, dataKey, bandSize) => {
  var {
    chartData,
    dataStartIndex,
    dataEndIndex
  } = _ref;
  if (radiusAxis == null || angleAxis == null || chartData == null || bandSize == null || dataKey == null) {
    return undefined;
  }
  var displayedData = chartData.slice(dataStartIndex, dataEndIndex + 1);
  return computeRadarPoints({
    radiusAxis,
    angleAxis,
    displayedData,
    dataKey,
    bandSize
  });
});
;// ./node_modules/recharts/es6/state/SetLegendPayload.js





function SetLegendPayload(_ref) {
  var {
    legendPayload
  } = _ref;
  var dispatch = useAppDispatch();
  var isPanorama = useIsPanorama();
  var prevPayloadRef = (0,external_React_.useRef)(null);
  (0,external_React_.useLayoutEffect)(() => {
    if (isPanorama) {
      return;
    }
    if (prevPayloadRef.current === null) {
      dispatch(addLegendPayload(legendPayload));
    } else if (prevPayloadRef.current !== legendPayload) {
      dispatch(replaceLegendPayload({
        prev: prevPayloadRef.current,
        next: legendPayload
      }));
    }
    prevPayloadRef.current = legendPayload;
  }, [dispatch, isPanorama, legendPayload]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevPayloadRef.current) {
        dispatch(removeLegendPayload(prevPayloadRef.current));
        prevPayloadRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
}
function SetPolarLegendPayload(_ref2) {
  var {
    legendPayload
  } = _ref2;
  var dispatch = useAppDispatch();
  var layout = useAppSelector(selectChartLayout);
  var prevPayloadRef = (0,external_React_.useRef)(null);
  (0,external_React_.useLayoutEffect)(() => {
    if (layout !== 'centric' && layout !== 'radial') {
      return;
    }
    if (prevPayloadRef.current === null) {
      dispatch(addLegendPayload(legendPayload));
    } else if (prevPayloadRef.current !== legendPayload) {
      dispatch(replaceLegendPayload({
        prev: prevPayloadRef.current,
        next: legendPayload
      }));
    }
    prevPayloadRef.current = legendPayload;
  }, [dispatch, layout, legendPayload]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevPayloadRef.current) {
        dispatch(removeLegendPayload(prevPayloadRef.current));
        prevPayloadRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
}
;// ./node_modules/recharts/es6/util/useAnimationId.js



/**
 * This hook returns a unique animation id for the object input.
 * If input changes (as in, reference equality is different), the animation id will change.
 * If input does not change, the animation id will not change.
 *
 * This is useful for animations. The Animate component
 * does have a `shouldReAnimate` prop but that doesn't seem to be doing what the name implies.
 * Also, we don't always want to re-animate on every render;
 * we only want to re-animate when the input changes. Not the internal state (e.g. `isAnimating`).
 *
 * @param input The object to check for changes. Uses reference equality (=== operator)
 * @param prefix Optional prefix to use for the animation id
 * @returns A unique animation id
 */
function useAnimationId(input) {
  var prefix = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'animation-';
  var animationId = (0,external_React_.useRef)(uniqueId(prefix));
  var prevProps = (0,external_React_.useRef)(input);
  if (prevProps.current !== input) {
    animationId.current = uniqueId(prefix);
    prevProps.current = input;
  }
  return animationId.current;
}
;// ./node_modules/recharts/es6/util/useId.js
var _ref;



/**
 * Fallback for React.useId() for versions prior to React 18.
 * Generates a unique ID using a simple counter and a prefix.
 *
 * @returns A unique ID that remains consistent across renders.
 */
var useIdFallback = () => {
  var [id] = external_React_.useState(() => uniqueId('uid-'));
  return id;
};

/*
 * This weird syntax is used to avoid a build-time error in React 17 and earlier when building with Webpack.
 * See https://github.com/webpack/webpack/issues/14814
 */
var useId = (_ref = external_React_['useId'.toString()]) !== null && _ref !== void 0 ? _ref : useIdFallback;
;// ./node_modules/recharts/es6/util/useUniqueId.js


/**
 * A hook that generates a unique ID. It uses React.useId() in React 18+ for SSR safety
 * and falls back to a client-side-only unique ID generator for older versions.
 *
 * The ID will stay the same across renders, and you can optionally provide a prefix.
 *
 * @param [prefix] - An optional prefix for the generated ID.
 * @param [customId] - An optional custom ID to override the generated one.
 * @returns The unique ID.
 */
function useUniqueId(prefix, customId) {
  /*
   * We have to call this hook here even if we don't use the result because
   * rules of hooks demand that hooks are never called conditionally.
   */
  var generatedId = useId();

  // If a custom ID is provided, it always takes precedence.
  if (customId) {
    return customId;
  }

  // Apply the prefix if one was provided.
  return prefix ? "".concat(prefix, "-").concat(generatedId) : generatedId;
}

/**
 * The useUniqueId hook returns a unique ID that is either reused from external props or generated internally.
 * Either way the ID is now guaranteed to be present so no more nulls or undefined.
 */
;// ./node_modules/recharts/es6/context/RegisterGraphicalItemId.js



var GraphicalItemIdContext = /*#__PURE__*/(0,external_React_.createContext)(undefined);
var RegisterGraphicalItemId = _ref => {
  var {
    id,
    type,
    children
  } = _ref;
  var resolvedId = useUniqueId("recharts-".concat(type), id);
  return /*#__PURE__*/external_React_.createElement(GraphicalItemIdContext.Provider, {
    value: resolvedId
  }, children(resolvedId));
};
function useGraphicalItemId() {
  return (0,external_React_.useContext)(GraphicalItemIdContext);
}
;// ./node_modules/recharts/es6/state/SetGraphicalItem.js



var SetCartesianGraphicalItemImpl = props => {
  var dispatch = useAppDispatch();
  var prevPropsRef = (0,external_React_.useRef)(null);
  (0,external_React_.useLayoutEffect)(() => {
    if (prevPropsRef.current === null) {
      dispatch(addCartesianGraphicalItem(props));
    } else if (prevPropsRef.current !== props) {
      dispatch(replaceCartesianGraphicalItem({
        prev: prevPropsRef.current,
        next: props
      }));
    }
    prevPropsRef.current = props;
  }, [dispatch, props]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevPropsRef.current) {
        dispatch(removeCartesianGraphicalItem(prevPropsRef.current));
        /*
         * Here we have to reset the ref to null because in StrictMode, the effect will run twice,
         * but it will keep the same ref value from the first render.
         *
         * In browser, React will clear the ref after the first effect cleanup,
         * so that wouldn't be an issue.
         *
         * In StrictMode, however, the ref is kept,
         * and in the hook above the code checks for `prevPropsRef.current === null`
         * which would be false so it would not dispatch the `addCartesianGraphicalItem` action again.
         *
         * https://github.com/recharts/recharts/issues/6022
         */
        prevPropsRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
};
var SetCartesianGraphicalItem = /*#__PURE__*/(0,external_React_.memo)(SetCartesianGraphicalItemImpl);
function SetPolarGraphicalItem(props) {
  var dispatch = useAppDispatch();
  (0,external_React_.useLayoutEffect)(() => {
    dispatch(addPolarGraphicalItem(props));
    return () => {
      dispatch(removePolarGraphicalItem(props));
    };
  }, [dispatch, props]);
  return null;
}
;// ./node_modules/recharts/es6/animation/util.js
function util_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function util_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? util_ownKeys(Object(t), !0).forEach(function (r) { util_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : util_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function util_defineProperty(e, r, t) { return (r = util_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function util_toPropertyKey(t) { var i = util_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function util_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/*
 * @description: convert camel case to dash case
 * string => string
 */
var getDashCase = name => name.replace(/([A-Z])/g, v => "-".concat(v.toLowerCase()));
var getTransitionVal = (props, duration, easing) => props.map(prop => "".concat(getDashCase(prop), " ").concat(duration, "ms ").concat(easing)).join(',');

/**
 * Finds the intersection of keys between two objects
 * @param {object} preObj previous object
 * @param {object} nextObj next object
 * @returns an array of keys that exist in both objects
 */
var getIntersectionKeys = (preObj, nextObj) => [Object.keys(preObj), Object.keys(nextObj)].reduce((a, b) => a.filter(c => b.includes(c)));

/**
 * Maps an object to another object
 * @param {function} fn function to map
 * @param {object} obj object to map
 * @returns mapped object
 */
var mapObject = (fn, obj) => Object.keys(obj).reduce((res, key) => util_objectSpread(util_objectSpread({}, res), {}, {
  [key]: fn(key, obj[key])
}), {});
;// ./node_modules/recharts/es6/animation/configUpdate.js
function configUpdate_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function configUpdate_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? configUpdate_ownKeys(Object(t), !0).forEach(function (r) { configUpdate_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : configUpdate_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function configUpdate_defineProperty(e, r, t) { return (r = configUpdate_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function configUpdate_toPropertyKey(t) { var i = configUpdate_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function configUpdate_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }

var alpha = (begin, end, k) => begin + (end - begin) * k;
var needContinue = _ref => {
  var {
    from,
    to
  } = _ref;
  return from !== to;
};
/*
 * @description: cal new from value and velocity in each stepper
 * @return: { [styleProperty]: { from, to, velocity } }
 */
var calStepperVals = (easing, preVals, steps) => {
  var nextStepVals = mapObject((key, val) => {
    if (needContinue(val)) {
      var [newX, newV] = easing(val.from, val.to, val.velocity);
      return configUpdate_objectSpread(configUpdate_objectSpread({}, val), {}, {
        from: newX,
        velocity: newV
      });
    }
    return val;
  }, preVals);
  if (steps < 1) {
    return mapObject((key, val) => {
      if (needContinue(val) && nextStepVals[key] != null) {
        return configUpdate_objectSpread(configUpdate_objectSpread({}, val), {}, {
          velocity: alpha(val.velocity, nextStepVals[key].velocity, steps),
          from: alpha(val.from, nextStepVals[key].from, steps)
        });
      }
      return val;
    }, preVals);
  }
  return calStepperVals(easing, nextStepVals, steps - 1);
};
function createStepperUpdate(from, to, easing, interKeys, render, timeoutController) {
  var preTime;
  var stepperStyle = interKeys.reduce((res, key) => configUpdate_objectSpread(configUpdate_objectSpread({}, res), {}, {
    [key]: {
      from: from[key],
      velocity: 0,
      to: to[key]
    }
  }), {});
  var getCurrStyle = () => mapObject((key, val) => val.from, stepperStyle);
  var shouldStopAnimation = () => !Object.values(stepperStyle).filter(needContinue).length;
  var stopAnimation = null;
  var stepperUpdate = now => {
    if (!preTime) {
      preTime = now;
    }
    var deltaTime = now - preTime;
    var steps = deltaTime / easing.dt;
    stepperStyle = calStepperVals(easing, stepperStyle, steps);
    // get union set and add compatible prefix
    render(configUpdate_objectSpread(configUpdate_objectSpread(configUpdate_objectSpread({}, from), to), getCurrStyle()));
    preTime = now;
    if (!shouldStopAnimation()) {
      stopAnimation = timeoutController.setTimeout(stepperUpdate);
    }
  };

  // return start animation method
  return () => {
    stopAnimation = timeoutController.setTimeout(stepperUpdate);

    // return stop animation method
    return () => {
      var _stopAnimation;
      (_stopAnimation = stopAnimation) === null || _stopAnimation === void 0 || _stopAnimation();
    };
  };
}
function createTimingUpdate(from, to, easing, duration, interKeys, render, timeoutController) {
  var stopAnimation = null;
  var timingStyle = interKeys.reduce((res, key) => {
    var fromElement = from[key];
    var toElement = to[key];
    if (fromElement == null || toElement == null) {
      return res;
    }
    return configUpdate_objectSpread(configUpdate_objectSpread({}, res), {}, {
      [key]: [fromElement, toElement]
    });
  }, {});
  var beginTime;
  var timingUpdate = now => {
    if (!beginTime) {
      beginTime = now;
    }
    var t = (now - beginTime) / duration;
    var currStyle = mapObject((key, val) => alpha(...val, easing(t)), timingStyle);

    // get union set and add compatible prefix
    render(configUpdate_objectSpread(configUpdate_objectSpread(configUpdate_objectSpread({}, from), to), currStyle));
    if (t < 1) {
      stopAnimation = timeoutController.setTimeout(timingUpdate);
    } else {
      var finalStyle = mapObject((key, val) => alpha(...val, easing(1)), timingStyle);
      render(configUpdate_objectSpread(configUpdate_objectSpread(configUpdate_objectSpread({}, from), to), finalStyle));
    }
  };

  // return start animation method
  return () => {
    stopAnimation = timeoutController.setTimeout(timingUpdate);

    // return stop animation method
    return () => {
      var _stopAnimation2;
      (_stopAnimation2 = stopAnimation) === null || _stopAnimation2 === void 0 || _stopAnimation2();
    };
  };
}

// configure update function
// eslint-disable-next-line import/no-default-export
/* harmony default export */ const configUpdate = ((from, to, easing, duration, render, timeoutController) => {
  var interKeys = getIntersectionKeys(from, to);
  if (easing == null) {
    // no animation, just set to final state
    return () => {
      render(configUpdate_objectSpread(configUpdate_objectSpread({}, from), to));
      return () => {};
    };
  }
  return easing.isStepper === true ? createStepperUpdate(from, to, easing, interKeys, render, timeoutController) : createTimingUpdate(from, to, easing, duration, interKeys, render, timeoutController);
});
;// ./node_modules/recharts/es6/animation/easing.js
var ACCURACY = 1e-4;
var cubicBezierFactor = (c1, c2) => [0, 3 * c1, 3 * c2 - 6 * c1, 3 * c1 - 3 * c2 + 1];
var evaluatePolynomial = (params, t) => params.map((param, i) => param * t ** i).reduce((pre, curr) => pre + curr);
var cubicBezier = (c1, c2) => t => {
  var params = cubicBezierFactor(c1, c2);
  return evaluatePolynomial(params, t);
};
var derivativeCubicBezier = (c1, c2) => t => {
  var params = cubicBezierFactor(c1, c2);
  var newParams = [...params.map((param, i) => param * i).slice(1), 0];
  return evaluatePolynomial(newParams, t);
};
var parseCubicBezier = easing => {
  var _easingParts$;
  var easingParts = easing.split('(');
  if (easingParts.length !== 2 || easingParts[0] !== 'cubic-bezier') {
    return null;
  }
  var numbers = (_easingParts$ = easingParts[1]) === null || _easingParts$ === void 0 || (_easingParts$ = _easingParts$.split(')')[0]) === null || _easingParts$ === void 0 ? void 0 : _easingParts$.split(',');
  if (numbers == null || numbers.length !== 4) {
    return null;
  }
  var coords = numbers.map(x => parseFloat(x));
  return [coords[0], coords[1], coords[2], coords[3]];
};
var getBezierCoordinates = function getBezierCoordinates() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  if (args.length === 1) {
    switch (args[0]) {
      case 'linear':
        return [0.0, 0.0, 1.0, 1.0];
      case 'ease':
        return [0.25, 0.1, 0.25, 1.0];
      case 'ease-in':
        return [0.42, 0.0, 1.0, 1.0];
      case 'ease-out':
        return [0.42, 0.0, 0.58, 1.0];
      case 'ease-in-out':
        return [0.0, 0.0, 0.58, 1.0];
      default:
        {
          var easing = parseCubicBezier(args[0]);
          if (easing) {
            return easing;
          }
        }
    }
  }
  if (args.length === 4) {
    return args;
  }

  // Fallback for invalid inputs. The previous implementation was buggy and would lead to NaN.
  // Returning linear easing is a safe default.
  return [0.0, 0.0, 1.0, 1.0];
};
var createBezierEasing = (x1, y1, x2, y2) => {
  var curveX = cubicBezier(x1, x2);
  var curveY = cubicBezier(y1, y2);
  var derCurveX = derivativeCubicBezier(x1, x2);
  var rangeValue = value => {
    if (value > 1) {
      return 1;
    }
    if (value < 0) {
      return 0;
    }
    return value;
  };
  var bezier = _t => {
    var t = _t > 1 ? 1 : _t;
    var x = t;
    for (var i = 0; i < 8; ++i) {
      var evalT = curveX(x) - t;
      var derVal = derCurveX(x);
      if (Math.abs(evalT - t) < ACCURACY || derVal < ACCURACY) {
        return curveY(x);
      }
      x = rangeValue(x - evalT / derVal);
    }
    return curveY(x);
  };
  bezier.isStepper = false;
  return bezier;
};

// calculate cubic-bezier using Newton's method
var configBezier = function configBezier() {
  return createBezierEasing(...getBezierCoordinates(...arguments));
};
var configSpring = function configSpring() {
  var config = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var {
    stiff = 100,
    damping = 8,
    dt = 17
  } = config;
  var stepper = (currX, destX, currV) => {
    var FSpring = -(currX - destX) * stiff;
    var FDamping = currV * damping;
    var newV = currV + (FSpring - FDamping) * dt / 1000;
    var newX = currV * dt / 1000 + currX;
    if (Math.abs(newX - destX) < ACCURACY && Math.abs(newV) < ACCURACY) {
      return [destX, 0];
    }
    return [newX, newV];
  };
  stepper.isStepper = true;
  stepper.dt = dt;
  return stepper;
};
var configEasing = easing => {
  if (typeof easing === 'string') {
    switch (easing) {
      case 'ease':
      case 'ease-in-out':
      case 'ease-out':
      case 'ease-in':
      case 'linear':
        return configBezier(easing);
      case 'spring':
        return configSpring();
      default:
        if (easing.split('(')[0] === 'cubic-bezier') {
          return configBezier(easing);
        }
    }
  }
  if (typeof easing === 'function') {
    return easing;
  }
  return null;
};
;// ./node_modules/recharts/es6/animation/AnimationManager.js
/**
 * Represents a single item in the ReactSmoothQueue.
 * The item can be:
 * - A number representing a delay in milliseconds.
 * - An object representing a style change
 * - A StartAnimationFunction that starts eased transition and calls different render
 *      because of course in Recharts we have to have three ways to do everything
 * - An arbitrary function to be executed
 */

function createAnimateManager(timeoutController) {
  var currStyle;
  var handleChange = () => null;
  var shouldStop = false;
  var cancelTimeout = null;
  var setStyle = _style => {
    if (shouldStop) {
      return;
    }
    if (Array.isArray(_style)) {
      if (!_style.length) {
        return;
      }
      var styles = _style;
      var [curr, ...restStyles] = styles;
      if (typeof curr === 'number') {
        cancelTimeout = timeoutController.setTimeout(setStyle.bind(null, restStyles), curr);
        return;
      }
      setStyle(curr);
      cancelTimeout = timeoutController.setTimeout(setStyle.bind(null, restStyles));
      return;
    }
    if (typeof _style === 'string') {
      currStyle = _style;
      handleChange(currStyle);
    }
    if (typeof _style === 'object') {
      currStyle = _style;
      handleChange(currStyle);
    }
    if (typeof _style === 'function') {
      _style();
    }
  };
  return {
    stop: () => {
      shouldStop = true;
    },
    start: style => {
      shouldStop = false;
      if (cancelTimeout) {
        cancelTimeout();
        cancelTimeout = null;
      }
      setStyle(style);
    },
    subscribe: _handleChange => {
      handleChange = _handleChange;
      return () => {
        handleChange = () => null;
      };
    },
    getTimeoutController: () => timeoutController
  };
}
;// ./node_modules/recharts/es6/animation/timeoutController.js
/**
 * Callback type for the timeout function.
 * Receives current time in milliseconds as an argument.
 */

/**
 * A function that, when called, cancels the timeout.
 */

class RequestAnimationFrameTimeoutController {
  setTimeout(callback) {
    var delay = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
    var startTime = performance.now();
    var requestId = null;
    var executeCallback = now => {
      if (now - startTime >= delay) {
        callback(now);
        // tests fail without the extra if, even when five lines below it's not needed
        // TODO finish transition to the mocked timeout controller and then remove this condition
      } else if (typeof requestAnimationFrame === 'function') {
        requestId = requestAnimationFrame(executeCallback);
      }
    };
    requestId = requestAnimationFrame(executeCallback);
    return () => {
      if (requestId != null) {
        cancelAnimationFrame(requestId);
      }
    };
  }
}
;// ./node_modules/recharts/es6/animation/createDefaultAnimationManager.js


function createDefaultAnimationManager() {
  return createAnimateManager(new RequestAnimationFrameTimeoutController());
}
;// ./node_modules/recharts/es6/animation/useAnimationManager.js


var AnimationManagerContext = /*#__PURE__*/(0,external_React_.createContext)(createDefaultAnimationManager);
function useAnimationManager(animationId, animationManagerFromProps) {
  var contextAnimationManager = (0,external_React_.useContext)(AnimationManagerContext);
  return (0,external_React_.useMemo)(() => animationManagerFromProps !== null && animationManagerFromProps !== void 0 ? animationManagerFromProps : contextAnimationManager(animationId), [animationId, animationManagerFromProps, contextAnimationManager]);
}
;// ./node_modules/recharts/es6/animation/JavascriptAnimate.js







var defaultJavascriptAnimateProps = {
  begin: 0,
  duration: 1000,
  easing: 'ease',
  isActive: true,
  canBegin: true,
  onAnimationEnd: () => {},
  onAnimationStart: () => {}
};
var from = {
  t: 0
};
var to = {
  t: 1
};
function JavascriptAnimate(outsideProps) {
  var props = resolveDefaultProps(outsideProps, defaultJavascriptAnimateProps);
  var {
    isActive: isActiveProp,
    canBegin,
    duration,
    easing,
    begin,
    onAnimationEnd,
    onAnimationStart,
    children
  } = props;
  var isActive = isActiveProp === 'auto' ? !Global.isSsr : isActiveProp;
  var animationManager = useAnimationManager(props.animationId, props.animationManager);
  var [style, setStyle] = (0,external_React_.useState)(isActive ? from : to);
  var stopJSAnimation = (0,external_React_.useRef)(null);
  (0,external_React_.useEffect)(() => {
    if (!isActive) {
      setStyle(to);
    }
  }, [isActive]);
  (0,external_React_.useEffect)(() => {
    if (!isActive || !canBegin) {
      return noop;
    }
    var startAnimation = configUpdate(from, to, configEasing(easing), duration, setStyle, animationManager.getTimeoutController());
    var onAnimationActive = () => {
      stopJSAnimation.current = startAnimation();
    };
    animationManager.start([onAnimationStart, begin, onAnimationActive, duration, onAnimationEnd]);
    return () => {
      animationManager.stop();
      if (stopJSAnimation.current) {
        stopJSAnimation.current();
      }
      onAnimationEnd();
    };
  }, [isActive, canBegin, duration, easing, begin, onAnimationStart, onAnimationEnd, animationManager]);
  return children(style.t);
}
;// ./node_modules/recharts/es6/polar/Radar.js
var Radar_excluded = ["id"];
function Radar_extends() { return Radar_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Radar_extends.apply(null, arguments); }
function Radar_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Radar_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Radar_ownKeys(Object(t), !0).forEach(function (r) { Radar_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Radar_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Radar_defineProperty(e, r, t) { return (r = Radar_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Radar_toPropertyKey(t) { var i = Radar_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Radar_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Radar_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Radar_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Radar_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }


























function getLegendItemColor(stroke, fill) {
  return stroke && stroke !== 'none' ? stroke : fill;
}
var computeLegendPayloadFromRadarSectors = props => {
  var {
    dataKey,
    name,
    stroke,
    fill,
    legendType,
    hide
  } = props;
  return [{
    inactive: hide,
    dataKey,
    type: legendType,
    color: getLegendItemColor(stroke, fill),
    value: getTooltipNameProp(name, dataKey),
    payload: props
  }];
};
var SetRadarTooltipEntrySettings = /*#__PURE__*/external_React_.memo(_ref => {
  var {
    dataKey,
    stroke,
    strokeWidth,
    fill,
    name,
    hide,
    tooltipType,
    id
  } = _ref;
  var tooltipEntrySettings = {
    /*
     * I suppose this here _could_ return props.points
     * because while Radar does not support item tooltip mode, it _could_ support it.
     * But when I actually do return the points here, a defaultIndex test starts failing.
     * So, undefined it is.
     */
    dataDefinedOnItem: undefined,
    getPosition: noop,
    settings: {
      stroke,
      strokeWidth,
      fill,
      nameKey: undefined,
      // RadarChart does not have nameKey unfortunately
      dataKey,
      name: getTooltipNameProp(name, dataKey),
      hide,
      type: tooltipType,
      color: getLegendItemColor(stroke, fill),
      unit: '',
      // why doesn't Radar support unit?
      graphicalItemId: id
    }
  };
  return /*#__PURE__*/external_React_.createElement(SetTooltipEntrySettings, {
    tooltipEntrySettings: tooltipEntrySettings
  });
});
function RadarDotsWrapper(_ref2) {
  var {
    points,
    props
  } = _ref2;
  var {
    dot,
    dataKey
  } = props;
  var {
      id
    } = props,
    propsWithoutId = Radar_objectWithoutProperties(props, Radar_excluded);
  var baseProps = svgPropertiesNoEvents(propsWithoutId);
  return /*#__PURE__*/external_React_.createElement(Dots, {
    points: points,
    dot: dot,
    className: "recharts-radar-dots",
    dotClassName: "recharts-radar-dot",
    dataKey: dataKey,
    baseProps: baseProps
  });
}
function computeRadarPoints(_ref3) {
  var {
    radiusAxis,
    angleAxis,
    displayedData,
    dataKey,
    bandSize
  } = _ref3;
  var {
    cx,
    cy
  } = angleAxis;
  var isRange = false;
  var points = [];
  var angleBandSize = angleAxis.type !== 'number' ? bandSize !== null && bandSize !== void 0 ? bandSize : 0 : 0;
  displayedData.forEach((entry, i) => {
    var _angleAxis$scale$map, _radiusAxis$scale$map;
    var name = getValueByDataKey(entry, angleAxis.dataKey, i);
    var value = getValueByDataKey(entry, dataKey);
    var angle = ((_angleAxis$scale$map = angleAxis.scale.map(name)) !== null && _angleAxis$scale$map !== void 0 ? _angleAxis$scale$map : 0) + angleBandSize;
    var pointValue = Array.isArray(value) ? last_default()(value) : value;
    var radius = isNullish(pointValue) ? 0 : (_radiusAxis$scale$map = radiusAxis.scale.map(pointValue)) !== null && _radiusAxis$scale$map !== void 0 ? _radiusAxis$scale$map : 0;
    if (Array.isArray(value) && value.length >= 2) {
      isRange = true;
    }
    points.push(Radar_objectSpread(Radar_objectSpread({}, polarToCartesian(cx, cy, radius, angle)), {}, {
      // @ts-expect-error getValueByDataKey does not validate the output type
      name,
      // @ts-expect-error getValueByDataKey does not validate the output type
      value,
      cx,
      cy,
      radius,
      angle,
      payload: entry
    }));
  });
  var baseLinePoints = [];
  if (isRange) {
    points.forEach(point => {
      if (Array.isArray(point.value)) {
        var _radiusAxis$scale$map2;
        var baseValue = point.value[0];
        var radius = isNullish(baseValue) ? 0 : (_radiusAxis$scale$map2 = radiusAxis.scale.map(baseValue)) !== null && _radiusAxis$scale$map2 !== void 0 ? _radiusAxis$scale$map2 : 0;
        baseLinePoints.push(Radar_objectSpread(Radar_objectSpread({}, point), {}, {
          radius
        }, polarToCartesian(cx, cy, radius, point.angle)));
      } else {
        baseLinePoints.push(point);
      }
    });
  }
  return {
    points,
    isRange,
    baseLinePoints
  };
}
function RadarLabelListProvider(_ref4) {
  var {
    showLabels,
    points,
    children
  } = _ref4;
  /*
   * Radar provides a Cartesian label list context. Do we want to also provide a polar label list context?
   * That way, users can choose to use polar positions for the Radar labels.
   */
  // const labelListEntries: ReadonlyArray<PolarLabelListEntry> = points.map(
  //   (point): PolarLabelListEntry => ({
  //     value: point.value,
  //     payload: point.payload,
  //     parentViewBox: undefined,
  //     clockWise: false,
  //     viewBox: {
  //       cx: point.cx,
  //       cy: point.cy,
  //       innerRadius: point.radius,
  //       outerRadius: point.radius,
  //       startAngle: point.angle,
  //       endAngle: point.angle,
  //       clockWise: false,
  //     },
  //   }),
  // );

  var labelListEntries = points.map(point => {
    var _point$value;
    var viewBox = {
      x: point.x,
      y: point.y,
      width: 0,
      lowerWidth: 0,
      upperWidth: 0,
      height: 0
    };
    return Radar_objectSpread(Radar_objectSpread({}, viewBox), {}, {
      value: (_point$value = point.value) !== null && _point$value !== void 0 ? _point$value : '',
      payload: point.payload,
      parentViewBox: undefined,
      viewBox,
      fill: undefined
    });
  });
  return /*#__PURE__*/external_React_.createElement(CartesianLabelListContextProvider, {
    value: showLabels ? labelListEntries : undefined
  }, children);
}
function StaticPolygon(_ref5) {
  var {
    points,
    baseLinePoints,
    props
  } = _ref5;
  if (points == null) {
    return null;
  }
  var {
    shape,
    isRange,
    connectNulls
  } = props;
  var handleMouseEnter = e => {
    var {
      onMouseEnter
    } = props;
    if (onMouseEnter) {
      onMouseEnter(props, e);
    }
  };
  var handleMouseLeave = e => {
    var {
      onMouseLeave
    } = props;
    if (onMouseLeave) {
      onMouseLeave(props, e);
    }
  };
  var radar;
  if (/*#__PURE__*/external_React_.isValidElement(shape)) {
    radar = /*#__PURE__*/external_React_.cloneElement(shape, Radar_objectSpread(Radar_objectSpread({}, props), {}, {
      points
    }));
  } else if (typeof shape === 'function') {
    radar = shape(Radar_objectSpread(Radar_objectSpread({}, props), {}, {
      points
    }));
  } else {
    radar = /*#__PURE__*/external_React_.createElement(Polygon, Radar_extends({}, svgPropertiesAndEvents(props), {
      onMouseEnter: handleMouseEnter,
      onMouseLeave: handleMouseLeave,
      points: points,
      baseLinePoints: isRange ? baseLinePoints : undefined,
      connectNulls: connectNulls
    }));
  }
  return /*#__PURE__*/external_React_.createElement(Layer, {
    className: "recharts-radar-polygon"
  }, radar, /*#__PURE__*/external_React_.createElement(RadarDotsWrapper, {
    props: props,
    points: points
  }));
}
var interpolatePolarPoint = (prevPoints, prevPointsDiffFactor, t) => (entry, index) => {
  var prev = prevPoints && prevPoints[Math.floor(index * prevPointsDiffFactor)];
  if (prev) {
    return Radar_objectSpread(Radar_objectSpread({}, entry), {}, {
      x: interpolate(prev.x, entry.x, t),
      y: interpolate(prev.y, entry.y, t)
    });
  }
  return Radar_objectSpread(Radar_objectSpread({}, entry), {}, {
    x: interpolate(entry.cx, entry.x, t),
    y: interpolate(entry.cy, entry.y, t)
  });
};
function PolygonWithAnimation(_ref6) {
  var {
    props,
    previousPointsRef,
    previousBaseLinePointsRef
  } = _ref6;
  var {
    points,
    baseLinePoints,
    isAnimationActive,
    animationBegin,
    animationDuration,
    animationEasing,
    onAnimationEnd,
    onAnimationStart
  } = props;
  var prevPoints = previousPointsRef.current;
  var prevBaseLinePoints = previousBaseLinePointsRef.current;
  var prevPointsDiffFactor = prevPoints ? prevPoints.length / points.length : 1;
  var prevBaseLinePointsDiffFactor = prevBaseLinePoints ? prevBaseLinePoints.length / baseLinePoints.length : 1;
  var animationId = useAnimationId(props, 'recharts-radar-');
  var [isAnimating, setIsAnimating] = (0,external_React_.useState)(false);
  var showLabels = !isAnimating;
  var handleAnimationEnd = (0,external_React_.useCallback)(() => {
    if (typeof onAnimationEnd === 'function') {
      onAnimationEnd();
    }
    setIsAnimating(false);
  }, [onAnimationEnd]);
  var handleAnimationStart = (0,external_React_.useCallback)(() => {
    if (typeof onAnimationStart === 'function') {
      onAnimationStart();
    }
    setIsAnimating(true);
  }, [onAnimationStart]);
  return /*#__PURE__*/external_React_.createElement(RadarLabelListProvider, {
    showLabels: showLabels,
    points: points
  }, /*#__PURE__*/external_React_.createElement(JavascriptAnimate, {
    animationId: animationId,
    begin: animationBegin,
    duration: animationDuration,
    isActive: isAnimationActive,
    easing: animationEasing,
    key: "radar-".concat(animationId),
    onAnimationEnd: handleAnimationEnd,
    onAnimationStart: handleAnimationStart
  }, t => {
    var stepData = t === 1 ? points : points.map(interpolatePolarPoint(prevPoints, prevPointsDiffFactor, t));
    var stepBaseLinePoints = t === 1 ? baseLinePoints : baseLinePoints === null || baseLinePoints === void 0 ? void 0 : baseLinePoints.map(interpolatePolarPoint(prevBaseLinePoints, prevBaseLinePointsDiffFactor, t));
    if (t > 0) {
      // eslint-disable-next-line no-param-reassign
      previousPointsRef.current = stepData;
      // eslint-disable-next-line no-param-reassign
      previousBaseLinePointsRef.current = stepBaseLinePoints;
    }
    return /*#__PURE__*/external_React_.createElement(StaticPolygon, {
      points: stepData,
      baseLinePoints: stepBaseLinePoints,
      props: props
    });
  }), /*#__PURE__*/external_React_.createElement(LabelListFromLabelProp, {
    label: props.label
  }), props.children);
}
function RenderPolygon(props) {
  var previousPointsRef = (0,external_React_.useRef)(undefined);
  var previousBaseLinePointsRef = (0,external_React_.useRef)(undefined);
  return /*#__PURE__*/external_React_.createElement(PolygonWithAnimation, {
    props: props,
    previousPointsRef: previousPointsRef,
    previousBaseLinePointsRef: previousBaseLinePointsRef
  });
}
var defaultRadarProps = {
  activeDot: true,
  angleAxisId: 0,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: 'ease',
  dot: false,
  hide: false,
  isAnimationActive: 'auto',
  label: false,
  legendType: 'rect',
  radiusAxisId: 0,
  zIndex: DefaultZIndexes.area
};
function RadarWithState(props) {
  var {
    hide,
    className,
    points
  } = props;
  if (hide) {
    return null;
  }
  var layerClass = clsx('recharts-radar', className);
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(Layer, {
    className: layerClass
  }, /*#__PURE__*/external_React_.createElement(RenderPolygon, props)), /*#__PURE__*/external_React_.createElement(ActivePoints, {
    points: points,
    mainColor: getLegendItemColor(props.stroke, props.fill),
    itemDataKey: props.dataKey,
    activeDot: props.activeDot
  }));
}
function RadarImpl(props) {
  var isPanorama = useIsPanorama();
  var radarPoints = useAppSelector(state => selectRadarPoints(state, props.radiusAxisId, props.angleAxisId, isPanorama, props.id));
  if ((radarPoints === null || radarPoints === void 0 ? void 0 : radarPoints.points) == null) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(RadarWithState, Radar_extends({}, props, {
    points: radarPoints === null || radarPoints === void 0 ? void 0 : radarPoints.points,
    baseLinePoints: radarPoints === null || radarPoints === void 0 ? void 0 : radarPoints.baseLinePoints,
    isRange: radarPoints === null || radarPoints === void 0 ? void 0 : radarPoints.isRange
  }));
}

/**
 * @consumes PolarChartContext
 * @provides LabelListContext
 */
function Radar(outsideProps) {
  var props = resolveDefaultProps(outsideProps, defaultRadarProps);
  return /*#__PURE__*/external_React_.createElement(RegisterGraphicalItemId, {
    id: props.id,
    type: "radar"
  }, id => /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(SetPolarGraphicalItem, {
    type: "radar",
    id: id,
    data: undefined // Radar does not have data prop, why?
    ,
    dataKey: props.dataKey,
    hide: props.hide,
    angleAxisId: props.angleAxisId,
    radiusAxisId: props.radiusAxisId
  }), /*#__PURE__*/external_React_.createElement(SetPolarLegendPayload, {
    legendPayload: computeLegendPayloadFromRadarSectors(props)
  }), /*#__PURE__*/external_React_.createElement(SetRadarTooltipEntrySettings, {
    dataKey: props.dataKey,
    stroke: props.stroke,
    strokeWidth: props.strokeWidth,
    fill: props.fill,
    name: props.name,
    hide: props.hide,
    tooltipType: props.tooltipType,
    id: id
  }), /*#__PURE__*/external_React_.createElement(RadarImpl, Radar_extends({}, props, {
    id: id
  }))));
}
Radar.displayName = 'Radar';
;// ./node_modules/d3-shape/src/math.js
const abs = Math.abs;
const atan2 = Math.atan2;
const cos = Math.cos;
const math_max = Math.max;
const math_min = Math.min;
const sin = Math.sin;
const math_sqrt = Math.sqrt;

const epsilon = 1e-12;
const pi = Math.PI;
const halfPi = pi / 2;
const tau = 2 * pi;

function acos(x) {
  return x > 1 ? 0 : x < -1 ? pi : Math.acos(x);
}

function asin(x) {
  return x >= 1 ? halfPi : x <= -1 ? -halfPi : Math.asin(x);
}

;// ./node_modules/d3-shape/src/symbol/circle.js


/* harmony default export */ const circle = ({
  draw(context, size) {
    const r = math_sqrt(size / pi);
    context.moveTo(r, 0);
    context.arc(0, 0, r, 0, tau);
  }
});

;// ./node_modules/d3-shape/src/symbol/cross.js


/* harmony default export */ const cross = ({
  draw(context, size) {
    const r = math_sqrt(size / 5) / 2;
    context.moveTo(-3 * r, -r);
    context.lineTo(-r, -r);
    context.lineTo(-r, -3 * r);
    context.lineTo(r, -3 * r);
    context.lineTo(r, -r);
    context.lineTo(3 * r, -r);
    context.lineTo(3 * r, r);
    context.lineTo(r, r);
    context.lineTo(r, 3 * r);
    context.lineTo(-r, 3 * r);
    context.lineTo(-r, r);
    context.lineTo(-3 * r, r);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/diamond.js


const tan30 = math_sqrt(1 / 3);
const tan30_2 = tan30 * 2;

/* harmony default export */ const diamond = ({
  draw(context, size) {
    const y = math_sqrt(size / tan30_2);
    const x = y * tan30;
    context.moveTo(0, -y);
    context.lineTo(x, 0);
    context.lineTo(0, y);
    context.lineTo(-x, 0);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/square.js


/* harmony default export */ const symbol_square = ({
  draw(context, size) {
    const w = math_sqrt(size);
    const x = -w / 2;
    context.rect(x, x, w, w);
  }
});

;// ./node_modules/d3-shape/src/symbol/star.js


const ka = 0.89081309152928522810;
const kr = sin(pi / 10) / sin(7 * pi / 10);
const kx = sin(tau / 10) * kr;
const ky = -cos(tau / 10) * kr;

/* harmony default export */ const star = ({
  draw(context, size) {
    const r = math_sqrt(size * ka);
    const x = kx * r;
    const y = ky * r;
    context.moveTo(0, -r);
    context.lineTo(x, y);
    for (let i = 1; i < 5; ++i) {
      const a = tau * i / 5;
      const c = cos(a);
      const s = sin(a);
      context.lineTo(s * r, -c * r);
      context.lineTo(c * x - s * y, s * x + c * y);
    }
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/triangle.js


const sqrt3 = math_sqrt(3);

/* harmony default export */ const triangle = ({
  draw(context, size) {
    const y = -math_sqrt(size / (sqrt3 * 3));
    context.moveTo(0, y * 2);
    context.lineTo(-sqrt3 * y, -y);
    context.lineTo(sqrt3 * y, -y);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/wye.js


const c = -0.5;
const s = math_sqrt(3) / 2;
const k = 1 / math_sqrt(12);
const a = (k / 2 + 1) * 3;

/* harmony default export */ const wye = ({
  draw(context, size) {
    const r = math_sqrt(size / a);
    const x0 = r / 2, y0 = r * k;
    const x1 = x0, y1 = r * k + r;
    const x2 = -x1, y2 = y1;
    context.moveTo(x0, y0);
    context.lineTo(x1, y1);
    context.lineTo(x2, y2);
    context.lineTo(c * x0 - s * y0, s * x0 + c * y0);
    context.lineTo(c * x1 - s * y1, s * x1 + c * y1);
    context.lineTo(c * x2 - s * y2, s * x2 + c * y2);
    context.lineTo(c * x0 + s * y0, c * y0 - s * x0);
    context.lineTo(c * x1 + s * y1, c * y1 - s * x1);
    context.lineTo(c * x2 + s * y2, c * y2 - s * x2);
    context.closePath();
  }
});

;// ./node_modules/d3-path/src/path.js
const path_pi = Math.PI,
    path_tau = 2 * path_pi,
    path_epsilon = 1e-6,
    tauEpsilon = path_tau - path_epsilon;

function append(strings) {
  this._ += strings[0];
  for (let i = 1, n = strings.length; i < n; ++i) {
    this._ += arguments[i] + strings[i];
  }
}

function appendRound(digits) {
  let d = Math.floor(digits);
  if (!(d >= 0)) throw new Error(`invalid digits: ${digits}`);
  if (d > 15) return append;
  const k = 10 ** d;
  return function(strings) {
    this._ += strings[0];
    for (let i = 1, n = strings.length; i < n; ++i) {
      this._ += Math.round(arguments[i] * k) / k + strings[i];
    }
  };
}

class Path {
  constructor(digits) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null; // end of current subpath
    this._ = "";
    this._append = digits == null ? append : appendRound(digits);
  }
  moveTo(x, y) {
    this._append`M${this._x0 = this._x1 = +x},${this._y0 = this._y1 = +y}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._append`Z`;
    }
  }
  lineTo(x, y) {
    this._append`L${this._x1 = +x},${this._y1 = +y}`;
  }
  quadraticCurveTo(x1, y1, x, y) {
    this._append`Q${+x1},${+y1},${this._x1 = +x},${this._y1 = +y}`;
  }
  bezierCurveTo(x1, y1, x2, y2, x, y) {
    this._append`C${+x1},${+y1},${+x2},${+y2},${this._x1 = +x},${this._y1 = +y}`;
  }
  arcTo(x1, y1, x2, y2, r) {
    x1 = +x1, y1 = +y1, x2 = +x2, y2 = +y2, r = +r;

    // Is the radius negative? Error.
    if (r < 0) throw new Error(`negative radius: ${r}`);

    let x0 = this._x1,
        y0 = this._y1,
        x21 = x2 - x1,
        y21 = y2 - y1,
        x01 = x0 - x1,
        y01 = y0 - y1,
        l01_2 = x01 * x01 + y01 * y01;

    // Is this path empty? Move to (x1,y1).
    if (this._x1 === null) {
      this._append`M${this._x1 = x1},${this._y1 = y1}`;
    }

    // Or, is (x1,y1) coincident with (x0,y0)? Do nothing.
    else if (!(l01_2 > path_epsilon));

    // Or, are (x0,y0), (x1,y1) and (x2,y2) collinear?
    // Equivalently, is (x1,y1) coincident with (x2,y2)?
    // Or, is the radius zero? Line to (x1,y1).
    else if (!(Math.abs(y01 * x21 - y21 * x01) > path_epsilon) || !r) {
      this._append`L${this._x1 = x1},${this._y1 = y1}`;
    }

    // Otherwise, draw an arc!
    else {
      let x20 = x2 - x0,
          y20 = y2 - y0,
          l21_2 = x21 * x21 + y21 * y21,
          l20_2 = x20 * x20 + y20 * y20,
          l21 = Math.sqrt(l21_2),
          l01 = Math.sqrt(l01_2),
          l = r * Math.tan((path_pi - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2),
          t01 = l / l01,
          t21 = l / l21;

      // If the start tangent is not coincident with (x0,y0), line to.
      if (Math.abs(t01 - 1) > path_epsilon) {
        this._append`L${x1 + t01 * x01},${y1 + t01 * y01}`;
      }

      this._append`A${r},${r},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x1 + t21 * x21},${this._y1 = y1 + t21 * y21}`;
    }
  }
  arc(x, y, r, a0, a1, ccw) {
    x = +x, y = +y, r = +r, ccw = !!ccw;

    // Is the radius negative? Error.
    if (r < 0) throw new Error(`negative radius: ${r}`);

    let dx = r * Math.cos(a0),
        dy = r * Math.sin(a0),
        x0 = x + dx,
        y0 = y + dy,
        cw = 1 ^ ccw,
        da = ccw ? a0 - a1 : a1 - a0;

    // Is this path empty? Move to (x0,y0).
    if (this._x1 === null) {
      this._append`M${x0},${y0}`;
    }

    // Or, is (x0,y0) not coincident with the previous point? Line to (x0,y0).
    else if (Math.abs(this._x1 - x0) > path_epsilon || Math.abs(this._y1 - y0) > path_epsilon) {
      this._append`L${x0},${y0}`;
    }

    // Is this arc empty? We’re done.
    if (!r) return;

    // Does the angle go the wrong way? Flip the direction.
    if (da < 0) da = da % path_tau + path_tau;

    // Is this a complete circle? Draw two arcs to complete the circle.
    if (da > tauEpsilon) {
      this._append`A${r},${r},0,1,${cw},${x - dx},${y - dy}A${r},${r},0,1,${cw},${this._x1 = x0},${this._y1 = y0}`;
    }

    // Is this arc non-empty? Draw an arc!
    else if (da > path_epsilon) {
      this._append`A${r},${r},0,${+(da >= path_pi)},${cw},${this._x1 = x + r * Math.cos(a1)},${this._y1 = y + r * Math.sin(a1)}`;
    }
  }
  rect(x, y, w, h) {
    this._append`M${this._x0 = this._x1 = +x},${this._y0 = this._y1 = +y}h${w = +w}v${+h}h${-w}Z`;
  }
  toString() {
    return this._;
  }
}

function path() {
  return new Path;
}

// Allow instanceof d3.path
path.prototype = Path.prototype;

function pathRound(digits = 3) {
  return new Path(+digits);
}

;// ./node_modules/d3-shape/src/path.js


function withPath(shape) {
  let digits = 3;

  shape.digits = function(_) {
    if (!arguments.length) return digits;
    if (_ == null) {
      digits = null;
    } else {
      const d = Math.floor(_);
      if (!(d >= 0)) throw new RangeError(`invalid digits: ${_}`);
      digits = d;
    }
    return shape;
  };

  return () => new Path(digits);
}

;// ./node_modules/d3-shape/src/symbol/asterisk.js


const asterisk_sqrt3 = math_sqrt(3);

/* harmony default export */ const asterisk = ({
  draw(context, size) {
    const r = math_sqrt(size + math_min(size / 28, 0.75)) * 0.59436;
    const t = r / 2;
    const u = t * asterisk_sqrt3;
    context.moveTo(0, r);
    context.lineTo(0, -r);
    context.moveTo(-u, -t);
    context.lineTo(u, t);
    context.moveTo(-u, t);
    context.lineTo(u, -t);
  }
});

;// ./node_modules/d3-shape/src/symbol/diamond2.js


/* harmony default export */ const diamond2 = ({
  draw(context, size) {
    const r = math_sqrt(size) * 0.62625;
    context.moveTo(0, -r);
    context.lineTo(r, 0);
    context.lineTo(0, r);
    context.lineTo(-r, 0);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/plus.js


/* harmony default export */ const plus = ({
  draw(context, size) {
    const r = math_sqrt(size - math_min(size / 7, 2)) * 0.87559;
    context.moveTo(-r, 0);
    context.lineTo(r, 0);
    context.moveTo(0, r);
    context.lineTo(0, -r);
  }
});

;// ./node_modules/d3-shape/src/symbol/square2.js


/* harmony default export */ const square2 = ({
  draw(context, size) {
    const r = math_sqrt(size) * 0.4431;
    context.moveTo(r, r);
    context.lineTo(r, -r);
    context.lineTo(-r, -r);
    context.lineTo(-r, r);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/triangle2.js


const triangle2_sqrt3 = math_sqrt(3);

/* harmony default export */ const triangle2 = ({
  draw(context, size) {
    const s = math_sqrt(size) * 0.6824;
    const t = s  / 2;
    const u = (s * triangle2_sqrt3) / 2; // cos(Math.PI / 6)
    context.moveTo(0, -s);
    context.lineTo(u, t);
    context.lineTo(-u, t);
    context.closePath();
  }
});

;// ./node_modules/d3-shape/src/symbol/times.js


/* harmony default export */ const times = ({
  draw(context, size) {
    const r = math_sqrt(size - math_min(size / 6, 1.7)) * 0.6189;
    context.moveTo(-r, -r);
    context.lineTo(r, r);
    context.moveTo(-r, r);
    context.lineTo(r, -r);
  }
});

;// ./node_modules/d3-shape/src/symbol.js
















// These symbols are designed to be filled.
const symbolsFill = [
  circle,
  cross,
  diamond,
  symbol_square,
  star,
  triangle,
  wye
];

// These symbols are designed to be stroked (with a width of 1.5px and round caps).
const symbolsStroke = [
  circle,
  plus,
  times,
  triangle2,
  asterisk,
  square2,
  diamond2
];

function symbol_Symbol(type, size) {
  let context = null,
      path = withPath(symbol);

  type = typeof type === "function" ? type : constant(type || circle);
  size = typeof size === "function" ? size : constant(size === undefined ? 64 : +size);

  function symbol() {
    let buffer;
    if (!context) context = buffer = path();
    type.apply(this, arguments).draw(context, +size.apply(this, arguments));
    if (buffer) return context = null, buffer + "" || null;
  }

  symbol.type = function(_) {
    return arguments.length ? (type = typeof _ === "function" ? _ : constant(_), symbol) : type;
  };

  symbol.size = function(_) {
    return arguments.length ? (size = typeof _ === "function" ? _ : constant(+_), symbol) : size;
  };

  symbol.context = function(_) {
    return arguments.length ? (context = _ == null ? null : _, symbol) : context;
  };

  return symbol;
}

;// ./node_modules/recharts/es6/shape/Symbols.js
var Symbols_excluded = ["type", "size", "sizeType"];
function Symbols_extends() { return Symbols_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Symbols_extends.apply(null, arguments); }
function Symbols_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Symbols_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Symbols_ownKeys(Object(t), !0).forEach(function (r) { Symbols_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Symbols_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Symbols_defineProperty(e, r, t) { return (r = Symbols_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Symbols_toPropertyKey(t) { var i = Symbols_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Symbols_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Symbols_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Symbols_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Symbols_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }





var symbolFactories = {
  symbolCircle: circle,
  symbolCross: cross,
  symbolDiamond: diamond,
  symbolSquare: symbol_square,
  symbolStar: star,
  symbolTriangle: triangle,
  symbolWye: wye
};
var Symbols_RADIAN = Math.PI / 180;
var getSymbolFactory = type => {
  var name = "symbol".concat(upperFirst(type));
  return symbolFactories[name] || circle;
};
var calculateAreaSize = (size, sizeType, type) => {
  if (sizeType === 'area') {
    return size;
  }
  switch (type) {
    case 'cross':
      return 5 * size * size / 9;
    case 'diamond':
      return 0.5 * size * size / Math.sqrt(3);
    case 'square':
      return size * size;
    case 'star':
      {
        var angle = 18 * Symbols_RADIAN;
        return 1.25 * size * size * (Math.tan(angle) - Math.tan(angle * 2) * Math.tan(angle) ** 2);
      }
    case 'triangle':
      return Math.sqrt(3) * size * size / 4;
    case 'wye':
      return (21 - 10 * Math.sqrt(3)) * size * size / 8;
    default:
      return Math.PI * size * size / 4;
  }
};
var registerSymbol = (key, factory) => {
  symbolFactories["symbol".concat(upperFirst(key))] = factory;
};

/**
 * Renders a symbol from a set of predefined shapes.
 */
var Symbols = _ref => {
  var {
      type = 'circle',
      size = 64,
      sizeType = 'area'
    } = _ref,
    rest = Symbols_objectWithoutProperties(_ref, Symbols_excluded);
  var props = Symbols_objectSpread(Symbols_objectSpread({}, rest), {}, {
    type,
    size,
    sizeType
  });
  var realType = 'circle';
  if (typeof type === 'string') {
    /*
     * Our type guard is not as strong as it could be (i.e. non-existent),
     * and so despite the typescript type saying that `type` is a `SymbolType`,
     * we can get numbers or really anything, so let's have a runtime check here to fix the exception.
     *
     * https://github.com/recharts/recharts/issues/6197
     */
    realType = type;
  }

  /**
   * Calculate the path of curve
   * @return {String} path
   */
  var getPath = () => {
    var symbolFactory = getSymbolFactory(realType);
    var symbol = symbol_Symbol().type(symbolFactory).size(calculateAreaSize(size, sizeType, realType));
    var s = symbol();
    if (s === null) {
      return undefined;
    }
    return s;
  };
  var {
    className,
    cx,
    cy
  } = props;
  var filteredProps = svgPropertiesAndEvents(props);
  if (isNumber(cx) && isNumber(cy) && isNumber(size)) {
    return /*#__PURE__*/external_React_.createElement("path", Symbols_extends({}, filteredProps, {
      className: clsx('recharts-symbols', className),
      transform: "translate(".concat(cx, ", ").concat(cy, ")"),
      d: getPath()
    }));
  }
  return null;
};
Symbols.registerSymbol = registerSymbol;
;// ./node_modules/recharts/es6/component/DefaultLegendContent.js
function DefaultLegendContent_extends() { return DefaultLegendContent_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, DefaultLegendContent_extends.apply(null, arguments); }
function DefaultLegendContent_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function DefaultLegendContent_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? DefaultLegendContent_ownKeys(Object(t), !0).forEach(function (r) { DefaultLegendContent_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : DefaultLegendContent_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function DefaultLegendContent_defineProperty(e, r, t) { return (r = DefaultLegendContent_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function DefaultLegendContent_toPropertyKey(t) { var i = DefaultLegendContent_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function DefaultLegendContent_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }






var SIZE = 32;
var defaultLegendContentDefaultProps = {
  align: 'center',
  iconSize: 14,
  inactiveColor: '#ccc',
  layout: 'horizontal',
  verticalAlign: 'middle'
};
function Icon(_ref) {
  var {
    data,
    iconType,
    inactiveColor
  } = _ref;
  var halfSize = SIZE / 2;
  var sixthSize = SIZE / 6;
  var thirdSize = SIZE / 3;
  var color = data.inactive ? inactiveColor : data.color;
  var preferredIcon = iconType !== null && iconType !== void 0 ? iconType : data.type;
  if (preferredIcon === 'none') {
    return null;
  }
  if (preferredIcon === 'plainline') {
    var _data$payload;
    return /*#__PURE__*/external_React_.createElement("line", {
      strokeWidth: 4,
      fill: "none",
      stroke: color,
      strokeDasharray: (_data$payload = data.payload) === null || _data$payload === void 0 ? void 0 : _data$payload.strokeDasharray,
      x1: 0,
      y1: halfSize,
      x2: SIZE,
      y2: halfSize,
      className: "recharts-legend-icon"
    });
  }
  if (preferredIcon === 'line') {
    return /*#__PURE__*/external_React_.createElement("path", {
      strokeWidth: 4,
      fill: "none",
      stroke: color,
      d: "M0,".concat(halfSize, "h").concat(thirdSize, "\n            A").concat(sixthSize, ",").concat(sixthSize, ",0,1,1,").concat(2 * thirdSize, ",").concat(halfSize, "\n            H").concat(SIZE, "M").concat(2 * thirdSize, ",").concat(halfSize, "\n            A").concat(sixthSize, ",").concat(sixthSize, ",0,1,1,").concat(thirdSize, ",").concat(halfSize),
      className: "recharts-legend-icon"
    });
  }
  if (preferredIcon === 'rect') {
    return /*#__PURE__*/external_React_.createElement("path", {
      stroke: "none",
      fill: color,
      d: "M0,".concat(SIZE / 8, "h").concat(SIZE, "v").concat(SIZE * 3 / 4, "h").concat(-SIZE, "z"),
      className: "recharts-legend-icon"
    });
  }
  if (/*#__PURE__*/external_React_.isValidElement(data.legendIcon)) {
    var iconProps = DefaultLegendContent_objectSpread({}, data);
    delete iconProps.legendIcon;
    return /*#__PURE__*/external_React_.cloneElement(data.legendIcon, iconProps);
  }
  return /*#__PURE__*/external_React_.createElement(Symbols, {
    fill: color,
    cx: halfSize,
    cy: halfSize,
    size: SIZE,
    sizeType: "diameter",
    type: preferredIcon
  });
}
function Items(props) {
  var {
    payload,
    iconSize,
    layout,
    formatter,
    inactiveColor,
    iconType
  } = props;
  var viewBox = {
    x: 0,
    y: 0,
    width: SIZE,
    height: SIZE
  };
  var itemStyle = {
    display: layout === 'horizontal' ? 'inline-block' : 'block',
    marginRight: 10
  };
  var svgStyle = {
    display: 'inline-block',
    verticalAlign: 'middle',
    marginRight: 4
  };
  return payload.map((entry, i) => {
    var finalFormatter = entry.formatter || formatter;
    var className = clsx({
      'recharts-legend-item': true,
      ["legend-item-".concat(i)]: true,
      inactive: entry.inactive
    });
    if (entry.type === 'none') {
      return null;
    }
    var color = entry.inactive ? inactiveColor : entry.color;
    var finalValue = finalFormatter ? finalFormatter(entry.value, entry, i) : entry.value;
    return /*#__PURE__*/external_React_.createElement("li", DefaultLegendContent_extends({
      className: className,
      style: itemStyle,
      key: "legend-item-".concat(i)
    }, adaptEventsOfChild(props, entry, i)), /*#__PURE__*/external_React_.createElement(Surface, {
      width: iconSize,
      height: iconSize,
      viewBox: viewBox,
      style: svgStyle,
      "aria-label": "".concat(finalValue, " legend icon")
    }, /*#__PURE__*/external_React_.createElement(Icon, {
      data: entry,
      iconType: iconType,
      inactiveColor: inactiveColor
    })), /*#__PURE__*/external_React_.createElement("span", {
      className: "recharts-legend-item-text",
      style: {
        color
      }
    }, finalValue));
  });
}

/**
 * This component is by default rendered inside the {@link Legend} component. You would not use it directly.
 *
 * You can use this component to customize the content of the legend,
 * or you can provide your own completely independent content.
 */
var DefaultLegendContent = outsideProps => {
  var props = resolveDefaultProps(outsideProps, defaultLegendContentDefaultProps);
  var {
    payload,
    layout,
    align
  } = props;
  if (!payload || !payload.length) {
    return null;
  }
  var finalStyle = {
    padding: 0,
    margin: 0,
    textAlign: layout === 'horizontal' ? align : 'left'
  };
  return /*#__PURE__*/external_React_.createElement("ul", {
    className: "recharts-default-legend",
    style: finalStyle
  }, /*#__PURE__*/external_React_.createElement(Items, DefaultLegendContent_extends({}, props, {
    payload: payload
  })));
};
// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/uniqBy.js
var uniqBy = __webpack_require__(1081);
var uniqBy_default = /*#__PURE__*/__webpack_require__.n(uniqBy);
;// ./node_modules/recharts/es6/util/payload/getUniqPayload.js


/**
 * This is configuration option that decides how to filter for unique values only:
 *
 * - `false` means "no filter"
 * - `true` means "use recharts default filter"
 * - function means "use return of this function as the default key"
 */

function getUniqPayload(payload, option, defaultUniqBy) {
  if (option === true) {
    return uniqBy_default()(payload, defaultUniqBy);
  }
  if (typeof option === 'function') {
    return uniqBy_default()(payload, option);
  }
  return payload;
}
;// ./node_modules/recharts/es6/context/legendPayloadContext.js



/**
 * Use this hook in Legend, or anywhere else where you want to read the current Legend items.
 * @return all Legend items ready to be rendered
 */
function useLegendPayload() {
  return useAppSelector(selectLegendPayload);
}
;// ./node_modules/recharts/es6/util/useElementOffset.js

var EPS = 1;

/**
 * TODO this documentation does not reflect what this hook is doing, update it.
 * Stores the `offsetHeight`, `offsetLeft`, `offsetTop`, and `offsetWidth` of a DOM element.
 */

/**
 * Use this to listen to element layout changes.
 *
 * Very useful for reading actual sizes of DOM elements relative to the viewport.
 *
 * @param extraDependencies use this to trigger new DOM dimensions read when any of these change. Good for things like payload and label, that will re-render something down in the children array, but you want to read the layout box of a parent.
 * @returns [lastElementOffset, updateElementOffset] most recent value, and setter. Pass the setter to a DOM element ref like this: `<div ref={updateElementOffset}>`
 */
function useElementOffset() {
  var extraDependencies = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var [lastBoundingBox, setLastBoundingBox] = (0,external_React_.useState)({
    height: 0,
    left: 0,
    top: 0,
    width: 0
  });
  var updateBoundingBox = (0,external_React_.useCallback)(node => {
    if (node != null) {
      var rect = node.getBoundingClientRect();
      var box = {
        height: rect.height,
        left: rect.left,
        top: rect.top,
        width: rect.width
      };
      if (Math.abs(box.height - lastBoundingBox.height) > EPS || Math.abs(box.left - lastBoundingBox.left) > EPS || Math.abs(box.top - lastBoundingBox.top) > EPS || Math.abs(box.width - lastBoundingBox.width) > EPS) {
        setLastBoundingBox({
          height: box.height,
          left: box.left,
          top: box.top,
          width: box.width
        });
      }
    }
  },
  // eslint-disable-next-line react-hooks/exhaustive-deps
  [lastBoundingBox.width, lastBoundingBox.height, lastBoundingBox.top, lastBoundingBox.left, ...extraDependencies]);
  return [lastBoundingBox, updateBoundingBox];
}
;// ./node_modules/recharts/es6/component/Legend.js
var Legend_excluded = ["contextPayload"];
function Legend_extends() { return Legend_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Legend_extends.apply(null, arguments); }
function Legend_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Legend_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Legend_ownKeys(Object(t), !0).forEach(function (r) { Legend_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Legend_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Legend_defineProperty(e, r, t) { return (r = Legend_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Legend_toPropertyKey(t) { var i = Legend_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Legend_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Legend_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Legend_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Legend_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }












function defaultUniqBy(entry) {
  return entry.value;
}
function LegendContent(props) {
  var {
      contextPayload
    } = props,
    otherProps = Legend_objectWithoutProperties(props, Legend_excluded);
  var finalPayload = getUniqPayload(contextPayload, props.payloadUniqBy, defaultUniqBy);
  var contentProps = Legend_objectSpread(Legend_objectSpread({}, otherProps), {}, {
    payload: finalPayload
  });
  if (/*#__PURE__*/external_React_.isValidElement(props.content)) {
    return /*#__PURE__*/external_React_.cloneElement(props.content, contentProps);
  }
  if (typeof props.content === 'function') {
    return /*#__PURE__*/external_React_.createElement(props.content, contentProps);
  }
  return /*#__PURE__*/external_React_.createElement(DefaultLegendContent, contentProps);
}
function getDefaultPosition(style, props, margin, chartWidth, chartHeight, box) {
  var {
    layout,
    align,
    verticalAlign
  } = props;
  var hPos, vPos;
  if (!style || (style.left === undefined || style.left === null) && (style.right === undefined || style.right === null)) {
    if (align === 'center' && layout === 'vertical') {
      hPos = {
        left: ((chartWidth || 0) - box.width) / 2
      };
    } else {
      hPos = align === 'right' ? {
        right: margin && margin.right || 0
      } : {
        left: margin && margin.left || 0
      };
    }
  }
  if (!style || (style.top === undefined || style.top === null) && (style.bottom === undefined || style.bottom === null)) {
    if (verticalAlign === 'middle') {
      vPos = {
        top: ((chartHeight || 0) - box.height) / 2
      };
    } else {
      vPos = verticalAlign === 'bottom' ? {
        bottom: margin && margin.bottom || 0
      } : {
        top: margin && margin.top || 0
      };
    }
  }
  return Legend_objectSpread(Legend_objectSpread({}, hPos), vPos);
}
function LegendSettingsDispatcher(props) {
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(setLegendSettings(props));
  }, [dispatch, props]);
  return null;
}
function LegendSizeDispatcher(props) {
  var dispatch = useAppDispatch();
  (0,external_React_.useEffect)(() => {
    dispatch(setLegendSize(props));
    return () => {
      dispatch(setLegendSize({
        width: 0,
        height: 0
      }));
    };
  }, [dispatch, props]);
  return null;
}
function getWidthOrHeight(layout, height, width, maxWidth) {
  if (layout === 'vertical' && height != null) {
    return {
      height
    };
  }
  if (layout === 'horizontal') {
    return {
      width: width || maxWidth
    };
  }
  return null;
}
var legendDefaultProps = {
  align: 'center',
  iconSize: 14,
  inactiveColor: '#ccc',
  itemSorter: 'value',
  layout: 'horizontal',
  verticalAlign: 'bottom'
};

/**
 * @consumes CartesianChartContext
 * @consumes PolarChartContext
 */
function Legend(outsideProps) {
  var props = resolveDefaultProps(outsideProps, legendDefaultProps);
  var contextPayload = useLegendPayload();
  var legendPortalFromContext = useLegendPortal();
  var margin = useMargin();
  var {
    width: widthFromProps,
    height: heightFromProps,
    wrapperStyle,
    portal: portalFromProps
  } = props;
  // The contextPayload is not used directly inside the hook, but we need the onBBoxUpdate call
  // when the payload changes, therefore it's here as a dependency.
  var [lastBoundingBox, updateBoundingBox] = useElementOffset([contextPayload]);
  var chartWidth = useChartWidth();
  var chartHeight = useChartHeight();
  if (chartWidth == null || chartHeight == null) {
    return null;
  }
  var maxWidth = chartWidth - ((margin === null || margin === void 0 ? void 0 : margin.left) || 0) - ((margin === null || margin === void 0 ? void 0 : margin.right) || 0);
  var widthOrHeight = getWidthOrHeight(props.layout, heightFromProps, widthFromProps, maxWidth);
  // if the user supplies their own portal, only use their defined wrapper styles
  var outerStyle = portalFromProps ? wrapperStyle : Legend_objectSpread(Legend_objectSpread({
    position: 'absolute',
    width: (widthOrHeight === null || widthOrHeight === void 0 ? void 0 : widthOrHeight.width) || widthFromProps || 'auto',
    height: (widthOrHeight === null || widthOrHeight === void 0 ? void 0 : widthOrHeight.height) || heightFromProps || 'auto'
  }, getDefaultPosition(wrapperStyle, props, margin, chartWidth, chartHeight, lastBoundingBox)), wrapperStyle);
  var legendPortal = portalFromProps !== null && portalFromProps !== void 0 ? portalFromProps : legendPortalFromContext;
  if (legendPortal == null || contextPayload == null) {
    return null;
  }
  var legendElement = /*#__PURE__*/external_React_.createElement("div", {
    className: "recharts-legend-wrapper",
    style: outerStyle,
    ref: updateBoundingBox
  }, /*#__PURE__*/external_React_.createElement(LegendSettingsDispatcher, {
    layout: props.layout,
    align: props.align,
    verticalAlign: props.verticalAlign,
    itemSorter: props.itemSorter
  }), !portalFromProps && /*#__PURE__*/external_React_.createElement(LegendSizeDispatcher, {
    width: lastBoundingBox.width,
    height: lastBoundingBox.height
  }), /*#__PURE__*/external_React_.createElement(LegendContent, Legend_extends({}, props, widthOrHeight, {
    margin: margin,
    chartWidth: chartWidth,
    chartHeight: chartHeight,
    contextPayload: contextPayload
  })));
  return /*#__PURE__*/(0,external_ReactDOM_namespaceObject.createPortal)(legendElement, legendPortal);
}
Legend.displayName = 'Legend';
;// ./node_modules/recharts/es6/chart/CartesianChart.js
function CartesianChart_extends() { return CartesianChart_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, CartesianChart_extends.apply(null, arguments); }








var CartesianChart_defaultMargin = {
  top: 5,
  right: 5,
  bottom: 5,
  left: 5
};
var defaultCartesianChartProps = {
  accessibilityLayer: true,
  barCategoryGap: '10%',
  barGap: 4,
  layout: 'horizontal',
  margin: CartesianChart_defaultMargin,
  responsive: false,
  reverseStackOrder: false,
  stackOffset: 'none',
  syncMethod: 'index'
};

/**
 * These are one-time, immutable options that decide the chart's behavior.
 * Users who wish to call CartesianChart may decide to pass these options explicitly,
 * but usually we would expect that they use one of the convenience components like BarChart, LineChart, etc.
 */

var CartesianChart = /*#__PURE__*/(0,external_React_.forwardRef)(function CartesianChart(props, ref) {
  var _categoricalChartProp;
  var rootChartProps = resolveDefaultProps(props.categoricalChartProps, defaultCartesianChartProps);
  var {
    chartName,
    defaultTooltipEventType,
    validateTooltipEventTypes,
    tooltipPayloadSearcher,
    categoricalChartProps
  } = props;
  var options = {
    chartName,
    defaultTooltipEventType,
    validateTooltipEventTypes,
    tooltipPayloadSearcher,
    eventEmitter: undefined
  };
  return /*#__PURE__*/external_React_.createElement(RechartsStoreProvider, {
    preloadedState: {
      options
    },
    reduxStoreName: (_categoricalChartProp = categoricalChartProps.id) !== null && _categoricalChartProp !== void 0 ? _categoricalChartProp : chartName
  }, /*#__PURE__*/external_React_.createElement(ChartDataContextProvider, {
    chartData: categoricalChartProps.data
  }), /*#__PURE__*/external_React_.createElement(ReportMainChartProps, {
    layout: rootChartProps.layout,
    margin: rootChartProps.margin
  }), /*#__PURE__*/external_React_.createElement(ReportChartProps, {
    baseValue: rootChartProps.baseValue,
    accessibilityLayer: rootChartProps.accessibilityLayer,
    barCategoryGap: rootChartProps.barCategoryGap,
    maxBarSize: rootChartProps.maxBarSize,
    stackOffset: rootChartProps.stackOffset,
    barGap: rootChartProps.barGap,
    barSize: rootChartProps.barSize,
    syncId: rootChartProps.syncId,
    syncMethod: rootChartProps.syncMethod,
    className: rootChartProps.className,
    reverseStackOrder: rootChartProps.reverseStackOrder
  }), /*#__PURE__*/external_React_.createElement(CategoricalChart, CartesianChart_extends({}, rootChartProps, {
    ref: ref
  })));
});
;// ./node_modules/recharts/es6/chart/BarChart.js




var BarChart_allowedTooltipTypes = ['axis', 'item'];

/**
 * @consumes ResponsiveContainerContext
 * @provides CartesianViewBoxContext
 * @provides CartesianChartContext
 */
var BarChart = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  return /*#__PURE__*/external_React_.createElement(CartesianChart, {
    chartName: "BarChart",
    defaultTooltipEventType: "axis",
    validateTooltipEventTypes: BarChart_allowedTooltipTypes,
    tooltipPayloadSearcher: arrayTooltipSearcher,
    categoricalChartProps: props,
    ref: ref
  });
});
;// ./node_modules/recharts/es6/util/CartesianUtils.js
var rectWithPoints = (_ref, _ref2) => {
  var {
    x: x1,
    y: y1
  } = _ref;
  var {
    x: x2,
    y: y2
  } = _ref2;
  return {
    x: Math.min(x1, x2),
    y: Math.min(y1, y2),
    width: Math.abs(x2 - x1),
    height: Math.abs(y2 - y1)
  };
};

/**
 * Compute the x, y, width, and height of a box from two reference points.
 * @param  {Object} coords     x1, x2, y1, and y2
 * @return {Object} object
 */
var rectWithCoords = _ref3 => {
  var {
    x1,
    y1,
    x2,
    y2
  } = _ref3;
  return rectWithPoints({
    x: x1,
    y: y1
  }, {
    x: x2,
    y: y2
  });
};

/** Normalizes the angle so that 0 <= angle < 180.
 * @param {number} angle Angle in degrees.
 * @return {number} the normalized angle with a value of at least 0 and never greater or equal to 180. */
function normalizeAngle(angle) {
  return (angle % 180 + 180) % 180;
}

/** Calculates the width of the largest horizontal line that fits inside a rectangle that is displayed at an angle.
 * @param {Object} size Width and height of the text in a horizontal position.
 * @param {number} angle Angle in degrees in which the text is displayed.
 * @return {number} The width of the largest horizontal line that fits inside a rectangle that is displayed at an angle.
 */
var getAngledRectangleWidth = function getAngledRectangleWidth(_ref4) {
  var {
    width,
    height
  } = _ref4;
  var angle = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  // Ensure angle is >= 0 && < 180
  var normalizedAngle = normalizeAngle(angle);
  var angleRadians = normalizedAngle * Math.PI / 180;

  /* Depending on the height and width of the rectangle, we may need to use different formulas to calculate the angled
   * width. This threshold defines when each formula should kick in. */
  var angleThreshold = Math.atan(height / width);
  var angledWidth = angleRadians > angleThreshold && angleRadians < Math.PI - angleThreshold ? height / Math.sin(angleRadians) : width / Math.cos(angleRadians);
  return Math.abs(angledWidth);
};
;// ./node_modules/recharts/es6/util/getEveryNth.js
/**
 * Given an array and a number N, return a new array which contains every nTh
 * element of the input array. For n below 1, an empty array is returned.
 * For n equal to 1, the input array is returned as is.
 * For n greater than the length of the array, an array containing the first element
 * and every nTh element after that (if any) is returned.
 *
 * @param array An input array.
 * @param n A number specifying which elements to take.
 * @returns The result array of the same type as the input array.
 */
function getEveryNth(array, n) {
  if (n < 1) {
    return [];
  }
  if (n === 1) {
    return array;
  }
  var result = [];
  for (var i = 0; i < array.length; i += n) {
    var item = array[i];
    if (item !== undefined) {
      result.push(item);
    }
  }
  return result;
}
;// ./node_modules/recharts/es6/util/TickUtils.js


function getAngledTickWidth(contentSize, unitSize, angle) {
  var size = {
    width: contentSize.width + unitSize.width,
    height: contentSize.height + unitSize.height
  };
  return getAngledRectangleWidth(size, angle);
}
function getTickBoundaries(viewBox, sign, sizeKey) {
  var isWidth = sizeKey === 'width';
  var {
    x,
    y,
    width,
    height
  } = viewBox;
  if (sign === 1) {
    return {
      start: isWidth ? x : y,
      end: isWidth ? x + width : y + height
    };
  }
  return {
    start: isWidth ? x + width : y + height,
    end: isWidth ? x : y
  };
}
function isVisible(sign, tickPosition, getSize, start, end) {
  /* Since getSize() is expensive (it reads the ticks' size from the DOM), we do this check first to avoid calculating
   * the tick's size. */
  if (sign * tickPosition < sign * start || sign * tickPosition > sign * end) {
    return false;
  }
  var size = getSize();
  return sign * (tickPosition - sign * size / 2 - start) >= 0 && sign * (tickPosition + sign * size / 2 - end) <= 0;
}
function getNumberIntervalTicks(ticks, interval) {
  return getEveryNth(ticks, interval + 1);
}
;// ./node_modules/recharts/es6/cartesian/getEquidistantTicks.js


function getEquidistantTicks(sign, boundaries, getTickSize, ticks, minTickGap) {
  // If the ticks are readonly, then the slice might not be necessary
  var result = (ticks || []).slice();
  var {
    start: initialStart,
    end
  } = boundaries;
  var index = 0;
  // Premature optimisation idea 1: Estimate a lower bound, and start from there.
  // For now, start from every tick
  var stepsize = 1;
  var start = initialStart;
  var _loop = function _loop() {
      // Given stepsize, evaluate whether every stepsize-th tick can be shown.
      // If it can not, then increase the stepsize by 1, and try again.

      var entry = ticks === null || ticks === void 0 ? void 0 : ticks[index];

      // Break condition - If we have evaluated all the ticks, then we are done.
      if (entry === undefined) {
        return {
          v: getEveryNth(ticks, stepsize)
        };
      }

      // Check if the element collides with the next element
      var i = index;
      var size;
      var getSize = () => {
        if (size === undefined) {
          size = getTickSize(entry, i);
        }
        return size;
      };
      var tickCoord = entry.coordinate;
      // We will always show the first tick.
      var isShow = index === 0 || isVisible(sign, tickCoord, getSize, start, end);
      if (!isShow) {
        // Start all over with a larger stepsize
        index = 0;
        start = initialStart;
        stepsize += 1;
      }
      if (isShow) {
        // If it can be shown, update the start
        start = tickCoord + sign * (getSize() / 2 + minTickGap);
        index += stepsize;
      }
    },
    _ret;
  while (stepsize <= result.length) {
    _ret = _loop();
    if (_ret) return _ret.v;
  }
  return [];
}
function getEquidistantPreserveEndTicks(sign, boundaries, getTickSize, ticks, minTickGap) {
  // If the ticks are readonly, then the slice might not be necessary
  // Reworked logic for getEquidistantPreserveEndTicks
  var result = (ticks || []).slice();
  var len = result.length;
  if (len === 0) {
    return [];
  }
  var {
    start: initialStart,
    end
  } = boundaries;

  // Start with stepsize = 1 (every tick) up to the maximum possible stepsize (len)
  for (var stepsize = 1; stepsize <= len; stepsize++) {
    // 1. Calculate the offset so the last tick (index len - 1) is always included in the sequence.
    var offset = (len - 1) % stepsize;
    var start = initialStart; // `start` tracks the coordinate of the last successfully drawn tick + gap
    var ok = true;

    // 2. Iterate through the end-anchored sequence: offset, offset + stepsize, ..., len - 1
    var _loop2 = function _loop2() {
        var entry = ticks[index];
        if (entry == null) {
          return 0; // continue
        }
        var i = index;
        var size;

        // Use a function to get size, as in the original code
        var getSize = () => {
          if (size === undefined) {
            size = getTickSize(entry, i);
          }
          return size;
        };
        var tickCoord = entry.coordinate;

        // 3. Apply visibility logic (including the first tick special case)
        // The reviewer says *not* to unconditionally bypass checks for the last tick.
        var isShow = index === offset || isVisible(sign, tickCoord, getSize, start, end);
        if (!isShow) {
          // If any tick in this end-anchored sequence fails visibility/collision,
          // reject this stepsize and move to the next iteration (larger stepsize).
          ok = false;
          return 1; // break
        }

        // 4. If showable, update the 'start' coordinate for the next collision check
        if (isShow) {
          start = tickCoord + sign * (getSize() / 2 + minTickGap);
        }
      },
      _ret2;
    for (var index = offset; index < len; index += stepsize) {
      _ret2 = _loop2();
      if (_ret2 === 0) continue;
      if (_ret2 === 1) break;
    }

    // 5. If the entire sequence for this stepsize passed the visibility check, return the result
    if (ok) {
      // Build the final result array explicitly using the validated stepsize and offset.
      var finalTicks = [];
      for (var _index = offset; _index < len; _index += stepsize) {
        var tick = ticks[_index];
        if (tick != null) {
          finalTicks.push(tick);
        }
      }
      return finalTicks;
    }
  }

  // If no stepsize works (this shouldn't happen unless minTickGap is huge), return an empty array.
  return [];
}
;// ./node_modules/recharts/es6/cartesian/getTicks.js
function getTicks_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function getTicks_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? getTicks_ownKeys(Object(t), !0).forEach(function (r) { getTicks_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : getTicks_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function getTicks_defineProperty(e, r, t) { return (r = getTicks_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function getTicks_toPropertyKey(t) { var i = getTicks_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function getTicks_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }





function getTicksEnd(sign, boundaries, getTickSize, ticks, minTickGap) {
  var result = (ticks || []).slice();
  var len = result.length;
  var {
    start
  } = boundaries;
  var {
    end
  } = boundaries;
  var _loop = function _loop(i) {
    var initialEntry = result[i];
    if (initialEntry == null) {
      return 1; // continue
    }
    var entry = initialEntry;
    var size;
    var getSize = () => {
      if (size === undefined) {
        size = getTickSize(initialEntry, i);
      }
      return size;
    };
    if (i === len - 1) {
      var gap = sign * (entry.coordinate + sign * getSize() / 2 - end);
      result[i] = entry = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
        tickCoord: gap > 0 ? entry.coordinate - gap * sign : entry.coordinate
      });
    } else {
      result[i] = entry = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
        tickCoord: entry.coordinate
      });
    }
    if (entry.tickCoord != null) {
      var isShow = isVisible(sign, entry.tickCoord, getSize, start, end);
      if (isShow) {
        end = entry.tickCoord - sign * (getSize() / 2 + minTickGap);
        result[i] = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
          isShow: true
        });
      }
    }
  };
  for (var i = len - 1; i >= 0; i--) {
    if (_loop(i)) continue;
  }
  return result;
}
function getTicksStart(sign, boundaries, getTickSize, ticks, minTickGap, preserveEnd) {
  // This method is mutating the array so clone is indeed necessary here
  var result = (ticks || []).slice();
  var len = result.length;
  var {
    start,
    end
  } = boundaries;
  if (preserveEnd) {
    // Try to guarantee the tail to be displayed
    var tail = ticks[len - 1];
    if (tail != null) {
      var tailSize = getTickSize(tail, len - 1);
      var tailGap = sign * (tail.coordinate + sign * tailSize / 2 - end);
      result[len - 1] = tail = getTicks_objectSpread(getTicks_objectSpread({}, tail), {}, {
        tickCoord: tailGap > 0 ? tail.coordinate - tailGap * sign : tail.coordinate
      });
      if (tail.tickCoord != null) {
        var isTailShow = isVisible(sign, tail.tickCoord, () => tailSize, start, end);
        if (isTailShow) {
          end = tail.tickCoord - sign * (tailSize / 2 + minTickGap);
          result[len - 1] = getTicks_objectSpread(getTicks_objectSpread({}, tail), {}, {
            isShow: true
          });
        }
      }
    }
  }
  var count = preserveEnd ? len - 1 : len;
  var _loop2 = function _loop2(i) {
    var initialEntry = result[i];
    if (initialEntry == null) {
      return 1; // continue
    }
    var entry = initialEntry;
    var size;
    var getSize = () => {
      if (size === undefined) {
        size = getTickSize(initialEntry, i);
      }
      return size;
    };
    if (i === 0) {
      var gap = sign * (entry.coordinate - sign * getSize() / 2 - start);
      result[i] = entry = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
        tickCoord: gap < 0 ? entry.coordinate - gap * sign : entry.coordinate
      });
    } else {
      result[i] = entry = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
        tickCoord: entry.coordinate
      });
    }
    if (entry.tickCoord != null) {
      var isShow = isVisible(sign, entry.tickCoord, getSize, start, end);
      if (isShow) {
        start = entry.tickCoord + sign * (getSize() / 2 + minTickGap);
        result[i] = getTicks_objectSpread(getTicks_objectSpread({}, entry), {}, {
          isShow: true
        });
      }
    }
  };
  for (var i = 0; i < count; i++) {
    if (_loop2(i)) continue;
  }
  return result;
}
function getTicks(props, fontSize, letterSpacing) {
  var {
    tick,
    ticks,
    viewBox,
    minTickGap,
    orientation,
    interval,
    tickFormatter,
    unit,
    angle
  } = props;
  if (!ticks || !ticks.length || !tick) {
    return [];
  }
  if (isNumber(interval) || Global.isSsr) {
    var _getNumberIntervalTic;
    return (_getNumberIntervalTic = getNumberIntervalTicks(ticks, isNumber(interval) ? interval : 0)) !== null && _getNumberIntervalTic !== void 0 ? _getNumberIntervalTic : [];
  }
  var candidates = [];
  var sizeKey = orientation === 'top' || orientation === 'bottom' ? 'width' : 'height';
  var unitSize = unit && sizeKey === 'width' ? getStringSize(unit, {
    fontSize,
    letterSpacing
  }) : {
    width: 0,
    height: 0
  };
  var getTickSize = (content, index) => {
    var value = typeof tickFormatter === 'function' ? tickFormatter(content.value, index) : content.value;
    // Recharts only supports angles when sizeKey === 'width'
    return sizeKey === 'width' ? getAngledTickWidth(getStringSize(value, {
      fontSize,
      letterSpacing
    }), unitSize, angle) : getStringSize(value, {
      fontSize,
      letterSpacing
    })[sizeKey];
  };
  var tick0 = ticks[0];
  var tick1 = ticks[1];
  var sign = ticks.length >= 2 && tick0 != null && tick1 != null ? mathSign(tick1.coordinate - tick0.coordinate) : 1;
  var boundaries = getTickBoundaries(viewBox, sign, sizeKey);
  if (interval === 'equidistantPreserveStart') {
    return getEquidistantTicks(sign, boundaries, getTickSize, ticks, minTickGap);
  }
  if (interval === 'equidistantPreserveEnd') {
    return getEquidistantPreserveEndTicks(sign, boundaries, getTickSize, ticks, minTickGap);
  }
  if (interval === 'preserveStart' || interval === 'preserveStartEnd') {
    candidates = getTicksStart(sign, boundaries, getTickSize, ticks, minTickGap, interval === 'preserveStartEnd');
  } else {
    candidates = getTicksEnd(sign, boundaries, getTickSize, ticks, minTickGap);
  }
  return candidates.filter(entry => entry.isShow);
}
;// ./node_modules/recharts/es6/util/YAxisUtils.js
/**
 * Calculates the width of the Y-axis based on the tick labels and the axis label.
 * @param params - The parameters object.
 * @param [params.ticks] - An array-like object of tick elements, each with a `getBoundingClientRect` method.
 * @param [params.label] - The axis label element, with a `getBoundingClientRect` method.
 * @param [params.labelGapWithTick=5] - The gap between the label and the tick.
 * @param [params.tickSize=0] - The length of the tick line.
 * @param [params.tickMargin=0] - The margin between the tick line and the tick text.
 * @returns The calculated width of the Y-axis.
 */
var getCalculatedYAxisWidth = _ref => {
  var {
    ticks,
    label,
    labelGapWithTick = 5,
    // Default gap between label and tick
    tickSize = 0,
    tickMargin = 0
  } = _ref;
  // find the max width of the tick labels
  var maxTickWidth = 0;
  if (ticks) {
    Array.from(ticks).forEach(tickNode => {
      if (tickNode) {
        var bbox = tickNode.getBoundingClientRect();
        if (bbox.width > maxTickWidth) {
          maxTickWidth = bbox.width;
        }
      }
    });

    // calculate width of the axis label
    var labelWidth = label ? label.getBoundingClientRect().width : 0;
    var tickWidth = tickSize + tickMargin;

    // calculate the updated width of the y-axis
    var updatedYAxisWidth = maxTickWidth + tickWidth + labelWidth + (label ? labelGapWithTick : 0);
    return Math.round(updatedYAxisWidth);
  }
  return 0;
};
;// ./node_modules/recharts/es6/cartesian/CartesianAxis.js
var CartesianAxis_excluded = ["axisLine", "width", "height", "className", "hide", "ticks", "axisType"];
function CartesianAxis_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = CartesianAxis_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function CartesianAxis_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function CartesianAxis_extends() { return CartesianAxis_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, CartesianAxis_extends.apply(null, arguments); }
function CartesianAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function CartesianAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? CartesianAxis_ownKeys(Object(t), !0).forEach(function (r) { CartesianAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : CartesianAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function CartesianAxis_defineProperty(e, r, t) { return (r = CartesianAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function CartesianAxis_toPropertyKey(t) { var i = CartesianAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function CartesianAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileOverview Cartesian Axis
 */

















/** The orientation of the axis in correspondence to the chart */

/** A unit to be appended to a value */

/** The formatter function of tick */

var defaultCartesianAxisProps = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  viewBox: {
    x: 0,
    y: 0,
    width: 0,
    height: 0
  },
  // The orientation of axis
  orientation: 'bottom',
  // The ticks
  ticks: [],
  stroke: '#666',
  tickLine: true,
  axisLine: true,
  tick: true,
  mirror: false,
  minTickGap: 5,
  // The width or height of tick
  tickSize: 6,
  tickMargin: 2,
  interval: 'preserveEnd',
  zIndex: DefaultZIndexes.axis
};

/*
 * `viewBox` and `scale` are SVG attributes.
 * Recharts however - unfortunately - has its own attributes named `viewBox` and `scale`
 * that are completely different data shape and different purpose.
 */

function CartesianAxis_AxisLine(axisLineProps) {
  var {
    x,
    y,
    width,
    height,
    orientation,
    mirror,
    axisLine,
    otherSvgProps
  } = axisLineProps;
  if (!axisLine) {
    return null;
  }
  var props = CartesianAxis_objectSpread(CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, otherSvgProps), svgPropertiesNoEvents(axisLine)), {}, {
    fill: 'none'
  });
  if (orientation === 'top' || orientation === 'bottom') {
    var needHeight = +(orientation === 'top' && !mirror || orientation === 'bottom' && mirror);
    props = CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, props), {}, {
      x1: x,
      y1: y + needHeight * height,
      x2: x + width,
      y2: y + needHeight * height
    });
  } else {
    var needWidth = +(orientation === 'left' && !mirror || orientation === 'right' && mirror);
    props = CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, props), {}, {
      x1: x + needWidth * width,
      y1: y,
      x2: x + needWidth * width,
      y2: y + height
    });
  }
  return /*#__PURE__*/external_React_.createElement("line", CartesianAxis_extends({}, props, {
    className: clsx('recharts-cartesian-axis-line', get_default()(axisLine, 'className'))
  }));
}

/**
 * Calculate the coordinates of endpoints in ticks.
 * @param data The data of a simple tick.
 * @param x The x-coordinate of the axis.
 * @param y The y-coordinate of the axis.
 * @param width The width of the axis.
 * @param height The height of the axis.
 * @param orientation The orientation of the axis.
 * @param tickSize The length of the tick line.
 * @param mirror If true, the ticks are mirrored.
 * @param tickMargin The margin between the tick line and the tick text.
 * @returns An object with `line` and `tick` coordinates.
 * `line` is the coordinates for the tick line, and `tick` is the coordinate for the tick text.
 */
function CartesianAxis_getTickLineCoord(data, x, y, width, height, orientation, tickSize, mirror, tickMargin) {
  var x1, x2, y1, y2, tx, ty;
  var sign = mirror ? -1 : 1;
  var finalTickSize = data.tickSize || tickSize;
  var tickCoord = isNumber(data.tickCoord) ? data.tickCoord : data.coordinate;
  switch (orientation) {
    case 'top':
      x1 = x2 = data.coordinate;
      y2 = y + +!mirror * height;
      y1 = y2 - sign * finalTickSize;
      ty = y1 - sign * tickMargin;
      tx = tickCoord;
      break;
    case 'left':
      y1 = y2 = data.coordinate;
      x2 = x + +!mirror * width;
      x1 = x2 - sign * finalTickSize;
      tx = x1 - sign * tickMargin;
      ty = tickCoord;
      break;
    case 'right':
      y1 = y2 = data.coordinate;
      x2 = x + +mirror * width;
      x1 = x2 + sign * finalTickSize;
      tx = x1 + sign * tickMargin;
      ty = tickCoord;
      break;
    default:
      x1 = x2 = data.coordinate;
      y2 = y + +mirror * height;
      y1 = y2 + sign * finalTickSize;
      ty = y1 + sign * tickMargin;
      tx = tickCoord;
      break;
  }
  return {
    line: {
      x1,
      y1,
      x2,
      y2
    },
    tick: {
      x: tx,
      y: ty
    }
  };
}

/**
 * @param orientation The orientation of the axis.
 * @param mirror If true, the ticks are mirrored.
 * @returns The text anchor of the tick.
 */
function CartesianAxis_getTickTextAnchor(orientation, mirror) {
  switch (orientation) {
    case 'left':
      return mirror ? 'start' : 'end';
    case 'right':
      return mirror ? 'end' : 'start';
    default:
      return 'middle';
  }
}

/**
 * @param orientation The orientation of the axis.
 * @param mirror If true, the ticks are mirrored.
 * @returns The vertical text anchor of the tick.
 */
function getTickVerticalAnchor(orientation, mirror) {
  switch (orientation) {
    case 'left':
    case 'right':
      return 'middle';
    case 'top':
      return mirror ? 'start' : 'end';
    default:
      return mirror ? 'end' : 'start';
  }
}
function TickItem(props) {
  var {
    option,
    tickProps,
    value
  } = props;
  var tickItem;
  var combinedClassName = clsx(tickProps.className, 'recharts-cartesian-axis-tick-value');
  if (/*#__PURE__*/external_React_.isValidElement(option)) {
    // @ts-expect-error element cloning is not typed
    tickItem = /*#__PURE__*/external_React_.cloneElement(option, CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, tickProps), {}, {
      className: combinedClassName
    }));
  } else if (typeof option === 'function') {
    tickItem = option(CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, tickProps), {}, {
      className: combinedClassName
    }));
  } else {
    var className = 'recharts-cartesian-axis-tick-value';
    if (typeof option !== 'boolean') {
      className = clsx(className, getClassNameFromUnknown(option));
    }
    tickItem = /*#__PURE__*/external_React_.createElement(Text, CartesianAxis_extends({}, tickProps, {
      className: className
    }), value);
  }
  return tickItem;
}
var CartesianAxis_Ticks = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
    ticks = [],
    tick,
    tickLine,
    stroke,
    tickFormatter,
    unit,
    padding,
    tickTextProps,
    orientation,
    mirror,
    x,
    y,
    width,
    height,
    tickSize,
    tickMargin,
    fontSize,
    letterSpacing,
    getTicksConfig,
    events,
    axisType
  } = props;
  // @ts-expect-error some properties are optional in props but required in getTicks
  var finalTicks = getTicks(CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, getTicksConfig), {}, {
    ticks
  }), fontSize, letterSpacing);
  var textAnchor = CartesianAxis_getTickTextAnchor(orientation, mirror);
  var verticalAnchor = getTickVerticalAnchor(orientation, mirror);
  var axisProps = svgPropertiesNoEvents(getTicksConfig);
  var customTickProps = svgPropertiesNoEventsFromUnknown(tick);
  var tickLinePropsObject = {};
  if (typeof tickLine === 'object') {
    tickLinePropsObject = tickLine;
  }
  var tickLineProps = CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, axisProps), {}, {
    fill: 'none'
  }, tickLinePropsObject);
  var tickLineCoords = finalTicks.map(entry => CartesianAxis_objectSpread({
    entry
  }, CartesianAxis_getTickLineCoord(entry, x, y, width, height, orientation, tickSize, mirror, tickMargin)));
  var tickLines = tickLineCoords.map(_ref => {
    var {
      entry,
      line: lineCoord
    } = _ref;
    return /*#__PURE__*/external_React_.createElement(Layer, {
      className: "recharts-cartesian-axis-tick",
      key: "tick-".concat(entry.value, "-").concat(entry.coordinate, "-").concat(entry.tickCoord)
    }, tickLine && /*#__PURE__*/external_React_.createElement("line", CartesianAxis_extends({}, tickLineProps, lineCoord, {
      className: clsx('recharts-cartesian-axis-tick-line', get_default()(tickLine, 'className'))
    })));
  });
  var tickLabels = tickLineCoords.map((_ref2, i) => {
    var _ref3, _tickTextProps$angle;
    var {
      entry,
      tick: tickCoord
    } = _ref2;
    // @ts-expect-error we're not checking that padding and orientation types are in sync
    var tickProps = CartesianAxis_objectSpread(CartesianAxis_objectSpread(CartesianAxis_objectSpread(CartesianAxis_objectSpread({
      verticalAnchor
    }, axisProps), {}, {
      textAnchor,
      stroke: 'none',
      fill: stroke
    }, tickCoord), {}, {
      index: i,
      payload: entry,
      visibleTicksCount: finalTicks.length,
      tickFormatter,
      padding
    }, tickTextProps), {}, {
      angle: (_ref3 = (_tickTextProps$angle = tickTextProps === null || tickTextProps === void 0 ? void 0 : tickTextProps.angle) !== null && _tickTextProps$angle !== void 0 ? _tickTextProps$angle : axisProps.angle) !== null && _ref3 !== void 0 ? _ref3 : 0
    });

    // @ts-expect-error customTickProps is contributing unknown props which we don't type properly
    var finalTickProps = CartesianAxis_objectSpread(CartesianAxis_objectSpread({}, tickProps), customTickProps);
    return /*#__PURE__*/external_React_.createElement(Layer, CartesianAxis_extends({
      className: "recharts-cartesian-axis-tick-label",
      key: "tick-label-".concat(entry.value, "-").concat(entry.coordinate, "-").concat(entry.tickCoord)
    }, adaptEventsOfChild(events, entry, i)), tick && /*#__PURE__*/external_React_.createElement(TickItem, {
      option: tick,
      tickProps: finalTickProps,
      value: "".concat(typeof tickFormatter === 'function' ? tickFormatter(entry.value, i) : entry.value).concat(unit || '')
    }));
  });
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-axis-ticks recharts-".concat(axisType, "-ticks")
  }, tickLabels.length > 0 && /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: DefaultZIndexes.label
  }, /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-axis-tick-labels recharts-".concat(axisType, "-tick-labels"),
    ref: ref
  }, tickLabels)), tickLines.length > 0 && /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-axis-tick-lines recharts-".concat(axisType, "-tick-lines")
  }, tickLines));
});
var CartesianAxisComponent = /*#__PURE__*/(0,external_React_.forwardRef)((props, ref) => {
  var {
      axisLine,
      width,
      height,
      className,
      hide,
      ticks,
      axisType
    } = props,
    rest = CartesianAxis_objectWithoutProperties(props, CartesianAxis_excluded);
  var [fontSize, setFontSize] = (0,external_React_.useState)('');
  var [letterSpacing, setLetterSpacing] = (0,external_React_.useState)('');
  var tickRefs = (0,external_React_.useRef)(null);
  (0,external_React_.useImperativeHandle)(ref, () => ({
    getCalculatedWidth: () => {
      var _props$labelRef;
      return getCalculatedYAxisWidth({
        ticks: tickRefs.current,
        label: (_props$labelRef = props.labelRef) === null || _props$labelRef === void 0 ? void 0 : _props$labelRef.current,
        labelGapWithTick: 5,
        tickSize: props.tickSize,
        tickMargin: props.tickMargin
      });
    }
  }));
  var layerRef = (0,external_React_.useCallback)(el => {
    if (el) {
      var tickNodes = el.getElementsByClassName('recharts-cartesian-axis-tick-value');
      tickRefs.current = tickNodes;
      var tick = tickNodes[0];
      if (tick) {
        var computedStyle = window.getComputedStyle(tick);
        var calculatedFontSize = computedStyle.fontSize;
        var calculatedLetterSpacing = computedStyle.letterSpacing;
        if (calculatedFontSize !== fontSize || calculatedLetterSpacing !== letterSpacing) {
          setFontSize(calculatedFontSize);
          setLetterSpacing(calculatedLetterSpacing);
        }
      }
    }
  }, [fontSize, letterSpacing]);
  if (hide) {
    return null;
  }

  /*
   * This is different condition from what validateWidthHeight is doing;
   * the CartesianAxis does allow width or height to be undefined.
   */
  if (width != null && width <= 0 || height != null && height <= 0) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(Layer, {
    className: clsx('recharts-cartesian-axis', className)
  }, /*#__PURE__*/external_React_.createElement(CartesianAxis_AxisLine, {
    x: props.x,
    y: props.y,
    width: width,
    height: height,
    orientation: props.orientation,
    mirror: props.mirror,
    axisLine: axisLine,
    otherSvgProps: svgPropertiesNoEvents(props)
  }), /*#__PURE__*/external_React_.createElement(CartesianAxis_Ticks, {
    ref: layerRef,
    axisType: axisType,
    events: rest,
    fontSize: fontSize,
    getTicksConfig: props,
    height: props.height,
    letterSpacing: letterSpacing,
    mirror: props.mirror,
    orientation: props.orientation,
    padding: props.padding,
    stroke: props.stroke,
    tick: props.tick,
    tickFormatter: props.tickFormatter,
    tickLine: props.tickLine,
    tickMargin: props.tickMargin,
    tickSize: props.tickSize,
    tickTextProps: props.tickTextProps,
    ticks: ticks,
    unit: props.unit,
    width: props.width,
    x: props.x,
    y: props.y
  }), /*#__PURE__*/external_React_.createElement(CartesianLabelContextProvider, {
    x: props.x,
    y: props.y,
    width: props.width,
    height: props.height,
    lowerWidth: props.width,
    upperWidth: props.width
  }, /*#__PURE__*/external_React_.createElement(CartesianLabelFromLabelProp, {
    label: props.label,
    labelRef: props.labelRef
  }), props.children)));
});

/**
 * @deprecated
 *
 * This component is not meant to be used directly in app code.
 * Use XAxis or YAxis instead.
 *
 * Starting from Recharts v4.0 we will make this component internal only.
 */
var CartesianAxis = /*#__PURE__*/external_React_.forwardRef((outsideProps, ref) => {
  var props = resolveDefaultProps(outsideProps, defaultCartesianAxisProps);
  return /*#__PURE__*/external_React_.createElement(CartesianAxisComponent, CartesianAxis_extends({}, props, {
    ref: ref
  }));
});
CartesianAxis.displayName = 'CartesianAxis';
;// ./node_modules/recharts/es6/cartesian/CartesianGrid.js
var CartesianGrid_excluded = ["x1", "y1", "x2", "y2", "key"],
  CartesianGrid_excluded2 = ["offset"],
  CartesianGrid_excluded3 = ["xAxisId", "yAxisId"],
  _excluded4 = ["xAxisId", "yAxisId"];
function CartesianGrid_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function CartesianGrid_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? CartesianGrid_ownKeys(Object(t), !0).forEach(function (r) { CartesianGrid_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : CartesianGrid_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function CartesianGrid_defineProperty(e, r, t) { return (r = CartesianGrid_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function CartesianGrid_toPropertyKey(t) { var i = CartesianGrid_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function CartesianGrid_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function CartesianGrid_extends() { return CartesianGrid_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, CartesianGrid_extends.apply(null, arguments); }
function CartesianGrid_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = CartesianGrid_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function CartesianGrid_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
















/**
 * The <CartesianGrid horizontal
 */

var Background = props => {
  var {
    fill
  } = props;
  if (!fill || fill === 'none') {
    return null;
  }
  var {
    fillOpacity,
    x,
    y,
    width,
    height,
    ry
  } = props;
  return /*#__PURE__*/external_React_.createElement("rect", {
    x: x,
    y: y,
    ry: ry,
    width: width,
    height: height,
    stroke: "none",
    fill: fill,
    fillOpacity: fillOpacity,
    className: "recharts-cartesian-grid-bg"
  });
};
function LineItem(_ref) {
  var {
    option,
    lineItemProps
  } = _ref;
  var lineItem;
  if (/*#__PURE__*/external_React_.isValidElement(option)) {
    // @ts-expect-error typescript does not see the props type when cloning an element
    lineItem = /*#__PURE__*/external_React_.cloneElement(option, lineItemProps);
  } else if (typeof option === 'function') {
    lineItem = option(lineItemProps);
  } else {
    var _svgPropertiesNoEvent;
    var {
        x1,
        y1,
        x2,
        y2,
        key
      } = lineItemProps,
      others = CartesianGrid_objectWithoutProperties(lineItemProps, CartesianGrid_excluded);
    var _ref2 = (_svgPropertiesNoEvent = svgPropertiesNoEvents(others)) !== null && _svgPropertiesNoEvent !== void 0 ? _svgPropertiesNoEvent : {},
      {
        offset: __
      } = _ref2,
      restOfFilteredProps = CartesianGrid_objectWithoutProperties(_ref2, CartesianGrid_excluded2);
    lineItem = /*#__PURE__*/external_React_.createElement("line", CartesianGrid_extends({}, restOfFilteredProps, {
      x1: x1,
      y1: y1,
      x2: x2,
      y2: y2,
      fill: "none",
      key: key
    }));
  }
  return lineItem;
}
function HorizontalGridLines(props) {
  var {
    x,
    width,
    horizontal = true,
    horizontalPoints
  } = props;
  if (!horizontal || !horizontalPoints || !horizontalPoints.length) {
    return null;
  }
  var {
      xAxisId,
      yAxisId
    } = props,
    otherLineItemProps = CartesianGrid_objectWithoutProperties(props, CartesianGrid_excluded3);
  var items = horizontalPoints.map((entry, i) => {
    var lineItemProps = CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, otherLineItemProps), {}, {
      x1: x,
      y1: entry,
      x2: x + width,
      y2: entry,
      key: "line-".concat(i),
      index: i
    });
    return /*#__PURE__*/external_React_.createElement(LineItem, {
      key: "line-".concat(i),
      option: horizontal,
      lineItemProps: lineItemProps
    });
  });
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-grid-horizontal"
  }, items);
}
function VerticalGridLines(props) {
  var {
    y,
    height,
    vertical = true,
    verticalPoints
  } = props;
  if (!vertical || !verticalPoints || !verticalPoints.length) {
    return null;
  }
  var {
      xAxisId,
      yAxisId
    } = props,
    otherLineItemProps = CartesianGrid_objectWithoutProperties(props, _excluded4);
  var items = verticalPoints.map((entry, i) => {
    var lineItemProps = CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, otherLineItemProps), {}, {
      x1: entry,
      y1: y,
      x2: entry,
      y2: y + height,
      key: "line-".concat(i),
      index: i
    });
    return /*#__PURE__*/external_React_.createElement(LineItem, {
      option: vertical,
      lineItemProps: lineItemProps,
      key: "line-".concat(i)
    });
  });
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-grid-vertical"
  }, items);
}
function HorizontalStripes(props) {
  var {
    horizontalFill,
    fillOpacity,
    x,
    y,
    width,
    height,
    horizontalPoints,
    horizontal = true
  } = props;
  if (!horizontal || !horizontalFill || !horizontalFill.length || horizontalPoints == null) {
    return null;
  }
  var roundedSortedHorizontalPoints = horizontalPoints.map(e => Math.round(e + y - y)).sort((a, b) => a - b);
  // Why is this condition `!==` instead of `<=` ?
  if (y !== roundedSortedHorizontalPoints[0]) {
    roundedSortedHorizontalPoints.unshift(0);
  }
  var items = roundedSortedHorizontalPoints.map((entry, i) => {
    // Why do we strip only the last stripe if it is invisible, and not all invisible stripes?
    var nextPoint = roundedSortedHorizontalPoints[i + 1];
    var lastStripe = nextPoint == null;
    var lineHeight = lastStripe ? y + height - entry : nextPoint - entry;
    if (lineHeight <= 0) {
      return null;
    }
    var colorIndex = i % horizontalFill.length;
    return /*#__PURE__*/external_React_.createElement("rect", {
      key: "react-".concat(i),
      y: entry,
      x: x,
      height: lineHeight,
      width: width,
      stroke: "none",
      fill: horizontalFill[colorIndex],
      fillOpacity: fillOpacity,
      className: "recharts-cartesian-grid-bg"
    });
  });
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-gridstripes-horizontal"
  }, items);
}
function VerticalStripes(props) {
  var {
    vertical = true,
    verticalFill,
    fillOpacity,
    x,
    y,
    width,
    height,
    verticalPoints
  } = props;
  if (!vertical || !verticalFill || !verticalFill.length) {
    return null;
  }
  var roundedSortedVerticalPoints = verticalPoints.map(e => Math.round(e + x - x)).sort((a, b) => a - b);
  if (x !== roundedSortedVerticalPoints[0]) {
    roundedSortedVerticalPoints.unshift(0);
  }
  var items = roundedSortedVerticalPoints.map((entry, i) => {
    var nextPoint = roundedSortedVerticalPoints[i + 1];
    var lastStripe = nextPoint == null;
    var lineWidth = lastStripe ? x + width - entry : nextPoint - entry;
    if (lineWidth <= 0) {
      return null;
    }
    var colorIndex = i % verticalFill.length;
    return /*#__PURE__*/external_React_.createElement("rect", {
      key: "react-".concat(i),
      x: entry,
      y: y,
      width: lineWidth,
      height: height,
      stroke: "none",
      fill: verticalFill[colorIndex],
      fillOpacity: fillOpacity,
      className: "recharts-cartesian-grid-bg"
    });
  });
  return /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-gridstripes-vertical"
  }, items);
}
var defaultVerticalCoordinatesGenerator = (_ref3, syncWithTicks) => {
  var {
    xAxis,
    width,
    height,
    offset
  } = _ref3;
  return getCoordinatesOfGrid(getTicks(CartesianGrid_objectSpread(CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, defaultCartesianAxisProps), xAxis), {}, {
    ticks: getTicksOfAxis(xAxis, true),
    viewBox: {
      x: 0,
      y: 0,
      width,
      height
    }
  })), offset.left, offset.left + offset.width, syncWithTicks);
};
var defaultHorizontalCoordinatesGenerator = (_ref4, syncWithTicks) => {
  var {
    yAxis,
    width,
    height,
    offset
  } = _ref4;
  return getCoordinatesOfGrid(getTicks(CartesianGrid_objectSpread(CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, defaultCartesianAxisProps), yAxis), {}, {
    ticks: getTicksOfAxis(yAxis, true),
    viewBox: {
      x: 0,
      y: 0,
      width,
      height
    }
  })), offset.top, offset.top + offset.height, syncWithTicks);
};
var defaultCartesianGridProps = {
  horizontal: true,
  vertical: true,
  // The ordinates of horizontal grid lines
  horizontalPoints: [],
  // The abscissas of vertical grid lines
  verticalPoints: [],
  stroke: '#ccc',
  fill: 'none',
  // The fill of colors of grid lines
  verticalFill: [],
  horizontalFill: [],
  xAxisId: 0,
  yAxisId: 0,
  syncWithTicks: false,
  zIndex: DefaultZIndexes.grid
};

/**
 * Renders background grid with lines and fill colors in a Cartesian chart.
 *
 * @consumes CartesianChartContext
 */
function CartesianGrid(props) {
  var chartWidth = useChartWidth();
  var chartHeight = useChartHeight();
  var offset = useOffsetInternal();
  var propsIncludingDefaults = CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, resolveDefaultProps(props, defaultCartesianGridProps)), {}, {
    x: isNumber(props.x) ? props.x : offset.left,
    y: isNumber(props.y) ? props.y : offset.top,
    width: isNumber(props.width) ? props.width : offset.width,
    height: isNumber(props.height) ? props.height : offset.height
  });
  var {
    xAxisId,
    yAxisId,
    x,
    y,
    width,
    height,
    syncWithTicks,
    horizontalValues,
    verticalValues
  } = propsIncludingDefaults;
  var isPanorama = useIsPanorama();
  var xAxis = useAppSelector(state => selectAxisPropsNeededForCartesianGridTicksGenerator(state, 'xAxis', xAxisId, isPanorama));
  var yAxis = useAppSelector(state => selectAxisPropsNeededForCartesianGridTicksGenerator(state, 'yAxis', yAxisId, isPanorama));
  if (!isPositiveNumber(width) || !isPositiveNumber(height) || !isNumber(x) || !isNumber(y)) {
    return null;
  }

  /*
   * verticalCoordinatesGenerator and horizontalCoordinatesGenerator are defined
   * outside the propsIncludingDefaults because they were never part of the original props
   * and they were never passed as a prop down to horizontal/vertical custom elements.
   * If we add these two to propsIncludingDefaults then we are changing public API.
   * Not a bad thing per se but also not necessary.
   */
  var verticalCoordinatesGenerator = propsIncludingDefaults.verticalCoordinatesGenerator || defaultVerticalCoordinatesGenerator;
  var horizontalCoordinatesGenerator = propsIncludingDefaults.horizontalCoordinatesGenerator || defaultHorizontalCoordinatesGenerator;
  var {
    horizontalPoints,
    verticalPoints
  } = propsIncludingDefaults;

  // No horizontal points are specified
  if ((!horizontalPoints || !horizontalPoints.length) && typeof horizontalCoordinatesGenerator === 'function') {
    var isHorizontalValues = horizontalValues && horizontalValues.length;
    var generatorResult = horizontalCoordinatesGenerator({
      yAxis: yAxis ? CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, yAxis), {}, {
        ticks: isHorizontalValues ? horizontalValues : yAxis.ticks
      }) : undefined,
      width: chartWidth !== null && chartWidth !== void 0 ? chartWidth : width,
      height: chartHeight !== null && chartHeight !== void 0 ? chartHeight : height,
      offset
    }, isHorizontalValues ? true : syncWithTicks);
    warn(Array.isArray(generatorResult), "horizontalCoordinatesGenerator should return Array but instead it returned [".concat(typeof generatorResult, "]"));
    if (Array.isArray(generatorResult)) {
      horizontalPoints = generatorResult;
    }
  }

  // No vertical points are specified
  if ((!verticalPoints || !verticalPoints.length) && typeof verticalCoordinatesGenerator === 'function') {
    var isVerticalValues = verticalValues && verticalValues.length;
    var _generatorResult = verticalCoordinatesGenerator({
      xAxis: xAxis ? CartesianGrid_objectSpread(CartesianGrid_objectSpread({}, xAxis), {}, {
        ticks: isVerticalValues ? verticalValues : xAxis.ticks
      }) : undefined,
      width: chartWidth !== null && chartWidth !== void 0 ? chartWidth : width,
      height: chartHeight !== null && chartHeight !== void 0 ? chartHeight : height,
      offset
    }, isVerticalValues ? true : syncWithTicks);
    warn(Array.isArray(_generatorResult), "verticalCoordinatesGenerator should return Array but instead it returned [".concat(typeof _generatorResult, "]"));
    if (Array.isArray(_generatorResult)) {
      verticalPoints = _generatorResult;
    }
  }
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: propsIncludingDefaults.zIndex
  }, /*#__PURE__*/external_React_.createElement("g", {
    className: "recharts-cartesian-grid"
  }, /*#__PURE__*/external_React_.createElement(Background, {
    fill: propsIncludingDefaults.fill,
    fillOpacity: propsIncludingDefaults.fillOpacity,
    x: propsIncludingDefaults.x,
    y: propsIncludingDefaults.y,
    width: propsIncludingDefaults.width,
    height: propsIncludingDefaults.height,
    ry: propsIncludingDefaults.ry
  }), /*#__PURE__*/external_React_.createElement(HorizontalStripes, CartesianGrid_extends({}, propsIncludingDefaults, {
    horizontalPoints: horizontalPoints
  })), /*#__PURE__*/external_React_.createElement(VerticalStripes, CartesianGrid_extends({}, propsIncludingDefaults, {
    verticalPoints: verticalPoints
  })), /*#__PURE__*/external_React_.createElement(HorizontalGridLines, CartesianGrid_extends({}, propsIncludingDefaults, {
    offset: offset,
    horizontalPoints: horizontalPoints,
    xAxis: xAxis,
    yAxis: yAxis
  })), /*#__PURE__*/external_React_.createElement(VerticalGridLines, CartesianGrid_extends({}, propsIncludingDefaults, {
    offset: offset,
    verticalPoints: verticalPoints,
    xAxis: xAxis,
    yAxis: yAxis
  }))));
}
CartesianGrid.displayName = 'CartesianGrid';
;// ./node_modules/recharts/es6/util/axisPropsAreEqual.js
var axisPropsAreEqual_excluded = ["domain", "range"],
  axisPropsAreEqual_excluded2 = ["domain", "range"];
function axisPropsAreEqual_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = axisPropsAreEqual_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function axisPropsAreEqual_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }

function shortArraysAreEqual(arr1, arr2) {
  if (arr1 === arr2) {
    return true;
  }
  if (Array.isArray(arr1) && arr1.length === 2 && Array.isArray(arr2) && arr2.length === 2) {
    return arr1[0] === arr2[0] && arr1[1] === arr2[1];
  }
  return false;
}

/**
 * Usually we would not compare array props deeply for performance consideration.
 * However, for axis props, domain is sometimes defined as a two-elements array, and range is always
 * a two-elements array. So we can do a shallow comparison for the rest props and a shallow
 * comparison for these two array props.
 * @param prevProps
 * @param nextProps
 */
function axisPropsAreEqual(prevProps, nextProps) {
  if (prevProps === nextProps) {
    return true;
  }
  var {
      domain: prevDomain,
      range: prevRange
    } = prevProps,
    prevRest = axisPropsAreEqual_objectWithoutProperties(prevProps, axisPropsAreEqual_excluded);
  var {
      domain: nextDomain,
      range: nextRange
    } = nextProps,
    nextRest = axisPropsAreEqual_objectWithoutProperties(nextProps, axisPropsAreEqual_excluded2);
  if (!shortArraysAreEqual(prevDomain, nextDomain)) {
    return false;
  }
  if (!shortArraysAreEqual(prevRange, nextRange)) {
    return false;
  }
  return propsAreEqual(prevRest, nextRest);
}
;// ./node_modules/recharts/es6/cartesian/XAxis.js
var XAxis_excluded = ["type"],
  XAxis_excluded2 = ["dangerouslySetInnerHTML", "ticks", "scale"],
  XAxis_excluded3 = ["id", "scale"];
function XAxis_extends() { return XAxis_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, XAxis_extends.apply(null, arguments); }
function XAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function XAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? XAxis_ownKeys(Object(t), !0).forEach(function (r) { XAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : XAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function XAxis_defineProperty(e, r, t) { return (r = XAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function XAxis_toPropertyKey(t) { var i = XAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function XAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function XAxis_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = XAxis_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function XAxis_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
/**
 * @fileOverview X Axis
 */













function SetXAxisSettings(props) {
  var dispatch = useAppDispatch();
  var prevSettingsRef = (0,external_React_.useRef)(null);
  var layout = useCartesianChartLayout();
  var {
      type: typeFromProps
    } = props,
    restProps = XAxis_objectWithoutProperties(props, XAxis_excluded);
  var evaluatedType = getAxisTypeBasedOnLayout(layout, 'xAxis', typeFromProps);
  var settings = (0,external_React_.useMemo)(() => {
    if (evaluatedType == null) {
      return undefined;
    }
    return XAxis_objectSpread(XAxis_objectSpread({}, restProps), {}, {
      type: evaluatedType
    });
  }, [restProps, evaluatedType]);
  (0,external_React_.useLayoutEffect)(() => {
    if (settings == null) {
      return;
    }
    if (prevSettingsRef.current === null) {
      dispatch(addXAxis(settings));
    } else if (prevSettingsRef.current !== settings) {
      dispatch(replaceXAxis({
        prev: prevSettingsRef.current,
        next: settings
      }));
    }
    prevSettingsRef.current = settings;
  }, [settings, dispatch]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevSettingsRef.current) {
        dispatch(removeXAxis(prevSettingsRef.current));
        prevSettingsRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
}
var XAxisImpl = props => {
  var {
    xAxisId,
    className
  } = props;
  var viewBox = useAppSelector(selectAxisViewBox);
  var isPanorama = useIsPanorama();
  var axisType = 'xAxis';
  var cartesianTickItems = useAppSelector(state => selectTicksOfAxis(state, axisType, xAxisId, isPanorama));
  var axisSize = useAppSelector(state => selectXAxisSize(state, xAxisId));
  var position = useAppSelector(state => selectXAxisPosition(state, xAxisId));
  /*
   * Here we select settings from the store and prefer to use them instead of the actual props
   * so that the chart is consistent. If we used the props directly, some components will use axis settings
   * from state and some from props and because there is a render step between these two, they might be showing different things.
   * https://github.com/recharts/recharts/issues/6257
   */
  var synchronizedSettings = useAppSelector(state => selectXAxisSettingsNoDefaults(state, xAxisId));
  if (axisSize == null || position == null || synchronizedSettings == null) {
    return null;
  }
  var {
      dangerouslySetInnerHTML,
      ticks,
      scale: del
    } = props,
    allOtherProps = XAxis_objectWithoutProperties(props, XAxis_excluded2);
  var {
      id,
      scale: del2
    } = synchronizedSettings,
    restSynchronizedSettings = XAxis_objectWithoutProperties(synchronizedSettings, XAxis_excluded3);
  return /*#__PURE__*/external_React_.createElement(CartesianAxis, XAxis_extends({}, allOtherProps, restSynchronizedSettings, {
    x: position.x,
    y: position.y,
    width: axisSize.width,
    height: axisSize.height,
    className: clsx("recharts-".concat(axisType, " ").concat(axisType), className),
    viewBox: viewBox,
    ticks: cartesianTickItems,
    axisType: axisType
  }));
};
var xAxisDefaultProps = {
  allowDataOverflow: implicitXAxis.allowDataOverflow,
  allowDecimals: implicitXAxis.allowDecimals,
  allowDuplicatedCategory: implicitXAxis.allowDuplicatedCategory,
  angle: implicitXAxis.angle,
  axisLine: defaultCartesianAxisProps.axisLine,
  height: implicitXAxis.height,
  hide: false,
  includeHidden: implicitXAxis.includeHidden,
  interval: implicitXAxis.interval,
  label: false,
  minTickGap: implicitXAxis.minTickGap,
  mirror: implicitXAxis.mirror,
  orientation: implicitXAxis.orientation,
  padding: implicitXAxis.padding,
  reversed: implicitXAxis.reversed,
  scale: implicitXAxis.scale,
  tick: implicitXAxis.tick,
  tickCount: implicitXAxis.tickCount,
  tickLine: defaultCartesianAxisProps.tickLine,
  tickSize: defaultCartesianAxisProps.tickSize,
  type: implicitXAxis.type,
  xAxisId: 0
};
var XAxisSettingsDispatcher = outsideProps => {
  var props = resolveDefaultProps(outsideProps, xAxisDefaultProps);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(SetXAxisSettings, {
    allowDataOverflow: props.allowDataOverflow,
    allowDecimals: props.allowDecimals,
    allowDuplicatedCategory: props.allowDuplicatedCategory,
    angle: props.angle,
    dataKey: props.dataKey,
    domain: props.domain,
    height: props.height,
    hide: props.hide,
    id: props.xAxisId,
    includeHidden: props.includeHidden,
    interval: props.interval,
    minTickGap: props.minTickGap,
    mirror: props.mirror,
    name: props.name,
    orientation: props.orientation,
    padding: props.padding,
    reversed: props.reversed,
    scale: props.scale,
    tick: props.tick,
    tickCount: props.tickCount,
    tickFormatter: props.tickFormatter,
    ticks: props.ticks,
    type: props.type,
    unit: props.unit
  }), /*#__PURE__*/external_React_.createElement(XAxisImpl, props));
};

/**
 * @consumes CartesianViewBoxContext
 * @provides CartesianLabelContext
 */
var XAxis = /*#__PURE__*/external_React_.memo(XAxisSettingsDispatcher, axisPropsAreEqual);
XAxis.displayName = 'XAxis';
;// ./node_modules/recharts/es6/cartesian/YAxis.js
var YAxis_excluded = ["type"],
  YAxis_excluded2 = ["dangerouslySetInnerHTML", "ticks", "scale"],
  YAxis_excluded3 = ["id", "scale"];
function YAxis_extends() { return YAxis_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, YAxis_extends.apply(null, arguments); }
function YAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function YAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? YAxis_ownKeys(Object(t), !0).forEach(function (r) { YAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : YAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function YAxis_defineProperty(e, r, t) { return (r = YAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function YAxis_toPropertyKey(t) { var i = YAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function YAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function YAxis_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = YAxis_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function YAxis_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }














function SetYAxisSettings(props) {
  var dispatch = useAppDispatch();
  var prevSettingsRef = (0,external_React_.useRef)(null);
  var layout = useCartesianChartLayout();
  var {
      type: typeFromProps
    } = props,
    restProps = YAxis_objectWithoutProperties(props, YAxis_excluded);
  var evaluatedType = getAxisTypeBasedOnLayout(layout, 'yAxis', typeFromProps);
  var settings = (0,external_React_.useMemo)(() => {
    if (evaluatedType == null) {
      return undefined;
    }
    return YAxis_objectSpread(YAxis_objectSpread({}, restProps), {}, {
      type: evaluatedType
    });
  }, [evaluatedType, restProps]);
  (0,external_React_.useLayoutEffect)(() => {
    if (settings == null) {
      return;
    }
    if (prevSettingsRef.current === null) {
      dispatch(addYAxis(settings));
    } else if (prevSettingsRef.current !== settings) {
      dispatch(replaceYAxis({
        prev: prevSettingsRef.current,
        next: settings
      }));
    }
    prevSettingsRef.current = settings;
  }, [settings, dispatch]);
  (0,external_React_.useLayoutEffect)(() => {
    return () => {
      if (prevSettingsRef.current) {
        dispatch(removeYAxis(prevSettingsRef.current));
        prevSettingsRef.current = null;
      }
    };
  }, [dispatch]);
  return null;
}
function YAxisImpl(props) {
  var {
    yAxisId,
    className,
    width,
    label
  } = props;
  var cartesianAxisRef = (0,external_React_.useRef)(null);
  var labelRef = (0,external_React_.useRef)(null);
  var viewBox = useAppSelector(selectAxisViewBox);
  var isPanorama = useIsPanorama();
  var dispatch = useAppDispatch();
  var axisType = 'yAxis';
  var axisSize = useAppSelector(state => selectYAxisSize(state, yAxisId));
  var position = useAppSelector(state => selectYAxisPosition(state, yAxisId));
  var cartesianTickItems = useAppSelector(state => selectTicksOfAxis(state, axisType, yAxisId, isPanorama));
  /*
   * Here we select settings from the store and prefer to use them instead of the actual props
   * so that the chart is consistent. If we used the props directly, some components will use axis settings
   * from state and some from props and because there is a render step between these two, they might be showing different things.
   * https://github.com/recharts/recharts/issues/6257
   */
  var synchronizedSettings = useAppSelector(state => selectYAxisSettingsNoDefaults(state, yAxisId));
  (0,external_React_.useLayoutEffect)(() => {
    // No dynamic width calculation is done when width !== 'auto'
    // or when a function/react element is used for label
    if (width !== 'auto' || !axisSize || isLabelContentAFunction(label) || /*#__PURE__*/(0,external_React_.isValidElement)(label) || synchronizedSettings == null) {
      return;
    }
    var axisComponent = cartesianAxisRef.current;
    if (!axisComponent) {
      return;
    }
    var updatedYAxisWidth = axisComponent.getCalculatedWidth();

    // if the width has changed, dispatch an action to update the width
    if (Math.round(axisSize.width) !== Math.round(updatedYAxisWidth)) {
      dispatch(updateYAxisWidth({
        id: yAxisId,
        width: updatedYAxisWidth
      }));
    }
  }, [
  // The dependency on cartesianAxisRef.current is not needed because useLayoutEffect will run after every render.
  // The ref will be populated by then.
  // To re-run this effect when ticks change, we can depend on the ticks array from the store.
  cartesianTickItems, axisSize, dispatch, label, yAxisId, width, synchronizedSettings]);
  if (axisSize == null || position == null || synchronizedSettings == null) {
    return null;
  }
  var {
      dangerouslySetInnerHTML,
      ticks,
      scale: del
    } = props,
    allOtherProps = YAxis_objectWithoutProperties(props, YAxis_excluded2);
  var {
      id,
      scale: del2
    } = synchronizedSettings,
    restSynchronizedSettings = YAxis_objectWithoutProperties(synchronizedSettings, YAxis_excluded3);
  return /*#__PURE__*/external_React_.createElement(CartesianAxis, YAxis_extends({}, allOtherProps, restSynchronizedSettings, {
    ref: cartesianAxisRef,
    labelRef: labelRef,
    x: position.x,
    y: position.y,
    tickTextProps: width === 'auto' ? {
      width: undefined
    } : {
      width
    },
    width: axisSize.width,
    height: axisSize.height,
    className: clsx("recharts-".concat(axisType, " ").concat(axisType), className),
    viewBox: viewBox,
    ticks: cartesianTickItems,
    axisType: axisType
  }));
}
var yAxisDefaultProps = {
  allowDataOverflow: implicitYAxis.allowDataOverflow,
  allowDecimals: implicitYAxis.allowDecimals,
  allowDuplicatedCategory: implicitYAxis.allowDuplicatedCategory,
  angle: implicitYAxis.angle,
  axisLine: defaultCartesianAxisProps.axisLine,
  hide: false,
  includeHidden: implicitYAxis.includeHidden,
  interval: implicitYAxis.interval,
  label: false,
  minTickGap: implicitYAxis.minTickGap,
  mirror: implicitYAxis.mirror,
  orientation: implicitYAxis.orientation,
  padding: implicitYAxis.padding,
  reversed: implicitYAxis.reversed,
  scale: implicitYAxis.scale,
  tick: implicitYAxis.tick,
  tickCount: implicitYAxis.tickCount,
  tickLine: defaultCartesianAxisProps.tickLine,
  tickSize: defaultCartesianAxisProps.tickSize,
  type: implicitYAxis.type,
  width: implicitYAxis.width,
  yAxisId: 0
};
var YAxisSettingsDispatcher = outsideProps => {
  var props = resolveDefaultProps(outsideProps, yAxisDefaultProps);
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(SetYAxisSettings, {
    interval: props.interval,
    id: props.yAxisId,
    scale: props.scale,
    type: props.type,
    domain: props.domain,
    allowDataOverflow: props.allowDataOverflow,
    dataKey: props.dataKey,
    allowDuplicatedCategory: props.allowDuplicatedCategory,
    allowDecimals: props.allowDecimals,
    tickCount: props.tickCount,
    padding: props.padding,
    includeHidden: props.includeHidden,
    reversed: props.reversed,
    ticks: props.ticks,
    width: props.width,
    orientation: props.orientation,
    mirror: props.mirror,
    hide: props.hide,
    unit: props.unit,
    name: props.name,
    angle: props.angle,
    minTickGap: props.minTickGap,
    tick: props.tick,
    tickFormatter: props.tickFormatter
  }), /*#__PURE__*/external_React_.createElement(YAxisImpl, props));
};

/**
 * @consumes CartesianViewBoxContext
 * @provides CartesianLabelContext
 */
var YAxis = /*#__PURE__*/external_React_.memo(YAxisSettingsDispatcher, axisPropsAreEqual);
YAxis.displayName = 'YAxis';
;// ./node_modules/recharts/es6/component/DefaultTooltipContent.js
function DefaultTooltipContent_extends() { return DefaultTooltipContent_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, DefaultTooltipContent_extends.apply(null, arguments); }
function DefaultTooltipContent_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function DefaultTooltipContent_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? DefaultTooltipContent_ownKeys(Object(t), !0).forEach(function (r) { DefaultTooltipContent_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : DefaultTooltipContent_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function DefaultTooltipContent_defineProperty(e, r, t) { return (r = DefaultTooltipContent_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function DefaultTooltipContent_toPropertyKey(t) { var i = DefaultTooltipContent_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function DefaultTooltipContent_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileOverview Default Tooltip Content
 */





function defaultFormatter(value) {
  return Array.isArray(value) && isNumOrStr(value[0]) && isNumOrStr(value[1]) ? value.join(' ~ ') : value;
}
var defaultDefaultTooltipContentProps = {
  separator: ' : ',
  contentStyle: {
    margin: 0,
    padding: 10,
    backgroundColor: '#fff',
    border: '1px solid #ccc',
    whiteSpace: 'nowrap'
  },
  itemStyle: {
    display: 'block',
    paddingTop: 4,
    paddingBottom: 4,
    color: '#000'
  },
  labelStyle: {},
  accessibilityLayer: false
};

/**
 * This component is by default rendered inside the {@link Tooltip} component. You would not use it directly.
 *
 * You can use this component to customize the content of the tooltip,
 * or you can provide your own completely independent content.
 */
var DefaultTooltipContent = props => {
  var {
    separator = defaultDefaultTooltipContentProps.separator,
    contentStyle,
    itemStyle,
    labelStyle = defaultDefaultTooltipContentProps.labelStyle,
    payload,
    formatter,
    itemSorter,
    wrapperClassName,
    labelClassName,
    label,
    labelFormatter,
    accessibilityLayer = defaultDefaultTooltipContentProps.accessibilityLayer
  } = props;
  var renderContent = () => {
    if (payload && payload.length) {
      var listStyle = {
        padding: 0,
        margin: 0
      };
      var items = (itemSorter ? sortBy_default()(payload, itemSorter) : payload).map((entry, i) => {
        if (entry.type === 'none') {
          return null;
        }
        var finalFormatter = entry.formatter || formatter || defaultFormatter;
        var {
          value,
          name
        } = entry;
        var finalValue = value;
        var finalName = name;
        if (finalFormatter) {
          var formatted = finalFormatter(value, name, entry, i, payload);
          if (Array.isArray(formatted)) {
            [finalValue, finalName] = formatted;
          } else if (formatted != null) {
            finalValue = formatted;
          } else {
            return null;
          }
        }
        var finalItemStyle = DefaultTooltipContent_objectSpread(DefaultTooltipContent_objectSpread({}, defaultDefaultTooltipContentProps.itemStyle), {}, {
          color: entry.color || defaultDefaultTooltipContentProps.itemStyle.color
        }, itemStyle);
        return /*#__PURE__*/external_React_.createElement("li", {
          className: "recharts-tooltip-item",
          key: "tooltip-item-".concat(i),
          style: finalItemStyle
        }, isNumOrStr(finalName) ? /*#__PURE__*/external_React_.createElement("span", {
          className: "recharts-tooltip-item-name"
        }, finalName) : null, isNumOrStr(finalName) ? /*#__PURE__*/external_React_.createElement("span", {
          className: "recharts-tooltip-item-separator"
        }, separator) : null, /*#__PURE__*/external_React_.createElement("span", {
          className: "recharts-tooltip-item-value"
        }, finalValue), /*#__PURE__*/external_React_.createElement("span", {
          className: "recharts-tooltip-item-unit"
        }, entry.unit || ''));
      });
      return /*#__PURE__*/external_React_.createElement("ul", {
        className: "recharts-tooltip-item-list",
        style: listStyle
      }, items);
    }
    return null;
  };
  var finalStyle = DefaultTooltipContent_objectSpread(DefaultTooltipContent_objectSpread({}, defaultDefaultTooltipContentProps.contentStyle), contentStyle);
  var finalLabelStyle = DefaultTooltipContent_objectSpread({
    margin: 0
  }, labelStyle);
  var hasLabel = !isNullish(label);
  var finalLabel = hasLabel ? label : '';
  var wrapperCN = clsx('recharts-default-tooltip', wrapperClassName);
  var labelCN = clsx('recharts-tooltip-label', labelClassName);
  if (hasLabel && labelFormatter && payload !== undefined && payload !== null) {
    finalLabel = labelFormatter(label, payload);
  }
  var accessibilityAttributes = accessibilityLayer ? {
    role: 'status',
    'aria-live': 'assertive'
  } : {};
  return /*#__PURE__*/external_React_.createElement("div", DefaultTooltipContent_extends({
    className: wrapperCN,
    style: finalStyle
  }, accessibilityAttributes), /*#__PURE__*/external_React_.createElement("p", {
    className: labelCN,
    style: finalLabelStyle
  }, /*#__PURE__*/external_React_.isValidElement(finalLabel) ? finalLabel : "".concat(finalLabel)), renderContent());
};
;// ./node_modules/recharts/es6/util/tooltip/translate.js


var CSS_CLASS_PREFIX = 'recharts-tooltip-wrapper';
var TOOLTIP_HIDDEN = {
  visibility: 'hidden'
};
function getTooltipCSSClassName(_ref) {
  var {
    coordinate,
    translateX,
    translateY
  } = _ref;
  return clsx(CSS_CLASS_PREFIX, {
    ["".concat(CSS_CLASS_PREFIX, "-right")]: isNumber(translateX) && coordinate && isNumber(coordinate.x) && translateX >= coordinate.x,
    ["".concat(CSS_CLASS_PREFIX, "-left")]: isNumber(translateX) && coordinate && isNumber(coordinate.x) && translateX < coordinate.x,
    ["".concat(CSS_CLASS_PREFIX, "-bottom")]: isNumber(translateY) && coordinate && isNumber(coordinate.y) && translateY >= coordinate.y,
    ["".concat(CSS_CLASS_PREFIX, "-top")]: isNumber(translateY) && coordinate && isNumber(coordinate.y) && translateY < coordinate.y
  });
}
function getTooltipTranslateXY(_ref2) {
  var {
    allowEscapeViewBox,
    coordinate,
    key,
    offset,
    position,
    reverseDirection,
    tooltipDimension,
    viewBox,
    viewBoxDimension
  } = _ref2;
  if (position && isNumber(position[key])) {
    return position[key];
  }
  var negative = coordinate[key] - tooltipDimension - (offset > 0 ? offset : 0);
  var positive = coordinate[key] + offset;
  if (allowEscapeViewBox[key]) {
    return reverseDirection[key] ? negative : positive;
  }
  var viewBoxKey = viewBox[key];
  if (viewBoxKey == null) {
    return 0;
  }
  if (reverseDirection[key]) {
    var _tooltipBoundary = negative;
    var _viewBoxBoundary = viewBoxKey;
    if (_tooltipBoundary < _viewBoxBoundary) {
      return Math.max(positive, viewBoxKey);
    }
    return Math.max(negative, viewBoxKey);
  }
  if (viewBoxDimension == null) {
    return 0;
  }
  var tooltipBoundary = positive + tooltipDimension;
  var viewBoxBoundary = viewBoxKey + viewBoxDimension;
  if (tooltipBoundary > viewBoxBoundary) {
    return Math.max(negative, viewBoxKey);
  }
  return Math.max(positive, viewBoxKey);
}
function getTransformStyle(_ref3) {
  var {
    translateX,
    translateY,
    useTranslate3d
  } = _ref3;
  return {
    transform: useTranslate3d ? "translate3d(".concat(translateX, "px, ").concat(translateY, "px, 0)") : "translate(".concat(translateX, "px, ").concat(translateY, "px)")
  };
}
function getTooltipTranslate(_ref4) {
  var {
    allowEscapeViewBox,
    coordinate,
    offsetTop,
    offsetLeft,
    position,
    reverseDirection,
    tooltipBox,
    useTranslate3d,
    viewBox
  } = _ref4;
  var cssProperties, translateX, translateY;
  if (tooltipBox.height > 0 && tooltipBox.width > 0 && coordinate) {
    translateX = getTooltipTranslateXY({
      allowEscapeViewBox,
      coordinate,
      key: 'x',
      offset: offsetLeft,
      position,
      reverseDirection,
      tooltipDimension: tooltipBox.width,
      viewBox,
      viewBoxDimension: viewBox.width
    });
    translateY = getTooltipTranslateXY({
      allowEscapeViewBox,
      coordinate,
      key: 'y',
      offset: offsetTop,
      position,
      reverseDirection,
      tooltipDimension: tooltipBox.height,
      viewBox,
      viewBoxDimension: viewBox.height
    });
    cssProperties = getTransformStyle({
      translateX,
      translateY,
      useTranslate3d
    });
  } else {
    cssProperties = TOOLTIP_HIDDEN;
  }
  return {
    cssProperties,
    cssClasses: getTooltipCSSClassName({
      translateX,
      translateY,
      coordinate
    })
  };
}
;// ./node_modules/recharts/es6/component/TooltipBoundingBox.js
function TooltipBoundingBox_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function TooltipBoundingBox_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? TooltipBoundingBox_ownKeys(Object(t), !0).forEach(function (r) { TooltipBoundingBox_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : TooltipBoundingBox_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function TooltipBoundingBox_defineProperty(e, r, t) { return (r = TooltipBoundingBox_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function TooltipBoundingBox_toPropertyKey(t) { var i = TooltipBoundingBox_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function TooltipBoundingBox_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }



class TooltipBoundingBox extends external_React_.PureComponent {
  constructor() {
    super(...arguments);
    TooltipBoundingBox_defineProperty(this, "state", {
      dismissed: false,
      dismissedAtCoordinate: {
        x: 0,
        y: 0
      }
    });
    TooltipBoundingBox_defineProperty(this, "handleKeyDown", event => {
      if (event.key === 'Escape') {
        var _this$props$coordinat, _this$props$coordinat2, _this$props$coordinat3, _this$props$coordinat4;
        this.setState({
          dismissed: true,
          dismissedAtCoordinate: {
            x: (_this$props$coordinat = (_this$props$coordinat2 = this.props.coordinate) === null || _this$props$coordinat2 === void 0 ? void 0 : _this$props$coordinat2.x) !== null && _this$props$coordinat !== void 0 ? _this$props$coordinat : 0,
            y: (_this$props$coordinat3 = (_this$props$coordinat4 = this.props.coordinate) === null || _this$props$coordinat4 === void 0 ? void 0 : _this$props$coordinat4.y) !== null && _this$props$coordinat3 !== void 0 ? _this$props$coordinat3 : 0
          }
        });
      }
    });
  }
  componentDidMount() {
    document.addEventListener('keydown', this.handleKeyDown);
  }
  componentWillUnmount() {
    document.removeEventListener('keydown', this.handleKeyDown);
  }
  componentDidUpdate() {
    var _this$props$coordinat5, _this$props$coordinat6;
    if (!this.state.dismissed) {
      return;
    }
    if (((_this$props$coordinat5 = this.props.coordinate) === null || _this$props$coordinat5 === void 0 ? void 0 : _this$props$coordinat5.x) !== this.state.dismissedAtCoordinate.x || ((_this$props$coordinat6 = this.props.coordinate) === null || _this$props$coordinat6 === void 0 ? void 0 : _this$props$coordinat6.y) !== this.state.dismissedAtCoordinate.y) {
      this.state.dismissed = false;
    }
  }
  render() {
    var {
      active,
      allowEscapeViewBox,
      animationDuration,
      animationEasing,
      children,
      coordinate,
      hasPayload,
      isAnimationActive,
      offset,
      position,
      reverseDirection,
      useTranslate3d,
      viewBox,
      wrapperStyle,
      lastBoundingBox,
      innerRef,
      hasPortalFromProps
    } = this.props;
    var offsetLeft = typeof offset === 'number' ? offset : offset.x;
    var offsetTop = typeof offset === 'number' ? offset : offset.y;
    var {
      cssClasses,
      cssProperties
    } = getTooltipTranslate({
      allowEscapeViewBox,
      coordinate,
      offsetLeft,
      offsetTop,
      position,
      reverseDirection,
      tooltipBox: {
        height: lastBoundingBox.height,
        width: lastBoundingBox.width
      },
      useTranslate3d,
      viewBox
    });

    // do not use absolute styles if the user has passed a custom portal prop
    var positionStyles = hasPortalFromProps ? {} : TooltipBoundingBox_objectSpread(TooltipBoundingBox_objectSpread({
      transition: isAnimationActive && active ? "transform ".concat(animationDuration, "ms ").concat(animationEasing) : undefined
    }, cssProperties), {}, {
      pointerEvents: 'none',
      visibility: !this.state.dismissed && active && hasPayload ? 'visible' : 'hidden',
      position: 'absolute',
      top: 0,
      left: 0
    });
    var outerStyle = TooltipBoundingBox_objectSpread(TooltipBoundingBox_objectSpread({}, positionStyles), {}, {
      visibility: !this.state.dismissed && active && hasPayload ? 'visible' : 'hidden'
    }, wrapperStyle);
    return (
      /*#__PURE__*/
      // This element allow listening to the `Escape` key. See https://github.com/recharts/recharts/pull/2925
      external_React_.createElement("div", {
        // @ts-expect-error typescript library does not recognize xmlns attribute, but it's required for an HTML chunk inside SVG.
        xmlns: "http://www.w3.org/1999/xhtml",
        tabIndex: -1,
        className: cssClasses,
        style: outerStyle,
        ref: innerRef
      }, children)
    );
  }
}
;// ./node_modules/d3-shape/src/noop.js
/* harmony default export */ function src_noop() {}

;// ./node_modules/d3-shape/src/curve/basis.js
function basis_point(that, x, y) {
  that._context.bezierCurveTo(
    (2 * that._x0 + that._x1) / 3,
    (2 * that._y0 + that._y1) / 3,
    (that._x0 + 2 * that._x1) / 3,
    (that._y0 + 2 * that._y1) / 3,
    (that._x0 + 4 * that._x1 + x) / 6,
    (that._y0 + 4 * that._y1 + y) / 6
  );
}

function Basis(context) {
  this._context = context;
}

Basis.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 =
    this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3: basis_point(this, this._x1, this._y1); // falls through
      case 2: this._context.lineTo(this._x1, this._y1); break;
    }
    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
      case 1: this._point = 2; break;
      case 2: this._point = 3; this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6); // falls through
      default: basis_point(this, x, y); break;
    }
    this._x0 = this._x1, this._x1 = x;
    this._y0 = this._y1, this._y1 = y;
  }
};

/* harmony default export */ function curve_basis(context) {
  return new Basis(context);
}

;// ./node_modules/d3-shape/src/curve/basisClosed.js



function BasisClosed(context) {
  this._context = context;
}

BasisClosed.prototype = {
  areaStart: src_noop,
  areaEnd: src_noop,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 =
    this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2);
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: this._point = 1; this._x2 = x, this._y2 = y; break;
      case 1: this._point = 2; this._x3 = x, this._y3 = y; break;
      case 2: this._point = 3; this._x4 = x, this._y4 = y; this._context.moveTo((this._x0 + 4 * this._x1 + x) / 6, (this._y0 + 4 * this._y1 + y) / 6); break;
      default: basis_point(this, x, y); break;
    }
    this._x0 = this._x1, this._x1 = x;
    this._y0 = this._y1, this._y1 = y;
  }
};

/* harmony default export */ function curve_basisClosed(context) {
  return new BasisClosed(context);
}

;// ./node_modules/d3-shape/src/curve/basisOpen.js


function BasisOpen(context) {
  this._context = context;
}

BasisOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 =
    this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || (this._line !== 0 && this._point === 3)) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: this._point = 1; break;
      case 1: this._point = 2; break;
      case 2: this._point = 3; var x0 = (this._x0 + 4 * this._x1 + x) / 6, y0 = (this._y0 + 4 * this._y1 + y) / 6; this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0); break;
      case 3: this._point = 4; // falls through
      default: basis_point(this, x, y); break;
    }
    this._x0 = this._x1, this._x1 = x;
    this._y0 = this._y1, this._y1 = y;
  }
};

/* harmony default export */ function basisOpen(context) {
  return new BasisOpen(context);
}

;// ./node_modules/d3-shape/src/pointRadial.js
/* harmony default export */ function pointRadial(x, y) {
  return [(y = +y) * Math.cos(x -= Math.PI / 2), y * Math.sin(x)];
}

;// ./node_modules/d3-shape/src/curve/bump.js


class Bump {
  constructor(context, x) {
    this._context = context;
    this._x = x;
  }
  areaStart() {
    this._line = 0;
  }
  areaEnd() {
    this._line = NaN;
  }
  lineStart() {
    this._point = 0;
  }
  lineEnd() {
    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
    this._line = 1 - this._line;
  }
  point(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: {
        this._point = 1;
        if (this._line) this._context.lineTo(x, y);
        else this._context.moveTo(x, y);
        break;
      }
      case 1: this._point = 2; // falls through
      default: {
        if (this._x) this._context.bezierCurveTo(this._x0 = (this._x0 + x) / 2, this._y0, this._x0, y, x, y);
        else this._context.bezierCurveTo(this._x0, this._y0 = (this._y0 + y) / 2, x, this._y0, x, y);
        break;
      }
    }
    this._x0 = x, this._y0 = y;
  }
}

class BumpRadial {
  constructor(context) {
    this._context = context;
  }
  lineStart() {
    this._point = 0;
  }
  lineEnd() {}
  point(x, y) {
    x = +x, y = +y;
    if (this._point === 0) {
      this._point = 1;
    } else {
      const p0 = pointRadial(this._x0, this._y0);
      const p1 = pointRadial(this._x0, this._y0 = (this._y0 + y) / 2);
      const p2 = pointRadial(x, this._y0);
      const p3 = pointRadial(x, y);
      this._context.moveTo(...p0);
      this._context.bezierCurveTo(...p1, ...p2, ...p3);
    }
    this._x0 = x, this._y0 = y;
  }
}

function bumpX(context) {
  return new Bump(context, true);
}

function bumpY(context) {
  return new Bump(context, false);
}

function bumpRadial(context) {
  return new BumpRadial(context);
}

;// ./node_modules/d3-shape/src/curve/linearClosed.js


function LinearClosed(context) {
  this._context = context;
}

LinearClosed.prototype = {
  areaStart: src_noop,
  areaEnd: src_noop,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._point) this._context.closePath();
  },
  point: function(x, y) {
    x = +x, y = +y;
    if (this._point) this._context.lineTo(x, y);
    else this._point = 1, this._context.moveTo(x, y);
  }
};

/* harmony default export */ function linearClosed(context) {
  return new LinearClosed(context);
}

;// ./node_modules/d3-shape/src/curve/linear.js
function Linear(context) {
  this._context = context;
}

Linear.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
      case 1: this._point = 2; // falls through
      default: this._context.lineTo(x, y); break;
    }
  }
};

/* harmony default export */ function curve_linear(context) {
  return new Linear(context);
}

;// ./node_modules/d3-shape/src/curve/monotone.js
function sign(x) {
  return x < 0 ? -1 : 1;
}

// Calculate the slopes of the tangents (Hermite-type interpolation) based on
// the following paper: Steffen, M. 1990. A Simple Method for Monotonic
// Interpolation in One Dimension. Astronomy and Astrophysics, Vol. 239, NO.
// NOV(II), P. 443, 1990.
function slope3(that, x2, y2) {
  var h0 = that._x1 - that._x0,
      h1 = x2 - that._x1,
      s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0),
      s1 = (y2 - that._y1) / (h1 || h0 < 0 && -0),
      p = (s0 * h1 + s1 * h0) / (h0 + h1);
  return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p)) || 0;
}

// Calculate a one-sided slope.
function slope2(that, t) {
  var h = that._x1 - that._x0;
  return h ? (3 * (that._y1 - that._y0) / h - t) / 2 : t;
}

// According to https://en.wikipedia.org/wiki/Cubic_Hermite_spline#Representations
// "you can express cubic Hermite interpolation in terms of cubic Bézier curves
// with respect to the four values p0, p0 + m0 / 3, p1 - m1 / 3, p1".
function monotone_point(that, t0, t1) {
  var x0 = that._x0,
      y0 = that._y0,
      x1 = that._x1,
      y1 = that._y1,
      dx = (x1 - x0) / 3;
  that._context.bezierCurveTo(x0 + dx, y0 + dx * t0, x1 - dx, y1 - dx * t1, x1, y1);
}

function MonotoneX(context) {
  this._context = context;
}

MonotoneX.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 =
    this._y0 = this._y1 =
    this._t0 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2: this._context.lineTo(this._x1, this._y1); break;
      case 3: monotone_point(this, this._t0, slope2(this, this._t0)); break;
    }
    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x, y) {
    var t1 = NaN;

    x = +x, y = +y;
    if (x === this._x1 && y === this._y1) return; // Ignore coincident points.
    switch (this._point) {
      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
      case 1: this._point = 2; break;
      case 2: this._point = 3; monotone_point(this, slope2(this, t1 = slope3(this, x, y)), t1); break;
      default: monotone_point(this, this._t0, t1 = slope3(this, x, y)); break;
    }

    this._x0 = this._x1, this._x1 = x;
    this._y0 = this._y1, this._y1 = y;
    this._t0 = t1;
  }
}

function MonotoneY(context) {
  this._context = new ReflectContext(context);
}

(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x, y) {
  MonotoneX.prototype.point.call(this, y, x);
};

function ReflectContext(context) {
  this._context = context;
}

ReflectContext.prototype = {
  moveTo: function(x, y) { this._context.moveTo(y, x); },
  closePath: function() { this._context.closePath(); },
  lineTo: function(x, y) { this._context.lineTo(y, x); },
  bezierCurveTo: function(x1, y1, x2, y2, x, y) { this._context.bezierCurveTo(y1, x1, y2, x2, y, x); }
};

function monotoneX(context) {
  return new MonotoneX(context);
}

function monotoneY(context) {
  return new MonotoneY(context);
}

;// ./node_modules/d3-shape/src/curve/natural.js
function Natural(context) {
  this._context = context;
}

Natural.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [];
    this._y = [];
  },
  lineEnd: function() {
    var x = this._x,
        y = this._y,
        n = x.length;

    if (n) {
      this._line ? this._context.lineTo(x[0], y[0]) : this._context.moveTo(x[0], y[0]);
      if (n === 2) {
        this._context.lineTo(x[1], y[1]);
      } else {
        var px = controlPoints(x),
            py = controlPoints(y);
        for (var i0 = 0, i1 = 1; i1 < n; ++i0, ++i1) {
          this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x[i1], y[i1]);
        }
      }
    }

    if (this._line || (this._line !== 0 && n === 1)) this._context.closePath();
    this._line = 1 - this._line;
    this._x = this._y = null;
  },
  point: function(x, y) {
    this._x.push(+x);
    this._y.push(+y);
  }
};

// See https://www.particleincell.com/2012/bezier-splines/ for derivation.
function controlPoints(x) {
  var i,
      n = x.length - 1,
      m,
      a = new Array(n),
      b = new Array(n),
      r = new Array(n);
  a[0] = 0, b[0] = 2, r[0] = x[0] + 2 * x[1];
  for (i = 1; i < n - 1; ++i) a[i] = 1, b[i] = 4, r[i] = 4 * x[i] + 2 * x[i + 1];
  a[n - 1] = 2, b[n - 1] = 7, r[n - 1] = 8 * x[n - 1] + x[n];
  for (i = 1; i < n; ++i) m = a[i] / b[i - 1], b[i] -= m, r[i] -= m * r[i - 1];
  a[n - 1] = r[n - 1] / b[n - 1];
  for (i = n - 2; i >= 0; --i) a[i] = (r[i] - a[i + 1]) / b[i];
  b[n - 1] = (x[n] + a[n - 1]) / 2;
  for (i = 0; i < n - 1; ++i) b[i] = 2 * x[i + 1] - a[i + 1];
  return [a, b];
}

/* harmony default export */ function natural(context) {
  return new Natural(context);
}

;// ./node_modules/d3-shape/src/curve/step.js
function Step(context, t) {
  this._context = context;
  this._t = t;
}

Step.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
  },
  point: function(x, y) {
    x = +x, y = +y;
    switch (this._point) {
      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
      case 1: this._point = 2; // falls through
      default: {
        if (this._t <= 0) {
          this._context.lineTo(this._x, y);
          this._context.lineTo(x, y);
        } else {
          var x1 = this._x * (1 - this._t) + x * this._t;
          this._context.lineTo(x1, this._y);
          this._context.lineTo(x1, y);
        }
        break;
      }
    }
    this._x = x, this._y = y;
  }
};

/* harmony default export */ function step(context) {
  return new Step(context, 0.5);
}

function stepBefore(context) {
  return new Step(context, 0);
}

function stepAfter(context) {
  return new Step(context, 1);
}

;// ./node_modules/d3-shape/src/point.js
function point_x(p) {
  return p[0];
}

function point_y(p) {
  return p[1];
}

;// ./node_modules/d3-shape/src/line.js






/* harmony default export */ function line(x, y) {
  var defined = constant(true),
      context = null,
      curve = curve_linear,
      output = null,
      path = withPath(line);

  x = typeof x === "function" ? x : (x === undefined) ? point_x : constant(x);
  y = typeof y === "function" ? y : (y === undefined) ? point_y : constant(y);

  function line(data) {
    var i,
        n = (data = array(data)).length,
        d,
        defined0 = false,
        buffer;

    if (context == null) output = curve(buffer = path());

    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined(d = data[i], i, data)) === defined0) {
        if (defined0 = !defined0) output.lineStart();
        else output.lineEnd();
      }
      if (defined0) output.point(+x(d, i, data), +y(d, i, data));
    }

    if (buffer) return output = null, buffer + "" || null;
  }

  line.x = function(_) {
    return arguments.length ? (x = typeof _ === "function" ? _ : constant(+_), line) : x;
  };

  line.y = function(_) {
    return arguments.length ? (y = typeof _ === "function" ? _ : constant(+_), line) : y;
  };

  line.defined = function(_) {
    return arguments.length ? (defined = typeof _ === "function" ? _ : constant(!!_), line) : defined;
  };

  line.curve = function(_) {
    return arguments.length ? (curve = _, context != null && (output = curve(context)), line) : curve;
  };

  line.context = function(_) {
    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), line) : context;
  };

  return line;
}

;// ./node_modules/d3-shape/src/area.js







/* harmony default export */ function src_area(x0, y0, y1) {
  var x1 = null,
      defined = constant(true),
      context = null,
      curve = curve_linear,
      output = null,
      path = withPath(area);

  x0 = typeof x0 === "function" ? x0 : (x0 === undefined) ? point_x : constant(+x0);
  y0 = typeof y0 === "function" ? y0 : (y0 === undefined) ? constant(0) : constant(+y0);
  y1 = typeof y1 === "function" ? y1 : (y1 === undefined) ? point_y : constant(+y1);

  function area(data) {
    var i,
        j,
        k,
        n = (data = array(data)).length,
        d,
        defined0 = false,
        buffer,
        x0z = new Array(n),
        y0z = new Array(n);

    if (context == null) output = curve(buffer = path());

    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined(d = data[i], i, data)) === defined0) {
        if (defined0 = !defined0) {
          j = i;
          output.areaStart();
          output.lineStart();
        } else {
          output.lineEnd();
          output.lineStart();
          for (k = i - 1; k >= j; --k) {
            output.point(x0z[k], y0z[k]);
          }
          output.lineEnd();
          output.areaEnd();
        }
      }
      if (defined0) {
        x0z[i] = +x0(d, i, data), y0z[i] = +y0(d, i, data);
        output.point(x1 ? +x1(d, i, data) : x0z[i], y1 ? +y1(d, i, data) : y0z[i]);
      }
    }

    if (buffer) return output = null, buffer + "" || null;
  }

  function arealine() {
    return line().defined(defined).curve(curve).context(context);
  }

  area.x = function(_) {
    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant(+_), x1 = null, area) : x0;
  };

  area.x0 = function(_) {
    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant(+_), area) : x0;
  };

  area.x1 = function(_) {
    return arguments.length ? (x1 = _ == null ? null : typeof _ === "function" ? _ : constant(+_), area) : x1;
  };

  area.y = function(_) {
    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant(+_), y1 = null, area) : y0;
  };

  area.y0 = function(_) {
    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant(+_), area) : y0;
  };

  area.y1 = function(_) {
    return arguments.length ? (y1 = _ == null ? null : typeof _ === "function" ? _ : constant(+_), area) : y1;
  };

  area.lineX0 =
  area.lineY0 = function() {
    return arealine().x(x0).y(y0);
  };

  area.lineY1 = function() {
    return arealine().x(x0).y(y1);
  };

  area.lineX1 = function() {
    return arealine().x(x1).y(y0);
  };

  area.defined = function(_) {
    return arguments.length ? (defined = typeof _ === "function" ? _ : constant(!!_), area) : defined;
  };

  area.curve = function(_) {
    return arguments.length ? (curve = _, context != null && (output = curve(context)), area) : curve;
  };

  area.context = function(_) {
    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), area) : context;
  };

  return area;
}

;// ./node_modules/recharts/es6/shape/Curve.js
function Curve_extends() { return Curve_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Curve_extends.apply(null, arguments); }
function Curve_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Curve_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Curve_ownKeys(Object(t), !0).forEach(function (r) { Curve_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Curve_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Curve_defineProperty(e, r, t) { return (r = Curve_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Curve_toPropertyKey(t) { var i = Curve_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Curve_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * @fileOverview Curve
 */








var CURVE_FACTORIES = {
  curveBasisClosed: curve_basisClosed,
  curveBasisOpen: basisOpen,
  curveBasis: curve_basis,
  curveBumpX: bumpX,
  curveBumpY: bumpY,
  curveLinearClosed: linearClosed,
  curveLinear: curve_linear,
  curveMonotoneX: monotoneX,
  curveMonotoneY: monotoneY,
  curveNatural: natural,
  curveStep: step,
  curveStepAfter: stepAfter,
  curveStepBefore: stepBefore
};

/**
 * @inline
 */

var defined = p => isWellBehavedNumber(p.x) && isWellBehavedNumber(p.y);
var areaDefined = d => d.base != null && defined(d.base) && defined(d);
var getX = p => p.x;
var getY = p => p.y;
var getCurveFactory = (type, layout) => {
  if (typeof type === 'function') {
    return type;
  }
  var name = "curve".concat(upperFirst(type));
  if ((name === 'curveMonotone' || name === 'curveBump') && layout) {
    var factory = CURVE_FACTORIES["".concat(name).concat(layout === 'vertical' ? 'Y' : 'X')];
    if (factory) {
      return factory;
    }
  }
  return CURVE_FACTORIES[name] || curve_linear;
};

// Mouse event handlers receive the full Props, including the event handlers themselves.

var defaultCurveProps = {
  connectNulls: false,
  type: 'linear'
};

/**
 * Calculate the path of curve. Returns null if points is an empty array.
 * @return path or null
 */
var getPath = _ref => {
  var {
    type = defaultCurveProps.type,
    points = [],
    baseLine,
    layout,
    connectNulls = defaultCurveProps.connectNulls
  } = _ref;
  var curveFactory = getCurveFactory(type, layout);
  var formatPoints = connectNulls ? points.filter(defined) : points;

  // When dealing with an area chart (where `baseLine` is an array),
  // we need to pair points with their corresponding `baseLine` points first.
  // This is to ensure that we filter points and their baseline counterparts together,
  // preventing errors from mismatched array lengths and ensuring `defined` checks both.
  if (Array.isArray(baseLine)) {
    var _lineFunction;
    var areaPoints = points.map((entry, index) => Curve_objectSpread(Curve_objectSpread({}, entry), {}, {
      base: baseLine[index]
    }));
    if (layout === 'vertical') {
      _lineFunction = src_area().y(getY).x1(getX).x0(d => d.base.x);
    } else {
      _lineFunction = src_area().x(getX).y1(getY).y0(d => d.base.y);
    }
    /*
     * What happens here is that the `.defined()` call will make it so that this function can accept
     * nullable points, and internally it will filter them out and skip when generating the path.
     * So on the input it accepts NullableCoordinate, but it never calls getX/getY on null points because of the defined() filter.
     *
     * The d3 type definition has only one generic so it doesn't allow to describe this properly.
     * However. d3 types are mutable, but we can pretend that they are not, and we can pretend
     * that calling defined() returns a new function with a different generic type.
     */
    // @ts-expect-error the defined call changes the generic type internally but d3 types don't reflect that
    var _nullableLineFunction = _lineFunction.defined(areaDefined).curve(curveFactory);
    var finalPoints = connectNulls ? areaPoints.filter(areaDefined) : areaPoints;
    return _nullableLineFunction(finalPoints);
  }
  var lineFunction;
  if (layout === 'vertical' && isNumber(baseLine)) {
    lineFunction = src_area().y(getY).x1(getX).x0(baseLine);
  } else if (isNumber(baseLine)) {
    lineFunction = src_area().x(getX).y1(getY).y0(baseLine);
  } else {
    lineFunction = line().x(getX).y(getY);
  }

  // @ts-expect-error the defined call changes the generic type internally but d3 types don't reflect that
  var nullableLineFunction = lineFunction.defined(defined).curve(curveFactory);
  return nullableLineFunction(formatPoints);
};
var Curve = props => {
  var {
    className,
    points,
    path,
    pathRef
  } = props;
  var layout = useChartLayout();
  if ((!points || !points.length) && !path) {
    return null;
  }
  var getPathInput = {
    type: props.type,
    points: props.points,
    baseLine: props.baseLine,
    layout: props.layout || layout,
    connectNulls: props.connectNulls
  };
  var realPath = points && points.length ? getPath(getPathInput) : path;
  return /*#__PURE__*/external_React_.createElement("path", Curve_extends({}, svgPropertiesNoEvents(props), adaptEventHandlers(props), {
    className: clsx('recharts-curve', className),
    d: realPath === null ? undefined : realPath,
    ref: pathRef
  }));
};
;// ./node_modules/recharts/es6/shape/Cross.js
var Cross_excluded = ["x", "y", "top", "left", "width", "height", "className"];
function Cross_extends() { return Cross_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Cross_extends.apply(null, arguments); }
function Cross_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Cross_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Cross_ownKeys(Object(t), !0).forEach(function (r) { Cross_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Cross_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Cross_defineProperty(e, r, t) { return (r = Cross_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Cross_toPropertyKey(t) { var i = Cross_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Cross_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Cross_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Cross_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Cross_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
/**
 * @fileOverview Cross
 */




var Cross_getPath = (x, y, width, height, top, left) => {
  return "M".concat(x, ",").concat(top, "v").concat(height, "M").concat(left, ",").concat(y, "h").concat(width);
};
var Cross = _ref => {
  var {
      x = 0,
      y = 0,
      top = 0,
      left = 0,
      width = 0,
      height = 0,
      className
    } = _ref,
    rest = Cross_objectWithoutProperties(_ref, Cross_excluded);
  var props = Cross_objectSpread({
    x,
    y,
    top,
    left,
    width,
    height
  }, rest);
  if (!isNumber(x) || !isNumber(y) || !isNumber(width) || !isNumber(height) || !isNumber(top) || !isNumber(left)) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement("path", Cross_extends({}, svgPropertiesAndEvents(props), {
    className: clsx('recharts-cross', className),
    d: Cross_getPath(x, y, width, height, top, left)
  }));
};
;// ./node_modules/recharts/es6/util/cursor/getCursorRectangle.js
function getCursorRectangle(layout, activeCoordinate, offset, tooltipAxisBandSize) {
  var halfSize = tooltipAxisBandSize / 2;
  return {
    stroke: 'none',
    fill: '#ccc',
    x: layout === 'horizontal' ? activeCoordinate.x - halfSize : offset.left + 0.5,
    y: layout === 'horizontal' ? offset.top + 0.5 : activeCoordinate.y - halfSize,
    width: layout === 'horizontal' ? tooltipAxisBandSize : offset.width - 1,
    height: layout === 'horizontal' ? offset.height - 1 : tooltipAxisBandSize
  };
}
;// ./node_modules/recharts/es6/shape/Rectangle.js
var Rectangle_excluded = ["radius"],
  Rectangle_excluded2 = ["radius"];
var Rectangle_templateObject, _templateObject2, _templateObject3, _templateObject4, _templateObject5, _templateObject6, _templateObject7, _templateObject8, _templateObject9, _templateObject0;
function Rectangle_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Rectangle_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Rectangle_ownKeys(Object(t), !0).forEach(function (r) { Rectangle_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Rectangle_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Rectangle_defineProperty(e, r, t) { return (r = Rectangle_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Rectangle_toPropertyKey(t) { var i = Rectangle_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Rectangle_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Rectangle_extends() { return Rectangle_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Rectangle_extends.apply(null, arguments); }
function Rectangle_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Rectangle_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Rectangle_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function Rectangle_taggedTemplateLiteral(e, t) { return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } })); }
/**
 * @fileOverview Rectangle
 */











/**
 * @inline
 */

var getRectanglePath = (x, y, width, height, radius) => {
  var roundedWidth = round(width);
  var roundedHeight = round(height);
  var maxRadius = Math.min(Math.abs(roundedWidth) / 2, Math.abs(roundedHeight) / 2);
  var ySign = roundedHeight >= 0 ? 1 : -1;
  var xSign = roundedWidth >= 0 ? 1 : -1;
  var clockWise = roundedHeight >= 0 && roundedWidth >= 0 || roundedHeight < 0 && roundedWidth < 0 ? 1 : 0;
  var path;
  if (maxRadius > 0 && Array.isArray(radius)) {
    var newRadius = [0, 0, 0, 0];
    for (var i = 0, len = 4; i < len; i++) {
      var _radius$i;
      var r = (_radius$i = radius[i]) !== null && _radius$i !== void 0 ? _radius$i : 0;
      newRadius[i] = r > maxRadius ? maxRadius : r;
    }
    path = roundTemplateLiteral(Rectangle_templateObject || (Rectangle_templateObject = Rectangle_taggedTemplateLiteral(["M", ",", ""])), x, y + ySign * newRadius[0]);
    if (newRadius[0] > 0) {
      path += roundTemplateLiteral(_templateObject2 || (_templateObject2 = Rectangle_taggedTemplateLiteral(["A ", ",", ",0,0,", ",", ",", ""])), newRadius[0], newRadius[0], clockWise, x + xSign * newRadius[0], y);
    }
    path += roundTemplateLiteral(_templateObject3 || (_templateObject3 = Rectangle_taggedTemplateLiteral(["L ", ",", ""])), x + width - xSign * newRadius[1], y);
    if (newRadius[1] > 0) {
      path += roundTemplateLiteral(_templateObject4 || (_templateObject4 = Rectangle_taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[1], newRadius[1], clockWise, x + width, y + ySign * newRadius[1]);
    }
    path += roundTemplateLiteral(_templateObject5 || (_templateObject5 = Rectangle_taggedTemplateLiteral(["L ", ",", ""])), x + width, y + height - ySign * newRadius[2]);
    if (newRadius[2] > 0) {
      path += roundTemplateLiteral(_templateObject6 || (_templateObject6 = Rectangle_taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[2], newRadius[2], clockWise, x + width - xSign * newRadius[2], y + height);
    }
    path += roundTemplateLiteral(_templateObject7 || (_templateObject7 = Rectangle_taggedTemplateLiteral(["L ", ",", ""])), x + xSign * newRadius[3], y + height);
    if (newRadius[3] > 0) {
      path += roundTemplateLiteral(_templateObject8 || (_templateObject8 = Rectangle_taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[3], newRadius[3], clockWise, x, y + height - ySign * newRadius[3]);
    }
    path += 'Z';
  } else if (maxRadius > 0 && radius === +radius && radius > 0) {
    var _newRadius = Math.min(maxRadius, radius);
    path = roundTemplateLiteral(_templateObject9 || (_templateObject9 = Rectangle_taggedTemplateLiteral(["M ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", " Z"])), x, y + ySign * _newRadius, _newRadius, _newRadius, clockWise, x + xSign * _newRadius, y, x + width - xSign * _newRadius, y, _newRadius, _newRadius, clockWise, x + width, y + ySign * _newRadius, x + width, y + height - ySign * _newRadius, _newRadius, _newRadius, clockWise, x + width - xSign * _newRadius, y + height, x + xSign * _newRadius, y + height, _newRadius, _newRadius, clockWise, x, y + height - ySign * _newRadius);
  } else {
    path = roundTemplateLiteral(_templateObject0 || (_templateObject0 = Rectangle_taggedTemplateLiteral(["M ", ",", " h ", " v ", " h ", " Z"])), x, y, width, height, -width);
  }
  return path;
};
var defaultRectangleProps = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  radius: 0,
  isAnimationActive: false,
  isUpdateAnimationActive: false,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: 'ease'
};

/**
 * Renders a rectangle element. Unlike the {@link https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/rect rect SVG element}, this component supports rounded corners
 * and animation.
 *
 * This component accepts X and Y coordinates in pixels.
 * If you need to position the rectangle based on your chart's data,
 * consider using the {@link ReferenceArea} component instead.
 *
 * @param rectangleProps
 * @constructor
 */
var Rectangle = rectangleProps => {
  var props = resolveDefaultProps(rectangleProps, defaultRectangleProps);
  var pathRef = (0,external_React_.useRef)(null);
  var [totalLength, setTotalLength] = (0,external_React_.useState)(-1);
  (0,external_React_.useEffect)(() => {
    if (pathRef.current && pathRef.current.getTotalLength) {
      try {
        var pathTotalLength = pathRef.current.getTotalLength();
        if (pathTotalLength) {
          setTotalLength(pathTotalLength);
        }
      } catch (_unused) {
        // calculate total length error
      }
    }
  }, []);
  var {
    x,
    y,
    width,
    height,
    radius,
    className
  } = props;
  var {
    animationEasing,
    animationDuration,
    animationBegin,
    isAnimationActive,
    isUpdateAnimationActive
  } = props;
  var prevWidthRef = (0,external_React_.useRef)(width);
  var prevHeightRef = (0,external_React_.useRef)(height);
  var prevXRef = (0,external_React_.useRef)(x);
  var prevYRef = (0,external_React_.useRef)(y);
  var animationIdInput = (0,external_React_.useMemo)(() => ({
    x,
    y,
    width,
    height,
    radius
  }), [x, y, width, height, radius]);
  var animationId = useAnimationId(animationIdInput, 'rectangle-');
  if (x !== +x || y !== +y || width !== +width || height !== +height || width === 0 || height === 0) {
    return null;
  }
  var layerClass = clsx('recharts-rectangle', className);
  if (!isUpdateAnimationActive) {
    var _svgPropertiesAndEven = svgPropertiesAndEvents(props),
      {
        radius: _
      } = _svgPropertiesAndEven,
      otherPathProps = Rectangle_objectWithoutProperties(_svgPropertiesAndEven, Rectangle_excluded);
    return /*#__PURE__*/external_React_.createElement("path", Rectangle_extends({}, otherPathProps, {
      x: round(x),
      y: round(y),
      width: round(width),
      height: round(height),
      radius: typeof radius === 'number' ? radius : undefined,
      className: layerClass,
      d: getRectanglePath(x, y, width, height, radius)
    }));
  }
  var prevWidth = prevWidthRef.current;
  var prevHeight = prevHeightRef.current;
  var prevX = prevXRef.current;
  var prevY = prevYRef.current;
  var from = "0px ".concat(totalLength === -1 ? 1 : totalLength, "px");
  var to = "".concat(totalLength, "px 0px");
  var transition = getTransitionVal(['strokeDasharray'], animationDuration, typeof animationEasing === 'string' ? animationEasing : defaultRectangleProps.animationEasing);
  return /*#__PURE__*/external_React_.createElement(JavascriptAnimate, {
    animationId: animationId,
    key: animationId,
    canBegin: totalLength > 0,
    duration: animationDuration,
    easing: animationEasing,
    isActive: isUpdateAnimationActive,
    begin: animationBegin
  }, t => {
    var currWidth = interpolate(prevWidth, width, t);
    var currHeight = interpolate(prevHeight, height, t);
    var currX = interpolate(prevX, x, t);
    var currY = interpolate(prevY, y, t);
    if (pathRef.current) {
      prevWidthRef.current = currWidth;
      prevHeightRef.current = currHeight;
      prevXRef.current = currX;
      prevYRef.current = currY;
    }
    var animationStyle;
    if (!isAnimationActive) {
      animationStyle = {
        strokeDasharray: to
      };
    } else if (t > 0) {
      animationStyle = {
        transition,
        strokeDasharray: to
      };
    } else {
      animationStyle = {
        strokeDasharray: from
      };
    }
    var _svgPropertiesAndEven2 = svgPropertiesAndEvents(props),
      {
        radius: _
      } = _svgPropertiesAndEven2,
      otherPathProps = Rectangle_objectWithoutProperties(_svgPropertiesAndEven2, Rectangle_excluded2);
    return /*#__PURE__*/external_React_.createElement("path", Rectangle_extends({}, otherPathProps, {
      radius: typeof radius === 'number' ? radius : undefined,
      className: layerClass,
      d: getRectanglePath(currX, currY, currWidth, currHeight, radius),
      ref: pathRef,
      style: Rectangle_objectSpread(Rectangle_objectSpread({}, animationStyle), props.style)
    }));
  });
};
;// ./node_modules/recharts/es6/util/cursor/getRadialCursorPoints.js

/**
 * Only applicable for radial layouts
 * @param {Object} activeCoordinate ChartCoordinate
 * @returns {Object} RadialCursorPoints
 */
function getRadialCursorPoints(activeCoordinate) {
  var {
    cx,
    cy,
    radius,
    startAngle,
    endAngle
  } = activeCoordinate;
  var startPoint = polarToCartesian(cx, cy, radius, startAngle);
  var endPoint = polarToCartesian(cx, cy, radius, endAngle);
  return {
    points: [startPoint, endPoint],
    cx,
    cy,
    radius,
    startAngle,
    endAngle
  };
}
;// ./node_modules/recharts/es6/shape/Sector.js
var Sector_templateObject, Sector_templateObject2, Sector_templateObject3, Sector_templateObject4, Sector_templateObject5, Sector_templateObject6, Sector_templateObject7;
function Sector_extends() { return Sector_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Sector_extends.apply(null, arguments); }
function Sector_taggedTemplateLiteral(e, t) { return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } })); }







var Sector_getDeltaAngle = (startAngle, endAngle) => {
  var sign = mathSign(endAngle - startAngle);
  var deltaAngle = Math.min(Math.abs(endAngle - startAngle), 359.999);
  return sign * deltaAngle;
};
var getTangentCircle = _ref => {
  var {
    cx,
    cy,
    radius,
    angle,
    sign,
    isExternal,
    cornerRadius,
    cornerIsExternal
  } = _ref;
  var centerRadius = cornerRadius * (isExternal ? 1 : -1) + radius;
  var theta = Math.asin(cornerRadius / centerRadius) / RADIAN;
  var centerAngle = cornerIsExternal ? angle : angle + sign * theta;
  var center = polarToCartesian(cx, cy, centerRadius, centerAngle);
  // The coordinate of point which is tangent to the circle
  var circleTangency = polarToCartesian(cx, cy, radius, centerAngle);
  // The coordinate of point which is tangent to the radius line
  var lineTangencyAngle = cornerIsExternal ? angle - sign * theta : angle;
  var lineTangency = polarToCartesian(cx, cy, centerRadius * Math.cos(theta * RADIAN), lineTangencyAngle);
  return {
    center,
    circleTangency,
    lineTangency,
    theta
  };
};
var getSectorPath = _ref2 => {
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    startAngle,
    endAngle
  } = _ref2;
  var angle = Sector_getDeltaAngle(startAngle, endAngle);

  // When the angle of sector equals to 360, star point and end point coincide
  var tempEndAngle = startAngle + angle;
  var outerStartPoint = polarToCartesian(cx, cy, outerRadius, startAngle);
  var outerEndPoint = polarToCartesian(cx, cy, outerRadius, tempEndAngle);
  var path = roundTemplateLiteral(Sector_templateObject || (Sector_templateObject = Sector_taggedTemplateLiteral(["M ", ",", "\n    A ", ",", ",0,\n    ", ",", ",\n    ", ",", "\n  "])), outerStartPoint.x, outerStartPoint.y, outerRadius, outerRadius, +(Math.abs(angle) > 180), +(startAngle > tempEndAngle), outerEndPoint.x, outerEndPoint.y);
  if (innerRadius > 0) {
    var innerStartPoint = polarToCartesian(cx, cy, innerRadius, startAngle);
    var innerEndPoint = polarToCartesian(cx, cy, innerRadius, tempEndAngle);
    path += roundTemplateLiteral(Sector_templateObject2 || (Sector_templateObject2 = Sector_taggedTemplateLiteral(["L ", ",", "\n            A ", ",", ",0,\n            ", ",", ",\n            ", ",", " Z"])), innerEndPoint.x, innerEndPoint.y, innerRadius, innerRadius, +(Math.abs(angle) > 180), +(startAngle <= tempEndAngle), innerStartPoint.x, innerStartPoint.y);
  } else {
    path += roundTemplateLiteral(Sector_templateObject3 || (Sector_templateObject3 = Sector_taggedTemplateLiteral(["L ", ",", " Z"])), cx, cy);
  }
  return path;
};
var getSectorWithCorner = _ref3 => {
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    cornerRadius,
    forceCornerRadius,
    cornerIsExternal,
    startAngle,
    endAngle
  } = _ref3;
  var sign = mathSign(endAngle - startAngle);
  var {
    circleTangency: soct,
    lineTangency: solt,
    theta: sot
  } = getTangentCircle({
    cx,
    cy,
    radius: outerRadius,
    angle: startAngle,
    sign,
    cornerRadius,
    cornerIsExternal
  });
  var {
    circleTangency: eoct,
    lineTangency: eolt,
    theta: eot
  } = getTangentCircle({
    cx,
    cy,
    radius: outerRadius,
    angle: endAngle,
    sign: -sign,
    cornerRadius,
    cornerIsExternal
  });
  var outerArcAngle = cornerIsExternal ? Math.abs(startAngle - endAngle) : Math.abs(startAngle - endAngle) - sot - eot;
  if (outerArcAngle < 0) {
    if (forceCornerRadius) {
      return roundTemplateLiteral(Sector_templateObject4 || (Sector_templateObject4 = Sector_taggedTemplateLiteral(["M ", ",", "\n        a", ",", ",0,0,1,", ",0\n        a", ",", ",0,0,1,", ",0\n      "])), solt.x, solt.y, cornerRadius, cornerRadius, cornerRadius * 2, cornerRadius, cornerRadius, -cornerRadius * 2);
    }
    return getSectorPath({
      cx,
      cy,
      innerRadius,
      outerRadius,
      startAngle,
      endAngle
    });
  }
  var path = roundTemplateLiteral(Sector_templateObject5 || (Sector_templateObject5 = Sector_taggedTemplateLiteral(["M ", ",", "\n    A", ",", ",0,0,", ",", ",", "\n    A", ",", ",0,", ",", ",", ",", "\n    A", ",", ",0,0,", ",", ",", "\n  "])), solt.x, solt.y, cornerRadius, cornerRadius, +(sign < 0), soct.x, soct.y, outerRadius, outerRadius, +(outerArcAngle > 180), +(sign < 0), eoct.x, eoct.y, cornerRadius, cornerRadius, +(sign < 0), eolt.x, eolt.y);
  if (innerRadius > 0) {
    var {
      circleTangency: sict,
      lineTangency: silt,
      theta: sit
    } = getTangentCircle({
      cx,
      cy,
      radius: innerRadius,
      angle: startAngle,
      sign,
      isExternal: true,
      cornerRadius,
      cornerIsExternal
    });
    var {
      circleTangency: eict,
      lineTangency: eilt,
      theta: eit
    } = getTangentCircle({
      cx,
      cy,
      radius: innerRadius,
      angle: endAngle,
      sign: -sign,
      isExternal: true,
      cornerRadius,
      cornerIsExternal
    });
    var innerArcAngle = cornerIsExternal ? Math.abs(startAngle - endAngle) : Math.abs(startAngle - endAngle) - sit - eit;
    if (innerArcAngle < 0 && cornerRadius === 0) {
      return "".concat(path, "L").concat(cx, ",").concat(cy, "Z");
    }
    path += roundTemplateLiteral(Sector_templateObject6 || (Sector_templateObject6 = Sector_taggedTemplateLiteral(["L", ",", "\n      A", ",", ",0,0,", ",", ",", "\n      A", ",", ",0,", ",", ",", ",", "\n      A", ",", ",0,0,", ",", ",", "Z"])), eilt.x, eilt.y, cornerRadius, cornerRadius, +(sign < 0), eict.x, eict.y, innerRadius, innerRadius, +(innerArcAngle > 180), +(sign > 0), sict.x, sict.y, cornerRadius, cornerRadius, +(sign < 0), silt.x, silt.y);
  } else {
    path += roundTemplateLiteral(Sector_templateObject7 || (Sector_templateObject7 = Sector_taggedTemplateLiteral(["L", ",", "Z"])), cx, cy);
  }
  return path;
};

/**
 * SVG cx, cy are `string | number | undefined`, but internally we use `number` so let's
 * override the types here.
 */

var defaultSectorProps = {
  cx: 0,
  cy: 0,
  innerRadius: 0,
  outerRadius: 0,
  startAngle: 0,
  endAngle: 0,
  cornerRadius: 0,
  forceCornerRadius: false,
  cornerIsExternal: false
};
var Sector = sectorProps => {
  var props = resolveDefaultProps(sectorProps, defaultSectorProps);
  var {
    cx,
    cy,
    innerRadius,
    outerRadius,
    cornerRadius,
    forceCornerRadius,
    cornerIsExternal,
    startAngle,
    endAngle,
    className
  } = props;
  if (outerRadius < innerRadius || startAngle === endAngle) {
    return null;
  }
  var layerClass = clsx('recharts-sector', className);
  var deltaRadius = outerRadius - innerRadius;
  var cr = getPercentValue(cornerRadius, deltaRadius, 0, true);
  var path;
  if (cr > 0 && Math.abs(startAngle - endAngle) < 360) {
    path = getSectorWithCorner({
      cx,
      cy,
      innerRadius,
      outerRadius,
      cornerRadius: Math.min(cr, deltaRadius / 2),
      forceCornerRadius,
      cornerIsExternal,
      startAngle,
      endAngle
    });
  } else {
    path = getSectorPath({
      cx,
      cy,
      innerRadius,
      outerRadius,
      startAngle,
      endAngle
    });
  }
  return /*#__PURE__*/external_React_.createElement("path", Sector_extends({}, svgPropertiesAndEvents(props), {
    className: layerClass,
    d: path
  }));
};
;// ./node_modules/recharts/es6/util/cursor/getCursorPoints.js



function getCursorPoints(layout, activeCoordinate, offset) {
  if (layout === 'horizontal') {
    return [{
      x: activeCoordinate.x,
      y: offset.top
    }, {
      x: activeCoordinate.x,
      y: offset.top + offset.height
    }];
  }
  if (layout === 'vertical') {
    return [{
      x: offset.left,
      y: activeCoordinate.y
    }, {
      x: offset.left + offset.width,
      y: activeCoordinate.y
    }];
  }
  if (isPolarCoordinate(activeCoordinate)) {
    if (layout === 'centric') {
      var {
        cx,
        cy,
        innerRadius,
        outerRadius,
        angle
      } = activeCoordinate;
      var innerPoint = polarToCartesian(cx, cy, innerRadius, angle);
      var outerPoint = polarToCartesian(cx, cy, outerRadius, angle);
      return [{
        x: innerPoint.x,
        y: innerPoint.y
      }, {
        x: outerPoint.x,
        y: outerPoint.y
      }];
    }
    return getRadialCursorPoints(activeCoordinate);
  }
  return undefined;
}
;// ./node_modules/recharts/es6/context/useTooltipAxis.js
function useTooltipAxis_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function useTooltipAxis_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? useTooltipAxis_ownKeys(Object(t), !0).forEach(function (r) { useTooltipAxis_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : useTooltipAxis_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function useTooltipAxis_defineProperty(e, r, t) { return (r = useTooltipAxis_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function useTooltipAxis_toPropertyKey(t) { var i = useTooltipAxis_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function useTooltipAxis_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }




var useTooltipAxis = () => useAppSelector(selectTooltipAxis);
var useTooltipAxisBandSize = () => {
  var tooltipAxis = useTooltipAxis();
  var tooltipTicks = useAppSelector(selectTooltipAxisTicks);
  var tooltipAxisScale = useAppSelector(selectTooltipAxisScale);
  if (!tooltipAxis || !tooltipAxisScale) {
    return getBandSizeOfAxis(undefined, tooltipTicks);
  }
  return getBandSizeOfAxis(useTooltipAxis_objectSpread(useTooltipAxis_objectSpread({}, tooltipAxis), {}, {
    scale: tooltipAxisScale
  }), tooltipTicks);
};
;// ./node_modules/recharts/es6/component/Cursor.js
function Cursor_extends() { return Cursor_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Cursor_extends.apply(null, arguments); }
function Cursor_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Cursor_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Cursor_ownKeys(Object(t), !0).forEach(function (r) { Cursor_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Cursor_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Cursor_defineProperty(e, r, t) { return (r = Cursor_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Cursor_toPropertyKey(t) { var i = Cursor_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Cursor_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


















/**
 * If set false, no cursor will be drawn when tooltip is active.
 * If set an object, the option is the configuration of cursor.
 * If set a React element, the option is the custom react element of drawing cursor
 */

function RenderCursor(_ref) {
  var {
    cursor,
    cursorComp,
    cursorProps
  } = _ref;
  if (/*#__PURE__*/(0,external_React_.isValidElement)(cursor)) {
    return /*#__PURE__*/(0,external_React_.cloneElement)(cursor, cursorProps);
  }
  return /*#__PURE__*/(0,external_React_.createElement)(cursorComp, cursorProps);
}
function CursorInternal(props) {
  var _props$zIndex;
  var {
    coordinate,
    payload,
    index,
    offset,
    tooltipAxisBandSize,
    layout,
    cursor,
    tooltipEventType,
    chartName
  } = props;

  // The cursor is a part of the Tooltip, and it should be shown (by default) when the Tooltip is active.
  var activeCoordinate = coordinate;
  var activePayload = payload;
  var activeTooltipIndex = index;
  if (!cursor || !activeCoordinate || chartName !== 'ScatterChart' && tooltipEventType !== 'axis') {
    return null;
  }
  var restProps, cursorComp, preferredZIndex;
  if (chartName === 'ScatterChart') {
    restProps = activeCoordinate;
    cursorComp = Cross;
    preferredZIndex = DefaultZIndexes.cursorLine;
  } else if (chartName === 'BarChart') {
    restProps = getCursorRectangle(layout, activeCoordinate, offset, tooltipAxisBandSize);
    cursorComp = Rectangle;
    preferredZIndex = DefaultZIndexes.cursorRectangle;
  } else if (layout === 'radial' && isPolarCoordinate(activeCoordinate)) {
    var {
      cx,
      cy,
      radius,
      startAngle,
      endAngle
    } = getRadialCursorPoints(activeCoordinate);
    restProps = {
      cx,
      cy,
      startAngle,
      endAngle,
      innerRadius: radius,
      outerRadius: radius
    };
    cursorComp = Sector;
    preferredZIndex = DefaultZIndexes.cursorLine;
  } else {
    restProps = {
      points: getCursorPoints(layout, activeCoordinate, offset)
    };
    cursorComp = Curve;
    preferredZIndex = DefaultZIndexes.cursorLine;
  }
  var extraClassName = typeof cursor === 'object' && 'className' in cursor ? cursor.className : undefined;
  var cursorProps = Cursor_objectSpread(Cursor_objectSpread(Cursor_objectSpread(Cursor_objectSpread({
    stroke: '#ccc',
    pointerEvents: 'none'
  }, offset), restProps), svgPropertiesNoEventsFromUnknown(cursor)), {}, {
    payload: activePayload,
    payloadIndex: activeTooltipIndex,
    className: clsx('recharts-tooltip-cursor', extraClassName)
  });
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: (_props$zIndex = props.zIndex) !== null && _props$zIndex !== void 0 ? _props$zIndex : preferredZIndex
  }, /*#__PURE__*/external_React_.createElement(RenderCursor, {
    cursor: cursor,
    cursorComp: cursorComp,
    cursorProps: cursorProps
  }));
}

/*
 * Cursor is the background, or a highlight,
 * that shows when user mouses over or activates
 * an area.
 *
 * It usually shows together with a tooltip
 * to emphasise which part of the chart does the tooltip refer to.
 */
function Cursor(props) {
  var tooltipAxisBandSize = useTooltipAxisBandSize();
  var offset = useOffsetInternal();
  var layout = useChartLayout();
  var chartName = useChartName();
  if (tooltipAxisBandSize == null || offset == null || layout == null || chartName == null) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(CursorInternal, Cursor_extends({}, props, {
    offset: offset,
    layout: layout,
    tooltipAxisBandSize: tooltipAxisBandSize,
    chartName: chartName
  }));
}
;// ./node_modules/recharts/es6/component/Tooltip.js
function Tooltip_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Tooltip_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Tooltip_ownKeys(Object(t), !0).forEach(function (r) { Tooltip_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Tooltip_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Tooltip_defineProperty(e, r, t) { return (r = Tooltip_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Tooltip_toPropertyKey(t) { var i = Tooltip_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Tooltip_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }

















function Tooltip_defaultUniqBy(entry) {
  return entry.dataKey;
}
function renderContent(content, props) {
  if (/*#__PURE__*/external_React_.isValidElement(content)) {
    return /*#__PURE__*/external_React_.cloneElement(content, props);
  }
  if (typeof content === 'function') {
    return /*#__PURE__*/external_React_.createElement(content, props);
  }
  return /*#__PURE__*/external_React_.createElement(DefaultTooltipContent, props);
}
var emptyPayload = [];
var defaultTooltipProps = {
  allowEscapeViewBox: {
    x: false,
    y: false
  },
  animationDuration: 400,
  animationEasing: 'ease',
  axisId: 0,
  contentStyle: {},
  cursor: true,
  filterNull: true,
  includeHidden: false,
  isAnimationActive: 'auto',
  itemSorter: 'name',
  itemStyle: {},
  labelStyle: {},
  offset: 10,
  reverseDirection: {
    x: false,
    y: false
  },
  separator: ' : ',
  trigger: 'hover',
  useTranslate3d: false,
  wrapperStyle: {}
};

/**
 * The Tooltip component displays a floating box with data values when hovering over or clicking on chart elements.
 *
 * It can be configured to show information for individual data points or for all points at a specific axis coordinate.
 * The appearance and content of the tooltip can be customized via props.
 *
 * @see {@link https://github.com/recharts/recharts/wiki/Tooltip-event-type-and-shared-prop Tooltip event type and shared prop wiki page}
 * @see {@link https://recharts.github.io/en-US/guide/activeIndex/ Active index replacement when migrating from Recharts v2 to v3}
 *
 * @consumes CartesianChartContext
 * @consumes PolarChartContext
 * @consumes TooltipEntrySettings
 */
function Tooltip(outsideProps) {
  var _useAppSelector, _ref;
  var props = resolveDefaultProps(outsideProps, defaultTooltipProps);
  var {
    active: activeFromProps,
    allowEscapeViewBox,
    animationDuration,
    animationEasing,
    content,
    filterNull,
    isAnimationActive,
    offset,
    payloadUniqBy,
    position,
    reverseDirection,
    useTranslate3d,
    wrapperStyle,
    cursor,
    shared,
    trigger,
    defaultIndex,
    portal: portalFromProps,
    axisId
  } = props;
  var dispatch = useAppDispatch();
  var defaultIndexAsString = typeof defaultIndex === 'number' ? String(defaultIndex) : defaultIndex;
  (0,external_React_.useEffect)(() => {
    dispatch(setTooltipSettingsState({
      shared,
      trigger,
      axisId,
      active: activeFromProps,
      defaultIndex: defaultIndexAsString
    }));
  }, [dispatch, shared, trigger, axisId, activeFromProps, defaultIndexAsString]);
  var viewBox = useViewBox();
  var accessibilityLayer = useAccessibilityLayer();
  var tooltipEventType = useTooltipEventType(shared);
  var {
    activeIndex,
    isActive
  } = (_useAppSelector = useAppSelector(state => selectors_selectIsTooltipActive(state, tooltipEventType, trigger, defaultIndexAsString))) !== null && _useAppSelector !== void 0 ? _useAppSelector : {};
  var payloadFromRedux = useAppSelector(state => selectTooltipPayload(state, tooltipEventType, trigger, defaultIndexAsString));
  var labelFromRedux = useAppSelector(state => selectors_selectActiveLabel(state, tooltipEventType, trigger, defaultIndexAsString));
  var coordinate = useAppSelector(state => selectActiveCoordinate(state, tooltipEventType, trigger, defaultIndexAsString));
  var payload = payloadFromRedux;
  var tooltipPortalFromContext = useTooltipPortal();
  /*
   * The user can set `active=true` on the Tooltip in which case the Tooltip will stay always active,
   * or `active=false` in which case the Tooltip never shows.
   *
   * If the `active` prop is not defined then it will show and hide based on mouse or keyboard activity.
   */
  var finalIsActive = (_ref = activeFromProps !== null && activeFromProps !== void 0 ? activeFromProps : isActive) !== null && _ref !== void 0 ? _ref : false;
  var [lastBoundingBox, updateBoundingBox] = useElementOffset([payload, finalIsActive]);
  var finalLabel = tooltipEventType === 'axis' ? labelFromRedux : undefined;
  useTooltipChartSynchronisation(tooltipEventType, trigger, coordinate, finalLabel, activeIndex, finalIsActive);
  var tooltipPortal = portalFromProps !== null && portalFromProps !== void 0 ? portalFromProps : tooltipPortalFromContext;
  if (tooltipPortal == null || viewBox == null || tooltipEventType == null) {
    return null;
  }
  var finalPayload = payload !== null && payload !== void 0 ? payload : emptyPayload;
  if (!finalIsActive) {
    finalPayload = emptyPayload;
  }
  if (filterNull && finalPayload.length) {
    finalPayload = getUniqPayload(finalPayload.filter(entry => entry.value != null && (entry.hide !== true || props.includeHidden)), payloadUniqBy, Tooltip_defaultUniqBy);
  }
  var hasPayload = finalPayload.length > 0;
  var tooltipElement = /*#__PURE__*/external_React_.createElement(TooltipBoundingBox, {
    allowEscapeViewBox: allowEscapeViewBox,
    animationDuration: animationDuration,
    animationEasing: animationEasing,
    isAnimationActive: isAnimationActive,
    active: finalIsActive,
    coordinate: coordinate,
    hasPayload: hasPayload,
    offset: offset,
    position: position,
    reverseDirection: reverseDirection,
    useTranslate3d: useTranslate3d,
    viewBox: viewBox,
    wrapperStyle: wrapperStyle,
    lastBoundingBox: lastBoundingBox,
    innerRef: updateBoundingBox,
    hasPortalFromProps: Boolean(portalFromProps)
  }, renderContent(content, Tooltip_objectSpread(Tooltip_objectSpread({}, props), {}, {
    payload: finalPayload,
    label: finalLabel,
    active: finalIsActive,
    activeIndex,
    coordinate,
    accessibilityLayer
  })));
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/(0,external_ReactDOM_namespaceObject.createPortal)(tooltipElement, tooltipPortal), finalIsActive && /*#__PURE__*/external_React_.createElement(Cursor, {
    cursor: cursor,
    tooltipEventType: tooltipEventType,
    coordinate: coordinate,
    payload: finalPayload,
    index: activeIndex
  }));
}
;// ./node_modules/recharts/es6/component/Cell.js
/**
 * Cell component used to define colors and styles of chart elements.
 *
 * This component is now deprecated and will be removed in Recharts 4.0.
 *
 * Please use the `shape` prop or `content` prop on the respective chart components
 * to customize the rendering of chart elements instead of using `Cell`.
 *
 * @see {@link https://recharts.github.io/en-US/guide/cell/ Guide: Migrate from Cell component to shape prop}
 *
 * @deprecated
 * @consumes CellReader
 */
var Cell_Cell = _props => null;
Cell_Cell.displayName = 'Cell';
;// ./node_modules/tiny-invariant/dist/esm/tiny-invariant.js
var isProduction = "production" === 'production';
var prefix = 'Invariant failed';
function invariant(condition, message) {
    if (condition) {
        return;
    }
    if (isProduction) {
        throw new Error(prefix);
    }
    var provided = typeof message === 'function' ? message() : message;
    var value = provided ? "".concat(prefix, ": ").concat(provided) : prefix;
    throw new Error(value);
}



// EXTERNAL MODULE: ./node_modules/es-toolkit/compat/isPlainObject.js
var compat_isPlainObject = __webpack_require__(2938);
var isPlainObject_default = /*#__PURE__*/__webpack_require__.n(compat_isPlainObject);
;// ./node_modules/recharts/es6/shape/Trapezoid.js
var Trapezoid_templateObject, Trapezoid_templateObject2, Trapezoid_templateObject3, Trapezoid_templateObject4, Trapezoid_templateObject5;
function Trapezoid_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Trapezoid_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Trapezoid_ownKeys(Object(t), !0).forEach(function (r) { Trapezoid_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Trapezoid_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Trapezoid_defineProperty(e, r, t) { return (r = Trapezoid_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Trapezoid_toPropertyKey(t) { var i = Trapezoid_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Trapezoid_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Trapezoid_extends() { return Trapezoid_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Trapezoid_extends.apply(null, arguments); }
function Trapezoid_taggedTemplateLiteral(e, t) { return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } })); }
/**
 * @fileOverview Rectangle
 */










var getTrapezoidPath = (x, y, upperWidth, lowerWidth, height) => {
  var widthGap = upperWidth - lowerWidth;
  var path;
  path = roundTemplateLiteral(Trapezoid_templateObject || (Trapezoid_templateObject = Trapezoid_taggedTemplateLiteral(["M ", ",", ""])), x, y);
  path += roundTemplateLiteral(Trapezoid_templateObject2 || (Trapezoid_templateObject2 = Trapezoid_taggedTemplateLiteral(["L ", ",", ""])), x + upperWidth, y);
  path += roundTemplateLiteral(Trapezoid_templateObject3 || (Trapezoid_templateObject3 = Trapezoid_taggedTemplateLiteral(["L ", ",", ""])), x + upperWidth - widthGap / 2, y + height);
  path += roundTemplateLiteral(Trapezoid_templateObject4 || (Trapezoid_templateObject4 = Trapezoid_taggedTemplateLiteral(["L ", ",", ""])), x + upperWidth - widthGap / 2 - lowerWidth, y + height);
  path += roundTemplateLiteral(Trapezoid_templateObject5 || (Trapezoid_templateObject5 = Trapezoid_taggedTemplateLiteral(["L ", ",", " Z"])), x, y);
  return path;
};
var defaultTrapezoidProps = {
  x: 0,
  y: 0,
  upperWidth: 0,
  lowerWidth: 0,
  height: 0,
  isUpdateAnimationActive: false,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: 'ease'
};
var Trapezoid = outsideProps => {
  var trapezoidProps = resolveDefaultProps(outsideProps, defaultTrapezoidProps);
  var {
    x,
    y,
    upperWidth,
    lowerWidth,
    height,
    className
  } = trapezoidProps;
  var {
    animationEasing,
    animationDuration,
    animationBegin,
    isUpdateAnimationActive
  } = trapezoidProps;
  var pathRef = (0,external_React_.useRef)(null);
  var [totalLength, setTotalLength] = (0,external_React_.useState)(-1);
  var prevUpperWidthRef = (0,external_React_.useRef)(upperWidth);
  var prevLowerWidthRef = (0,external_React_.useRef)(lowerWidth);
  var prevHeightRef = (0,external_React_.useRef)(height);
  var prevXRef = (0,external_React_.useRef)(x);
  var prevYRef = (0,external_React_.useRef)(y);
  var animationId = useAnimationId(outsideProps, 'trapezoid-');
  (0,external_React_.useEffect)(() => {
    if (pathRef.current && pathRef.current.getTotalLength) {
      try {
        var pathTotalLength = pathRef.current.getTotalLength();
        if (pathTotalLength) {
          setTotalLength(pathTotalLength);
        }
      } catch (_unused) {
        // calculate total length error
      }
    }
  }, []);
  if (x !== +x || y !== +y || upperWidth !== +upperWidth || lowerWidth !== +lowerWidth || height !== +height || upperWidth === 0 && lowerWidth === 0 || height === 0) {
    return null;
  }
  var layerClass = clsx('recharts-trapezoid', className);
  if (!isUpdateAnimationActive) {
    return /*#__PURE__*/external_React_.createElement("g", null, /*#__PURE__*/external_React_.createElement("path", Trapezoid_extends({}, svgPropertiesAndEvents(trapezoidProps), {
      className: layerClass,
      d: getTrapezoidPath(x, y, upperWidth, lowerWidth, height)
    })));
  }
  var prevUpperWidth = prevUpperWidthRef.current;
  var prevLowerWidth = prevLowerWidthRef.current;
  var prevHeight = prevHeightRef.current;
  var prevX = prevXRef.current;
  var prevY = prevYRef.current;
  var from = "0px ".concat(totalLength === -1 ? 1 : totalLength, "px");
  var to = "".concat(totalLength, "px 0px");
  var transition = getTransitionVal(['strokeDasharray'], animationDuration, animationEasing);
  return /*#__PURE__*/external_React_.createElement(JavascriptAnimate, {
    animationId: animationId,
    key: animationId,
    canBegin: totalLength > 0,
    duration: animationDuration,
    easing: animationEasing,
    isActive: isUpdateAnimationActive,
    begin: animationBegin
  }, t => {
    var currUpperWidth = interpolate(prevUpperWidth, upperWidth, t);
    var currLowerWidth = interpolate(prevLowerWidth, lowerWidth, t);
    var currHeight = interpolate(prevHeight, height, t);
    var currX = interpolate(prevX, x, t);
    var currY = interpolate(prevY, y, t);
    if (pathRef.current) {
      prevUpperWidthRef.current = currUpperWidth;
      prevLowerWidthRef.current = currLowerWidth;
      prevHeightRef.current = currHeight;
      prevXRef.current = currX;
      prevYRef.current = currY;
    }
    var animationStyle = t > 0 ? {
      transition,
      strokeDasharray: to
    } : {
      strokeDasharray: from
    };
    return /*#__PURE__*/external_React_.createElement("path", Trapezoid_extends({}, svgPropertiesAndEvents(trapezoidProps), {
      className: layerClass,
      d: getTrapezoidPath(currX, currY, currUpperWidth, currLowerWidth, currHeight),
      ref: pathRef,
      style: Trapezoid_objectSpread(Trapezoid_objectSpread({}, animationStyle), trapezoidProps.style)
    }));
  });
};
;// ./node_modules/recharts/es6/util/ActiveShapeUtils.js
var ActiveShapeUtils_excluded = ["option", "shapeType", "activeClassName"];
function ActiveShapeUtils_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = ActiveShapeUtils_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function ActiveShapeUtils_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function ActiveShapeUtils_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function ActiveShapeUtils_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ActiveShapeUtils_ownKeys(Object(t), !0).forEach(function (r) { ActiveShapeUtils_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ActiveShapeUtils_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function ActiveShapeUtils_defineProperty(e, r, t) { return (r = ActiveShapeUtils_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function ActiveShapeUtils_toPropertyKey(t) { var i = ActiveShapeUtils_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function ActiveShapeUtils_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }










/**
 * This is an abstraction for rendering a user defined prop for a customized shape in several forms.
 *
 * <Shape /> is the root and will handle taking in:
 *  - an object of svg properties
 *  - a boolean
 *  - a render prop(inline function that returns jsx)
 *  - a React element
 *
 * <ShapeSelector /> is a subcomponent of <Shape /> and used to match a component
 * to the value of props.shapeType that is passed to the root.
 *
 */

function defaultPropTransformer(option, props) {
  return ActiveShapeUtils_objectSpread(ActiveShapeUtils_objectSpread({}, props), option);
}
function isSymbolsProps(shapeType, _elementProps) {
  return shapeType === 'symbols';
}
function ShapeSelector(_ref) {
  var {
    shapeType,
    elementProps
  } = _ref;
  switch (shapeType) {
    case 'rectangle':
      return /*#__PURE__*/external_React_.createElement(Rectangle, elementProps);
    case 'trapezoid':
      return /*#__PURE__*/external_React_.createElement(Trapezoid, elementProps);
    case 'sector':
      return /*#__PURE__*/external_React_.createElement(Sector, elementProps);
    case 'symbols':
      if (isSymbolsProps(shapeType, elementProps)) {
        return /*#__PURE__*/external_React_.createElement(Symbols, elementProps);
      }
      break;
    case 'curve':
      return /*#__PURE__*/external_React_.createElement(Curve, elementProps);
    default:
      return null;
  }
}
function getPropsFromShapeOption(option) {
  if (/*#__PURE__*/(0,external_React_.isValidElement)(option)) {
    return option.props;
  }
  return option;
}
function Shape(_ref2) {
  var {
      option,
      shapeType,
      activeClassName = 'recharts-active-shape'
    } = _ref2,
    props = ActiveShapeUtils_objectWithoutProperties(_ref2, ActiveShapeUtils_excluded);
  var shape;
  if (/*#__PURE__*/(0,external_React_.isValidElement)(option)) {
    // @ts-expect-error we can't know the type of cloned element props
    shape = /*#__PURE__*/(0,external_React_.cloneElement)(option, ActiveShapeUtils_objectSpread(ActiveShapeUtils_objectSpread({}, props), getPropsFromShapeOption(option)));
  } else if (typeof option === 'function') {
    shape = option(props, props.index);
  } else if (isPlainObject_default()(option) && typeof option !== 'boolean') {
    var nextProps = defaultPropTransformer(option, props);
    shape = /*#__PURE__*/external_React_.createElement(ShapeSelector, {
      shapeType: shapeType,
      elementProps: nextProps
    });
  } else {
    var elementProps = props;
    shape = /*#__PURE__*/external_React_.createElement(ShapeSelector, {
      shapeType: shapeType,
      elementProps: elementProps
    });
  }
  if (props.isActive) {
    return /*#__PURE__*/external_React_.createElement(Layer, {
      className: activeClassName
    }, shape);
  }
  return shape;
}
;// ./node_modules/recharts/es6/util/BarUtils.js
function BarUtils_extends() { return BarUtils_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, BarUtils_extends.apply(null, arguments); }




function BarRectangle(props) {
  return /*#__PURE__*/external_React_.createElement(Shape, BarUtils_extends({
    shapeType: "rectangle",
    activeClassName: "recharts-active-bar"
  }, props));
}
/**
 * Safely gets minPointSize from the minPointSize prop if it is a function
 * @param minPointSize minPointSize as passed to the Bar component
 * @param defaultValue default minPointSize
 * @returns minPointSize
 */
var minPointSizeCallback = function minPointSizeCallback(minPointSize) {
  var defaultValue = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  return (value, index) => {
    if (isNumber(minPointSize)) return minPointSize;
    var isValueNumberOrNil = isNumber(value) || isNullish(value);
    if (isValueNumberOrNil) {
      return minPointSize(value, index);
    }
    !isValueNumberOrNil ?  true ? invariant(false, "minPointSize callback function received a value with type of ".concat(typeof value, ". Currently only numbers or null/undefined are supported.")) : 0 : void 0;
    return defaultValue;
  };
};
;// ./node_modules/recharts/es6/context/tooltipContext.js


var useMouseEnterItemDispatch = (onMouseEnterFromProps, dataKey, graphicalItemId) => {
  var dispatch = useAppDispatch();
  return (data, index) => event => {
    onMouseEnterFromProps === null || onMouseEnterFromProps === void 0 || onMouseEnterFromProps(data, index, event);
    dispatch(setActiveMouseOverItemIndex({
      activeIndex: String(index),
      activeDataKey: dataKey,
      activeCoordinate: data.tooltipPosition,
      activeGraphicalItemId: graphicalItemId
    }));
  };
};
var useMouseLeaveItemDispatch = onMouseLeaveFromProps => {
  var dispatch = useAppDispatch();
  return (data, index) => event => {
    onMouseLeaveFromProps === null || onMouseLeaveFromProps === void 0 || onMouseLeaveFromProps(data, index, event);
    dispatch(mouseLeaveItem());
  };
};
var useMouseClickItemDispatch = (onMouseClickFromProps, dataKey, graphicalItemId) => {
  var dispatch = useAppDispatch();
  return (data, index) => event => {
    onMouseClickFromProps === null || onMouseClickFromProps === void 0 || onMouseClickFromProps(data, index, event);
    dispatch(setActiveClickItemIndex({
      activeIndex: String(index),
      activeDataKey: dataKey,
      activeCoordinate: data.tooltipPosition,
      activeGraphicalItemId: graphicalItemId
    }));
  };
};
;// ./node_modules/recharts/es6/context/ErrorBarContext.js
var ErrorBarContext_excluded = ["children"];
function ErrorBarContext_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = ErrorBarContext_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function ErrorBarContext_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }





var initialContextState = {
  data: [],
  xAxisId: 'xAxis-0',
  yAxisId: 'yAxis-0',
  dataPointFormatter: () => ({
    x: 0,
    y: 0,
    value: 0
  }),
  errorBarOffset: 0
};
var ErrorBarContext = /*#__PURE__*/(0,external_React_.createContext)(initialContextState);
function SetErrorBarContext(props) {
  var {
      children
    } = props,
    rest = ErrorBarContext_objectWithoutProperties(props, ErrorBarContext_excluded);
  return /*#__PURE__*/external_React_.createElement(ErrorBarContext.Provider, {
    value: rest
  }, children);
}
var useErrorBarContext = () => (0,external_React_.useContext)(ErrorBarContext);
function ReportErrorBarSettings(props) {
  var dispatch = useAppDispatch();
  var graphicalItemId = useGraphicalItemId();
  var prevPropsRef = (0,external_React_.useRef)(null);
  (0,external_React_.useEffect)(() => {
    if (graphicalItemId == null) {
      // ErrorBar outside a graphical item context does not do anything.
      return;
    }
    if (prevPropsRef.current === null) {
      dispatch(addErrorBar({
        itemId: graphicalItemId,
        errorBar: props
      }));
    } else if (prevPropsRef.current !== props) {
      dispatch(replaceErrorBar({
        itemId: graphicalItemId,
        prev: prevPropsRef.current,
        next: props
      }));
    }
    prevPropsRef.current = props;
  }, [dispatch, graphicalItemId, props]);
  (0,external_React_.useEffect)(() => {
    return () => {
      if (prevPropsRef.current != null && graphicalItemId != null) {
        dispatch(removeErrorBar({
          itemId: graphicalItemId,
          errorBar: prevPropsRef.current
        }));
        prevPropsRef.current = null;
      }
    };
  }, [dispatch, graphicalItemId]);
  return null;
}
;// ./node_modules/recharts/es6/cartesian/GraphicalItemClipPath.js




function useNeedsClip(xAxisId, yAxisId) {
  var _xAxis$allowDataOverf, _yAxis$allowDataOverf;
  var xAxis = useAppSelector(state => selectXAxisSettings(state, xAxisId));
  var yAxis = useAppSelector(state => selectYAxisSettings(state, yAxisId));
  var needClipX = (_xAxis$allowDataOverf = xAxis === null || xAxis === void 0 ? void 0 : xAxis.allowDataOverflow) !== null && _xAxis$allowDataOverf !== void 0 ? _xAxis$allowDataOverf : implicitXAxis.allowDataOverflow;
  var needClipY = (_yAxis$allowDataOverf = yAxis === null || yAxis === void 0 ? void 0 : yAxis.allowDataOverflow) !== null && _yAxis$allowDataOverf !== void 0 ? _yAxis$allowDataOverf : implicitYAxis.allowDataOverflow;
  var needClip = needClipX || needClipY;
  return {
    needClip,
    needClipX,
    needClipY
  };
}
function GraphicalItemClipPath(_ref) {
  var {
    xAxisId,
    yAxisId,
    clipPathId
  } = _ref;
  var plotArea = usePlotArea();
  var {
    needClipX,
    needClipY,
    needClip
  } = useNeedsClip(xAxisId, yAxisId);
  if (!needClip || !plotArea) {
    return null;
  }
  var {
    x,
    y,
    width,
    height
  } = plotArea;
  return /*#__PURE__*/external_React_.createElement("clipPath", {
    id: "clipPath-".concat(clipPathId)
  }, /*#__PURE__*/external_React_.createElement("rect", {
    x: needClipX ? x : x - width / 2,
    y: needClipY ? y : y - height / 2,
    width: needClipX ? width : width * 2,
    height: needClipY ? height : height * 2
  }));
}
;// ./node_modules/recharts/es6/state/selectors/combiners/combineBarSizeList.js


var getBarSize = (globalSize, totalSize, selfSize) => {
  var barSize = selfSize !== null && selfSize !== void 0 ? selfSize : globalSize;
  if (isNullish(barSize)) {
    return undefined;
  }
  return getPercentValue(barSize, totalSize, 0);
};
var combineBarSizeList = (allBars, globalSize, totalSize) => {
  var initialValue = {};
  var stackedBars = allBars.filter(isStacked);
  var unstackedBars = allBars.filter(b => b.stackId == null);
  var groupByStack = stackedBars.reduce((acc, bar) => {
    var s = acc[bar.stackId];
    if (s == null) {
      s = [];
    }
    s.push(bar);
    acc[bar.stackId] = s;
    return acc;
  }, initialValue);
  var stackedSizeList = Object.entries(groupByStack).map(_ref => {
    var _bars$;
    var [stackId, bars] = _ref;
    var dataKeys = bars.map(b => b.dataKey);
    var barSize = getBarSize(globalSize, totalSize, (_bars$ = bars[0]) === null || _bars$ === void 0 ? void 0 : _bars$.barSize);
    return {
      stackId,
      dataKeys,
      barSize
    };
  });
  var unstackedSizeList = unstackedBars.map(b => {
    var dataKeys = [b.dataKey].filter(dk => dk != null);
    var barSize = getBarSize(globalSize, totalSize, b.barSize);
    return {
      stackId: undefined,
      dataKeys,
      barSize
    };
  });
  return [...stackedSizeList, ...unstackedSizeList];
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineAllBarPositions.js
function combineAllBarPositions_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function combineAllBarPositions_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? combineAllBarPositions_ownKeys(Object(t), !0).forEach(function (r) { combineAllBarPositions_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : combineAllBarPositions_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function combineAllBarPositions_defineProperty(e, r, t) { return (r = combineAllBarPositions_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function combineAllBarPositions_toPropertyKey(t) { var i = combineAllBarPositions_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function combineAllBarPositions_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


function getBarPositions(barGap, barCategoryGap, bandSize, sizeList, maxBarSize) {
  var _sizeList$;
  var len = sizeList.length;
  if (len < 1) {
    return undefined;
  }
  var realBarGap = getPercentValue(barGap, bandSize, 0, true);
  var result;
  var initialValue = [];

  // whether is barSize set by user
  // Okay but why does it check only for the first element? What if the first element is set but others are not?
  if (isWellBehavedNumber((_sizeList$ = sizeList[0]) === null || _sizeList$ === void 0 ? void 0 : _sizeList$.barSize)) {
    var useFull = false;
    var fullBarSize = bandSize / len;
    var sum = sizeList.reduce((res, entry) => res + (entry.barSize || 0), 0);
    sum += (len - 1) * realBarGap;
    if (sum >= bandSize) {
      sum -= (len - 1) * realBarGap;
      realBarGap = 0;
    }
    if (sum >= bandSize && fullBarSize > 0) {
      useFull = true;
      fullBarSize *= 0.9;
      sum = len * fullBarSize;
    }
    var offset = (bandSize - sum) / 2 >> 0;
    var prev = {
      offset: offset - realBarGap,
      size: 0
    };
    result = sizeList.reduce((res, entry) => {
      var _entry$barSize;
      var newPosition = {
        stackId: entry.stackId,
        dataKeys: entry.dataKeys,
        position: {
          offset: prev.offset + prev.size + realBarGap,
          size: useFull ? fullBarSize : (_entry$barSize = entry.barSize) !== null && _entry$barSize !== void 0 ? _entry$barSize : 0
        }
      };
      var newRes = [...res, newPosition];
      prev = newPosition.position;
      return newRes;
    }, initialValue);
  } else {
    var _offset = getPercentValue(barCategoryGap, bandSize, 0, true);
    if (bandSize - 2 * _offset - (len - 1) * realBarGap <= 0) {
      realBarGap = 0;
    }
    var originalSize = (bandSize - 2 * _offset - (len - 1) * realBarGap) / len;
    if (originalSize > 1) {
      originalSize >>= 0;
    }
    var size = isWellBehavedNumber(maxBarSize) ? Math.min(originalSize, maxBarSize) : originalSize;
    result = sizeList.reduce((res, entry, i) => [...res, {
      stackId: entry.stackId,
      dataKeys: entry.dataKeys,
      position: {
        offset: _offset + (originalSize + realBarGap) * i + (originalSize - size) / 2,
        size
      }
    }], initialValue);
  }
  return result;
}
var combineAllBarPositions = (sizeList, globalMaxBarSize, barGap, barCategoryGap, barBandSize, bandSize, childMaxBarSize) => {
  var maxBarSize = isNullish(childMaxBarSize) ? globalMaxBarSize : childMaxBarSize;
  var allBarPositions = getBarPositions(barGap, barCategoryGap, barBandSize !== bandSize ? barBandSize : bandSize, sizeList, maxBarSize);
  if (barBandSize !== bandSize && allBarPositions != null) {
    allBarPositions = allBarPositions.map(pos => combineAllBarPositions_objectSpread(combineAllBarPositions_objectSpread({}, pos), {}, {
      position: combineAllBarPositions_objectSpread(combineAllBarPositions_objectSpread({}, pos.position), {}, {
        offset: pos.position.offset - barBandSize / 2
      })
    }));
  }
  return allBarPositions;
};
;// ./node_modules/recharts/es6/state/selectors/combiners/combineStackedData.js

var combineStackedData = (stackGroups, barSettings) => {
  var stackSeriesIdentifier = getStackSeriesIdentifier(barSettings);
  if (!stackGroups || stackSeriesIdentifier == null || barSettings == null) {
    return undefined;
  }
  var {
    stackId
  } = barSettings;
  if (stackId == null) {
    return undefined;
  }
  var stackGroup = stackGroups[stackId];
  if (!stackGroup) {
    return undefined;
  }
  var {
    stackedData
  } = stackGroup;
  if (!stackedData) {
    return undefined;
  }
  return stackedData.find(sd => sd.key === stackSeriesIdentifier);
};
;// ./node_modules/recharts/es6/state/selectors/graphicalItemSelectors.js

function selectXAxisIdFromGraphicalItemId(state, id) {
  var _state$graphicalItems, _state$graphicalItems2;
  return (_state$graphicalItems = (_state$graphicalItems2 = state.graphicalItems.cartesianItems.find(item => item.id === id)) === null || _state$graphicalItems2 === void 0 ? void 0 : _state$graphicalItems2.xAxisId) !== null && _state$graphicalItems !== void 0 ? _state$graphicalItems : defaultAxisId;
}
function selectYAxisIdFromGraphicalItemId(state, id) {
  var _state$graphicalItems3, _state$graphicalItems4;
  return (_state$graphicalItems3 = (_state$graphicalItems4 = state.graphicalItems.cartesianItems.find(item => item.id === id)) === null || _state$graphicalItems4 === void 0 ? void 0 : _state$graphicalItems4.yAxisId) !== null && _state$graphicalItems3 !== void 0 ? _state$graphicalItems3 : defaultAxisId;
}
;// ./node_modules/recharts/es6/state/selectors/combiners/combineBarPosition.js
var combineBarPosition = (allBarPositions, barSettings) => {
  if (allBarPositions == null || barSettings == null) {
    return undefined;
  }
  var position = allBarPositions.find(p => p.stackId === barSettings.stackId && barSettings.dataKey != null && p.dataKeys.includes(barSettings.dataKey));
  if (position == null) {
    return undefined;
  }
  return position.position;
};
;// ./node_modules/recharts/es6/state/selectors/barSelectors.js














var pickIsPanorama = (_state, _id, isPanorama) => isPanorama;
var pickBarId = (_state, id) => id;
var selectSynchronisedBarSettings = createSelector([selectUnfilteredCartesianItems, pickBarId], (graphicalItems, id) => graphicalItems.filter(item => item.type === 'bar').find(item => item.id === id));
var selectMaxBarSize = createSelector([selectSynchronisedBarSettings], barSettings => barSettings === null || barSettings === void 0 ? void 0 : barSettings.maxBarSize);
var pickCells = (_state, _id, _isPanorama, cells) => cells;
var selectAllVisibleBars = createSelector([selectChartLayout, selectUnfilteredCartesianItems, selectXAxisIdFromGraphicalItemId, selectYAxisIdFromGraphicalItemId, pickIsPanorama], (layout, allItems, xAxisId, yAxisId, isPanorama) => allItems.filter(i => {
  if (layout === 'horizontal') {
    return i.xAxisId === xAxisId;
  }
  return i.yAxisId === yAxisId;
}).filter(i => i.isPanorama === isPanorama).filter(i => i.hide === false).filter(i => i.type === 'bar'));
var selectBarStackGroups = (state, id, isPanorama) => {
  var layout = selectChartLayout(state);
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null || yAxisId == null) {
    return undefined;
  }
  if (layout === 'horizontal') {
    return selectStackGroups(state, 'yAxis', yAxisId, isPanorama);
  }
  return selectStackGroups(state, 'xAxis', xAxisId, isPanorama);
};
var selectBarCartesianAxisSize = (state, id) => {
  var layout = selectChartLayout(state);
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null || yAxisId == null) {
    return undefined;
  }
  if (layout === 'horizontal') {
    return selectCartesianAxisSize(state, 'xAxis', xAxisId);
  }
  return selectCartesianAxisSize(state, 'yAxis', yAxisId);
};
var selectBarSizeList = createSelector([selectAllVisibleBars, selectRootBarSize, selectBarCartesianAxisSize], combineBarSizeList);
var selectBarBandSize = (state, id, isPanorama) => {
  var _ref, _getBandSizeOfAxis;
  var barSettings = selectSynchronisedBarSettings(state, id);
  if (barSettings == null) {
    return 0;
  }
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null || yAxisId == null) {
    return 0;
  }
  var layout = selectChartLayout(state);
  var globalMaxBarSize = selectRootMaxBarSize(state);
  var {
    maxBarSize: childMaxBarSize
  } = barSettings;
  var maxBarSize = isNullish(childMaxBarSize) ? globalMaxBarSize : childMaxBarSize;
  var axis, ticks;
  if (layout === 'horizontal') {
    axis = selectAxisWithScale(state, 'xAxis', xAxisId, isPanorama);
    ticks = selectTicksOfGraphicalItem(state, 'xAxis', xAxisId, isPanorama);
  } else {
    axis = selectAxisWithScale(state, 'yAxis', yAxisId, isPanorama);
    ticks = selectTicksOfGraphicalItem(state, 'yAxis', yAxisId, isPanorama);
  }
  return (_ref = (_getBandSizeOfAxis = getBandSizeOfAxis(axis, ticks, true)) !== null && _getBandSizeOfAxis !== void 0 ? _getBandSizeOfAxis : maxBarSize) !== null && _ref !== void 0 ? _ref : 0;
};
var selectAxisBandSize = (state, id, isPanorama) => {
  var layout = selectChartLayout(state);
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null || yAxisId == null) {
    return undefined;
  }
  var axis, ticks;
  if (layout === 'horizontal') {
    axis = selectAxisWithScale(state, 'xAxis', xAxisId, isPanorama);
    ticks = selectTicksOfGraphicalItem(state, 'xAxis', xAxisId, isPanorama);
  } else {
    axis = selectAxisWithScale(state, 'yAxis', yAxisId, isPanorama);
    ticks = selectTicksOfGraphicalItem(state, 'yAxis', yAxisId, isPanorama);
  }
  return getBandSizeOfAxis(axis, ticks);
};
var selectAllBarPositions = createSelector([selectBarSizeList, selectRootMaxBarSize, selectBarGap, selectBarCategoryGap, selectBarBandSize, selectAxisBandSize, selectMaxBarSize], combineAllBarPositions);
var selectXAxisWithScale = (state, id, isPanorama) => {
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null) {
    return undefined;
  }
  return selectAxisWithScale(state, 'xAxis', xAxisId, isPanorama);
};
var selectYAxisWithScale = (state, id, isPanorama) => {
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (yAxisId == null) {
    return undefined;
  }
  return selectAxisWithScale(state, 'yAxis', yAxisId, isPanorama);
};
var selectXAxisTicks = (state, id, isPanorama) => {
  var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
  if (xAxisId == null) {
    return undefined;
  }
  return selectTicksOfGraphicalItem(state, 'xAxis', xAxisId, isPanorama);
};
var selectYAxisTicks = (state, id, isPanorama) => {
  var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
  if (yAxisId == null) {
    return undefined;
  }
  return selectTicksOfGraphicalItem(state, 'yAxis', yAxisId, isPanorama);
};
var selectBarPosition = createSelector([selectAllBarPositions, selectSynchronisedBarSettings], combineBarPosition);
var selectStackedDataOfItem = createSelector([selectBarStackGroups, selectSynchronisedBarSettings], combineStackedData);
var selectBarRectangles = createSelector([selectChartOffsetInternal, selectAxisViewBox, selectXAxisWithScale, selectYAxisWithScale, selectXAxisTicks, selectYAxisTicks, selectBarPosition, selectChartLayout, selectChartDataWithIndexesIfNotInPanoramaPosition3, selectAxisBandSize, selectStackedDataOfItem, selectSynchronisedBarSettings, pickCells], (offset, axisViewBox, xAxis, yAxis, xAxisTicks, yAxisTicks, pos, layout, _ref2, bandSize, stackedData, barSettings, cells) => {
  var {
    chartData,
    dataStartIndex,
    dataEndIndex
  } = _ref2;
  if (barSettings == null || pos == null || axisViewBox == null || layout !== 'horizontal' && layout !== 'vertical' || xAxis == null || yAxis == null || xAxisTicks == null || yAxisTicks == null || bandSize == null) {
    return undefined;
  }
  var {
    data
  } = barSettings;
  var displayedData;
  if (data != null && data.length > 0) {
    displayedData = data;
  } else {
    displayedData = chartData === null || chartData === void 0 ? void 0 : chartData.slice(dataStartIndex, dataEndIndex + 1);
  }
  if (displayedData == null) {
    return undefined;
  }
  return computeBarRectangles({
    layout,
    barSettings,
    pos,
    parentViewBox: axisViewBox,
    bandSize,
    xAxis,
    yAxis,
    xAxisTicks,
    yAxisTicks,
    stackedData,
    displayedData,
    offset,
    cells,
    dataStartIndex
  });
});
;// ./node_modules/recharts/es6/zIndex/getZIndexFromUnknown.js

function getZIndexFromUnknown(input, defaultZIndex) {
  if (input && typeof input === 'object' && 'zIndex' in input && typeof input.zIndex === 'number' && isWellBehavedNumber(input.zIndex)) {
    return input.zIndex;
  }
  return defaultZIndex;
}
;// ./node_modules/recharts/es6/state/selectors/barStackSelectors.js



var pickStackId = (state, stackId) => stackId;
var barStackSelectors_pickIsPanorama = (state, stackId, isPanorama) => isPanorama;
var selectAllBarsInStack = createSelector([pickStackId, selectUnfilteredCartesianItems, barStackSelectors_pickIsPanorama], (stackId, allItems, isPanorama) => {
  return allItems.filter(i => i.type === 'bar').filter(i => i.stackId === stackId).filter(i => i.isPanorama === isPanorama).filter(i => !i.hide);
});
var selectAllBarIdsInStack = createSelector([selectAllBarsInStack], allBars => {
  return allBars.map(bar => bar.id);
});
/**
 * Takes two rectangles and returns a new rectangle that encompasses both.
 * It takes the minimum x and y, and the maximum width and height.
 * It handles overlapping rectangles, and rectangles with a gap between them.
 * @param rect1
 * @param rect2
 */
var expandRectangle = (rect1, rect2) => {
  if (!rect1) {
    return rect2;
  }
  if (!rect2) {
    return rect1;
  }
  var x = Math.min(rect1.x, rect1.x + rect1.width, rect2.x, rect2.x + rect2.width);
  var y = Math.min(rect1.y, rect1.y + rect1.height, rect2.y, rect2.y + rect2.height);
  var maxX = Math.max(rect1.x, rect1.x + rect1.width, rect2.x, rect2.x + rect2.width);
  var maxY = Math.max(rect1.y, rect1.y + rect1.height, rect2.y, rect2.y + rect2.height);
  var width = maxX - x;
  var height = maxY - y;
  return {
    x,
    y,
    width,
    height
  };
};
var combineStackRects = (state, stackId, isPanorama) => {
  var allBarIds = selectAllBarIdsInStack(state, stackId, isPanorama);
  var stackRects = [];
  allBarIds.forEach(barId => {
    var rectangles = selectBarRectangles(state, barId, isPanorama, undefined);
    rectangles === null || rectangles === void 0 || rectangles.forEach((rect, index) => {
      stackRects[index] = expandRectangle(stackRects[index], rect);
    });
  });
  return stackRects;
};
var selectStackRects = createSelector([state => state, pickStackId, barStackSelectors_pickIsPanorama], combineStackRects);
;// ./node_modules/recharts/es6/cartesian/BarStack.js
var BarStack_excluded = ["index"];
function BarStack_extends() { return BarStack_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, BarStack_extends.apply(null, arguments); }
function BarStack_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = BarStack_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function BarStack_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }











var BarStackContext = /*#__PURE__*/(0,external_React_.createContext)(undefined);

/**
 * Hook to resolve the stack ID for a Bar component.
 * If a stack ID is provided via props, it is used directly.
 * Otherwise, this will read stack ID from BarStack context if available.
 * If both are undefined, it returns undefined.
 * @param childStackId
 */
var useStackId = childStackId => {
  var stackSettings = (0,external_React_.useContext)(BarStackContext);
  if (stackSettings != null) {
    return stackSettings.stackId;
  }
  if (childStackId == null) {
    return undefined;
  }
  return getNormalizedStackId(childStackId);
};
var defaultBarStackProps = {
  radius: 0
};
var getClipPathId = (stackId, index) => {
  return "recharts-bar-stack-clip-path-".concat(stackId, "-").concat(index);
};
var useBarStackClipPathUrl = index => {
  var barStackContext = (0,external_React_.useContext)(BarStackContext);
  if (barStackContext == null) {
    return undefined;
  }
  var {
    stackId
  } = barStackContext;
  return "url(#".concat(getClipPathId(stackId, index), ")");
};
var BarStackClipLayer = _ref => {
  var {
      index
    } = _ref,
    rest = BarStack_objectWithoutProperties(_ref, BarStack_excluded);
  var clipPathUrl = useBarStackClipPathUrl(index);
  return /*#__PURE__*/external_React_.createElement(Layer, BarStack_extends({
    className: "recharts-bar-stack-layer",
    clipPath: clipPathUrl
  }, rest));
};

/**
 * This React component will render a clipPath that the individual bars in the stack will reference
 * to achieve rounded corners for the entire stack.
 */
var BarStackClipPath = _ref2 => {
  var {
    stackId,
    radius
  } = _ref2;
  var isPanorama = useIsPanorama();
  var positions = useAppSelector(state => selectStackRects(state, stackId, isPanorama));
  if (positions == null || positions.length === 0) {
    return null;
  }
  /*
   * Render one clipPath per rectangle in the stack.
   * Each rectangle corresponds to one data entry in the chart.
   */
  return /*#__PURE__*/external_React_.createElement("defs", null, positions.map((pos, index) => {
    if (pos == null) {
      return null;
    }
    var clipPathId = getClipPathId(stackId, index);
    return /*#__PURE__*/external_React_.createElement("clipPath", {
      key: clipPathId,
      id: clipPathId
    }, /*#__PURE__*/external_React_.createElement(Rectangle, {
      isAnimationActive: false,
      isUpdateAnimationActive: false,
      x: pos.x,
      y: pos.y,
      width: pos.width,
      height: pos.height,
      radius: radius
    }));
  }));
};
var BarStackImpl = props => {
  var resolvedStackId = useUniqueId('recharts-bar-stack', getNormalizedStackId(props.stackId));
  var {
    children,
    radius
  } = resolveDefaultProps(props, defaultBarStackProps);
  var context = (0,external_React_.useMemo)(() => ({
    stackId: resolvedStackId,
    radius
  }), [resolvedStackId, radius]);
  return /*#__PURE__*/external_React_.createElement(BarStackContext.Provider, {
    value: context
  }, /*#__PURE__*/external_React_.createElement(BarStackClipPath, {
    stackId: resolvedStackId,
    radius: radius
  }), children);
};

/**
 * @provides BarStackContext
 * @since 3.6
 */
var BarStack = /*#__PURE__*/external_React_.memo(BarStackImpl, propsAreEqual);
;// ./node_modules/recharts/es6/cartesian/Bar.js
var Bar_excluded = ["onMouseEnter", "onMouseLeave", "onClick"],
  Bar_excluded2 = ["value", "background", "tooltipPosition"],
  Bar_excluded3 = ["id"],
  Bar_excluded4 = ["onMouseEnter", "onClick", "onMouseLeave"];
function Bar_extends() { return Bar_extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, Bar_extends.apply(null, arguments); }
function Bar_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function Bar_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? Bar_ownKeys(Object(t), !0).forEach(function (r) { Bar_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : Bar_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function Bar_defineProperty(e, r, t) { return (r = Bar_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function Bar_toPropertyKey(t) { var i = Bar_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function Bar_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function Bar_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = Bar_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function Bar_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
































var computeLegendPayloadFromBarData = props => {
  var {
    dataKey,
    name,
    fill,
    legendType,
    hide
  } = props;
  return [{
    inactive: hide,
    dataKey,
    type: legendType,
    color: fill,
    value: getTooltipNameProp(name, dataKey),
    payload: props
  }];
};
var SetBarTooltipEntrySettings = /*#__PURE__*/external_React_.memo(_ref => {
  var {
    dataKey,
    stroke,
    strokeWidth,
    fill,
    name,
    hide,
    unit,
    tooltipType,
    id
  } = _ref;
  var tooltipEntrySettings = {
    dataDefinedOnItem: undefined,
    getPosition: noop,
    settings: {
      stroke,
      strokeWidth,
      fill,
      dataKey,
      nameKey: undefined,
      name: getTooltipNameProp(name, dataKey),
      hide,
      type: tooltipType,
      color: fill,
      unit,
      graphicalItemId: id
    }
  };
  return /*#__PURE__*/external_React_.createElement(SetTooltipEntrySettings, {
    tooltipEntrySettings: tooltipEntrySettings
  });
});
function BarBackground(props) {
  var activeIndex = useAppSelector(selectActiveTooltipIndex);
  var {
    data,
    dataKey,
    background: backgroundFromProps,
    allOtherBarProps
  } = props;
  var {
      onMouseEnter: onMouseEnterFromProps,
      onMouseLeave: onMouseLeaveFromProps,
      onClick: onItemClickFromProps
    } = allOtherBarProps,
    restOfAllOtherProps = Bar_objectWithoutProperties(allOtherBarProps, Bar_excluded);

  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onMouseEnterFromContext = useMouseEnterItemDispatch(onMouseEnterFromProps, dataKey, allOtherBarProps.id);
  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onMouseLeaveFromContext = useMouseLeaveItemDispatch(onMouseLeaveFromProps);
  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onClickFromContext = useMouseClickItemDispatch(onItemClickFromProps, dataKey, allOtherBarProps.id);
  if (!backgroundFromProps || data == null) {
    return null;
  }
  var backgroundProps = svgPropertiesNoEventsFromUnknown(backgroundFromProps);
  return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: getZIndexFromUnknown(backgroundFromProps, DefaultZIndexes.barBackground)
  }, data.map((entry, i) => {
    var {
        value,
        background: backgroundFromDataEntry,
        tooltipPosition
      } = entry,
      rest = Bar_objectWithoutProperties(entry, Bar_excluded2);
    if (!backgroundFromDataEntry) {
      return null;
    }

    // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
    var onMouseEnter = onMouseEnterFromContext(entry, i);
    // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
    var onMouseLeave = onMouseLeaveFromContext(entry, i);
    // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
    var onClick = onClickFromContext(entry, i);
    var barRectangleProps = Bar_objectSpread(Bar_objectSpread(Bar_objectSpread(Bar_objectSpread(Bar_objectSpread({
      option: backgroundFromProps,
      isActive: String(i) === activeIndex
    }, rest), {}, {
      // @ts-expect-error backgroundProps is contributing unknown props
      fill: '#eee'
    }, backgroundFromDataEntry), backgroundProps), adaptEventsOfChild(restOfAllOtherProps, entry, i)), {}, {
      onMouseEnter,
      onMouseLeave,
      onClick,
      dataKey,
      index: i,
      className: 'recharts-bar-background-rectangle'
    });
    return /*#__PURE__*/external_React_.createElement(BarRectangle, Bar_extends({
      key: "background-bar-".concat(i)
    }, barRectangleProps));
  }));
}
function BarLabelListProvider(_ref2) {
  var {
    showLabels,
    children,
    rects
  } = _ref2;
  var labelListEntries = rects === null || rects === void 0 ? void 0 : rects.map(entry => {
    var viewBox = {
      x: entry.x,
      y: entry.y,
      width: entry.width,
      lowerWidth: entry.width,
      upperWidth: entry.width,
      height: entry.height
    };
    return Bar_objectSpread(Bar_objectSpread({}, viewBox), {}, {
      value: entry.value,
      payload: entry.payload,
      parentViewBox: entry.parentViewBox,
      viewBox,
      fill: entry.fill
    });
  });
  return /*#__PURE__*/external_React_.createElement(CartesianLabelListContextProvider, {
    value: showLabels ? labelListEntries : undefined
  }, children);
}
function BarRectangleWithActiveState(props) {
  var {
    shape,
    activeBar,
    baseProps,
    entry,
    index,
    dataKey
  } = props;
  var activeIndex = useAppSelector(selectActiveTooltipIndex);
  var activeDataKey = useAppSelector(selectActiveTooltipDataKey);
  /*
   * Bars support stacking, meaning that there can be multiple bars at the same x value.
   * With Tooltip shared=false we only want to highlight the currently active Bar, not all.
   *
   * Also, if the tooltip is shared, we want to highlight all bars at the same x value
   * regardless of the dataKey.
   *
   * With shared Tooltip, the activeDataKey is undefined.
   */
  var isActive = activeBar && String(index) === activeIndex && (activeDataKey == null || dataKey === activeDataKey);
  var option = isActive ? activeBar : shape;
  if (isActive) {
    return /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
      zIndex: DefaultZIndexes.activeBar
    }, /*#__PURE__*/external_React_.createElement(BarStackClipLayer, {
      index: index
    }, /*#__PURE__*/external_React_.createElement(BarRectangle, Bar_extends({}, baseProps, {
      name: String(baseProps.name)
    }, entry, {
      isActive: isActive,
      option: option,
      index: index,
      dataKey: dataKey
    }))));
  }
  return /*#__PURE__*/external_React_.createElement(BarRectangle, Bar_extends({}, baseProps, {
    name: String(baseProps.name)
  }, entry, {
    isActive: isActive,
    option: option,
    index: index,
    dataKey: dataKey
  }));
}
function BarRectangleNeverActive(props) {
  var {
    shape,
    baseProps,
    entry,
    index,
    dataKey
  } = props;
  return /*#__PURE__*/external_React_.createElement(BarRectangle, Bar_extends({}, baseProps, {
    name: String(baseProps.name)
  }, entry, {
    isActive: false,
    option: shape,
    index: index,
    dataKey: dataKey
  }));
}
function BarRectangles(_ref3) {
  var _svgPropertiesNoEvent;
  var {
    data,
    props
  } = _ref3;
  var _ref4 = (_svgPropertiesNoEvent = svgPropertiesNoEvents(props)) !== null && _svgPropertiesNoEvent !== void 0 ? _svgPropertiesNoEvent : {},
    {
      id
    } = _ref4,
    baseProps = Bar_objectWithoutProperties(_ref4, Bar_excluded3);
  var {
    shape,
    dataKey,
    activeBar
  } = props;
  var {
      onMouseEnter: onMouseEnterFromProps,
      onClick: onItemClickFromProps,
      onMouseLeave: onMouseLeaveFromProps
    } = props,
    restOfAllOtherProps = Bar_objectWithoutProperties(props, Bar_excluded4);

  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onMouseEnterFromContext = useMouseEnterItemDispatch(onMouseEnterFromProps, dataKey, id);
  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onMouseLeaveFromContext = useMouseLeaveItemDispatch(onMouseLeaveFromProps);
  // @ts-expect-error bar mouse events are not compatible with recharts mouse events
  var onClickFromContext = useMouseClickItemDispatch(onItemClickFromProps, dataKey, id);
  if (!data) {
    return null;
  }
  return /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, data.map((entry, i) => {
    return /*#__PURE__*/external_React_.createElement(BarStackClipLayer, Bar_extends({
      index: i
      // https://github.com/recharts/recharts/issues/5415
      ,
      key: "rectangle-".concat(entry === null || entry === void 0 ? void 0 : entry.x, "-").concat(entry === null || entry === void 0 ? void 0 : entry.y, "-").concat(entry === null || entry === void 0 ? void 0 : entry.value, "-").concat(i),
      className: "recharts-bar-rectangle"
    }, adaptEventsOfChild(restOfAllOtherProps, entry, i), {
      // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
      onMouseEnter: onMouseEnterFromContext(entry, i)
      // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
      ,
      onMouseLeave: onMouseLeaveFromContext(entry, i)
      // @ts-expect-error BarRectangleItem type definition says it's missing properties, but I can see them present in debugger!
      ,
      onClick: onClickFromContext(entry, i)
    }), activeBar ? /*#__PURE__*/external_React_.createElement(BarRectangleWithActiveState, {
      shape: shape,
      activeBar: activeBar,
      baseProps: baseProps,
      entry: entry,
      index: i,
      dataKey: dataKey
    }) :
    /*#__PURE__*/
    /*
     * If the `activeBar` prop is falsy, then let's call the variant without hooks.
     * Using the `selectActiveTooltipIndex` selector is usually fast
     * but in charts with large-ish amount of data even the few nanoseconds add up to a noticeable jank.
     * If the activeBar is false then we don't need to know which index is active - because we won't use it anyway.
     * So let's just skip the hooks altogether. That way, React can skip rendering the component,
     * and can skip the tree reconciliation for its children too.
     * Because we can't call hooks conditionally, we need to have a separate component for that.
     */
    external_React_.createElement(BarRectangleNeverActive, {
      shape: shape,
      baseProps: baseProps,
      entry: entry,
      index: i,
      dataKey: dataKey
    }));
  }));
}
function RectanglesWithAnimation(_ref5) {
  var {
    props,
    previousRectanglesRef
  } = _ref5;
  var {
    data,
    layout,
    isAnimationActive,
    animationBegin,
    animationDuration,
    animationEasing,
    onAnimationEnd,
    onAnimationStart
  } = props;
  var prevData = previousRectanglesRef.current;
  var animationId = useAnimationId(props, 'recharts-bar-');
  var [isAnimating, setIsAnimating] = (0,external_React_.useState)(false);
  var showLabels = !isAnimating;
  var handleAnimationEnd = (0,external_React_.useCallback)(() => {
    if (typeof onAnimationEnd === 'function') {
      onAnimationEnd();
    }
    setIsAnimating(false);
  }, [onAnimationEnd]);
  var handleAnimationStart = (0,external_React_.useCallback)(() => {
    if (typeof onAnimationStart === 'function') {
      onAnimationStart();
    }
    setIsAnimating(true);
  }, [onAnimationStart]);
  return /*#__PURE__*/external_React_.createElement(BarLabelListProvider, {
    showLabels: showLabels,
    rects: data
  }, /*#__PURE__*/external_React_.createElement(JavascriptAnimate, {
    animationId: animationId,
    begin: animationBegin,
    duration: animationDuration,
    isActive: isAnimationActive,
    easing: animationEasing,
    onAnimationEnd: handleAnimationEnd,
    onAnimationStart: handleAnimationStart,
    key: animationId
  }, t => {
    var stepData = t === 1 ? data : data === null || data === void 0 ? void 0 : data.map((entry, index) => {
      var prev = prevData && prevData[index];
      if (prev) {
        return Bar_objectSpread(Bar_objectSpread({}, entry), {}, {
          x: interpolate(prev.x, entry.x, t),
          y: interpolate(prev.y, entry.y, t),
          width: interpolate(prev.width, entry.width, t),
          height: interpolate(prev.height, entry.height, t)
        });
      }
      if (layout === 'horizontal') {
        var height = interpolate(0, entry.height, t);
        var y = interpolate(entry.stackedBarStart, entry.y, t);
        return Bar_objectSpread(Bar_objectSpread({}, entry), {}, {
          y,
          height
        });
      }
      var w = interpolate(0, entry.width, t);
      var x = interpolate(entry.stackedBarStart, entry.x, t);
      return Bar_objectSpread(Bar_objectSpread({}, entry), {}, {
        width: w,
        x
      });
    });
    if (t > 0) {
      // eslint-disable-next-line no-param-reassign
      previousRectanglesRef.current = stepData !== null && stepData !== void 0 ? stepData : null;
    }
    if (stepData == null) {
      return null;
    }
    return /*#__PURE__*/external_React_.createElement(Layer, null, /*#__PURE__*/external_React_.createElement(BarRectangles, {
      props: props,
      data: stepData
    }));
  }), /*#__PURE__*/external_React_.createElement(LabelListFromLabelProp, {
    label: props.label
  }), props.children);
}
function RenderRectangles(props) {
  var previousRectanglesRef = (0,external_React_.useRef)(null);
  return /*#__PURE__*/external_React_.createElement(RectanglesWithAnimation, {
    previousRectanglesRef: previousRectanglesRef,
    props: props
  });
}
var defaultMinPointSize = 0;
var errorBarDataPointFormatter = (dataPoint, dataKey) => {
  /**
   * if the value coming from `selectBarRectangles` is an array then this is a stacked bar chart.
   * arr[1] represents end value of the bar since the data is in the form of [startValue, endValue].
   * */
  var value = Array.isArray(dataPoint.value) ? dataPoint.value[1] : dataPoint.value;
  return {
    x: dataPoint.x,
    y: dataPoint.y,
    value,
    // @ts-expect-error getValueByDataKey does not validate the output type
    errorVal: getValueByDataKey(dataPoint, dataKey)
  };
};
class BarWithState extends external_React_.PureComponent {
  render() {
    var {
      hide,
      data,
      dataKey,
      className,
      xAxisId,
      yAxisId,
      needClip,
      background,
      id
    } = this.props;
    if (hide || data == null) {
      return null;
    }
    var layerClass = clsx('recharts-bar', className);
    var clipPathId = id;
    return /*#__PURE__*/external_React_.createElement(Layer, {
      className: layerClass,
      id: id
    }, needClip && /*#__PURE__*/external_React_.createElement("defs", null, /*#__PURE__*/external_React_.createElement(GraphicalItemClipPath, {
      clipPathId: clipPathId,
      xAxisId: xAxisId,
      yAxisId: yAxisId
    })), /*#__PURE__*/external_React_.createElement(Layer, {
      className: "recharts-bar-rectangles",
      clipPath: needClip ? "url(#clipPath-".concat(clipPathId, ")") : undefined
    }, /*#__PURE__*/external_React_.createElement(BarBackground, {
      data: data,
      dataKey: dataKey,
      background: background,
      allOtherBarProps: this.props
    }), /*#__PURE__*/external_React_.createElement(RenderRectangles, this.props)));
  }
}
var defaultBarProps = {
  activeBar: false,
  animationBegin: 0,
  animationDuration: 400,
  animationEasing: 'ease',
  background: false,
  hide: false,
  isAnimationActive: 'auto',
  label: false,
  legendType: 'rect',
  minPointSize: defaultMinPointSize,
  xAxisId: 0,
  yAxisId: 0,
  zIndex: DefaultZIndexes.bar
};
function BarImpl(props) {
  var {
    xAxisId,
    yAxisId,
    hide,
    legendType,
    minPointSize,
    activeBar,
    animationBegin,
    animationDuration,
    animationEasing,
    isAnimationActive
  } = props;
  var {
    needClip
  } = useNeedsClip(xAxisId, yAxisId);
  var layout = useChartLayout();
  var isPanorama = useIsPanorama();
  var cells = findAllByType(props.children, Cell_Cell);
  var rects = useAppSelector(state => selectBarRectangles(state, props.id, isPanorama, cells));
  if (layout !== 'vertical' && layout !== 'horizontal') {
    return null;
  }
  var errorBarOffset;
  var firstDataPoint = rects === null || rects === void 0 ? void 0 : rects[0];
  if (firstDataPoint == null || firstDataPoint.height == null || firstDataPoint.width == null) {
    errorBarOffset = 0;
  } else {
    errorBarOffset = layout === 'vertical' ? firstDataPoint.height / 2 : firstDataPoint.width / 2;
  }
  return /*#__PURE__*/external_React_.createElement(SetErrorBarContext, {
    xAxisId: xAxisId,
    yAxisId: yAxisId,
    data: rects,
    dataPointFormatter: errorBarDataPointFormatter,
    errorBarOffset: errorBarOffset
  }, /*#__PURE__*/external_React_.createElement(BarWithState, Bar_extends({}, props, {
    layout: layout,
    needClip: needClip,
    data: rects,
    xAxisId: xAxisId,
    yAxisId: yAxisId,
    hide: hide,
    legendType: legendType,
    minPointSize: minPointSize,
    activeBar: activeBar,
    animationBegin: animationBegin,
    animationDuration: animationDuration,
    animationEasing: animationEasing,
    isAnimationActive: isAnimationActive
  })));
}
function computeBarRectangles(_ref6) {
  var {
    layout,
    barSettings: {
      dataKey,
      minPointSize: minPointSizeProp
    },
    pos,
    bandSize,
    xAxis,
    yAxis,
    xAxisTicks,
    yAxisTicks,
    stackedData,
    displayedData,
    offset,
    cells,
    parentViewBox,
    dataStartIndex
  } = _ref6;
  var numericAxis = layout === 'horizontal' ? yAxis : xAxis;
  // @ts-expect-error this assumes that the domain is always numeric, but doesn't check for it
  var stackedDomain = stackedData ? numericAxis.scale.domain() : null;
  var baseValue = getBaseValueOfBar({
    numericAxis
  });
  var stackedBarStart = numericAxis.scale.map(baseValue);
  return displayedData.map((entry, index) => {
    var value, x, y, width, height, background;
    if (stackedData) {
      // Use dataStartIndex to access the correct element in the full stackedData array
      var untruncatedValue = stackedData[index + dataStartIndex];
      if (untruncatedValue == null) {
        return null;
      }
      value = truncateByDomain(untruncatedValue, stackedDomain);
    } else {
      value = getValueByDataKey(entry, dataKey);
      if (!Array.isArray(value)) {
        value = [baseValue, value];
      }
    }
    var minPointSize = minPointSizeCallback(minPointSizeProp, defaultMinPointSize)(value[1], index);
    if (layout === 'horizontal') {
      var _ref7;
      var baseValueScale = yAxis.scale.map(value[0]);
      var currentValueScale = yAxis.scale.map(value[1]);
      if (baseValueScale == null || currentValueScale == null) {
        return null;
      }
      x = getCateCoordinateOfBar({
        axis: xAxis,
        ticks: xAxisTicks,
        bandSize,
        offset: pos.offset,
        entry,
        index
      });
      y = (_ref7 = currentValueScale !== null && currentValueScale !== void 0 ? currentValueScale : baseValueScale) !== null && _ref7 !== void 0 ? _ref7 : undefined;
      width = pos.size;
      var computedHeight = baseValueScale - currentValueScale;
      height = isNan(computedHeight) ? 0 : computedHeight;
      background = {
        x,
        y: offset.top,
        width,
        height: offset.height
      };
      if (Math.abs(minPointSize) > 0 && Math.abs(height) < Math.abs(minPointSize)) {
        var delta = mathSign(height || minPointSize) * (Math.abs(minPointSize) - Math.abs(height));
        y -= delta;
        height += delta;
      }
    } else {
      var _baseValueScale = xAxis.scale.map(value[0]);
      var _currentValueScale = xAxis.scale.map(value[1]);
      if (_baseValueScale == null || _currentValueScale == null) {
        return null;
      }
      x = _baseValueScale;
      y = getCateCoordinateOfBar({
        axis: yAxis,
        ticks: yAxisTicks,
        bandSize,
        offset: pos.offset,
        entry,
        index
      });
      width = _currentValueScale - _baseValueScale;
      height = pos.size;
      background = {
        x: offset.left,
        y,
        width: offset.width,
        height
      };
      if (Math.abs(minPointSize) > 0 && Math.abs(width) < Math.abs(minPointSize)) {
        var _delta = mathSign(width || minPointSize) * (Math.abs(minPointSize) - Math.abs(width));
        width += _delta;
      }
    }
    if (x == null || y == null || width == null || height == null) {
      return null;
    }
    var barRectangleItem = Bar_objectSpread(Bar_objectSpread({}, entry), {}, {
      stackedBarStart,
      x,
      y,
      width,
      height,
      value: stackedData ? value : value[1],
      payload: entry,
      background,
      tooltipPosition: {
        x: x + width / 2,
        y: y + height / 2
      },
      parentViewBox
    }, cells && cells[index] && cells[index].props);
    return barRectangleItem;
  }).filter(Boolean);
}
function BarFn(outsideProps) {
  var props = resolveDefaultProps(outsideProps, defaultBarProps);
  // stackId may arrive from props or from BarStack context
  var stackId = useStackId(props.stackId);
  var isPanorama = useIsPanorama();
  // Report all props to Redux store first, before calling any hooks, to avoid circular dependencies.
  return /*#__PURE__*/external_React_.createElement(RegisterGraphicalItemId, {
    id: props.id,
    type: "bar"
  }, id => /*#__PURE__*/external_React_.createElement(external_React_.Fragment, null, /*#__PURE__*/external_React_.createElement(SetLegendPayload, {
    legendPayload: computeLegendPayloadFromBarData(props)
  }), /*#__PURE__*/external_React_.createElement(SetBarTooltipEntrySettings, {
    dataKey: props.dataKey,
    stroke: props.stroke,
    strokeWidth: props.strokeWidth,
    fill: props.fill,
    name: props.name,
    hide: props.hide,
    unit: props.unit,
    tooltipType: props.tooltipType,
    id: id
  }), /*#__PURE__*/external_React_.createElement(SetCartesianGraphicalItem, {
    type: "bar",
    id: id
    // Bar does not allow setting data directly on the graphical item (why?)
    ,
    data: undefined,
    xAxisId: props.xAxisId,
    yAxisId: props.yAxisId,
    zAxisId: 0,
    dataKey: props.dataKey,
    stackId: stackId,
    hide: props.hide,
    barSize: props.barSize,
    minPointSize: props.minPointSize,
    maxBarSize: props.maxBarSize,
    isPanorama: isPanorama
  }), /*#__PURE__*/external_React_.createElement(ZIndexLayer, {
    zIndex: props.zIndex
  }, /*#__PURE__*/external_React_.createElement(BarImpl, Bar_extends({}, props, {
    id: id
  })))));
}

/**
 * @provides ErrorBarContext
 * @provides LabelListContext
 * @provides CellReader
 * @consumes CartesianChartContext
 * @consumes BarStackContext
 */
var Bar = /*#__PURE__*/external_React_.memo(BarFn, propsAreEqual);
Bar.displayName = 'Bar';
;// ./rackets-db.json
const rackets_db_namespaceObject = /*#__PURE__*/JSON.parse('[{"id":"adidas-adipower-ctrl-2025","name":"Adidas Adipower CTRL 3.4 2025","shortName":"Adipower CTRL","brand":"Adidas","shape":"Ronde","weight":"365-375g","balance":"Moyen","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"200-270€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/adidas-adipower-multiweight-ctrl-3-4-2025-alex-ruiz.png","year":2025,"category":"avance","scores":{"Puissance":6,"Contrôle":8.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":8.5},"verdict":"Ronde carbone pour le contrôle pur. Grande tolérance grâce à la forme ronde malgré le carbone. Pour les défenseurs avancés.","editorial":"L\'Adipower CTRL 2025 est la porte d\'entrée de la gamme contrôle Adidas. Surface carbone standard pour une rigidité mesurée, noyau EVA classique pour un toucher neutre. Forme ronde qui pardonne les approximations. Pas de technologie spectaculaire, mais une exécution solide et fiable. C\'est la pala du joueur avancé qui veut du contrôle sans chichis — efficace, sobre, sans surprise.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — rigidité honnête, toucher direct sans excès"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse EVA référence — densité moyenne, bon compromis confort/réactivité"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré, grande tolérance sur les frappes décentrées"},{"label":"Équilibre","value":"Moyen","detail":"Ni en tête ni en manche — maniabilité neutre, adaptée à tous les coups"}],"targetProfile":"Joueurs avancés défensifs ou polyvalents. Bonne pala de progression pour le joueur intermédiaire qui passe au carbone."},{"id":"adidas-adipower-jr-2025","name":"Adidas Adipower Junior 3.4 2025","shortName":"Adipower Jr 3.4","brand":"Adidas","shape":"Ronde","weight":"310-330g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"62-85€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/adidas-adipower-junior-3-4-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":9,"Tolérance":9},"verdict":"Version junior de l\'Adipower. Forme ronde très tolérante avec mousse EVA Soft pour un maximum de confort et de facilité de jeu.","editorial":"L\'Adipower Jr 2025 est la junior Adidas. Fibre de verre et Soft EVA en ronde — construction ultra-légère et confortable pour les jeunes joueurs. Le format réduit est adapté aux mains d\'enfant. L\'Adipower Jr est l\'initiation au padel chez Adidas.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux pour les juniors"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection des articulations jeunes"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale — confiance pour les jeunes joueurs"}],"targetProfile":"Juniors. L\'Adidas pour les jeunes qui découvrent le padel."},{"id":"adidas-adipower-light-2025","name":"Adidas Adipower Light 3.4 2025","shortName":"Adipower Light","brand":"Adidas","shape":"Ronde","weight":"350-360g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"70-100€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/adidas-adipower-light-3-4-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Version light de l\'Adipower, parfaite pour les débutants. Mousse EVA Soft pour un confort maximal et forme ronde très tolérante.","editorial":"L\'Adipower Light 2025 est la pala d\'initiation d\'Adidas. Fibre de verre et Soft EVA en ronde — les matériaux les plus confortables et tolérants. Légère, maniable, indulgente. C\'est la pala idéale pour les premiers mois de padel : elle pardonne tout et encourage à jouer.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux et confortable"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — maximum de confort pour les débutants"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré et large — tolérance maximale"}],"targetProfile":"Débutants. L\'Adidas pour apprendre en confiance."},{"id":"adidas-adipower-multiweight-2025","name":"Adidas Adipower Multiweight 2025","shortName":"Adipower Multi","brand":"Adidas","shape":"Goutte d\'eau","weight":"355-375g","balance":"Moyen","surface":"Hybrid carbon/fiberglass","core":"Standard EVA","antivib":"—","price":"150-200€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/adidas-adipower-multiweight-2023.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":8,"Confort":7,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Raquette polyvalente avec système de poids ajustable. Permet d\'adapter l\'équilibre selon son jeu, très bonne idée pour les joueurs en progression.","editorial":"L\'Adipower Multiweight 2025 est la polyvalente intermédiaire d\'Adidas. Hybride carbone/fibre de verre pour un toucher accessible, EVA standard pour la neutralité. Forme goutte d\'eau classique. Le Multiweight permet d\'ajuster le poids avec des lests — une fonctionnalité rare à ce niveau de gamme qui aide à trouver son équilibre idéal.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible — rigidité modérée pour un toucher doux"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — sweet spot intermédiaire tolérant"},{"label":"Technologie","value":"Multiweight","detail":"Système de lests ajustables — personnalisez le poids selon votre style"}],"targetProfile":"Joueurs intermédiaires polyvalents. Le système Multiweight aide à trouver son réglage idéal."},{"id":"adidas-metalbone-2025","name":"Adidas Metalbone 3.4 2025","shortName":"Metalbone 3.4","brand":"Adidas","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"180-250€","player":"Ale Galán","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-3-4-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":9,"Contrôle":5.5,"Confort":5.5,"Spin":6,"Maniabilité":6.5,"Tolérance":5.5},"verdict":"Diamant puissante inspirée d\'Ale Galán. Frappe explosive mais demande de la technique. Pour intermédiaires confirmés à l\'aise en attaque.","editorial":"La Metalbone 2025 version intermédiaire est la porte d\'entrée de la gamme mythique. Carbone standard et EVA standard en diamant — on retrouve l\'ADN offensif Metalbone dans un format accessible. Moins rigide que les versions expert, mais le nom Metalbone et la forme diamant posent les bases d\'un jeu offensif en développement.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone d\'entrée de gamme — première découverte de la rigidité Metalbone"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour un toucher accessible"},{"label":"Forme","value":"Diamant","detail":"L\'ADN offensif Metalbone — sweet spot haut"}],"targetProfile":"Joueurs intermédiaires offensifs. La première Metalbone pour apprendre le jeu d\'attaque."},{"id":"adidas-metalbone-hrd-2025","name":"Adidas Metalbone HRD 3.4 2025","shortName":"Metalbone HRD","brand":"Adidas","shape":"Diamant","weight":"365-380g","balance":"Haut","surface":"Carbon 18K","core":"Hard EVA","antivib":"—","price":"280-380€","player":"Ale Galán","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-3-4-hrd-2025.png","year":2025,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":8,"Maniabilité":5,"Tolérance":5},"verdict":"La plus puissante du marché. Carbone 18K + EVA dure + balance haute = frappe dévastatrice. Attention : confort très limité, bras sensibles s\'abstenir.","editorial":"La Metalbone HRD 2025 est la version précédente de l\'arme fatale d\'Adidas. Carbone 18K et mousse Hard EVA pour une puissance sans filtre. Moins de technologie que la 2026 (pas de Weight & Balance System), mais le même ADN radical : diamant, rigide, puissant. Pour ceux qui veulent la Metalbone HRD à prix réduit maintenant que la 2026 est sortie — un excellent plan.","techHighlights":[{"label":"Surface","value":"Carbon 18K","detail":"Carbone haute densité — rigide et puissant, le standard HRD avant le passage au 24K"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse ultra-dense — sortie de balle maximale, confort minimal"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — la signature de la gamme Metalbone"},{"label":"Sweet spot","value":"Haut et exigeant","detail":"Zone de frappe réduite mais explosive quand elle est trouvée"}],"targetProfile":"Joueurs experts offensifs. Alternative économique à la HRD 2026 avec 90% des performances."},{"id":"adidas-metalbone-youth-2025","name":"Adidas Metalbone Youth 3.4 2025","shortName":"Metalbone Youth","brand":"Adidas","shape":"Diamant","weight":"330-345g","balance":"Mi-haut","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"89-115€","player":"Ale Galán","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-youth-3-4-2025.png","year":2025,"category":"junior","scores":{"Puissance":8.5,"Contrôle":6,"Confort":8,"Spin":6,"Maniabilité":9,"Tolérance":5.5},"verdict":"Version junior de la Metalbone avec forme diamant pour une puissance maximale. Attention : tolérance limitée, convient mieux aux jeunes joueurs déjà à l\'aise techniquement.","editorial":"La Metalbone Youth 2025 est la junior offensive d\'Adidas. Le nom Metalbone sur une pala junior ! Fibre de verre et Soft EVA en diamant — l\'esprit offensif pour les jeunes compétiteurs. Pour les juniors qui veulent l\'image Metalbone.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — adapté aux jeunes"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection articulaire"},{"label":"Forme","value":"Diamant","detail":"L\'esprit offensif Metalbone en junior"}],"targetProfile":"Juniors offensifs. Le prestige Metalbone pour les jeunes compétiteurs."},{"id":"adidas-arrow-hit-2026","name":"Adidas Arrow Hit 2026","shortName":"Arrow Hit 26","brand":"Adidas","shape":"Diamant","weight":"360-375g","balance":"Moyen-Haut","surface":"Carbon Aluminized","core":"EVA Soft Performance","antivib":"IBS (Intelligent Balance System)","price":"250-310€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-arrow-hit-2026.png","year":2026,"category":"avance","scores":{"Puissance":8.5,"Contrôle":6,"Confort":6.5,"Spin":7,"Maniabilité":6.5,"Tolérance":5.5},"verdict":"Nouvelle gamme Adidas avec balance intelligente ajustable (IBS). Diamant polyvalent et adaptable. Pour joueurs avancés qui veulent personnaliser leur jeu.","editorial":"L\'Arrow Hit 2026 est la nouvelle gamme d\'Adidas — un compromis entre la puissance Metalbone et le contrôle Adipower. Carbone aluminisé en surface pour la réactivité, EVA Soft Performance pour le confort. Forme diamant pour l\'orientation offensive, mais un équilibre moins radical que la Metalbone. C\'est la pala de l\'attaquant intelligent qui veut de la puissance dosée.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé","detail":"Traitement aluminium pour plus de rigidité que le carbone brut"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Mousse confortable mais réactive — le meilleur compromis de la gamme Adidas"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive avec sweet spot haut"},{"label":"Technologie","value":"IBS + MPS ribs","detail":"Intelligent Balance System + nervures MPS pour la stabilité du cadre"}],"targetProfile":"Joueurs avancés offensifs qui veulent de la puissance avec plus de confort que la Metalbone."},{"name":"Adidas Arrow Hit Attack 2026","shortName":"Arrow Hit Atk 26","brand":"Adidas","shape":"Diamant","weight":"360g","balance":"Haute (270mm)","surface":"Carbon Aluminized 16K + IBS","core":"Power Foam","antivib":true,"price":"250-310€","player":"Avancé offensif","imageUrl":"https://www.padelful.com/images/rackets/adidas-arrow-hit-attack-2026.png","year":2026,"category":"avance","scores":{"Puissance":8.5,"Contrôle":6,"Confort":6,"Spin":7.5,"Maniabilité":6,"Tolérance":5.5},"verdict":"Nouvelle gamme Adidas 2026. IBS pour ajustement balance. Diamant 16K puissance.","id":"adidas-arrow-hit-attack-2026","editorial":"L\'Arrow Hit Attack est la version la plus offensive de la nouvelle gamme Arrow. Carbone aluminisé 16K avec IBS pour une frappe sèche et stable. Le Power Foam remplace ici l\'EVA Soft — plus de répondant, plus d\'explosivité. C\'est la passerelle entre l\'Arrow Hit standard et la Metalbone, pour les joueurs qui veulent monter en puissance progressivement.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 16K + IBS","detail":"16K aluminisé avec système IBS — rigidité contrôlée et stabilité au cadre"},{"label":"Noyau","value":"Power Foam","detail":"Mousse haute densité — plus explosive que l\'EVA Soft Performance"},{"label":"Forme","value":"Diamant","detail":"Offensive assumée — le cran au-dessus de l\'Arrow Hit standard"},{"label":"Technologie","value":"IBS + MPS ribs","detail":"Double système de stabilité qui compense la rigidité de la surface"}],"targetProfile":"Joueurs avancés offensifs en progression vers l\'expert. Le tremplin vers la gamme Metalbone."},{"id":"adidas-arrow-hit-ctrl-2026","name":"Adidas Arrow Hit CTRL 2026","shortName":"Arrow Hit CTRL 26","brand":"Adidas","shape":"Ronde","weight":"360-370g","balance":"Moyen","surface":"Carbon Aluminized","core":"EVA Soft Performance","antivib":"IBS + MPS ribs","price":"250-310€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-arrow-hit-ctrl-2026.png","year":2026,"category":"avance","scores":{"Puissance":6.5,"Contrôle":9,"Confort":7.5,"Spin":7,"Maniabilité":7.5,"Tolérance":8},"verdict":"Arrow Hit version contrôle ronde. IBS et nervures MPS pour stabilité maximale. Contact net et prévisible pour placement chirurgical.","editorial":"L\'Arrow Hit CTRL est la version contrôle de la gamme Arrow — forme ronde, carbone aluminisé, mousse EVA Soft Performance. L\'IBS et les nervures MPS stabilisent le cadre pour une réponse prévisible. C\'est la sœur défensive de l\'Arrow Hit, pour les joueurs qui veulent la technologie de la gamme Arrow au service du placement plutôt que de la puissance.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé","detail":"Même traitement aluminium que les versions offensives — rigidité au service du contrôle"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Confort et toucher pour le jeu de volée et de placement"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré — maximum de tolérance pour le joueur défensif"},{"label":"Technologie","value":"IBS + MPS ribs","detail":"Stabilité du cadre pour des frappes régulières et prévisibles"}],"targetProfile":"Joueurs avancés défensifs ou polyvalents. La version contrôle pour ceux qui aiment la gamme Arrow."},{"id":"adidas-arrow-hit-carbon-ctrl-2026","name":"Adidas Arrow Hit Carbon CTRL 2026","shortName":"Arrow Hit C.CTRL 26","brand":"Adidas","shape":"Ronde","weight":"360-370g","balance":"Moyen","surface":"Carbon 24K","core":"EVA Performance","antivib":"IBS + MPS ribs","price":"280-350€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-arrow-hit-carbon-ctrl-2026.png","year":2026,"category":"expert","scores":{"Puissance":7,"Contrôle":9.5,"Confort":7,"Spin":7.5,"Maniabilité":7,"Tolérance":7.5},"verdict":"Contrôle expert en full carbon 24K ronde. Structure extrêmement rigide et stable. Pour joueurs défensifs de haut niveau qui veulent placement millimétré.","editorial":"L\'Arrow Hit Carbon CTRL est le sommet de la gamme Arrow en mode contrôle. Carbone 24K — le même que sur la Metalbone HRD — mais en forme ronde. Le résultat est une pala de contrôle inhabituellement rigide : chaque volée claque, chaque contre est précis. L\'EVA Performance offre du répondant sans sacrifier le toucher. Pour les experts qui veulent dominer les échanges avec autorité.","techHighlights":[{"label":"Surface","value":"Carbon 24K","detail":"Le carbone le plus dense d\'Adidas — rigidité rare sur une forme ronde"},{"label":"Noyau","value":"EVA Performance","detail":"Mousse réactive — plus de répondant que l\'EVA Soft, bon compromis expert"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré pour le contrôle — combiné au 24K pour une rigidité exceptionnelle en ronde"},{"label":"Technologie","value":"IBS + MPS ribs","detail":"Stabilité structurelle essentielle avec cette rigidité de surface"}],"targetProfile":"Joueurs experts de contrôle exigeants. Pour ceux qui trouvent les rondes classiques trop souples."},{"id":"adidas-arrow-hit-jr-pink-2026","name":"Adidas Arrow Hit Junior Pink 2026","shortName":"Arrow Hit Jr 26","brand":"Adidas","shape":"Ronde","weight":"310-330g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"100-115€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/adidas-arrow-hit-junior-pink-2026.png","year":2026,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":9,"Tolérance":9},"verdict":"Raquette junior Adidas avec construction fibre de verre et mousse souple. Forme ronde pour un maximum de tolérance et de contrôle.","editorial":"L\'Arrow Hit Jr Pink 2026 est la junior de la nouvelle gamme Arrow. Fibre de verre et Soft EVA en ronde. Design coloré et attractif pour les jeunes joueuses. Construction légère et confortable.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — léger et confortable"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection junior"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale pour les jeunes"}],"targetProfile":"Juniors. L\'Arrow Hit en version jeune et colorée."},{"name":"Adidas Cross It Light 2026","shortName":"Cross It Light 26","brand":"Adidas","shape":"Ronde","weight":"355g","balance":"Moyenne-haute (268mm)","surface":"Carbon Aluminized 24K + DAF","core":"Power Foam","antivib":true,"price":"260-320€","player":"Avancé offensif rapide","imageUrl":"https://www.padelful.com/images/rackets/adidas-cross-it-light-2026-martita-ortega.png","year":2026,"category":"avance","scores":{"Puissance":6,"Contrôle":8.5,"Confort":7.5,"Spin":7.5,"Maniabilité":8,"Tolérance":8},"verdict":"Raquette Martita Ortega. Dynamic Air Flow + 24K. Légère et maniable.","id":"adidas-cross-it-light-2026","editorial":"La Cross It Light 2026 est la pala rapide d\'Adidas. Carbone aluminisé 24K avec DAF (Dynamic Air Flow) — des canaux d\'air dans le cadre qui réduisent la résistance et améliorent la maniabilité. Le Power Foam propulse la balle, la forme ronde offre de la tolérance. C\'est la pala de l\'attaquant léger qui mise sur la vitesse de bras plutôt que la masse.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 24K + DAF","detail":"Carbone haute densité avec Dynamic Air Flow — canaux d\'air pour la légèreté"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour compenser le poids réduit — frappe dynamique"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale — sécurité pour le jeu rapide où la précision est moindre"},{"label":"Poids","value":"Light","detail":"Version allégée pour une vitesse de bras maximale au filet"}],"targetProfile":"Joueurs avancés offensifs rapides. Parfaite pour les joueurs qui privilégient la vitesse et les réflexes."},{"name":"Adidas Cross It Team 2026","shortName":"Cross It Team 26","brand":"Adidas","shape":"Diamant","weight":"360g","balance":"Haute (270mm)","surface":"Fibre de verre + DAF","core":"Power Foam","antivib":true,"price":"140-180€","player":"Intermédiaire","imageUrl":"https://www.padelful.com/images/rackets/adidas-cross-it-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":6.5,"Confort":7.5,"Spin":7,"Maniabilité":6,"Tolérance":6},"verdict":"Cross It accessible. Diamant oversize + DAF. Bonne all-rounder intermédiaire.","id":"adidas-cross-it-team-2026","editorial":"La Cross It Team 2026 est l\'intermédiaire de la nouvelle gamme Cross It. Fibre de verre avec DAF (Dynamic Air Flow) en diamant — orientation offensive accessible. Le Power Foam apporte plus de réactivité que l\'EVA standard. C\'est la passerelle vers la Cross It Light carbone.","techHighlights":[{"label":"Surface","value":"Fibre de verre + DAF","detail":"Fibre de verre avec canaux d\'air — légèreté et maniabilité"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive — plus de punch que l\'EVA standard"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive accessible"}],"targetProfile":"Joueurs intermédiaires offensifs. L\'initiation à la gamme Cross It."},{"id":"adidas-cross-it-team-light-2026","name":"Adidas Cross It Team Light 2026","shortName":"Cross It TL 26","brand":"Adidas","shape":"Hybride","weight":"340-355g","balance":"Moyen","surface":"Carbon Aluminized","core":"EVA Soft Performance","antivib":"Dynamic Air Flow","price":"150-200€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-cross-it-team-light-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8.5,"Spin":7,"Maniabilité":8.5,"Tolérance":8},"verdict":"Hybride légère orientée contrôle. Dynamic Air Flow pour rapidité et maniabilité. Confort et tolérance pour intermédiaires en progression.","editorial":"La Cross It Team Light 2026 est la version légère et hybride de la gamme Cross It. Carbone aluminisé — un cran au-dessus de la fibre de verre — avec EVA Soft Performance pour le confort. Forme hybride pour la polyvalence. La version la plus équilibrée de la gamme Cross It, parfaite pour progresser en douceur.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé","detail":"Traitement aluminium — plus rigide que la fibre de verre de la Team"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Mousse confortable Adidas — progression sans douleur"},{"label":"Forme","value":"Hybride","detail":"Polyvalence — entre ronde et goutte"}],"targetProfile":"Joueurs intermédiaires polyvalents. Transition en douceur vers le carbone."},{"id":"adidas-drive-black-2026","name":"Adidas Drive Black 2026","shortName":"Drive Black 26","brand":"Adidas","shape":"Ronde","weight":"340-355g","balance":"Moyen","surface":"Fiberglass","core":"EVA Soft Performance","antivib":"Standard","price":"50-80€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-drive-black-2026.png","year":2026,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8,"Confort":9,"Spin":5,"Maniabilité":8.5,"Tolérance":9},"verdict":"Entrée de gamme Adidas. Ronde confortable et légère pour découvrir le padel. Fiberglass et EVA Soft pour maximum de tolérance sans effort.","editorial":"La Drive Black 2026 est la débutante de la nouvelle gamme Adidas. Fibre de verre et EVA Soft Performance en ronde. Un cran au-dessus de l\'Adipower Light grâce à la mousse EVA Soft Performance — plus de répondant tout en restant confortable. Pour les débutants qui veulent un peu plus de mordant.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — confort et tolérance"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Mousse légèrement plus réactive que le Soft EVA classique"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale pour l\'apprentissage"}],"targetProfile":"Débutants. L\'Adidas moderne pour commencer avec un peu de répondant."},{"id":"adidas-match-light-2026","name":"Adidas Match Light 2026","shortName":"Match Light 26","brand":"Adidas","shape":"Ronde","weight":"330-345g","balance":"Moyen","surface":"Fiberglass","core":"EVA Soft","antivib":"Standard","price":"40-70€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-match-light-2026.png","year":2026,"category":"debutant","scores":{"Puissance":5,"Contrôle":8,"Confort":9.5,"Spin":4.5,"Maniabilité":9.5,"Tolérance":9.5},"verdict":"La plus légère et accessible d\'Adidas. Parfaite pour premières sessions. Confort maximal, aucune exigence technique.","editorial":"La Match Light 2026 est la pala la plus accessible d\'Adidas. Fibre de verre et EVA Soft en ronde — construction ultra-légère et confortable. Le prix le plus bas de la gamme Adidas. C\'est la pala pour tester le padel sans investissement important.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le plus accessible"},{"label":"Noyau","value":"EVA Soft","detail":"Mousse très souple — confort absolu"},{"label":"Forme","value":"Ronde","detail":"Tolérance — idéale pour les premiers coups"}],"targetProfile":"Débutants et primo-accédants. Le padel au meilleur prix chez Adidas."},{"name":"Adidas Metalbone 2026","shortName":"Metalbone 26","brand":"Adidas","shape":"Diamant","weight":"355g","balance":"Haute (270mm)","surface":"Carbon Aluminized 16K + Spin Blade","core":"Power Foam","antivib":true,"price":"300-380€","player":"Expert offensif customisable","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-2026-ale-galan.png","year":2026,"category":"expert","scores":{"Puissance":8.5,"Contrôle":6,"Confort":5.5,"Spin":8,"Maniabilité":6,"Tolérance":5.5},"verdict":"Flagship Adidas Ale Galán. Weight & Balance System pour personnalisation totale.","id":"adidas-metalbone-2026","editorial":"La Metalbone 2026 est le cœur de la gamme Adidas — la version standard entre la HRD (extrême) et la Team (accessible). Carbone aluminisé 16K avec Spin Blade pour des effets accrocheurs, Power Foam pour l\'explosivité. C\'est probablement la Metalbone la plus équilibrée : suffisamment puissante pour finir les points, suffisamment tolérante pour construire. L\'idéal pour la majorité des joueurs offensifs.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 16K + Spin Blade","detail":"16K aluminisé avec texture Spin Blade — puissance ET effets dans la même surface"},{"label":"Noyau","value":"Power Foam","detail":"Mousse dense et réactive — bon retour d\'énergie sans la brutalité du Hard EVA"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — l\'ADN Metalbone"},{"label":"Technologie","value":"Spin Blade","detail":"Texture de surface qui accroche la balle pour plus de rotation au smash et au bandeja"}],"targetProfile":"Joueurs experts polyvalents-offensifs. La Metalbone de référence pour ceux qui ne veulent pas l\'extrémisme de la HRD."},{"id":"adidas-metalbone-ctrl-2026","name":"Adidas Metalbone CTRL 2026","shortName":"Metalbone CTRL 26","brand":"Adidas","shape":"Ronde","weight":"355-370g","balance":"Moyen","surface":"Carbon Aluminized 16K","core":"EVA Soft Performance","antivib":"Weight & Balance System","price":"300-370€","player":"Lorena Rufo","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-ctrl-2026.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":9,"Confort":7.5,"Spin":7.5,"Maniabilité":7.5,"Tolérance":8},"verdict":"Metalbone version contrôle ronde. Même qualité de construction avec focus sur précision et placement. Weight system personnalisable. Pour joueurs tactiques.","editorial":"La Metalbone CTRL 2026 incarne le parfait équilibre entre puissance et contrôle chez Adidas. Son carbone aluminisé 16K offre une rigidité maîtrisée qui canalise la frappe sans sacrifier le toucher de balle. La mousse EVA Soft Performance absorbe les vibrations tout en restituant assez d\'énergie pour des contres incisifs. C\'est la pala des stratèges qui veulent dicter le jeu depuis le fond du court.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 16K","detail":"Rigidité optimisée pour un retour d\'énergie maîtrisé sans perte de toucher"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Mousse à densité intermédiaire : confort au bandeja, répondant au smash"},{"label":"Technologie","value":"Weight & Balance System","detail":"Système de lests intégrés permettant d\'ajuster l\'équilibre selon votre style"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré et élargi, tolérance maximale sur les frappes décentrées"},{"label":"Sweet spot","value":"Large et centré","detail":"La forme ronde combinée au carbone 16K offre une zone de frappe très tolérante"}],"targetProfile":"Joueurs avancés à experts défensifs ou polyvalents. Idéale pour le joueur de gauche qui aime contrer et placer, ou le joueur de droite qui veut sécuriser ses volées tout en gardant du répondant.","proPlayerInfo":{"name":"Lorena Rufo","context":"Joueuse WPT connue pour son jeu de contrôle et sa régularité depuis le fond du court."}},{"name":"Adidas Metalbone Carbon 2026","shortName":"Metalbone Carbon 26","brand":"Adidas","shape":"Diamant","weight":"365g","balance":"Haute (270mm)","surface":"Carbone 6K + Spin Blade","core":"Power Foam","antivib":true,"price":"250-310€","player":"Avancé offensif","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-carbon-2026.png","year":2026,"category":"avance","scores":{"Puissance":9,"Contrôle":5.5,"Confort":5,"Spin":8,"Maniabilité":5.5,"Tolérance":5},"verdict":"Metalbone 6K pour maniabilité accrue. Bon compromis puissance-vitesse.","id":"adidas-metalbone-carbon-2026","editorial":"La Metalbone Carbon 2026 est le premier échelon carbone de la gamme Metalbone. Carbone 6K avec Spin Blade — moins rigide que le 16K ou le 24K, mais suffisamment nerveux pour sentir la différence avec la fibre de verre. Le Power Foam assure l\'explosivité. C\'est la porte d\'entrée pour découvrir la sensation Metalbone sans l\'exigence technique des versions haut de gamme.","techHighlights":[{"label":"Surface","value":"Carbone 6K + Spin Blade","detail":"Carbone d\'entrée de gamme avec texture Spin Blade — rigidité modérée, bons effets"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour compenser la rigidité moindre du 6K"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive maintenue sur toute la gamme Metalbone"},{"label":"Spin","value":"Spin Blade","detail":"Texture de surface pour l\'accroche — même technologie que les versions premium"}],"targetProfile":"Joueurs avancés qui veulent leur première Metalbone carbone. Transition parfaite depuis la Metalbone Team."},{"id":"adidas-metalbone-carbon-ctrl-2026","name":"Adidas Metalbone Carbon CTRL 2026","shortName":"Metalbone C.CTRL 26","brand":"Adidas","shape":"Ronde","weight":"355-370g","balance":"Moyen","surface":"Carbon Aluminized 24K","core":"EVA Soft Performance","antivib":"Weight & Balance System","price":"320-390€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-carbon-ctrl-2026.png","year":2026,"category":"expert","scores":{"Puissance":7,"Contrôle":9.5,"Confort":7,"Spin":8,"Maniabilité":7,"Tolérance":7.5},"verdict":"Contrôle premium en 24K Carbon ronde. Ultra-stable et précise. Pour experts défensifs exigeants qui veulent construction rigide et sensation sèche.","editorial":"La Metalbone Carbon CTRL 2026 est la version la plus extrême de la gamme contrôle Adidas. Le carbone aluminisé 24K — le même que sur la HRD — est ici associé à une mousse EVA Soft Performance et une forme ronde. Le résultat est unique : une rigidité de surface digne d\'une pala de puissance, combinée à un sweet spot centré et tolérant. Le Weight & Balance System permet de basculer entre un setup plus défensif ou plus offensif. L\'excellence du contrôle.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 24K","detail":"Le carbone le plus dense d\'Adidas — habituellement réservé aux palas de puissance, ici au service du contrôle"},{"label":"Noyau","value":"EVA Soft Performance","detail":"Mousse douce mais réactive — absorbe les chocs tout en conservant du répondant"},{"label":"Technologie","value":"Weight & Balance System","detail":"Lests ajustables pour personnaliser l\'équilibre — unique en ronde sur le marché"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré pour le contrôle maximal — associé au 24K pour de la rigidité rare en ronde"},{"label":"Sweet spot","value":"Centré mais rigide","detail":"Combinaison rare : tolérance de la ronde avec le retour d\'énergie du carbone 24K"}],"targetProfile":"Joueurs experts de contrôle exigeants. Pour ceux qui trouvent les rondes classiques trop molles et veulent de la rigidité sans sacrifier la tolérance de la forme ronde.","proPlayerInfo":null},{"name":"Adidas Metalbone HRD 2026","shortName":"Metalbone HRD 26","brand":"Adidas","shape":"Diamant","weight":"360g","balance":"Haute (272mm)","surface":"Carbon Aluminized 16K","core":"X-EVA hard","antivib":false,"price":"320-400€","player":"Expert puissance dure","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-hrd-2024.png","year":2026,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":8.5,"Maniabilité":5,"Tolérance":5},"verdict":"Metalbone hardcore. EVA Hard pour sortie de balle max. Puissance brute.","id":"adidas-metalbone-hrd-2026","editorial":"La Metalbone HRD 2026 est l\'arme absolue d\'Adidas — la plus radicale de la gamme. HRD pour \'Hard\', et le nom ne ment pas : le carbone 24K aluminisé, le plus dense jamais utilisé sur une pala, ne fléchit pas. Chaque frappe est transmise intégralement à la balle, sans filtre. Le noyau Hard EVA confirme le parti-pris : zéro confort, 100% sortie de balle. Le Weight & Balance System permet d\'ajuster les lests, seule concession à la personnalisation. C\'est une pala de gladiateur.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 24K","detail":"Le carbone le plus dense du marché — 24 000 filaments par mèche, traitement aluminium pour la rigidité absolue"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse ultra-dense — aucune absorption, toute l\'énergie est retransmise à la balle. Le plus dur du catalogue Adidas"},{"label":"Technologie","value":"Weight & Balance System","detail":"Système de lests amovibles dans le cadre — personnalisez l\'équilibre selon votre style de jeu"},{"label":"Forme","value":"Diamant","detail":"Masse concentrée en tête — effet de levier maximal pour les smashs"},{"label":"Sweet spot","value":"Haut et exigeant","detail":"Zone de frappe réduite mais dévastatrice quand elle est trouvée. Pour les joueurs précis uniquement"}],"targetProfile":"Joueurs experts offensifs purs avec un physique solide. Absolument déconseillée aux joueurs avec des douleurs articulaires. Réservée aux compétiteurs qui privilégient la puissance avant tout.","proPlayerInfo":{"name":"Ale Galán","context":"N°2 mondial, l\'un des attaquants les plus redoutés du circuit. Sa puissance au smash avec la Metalbone est devenue sa signature."}},{"name":"Adidas Metalbone Team 2026","shortName":"Metalbone Team 26","brand":"Adidas","shape":"Diamant","weight":"365g","balance":"Haute (270mm)","surface":"Fibre de verre + Spin Blade Gritt","core":"Power Foam","antivib":true,"price":"150-200€","player":"Intermédiaire offensif","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":7,"Spin":7.5,"Maniabilité":7,"Tolérance":6.5},"verdict":"Metalbone accessible. Fibre de verre pour tolérance, diamant pour puissance.","id":"adidas-metalbone-team-2026","editorial":"La Metalbone Team 2026 est la Metalbone intermédiaire. Fibre de verre avec Spin Blade Gritt — la texture d\'accroche Metalbone sur un matériau accessible — et Power Foam. Forme diamant pour garder l\'ADN offensif. C\'est la Metalbone pour ceux qui veulent le style et la philosophie sans l\'exigence du carbone.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Spin Blade Gritt","detail":"Fibre de verre avec texture de surface — effets accessibles"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive pour plus de punch"},{"label":"Forme","value":"Diamant","detail":"L\'ADN Metalbone — offensive même en intermédiaire"}],"targetProfile":"Joueurs intermédiaires offensifs. La Metalbone accessible avec les effets Spin Blade."},{"name":"Adidas Metalbone Team Light 2026","shortName":"Metalbone TL 26","brand":"Adidas","shape":"Diamant","weight":"350g","balance":"Moyenne-haute (268mm)","surface":"Fibre de verre","core":"Power Foam","antivib":true,"price":"130-170€","player":"Intermédiaire léger","imageUrl":"https://www.padelful.com/images/rackets/adidas-metalbone-team-light-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":8,"Spin":7,"Maniabilité":8.5,"Tolérance":6.5},"verdict":"Metalbone légère pour progression. Fibre de verre + EVA Soft pour confort.","id":"adidas-metalbone-team-light-2026","editorial":"La Metalbone Team Light 2026 est la version légère de la Metalbone Team. Fibre de verre et Power Foam en diamant, mais allégée. Sans la texture Spin Blade — construction plus simple. C\'est la Metalbone la plus accessible : légère, maniable, offensive. Idéale pour les joueurs intermédiaires légers ou les femmes qui veulent l\'esprit Metalbone.","techHighlights":[{"label":"Surface","value":"Fibre de verre","detail":"Construction légère et accessible"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive pour compenser la légèreté"},{"label":"Forme","value":"Diamant","detail":"L\'esprit Metalbone en version light"}],"targetProfile":"Joueurs intermédiaires légers. La Metalbone la plus maniable du catalogue."},{"id":"babolat-technical-viper-2024","name":"Babolat Technical Viper 2024","shortName":"Tech Viper 24","brand":"Babolat","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"Vibrasorb","price":"200-270€","player":"Juan Lebrón","imageUrl":"https://www.padelful.com/images/rackets/babolat-technical-viper-2024.png","year":2024,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Version 2024 de la Viper de Lebrón. Même qualité technique à prix déstocké. Excellent rapport qualité/prix pour joueurs avancés.","editorial":"La Technical Viper 2024 est un millésime qui a marqué Babolat — la pala qui a posé les bases de la gamme Viper actuelle. Carbone standard en surface, mousse EVA classique, forme goutte d\'eau. Pas de fioritures technologiques, mais un équilibre de construction qui a prouvé son efficacité. En 2024 c\'est une affaire si on la trouve en déstockage.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — la base éprouvée de Babolat"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse référence pour un toucher neutre et prévisible"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le choix sûr de Babolat pour ses modèles techniques"}],"targetProfile":"Joueurs avancés polyvalents. Excellent rapport qualité/prix en fin de série."},{"id":"babolat-air-viper-2025","name":"Babolat Air Viper 2025","shortName":"Air Viper 25","brand":"Babolat","shape":"Goutte d\'eau","weight":"355-365g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"170-220€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-air-viper-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Version allégée de la Viper avec un bon équilibre offensif/défensif. Le carbone et le balance mi-haut offrent une frappe plus directe.","editorial":"L\'Air Viper 2025 est l\'intermédiaire de la gamme Viper. Carbone standard et EVA standard en goutte d\'eau. C\'est la Viper d\'initiation — même philosophie offensive que les versions avancées, dans un format plus accessible et tolérant. Bonne pala de progression pour découvrir l\'attaque Babolat.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — première approche du toucher carbone Viper"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour la progression"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le format Viper accessible"}],"targetProfile":"Joueurs intermédiaires en progression vers le jeu offensif. L\'entrée dans la gamme Viper."},{"id":"babolat-alioth-mini-jr-2025","name":"Babolat Alioth Mini Junior 2025","shortName":"Alioth Mini Jr 25","brand":"Babolat","shape":"Ronde","weight":"290g ±10g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"90-100€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-alioth-mini-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5,"Contrôle":9,"Confort":9,"Spin":6,"Maniabilité":10,"Tolérance":9},"verdict":"La plus légère du marché junior (290g), forme ronde très tolérante. Parfaite pour les enfants de 7-10 ans ou petits gabarits qui découvrent le padel.","editorial":"L\'Alioth Mini Jr 2025 est la pala pour les plus petits chez Babolat. Fibre de verre et Soft EVA en ronde, format mini. Conçue pour les enfants de 4-8 ans qui découvrent le padel. Ultra-légère et facile à manier.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — zéro vibration"},{"label":"Forme","value":"Ronde","detail":"Format mini pour les plus petits"}],"targetProfile":"Juniors 4-8 ans. La première pala Babolat pour les tout-petits."},{"id":"babolat-alioth-pro-jr-2025","name":"Babolat Alioth Pro Junior 2025","shortName":"Alioth Pro Jr 25","brand":"Babolat","shape":"Goutte d\'eau","weight":"330g ±10g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"140-150€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-alioth-pro-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8.5,"Spin":6,"Maniabilité":9,"Tolérance":7.5},"verdict":"Raquette junior intermédiaire avec forme goutte d\'eau pour un peu plus de puissance. Bon compromis entre facilité et performance pour les jeunes en progression.","editorial":"L\'Alioth Pro Jr 2025 est la junior polyvalente de Babolat. Fibre de verre et Soft EVA en goutte d\'eau — un format légèrement plus offensif que la Mini. Pour les juniors de 8-14 ans qui commencent à développer un vrai jeu.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — qualité Babolat junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort pour les jeunes articulations"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — pour les juniors qui développent leur jeu"}],"targetProfile":"Juniors 8-14 ans. La Babolat de progression pour les jeunes joueurs."},{"id":"babolat-contact-2025","name":"Babolat Contact 2025","shortName":"Contact 25","brand":"Babolat","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-80€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-contact-2023.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Entrée de gamme Babolat, forme ronde très tolérante. Construction simple en fibre de verre pour débuter à petit prix.","editorial":"La Contact 2025 est la porte d\'entrée Babolat. Fibre de verre et Soft EVA en ronde — la formule universelle du débutant. Babolat y met son savoir-faire de construction issu du tennis. C\'est une pala d\'apprentissage fiable avec la qualité Babolat.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux Babolat"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort d\'apprentissage"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale — qualité Babolat"}],"targetProfile":"Débutants. La qualité Babolat pour apprendre."},{"id":"babolat-counter-viper-2025","name":"Babolat Counter Viper 2025","shortName":"Counter Viper","brand":"Babolat","shape":"Goutte d\'eau","weight":"360-370g","balance":"Moyen","surface":"Carbon","core":"Standard EVA","antivib":"Vibrasorb","price":"150-200€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-counter-viper-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6.5,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Goutte d\'eau polyvalente avec système anti-vibrations Vibrasorb. Bonne répartition des performances pour progresser sereinement.","editorial":"La Counter Viper 2025 est l\'intermédiaire de la gamme défensive Babolat. Carbone standard et EVA standard en goutte d\'eau. Construction solide pour développer un jeu de contrôle et de contre. Moins technique que la Counter Viper 2026, mais un bon tremplin pour apprendre le jeu tactique.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher neutre pour le contrôle"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence défensive — apprendre à contrer et placer"}],"targetProfile":"Joueurs intermédiaires défensifs. L\'apprentissage du contre et du placement."},{"id":"babolat-rebel-2025","name":"Babolat Rebel 2025","shortName":"Rebel 25","brand":"Babolat","shape":"Diamant","weight":"370-385g","balance":"Haut","surface":"Carbon 18K","core":"Hard EVA","antivib":"—","price":"300-400€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-rebel-2025.png","year":2025,"category":"expert","scores":{"Puissance":9,"Contrôle":5.5,"Confort":5,"Spin":7.5,"Maniabilité":5.5,"Tolérance":5},"verdict":"Diamant extrême Babolat. Carbone 18K brut pour une puissance maximale absolue. Zéro concession sur le confort.","editorial":"La Rebel 2025 est la pala la plus agressive de Babolat pour 2025. Carbone 18K en surface — haute rigidité — couplé à une mousse Hard EVA sans concession. Forme diamant radicale. C\'est la Babolat pour les frappeurs purs qui veulent de la sortie de balle brute. Moins sophistiquée que la Viper 3.0 Lebrón 2026, mais tout aussi efficace quand il s\'agit de claquer des smashs.","techHighlights":[{"label":"Surface","value":"Carbon 18K","detail":"Carbone haute densité — rigidité élevée pour les frappes puissantes"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — zéro absorption, sortie de balle maximale"},{"label":"Forme","value":"Diamant","detail":"Puissance pure en tête — pour les finisseurs"},{"label":"Sweet spot","value":"Haut et concentré","detail":"Zone de frappe exigeante qui récompense la précision"}],"targetProfile":"Joueurs experts offensifs purs. La Babolat la plus radicale du catalogue 2025."},{"id":"babolat-revenge-2025","name":"Babolat Revenge 2025","shortName":"Revenge 25","brand":"Babolat","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"70-90€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-revenge-2025.png","year":2025,"category":"debutant","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Goutte d\'eau accessible avec fibre de verre pour un bon confort. Premier pas vers une raquette plus polyvalente.","editorial":"La Revenge 2025 est la débutante polyvalente de Babolat. Fibre de verre et Soft EVA en goutte d\'eau — un format légèrement plus offensif que la Contact ronde. Pour les débutants qui veulent commencer avec un peu plus de punch.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher accessible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple pour le confort"},{"label":"Forme","value":"Goutte d\'eau","detail":"Un peu plus de punch que la ronde — pour les débutants ambitieux"}],"targetProfile":"Débutants dynamiques. La Babolat pour commencer avec un soupçon d\'attaque."},{"id":"babolat-viper-2025","name":"Babolat Technical Viper 2025","shortName":"Tech Viper 25","brand":"Babolat","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"Vibrasorb","price":"250-320€","player":"Juan Lebrón","imageUrl":"https://www.padelful.com/images/rackets/babolat-technical-viper-2025.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Signature Juan Lebrón. Goutte d\'eau technique avec surface texturée pour le spin. Le Vibrasorb atténue les vibrations du carbone.","editorial":"La Viper 2025 est la version standard de la gamme offensive Babolat. Carbone en surface, EVA standard, forme goutte d\'eau. Moins extrême que la Rebel, plus accessible que la Viper Lebrón. C\'est la Viper d\'entrée pour découvrir l\'ADN offensif de Babolat sans engagement radical. Bonne option pour un joueur avancé qui explore le jeu d\'attaque.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — bonne rigidité de base pour l\'attaque"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre — compromis entre confort et réactivité"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalente — offensive mais pas radicale"}],"targetProfile":"Joueurs avancés en transition vers le jeu offensif. La Viper pour commencer."},{"id":"babolat-viper-air-2025","name":"Babolat Viper Air 2025","shortName":"Viper Air 25","brand":"Babolat","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"220-280€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/babolat-air-viper-2025.png","year":2025,"category":"avance","scores":{"Puissance":9,"Contrôle":5.5,"Confort":5.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":5},"verdict":"Diamant offensive Babolat. Puissance brute avec carbone et balance haute. Pour les joueurs avancés qui dominent au filet.","editorial":"La Viper Air 2025 est la version aérodynamique de la Viper — même carbone, même EVA, mais en diamant et avec une construction optimisée pour la légèreté. Le gain en vitesse de bras se ressent immédiatement au filet. C\'est la Viper pour ceux qui veulent attaquer vite plutôt que fort.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — rigidité de base pour l\'attaque"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre, polyvalente"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — plus offensive que la Viper goutte"},{"label":"Construction","value":"Air (allégée)","detail":"Gain de poids pour la vitesse de bras"}],"targetProfile":"Joueurs avancés offensifs rapides. Version légère et diamant pour l\'attaque explosive."},{"name":"Babolat Air Veron 2026","shortName":"Air Veron 26","brand":"Babolat","shape":"Goutte d\'eau","weight":"355g","balance":"Moyenne (262mm)","surface":"CarbonFlex hybrid","core":"Power Foam","antivib":true,"price":"220-280€","player":"Intermédiaire-Avancé polyvalent","imageUrl":"https://www.padelful.com/images/rackets/babolat-air-veron-2025.png","year":2026,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":8,"Spin":7,"Maniabilité":8,"Tolérance":7.5},"verdict":"Polyvalente dynamique. CarbonFlex pour puissance facile. Excellente all-rounder.","id":"babolat-air-veron-2026","editorial":"L\'Air Veron 2026 est l\'intermédiaire de la nouvelle gamme Babolat. CarbonFlex Hybrid — le composite propriétaire dans sa version accessible — avec Power Foam en goutte d\'eau. Plus sophistiquée technologiquement que l\'Air Viper 2025. C\'est la pala de progression idéale pour accéder aux technologies Babolat modernes.","techHighlights":[{"label":"Surface","value":"CarbonFlex Hybrid","detail":"Composite propriétaire Babolat — flex accessible et toucher agréable"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive — plus de punch que l\'EVA standard"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — format de progression"}],"targetProfile":"Joueurs intermédiaires polyvalents. La technologie CarbonFlex Babolat accessible."},{"name":"Babolat Air Viper 2026","shortName":"Air Viper 26","brand":"Babolat","shape":"Goutte d\'eau","weight":"355g","balance":"Moyenne-haute (268mm)","surface":"Carbone 16K","core":"X-EVA hard","antivib":true,"price":"250-310€","player":"Avancé offensif rapide","imageUrl":"https://www.padelful.com/images/rackets/babolat-air-viper-2025.png","year":2026,"category":"avance","scores":{"Puissance":8,"Contrôle":7,"Maniabilité":7,"Confort":7,"Spin":7.5,"Tolérance":7.5},"verdict":"Viper aérodynamique. 16K + X-EVA pour réactivité. Transitions filet-fond rapides.","id":"babolat-air-viper-2026","editorial":"L\'Air Viper 2026 est la version aérienne de la gamme Viper de Babolat. Le carbone 16K offre un bon niveau de rigidité, et le X-EVA Hard propulse la balle avec autorité. Le \'Air\' du nom vient de sa construction optimisée pour la légèreté : même puissance que la Viper classique, mais un maniement plus rapide au filet. Pour les attaquants qui veulent de la vitesse de bras sans renoncer à la percussion au smash.","techHighlights":[{"label":"Surface","value":"Carbone 16K","detail":"Carbone à densité intermédiaire-haute — bon compromis rigidité/poids"},{"label":"Noyau","value":"X-EVA Hard","detail":"La mousse la plus dure de Babolat — retour d\'énergie maximal, toucher sec"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête pour les smashs — forme offensive assumée"},{"label":"Sweet spot","value":"Haut et dynamique","detail":"Zone de frappe haute, optimisée pour les coups au-dessus de la tête"},{"label":"Construction","value":"Air (légère)","detail":"Construction allégée par rapport à la Viper standard — gain en vitesse de bras"}],"targetProfile":"Joueurs avancés offensifs rapides. L\'attaquant qui mise sur la vitesse de bras et l\'accélération plutôt que sur la puissance brute par le poids.","proPlayerInfo":null},{"name":"Babolat Counter Veron 2026","shortName":"Counter Veron 26","brand":"Babolat","shape":"Ronde","weight":"365g","balance":"Haute (270mm)","surface":"CarbonFlex hybrid","core":"Power Foam","antivib":true,"price":"200-260€","player":"Intermédiaire-Avancé contrôle","imageUrl":"https://www.padelful.com/images/rackets/babolat-counter-veron-2024.png","year":2026,"category":"intermediaire","scores":{"Puissance":6.5,"Contrôle":9,"Confort":8,"Spin":7,"Maniabilité":7.5,"Tolérance":8.5},"verdict":"Contrôle accessible Babolat. CarbonFlex ronde pour tolérance maximale.","id":"babolat-counter-veron-2026","editorial":"La Counter Veron 2026 est la défensive intermédiaire de la nouvelle gamme. CarbonFlex Hybrid et Power Foam en ronde — orientation contrôle avec la technologie moderne Babolat. Le CarbonFlex offre un toucher plus fin que le carbone brut, idéal pour développer la précision des volées et des contres.","techHighlights":[{"label":"Surface","value":"CarbonFlex Hybrid","detail":"Composite flexible — toucher fin pour le placement"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive — du répondant même en ronde"},{"label":"Forme","value":"Ronde","detail":"Contrôle — sweet spot centré pour la défense"}],"targetProfile":"Joueurs intermédiaires défensifs. Le contrôle CarbonFlex en format ronde."},{"name":"Babolat Counter Viper 2026","shortName":"Counter Viper 26","brand":"Babolat","shape":"Ronde","weight":"365g","balance":"Haute (270mm)","surface":"Carbone 3K + Spin+","core":"Power Foam","antivib":true,"price":"230-290€","player":"Avancé défensif-contre","imageUrl":"https://www.padelful.com/images/rackets/babolat-counter-viper-2024.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":8.5,"Confort":7.5,"Spin":7.5,"Maniabilité":7.5,"Tolérance":8},"verdict":"Ronde offensive. 3K carbon pour précision + balance haute pour punch en contre.","id":"babolat-counter-viper-2026","editorial":"La Counter Viper 2026 est l\'anti-thèse de la Viper classique — même technologie, philosophie opposée. Le carbone 3K avec Spin+ en surface génère des effets redoutables sur les contres et les lobs, tandis que le Power Foam offre assez de répondant pour punir les balles hautes. C\'est la pala du contreur — celui qui retourne tout, qui lobe avec des effets impossibles, et qui attend patiemment que l\'adversaire craque.","techHighlights":[{"label":"Surface","value":"Carbone 3K + Spin+","detail":"Surface texturée Spin+ sur carbone 3K — accroche maximale pour les effets en défense"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive qui permet des contres puissants même depuis une position défensive"},{"label":"Forme","value":"Ronde","detail":"Maximum de contrôle et de tolérance — la forme naturelle du défenseur"},{"label":"Sweet spot","value":"Large et centré","detail":"Sweet spot ronde classique, élargi par la construction soignée de Babolat"},{"label":"Spin","value":"Spin+ Technology","detail":"Texture de surface qui génère jusqu\'à 20% plus de rotation qu\'une surface lisse"}],"targetProfile":"Joueurs avancés défensifs-contreurs. Le joueur de droite qui ne rate rien, qui lobe avec précision, et qui transforme chaque balle défensive en opportunité avec du spin.","proPlayerInfo":null},{"id":"babolat-technical-veron-2026","name":"Babolat Technical Veron 2026","shortName":"Technical Veron 26","brand":"Babolat","shape":"Diamant","weight":"360-370g","balance":"Moyen-Haut","surface":"CarbonFlex","core":"X-EVA","antivib":"Vibrabsorb","price":"250-310€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/babolat-technical-veron-2024.png","year":2026,"category":"avance","scores":{"Puissance":8,"Contrôle":7.5,"Confort":7,"Spin":7.5,"Maniabilité":6.5,"Tolérance":6.5},"verdict":"Diamant technique CarbonFlex. Plus tolérant que le Technical Viper avec bonne puissance offensive. Pour avancés qui veulent de l\'attaque avec du contrôle.","editorial":"La Technical Veron 2026 est la pala technique de la nouvelle gamme Babolat. CarbonFlex en surface — le composite propriétaire Babolat qui combine rigidité du carbone et flexibilité — avec noyau X-EVA pour un bon retour d\'énergie. Le Vibrabsorb filtre les vibrations. C\'est une pala de joueur technique qui veut du toucher et de la précision dans ses frappes.","techHighlights":[{"label":"Surface","value":"CarbonFlex","detail":"Composite propriétaire Babolat — flex contrôlé pour le toucher et le placement"},{"label":"Noyau","value":"X-EVA","detail":"Mousse haute performance Babolat — réactivité supérieure à l\'EVA standard"},{"label":"Technologie","value":"Vibrabsorb","detail":"Anti-vibrations Babolat pour le confort sur les frappes intenses"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive pour les frappes techniques"}],"targetProfile":"Joueurs avancés techniques qui veulent de la précision offensive. L\'option technique de la gamme Veron."},{"id":"babolat-technical-viper-2026","name":"Babolat Technical Viper 2026","shortName":"Technical Viper 26","brand":"Babolat","shape":"Diamant","weight":"365-375g","balance":"Haut","surface":"Carbon 12K","core":"X-EVA","antivib":"Vibrabsorb","price":"300-370€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/babolat-technical-viper-2024.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":7,"Confort":6,"Spin":8.5,"Maniabilité":5.5,"Tolérance":6},"verdict":"Diamant expert pour attaquants purs. Carbon 12K ultra-rigide et X-EVA pour sortie de balle explosive. Spin dévastateur grâce au 3D Spin+.","editorial":"La Technical Viper 2026 est la version purement offensive de la nouvelle gamme. Carbone 12K pour la rigidité, X-EVA pour l\'explosivité, forme diamant. Pas de CarbonFlex ici — Babolat veut de la rigidité pure. C\'est la Viper pour les compétiteurs qui veulent du retour d\'énergie sans compromis. La rivale directe de la Bullpadel Hack 04.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone haute densité — rigidité élevée pour la puissance"},{"label":"Noyau","value":"X-EVA","detail":"Mousse réactive haute performance Babolat"},{"label":"Forme","value":"Diamant","detail":"Puissance maximale — orientation compétition"},{"label":"Sweet spot","value":"Haut et exigeant","detail":"Zone de frappe concentrée pour les experts"}],"targetProfile":"Joueurs experts offensifs. La Viper de compétition pour les frappeurs techniques."},{"name":"Babolat Veron 3.0 Juan Lebrón 2026","shortName":"Veron 3.0 JL 26","brand":"Babolat","shape":"Diamant","weight":"360g","balance":"Haute (268mm)","surface":"CarbonFlex hybrid","core":"Power Foam","antivib":true,"price":"280-340€","player":"Avancé offensif polyvalent","imageUrl":"https://www.padelful.com/images/rackets/babolat-veron-3-0-2026-juan-lebron.png","year":2026,"category":"avance","scores":{"Puissance":8,"Contrôle":7.5,"Confort":7,"Spin":7.5,"Maniabilité":7,"Tolérance":6.5},"verdict":"Polyvalent offensif Lebrón. CarbonFlex pour tolérance + diamant pour punch.","id":"babolat-veron-3-0-juan-lebr-n-2026","editorial":"La Veron 3.0 Juan Lebrón 2026 est la version polyvalente de la gamme signature du champion. CarbonFlex Hybrid en surface — un mix propriétaire qui offre flex et réactivité — avec Power Foam pour l\'explosivité. C\'est la pala pour ceux qui veulent l\'ADN Lebrón avec plus de polyvalence que la Viper Soft. Forme diamant pour garder du punch.","techHighlights":[{"label":"Surface","value":"CarbonFlex Hybrid","detail":"Composite hybride Babolat — flex calibré entre rigidité et souplesse"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour les accélérations offensives"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — la marque de fabrique Lebrón"},{"label":"Signature","value":"Juan Lebrón 3.0","detail":"3ème génération de la gamme signature de l\'ex-N°1 mondial"}],"targetProfile":"Joueurs avancés offensifs polyvalents. La version accessible de la gamme Lebrón."},{"name":"Babolat Vertuo 2026","shortName":"Vertuo 26","brand":"Babolat","shape":"Ronde","weight":"350g","balance":"Moyenne (262mm)","surface":"Fibre de verre","core":"Power Foam soft","antivib":true,"price":"100-150€","player":"Débutant","imageUrl":"https://www.padelful.com/images/rackets/babolat-vertuo-2026.png","year":2026,"category":"debutant","scores":{"Puissance":5,"Contrôle":7.5,"Confort":9,"Spin":5.5,"Maniabilité":9,"Tolérance":9},"verdict":"Initiation Babolat. Ronde, légère, fibre de verre. Maximum tolérance, prix accessible.","id":"babolat-vertuo-2026","editorial":"La Vertuo 2026 est la nouvelle débutante Babolat. Fibre de verre et Power Foam Soft en ronde. Le Power Foam Soft est une innovation : la mousse reactive Power Foam dans une version adoucie. Plus de répondant qu\'une Soft EVA classique, sans sacrifier le confort. C\'est la débutante la plus évoluée de Babolat.","techHighlights":[{"label":"Surface","value":"Fibre de verre","detail":"Construction accessible et confortable"},{"label":"Noyau","value":"Power Foam Soft","detail":"Innovation — mousse réactive adoucie pour le confort avec du punch"},{"label":"Forme","value":"Ronde","detail":"Tolérance — la formule éprouvée pour débuter"}],"targetProfile":"Débutants. La Babolat nouvelle génération avec un peu plus de réactivité."},{"name":"Babolat Viper 3.0 Juan Lebrón 2026","shortName":"Viper 3.0 JL 26","brand":"Babolat","shape":"Diamant","weight":"370g","balance":"Haute (272mm)","surface":"Carbone 3K + Carbon Power Layer","core":"X-EVA hard","antivib":true,"price":"350-400€","player":"Expert puissance max","imageUrl":"https://www.padelful.com/images/rackets/babolat-viper-3-0-2026-juan-lebron.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":8,"Maniabilité":6,"Tolérance":5.5},"verdict":"Arme de Juan Lebrón. 3K + X-EVA pour smash dévastateur. Puissance brute experts.","id":"babolat-viper-3-0-juan-lebr-n-2026","editorial":"La Viper 3.0 Juan Lebrón 2026 est LE sommet de la gamme Babolat — la pala du champion en version maximale. Carbone 3K avec Carbon Power Layer — une surcouche de carbone qui amplifie la rigidité — et noyau X-EVA Hard, le plus dense du catalogue. C\'est la version de compétition ultime : chaque frappe est catapultée avec une précision chirurgicale. Réservée aux meilleurs.","techHighlights":[{"label":"Surface","value":"Carbone 3K + Carbon Power Layer","detail":"Double couche carbone — rigidité amplifiée pour une sortie de balle maximale"},{"label":"Noyau","value":"X-EVA Hard","detail":"La mousse la plus dure de Babolat — zéro compromis sur la puissance"},{"label":"Forme","value":"Diamant","detail":"Puissance radicale en tête — la pala de finition ultime"},{"label":"Signature","value":"Juan Lebrón compétition","detail":"La pala que Lebrón utilise en tournoi Premier Padel"}],"targetProfile":"Joueurs experts offensifs purs. La pala de compétition la plus radicale de Babolat."},{"id":"babolat-viper-soft-3.0-lebron-2026","name":"Babolat Viper Soft 3.0 Juan Lebrón 2026","shortName":"Viper Soft 3.0 26","brand":"Babolat","shape":"Diamant","weight":"365g","balance":"Haut","surface":"3K Soft Carbon","core":"X-EVA + Comfort Layer","antivib":"Vibrabsorb","price":"320-390€","player":"Juan Lebrón","imageUrl":"https://www.padelful.com/images/rackets/babolat-viper-soft-3-0-2026-juan-lebron.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":7,"Confort":7,"Spin":8,"Maniabilité":6,"Tolérance":6},"verdict":"Viper avec couche confort supplémentaire. Même puissance Lebrón avec toucher plus doux. Pour experts offensifs qui veulent ménager leur bras.","editorial":"La Viper Soft 3.0 est la version confort de l\'arme de Juan Lebrón. Babolat a gardé la puissance de la Viper originale — le carbone 3K Soft qui fléchit juste assez à l\'impact pour emmagasiner de l\'énergie — et a ajouté une couche de confort via le X-EVA avec Comfort Layer intégré. Le Vibrabsorb, système anti-vibrations maison, complète l\'équation. Résultat : une pala qui frappe fort mais qui ne vous détruit pas le bras.","techHighlights":[{"label":"Surface","value":"3K Soft Carbon","detail":"Carbone 3K avec traitement \'Soft\' — micro-flex contrôlé à l\'impact pour plus de confort sans perdre en rigidité"},{"label":"Noyau","value":"X-EVA + Comfort Layer","detail":"Double couche : X-EVA pour la réactivité, Comfort Layer pour filtrer les chocs. Le meilleur des deux mondes"},{"label":"Technologie","value":"Vibrabsorb","detail":"Système anti-vibrations Babolat intégré dans le manche — réduit les chocs transmis au poignet et au coude"},{"label":"Forme","value":"Diamant","detail":"Puissance assumée avec l\'équilibre en tête — compensée par le noyau Soft"},{"label":"Sweet spot","value":"Haut mais amorti","detail":"Le sweet spot diamant classique, mais le Soft Carbon pardonne davantage les frappes excentrées"}],"targetProfile":"Joueurs avancés offensifs qui ont besoin de confort. L\'attaquant qui a des douleurs au coude mais qui refuse de passer à une ronde. Solution idéale pour garder de la puissance en protégeant ses articulations.","proPlayerInfo":{"name":"Juan Lebrón","context":"Ex-N°1 mondial, l\'un des joueurs les plus complets de l\'histoire du padel. Puissance, toucher, intelligence tactique — il incarne la polyvalence au plus haut niveau."}},{"id":"bullpadel-vertex-04-2024","name":"Bullpadel Vertex 04 2024","shortName":"Vertex 04 24","brand":"Bullpadel","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon 12K","core":"Hard EVA","antivib":"Vibradrive","price":"200-280€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-04-2024.png","year":2024,"category":"avance","scores":{"Puissance":8.5,"Contrôle":6,"Confort":5.5,"Spin":7,"Maniabilité":6,"Tolérance":5.5},"verdict":"Vertex 2024 en déstockage. Même puissance dévastatrice que la 2025. Prix plus doux pour la même performance.","editorial":"La Vertex 04 2024 est le millésime qui a précédé la révolution GEO. Carbone 12K et mousse Hard EVA pour une puissance directe. Pas de CurvAktiv ni de GEO Geometry — c\'est du Bullpadel classique, efficace, sans technologie superflue. En déstockage, c\'est un excellent rapport qualité/puissance pour un joueur avancé.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K — la base solide de Bullpadel avant le passage au Xtend Carbon"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — sortie de balle directe et puissante"},{"label":"Forme","value":"Diamant","detail":"L\'ADN Vertex — puissance en tête depuis toujours"}],"targetProfile":"Joueurs avancés offensifs. Excellente affaire en fin de série."},{"id":"bullpadel-flow-2025","name":"Bullpadel Flow 2025","shortName":"Flow 25","brand":"Bullpadel","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"50-70€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-flow-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Raquette débutant Bullpadel, ronde et légère. Construction robuste pour apprendre les bases.","editorial":"La Flow 2025 est la débutante Bullpadel. Fibre de verre et Soft EVA en ronde. Construction simple et efficace avec la qualité Bullpadel. C\'est la pala pour découvrir le padel avec une marque de référence.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux Bullpadel"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort prioritaire"},{"label":"Forme","value":"Ronde","detail":"Tolérance — la Flow pour les premiers matchs"}],"targetProfile":"Débutants. L\'entrée chez Bullpadel."},{"id":"bullpadel-hack-03-2025","name":"Bullpadel Hack 03 2025","shortName":"Hack 03 25","brand":"Bullpadel","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"Vibradrive","price":"160-210€","player":"Paquito Navarro","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-03-2024.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":7},"verdict":"La Hack offre un bon compromis entre puissance et contrôle. Le Vibradrive aide au confort mais le carbone reste ferme. Bonne raquette de progression.","editorial":"La Hack 03 2025 est l\'intermédiaire de la gamme polyvalente Bullpadel. Carbone standard et EVA standard en goutte d\'eau. La Hack est la gamme la plus vendue de Bullpadel pour une bonne raison : elle fait tout correctement. La 03 est le tremplin vers la Hack Elite et la Hack 04.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher Bullpadel de base"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"La polyvalence Hack — le best-seller Bullpadel"}],"targetProfile":"Joueurs intermédiaires polyvalents. Le best-seller Bullpadel pour la progression."},{"id":"bullpadel-hack-elite-2025","name":"Bullpadel Hack Elite 2025","shortName":"Hack Elite 25","brand":"Bullpadel","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"Vibradrive","price":"220-300€","player":"Paquito Navarro","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-elite-2025.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":7},"verdict":"Version élite de la Hack de Paquito. Bon compromis puissance/contrôle pour joueurs avancés polyvalents.","editorial":"La Hack Elite 2025 est la version avancée de la gamme polyvalente de Bullpadel. Carbone standard en surface, EVA standard au cœur, forme goutte d\'eau. La Hack est historiquement la gamme la plus équilibrée de Bullpadel — ni trop offensive, ni trop défensive. La version Elite affine ce compromis avec une construction plus soignée que la Hack 03.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique Bullpadel — bon compromis rigidité/toucher"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour la polyvalence"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le choix polyvalent par excellence de la gamme Hack"}],"targetProfile":"Joueurs avancés polyvalents. La Hack pour ceux qui veulent plus que la version intermédiaire."},{"id":"bullpadel-hack-jr-2025","name":"Bullpadel Hack Junior 2025","shortName":"Hack Junior 25","brand":"Bullpadel","shape":"Diamant","weight":"340g","balance":"Mi-haut","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"58-95€","player":"Paquito Navarro","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":8.5,"Contrôle":5.5,"Confort":7.5,"Spin":6,"Maniabilité":9,"Tolérance":5},"verdict":"Version junior offensive inspirée de Paquito Navarro. Forme diamant pour puissance maximale, mais confort et tolérance limités. Pour jeunes joueurs offensifs.","editorial":"La Hack Jr 2025 est la junior de la gamme polyvalente Bullpadel. Fibre de verre et Soft EVA en diamant — l\'esprit offensif Hack dès le plus jeune âge. Pour les juniors compétiteurs qui veulent un format offensive.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre junior Bullpadel"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection junior"},{"label":"Forme","value":"Diamant","detail":"L\'esprit offensif Hack en junior"}],"targetProfile":"Juniors offensifs. Le Hack pour les jeunes compétiteurs."},{"id":"bullpadel-indiga-boy-2025","name":"Bullpadel Indiga Boy 2025","shortName":"Indiga Boy 25","brand":"Bullpadel","shape":"Goutte d\'eau","weight":"280g ±10g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"55-65€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-indiga-boy-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":8,"Confort":9,"Spin":6,"Maniabilité":10,"Tolérance":7.5},"verdict":"Ultra-légère (280g) pour les plus jeunes. Forme goutte d\'eau accessible avec excellent confort. La plus légère de sa catégorie.","editorial":"L\'Indiga Boy 2025 est la junior polyvalente de Bullpadel. Fibre de verre et Soft EVA en goutte d\'eau. Construction légère et confortable pour les jeunes joueurs qui découvrent le padel. Plus polyvalente que la Hack Jr.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — confort junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple pour les jeunes"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — la junior tout-terrain Bullpadel"}],"targetProfile":"Juniors. La Bullpadel polyvalente pour les jeunes."},{"id":"bullpadel-vertex-03-2025","name":"Bullpadel Vertex 03 2025","shortName":"Vertex 03 25","brand":"Bullpadel","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon","core":"Standard EVA","antivib":"Vibradrive","price":"180-240€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-03-2023.png","year":2025,"category":"intermediaire","scores":{"Puissance":9,"Contrôle":5.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":5.5},"verdict":"Diamant puissante avec balance haut. Le Vibradrive atténue un peu les vibrations mais le confort reste moyen. Pour intermédiaires offensifs.","editorial":"La Vertex 03 2025 est l\'intermédiaire de la gamme puissance Bullpadel. Carbone standard et EVA standard en diamant. La Vertex commence ici : forme diamant pour l\'offensive, matériaux accessibles pour la progression. C\'est la première étape vers la Vertex 04 et la Vertex 05.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone d\'entrée — première approche de la rigidité Vertex"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour la progression offensive"},{"label":"Forme","value":"Diamant","detail":"L\'ADN Vertex — puissance en tête dès l\'intermédiaire"}],"targetProfile":"Joueurs intermédiaires offensifs. L\'entrée dans la gamme Vertex."},{"id":"bullpadel-vertex-04-2025","name":"Bullpadel Vertex 04 2025","shortName":"Vertex 04","brand":"Bullpadel","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon 12K","core":"Hard EVA","antivib":"Vibradrive","price":"250-350€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-04-2025-juan-tello.png","year":2025,"category":"avance","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":7.5,"Maniabilité":6,"Tolérance":5},"verdict":"La Vertex ultime. Carbone 12K + EVA dure pour une sortie de balle explosive. Confort minimal — réservée aux experts avec bras solide.","editorial":"La Vertex 04 2025 est la mise à jour annuelle du best-seller offensif de Bullpadel. Même recette gagnante — carbone 12K, Hard EVA, diamant — avec des ajustements de construction pour plus d\'homogénéité. La Vertex est à Bullpadel ce que la Metalbone est à Adidas : la référence puissance. Cette version 2025 affine la recette sans la révolutionner.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K éprouvé — rigidité et puissance fiables"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure pour la sortie de balle — la signature Vertex"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — l\'ADN Vertex depuis les origines"}],"targetProfile":"Joueurs avancés offensifs. La Vertex de référence avant l\'arrivée de la 05 GEO."},{"id":"bullpadel-vertex-elite-2025","name":"Bullpadel Vertex Elite 2025","shortName":"Vertex Elite","brand":"Bullpadel","shape":"Diamant","weight":"370-385g","balance":"Haut","surface":"Carbon 18K","core":"Hard EVA","antivib":"Vibradrive","price":"350-420€","player":"Ale Galán","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-elite-2025.png","year":2025,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":7,"Maniabilité":5.5,"Tolérance":5},"verdict":"Le top absolu de Bullpadel. Carbone 18K + EVA Hard pour une puissance dévastatrice. Vibradrive aide un peu mais confort minimal.","editorial":"La Vertex Elite 2025 est le haut de gamme puissance de Bullpadel pour 2025. Carbone 18K — un cran au-dessus du 12K — et mousse Hard EVA pour une frappe dévastatrice. C\'est la Vertex pour les compétiteurs qui veulent le maximum de rigidité avant l\'ère du Xtend Carbon. Un classique de la puissance brute.","techHighlights":[{"label":"Surface","value":"Carbon 18K","detail":"Carbone haute densité — le plus rigide du catalogue 2025 de Bullpadel"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse ultra-dense — puissance maximale, confort minimal"},{"label":"Forme","value":"Diamant","detail":"Puissance radicale — la Vertex la plus extrême de 2025"}],"targetProfile":"Joueurs experts offensifs. La Vertex ultime 2025 pour les frappeurs sans concession."},{"id":"bullpadel-vertex-jr-2025","name":"Bullpadel Vertex Junior 2025","shortName":"Vertex Junior 25","brand":"Bullpadel","shape":"Diamant","weight":"330-350g","balance":"Mi-haut","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"75-109€","player":"Juan Tello","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":8,"Contrôle":5.5,"Confort":8,"Spin":6,"Maniabilité":8.5,"Tolérance":5},"verdict":"Version junior du best-seller Vertex. Forme diamant offensive avec bon confort grâce à la fibre de verre, mais tolérance limitée aux frappes décentrées.","editorial":"La Vertex Jr 2025 est la junior premium de Bullpadel. Fibre de verre et Soft EVA en diamant. Le nom Vertex — la gamme puissance — sur une pala de jeune. Pour les juniors ambitieux qui rêvent de la Vertex des grands.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — Vertex junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection des jeunes articulations"},{"label":"Forme","value":"Diamant","detail":"L\'ADN Vertex — puissance en junior"}],"targetProfile":"Juniors offensifs ambitieux. Le prestige Vertex pour les jeunes."},{"name":"Bullpadel Flow Legend 2026","shortName":"Flow Legend 26","brand":"Bullpadel","shape":"Goutte d\'eau","weight":"350g","balance":"Moyenne (262mm)","surface":"Xtend Carbon 3K + Fibrix","core":"MultiEva","antivib":true,"price":"200-260€","player":"Avancé léger / femmes","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-flow-legend-2026-alejandra-salazar.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":8,"Confort":7.5,"Spin":7.5,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Légère et punchy. Inspirée par Alejandra Salazar. Idéale joueuses avancées.","id":"bullpadel-flow-legend-2026","editorial":"La Flow Legend 2026 est la pala légère et maniable de Bullpadel. Xtend Carbon 3K avec Fibrix — un renfort en fibres qui ajoute de la résistance sans poids — et noyau MultiEva pour la polyvalence. Forme goutte d\'eau, construction aérienne. C\'est la Bullpadel des joueurs rapides, des femmes avancées, et de tous ceux qui privilégient la vitesse de bras.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 3K + Fibrix","detail":"Carbone extensible avec renfort Fibrix — rigidité sans poids supplémentaire"},{"label":"Noyau","value":"MultiEva","detail":"Mousse multi-densité pour la polyvalence"},{"label":"Forme","value":"Goutte d\'eau","detail":"Sweet spot intermédiaire — polyvalent et maniable"},{"label":"Poids","value":"Legend (léger)","detail":"Construction allégée pour la vitesse et la réactivité"}],"targetProfile":"Joueurs avancés légers ou rapides. Excellente option pour les femmes avancées et les joueurs de filet vifs."},{"name":"Bullpadel Hack 04 2026","shortName":"Hack 04 26","brand":"Bullpadel","shape":"Diamant","weight":"370g","balance":"Haute (272mm)","surface":"TriCarbon 18K + Total Channel","core":"Tricore hard","antivib":true,"price":"300-380€","player":"Expert puissance pure","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-04-2026-paquito-navarro.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":7.5,"Maniabilité":6,"Tolérance":5},"verdict":"Puissance max Bullpadel. TriCarbon 18K + Tricore. Raquette de Paquito Navarro.","id":"bullpadel-hack-04-2026","editorial":"La Hack 04 2026 est la version compétition de la gamme polyvalente Bullpadel. Le TriCarbon 18K avec Total Channel — le carbone le plus avancé de la marque — offre une rigidité supérieure canalisée par les canaux du cadre. Le noyau Tricore Hard est exclusif à ce modèle : trois densités de mousse pour une réponse progressive. C\'est la Hack pour les experts qui veulent une polyvalente capable de tout.","techHighlights":[{"label":"Surface","value":"TriCarbon 18K + Total Channel","detail":"Triple couche carbone 18K avec canaux de redistribution d\'énergie — le sommet technologique Bullpadel"},{"label":"Noyau","value":"Tricore Hard","detail":"Mousse exclusive à triple densité — dur au centre, progressif vers les bords"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive pour la compétition"},{"label":"Technologie","value":"Total Channel","detail":"Canaux dans le cadre qui redistribuent l\'énergie pour un sweet spot élargi"}],"targetProfile":"Joueurs experts polyvalents-offensifs. La Hack ultime pour la compétition."},{"id":"bullpadel-hack-04-comfort-2026","name":"Bullpadel Hack 04 Comfort 2026","shortName":"Hack 04 Comfort 26","brand":"Bullpadel","shape":"Goutte d\'eau","weight":"360-370g","balance":"Moyen","surface":"TriCarbon","core":"MultiEVA Soft","antivib":"Ease Vibe","price":"200-260€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-04-comfort-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":8,"Contrôle":7,"Confort":7,"Spin":7.5,"Maniabilité":6.5,"Tolérance":6},"verdict":"ADN offensif Hack avec confort accru. Goutte d\'eau et MultiEVA Soft pour sessions longues. Équilibre attaque/confort pour intermédiaires progressant.","editorial":"La Hack 04 Comfort 2026 est la version accessible de la Hack 04. TriCarbon — le nouveau carbone triple couche de Bullpadel — en version standard avec noyau MultiEVA Soft pour le confort. Forme goutte d\'eau pour la polyvalence. C\'est la porte d\'entrée vers la technologie TriCarbon sans l\'exigence de la version expert.","techHighlights":[{"label":"Surface","value":"TriCarbon","detail":"Triple couche carbone Bullpadel — technologie premium en version accessible"},{"label":"Noyau","value":"MultiEVA Soft","detail":"Mousse multi-densité souple — confort prioritaire"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence confortable"}],"targetProfile":"Joueurs intermédiaires qui veulent la technologie TriCarbon avec du confort."},{"id":"bullpadel-hack-04-hybrid-2026","name":"Bullpadel Hack 04 Hybrid 2026","shortName":"Hack 04 Hybrid 26","brand":"Bullpadel","shape":"Hybride","weight":"365-375g","balance":"Moyen","surface":"18K Aluminised Carbon","core":"MultiEVA","antivib":"Ease Vibe + Total Channel","price":"260-330€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/bullpadel-hack-04-hybrid-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":8,"Confort":7,"Spin":8,"Maniabilité":7.5,"Tolérance":7},"verdict":"Hack format hybride pour contre-attaque. Puissance dynamique avec meilleur contrôle défensif. Pour joueurs avancés offensifs polyvalents.","editorial":"La Hack 04 Hybrid 2026 est le tank de Bullpadel — la pala des gros frappeurs qui ne veulent pas sacrifier le toucher. Le carbone aluminisé 18K en surface est l\'un des plus rigides du catalogue, mais le noyau MultiEVA et le double système Ease Vibe + Total Channel tempèrent la brutalité. Le Total Channel — des canaux creusés dans le cadre — redistribue l\'énergie d\'impact pour un sweet spot plus homogène. C\'est la puissance civilisée.","techHighlights":[{"label":"Surface","value":"Carbone Aluminisé 18K","detail":"Traitement aluminium sur carbone 18K — rigidité élevée pour les frappes puissantes"},{"label":"Noyau","value":"MultiEVA","detail":"Mousse multi-densité — dur au centre, souple en périphérie pour le confort"},{"label":"Technologie","value":"Ease Vibe + Total Channel","detail":"Double système : Ease Vibe absorbe les vibrations, Total Channel redistribue l\'énergie dans le cadre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le compromis Hack — puissance de la gamme offensive avec la tolérance de la forme goutte"},{"label":"Sweet spot","value":"Élargi par Total Channel","detail":"Les canaux dans le cadre créent une zone de frappe plus large et plus uniforme"}],"targetProfile":"Joueurs avancés offensifs qui veulent de la puissance avec du confort. La passerelle entre les palas de contrôle et les diamants pures puissance.","proPlayerInfo":null},{"id":"bullpadel-icon-2026","name":"Bullpadel Icon 2026","shortName":"Icon 26","brand":"Bullpadel","shape":"Diamant","weight":"365-375g","balance":"Haut","surface":"12K Xtend Carbon","core":"MultiEVA","antivib":"Ease Vibe","price":"300-380€","player":"Juan Martín Díaz","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-icon-2026-juan-martin-diaz.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":7,"Confort":6.5,"Spin":8,"Maniabilité":6,"Tolérance":6.5},"verdict":"Hommage au légendaire Juan Martín Díaz. Puissance et contrôle premium pour joueurs d\'élite qui cherchent l\'excellence sur chaque point.","editorial":"La Icon 2026 est la signature de Juan Martín Díaz — \'El Lobo\', le loup du padel. À l\'image de son créateur, cette pala est polyvalente, intelligente et redoutable quand le point se tend. Le Xtend Carbon 12K offre un bon compromis entre rigidité et toucher, tandis que le noyau MultiEVA s\'adapte à tous les coups. L\'Ease Vibe assure un confort sur la durée. Une pala de vétéran pour joueurs matures.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 12K","detail":"Carbone 12K extensible — plus rigide que le 3K mais avec le flex Xtend pour le toucher"},{"label":"Noyau","value":"MultiEVA","detail":"Mousse polyvalente multi-densité — ni trop dure, ni trop souple, adaptée à tous les coups"},{"label":"Technologie","value":"Ease Vibe","detail":"Anti-vibrations Bullpadel pour le confort sur les longues sessions de jeu"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le compromis parfait — sweet spot intermédiaire entre ronde et diamant"},{"label":"Sweet spot","value":"Intermédiaire et polyvalent","detail":"Ni trop haut ni trop centré — s\'adapte au jeu de fond comme au jeu de filet"}],"targetProfile":"Joueurs avancés polyvalents. Le joueur expérimenté qui ne veut pas se spécialiser, qui défend puis attaque selon le point. Excellente première pala avancée.","proPlayerInfo":{"name":"Juan Martín Díaz","context":"Légende absolue du padel. Multiple champion du monde, surnommé \'El Lobo\'. Son style intelligent et son expérience ont façonné cette pala à son image."}},{"name":"Bullpadel Neuron 02 2026","shortName":"Neuron 02 26","brand":"Bullpadel","shape":"Hybride","weight":"365g","balance":"Moyenne (265mm)","surface":"Xtend Carbon 3K + PrismLock","core":"MultiEva","antivib":true,"price":"280-350€","player":"Avancé-Expert contrôle","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-neuron-02-2026-fede-chingotto.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":8.5,"Confort":7.5,"Spin":8,"Maniabilité":7.5,"Tolérance":7},"verdict":"Contrôle structurel. PrismLock réduit torsions. Raquette de Chingotto.","id":"bullpadel-neuron-02-2026","editorial":"La Neuron 02 2026 est la version hybride de la gamme contrôle Bullpadel. Xtend Carbon 3K avec PrismLock pour une réponse prévisible, noyau MultiEva polyvalent. La forme hybride — entre ronde et goutte — offre un sweet spot élargi tout en gardant un peu de punch. C\'est la Neuron pour les joueurs qui veulent du contrôle avec une pointe d\'attaque.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 3K + PrismLock","detail":"Carbone extensible avec verrouillage des fibres — réponse constante et prévisible"},{"label":"Noyau","value":"MultiEva","detail":"Mousse multi-densité polyvalente"},{"label":"Forme","value":"Hybride","detail":"Entre ronde et goutte — sweet spot élargi avec un peu plus de punch qu\'une ronde pure"},{"label":"Technologie","value":"PrismLock","detail":"Verrouillage des fibres de carbone pour une réponse identique frappe après frappe"}],"targetProfile":"Joueurs avancés contrôle-polyvalents. La Neuron accessible, moins radicale que l\'Edge."},{"id":"bullpadel-neuron-02-edge-2026","name":"Bullpadel Neuron 02 Edge 2026","shortName":"Neuron 02 Edge 26","brand":"Bullpadel","shape":"Hybride","weight":"365-375g","balance":"Moyen-Haut","surface":"3K Xtend Carbon","core":"MultiEVA","antivib":"PrismLock + Ease Vibe","price":"300-380€","player":"Federico Chingotto","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-neuron-02-edge-2026.png","year":2026,"category":"expert","scores":{"Puissance":7.5,"Contrôle":9,"Confort":7,"Spin":8.5,"Maniabilité":7,"Tolérance":6.5},"verdict":"Version pro du Neuron avec géométrie modifiée pour plus de punch. Précision de Chingotto avec boost offensif. Pour experts tactiques qui veulent aussi accélérer.","editorial":"La Neuron 02 Edge est la pala de Federico Chingotto — et elle lui ressemble : technique, précise, tactique. Le Xtend Carbon 3K est dosé pour offrir du toucher plutôt que de la brutalité. Le PrismLock, exclusivité Bullpadel, verrouille les fibres de carbone en position pour une réponse plus prévisible frappe après frappe. L\'Ease Vibe filtre les vibrations parasites. C\'est une pala d\'orfèvre, pas de bûcheron.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 3K","detail":"Carbone extensible à maillage fin — réactivité et toucher plutôt que rigidité brute"},{"label":"Noyau","value":"MultiEVA","detail":"Mousse multi-densité pour un équilibre contrôle/puissance adapté au jeu de volée"},{"label":"Technologie","value":"PrismLock + Ease Vibe","detail":"PrismLock verrouille les fibres pour une réponse constante, Ease Vibe absorbe les vibrations"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré et large — la forme des joueurs qui construisent les points avec intelligence"},{"label":"Sweet spot","value":"Large et centré","detail":"Maximum de tolérance pour le jeu de volée et les contres rapides au filet"}],"targetProfile":"Joueurs experts défensifs ou de contrôle. Le joueur de droite qui ne rate jamais une volée, qui place et qui attend l\'erreur adverse. Aussi pour les joueurs de gauche tactiques.","proPlayerInfo":{"name":"Federico Chingotto","context":"Joueur argentin Top 10, surnommé \'El Mago\' pour ses coups impossibles et sa créativité tactique unique sur le circuit."}},{"name":"Bullpadel Vertex 05 2026","shortName":"Vertex 05 26","brand":"Bullpadel","shape":"Diamant","weight":"365g","balance":"Haute (270mm)","surface":"Xtend Carbon 12K + CurvAktiv","core":"MultiEva","antivib":true,"price":"300-380€","player":"Expert polyvalent offensif","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-05-2026-juan-tello.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":6.5,"Confort":5.5,"Spin":8,"Maniabilité":6,"Tolérance":5},"verdict":"Flagship Bullpadel 2026. 12K + MultiEva pour puissance ET toucher. Raquette de Juan Tello.","id":"bullpadel-vertex-05-2026","editorial":"La Vertex 05 2026 est le sommet absolu de la puissance Bullpadel — au-dessus même de la GEO. Le Xtend Carbon 12K avec CurvAktiv — une géométrie de cadre incurvée qui concentre l\'énergie — couplé au noyau MultiEva multi-densité. C\'est la Vertex la plus avancée technologiquement. Pour les joueurs qui veulent chaque innovation Bullpadel au service de la puissance.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 12K + CurvAktiv","detail":"Carbone 12K extensible avec géométrie incurvée — concentration maximale de l\'énergie"},{"label":"Noyau","value":"MultiEva","detail":"Mousse multi-densité pour un confort relatif malgré la puissance"},{"label":"Forme","value":"Diamant","detail":"Puissance ultime en tête"},{"label":"Technologie","value":"CurvAktiv","detail":"Géométrie incurvée du cadre qui optimise le transfert d\'énergie au point d\'impact"}],"targetProfile":"Joueurs experts offensifs qui veulent la technologie Bullpadel la plus avancée."},{"id":"bullpadel-vertex-05-geo-2026","name":"Bullpadel Vertex 05 GEO 2026","shortName":"Vertex 05 GEO 26","brand":"Bullpadel","shape":"Diamant","weight":"365-375g","balance":"Haut","surface":"12K Xtend Carbon","core":"MultiEVA","antivib":"Ease Vibe","price":"300-380€","player":"Paquito Navarro","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-05-geo-2026-pablo-cardona.png","year":2026,"category":"expert","scores":{"Puissance":9.5,"Contrôle":6,"Confort":5,"Spin":8.5,"Maniabilité":5.5,"Tolérance":5.5},"verdict":"Version la plus agressive du Vertex. Géométrie optimisée pour surface de frappe élargie et balance encore plus haute. Puissance brute maximale.","editorial":"La Vertex 05 GEO 2026 est le vaisseau amiral de Bullpadel — la pala que les attaquants rêvent de manier. Le Xtend Carbon 3K sur la face offre une frappe sèche et directe, tandis que la géométrie GEO redistribue les forces d\'impact pour un sweet spot plus homogène. Le noyau MultiEVA stratifie plusieurs densités de mousse : dur au centre pour la puissance, plus souple en périphérie pour le confort. Un bijou d\'ingénierie.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 3K","detail":"Carbone extensible propriétaire Bullpadel — combine rigidité et capacité de déformation contrôlée"},{"label":"Noyau","value":"MultiEVA","detail":"Mousse multi-densité : couche dure au centre (puissance), couche souple en périphérie (confort)"},{"label":"Technologie","value":"GEO Geometry","detail":"Refonte géométrique du cadre qui redistribue les contraintes pour élargir le sweet spot"},{"label":"Forme","value":"Diamant","detail":"Équilibre en tête assumé — chaque gramme de masse contribue à la puissance du smash"},{"label":"Sweet spot","value":"Élargi par GEO","detail":"La technologie GEO compense le sweet spot naturellement réduit de la forme diamant"}],"targetProfile":"Joueurs experts offensifs. L\'attaquant complet qui smash mais qui veut garder du toucher sur les volées hautes. La version 05 est plus accessible que la 04.","proPlayerInfo":{"name":"Paquito Navarro","context":"Légende du padel mondial, connu pour ses smashs dévastateurs et son charisme sur le court. Ambassadeur historique de la gamme Vertex."}},{"name":"Bullpadel Vertex 05 Hybrid 2026","shortName":"Vertex 05 Hyb 26","brand":"Bullpadel","shape":"Hybride","weight":"365g","balance":"Moyenne (265mm)","surface":"Xtend Carbon 3K + Fibrix","core":"MultiEva","antivib":true,"price":"260-320€","player":"Avancé polyvalent","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-vertex-05-hybrid-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":8,"Confort":7,"Spin":8,"Maniabilité":7.5,"Tolérance":7},"verdict":"Vertex en goutte d\'eau. Plus de contrôle et maniabilité. Joueurs complets côté droit.","id":"bullpadel-vertex-05-hybrid-2026","editorial":"La Vertex 05 Hybrid est la version polyvalente de la Vertex 05. Le Xtend Carbon 3K avec Fibrix offre de la rigidité sans l\'extrémisme du 12K. La forme hybride abaisse le sweet spot pour plus de tolérance. C\'est la Vertex pour ceux qui veulent le nom et la technologie sans le côté radical de la diamant pure.","techHighlights":[{"label":"Surface","value":"Xtend Carbon 3K + Fibrix","detail":"Carbone extensible avec renfort Fibrix — rigidité maîtrisée"},{"label":"Noyau","value":"MultiEva","detail":"Mousse polyvalente multi-densité"},{"label":"Forme","value":"Hybride","detail":"Compromis entre diamant et goutte — sweet spot intermédiaire"},{"label":"Technologie","value":"Fibrix","detail":"Fibres de renfort qui ajoutent de la résistance sans alourdir"}],"targetProfile":"Joueurs avancés polyvalents qui veulent la technologie Vertex dans un format plus accessible."},{"name":"Bullpadel XPLO 2026","shortName":"XPLO 26","brand":"Bullpadel","shape":"Diamant","weight":"375g","balance":"Haute (274mm)","surface":"TriCarbon 18K","core":"Tricore hard","antivib":false,"price":"320-400€","player":"Expert puissance explosive","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-xplo-2026-martin-di-nenno.png","year":2026,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":8,"Maniabilité":5,"Tolérance":5},"verdict":"Plus puissante Bullpadel. Raquette de Di Nenno. Diamant extreme, technique requise.","id":"bullpadel-xplo-2026","editorial":"La XPLO 2026 est la bête de puissance brute de Bullpadel — plus radicale encore que la Vertex. TriCarbon 18K sans Total Channel — pas de redistribution d\'énergie, tout est concentré au point d\'impact. Noyau Tricore Hard pour une sortie de balle maximale. C\'est la pala la plus agressive du catalogue Bullpadel, réservée aux frappeurs sans peur.","techHighlights":[{"label":"Surface","value":"TriCarbon 18K","detail":"Triple couche carbone 18K — rigidité brute sans système de redistribution"},{"label":"Noyau","value":"Tricore Hard","detail":"Triple densité orientée puissance — le noyau le plus dur de Bullpadel"},{"label":"Forme","value":"Diamant","detail":"Puissance maximale — aucun compromis"}],"targetProfile":"Joueurs experts offensifs purs. La Bullpadel la plus radicale du marché."},{"name":"Bullpadel XPLO Comfort 2026","shortName":"XPLO Comfort 26","brand":"Bullpadel","shape":"Diamant","weight":"365g","balance":"Haute (270mm)","surface":"TriCarbon 12K + Fibrix","core":"MultiEva","antivib":true,"price":"280-340€","player":"Avancé puissance confort","imageUrl":"https://www.padelful.com/images/rackets/bullpadel-xplo-comfort-2026.png","year":2026,"category":"avance","scores":{"Puissance":8.5,"Contrôle":6.5,"Confort":7,"Spin":7.5,"Maniabilité":6.5,"Tolérance":6},"verdict":"XPLO apprivoisée. Même ADN avec plus de confort. Attaquants sensibles du bras.","id":"bullpadel-xplo-comfort-2026","editorial":"La XPLO Comfort est le paradoxe de Bullpadel — la puissance de la XPLO avec une dose de confort. Le TriCarbon 12K avec Fibrix réduit la rigidité d\'un cran, et le noyau MultiEva remplace le Tricore Hard pour plus de polyvalence. Forme diamant maintenue. C\'est la XPLO civilisée, pour ceux qui veulent sa puissance sans subir ses vibrations.","techHighlights":[{"label":"Surface","value":"TriCarbon 12K + Fibrix","detail":"Triple couche carbone atténuée avec Fibrix — rigidité forte mais pas extrême"},{"label":"Noyau","value":"MultiEva","detail":"Mousse multi-densité — plus confortable que le Tricore Hard"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête maintenue"},{"label":"Confort","value":"Version Comfort","detail":"Même ADN XPLO avec filtrage des vibrations grâce au Fibrix et au MultiEva"}],"targetProfile":"Joueurs avancés offensifs qui veulent de la puissance sans la brutalité de la XPLO standard."},{"id":"drop-shot-conqueror-2025","name":"Drop Shot Conqueror 10.0 2025","shortName":"Conqueror 10","brand":"Drop Shot","shape":"Diamant","weight":"365-380g","balance":"Haut","surface":"Carbon","core":"Hard EVA","antivib":"—","price":"200-280€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/drop-shot-conqueror-10-0-2025.png","year":2025,"category":"avance","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":6,"Maniabilité":6,"Tolérance":5},"verdict":"Diamant offensive Drop Shot pour les joueurs de puissance. Carbone + EVA dure pour un maximum de sortie de balle.","editorial":"La Conqueror 2025 est la pala phare de Drop Shot — la marque espagnole qui offre souvent des specs premium à prix compétitif. Carbone standard et mousse Hard EVA en diamant. Construction honnête, pas de technologie propriétaire complexe, mais un rapport qualité/prix souvent imbattable. Drop Shot est le choix malin pour le joueur avancé qui refuse de payer la prime de marque.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — efficace sans fioritures"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure pour la puissance — même matériau que les grandes marques"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — orientation offensive claire"},{"label":"Prix","value":"Compétitif","detail":"Drop Shot est reconnu pour ses tarifs agressifs face aux leaders du marché"}],"targetProfile":"Joueurs avancés offensifs au budget maîtrisé. Le meilleur rapport puissance/prix du marché."},{"id":"drop-shot-junior-2025","name":"Drop Shot Explorer Junior 2025","shortName":"Explorer Jr 25","brand":"Drop Shot","shape":"Ronde","weight":"300-320g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"40-55€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/drop-shot-explorer-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Entrée de gamme junior Drop Shot. Simple, efficace et abordable pour découvrir le padel.","editorial":"La Drop Shot Junior 2025 est la pala junior au meilleur prix du marché. Fibre de verre et Soft EVA en ronde. Drop Shot applique sa philosophie de prix agressif même en junior. Le choix malin pour équiper les jeunes sans se ruiner.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le plus accessible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort basique"},{"label":"Forme","value":"Ronde","detail":"Tolérance — le meilleur prix junior du marché"}],"targetProfile":"Juniors. Le meilleur prix du marché pour les jeunes."},{"id":"drop-shot-legend-2025","name":"Drop Shot Legend 4.0 2025","shortName":"Legend 4.0","brand":"Drop Shot","shape":"Goutte d\'eau","weight":"355-370g","balance":"Moyen","surface":"Hybrid carbon/fiberglass","core":"Standard EVA","antivib":"—","price":"120-160€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/drop-shot-legend-4-0.png","year":2025,"category":"intermediaire","scores":{"Puissance":6.5,"Contrôle":8,"Confort":7,"Spin":6,"Maniabilité":7,"Tolérance":7.5},"verdict":"Polyvalente et accessible, la Legend est un bon choix pour l\'intermédiaire qui veut progresser sans se spécialiser.","editorial":"La Legend 2025 est l\'intermédiaire de Drop Shot. Hybride carbone/fibre de verre et EVA standard en goutte d\'eau. Drop Shot offre des specs correctes à prix imbattable — c\'est l\'ADN de la marque. La Legend est une pala de progression sans risque financier.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible — bonne base de progression"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le format Drop Shot classique"}],"targetProfile":"Joueurs intermédiaires au budget maîtrisé. Drop Shot accessible et fiable."},{"id":"dunlop-galactica-jr-2025","name":"Dunlop Galactica Junior 2025","shortName":"Galactica Jr","brand":"Dunlop","shape":"Ronde","weight":"310-330g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"45-65€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/dunlop-galactica-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":9,"Tolérance":9},"verdict":"Raquette junior Dunlop accessible et tolérante. Construction classique fibre de verre pour un apprentissage en douceur.","editorial":"La Galactica Jr 2025 est la junior Dunlop. Fibre de verre et Soft EVA en ronde. Construction simple et fiable de la marque historique du tennis. Pour les jeunes qui font leurs premiers pas.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre Dunlop junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort junior"},{"label":"Forme","value":"Ronde","detail":"Tolérance — Dunlop fiable pour les jeunes"}],"targetProfile":"Juniors. Dunlop pour les premiers matchs."},{"id":"dunlop-galactica-pro-2025","name":"Dunlop Galactica Pro 2025","shortName":"Galactica Pro","brand":"Dunlop","shape":"Goutte d\'eau","weight":"355-370g","balance":"Moyen","surface":"Hybrid carbon/fiberglass","core":"Standard EVA","antivib":"—","price":"130-170€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/dunlop-galactica-pro-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":6.5,"Contrôle":8,"Confort":7,"Spin":6,"Maniabilité":7,"Tolérance":7.5},"verdict":"Polyvalente Dunlop avec mix carbone/verre. Bon confort pour une raquette carbone, idéale pour progresser.","editorial":"La Galactica Pro 2025 est l\'intermédiaire de Dunlop. Hybride carbone/fibre de verre et EVA standard en goutte d\'eau. Construction classique de la marque historique du tennis. Dunlop propose des specs honnêtes sans surenchère technologique ni surenchère tarifaire.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible — rigidité modérée"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Dunlop"}],"targetProfile":"Joueurs intermédiaires. Dunlop fiable et accessible."},{"id":"dunlop-rocket-ultra-2025","name":"Dunlop Rocket Ultra 2025","shortName":"Rocket Ultra","brand":"Dunlop","shape":"Ronde","weight":"350-360g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"55-75€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/dunlop-rocket-ultra-yellow-2023.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Ronde Dunlop légère et maniable. Fibre de verre souple pour un apprentissage confortable.","editorial":"La Rocket Ultra 2025 est la débutante de Dunlop. Fibre de verre et Soft EVA en ronde. La marque historique du tennis au service des premiers matchs de padel. Construction honnête et fiable.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre Dunlop"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort"},{"label":"Forme","value":"Ronde","detail":"Tolérance pour l\'apprentissage"}],"targetProfile":"Débutants. Dunlop fiable pour les premiers matchs."},{"id":"dunlop-fx-lite-26","brand":"Dunlop","name":"Dunlop FX Lite 2026","shortName":"FX Lite 26","year":2026,"category":"avance","shape":"Diamant","weight":"350-360","balance":"moyen","surface":"12K Carbon","core":"Soft EVA","description":"Diamant légère offensive. 12K Carbon + Soft EVA + Sonic Core Infinergy. Maniabilité sans sacrifier puissance.","scores":{"Puissance":7.5,"Contrôle":7,"Maniabilité":8,"Confort":7.5,"Spin":7.5,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/dunlop-fx-lite-2026.png","verdict":"Diamant légère qui privilégie la maniabilité sans sacrifier la frappe. Bon compromis pour attaquants qui veulent rester mobiles.","price":"170-230€","editorial":"La FX Lite 2026 est l\'outsider de Dunlop — la marque historique du tennis qui s\'impose progressivement en padel. Le carbone 12K offre une rigidité de bon niveau, et le noyau Soft EVA mise tout sur le confort et la maniabilité. C\'est une pala honnête, bien construite, sans prétention marketing exagérée. Dunlop vise les joueurs avancés qui veulent du matériel fiable sans payer le prix des technologies propriétaires des leaders.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K classique — bonne rigidité, réponse prévisible et fiable"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple pour un maximum de confort — idéale pour les sessions longues"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence et tolérance — le choix sûr pour un joueur avancé"},{"label":"Sweet spot","value":"Généreux","detail":"La combinaison forme goutte + EVA souple offre une grande zone de confort"},{"label":"Poids","value":"Lite","detail":"Version légère pour la vitesse de bras et la réduction de la fatigue"}],"targetProfile":"Joueurs avancés qui privilégient le confort et la maniabilité. Excellent choix pour les joueurs de 40+ ans ou ceux qui jouent 3-4 fois par semaine et veulent préserver leur corps.","proPlayerInfo":null},{"id":"dunlop-fx-pro-26","brand":"Dunlop","name":"Dunlop FX Pro 2026","shortName":"FX Pro 26","year":2026,"category":"expert","shape":"Diamant","weight":"365-375","balance":"haut","surface":"12K Carbon","core":"Pro EVA","description":"Diamant puissance premium. 12K Carbon + Pro EVA + Force Bridge. Head heavy pour smashes dévastateurs.","scores":{"Puissance":9,"Contrôle":6.5,"Maniabilité":6,"Confort":6,"Spin":7.5,"Tolérance":5.5},"imageUrl":"https://www.padelful.com/images/rackets/dunlop-fx-pro-2026.png","verdict":"Arme offensive pure, balance haute et carbone 12K pour des smashes dévastateurs. Réservée aux bras solides et aux frappes engagées.","price":"200-260€","editorial":"La FX Pro 2026 est le haut de gamme Dunlop — la preuve que la marque historique du tennis prend le padel au sérieux. Carbone 12K et Pro EVA en diamant. Construction soignée sans technologies propriétaires complexes, mais des matériaux de qualité et un équilibre bien calibré. Dunlop vise les joueurs experts qui veulent de la performance fiable sans le marketing des grandes marques de padel.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K — rigidité élevée pour les frappes puissantes"},{"label":"Noyau","value":"Pro EVA","detail":"Mousse haute performance Dunlop — bonne réactivité"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — orientation compétition"}],"targetProfile":"Joueurs experts offensifs. Dunlop haut de gamme à prix honnête."},{"id":"dunlop-fx-team-26","brand":"Dunlop","name":"Dunlop FX Team 2026","shortName":"FX Team 26","year":2026,"category":"intermediaire","shape":"Diamant","weight":"360-370","balance":"moyen","surface":"Premium Carbon","core":"Pro EVA","description":"Diamant accessible. SpinBoost + Power Holes + Force Bridge. Puissance contrôlée pour progression.","scores":{"Puissance":7.5,"Contrôle":6,"Maniabilité":7,"Confort":7.5,"Spin":7,"Tolérance":6},"imageUrl":"https://www.padelful.com/images/rackets/dunlop-fx-team-2026.png","verdict":"Entrée de gamme diamant accessible avec puissance contrôlée. Bonne option pour progresser vers un jeu plus offensif sans se ruiner.","price":"130-180€","editorial":"La FX Team 2026 est la nouvelle intermédiaire de Dunlop. Carbone Premium et Pro EVA en diamant — Dunlop monte en gamme. Plus offensive que la Galactica avec une forme diamant et des matériaux supérieurs. C\'est la preuve que Dunlop investit sérieusement dans le padel.","techHighlights":[{"label":"Surface","value":"Premium Carbon","detail":"Carbone qualité supérieure — montée en gamme Dunlop"},{"label":"Noyau","value":"Pro EVA","detail":"Mousse haute performance Dunlop"},{"label":"Forme","value":"Diamant","detail":"Offensive — nouvelle direction pour Dunlop"}],"targetProfile":"Joueurs intermédiaires offensifs. La nouvelle génération Dunlop."},{"id":"head-extreme-pro-2024","name":"Head Extreme Pro 2024","shortName":"Extreme Pro 24","brand":"Head","shape":"Goutte d\'eau","weight":"355-370g","balance":"Mi-haut","surface":"Carbon 3K","core":"Power foam","antivib":"Auxetic + Innegra","price":"180-250€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-pro-2024.png","year":2024,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Version 2024 de l\'Extreme Pro. Même ADN polyvalent avec Power Foam et double anti-vibrations. Excellent rapport qualité/prix en déstockage.","editorial":"L\'Extreme Pro 2024 est la version pré-Auxetic de la gamme offensive Head. Carbone 3K et Power Foam en goutte d\'eau. Pas d\'Auxetic 2.0 sur ce millésime — le sweet spot est donc naturel, sans élargissement technologique. C\'est du Head classique, bien construit, avec le toucher caractéristique de la marque. Un bon plan en déstockage.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone à maillage serré — réactivité et toucher Head classique"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante signature Head"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence offensive — le sweet spot Head classique"}],"targetProfile":"Joueurs avancés polyvalents. Bon rapport qualité/prix en fin de série."},{"id":"head-monster-kids-2024","name":"Head Monster Kids 2024","shortName":"Monster Kids 24","brand":"Head","shape":"Ronde","weight":"300g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"Innegra","price":"60-70€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-monster-kids-2024.png","year":2024,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":9,"Spin":6,"Maniabilité":10,"Tolérance":9},"verdict":"Ultra-légère et ultra-tolérante, la raquette parfaite pour les tout-petits ou vrais débutants. Zéro puissance mais un maximum de facilité et de confort.","editorial":"La Monster Kids 2024 est la pala Head pour les plus petits. Fibre de verre et Soft EVA en ronde — ultra-légère, ultra-confortable, ultra-fun. Le design Monster plaît aux enfants. Pour les 4-8 ans qui découvrent le padel.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le plus léger possible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — zéro vibration"},{"label":"Forme","value":"Ronde","detail":"Fun et tolérant — design Monster"}],"targetProfile":"Juniors 4-8 ans. Le fun Head pour les plus petits."},{"id":"head-speed-motion-2024","name":"Head Speed Motion 2024","shortName":"Speed Motion 24","brand":"Head","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Fiberglass","core":"Comfort foam","antivib":"Innegra","price":"80-120€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-speed-motion-2024.png","year":2024,"category":"debutant","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8.5,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Ancienne Speed Motion à prix réduit. Fibre de verre et Innegra pour un excellent confort. Très bon choix débutant en promotion.","editorial":"La Speed Motion 2024 est la débutante Head millésime 2024. Fibre de verre et Comfort Foam en goutte d\'eau. La Comfort Foam — la mousse la plus souple de Head — est un choix parfait pour l\'apprentissage. Bonne affaire en fin de série.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux"},{"label":"Noyau","value":"Comfort Foam","detail":"La mousse la plus confortable de Head — zéro vibration"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence accessible"}],"targetProfile":"Débutants. Le confort Head à petit prix en déstockage."},{"id":"head-extreme-jr-2025","name":"Head Extreme Junior 2025","shortName":"Extreme Jr 25","brand":"Head","shape":"Goutte d\'eau","weight":"320g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"Innegra","price":"120-140€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":7,"Contrôle":8,"Confort":9,"Spin":6,"Maniabilité":9.5,"Tolérance":7.5},"verdict":"Version junior de l\'Extreme avec mousse Power Foam dosée et anti-vibrations Innegra. Plus de puissance que la Speed Junior tout en gardant un excellent confort.","editorial":"L\'Extreme Jr 2025 est la junior polyvalente de Head. Fibre de verre et Soft EVA en goutte d\'eau. Pour les juniors qui commencent à vouloir un peu plus de punch que la Monster Kids.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — junior polyvalent"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort junior"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — la junior qui grandit"}],"targetProfile":"Juniors 8-14 ans. La Head junior polyvalente."},{"id":"head-extreme-motion-2025","name":"Head Extreme Motion 2025","shortName":"Extreme Motion","brand":"Head","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Hybrid carbon/fiberglass","core":"Comfort foam","antivib":"Auxetic 2.0","price":"130-170€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-motion-2026.png","year":2025,"category":"debutant","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Raquette polyvalente et confortable pour débuter ou reprendre. Le système Auxetic 2.0 absorbe bien les vibrations, forme goutte d\'eau équilibrée.","editorial":"L\'Extreme Motion 2025 est la débutante dynamique de Head. Hybride carbone/fibre de verre et Comfort Foam en goutte d\'eau. Un cran au-dessus de la Speed Motion avec un peu plus de rigidité. Pour les débutants sportifs qui progressent vite.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible — un soupçon de rigidité"},{"label":"Noyau","value":"Comfort Foam","detail":"Mousse confortable Head"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence pour les débutants dynamiques"}],"targetProfile":"Débutants sportifs. L\'Extreme pour progresser vite."},{"id":"head-extreme-one-2025","name":"Head Extreme One 2025","shortName":"Extreme One 25","brand":"Head","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Hybrid carbon/fiberglass","core":"Comfort foam","antivib":"Auxetic 2.0","price":"100-140€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-one-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Forme ronde ultra-tolérante avec anti-vibrations Auxetic. Le choix le plus sûr pour un débutant adulte qui veut apprendre sans douleur.","editorial":"L\'Extreme One 2025 est une autre option débutante de Head. Hybride carbone/fibre de verre et Comfort Foam en ronde. Même matériaux que l\'Extreme Motion mais en ronde pour plus de tolérance. Le choix sûr pour les vrais débutants.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible avec un peu de rigidité"},{"label":"Noyau","value":"Comfort Foam","detail":"Maximum de confort Head"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale pour l\'apprentissage"}],"targetProfile":"Débutants. Le confort Head en format ronde ultra-tolérant."},{"id":"head-extreme-pro-2025","name":"Head Extreme Pro 2025","shortName":"Extreme Pro 25","brand":"Head","shape":"Diamant","weight":"355-370g","balance":"Mi-haut","surface":"Hybrid carbon/fiberglass","core":"Power foam","antivib":"Auxetic 2.0 + Innegra","price":"200-260€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-pro-2024.png","year":2025,"category":"avance","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":8,"Maniabilité":6,"Tolérance":5},"verdict":"Goutte d\'eau polyvalente avec un bon compromis puissance/contrôle. La mousse Power Foam apporte de la réactivité compensée par les anti-vibrations Auxetic.","editorial":"L\'Extreme Pro 2025 fait évoluer la gamme avec un hybride carbone/fibre de verre qui adoucit la frappe tout en gardant du punch. Le Power Foam assure l\'explosivité. C\'est l\'Extreme pour les joueurs qui veulent de la puissance sans la sécheresse du carbone pur. Plus accessible que la Coello Pro, moins radicale que la Delta Elite.","techHighlights":[{"label":"Surface","value":"Hybrid carbon/fiberglass","detail":"Mix carbone/fibre de verre — rigidité atténuée pour un toucher plus doux"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour compenser la souplesse de la surface hybride"},{"label":"Forme","value":"Diamant","detail":"Orientation offensive avec sweet spot haut"}],"targetProfile":"Joueurs avancés offensifs qui veulent du punch avec du toucher. Alternative plus douce à la Coello Pro."},{"id":"head-gravity-motion-2025","name":"Head Gravity Motion 2025","shortName":"Gravity Motion 25","brand":"Head","shape":"Ronde","weight":"355g","balance":"Moyen (265mm)","surface":"Fiberglass","core":"Control Foam","antivib":"Auxetic 2.0","price":"150-200€","player":"Marina Guinart","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-motion-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":6,"Contrôle":8,"Confort":8.5,"Spin":6.5,"Maniabilité":8.5,"Tolérance":8.5},"verdict":"Ronde légère et très maniable. Fiberglass doux et large sweet spot. Excellente pour joueuses ou joueurs intermédiaires cherchant contrôle et confort maximal.","editorial":"La Gravity Motion 2025 est l\'intermédiaire de la gamme contrôle Head. Fibre de verre et Control Foam en ronde. On retrouve la mousse de contrôle signature Head — la même que sur la Gravity Pro — dans un format accessible. C\'est la meilleure initiation au toucher Head pour les joueurs de contrôle en développement.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher souple et accessible"},{"label":"Noyau","value":"Control Foam","detail":"Même mousse de contrôle que la Gravity Pro — le toucher Head dès l\'intermédiaire"},{"label":"Forme","value":"Ronde","detail":"Contrôle — la forme naturelle de la gamme Gravity"}],"targetProfile":"Joueurs intermédiaires défensifs. Découverte du toucher Control Foam Head."},{"id":"head-gravity-pro-2025","name":"Head Gravity Pro 2025","shortName":"Gravity Pro 25","brand":"Head","shape":"Ronde","weight":"365g","balance":"Moyen (265mm)","surface":"Carbon + Fiberglass Hybrid","core":"Control Foam","antivib":"Auxetic 2.0","price":"200-260€","player":"Daniel Windahl","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-pro-2025.png","year":2025,"category":"avance","scores":{"Puissance":6.5,"Contrôle":9,"Confort":8,"Spin":7.5,"Maniabilité":7.5,"Tolérance":8},"verdict":"Référence contrôle Head en forme ronde. Grand sweet spot, stable sur les blocks et les lobs. Pour joueurs de droite qui construisent le point avec patience et placement.","editorial":"La Gravity Pro 2025 est le bijou technologique de Head pour les joueurs de contrôle haut niveau. Son hybride carbone/fibre de verre crée un flex unique à l\'impact : la face absorbe puis restitue, offrant un toucher presque organique que les puristes adorent. L\'Auxetic 2.0, technologie issue du tennis, déforme la structure au point d\'impact pour agrandir virtuellement le sweet spot. C\'est la pala la plus intelligente du marché.","techHighlights":[{"label":"Surface","value":"Carbon + Fiberglass Hybrid","detail":"Le mix carbone/fibre de verre crée un flex contrôlé unique — rigide au centre, souple en périphérie"},{"label":"Noyau","value":"Control Foam","detail":"Mousse propriétaire Head à cellules fermées, optimisée pour le toucher et la précision des volées"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Structure auxétique qui s\'élargit à l\'impact, agrandissant le sweet spot de 23% selon Head"},{"label":"Forme","value":"Ronde","detail":"Sweet spot centré pour une tolérance maximale, pardon assuré sur les frappes décentrées"},{"label":"Sweet spot","value":"Élargi par Auxetic","detail":"La technologie auxétique compense les frappes excentrées en déformant la structure localement"}],"targetProfile":"Joueurs experts orientés contrôle. Le défenseur technique qui construit les points avec patience et précision. Parfaite aussi pour le joueur de droite qui enchaîne volées et bandejas.","proPlayerInfo":{"name":"Daniel Windahl","context":"Joueur suédois du circuit Premier Padel, reconnu pour son jeu tactique et son toucher exceptionnel."}},{"id":"head-gravity-team-light-2025","name":"Head Gravity Team Light 2025","shortName":"Gravity Team Light 25","brand":"Head","shape":"Ronde","weight":"340g","balance":"Moyen-Haut (270mm)","surface":"Fiberglass","core":"Control Foam","antivib":"Graphene 360+","price":"100-140€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-gravity-team-light-2024.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":7.5,"Confort":9,"Spin":5.5,"Maniabilité":9.5,"Tolérance":9},"verdict":"La plus légère de la gamme Gravity. Ronde ultra-confortable, idéale pour débutants, femmes et joueurs légers. Facile à manœuvrer avec bon contrôle.","editorial":"La Gravity Team Light 2025 est la débutante légère de la gamme contrôle. Fibre de verre et Control Foam en ronde. Le choix de la Control Foam même à ce niveau est remarquable — les débutants bénéficient du même type de toucher que la Gravity Pro. Version Light pour la maniabilité.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux"},{"label":"Noyau","value":"Control Foam","detail":"Mousse de contrôle Head — toucher premium même pour les débutants"},{"label":"Forme","value":"Ronde","detail":"Contrôle et tolérance — version Light"}],"targetProfile":"Débutants orientés contrôle. Le toucher Control Foam dès les premiers matchs."},{"id":"head-one-ultralight-2025","name":"Head One Ultralight 2025","shortName":"One Ultralight","brand":"Head","shape":"Ronde","weight":"300g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"Innegra","price":"107-165€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-one-ultralight-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":9,"Spin":6,"Maniabilité":10,"Tolérance":9},"verdict":"Ultra-légère avec anti-vibrations Innegra. Forme ronde pour une tolérance maximale. Idéale pour les juniors ou adultes recherchant un poids plume.","editorial":"La One Ultralight 2025 est la junior ultra-légère de Head. Fibre de verre et Soft EVA en ronde. La plus légère de la gamme junior Head — parfaite pour les petits gabarits.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — ultra-léger"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort maximal"},{"label":"Forme","value":"Ronde","detail":"Tolérance — la plus légère pour les petits gabarits"}],"targetProfile":"Juniors légers. La Head la plus légère pour les petits gabarits."},{"id":"head-speed-jr-2025","name":"Head Speed Junior 2025","shortName":"Speed Junior 25","brand":"Head","shape":"Goutte d\'eau","weight":"310g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"Innegra","price":"65-105€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-speed-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":6.5,"Contrôle":8,"Confort":9,"Spin":6,"Maniabilité":9.5,"Tolérance":7.5},"verdict":"Raquette junior Head avec technologie anti-vibrations Innegra. Forme goutte d\'eau pour un bon compromis puissance/contrôle avec un confort supérieur.","editorial":"La Speed Jr 2025 est la junior de la gamme Speed. Fibre de verre et Soft EVA en goutte d\'eau. L\'esprit rapide de la gamme Speed adapté aux jeunes joueurs. Pour les juniors dynamiques.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — Speed junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort et maniabilité"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence rapide pour les juniors"}],"targetProfile":"Juniors dynamiques. La Speed pour les jeunes joueurs rapides."},{"id":"head-speed-motion-2025","name":"Head Speed Motion 2025","shortName":"Speed Motion 25","brand":"Head","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Fiberglass","core":"Comfort foam","antivib":"Innegra","price":"100-130€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-speed-motion-2025.png","year":2025,"category":"debutant","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8.5,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Raquette débutant avec fibre de verre douce et anti-vibrations Innegra. Bon équilibre entre facilité et progression.","editorial":"La Speed Motion 2025 est la débutante classique de Head pour 2025. Fibre de verre et Comfort Foam en goutte d\'eau. Mise à jour cosmétique et de construction par rapport à la 2024. La formule éprouvée pour apprendre le padel avec Head.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — la base Head"},{"label":"Noyau","value":"Comfort Foam","detail":"Mousse ultra-confortable Head"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence accessible"}],"targetProfile":"Débutants. La Speed Motion éprouvée pour apprendre."},{"id":"head-speed-one-x-2025","name":"Head Speed One X 2025","shortName":"Speed One X 25","brand":"Head","shape":"Goutte d\'eau","weight":"350g","balance":"Moyen","surface":"2D Carbon","core":"Power Foam","antivib":"DAMP+ + Auxetic 2.0","price":"280-340€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-speed-one-x-2025.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":8,"Confort":8,"Spin":7.5,"Maniabilité":8,"Tolérance":7.5},"verdict":"Ultra-confortable grâce au DAMP+ dans le cœur. Polyvalente légère avec bon rendement. Pour joueurs confirmés qui cherchent sensations et protection du bras.","editorial":"La Speed One X 2025 est le haut de gamme polyvalent de Head — la pala couteau suisse. Son carbone 2D offre une rigidité homogène sur toute la surface de frappe, sans point dur. Le Power Foam propulse la balle avec autorité, tandis que le double système DAMP+ et Auxetic 2.0 filtre chaque vibration indésirable. Le résultat est une pala qui fait tout bien : smash, volée, bandeja, défense. Aucune faiblesse, aucun compromis.","techHighlights":[{"label":"Surface","value":"Carbon 2D","detail":"Carbone à tissage bidimensionnel — rigidité uniforme sur toute la face, pas de zones mortes"},{"label":"Noyau","value":"Power Foam","detail":"Mousse haute performance Head — retour d\'énergie optimal pour les frappes offensives"},{"label":"Technologie","value":"DAMP+ et Auxetic 2.0","detail":"Double technologie : DAMP+ absorbe les vibrations basses fréquences, Auxetic élargit le sweet spot"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence incarnée — sweet spot entre centre et haut de la pala"},{"label":"Sweet spot","value":"Large et tolérant","detail":"La combinaison forme goutte + Auxetic crée l\'une des zones de frappe les plus permissives du marché"}],"targetProfile":"Joueurs avancés à experts qui ne veulent aucune faiblesse. Le joueur complet qui change de rôle pendant le point — défense, transition, attaque. La pala de ceux qui refusent de choisir.","proPlayerInfo":null},{"id":"head-speed-pro-2025","name":"Head Speed Pro 2025","shortName":"Speed Pro 25","brand":"Head","shape":"Goutte d\'eau","weight":"355-370g","balance":"Moyen","surface":"Carbon","core":"Standard EVA","antivib":"Innegra","price":"180-230€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-speed-pro-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6.5,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Raquette de vitesse avec bon équilibre. Le carbone offre de la réactivité, l\'Innegra adoucit les vibrations. Pour joueurs intermédiaires cherchant la polyvalence.","editorial":"La Speed Pro 2025 est l\'intermédiaire de la gamme rapide Head. Carbone standard et EVA standard en goutte d\'eau. Construction classique, sans technologie spectaculaire, mais le toucher Head est présent. Bonne pala de progression pour les joueurs polyvalents rapides.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher Head direct"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence rapide"}],"targetProfile":"Joueurs intermédiaires polyvalents. La Speed pour progresser."},{"id":"head-speed-team-2025","name":"Head Speed Team 2025","shortName":"Speed Team 25","brand":"Head","shape":"Goutte d\'eau","weight":"360g","balance":"Moyen (265mm)","surface":"Fiberglass","core":"Power Foam","antivib":"Auxetic 2.0","price":"140-180€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-speed-team-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":8,"Spin":6.5,"Maniabilité":7.5,"Tolérance":8},"verdict":"Polyvalente accessible en fiberglass. Bon équilibre puissance/contrôle/confort pour joueurs intermédiaires. Facile à prendre en main avec un toucher doux.","editorial":"La Speed Team 2025 est la version accessible de la gamme Speed. Fibre de verre et Power Foam en goutte d\'eau — matériaux simples mais mousse performante. C\'est l\'entrée dans la gamme Speed pour les joueurs qui débutent le carbone ou qui veulent un toucher plus souple.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher doux et accessible"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive — du punch malgré la surface souple"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Speed"}],"targetProfile":"Joueurs intermédiaires. L\'entrée dans la gamme Speed Head."},{"id":"head-coello-junior-2026","name":"Head Coello Junior 2026","shortName":"Coello Junior 26","brand":"Head","shape":"Goutte d\'eau","weight":"310-320g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"Innegra + Auxetic 2.0","price":"200-250€","player":"Arturo Coello","imageUrl":"https://www.padelful.com/images/rackets/head-coello-junior-2026.png","year":2026,"category":"junior","scores":{"Puissance":7,"Contrôle":8,"Confort":9,"Spin":6,"Maniabilité":9.5,"Tolérance":7.5},"verdict":"Raquette junior haut de gamme avec technologies anti-vibrations premium. Très légère et confortable, idéale pour l\'apprentissage avec un excellent confort de frappe.","editorial":"La Coello Junior 2026 est la junior de la gamme du champion. Fibre de verre et Soft EVA en goutte d\'eau. Le rêve Coello pour les jeunes fans. Même design que la version adulte, format adapté.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — Coello junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — protection junior"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le rêve Coello en format junior"}],"targetProfile":"Juniors fans de Coello. Le prestige du champion pour les jeunes."},{"id":"head-coello-motion-2026","name":"Head Coello Motion 2026","shortName":"Coello Motion","brand":"Head","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon 3K","core":"Power foam","antivib":"Auxetic 2.0","price":"280-350€","player":"Arturo Coello","imageUrl":"https://www.padelful.com/images/rackets/head-coello-motion-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Signature Arturo Coello. Goutte d\'eau équilibrée avec Power Foam et Auxetic pour un mix puissance/toucher de balle remarquable.","editorial":"La Coello Motion 2026 est la version accessible de la gamme Arturo Coello. Même carbone 3K, même Power Foam, mais en forme goutte d\'eau pour plus de polyvalence. C\'est la Coello pour les joueurs avancés qui veulent la technologie et le prestige de la gamme sans l\'exigence de la forme diamant. Le tremplin parfait vers la Coello Pro.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Même carbone que la Coello Pro — réactivité et toucher premium"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante — ADN offensif de la gamme Coello"},{"label":"Forme","value":"Goutte d\'eau","detail":"Plus de tolérance que la Pro diamant — sweet spot intermédiaire"},{"label":"Gamme","value":"Coello accessible","detail":"Même technologie, format plus polyvalent et tolérant"}],"targetProfile":"Joueurs avancés qui veulent la gamme Coello sans l\'exigence de la diamant."},{"id":"head-coello-pro-2026","name":"Head Coello Pro 2026","shortName":"Coello Pro 26","brand":"Head","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon 3K","core":"Power foam","antivib":"Auxetic 2.0","price":"350-400€","player":"Arturo Coello","imageUrl":"https://www.padelful.com/images/rackets/head-coello-pro-2025.png","year":2026,"category":"expert","scores":{"Puissance":8,"Contrôle":7.5,"Confort":6,"Spin":8.5,"Maniabilité":6.5,"Tolérance":6.5},"verdict":"La raquette signature d\'Arturo Coello version pro. Carbone 3K premium avec Dual Spin pour un effet dévastateur. Réservée aux experts.","editorial":"La Coello Pro 2026 porte le nom du prodige espagnol qui a révolutionné le padel à 19 ans. Head a conçu une arme de compétition pure : carbone 3K pour la réactivité, Power Foam pour l\'explosivité, et l\'incontournable Auxetic 2.0 pour pardonner les frappes légèrement décentrées. La forme diamant assumée envoie un message clair — cette pala est faite pour finir les points, pas pour les subir.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone à maillage serré (3K = 3000 filaments par mèche) — réactivité immédiate, toucher direct"},{"label":"Noyau","value":"Power Foam","detail":"Mousse haute densité Head — conçue pour maximiser la sortie de balle au smash"},{"label":"Technologie","value":"Auxetic 2.0","detail":"La structure auxétique s\'élargit à l\'impact, crucial sur une diamant où le sweet spot est naturellement réduit"},{"label":"Forme","value":"Diamant","detail":"Équilibre en tête, puissance maximale — le choix des attaquants qui jouent en l\'air"},{"label":"Sweet spot","value":"Compensé par Auxetic","detail":"Le sweet spot diamant naturellement haut et étroit est élargi par la technologie Auxetic"}],"targetProfile":"Joueurs experts offensifs. Le joueur de gauche qui monte au filet et finit au smash. Nécessite un bon niveau technique pour exploiter la forme diamant.","proPlayerInfo":{"name":"Arturo Coello","context":"Plus jeune N°1 mondial de l\'histoire du padel (19 ans). Son jeu explosif et ses réflexes hors norme incarnent parfaitement cette pala."}},{"name":"Head Coello Team 2026","shortName":"Coello Team 26","brand":"Head","shape":"Diamant","weight":"360g","balance":"Haute (270mm)","surface":"Fibre de verre + Power Foam","core":"Power Foam","antivib":true,"price":"200-260€","player":"Intermédiaire-Avancé offensif","imageUrl":"https://www.padelful.com/images/rackets/head-coello-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":6,"Confort":7,"Spin":7,"Maniabilité":7,"Tolérance":6},"verdict":"Version accessible de la gamme Coello. Diamant fibre de verre pour puissance facile et confort.","id":"head-coello-team-2026","editorial":"La Coello Team 2026 est la version intermédiaire de la gamme Arturo Coello. Fibre de verre avec Power Foam en diamant — l\'esprit offensif Coello dans des matériaux accessibles. Le prestige du nom, la philosophie d\'attaque, sans l\'exigence du carbone 3K. Le rêve Coello accessible.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Power Foam","detail":"Construction accessible avec mousse performante"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive — explosivité même en fibre de verre"},{"label":"Forme","value":"Diamant","detail":"L\'ADN offensif Coello — diamant même en intermédiaire"}],"targetProfile":"Joueurs intermédiaires offensifs. Le prestige Coello dans un format accessible."},{"name":"Head Coello Vibe 2026","shortName":"Coello Vibe 26","brand":"Head","shape":"Goutte d\'eau","weight":"355g","balance":"Moyenne","surface":"Fibre de verre","core":"Power Foam","antivib":true,"price":"120-160€","player":"Débutant-Intermédiaire","imageUrl":"https://www.padelful.com/images/rackets/head-coello-vibe-2025.png","year":2026,"category":"debutant","scores":{"Puissance":6,"Contrôle":8.5,"Maniabilité":7,"Confort":8.5,"Spin":7,"Tolérance":7},"verdict":"Entrée de gamme Coello premium. Oversized teardrop, large sweet spot, tolérante et confortable.","id":"head-coello-vibe-2026","editorial":"La Coello Vibe 2026 est la débutante de la gamme Coello — le nom du champion sur une pala d\'apprentissage. Fibre de verre et Power Foam en goutte d\'eau. Le Power Foam apporte plus de réactivité que le Comfort Foam des autres débutantes Head. Pour les débutants qui veulent le prestige Coello avec du répondant.","techHighlights":[{"label":"Surface","value":"Fibre de verre","detail":"Construction accessible"},{"label":"Noyau","value":"Power Foam","detail":"Plus réactif que le Comfort Foam — du punch pour les débutants"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence avec le prestige Coello"}],"targetProfile":"Débutants dynamiques. Le nom Coello sur une pala d\'apprentissage réactive."},{"id":"head-delta-elite-2026","name":"Head Delta Elite 2026","shortName":"Delta Elite 26","brand":"Head","shape":"Diamant","weight":"370-385g","balance":"Haut","surface":"Carbon 3K","core":"Hard EVA","antivib":"Auxetic 2.0","price":"350-420€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-delta-elite-2022.png","year":2026,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":7.5,"Maniabilité":5,"Tolérance":5},"verdict":"Diamant ultime de Head. La plus puissante de la gamme avec carbone premium et EVA dure. Pour les attaquants d\'élite exclusivement.","editorial":"La Delta Elite 2026 est la déclaration de guerre de Head dans le segment puissance premium. Le carbone haute densité sur la face délivre une frappe sèche et claquante, typique des diamants haut de gamme. L\'Auxetic 2.0, ici plus utile que jamais, rattrape les petites erreurs de centrage inhérentes à la forme diamant. Le Power Foam complète l\'arsenal offensif. C\'est la rivale directe de la Metalbone HRD, avec un peu plus de pardon.","techHighlights":[{"label":"Surface","value":"Carbone Haute Densité","detail":"Couche de carbone rigide et homogène pour un transfert d\'énergie maximal au smash"},{"label":"Noyau","value":"Power Foam","detail":"Mousse dense et réactive — retour d\'énergie immédiat pour les frappes agressives"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Crucial sur une diamant : la déformation auxétique élargit le sweet spot là où c\'est le plus nécessaire"},{"label":"Forme","value":"Diamant","detail":"Équilibre en tête, puissance maximale — la forme des finisseurs"},{"label":"Sweet spot","value":"Diamant corrigé par Auxetic","detail":"Naturellement haut et étroit, mais l\'Auxetic offre plus de marge qu\'une diamant classique"}],"targetProfile":"Joueurs experts offensifs qui veulent la puissance du diamant avec un filet de sécurité. Alternative intéressante à la Metalbone HRD pour ceux qui veulent un peu plus de tolérance.","proPlayerInfo":null},{"id":"head-delta-pro-2026","name":"Head Delta Pro 2026","shortName":"Delta Pro 26","brand":"Head","shape":"Diamant","weight":"365-380g","balance":"Haut","surface":"Carbon 3K","core":"Power foam","antivib":"Auxetic 2.0","price":"300-380€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-delta-pro-2022.png","year":2026,"category":"avance","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":7.5,"Maniabilité":5.5,"Tolérance":5},"verdict":"Diamant ultra-offensive. Puissance maximale avec Power Foam et balance haute. Réservée aux attaquants confirmés avec bonne technique.","editorial":"La Delta Pro 2026 est la version standard de la gamme puissance Head. Carbone 3K et Power Foam en diamant — la recette offensive classique de Head. Pas d\'Auxetic ici, contrairement à l\'Elite. C\'est la Delta pour les joueurs qui veulent de la puissance pure sans assistance technologique. Le toucher est plus direct, plus honnête.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone classique Head — réactivité directe"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour l\'attaque"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — sans l\'Auxetic de l\'Elite"}],"targetProfile":"Joueurs avancés offensifs qui veulent une diamant pure sans technologie d\'assistance."},{"id":"head-elite-ltd-2026","name":"Head Elite LTD 2026","shortName":"Elite LTD 26","brand":"Head","shape":"Goutte d\'eau","weight":"355g","balance":"Moyen","surface":"Hybrid Woven","core":"Power Foam","antivib":"Auxetic 2.0","price":"250-300€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-elite-ltd-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":8,"Confort":7.5,"Spin":7.5,"Maniabilité":8,"Tolérance":7.5},"verdict":"Édition limitée 25ème anniversaire Head Padel (1000 exemplaires). Polyvalente avec technologies modernes dans un design rétro années 80. Collector.","editorial":"La Elite LTD 2026 est l\'édition limitée polyvalente de Head. Hybrid Woven en surface — le même tissage que sur la Gravity Pro 2026 — avec Power Foam pour l\'explosivité. L\'Auxetic 2.0 élargit le sweet spot. Forme goutte d\'eau pour la polyvalence. C\'est une pala premium qui fait tout bien, en édition limitée pour les collectionneurs et les joueurs exigeants.","techHighlights":[{"label":"Surface","value":"Hybrid Woven","detail":"Tissage hybride premium — même technologie que la Gravity Pro"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour les accélérations"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Sweet spot élargi pour la tolérance"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence premium en édition limitée"}],"targetProfile":"Joueurs avancés polyvalents exigeants. Édition limitée avec technologie Gravity Pro."},{"id":"head-evo-extreme-2026","name":"Head Evo Extreme 2026","shortName":"Evo Extreme 26","brand":"Head","shape":"Goutte d\'eau","weight":"340-360g","balance":"Moyen","surface":"Fiberglass","core":"EVA Soft","antivib":"Graphene 360+","price":"70-100€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-extreme-evo-2026.png","year":2026,"category":"debutant","scores":{"Puissance":6,"Contrôle":7,"Confort":9,"Spin":5,"Maniabilité":8.5,"Tolérance":9},"verdict":"Raquette récréative ultra-accessible. Grand sweet spot, puissance facile et maximum de confort pour débuter sereinement et prendre du plaisir.","editorial":"L\'Evo Extreme 2026 est la pala la plus accessible de la gamme Extreme. Fibre de verre et EVA Soft en goutte d\'eau. Construction simple et légère pour les tout premiers matchs. C\'est le point d\'entrée le plus économique dans l\'univers Head.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le plus accessible Head"},{"label":"Noyau","value":"EVA Soft","detail":"Mousse très souple — confort absolu"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence pour les premiers coups"}],"targetProfile":"Débutants. L\'entrée la plus économique chez Head."},{"name":"Head Extreme Motion 2026","shortName":"Extreme Motion 26","brand":"Head","shape":"Diamant","weight":"355g","balance":"Haute (268mm)","surface":"Carbone UD + Auxetic","core":"Power Foam","antivib":true,"price":"220-280€","player":"Avancé offensif","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-motion-2026.png","year":2026,"category":"avance","scores":{"Puissance":8.5,"Contrôle":6,"Maniabilité":6.5,"Confort":6.5,"Spin":8,"Tolérance":6},"verdict":"Extreme allégée. Diamant offensif + Auxetic pour punch et maniabilité. Vitesse de bras.","id":"head-extreme-motion-2026","editorial":"L\'Extreme Motion 2026 est la version avancée de la gamme Extreme. Carbone UD (unidirectionnel) avec Auxetic — une combinaison unique qui offre rigidité dans l\'axe de frappe et flex en torsion. Le Power Foam assure la puissance. Forme diamant pour l\'attaque. C\'est l\'Extreme la plus technologique de la gamme, juste en dessous de la Coello Pro.","techHighlights":[{"label":"Surface","value":"Carbone UD + Auxetic","detail":"Carbone unidirectionnel — rigide dans l\'axe de frappe, flexible en torsion"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour les frappes offensives"},{"label":"Technologie","value":"Auxetic","detail":"Élargissement du sweet spot par déformation auxétique"},{"label":"Forme","value":"Diamant","detail":"Offensive avec technologie Auxetic pour la tolérance"}],"targetProfile":"Joueurs avancés offensifs qui veulent la technologie Auxetic en diamant."},{"name":"Head Extreme One 2026","shortName":"Extreme One 26","brand":"Head","shape":"Goutte d\'eau","weight":"345g","balance":"Moyenne (262mm)","surface":"Carbone 2D + Extreme Spin","core":"Power Foam","antivib":true,"price":"130-180€","player":"Intermédiaire polyvalent","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-one-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":7.5,"Spin":7,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Plus légère des Extreme. Goutte d\'eau maniable avec carbone 2D. Bonne entrée gamme Extreme.","id":"head-extreme-one-2026","editorial":"L\'Extreme One 2026 est l\'intermédiaire de la gamme Extreme. Carbone 2D avec Extreme Spin — une texture de surface pour les effets — et Power Foam en goutte d\'eau. C\'est la pala des joueurs intermédiaires qui veulent commencer à travailler les effets avec une surface carbone accessible.","techHighlights":[{"label":"Surface","value":"Carbone 2D + Extreme Spin","detail":"Carbone avec texture d\'accroche — effets accessibles"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — apprendre les effets sans pression"}],"targetProfile":"Joueurs intermédiaires qui veulent développer leurs effets."},{"id":"head-extreme-pro-2026","name":"Head Extreme Pro 2026","shortName":"Extreme Pro 26","brand":"Head","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon 3K","core":"Power foam","antivib":"Auxetic 2.0 + Innegra","price":"280-350€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-pro-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6.5,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"La référence polyvalente pour joueurs avancés. Double anti-vibrations Auxetic+Innegra avec mousse Power Foam pour une frappe précise et puissante.","editorial":"L\'Extreme Pro 2026 est la version goutte d\'eau de la gamme Extreme — même carbone 3K et Power Foam que les Coello, mais dans un format plus polyvalent. C\'est la pala du joueur offensif qui veut garder de la défense. Le sweet spot goutte offre plus de tolérance que le diamant tout en conservant du punch dans les frappes hautes.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone premium Head — même qualité que la gamme Coello"},{"label":"Noyau","value":"Power Foam","detail":"Mousse puissante pour les accélérations"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence offensive — punch avec tolérance"}],"targetProfile":"Joueurs avancés polyvalents-offensifs. L\'Extreme pour attaquer tout en gardant de la sécurité."},{"name":"Head Extreme Team 2026","shortName":"Extreme Team 26","brand":"Head","shape":"Diamant","weight":"360g","balance":"Haute (268mm)","surface":"Fibre de verre + Extreme Spin","core":"Power Foam","antivib":true,"price":"150-200€","player":"Intermédiaire offensif","imageUrl":"https://www.padelful.com/images/rackets/head-extreme-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":6.5,"Maniabilité":5,"Confort":9,"Spin":6.5,"Tolérance":6},"verdict":"Diamant accessible avec fibre de verre pour tolérance. Puissance offensive pardonnante.","id":"head-extreme-team-2026","editorial":"L\'Extreme Team 2026 est la version diamant intermédiaire de la gamme Extreme. Fibre de verre avec Extreme Spin et Power Foam. Plus accessible que l\'Extreme One carbone, mais en diamant pour l\'orientation offensive. C\'est la pala des jeunes joueurs offensifs qui progressent vite.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Extreme Spin","detail":"Fibre de verre avec texture d\'effets — offensive accessible"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive pour le punch"},{"label":"Forme","value":"Diamant","detail":"Offensive — pour les intermédiaires ambitieux"}],"targetProfile":"Joueurs intermédiaires offensifs en progression rapide."},{"name":"Head Gravity Motion 2026","shortName":"Gravity Motion 26","brand":"Head","shape":"Ronde","weight":"355g","balance":"Moyenne (265mm)","surface":"Fibre de verre + Auxetic 2.0","core":"Control Foam","antivib":true,"price":"170-220€","player":"Intermédiaire contrôle","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-motion-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":9,"Spin":6.5,"Maniabilité":8.5,"Tolérance":9},"verdict":"Gravity allégée. Ronde fibre de verre pour sweet spot large et toucher doux.","id":"head-gravity-motion-2026","editorial":"La Gravity Motion 2026 fait évoluer l\'intermédiaire contrôle avec l\'Auxetic 2.0 — une première à ce niveau de gamme. Fibre de verre avec Auxetic et Control Foam en ronde. Le sweet spot élargi par Auxetic est un vrai plus pour les joueurs qui développent leur jeu de contrôle.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Auxetic 2.0","detail":"Auxetic rare à ce prix — sweet spot élargi pour la progression"},{"label":"Noyau","value":"Control Foam","detail":"Mousse de contrôle Head — toucher premium dès l\'intermédiaire"},{"label":"Forme","value":"Ronde","detail":"Contrôle maximum avec Auxetic"}],"targetProfile":"Joueurs intermédiaires défensifs. L\'Auxetic 2.0 accessible — excellent rapport technologie/prix."},{"name":"Head Gravity Pro 2026","shortName":"Gravity Pro 26","brand":"Head","shape":"Ronde","weight":"365g","balance":"Moyenne (265mm)","surface":"Carbone Hybrid Woven + Auxetic 2.0","core":"Control Foam","antivib":true,"price":"230-290€","player":"Avancé défensif-contrôle","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-pro-2026.png","year":2026,"category":"avance","scores":{"Puissance":6.5,"Contrôle":9,"Confort":8,"Spin":7,"Maniabilité":7.5,"Tolérance":8.5},"verdict":"Reine du contrôle Head. Ronde Hybrid Woven pour stabilité et toucher premium.","id":"head-gravity-pro-2026","editorial":"La Gravity Pro 2026 fait évoluer la formule gagnante de 2025 avec le carbone Hybrid Woven — un tissage hybride qui mêle fibres de carbone et fibres composites pour un flex encore plus calibré. La Control Foam reste au cœur de la pala pour ce toucher caractéristique que les joueurs de la Gravity adorent. L\'Auxetic 2.0 est toujours là pour élargir le sweet spot. Évolution plutôt que révolution, mais quand la base est aussi bonne, c\'est la bonne stratégie.","techHighlights":[{"label":"Surface","value":"Carbone Hybrid Woven + Auxetic 2.0","detail":"Tissage hybride nouvelle génération — flex optimisé par rapport au modèle 2025"},{"label":"Noyau","value":"Control Foam","detail":"Mousse propriétaire Head — le saint-graal du toucher de balle en padel"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Deuxième génération du système auxétique — sweet spot agrandi, réponse plus homogène"},{"label":"Forme","value":"Ronde","detail":"Maximum de contrôle et de tolérance — la forme des tacticiens"},{"label":"Sweet spot","value":"Le plus large de la gamme Head","detail":"Forme ronde + Auxetic 2.0 = probablement le sweet spot le plus tolérant du marché"}],"targetProfile":"Joueurs experts défensifs ou polyvalents-contrôle. Le joueur de droite ultime, ou le défenseur qui ne rate jamais. Aussi parfaite pour les joueurs avancés qui veulent monter de niveau en toute sécurité.","proPlayerInfo":{"name":"Style Windahl-like","context":"La Gravity Pro est le choix des joueurs tactiques du circuit qui construisent les points avec patience et précision."}},{"name":"Head Gravity Team 2026","shortName":"Gravity Team 26","brand":"Head","shape":"Ronde","weight":"360g","balance":"Moyenne (265mm)","surface":"Fibre de verre + Auxetic 2.0","core":"Control Foam","antivib":true,"price":"140-180€","player":"Intermédiaire","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":6,"Contrôle":8.5,"Confort":8.5,"Spin":6.5,"Maniabilité":8,"Tolérance":8.5},"verdict":"Contrôle confortable. Fibre de verre + Auxetic 2.0 pour tolérance maximale.","id":"head-gravity-team-2026","editorial":"La Gravity Team 2026 est la jumelle de la Gravity Motion avec un positionnement légèrement différent — même fibre de verre Auxetic 2.0 et Control Foam en ronde. Ajustements de poids et d\'équilibre pour un profil légèrement plus polyvalent. Pour les joueurs qui hésitent entre contrôle et polyvalence.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Auxetic 2.0","detail":"Sweet spot élargi par Auxetic — même technologie que la Gravity Pro"},{"label":"Noyau","value":"Control Foam","detail":"Toucher Head signature"},{"label":"Forme","value":"Ronde","detail":"Contrôle polyvalent"}],"targetProfile":"Joueurs intermédiaires contrôle-polyvalents."},{"name":"Head Gravity Team Light 2026","shortName":"Gravity Team Lt 26","brand":"Head","shape":"Ronde","weight":"340g","balance":"Moyenne-haute (270mm)","surface":"Fibre de verre","core":"Control Foam","antivib":true,"price":"100-140€","player":"Débutant","imageUrl":"https://www.padelful.com/images/rackets/head-gravity-team-light-2024.png","year":2026,"category":"debutant","scores":{"Puissance":5,"Contrôle":8,"Confort":9.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Ultra-légère 340g. Ronde pardonnante pour débuter sans fatigue.","id":"head-gravity-team-light-2026","editorial":"La Gravity Team Light 2026 est la mise à jour de la débutante contrôle. Fibre de verre et Control Foam en ronde — même formule éprouvée. Le Control Foam à ce prix reste un argument fort : le toucher premium Head accessible aux débutants.","techHighlights":[{"label":"Surface","value":"Fibre de verre","detail":"Construction accessible et légère"},{"label":"Noyau","value":"Control Foam","detail":"Toucher Control Foam premium pour les débutants"},{"label":"Forme","value":"Ronde","detail":"Contrôle et tolérance — version 2026"}],"targetProfile":"Débutants orientés contrôle. Le toucher Head premium au prix débutant."},{"name":"Head Radical Motion 2026","shortName":"Radical Motion 26","brand":"Head","shape":"Goutte d\'eau","weight":"355g","balance":"Moyenne-basse (260mm)","surface":"Carbone 3K + Auxetic 2.0","core":"Control Foam","antivib":true,"price":"230-290€","player":"Avancé contrôle rapide","imageUrl":"https://www.padelful.com/images/rackets/head-radical-motion-2026.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":7.5,"Maniabilité":7,"Confort":8,"Spin":7.5,"Tolérance":7.5},"verdict":"Radical allégée pour joueurs techniques rapides. Précision + maniabilité.","id":"head-radical-motion-2026","editorial":"La Radical Motion 2026 est la polyvalente-contrôle de Head. Carbone 3K avec Auxetic 2.0 pour le sweet spot élargi, Control Foam pour le toucher. Forme goutte d\'eau pour le compromis. C\'est la pala des joueurs qui construisent les points avec intelligence et qui veulent de la marge d\'erreur. Moins radicale que la Radical Pro, plus technique que la Speed Motion.","techHighlights":[{"label":"Surface","value":"Carbone 3K + Auxetic 2.0","detail":"Carbone premium avec sweet spot élargi par Auxetic"},{"label":"Noyau","value":"Control Foam","detail":"Mousse de contrôle Head — toucher et placement"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Sweet spot élargi pour la tolérance tactique"},{"label":"Forme","value":"Goutte d\'eau","detail":"Compromis contrôle/attaque pour les tacticiens"}],"targetProfile":"Joueurs avancés contrôle-polyvalents. La Radical accessible avec Auxetic."},{"name":"Head Radical Pro 2026","shortName":"Radical Pro 26","brand":"Head","shape":"Goutte d\'eau","weight":"370g","balance":"Moyenne-basse (260mm)","surface":"Carbone 3K Double + Auxetic 2.0","core":"Control Foam","antivib":true,"price":"280-350€","player":"Expert contrôle-puissance","imageUrl":"https://www.padelful.com/images/rackets/head-radical-pro-2026.png","year":2026,"category":"expert","scores":{"Puissance":7.5,"Contrôle":8,"Confort":7,"Spin":8,"Maniabilité":7,"Tolérance":7},"verdict":"Flagship contrôle Head 2026. 3K carbon + balance basse pour précision chirurgicale.","id":"head-radical-pro-2026","editorial":"La Radical Pro 2026 est la pala des tacticiens experts. Le double couche de carbone 3K crée une surface d\'une rigidité calculée — assez ferme pour punir, assez flexible pour placer. L\'Auxetic 2.0 est un atout décisif sur les frappes rapides au filet où la précision est primordiale. La Control Foam confirme l\'orientation : cette pala est faite pour dominer les échanges, pas pour les raccourcir.","techHighlights":[{"label":"Surface","value":"Carbone 3K Double + Auxetic 2.0","detail":"Double couche de carbone 3K — rigidité supérieure au simple 3K, avec l\'Auxetic pour la tolérance"},{"label":"Noyau","value":"Control Foam","detail":"La mousse de contrôle signature Head — toucher doux, placement millimétrique"},{"label":"Forme","value":"Goutte d\'eau","detail":"Sweet spot légèrement relevé — bon compromis entre contrôle et capacité offensive"},{"label":"Sweet spot","value":"Relevé et précis","detail":"La double couche carbone concentre la réponse au centre du sweet spot pour un placement chirurgical"}],"targetProfile":"Joueurs experts contrôle-puissance. Ceux qui construisent les points méthodiquement puis accélèrent au moment parfait.","proPlayerInfo":null},{"name":"Head Radical Team 2026","shortName":"Radical Team 26","brand":"Head","shape":"Goutte d\'eau","weight":"365g","balance":"Basse (255mm)","surface":"Fibre de verre + Auxetic","core":"Control Foam","antivib":true,"price":"180-230€","player":"Intermédiaire-Avancé contrôle","imageUrl":"https://www.padelful.com/images/rackets/head-radical-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8,"Spin":7,"Maniabilité":7.5,"Tolérance":8},"verdict":"Contrôle accessible. Fibre de verre + balance très basse pour maniabilité max.","id":"head-radical-team-2026","editorial":"La Radical Team 2026 est l\'intermédiaire de la gamme tactique Head. Fibre de verre avec Auxetic et Control Foam en goutte d\'eau. Le format goutte offre plus de polyvalence que la Gravity ronde, tout en gardant le toucher Control Foam. C\'est la pala des futurs tacticiens.","techHighlights":[{"label":"Surface","value":"Fibre de verre + Auxetic","detail":"Sweet spot élargi en format intermédiaire"},{"label":"Noyau","value":"Control Foam","detail":"Toucher tactique Head"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence tactique — plus de punch que la Gravity ronde"}],"targetProfile":"Joueurs intermédiaires polyvalents-tactiques. La Radical accessible."},{"name":"Head Radical Team Light 2026","shortName":"Radical Team Lt 26","brand":"Head","shape":"Ronde","weight":"350g","balance":"Basse (255mm)","surface":"Fibre de verre","core":"Control Foam","antivib":true,"price":"130-170€","player":"Débutant-Intermédiaire","imageUrl":"https://www.padelful.com/images/rackets/head-radical-team-light-2026.png","year":2026,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":7.5,"Confort":9,"Spin":6.5,"Maniabilité":9,"Tolérance":8.5},"verdict":"Plus légère Radical. Ronde + fibre de verre + balance basse = confort et contrôle pour débuter.","id":"head-radical-team-light-2026","editorial":"La Radical Team Light 2026 est la débutante tactique de Head. Fibre de verre et Control Foam en ronde. Même mousse que la Gravity Team Light, mais un positionnement légèrement différent dans la gamme. Pour les débutants qui veulent un toucher soigné.","techHighlights":[{"label":"Surface","value":"Fibre de verre","detail":"Construction légère et accessible"},{"label":"Noyau","value":"Control Foam","detail":"Toucher tactique Head"},{"label":"Forme","value":"Ronde","detail":"Tolérance et contrôle"}],"targetProfile":"Débutants. Alternative à la Gravity Team Light avec un design Radical."},{"name":"Head Speed Motion 2026","shortName":"Speed Motion 26","brand":"Head","shape":"Goutte d\'eau","weight":"355g","balance":"Moyenne (265mm)","surface":"Carbone Hybrid Woven + Auxetic 2.0","core":"Power Foam","antivib":true,"price":"230-290€","player":"Avancé polyvalent","imageUrl":"https://www.padelful.com/images/rackets/head-speed-motion-2026.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":8,"Confort":7.5,"Spin":7.5,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Polyvalente rapide. Hybrid Woven pour toucher précis. Couteau suisse Head 2026.","id":"head-speed-motion-2026","editorial":"La Speed Motion 2026 est la polyvalente par excellence de Head — rapide, réactive, adaptable. Le carbone Hybrid Woven avec Auxetic 2.0 offre le même niveau technologique que la Gravity Pro, mais orienté vers plus de punch. Le Power Foam remplace ici la Control Foam pour un retour d\'énergie supérieur. C\'est la pala caméléon : défensive quand il faut l\'être, offensive quand l\'opportunité se présente.","techHighlights":[{"label":"Surface","value":"Carbone Hybrid Woven + Auxetic 2.0","detail":"Tissage hybride dernière génération — flexibilité contrôlée pour un toucher naturel"},{"label":"Noyau","value":"Power Foam","detail":"Plus réactive que la Control Foam — meilleur retour d\'énergie pour les frappes offensives"},{"label":"Technologie","value":"Auxetic 2.0","detail":"Sweet spot élargi pour tolérer les frappes excentrées dans le feu de l\'action"},{"label":"Forme","value":"Goutte d\'eau","detail":"L\'ADN polyvalent — ni trop offensive, ni trop défensive, juste au milieu"},{"label":"Sweet spot","value":"Bien dimensionné","detail":"Goutte + Auxetic = zone de frappe généreuse, adaptée au jeu rapide"}],"targetProfile":"Joueurs avancés polyvalents qui veulent progresser sans se spécialiser. Excellente pala de transition vers le haut niveau.","proPlayerInfo":null},{"name":"Head Speed One X 2026","shortName":"Speed One X 26","brand":"Head","shape":"Goutte d\'eau","weight":"350g","balance":"Moyenne (262mm)","surface":"Carbone 2D + DAMP+","core":"Power Foam","antivib":true,"price":"160-210€","player":"Intermédiaire confort","imageUrl":"https://www.padelful.com/images/rackets/head-speed-one-x-2025.png","year":2026,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":8,"Confort":8,"Spin":7,"Maniabilité":8,"Tolérance":7.5},"verdict":"Confort maximal gamme Speed. DAMP+ pour absorption vibrations. Bras sensibles.","id":"head-speed-one-x-2026","editorial":"La Speed One X 2026 est l\'intermédiaire haut de gamme de la gamme Speed. Carbone 2D avec DAMP+ et Power Foam en goutte d\'eau. Le DAMP+ — système anti-vibrations avancé — est rare à ce niveau de gamme. C\'est probablement l\'intermédiaire la plus complète technologiquement de Head.","techHighlights":[{"label":"Surface","value":"Carbone 2D + DAMP+","detail":"Carbone avec anti-vibrations avancé — confort inhabituel pour une intermédiaire"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — la Speed One accessible"}],"targetProfile":"Joueurs intermédiaires ambitieux. L\'intermédiaire la plus technologique de Head."},{"id":"head-speed-pro-2026","name":"Head Speed Pro 2026","shortName":"Speed Pro 26","brand":"Head","shape":"Goutte d\'eau","weight":"360-375g","balance":"Moyen","surface":"Carbon 3K","core":"Standard EVA","antivib":"Innegra","price":"250-310€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/head-speed-pro-2026.png","year":2026,"category":"avance","scores":{"Puissance":7,"Contrôle":8,"Confort":6.5,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Raquette de vitesse avec excellent contrôle. Le carbone 3K offre de la précision, l\'Innegra protège le bras. Pour les joueurs techniques.","editorial":"La Speed Pro 2026 est la pala rapide avancée de Head. Carbone 3K et EVA standard en forme goutte d\'eau — construction classique mais efficace. Pas d\'Auxetic ni de mousse spéciale — Head mise ici sur la pureté de la construction carbone. C\'est la Speed pour les joueurs qui veulent sentir le carbone sans filtrage technologique.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone classique — toucher direct et honnête"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour un retour d\'information pur"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence rapide — maniabilité et réactivité"}],"targetProfile":"Joueurs avancés qui veulent une pala carbone pure et rapide, sans technologie superflue."},{"name":"Head Speed Team 2026","shortName":"Speed Team 26","brand":"Head","shape":"Goutte d\'eau","weight":"360g","balance":"Moyenne (265mm)","surface":"Fibre de verre + 3D Grain","core":"Power Foam","antivib":true,"price":"150-200€","player":"Intermédiaire polyvalent","imageUrl":"https://www.padelful.com/images/rackets/head-speed-team-2026.png","year":2026,"category":"intermediaire","scores":{"Puissance":6,"Contrôle":8.5,"Maniabilité":7,"Confort":8.5,"Spin":7.5,"Tolérance":7},"verdict":"Polyvalente confortable. Fibre de verre + Power Foam pour tolérance. Bonne all-rounder.","id":"head-speed-team-2026","editorial":"La Speed Team 2026 est la version accessible de la Speed. Fibre de verre avec 3D Grain — une texture de surface pour l\'accroche — et Power Foam en goutte d\'eau. Construction simple mais efficace, avec un bonus d\'effets grâce au 3D Grain. Bonne pala d\'initiation à la gamme Speed.","techHighlights":[{"label":"Surface","value":"Fibre de verre + 3D Grain","detail":"Texture de surface pour les premiers effets"},{"label":"Noyau","value":"Power Foam","detail":"Mousse réactive pour le punch"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Speed accessible"}],"targetProfile":"Joueurs intermédiaires. L\'entrée dans la gamme Speed 2026."},{"id":"head-vibe-2026","name":"Head Vibe 2026","shortName":"Vibe 26","brand":"Head","shape":"Goutte d\'eau","weight":"345-355g","balance":"Moyen","surface":"Fiberglass","core":"EVA Soft","antivib":"Graphene 360+","price":"70-100€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/head-vibe-2026.png","year":2026,"category":"debutant","scores":{"Puissance":6,"Contrôle":7,"Confort":9,"Spin":5,"Maniabilité":8.5,"Tolérance":9},"verdict":"Raquette débutant polyvalente et légère. Grand sweet spot et confort maximal pour découvrir le padel en confiance.","editorial":"La Vibe 2026 est la débutante universelle de Head. Fibre de verre et EVA Soft en goutte d\'eau. Construction simple, prix attractif, qualité Head. La pala pour emprunter au club ou offrir à quelqu\'un qui découvre le padel.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le standard débutant"},{"label":"Noyau","value":"EVA Soft","detail":"Mousse souple pour le confort"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence d\'apprentissage"}],"targetProfile":"Débutants. La Head universelle pour découvrir le padel."},{"id":"nox-ml10-pro-cup-2024","name":"Nox ML10 Pro Cup 2024","shortName":"ML10 PC 24","brand":"Nox","shape":"Ronde","weight":"360-375g","balance":"Moyen","surface":"Fiberglass","core":"Standard EVA","antivib":"—","price":"90-130€","player":"Miguel Lamperti","imageUrl":"https://www.padelful.com/images/rackets/nox-ml10-pro-cup-2024.png","year":2024,"category":"intermediaire","scores":{"Puissance":5.5,"Contrôle":9,"Confort":7.5,"Spin":6,"Maniabilité":7,"Tolérance":9},"verdict":"La ML10 2024, toujours aussi fiable. Contrôle et tolérance exceptionnels à prix doux en fin de série.","editorial":"La ML10 Pro Cup 2024 est le classique éternel de Nox — la pala la plus vendue de l\'histoire de la marque. Fibre de verre et EVA standard en ronde. Aucune prétention technologique, juste un équilibre parfait, un prix attractif, et une fiabilité légendaire. La ML10 est à Nox ce que la Corolla est à Toyota : indestructible et populaire.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre classique — toucher doux et tolérant"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre — le standard fiable"},{"label":"Forme","value":"Ronde","detail":"Le best-seller absolu — sweet spot centré, zéro risque"}],"targetProfile":"Joueurs intermédiaires de tous profils. Le choix sûr par excellence."},{"id":"nox-at10-genius-18k-2025","name":"Nox AT10 Genius 18K 2025","shortName":"AT10 18K","brand":"Nox","shape":"Ronde","weight":"365-375g","balance":"Moyen","surface":"Carbon 18K","core":"Standard EVA","antivib":"—","price":"280-370€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-18k-2025.png","year":2025,"category":"avance","scores":{"Puissance":6,"Contrôle":8.5,"Confort":5.5,"Spin":6,"Maniabilité":6.5,"Tolérance":8},"verdict":"La raquette d\'Agustín Tapia version pro. Carbone 18K rigide pour une sortie de balle nette. Contrôle exceptionnel mais confort limité.","editorial":"La AT10 Genius 18K 2025 est la version avancée de la gamme Tapia pour 2025. Carbone 18K pour la rigidité, EVA standard pour la polyvalence. Forme ronde — inhabituelle pour la gamme AT10 — qui offre un maximum de contrôle. C\'est la AT10 pour les joueurs défensifs qui veulent le prestige de la gamme Tapia dans un format contrôle.","techHighlights":[{"label":"Surface","value":"Carbon 18K","detail":"Carbone haute densité — rigidité élevée au service du contrôle"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre pour la polyvalence"},{"label":"Forme","value":"Ronde","detail":"Contrôle maximum — sweet spot centré et tolérant"}],"targetProfile":"Joueurs avancés défensifs. La AT10 en version contrôle pour les tacticiens."},{"id":"nox-at10-genius-2025","name":"Nox AT10 Genius 2025","shortName":"AT10 Genius 25","brand":"Nox","shape":"Ronde","weight":"360-375g","balance":"Moyen","surface":"Hybrid carbon/fiberglass","core":"Standard EVA","antivib":"—","price":"170-230€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":6,"Contrôle":8.5,"Confort":7,"Spin":6,"Maniabilité":7,"Tolérance":8.5},"verdict":"La raquette d\'Agustín Tapia en version intermédiaire. Forme ronde pour un contrôle exceptionnel et une grande tolérance.","editorial":"La AT10 Genius 2025 est la version intermédiaire de la gamme Tapia. Hybride carbone/fibre de verre et EVA standard en ronde. C\'est le prestige AT10 à prix accessible — le nom du champion sur une pala de progression. Bonne pala pour les fans de Tapia qui construisent leur jeu.","techHighlights":[{"label":"Surface","value":"Hybride carbone/fibre de verre","detail":"Mix accessible — première touche de rigidité carbone"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Ronde","detail":"Contrôle — même forme que les AT10 premium"}],"targetProfile":"Joueurs intermédiaires. L\'entrée dans l\'univers AT10 Tapia."},{"id":"nox-at10-genius-jr-2025","name":"Nox AT10 Genius Junior 2025","shortName":"AT10 Genius Jr","brand":"Nox","shape":"Ronde","weight":"300-325g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-70€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Version junior de la raquette d\'Agustín Tapia. Forme ronde très tolérante, idéale pour développer la technique et le contrôle.","editorial":"La AT10 Genius Jr 2025 est la junior de la gamme Tapia. Fibre de verre et Soft EVA en ronde. Le prestige AT10 pour les jeunes joueurs qui admirent Agustín Tapia.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — AT10 junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort junior"},{"label":"Forme","value":"Ronde","detail":"Tolérance — la AT10 pour les jeunes"}],"targetProfile":"Juniors fans de Tapia. Le prestige AT10 en junior."},{"id":"nox-at10-genius-luxury-2025","name":"Nox AT10 Genius Luxury 2025","shortName":"AT10 Luxury","brand":"Nox","shape":"Ronde","weight":"365-380g","balance":"Moyen","surface":"Carbon 18K","core":"Hard EVA","antivib":"—","price":"330-420€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-luxury-2025.png","year":2025,"category":"expert","scores":{"Puissance":6.5,"Contrôle":8.5,"Confort":5,"Spin":6,"Maniabilité":6,"Tolérance":8},"verdict":"La raquette de match d\'Agustín Tapia. Carbone 18K ultra-rigide pour une sortie de balle chirurgicale. Contrôle d\'exception mais bras en acier requis.","editorial":"La AT10 Genius Luxury 2025 est le sommet de la gamme Nox pour 2025. Carbone 18K et Hard EVA en forme ronde — combinaison inhabituelle de puissance et de contrôle. C\'est une ronde qui frappe : le carbone 18K et la mousse dure offrent une sortie de balle rare pour une forme ronde. Pour les experts qui veulent dominer les échanges sans passer en diamant.","techHighlights":[{"label":"Surface","value":"Carbon 18K","detail":"Carbone haute densité — rigidité rare sur une ronde"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — puissance inhabituelle pour une forme ronde"},{"label":"Forme","value":"Ronde","detail":"Contrôle et tolérance avec une frappe ferme inhabituelle"}],"targetProfile":"Joueurs experts contrôle-puissance. La ronde qui frappe fort — unique sur le marché."},{"id":"nox-ml10-pro-cup-2025","name":"Nox ML10 Pro Cup 2025","shortName":"ML10 Pro Cup","brand":"Nox","shape":"Ronde","weight":"360-375g","balance":"Moyen","surface":"Fiberglass","core":"Standard EVA","antivib":"—","price":"120-160€","player":"Miguel Lamperti","imageUrl":"https://www.padelful.com/images/rackets/nox-ml10-pro-cup-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":5.5,"Contrôle":9,"Confort":7.5,"Spin":6,"Maniabilité":7,"Tolérance":9},"verdict":"L\'iconique ML10, raquette la plus vendue au monde. Contrôle et tolérance exceptionnels, fiable pour les joueurs de tous niveaux.","editorial":"La ML10 Pro Cup 2025 est la mise à jour annuelle du classique intemporel. Même recette gagnante — fibre de verre, EVA standard, ronde — avec des ajustements cosmétiques et de construction. La ML10 ne change pas parce qu\'elle n\'a pas besoin de changer. Si vous connaissez la ML10, vous savez ce que vous achetez.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre éprouvée — le standard ML10"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre — fiabilité totale"},{"label":"Forme","value":"Ronde","detail":"Le classique absolu — aucune surprise, aucun défaut"}],"targetProfile":"Joueurs intermédiaires. La ML10 millésime 2025 — même fiabilité, même satisfaction."},{"id":"nox-ml10-beginner-2025","name":"Nox ML10 Pro Cup Beginner 2025","shortName":"ML10 Beginner","brand":"Nox","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"50-75€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/nox-ml10-pro-cup-beginner-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8,"Spin":6,"Maniabilité":7.5,"Tolérance":9},"verdict":"Version débutant de l\'iconique ML10. Le choix classique et éprouvé pour débuter le padel.","editorial":"La ML10 Beginner 2025 est exactement ce que son nom indique : la ML10 pour les débutants. Fibre de verre et Soft EVA en ronde. La légende ML10 dans sa version la plus accessible. Même philosophie de fiabilité et de confort que la Pro Cup, en version allégée.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — la ML10 pour les premiers matchs"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort ML10"},{"label":"Forme","value":"Ronde","detail":"La ronde ML10 — zéro risque, tout confort"}],"targetProfile":"Débutants. La légende ML10 en version d\'apprentissage."},{"id":"nox-ml10-jr-2025","name":"Nox ML10 Pro Cup Junior 2025","shortName":"ML10 Jr 25","brand":"Nox","shape":"Ronde","weight":"310-325g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"50-70€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/nox-ml10-pro-cup-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Version junior de l\'iconique ML10. Forme ronde ultra-tolérante, parfaite pour les premiers pas sur le court.","editorial":"La ML10 Jr 2025 est la version junior du best-seller Nox. Fibre de verre et Soft EVA en ronde. La légende ML10 pour les plus jeunes — même philosophie de fiabilité et de confort.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — ML10 junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort ML10"},{"label":"Forme","value":"Ronde","detail":"Le classique ML10 en junior"}],"targetProfile":"Juniors. La légende ML10 pour les plus jeunes."},{"id":"nox-nerbo-wpt-2025","name":"Nox Nerbo WPT 2025","shortName":"Nerbo WPT","brand":"Nox","shape":"Diamant","weight":"365-380g","balance":"Haut","surface":"Carbon","core":"Hard EVA","antivib":"—","price":"250-330€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/nox-nerbo-wpt-2025.png","year":2025,"category":"avance","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":5,"Spin":6,"Maniabilité":6,"Tolérance":5},"verdict":"Diamant de compétition Nox. Puissance brute maximale. Pour les joueurs confirmés avec technique irréprochable.","editorial":"La Nerbo WPT 2025 est la pala de puissance agressive de Nox. Carbone standard et Hard EVA en diamant — construction directe et sans compromis. Pas de Pulse System ni d\'anti-vibrations ici — Nox assume le choix de la performance brute. C\'est la Nox pour les attaquants qui veulent une diamant simple, efficace et accessible.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — rigidité directe"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — puissance sans filtre"},{"label":"Forme","value":"Diamant","detail":"Puissance pure en tête"}],"targetProfile":"Joueurs avancés offensifs. Diamant simple et efficace à prix accessible."},{"id":"nox-at10-luxury-genius-12k-xtrem-2026","name":"Nox AT10 Luxury Genius 12K Alum Xtrem 2026","shortName":"AT10 12K Xtrem 26","brand":"Nox","shape":"Goutte d\'eau","weight":"360-375g","balance":"Moyen-Haut","surface":"Carbon 12K Aluminisé Xtrem","core":"EVA HR3 Black","antivib":"Pulse System + EOS Tunnel","price":"280-350€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-luxury-genius-12k-alum-xtrem-2026-agustin-tapia.png","year":2026,"category":"expert","scores":{"Puissance":8.5,"Contrôle":7.5,"Confort":6.5,"Spin":8.5,"Maniabilité":7,"Tolérance":6.5},"verdict":"Version plus rigide du AT10. 12K Xtrem pour frappe plus sèche et puissante. Toucher dur et réactif pour joueurs offensifs techniques.","editorial":"La AT10 Luxury Genius 12K est LA pala de référence d\'Agustín Tapia en version plein poids. Le carbone 12K aluminisé Xtrem délivre une puissance dévastatrice au smash — chaque frappe semble catapultée. Le noyau EVA HR3 Black, le plus dense du catalogue Nox, est un choix radical : zéro compromis sur la dureté pour un maximum de sortie de balle. Réservée aux bras solides et aux ambitions de compétition.","techHighlights":[{"label":"Surface","value":"Carbone 12K Aluminisé Xtrem","detail":"Même surface que la version Lite — rigidité et puissance brute au rendez-vous"},{"label":"Noyau","value":"EVA HR3 Black","detail":"La mousse la plus dure de Nox. Retour d\'énergie maximal, toucher sec et direct. Pas de place pour l\'approximation"},{"label":"Technologie","value":"Pulse System + EOS Tunnel","detail":"Indispensable sur cette pala : le double système canalise les vibrations du carbone ultra-rigide"},{"label":"Forme","value":"Diamant","detail":"Sweet spot haut, équilibre en tête — toute la masse concentrée pour le smash"},{"label":"Sweet spot","value":"Haut et concentré","detail":"Zone de frappe optimale dans le tiers supérieur de la pala, exige de la précision"}],"targetProfile":"Joueurs experts offensifs purs. Le smasheur qui finit les points en l\'air. Déconseillée aux joueurs avec des douleurs au coude ou au poignet.","proPlayerInfo":{"name":"Agustín Tapia","context":"Utilise cette version en compétition officielle Premier Padel. Sa capacité à générer de la puissance avec ce cadre est unique sur le circuit."}},{"id":"nox-at10-luxury-genius-12k-xtrem-lite-2026","name":"Nox AT10 Luxury Genius 12K Xtrem Lite 2026","shortName":"AT10 12K Lite 26","brand":"Nox","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Carbon 12K Aluminisé Xtrem","core":"EVA HR4 White","antivib":"Pulse System + EOS Tunnel","price":"250-310€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-12k-alum-xtrem-lite-2026.png","year":2026,"category":"avance","scores":{"Puissance":8,"Contrôle":7.5,"Confort":7.5,"Spin":8,"Maniabilité":8,"Tolérance":7},"verdict":"AT10 allégée pour maniabilité. Même qualité Tapia avec moins de fatigue. Idéale joueuses ou joueurs techniques cherchant réactivité et confort.","editorial":"La AT10 Luxury Genius 12K Xtrem Lite est la signature d\'Agustín Tapia en version allégée. Même ADN de puissance brute avec le carbone 12K aluminisé Xtrem, mais un poids réduit qui libère la vitesse de bras. Le noyau EVA HR4 White — le plus récent de Nox — offre un retour d\'énergie phénoménal tout en gardant un confort surprenant pour une pala de ce calibre. Le Pulse System couplé à l\'EOS Tunnel canalise les vibrations vers le cadre.","techHighlights":[{"label":"Surface","value":"Carbone 12K Aluminisé Xtrem","detail":"Traitement aluminium sur carbone 12K : rigidité extrême, retour d\'énergie maximal au smash"},{"label":"Noyau","value":"EVA HR4 White","detail":"Dernière génération Nox — haute résilience, rebond puissant mais toucher préservé"},{"label":"Technologie","value":"Pulse System + EOS Tunnel","detail":"Double système anti-vibrations : le Pulse absorbe, l\'EOS canalise vers le cadre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Sweet spot relevé, compromis entre la puissance du diamant et la tolérance de la ronde"},{"label":"Poids","value":"Lite (355-365g)","detail":"Version allégée de la AT10 : vitesse de bras accrue sans sacrifier la puissance du cadre"}],"targetProfile":"Joueurs avancés à experts polyvalents qui cherchent de la puissance sans subir le poids. Parfaite pour les attaquants rapides qui aiment les volées agressives et les smashs en bandeja.","proPlayerInfo":{"name":"Agustín Tapia","context":"N°1 mondial 2024-2025. Considéré comme le joueur le plus talentueux de sa génération, jeu spectaculaire et imprévisible."}},{"name":"Nox AT10 Luxury Genius 18K Alum 2026","shortName":"AT10 18K 26","brand":"Nox","shape":"Goutte d\'eau","weight":"370g","balance":"Moyenne (265mm)","surface":"Carbone 18K Aluminisé + Dual Spin","core":"MultiEva","antivib":true,"price":"350-420€","player":"Expert polyvalent","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-luxury-genius-18k-alum-2026-agustin-tapia.png","year":2026,"category":"expert","scores":{"Puissance":8.5,"Contrôle":7.5,"Confort":6.5,"Spin":8.5,"Maniabilité":7,"Tolérance":6.5},"verdict":"Raquette Agustín Tapia. 18K Aluminisé + Dual Spin. Référence polyvalente expert.","id":"nox-at10-luxury-genius-18k-alum-2026","editorial":"La AT10 18K Alum 2026 est la version premium polyvalente de la gamme Tapia. Le carbone 18K aluminisé avec Dual Spin — texture de surface qui mord la balle — génère des effets dévastateurs. Le noyau MultiEva offre plus de polyvalence que le HR3 Black de la version puissance. C\'est la AT10 pour ceux qui veulent le prestige de la gamme Tapia avec un jeu plus complet et moins radical.","techHighlights":[{"label":"Surface","value":"Carbone 18K Aluminisé + Dual Spin","detail":"Carbone haute densité avec texture Dual Spin — accroche la balle pour des effets liftés et slicés"},{"label":"Noyau","value":"MultiEva","detail":"Mousse polyvalente multi-densité — plus confortable et polyvalente que le HR3 Black"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le compromis polyvalent — attaque et défense dans le même package"},{"label":"Sweet spot","value":"Élargi et texturé","detail":"La texture Dual Spin ajoute de l\'accroche même en périphérie du sweet spot"},{"label":"Spin","value":"Dual Spin Technology","detail":"Rugosité de surface calculée pour maximiser la rotation de balle — liftés et slicés plus efficaces"}],"targetProfile":"Joueurs experts polyvalents qui aiment varier les effets. Parfaite pour le joueur de gauche technique qui alterne smash, bandeja liftée et bajada slicée.","proPlayerInfo":{"name":"Gamme Agustín Tapia","context":"Version polyvalente de la gamme signature du N°1 mondial, pour les joueurs qui veulent sa technologie sans le radicalisme de la version diamant."}},{"id":"nox-at10-luxury-attack-12k-xtrem-2026","name":"Nox AT10 Luxury Genius Attack 12K Xtrem 2026","shortName":"AT10 Attack 26","brand":"Nox","shape":"Diamant","weight":"360-375g","balance":"Haut","surface":"Carbon 12K Aluminisé Xtrem","core":"EVA HR3 Black","antivib":"Pulse System + EOS Tunnel","price":"300-370€","player":"Agustín Tapia","imageUrl":"https://www.padelful.com/images/rackets/nox-at10-genius-attack-12k-alum-xtrem-2026.png","year":2026,"category":"expert","scores":{"Puissance":9,"Contrôle":6,"Confort":5.5,"Spin":8.5,"Maniabilité":5.5,"Tolérance":5},"verdict":"Version diamant offensive maximale de la gamme AT10. Puissance explosive pour joueurs de gauche experts. Dual Spin pour spin dévastateur.","editorial":"La AT10 Luxury Attack 12K Xtrem est la version diamant radicale de la gamme Tapia. Carbone 12K aluminisé Xtrem — même surface que les versions Genius — mais en forme diamant avec noyau HR3 Black, le plus dur de Nox. C\'est la AT10 la plus agressive : puissance brute, zéro compromis. Pour les smasheurs qui veulent la signature Tapia en mode destruction.","techHighlights":[{"label":"Surface","value":"Carbon 12K Aluminisé Xtrem","detail":"Carbone traité aluminium — rigidité extrême pour les smashs"},{"label":"Noyau","value":"EVA HR3 Black","detail":"La mousse la plus dure de Nox — zéro absorption, puissance maximale"},{"label":"Forme","value":"Diamant","detail":"Puissance radicale en tête — la AT10 la plus agressive"},{"label":"Technologie","value":"Pulse System + EOS Tunnel","detail":"Anti-vibrations essentiel sur cette construction ultra-rigide"}],"targetProfile":"Joueurs experts offensifs purs. La AT10 de compétition la plus radicale."},{"id":"nox-ventus-2026","name":"Nox Ventus 2026","shortName":"Ventus 26","brand":"Nox","shape":"Hybride","weight":"360-370g","balance":"Moyen","surface":"Carbon 12K","core":"EVA HR3","antivib":"Pulse System","price":"220-280€","player":null,"imageUrl":"https://www.padelful.com/images/rackets/nox-ventus-hybrid-12k-lite-2026.png","year":2026,"category":"avance","scores":{"Puissance":7.5,"Contrôle":8,"Confort":7.5,"Spin":7.5,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Nouvelle gamme hybride polyvalente Nox. Bon équilibre puissance/contrôle pour joueurs avancés qui ne sont pas purement offensifs.","editorial":"La Ventus 2026 est la polyvalente avancée de Nox. Carbone 12K et mousse EVA HR3 en forme hybride — un format qui offre un bon compromis entre puissance et contrôle. Le Pulse System filtre les vibrations. C\'est la Nox pour les joueurs complets qui ne veulent pas se limiter à un seul style de jeu.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K — bonne rigidité pour l\'attaque comme la défense"},{"label":"Noyau","value":"EVA HR3","detail":"Mousse haute résilience — bon retour d\'énergie avec du confort"},{"label":"Technologie","value":"Pulse System","detail":"Anti-vibrations Nox pour le confort sur la durée"},{"label":"Forme","value":"Hybride","detail":"Sweet spot intermédiaire — polyvalence maximale"}],"targetProfile":"Joueurs avancés polyvalents. La Nox tout-terrain pour les joueurs complets."},{"id":"siux-electra-st3-jr-2024","name":"Siux Electra ST3 Junior 2024","shortName":"Electra ST3 Jr","brand":"Siux","shape":"Ronde","weight":"320g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-70€","player":"Franco Stupaczuk","imageUrl":"https://www.padelful.com/images/rackets/siux-electra-st3-junior-2024.png","year":2024,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Version junior de la raquette de Franco Stupaczuk. Forme ronde très tolérante, construction fibre de verre légère et durable pour l\'apprentissage.","editorial":"L\'Electra ST3 Jr 2024 est la junior Siux. Fibre de verre et Soft EVA en ronde. Construction simple et abordable pour les jeunes joueurs. Siux offre des prix accessibles même en junior.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre Siux junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort junior"},{"label":"Forme","value":"Ronde","detail":"Tolérance — Siux au meilleur prix junior"}],"targetProfile":"Juniors. Siux accessible pour les jeunes."},{"id":"siux-beat-junior-2025","name":"Siux Beat Junior 2025","shortName":"Beat Junior 25","brand":"Siux","shape":"Ronde","weight":"300-330g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"53-75€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/siux-beat-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Raquette junior accessible et très tolérante. Prix attractif pour une première raquette, construction solide en fibre de verre.","editorial":"La Beat Junior 2025 est la junior 2025 de Siux. Fibre de verre et Soft EVA en ronde. Mise à jour de la gamme junior avec un design frais. Construction fiable à prix compétitif.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — junior 2025"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale pour les jeunes"}],"targetProfile":"Juniors. Siux junior 2025 à petit prix."},{"id":"siux-diablo-black-2025","name":"Siux Diablo Black Carbon 2025","shortName":"Diablo Black","brand":"Siux","shape":"Goutte d\'eau","weight":"370-380g","balance":"Mi-haut","surface":"Carbon 12K","core":"Hard EVA","antivib":"—","price":"280-360€","player":"Franco Stupaczuk","imageUrl":"https://www.padelful.com/images/rackets/siux-diablo-black-carbon-2025.png","year":2025,"category":"expert","scores":{"Puissance":8,"Contrôle":7,"Confort":5,"Spin":7.5,"Maniabilité":6,"Tolérance":6.5},"verdict":"Raquette de Stupaczuk version compétition. Carbone 12K rigide avec surface texturée pour spin+puissance. Experte et exigeante.","editorial":"La Diablo Black 2025 est la version premium de la gamme offensive Siux pour 2025. Carbone 12K et mousse Hard EVA en goutte d\'eau — une combinaison offensive dans un format tolérant. C\'est le Diablo pour les joueurs experts qui veulent de la puissance avec la sécurité de la forme goutte.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone haute densité — rigidité et puissance"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — sortie de balle directe"},{"label":"Forme","value":"Goutte d\'eau","detail":"Puissance avec tolérance — le compromis Diablo"}],"targetProfile":"Joueurs experts offensifs-polyvalents. Le Diablo premium en version goutte tolérante."},{"id":"siux-diablo-revolution-2025","name":"Siux Diablo Revolution 2025","shortName":"Diablo Revol","brand":"Siux","shape":"Goutte d\'eau","weight":"360-370g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"150-200€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/siux-diablo-revolution-fucsia.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Goutte d\'eau avec surface texturée pour un bon spin. Le carbone offre de la réactivité pour les joueurs cherchant plus de mordant.","editorial":"La Diablo Revolution 2025 est l\'intermédiaire de la gamme Siux. Carbone standard et EVA standard en goutte d\'eau. Construction honnête et accessible, tarif compétitif. Siux propose des specs correctes à prix serré — la Revolution est une bonne pala de progression sans pression financière.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher direct"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le Diablo accessible"}],"targetProfile":"Joueurs intermédiaires. Le Diablo d\'entrée de gamme à prix compétitif."},{"id":"siux-diablo-xtrem-2025","name":"Siux Diablo Xtrem Carbon 2025","shortName":"Diablo Xtrem","brand":"Siux","shape":"Diamant","weight":"365-380g","balance":"Haut","surface":"Carbon 12K","core":"Hard EVA","antivib":"—","price":"220-300€","player":"Franco Stupaczuk","imageUrl":"https://www.padelful.com/images/rackets/siux-diablo-xtrem-carbon-2025.png","year":2025,"category":"expert","scores":{"Puissance":9.5,"Contrôle":5.5,"Confort":4.5,"Spin":6,"Maniabilité":6,"Tolérance":5},"verdict":"Diamant extrême pour purs attaquants. Carbone 12K + EVA dure = sortie de balle explosive. Confort minimal, réservée aux experts.","editorial":"La Diablo Xtrem 2025 est la version la plus radicale de la gamme Siux 2025. Carbone 12K et Hard EVA en diamant — puissance pure sans concession. C\'est la Siux pour les frappeurs qui veulent du diamant abordable sans les technologies premium du TeXtreme.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K — rigidité élevée pour les frappes puissantes"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse ultra-dense — puissance maximale"},{"label":"Forme","value":"Diamant","detail":"Puissance radicale en tête"}],"targetProfile":"Joueurs experts offensifs. Diamant puissance à prix Siux."},{"id":"siux-optimus-2025","name":"Siux Optimus 2025","shortName":"Optimus 25","brand":"Siux","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"50-65€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/siux-optimus-5.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8,"Spin":6,"Maniabilité":7.5,"Tolérance":9},"verdict":"Entrée de gamme Siux abordable. Le strict nécessaire pour débuter le padel sans se ruiner.","editorial":"L\'Optimus 2025 est la débutante de Siux. Fibre de verre et Soft EVA en ronde. Construction simple et efficace à prix Siux — c\'est-à-dire compétitif. Pour les débutants qui veulent du matériel correct sans investir beaucoup.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — accessible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort basique"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale au meilleur prix"}],"targetProfile":"Débutants au budget serré. Siux accessible."},{"id":"siux-diablo-elite-26","brand":"Siux","name":"Siux Diablo Elite 6 2026","shortName":"Diablo Elite 6 26","year":2026,"category":"intermediaire","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen","surface":"3K Carbon","core":"EVA","description":"Goutte intermédiaire-avancé. Plus souple que le Pro, contrôle et confort accrus. Progression Siux.","scores":{"Puissance":6.5,"Contrôle":7.5,"Maniabilité":8,"Confort":8,"Spin":7,"Tolérance":7.5},"imageUrl":"https://www.padelful.com/images/rackets/siux-diablo-elite-6-2026.png","verdict":"Goutte confortable et maniable, idéale pour monter en niveau en douceur. Polyvalente sans être exceptionnelle dans un domaine.","price":"180-230€","editorial":"La Diablo Elite 2026 est la nouvelle intermédiaire premium de Siux. Carbone 3K et EVA en goutte d\'eau — une montée en gamme par rapport à la Revolution. Le 3K apporte plus de réactivité et de toucher que le carbone standard. C\'est le tremplin vers les versions Pro en TeXtreme.","techHighlights":[{"label":"Surface","value":"3K Carbon","detail":"Carbone 3K — réactivité supérieure au carbone standard"},{"label":"Noyau","value":"EVA","detail":"Mousse polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Diablo"}],"targetProfile":"Joueurs intermédiaires ambitieux. La passerelle vers le TeXtreme Siux."},{"id":"siux-diablo-pro-26","brand":"Siux","name":"Siux Diablo Pro 2026","shortName":"Diablo Pro 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen","surface":"18K TeXtreme + 3K Carbon","core":"Ultra Soft EVA","description":"Goutte hybride polyvalente signature Libaak. Texture 3D + Ultra Soft EVA. Contrôle-puissance équilibré.","scores":{"Puissance":7.5,"Contrôle":8,"Maniabilité":7.5,"Confort":8,"Spin":8,"Tolérance":7},"imageUrl":"https://www.padelful.com/images/rackets/siux-diablo-pro-night-blue-2026.png","verdict":"Polyvalente haut de gamme avec un équilibre rare entre puissance et contrôle. Son confort et son spin en font une valeur sûre pour joueurs complets.","price":"300-350€","player":"Martin Di Nenno / Federico Libaak","editorial":"La Diablo Pro 2026 est l\'arme secrète de Siux — et de Martin Di Nenno. Le TeXtreme 18K, technologie issue de la Formule 1 et de l\'aérospatiale, offre un ratio rigidité/poids inégalé sur le marché. Combiné au carbone 3K en sous-couche, la face délivre une frappe explosive et nette. Le noyau Ultra Soft EVA est un choix audacieux sur une pala de puissance : Siux parie sur le confort pour permettre de jouer des matchs entiers sans fatigue musculaire.","techHighlights":[{"label":"Surface","value":"TeXtreme 18K + Carbon 3K","detail":"TeXtreme 18K (technologie F1) en surface + 3K en sous-couche — le combo le plus rigide du marché"},{"label":"Noyau","value":"Ultra Soft EVA","detail":"Choix surprenant : mousse très souple pour compenser la rigidité extrême de la surface carbone"},{"label":"Forme","value":"Goutte d\'eau","detail":"Compromis parfait — sweet spot relevé pour la puissance, tolérance supérieure au diamant"},{"label":"Sweet spot","value":"Relevé et généreux","detail":"La forme goutte place le sweet spot entre le centre et le haut — polyvalent"},{"label":"Poids","value":"370-380g","detail":"Poids standard qui assure stabilité et puissance sans fatiguer le bras"}],"targetProfile":"Joueurs avancés à experts polyvalents-offensifs. Ceux qui veulent de la puissance en smash tout en gardant un maximum de confort. Excellente option pour les joueurs sujets aux tendinites qui refusent de sacrifier la performance.","proPlayerInfo":{"name":"Martin Di Nenno / Federico Libaak","context":"Di Nenno, joueur argentin Top 5 mondial, jeu puissant et régulier. Libaak, étoile montante du circuit, explosif et imprévisible."}},{"id":"siux-electra-pro-26","brand":"Siux","name":"Siux Electra Pro 2026","shortName":"Electra Pro 26","year":2026,"category":"expert","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen-haut","surface":"18K TeXtreme Carbon","core":"EVA","description":"Goutte signature Stupaczuk. Anti-vibration + TeXtreme 18K. Puissance explosive avec polyvalence.","scores":{"Puissance":8.5,"Contrôle":7.5,"Maniabilité":7,"Confort":7,"Spin":8,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/siux-electra-pro-fire-red-2026-franco-stupackzuk.png","verdict":"Puissance explosive en goutte d\'eau — un profil rare. Le TeXtreme 18K frappe fort tout en gardant du contrôle. Pour attaquants techniques.","price":"300-350€","player":"Franco Stupaczuk","editorial":"L\'Electra Pro 2026 est la machine offensive de Siux pour les joueurs qui veulent de la puissance sans compromis. Le TeXtreme 18K en surface — la même technologie que sur les monoplaces de F1 — offre une rigidité extrême pour un poids plume. Le noyau EVA standard laisse parler le carbone sans interférence. Pas d\'anti-vibrations, pas de technologie de confort : Siux assume le choix de la performance pure. C\'est la F1 des palas — rapide, nerveuse, exigeante.","techHighlights":[{"label":"Surface","value":"TeXtreme 18K Carbon","detail":"Technologie TeXtreme issue de la F1 — rapport rigidité/poids le plus élevé disponible en padel"},{"label":"Noyau","value":"EVA","detail":"Mousse EVA standard — laisse le carbone TeXtreme dicter le comportement de la pala"},{"label":"Forme","value":"Diamant","detail":"Puissance maximale, sweet spot haut — pour les joueurs qui finissent les points en l\'air"},{"label":"Sweet spot","value":"Haut et précis","detail":"Zone de frappe exigeante qui récompense la précision technique"},{"label":"Construction","value":"Minimaliste","detail":"Pas de système anti-vibrations — choix délibéré pour ne pas altérer le retour d\'information"}],"targetProfile":"Joueurs experts offensifs avec une technique solide et aucun problème articulaire. Pour les compétiteurs qui veulent sentir chaque frappe et maximiser la puissance.","proPlayerInfo":{"name":"Franco Stupaczuk","context":"Joueur argentin du circuit Premier Padel, connu pour ses smashs surpuissants et son physique imposant."}},{"id":"siux-fenix-pro-26","brand":"Siux","name":"Siux Fenix Pro 2026","shortName":"Fenix Pro 26","year":2026,"category":"expert","shape":"Diamant","weight":"360-380","balance":"haut","surface":"18K TeXtreme Carbon","core":"Hard EVA","description":"Diamant puissance max signature Augsburger. Balance haute + carbone TeXtreme. Offensif pur.","scores":{"Puissance":9.5,"Contrôle":6,"Maniabilité":5.5,"Confort":5.5,"Spin":7.5,"Tolérance":5},"imageUrl":"https://www.padelful.com/images/rackets/siux-fenix-pro-black-2026-leo-ausburger.png","verdict":"Canon offensif sans concession : 9.5 en puissance mais confort et tolérance sacrifiés. Pour experts qui finissent les points au filet.","price":"300-350€","player":"Benjamin Augsburger","editorial":"La Fenix Pro 2026 est la machine de guerre de Siux — TeXtreme 18K et Hard EVA en diamant. La même technologie F1 que la Diablo Pro, mais dans un format plus radical. Pas d\'Ultra Soft EVA ici — Siux pousse la philosophie TeXtreme jusqu\'au bout avec une mousse dure qui ne filtre rien. C\'est la Siux la plus puissante du catalogue.","techHighlights":[{"label":"Surface","value":"TeXtreme 18K Carbon","detail":"Technologie F1 — le carbone le plus rigide pour son poids sur le marché"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure — couplée au TeXtreme pour une puissance dévastatrice"},{"label":"Forme","value":"Diamant","detail":"Puissance maximale — la Siux la plus radicale"}],"targetProfile":"Joueurs experts offensifs purs. La puissance TeXtreme sans compromis de confort."},{"id":"siux-pegasus-elite-26","brand":"Siux","name":"Siux Pegasus Elite 4 2026","shortName":"Pegasus Elite 4 26","year":2026,"category":"intermediaire","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen","surface":"3K Carbon","core":"EVA","description":"Goutte polyvalente abordable. Contrôle hybride et confort. Pour joueurs intermédiaires ambitieux.","scores":{"Puissance":6.5,"Contrôle":7.5,"Maniabilité":7.5,"Confort":8,"Spin":7,"Tolérance":7.5},"imageUrl":"https://www.padelful.com/images/rackets/siux-pegasus-elite-4-2026.png","verdict":"Goutte polyvalente et confortable à prix contenu. Profil équilibré sans pic — rassurante pour intermédiaires qui cherchent de la régularité.","price":"180-230€","editorial":"La Pegasus Elite 2026 est la polyvalente intermédiaire de Siux. Même construction que la Diablo Elite — carbone 3K et EVA en goutte d\'eau — mais avec un positionnement plus polyvalent. La Pegasus est la gamme équilibrée de Siux, le Diablo étant plus offensif. Pour les joueurs intermédiaires qui ne veulent pas choisir entre attaque et défense.","techHighlights":[{"label":"Surface","value":"3K Carbon","detail":"Carbone 3K — bonne réactivité pour l\'intermédiaire"},{"label":"Noyau","value":"EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence maximale"}],"targetProfile":"Joueurs intermédiaires polyvalents. La Siux tout-terrain."},{"id":"siux-pegasus-pro-26","brand":"Siux","name":"Siux Pegasus Pro 2026","shortName":"Pegasus Pro 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen-haut","surface":"18K TeXtreme Carbon","core":"EVA","description":"Goutte premium offensive. TeXtreme 18K + texture satin 3D. Puissance avec précision pour avancés.","scores":{"Puissance":8,"Contrôle":7.5,"Maniabilité":7,"Confort":7,"Spin":8,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/siux-pegasus-pro-storm-grey-2026.png","verdict":"Montée en gamme avec TeXtreme 18K : plus de puissance et de spin que l\'Elite. Pour avancés qui veulent du mordant sans perdre en précision.","price":"300-350€","editorial":"La Pegasus Pro 2026 est la polyvalente haut de gamme de Siux. TeXtreme 18K et EVA standard en goutte d\'eau. Même surface que la Diablo Pro et la Fenix Pro, mais un noyau neutre qui laisse la pala s\'exprimer naturellement. C\'est le TeXtreme pour les joueurs polyvalents qui veulent la technologie premium sans s\'enfermer dans un style de jeu.","techHighlights":[{"label":"Surface","value":"TeXtreme 18K Carbon","detail":"Technologie premium F1 — rigidité optimale pour le poids"},{"label":"Noyau","value":"EVA","detail":"Mousse neutre pour un jeu naturel"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — attaque et défense"}],"targetProfile":"Joueurs avancés polyvalents. Le TeXtreme dans un format tout-terrain."},{"id":"siux-valkiria-pro-26","brand":"Siux","name":"Siux Valkiria Pro 2026","shortName":"Valkiria Pro 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"345-365","balance":"moyen","surface":"18K TeXtreme Carbon","core":"EVA","description":"Goutte signature Araujo, orientation féminine. Polyvalence puissance/contrôle, poids modéré pour maniabilité.","scores":{"Puissance":7.5,"Contrôle":8,"Maniabilité":8,"Confort":7.5,"Spin":7.5,"Tolérance":7},"imageUrl":"https://www.padelful.com/images/rackets/siux-valkiria-pro-2026.png","verdict":"Polyvalente signature Araujo, poids contenu pour une maniabilité supérieure. Excellent ratio puissance/contrôle, accessible aux gabarits légers.","price":"280-350€","player":"Delfina Araujo","editorial":"La Valkiria Pro 2026 est la pala féminine haut de gamme de Siux — mais son potentiel dépasse largement cette étiquette. TeXtreme 18K en surface pour la même technologie que les modèles masculins, noyau EVA polyvalent. Forme goutte d\'eau pour la polyvalence. C\'est la pala de Delfina Araujo, joueuse argentine au jeu explosif. Idéale pour les joueuses avancées et les joueurs qui veulent du TeXtreme dans un format accessible.","techHighlights":[{"label":"Surface","value":"TeXtreme 18K Carbon","detail":"Même technologie F1 que les modèles premium — pas de compromis"},{"label":"Noyau","value":"EVA","detail":"Mousse polyvalente — bon compromis confort/réactivité"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence et tolérance pour un jeu complet"}],"targetProfile":"Joueuses avancées et joueurs cherchant une goutte TeXtreme polyvalente."},{"id":"starvie-junior-2025","name":"Starvie Junior 2025","shortName":"Starvie Jr 25","brand":"Starvie","shape":"Ronde","weight":"<330g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"80-90€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/starvie-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":8.5},"verdict":"Raquette junior confortable et tolérante. Construction fibre de verre souple pour un apprentissage sans fatigue.","editorial":"La StarVie Junior 2025 est la pala junior de la marque madrilène. Fibre de verre et Soft EVA en ronde. Construction soignée avec la finition StarVie caractéristique. Pour les jeunes qui veulent se démarquer avec une marque alternative.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre StarVie"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple junior"},{"label":"Forme","value":"Ronde","detail":"Tolérance — finition StarVie soignée"}],"targetProfile":"Juniors. StarVie pour les jeunes qui veulent se démarquer."},{"id":"starvie-metheora-galaxy-2025","name":"Starvie Metheora Galaxy 2025","shortName":"Metheora Galaxy","brand":"Starvie","shape":"Goutte d\'eau","weight":"365-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"250-320€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/starvie-metheora-galaxy-2025.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6,"Spin":7.5,"Maniabilité":6.5,"Tolérance":7},"verdict":"Goutte d\'eau haut de gamme Starvie avec surface texturée pour le spin. Polyvalente et technique pour joueurs avancés.","editorial":"La Metheora Galaxy 2025 est la polyvalente avancée de StarVie. Carbone standard et EVA standard en goutte d\'eau — construction classique mais efficace. StarVie compense l\'absence de technologies propriétaires spectaculaires par un prix attractif et une construction soignée. La Galaxy est la version premium de la gamme Metheora.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — bonne rigidité de base StarVie"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le sweet spot StarVie classique"}],"targetProfile":"Joueurs avancés polyvalents. StarVie premium à prix compétitif."},{"id":"starvie-metheora-2025","name":"Starvie Metheora Warrior 2025","shortName":"Metheora","brand":"Starvie","shape":"Goutte d\'eau","weight":"360-375g","balance":"Moyen","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"140-180€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/starvie-metheora-warrior-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Goutte d\'eau Starvie polyvalente. Construction carbone/EVA classique pour un jeu équilibré.","editorial":"La Metheora 2025 est l\'intermédiaire de StarVie. Carbone standard et EVA standard en goutte d\'eau. StarVie offre une construction propre à prix compétitif — c\'est le positionnement de la marque. La Metheora est la pala de progression pour les joueurs attirés par l\'alternative StarVie.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique StarVie"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence StarVie à prix doux"}],"targetProfile":"Joueurs intermédiaires. StarVie accessible et fiable."},{"id":"starvie-raptor-2025","name":"Starvie Raptor 2025","shortName":"Raptor 25","brand":"Starvie","shape":"Ronde","weight":"355-365g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-80€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/starvie-raptor-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":7.5,"Tolérance":9},"verdict":"Ronde Starvie confortable et tolérante. Idéale pour les joueurs occasionnels qui veulent du plaisir sans effort.","editorial":"Le Raptor 2025 est la débutante de StarVie. Fibre de verre et Soft EVA en ronde. Construction simple avec la finition soignée caractéristique de StarVie. Un bon choix pour les débutants qui veulent une marque alternative aux leaders.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre StarVie"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort"},{"label":"Forme","value":"Ronde","detail":"Tolérance StarVie pour débuter"}],"targetProfile":"Débutants. StarVie pour les premiers matchs."},{"id":"starvie-black-titan-26","brand":"Starvie","name":"StarVie Black Titan 2026","shortName":"Black Titan 26","year":2026,"category":"expert","shape":"Hybride","weight":"360-380","balance":"moyen-haut","surface":"24K Carbon Master","core":"H-EVA Power","description":"Hybride flagship puissance. 24K Carbon Master + Dynamic Star ajustable. Anti-Vibe. Pour experts offensifs.","scores":{"Puissance":9,"Contrôle":7,"Maniabilité":6.5,"Confort":6.5,"Spin":8,"Tolérance":6},"imageUrl":"https://www.padelful.com/images/rackets/star-vie-black-titan-2026.png","verdict":"Flagship hybride avec carbone 24K et système Dynamic Star. Puissance expert avec un contrôle surprenant pour ce niveau de frappe.","price":"240-300€","editorial":"Le Black Titan 2026 est le haut de gamme absolu de StarVie. Carbone 24K Master — le plus dense du catalogue — avec noyau H-EVA Power en forme hybride. C\'est la pala la plus ambitieuse de StarVie, qui rivalise techniquement avec les grandes marques. Le carbone 24K offre une puissance rare, la forme hybride une polyvalence bienvenue. Pour les connaisseurs qui cherchent l\'alternative premium.","techHighlights":[{"label":"Surface","value":"24K Carbon Master","detail":"Carbone 24K — le plus dense du marché StarVie, rivalise avec les leaders"},{"label":"Noyau","value":"H-EVA Power","detail":"Mousse haute performance StarVie — réactivité maximale"},{"label":"Forme","value":"Hybride","detail":"Compromis puissance/contrôle — sweet spot intermédiaire"}],"targetProfile":"Joueurs experts à la recherche d\'une alternative premium aux grandes marques."},{"id":"starvie-raptor-26","brand":"Starvie","name":"StarVie Raptor 2026","shortName":"Raptor 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"355-375","balance":"moyen","surface":"3D Carbon","core":"M-EVA Balance","description":"Goutte polyvalente iconique renouvelée. Dynamic Star + Spin Boost Tech Pro + Anti-Vibe. All-court avancé.","scores":{"Puissance":7,"Contrôle":8,"Maniabilité":7.5,"Confort":7.5,"Spin":8,"Tolérance":7.5},"imageUrl":"https://www.padelful.com/images/rackets/star-vie-raptor-2026-plus.png","verdict":"Goutte all-court renouvelée : spin et contrôle en vedette avec un confort correct. Polyvalente fiable pour joueurs avancés complets.","price":"200-260€","editorial":"Le Raptor 2026 est le prédateur de StarVie — la marque madrilène qui monte en gamme année après année. Le carbone 3D, procédé de moulage tridimensionnel exclusif, crée une surface aux propriétés inhabituelles : plus réactive dans l\'axe de la frappe, plus tolérante en latéral. Le noyau M-EVA Balance porte bien son nom : une densité intermédiaire qui ne sacrifie ni la puissance ni le confort. StarVie vise le zéro défaut.","techHighlights":[{"label":"Surface","value":"Carbon 3D","detail":"Moulage tridimensionnel exclusif StarVie — comportement anisotrope : rigide dans l\'axe, souple en travers"},{"label":"Noyau","value":"M-EVA Balance","detail":"Mousse à densité calibrée — le juste milieu entre réactivité et absorption"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le choix polyvalent de StarVie pour ses modèles avancés"},{"label":"Sweet spot","value":"Optimisé par le 3D","detail":"Le carbone 3D crée un sweet spot aux contours progressifs — pas de rupture brutale entre zone optimale et périphérie"}],"targetProfile":"Joueurs avancés polyvalents qui cherchent une marque alternative avec un excellent rapport technologie/prix. StarVie offre souvent des specs comparables aux grandes marques pour 20-30% moins cher.","proPlayerInfo":null},{"id":"starvie-triton-balance-26","brand":"Starvie","name":"StarVie Triton Balance 2026","shortName":"Triton Balance 26","year":2026,"category":"avance","shape":"Diamant","weight":"360-375","balance":"moyen","surface":"18K Carbon Hybrid","core":"M-EVA Balance","description":"Diamant équilibrée. Plus souple que Power, bon compromis puissance/contrôle. TriTech Core + Z-Shock.","scores":{"Puissance":8,"Contrôle":7,"Maniabilité":6.5,"Confort":7,"Spin":7.5,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/star-vie-triton-balance-2026-plus.png","verdict":"Diamant tempérée — plus souple que la Power avec un meilleur équilibre puissance/contrôle. Bon compromis pour attaquants qui veulent de la marge.","price":"200-260€","editorial":"Le Triton Balance 2026 est la diamant équilibrée de StarVie. Carbone 18K Hybrid — mélange de fibres pour un flex contrôlé — avec noyau M-EVA Balance en diamant. Le nom dit tout : c\'est une diamant qui cherche l\'équilibre plutôt que l\'extrémisme. Puissance mesurée, tolérance acceptable, prix compétitif.","techHighlights":[{"label":"Surface","value":"18K Carbon Hybrid","detail":"Carbone hybride 18K — rigidité avec une touche de flex"},{"label":"Noyau","value":"M-EVA Balance","detail":"Mousse calibrée — ni trop dure, ni trop souple"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête mais avec équilibre — pas radical"}],"targetProfile":"Joueurs avancés offensifs qui veulent une diamant mesurée. StarVie accessible."},{"id":"starvie-triton-power-26","brand":"Starvie","name":"StarVie Triton Power 2026","shortName":"Triton Power 26","year":2026,"category":"expert","shape":"Diamant","weight":"360-380","balance":"haut","surface":"18K Carbon Hybrid","core":"H-EVA Power","description":"Diamant puissance explosive. 18K Carbon Hybrid + H-EVA Power haute densité + TriTech Core. Attaquant pur.","scores":{"Puissance":9.5,"Contrôle":6,"Maniabilité":5.5,"Confort":5.5,"Spin":7.5,"Tolérance":5},"imageUrl":"https://www.padelful.com/images/rackets/star-vie-triton-power-2026-plus.png","verdict":"Diamant brutale : 9.5 en puissance, carbone 18K hybride. Réservée aux experts physiques qui assument un jeu 100% offensif.","price":"220-280€","editorial":"Le Triton Power 2026 est la version radicale du Triton. Même carbone 18K Hybrid mais avec noyau H-EVA Power — la mousse la plus dure de StarVie. La différence avec le Balance est nette : plus de sortie de balle, moins de confort. C\'est le Triton pour les compétiteurs qui veulent de la puissance StarVie au maximum.","techHighlights":[{"label":"Surface","value":"18K Carbon Hybrid","detail":"Carbone hybride 18K — même surface que le Triton Balance"},{"label":"Noyau","value":"H-EVA Power","detail":"Mousse dure StarVie — puissance maximale, confort minimal"},{"label":"Forme","value":"Diamant","detail":"Puissance radicale en tête"}],"targetProfile":"Joueurs experts offensifs. Le Triton de puissance maximale."},{"id":"varlion-bourne-2025","name":"Varlion Bourne Carbon 2025","shortName":"Bourne Carbon","brand":"Varlion","shape":"Goutte d\'eau","weight":"360-375g","balance":"Moyen","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"140-180€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/varlion-bourne-carbon-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":7,"Tolérance":7},"verdict":"Goutte d\'eau carbone Varlion. Construction solide et équilibrée pour les joueurs intermédiaires polyvalents.","editorial":"La Bourne 2025 est l\'intermédiaire polyvalente de Varlion. Carbone standard et EVA standard en goutte d\'eau. Construction classique avec le toucher Varlion caractéristique. C\'est l\'entrée dans l\'univers Varlion pour les joueurs intermédiaires curieux du toucher carbone-titane des modèles supérieurs.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher Varlion de base"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Varlion"}],"targetProfile":"Joueurs intermédiaires polyvalents. L\'entrée dans l\'univers Varlion."},{"id":"varlion-canas-jr-2025","name":"Varlion Cañas Junior 2025","shortName":"Cañas Junior","brand":"Varlion","shape":"Ronde","weight":"310-320g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"55-75€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/varlion-canas-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Raquette junior Varlion, marque historique du padel. Forme ronde classique pour un apprentissage confortable et progressif.","editorial":"La Cañas Jr 2025 est la junior Varlion. Fibre de verre et Soft EVA en ronde. Le toucher Varlion pour les jeunes joueurs. Construction légère et confortable.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre Varlion junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — toucher Varlion doux"},{"label":"Forme","value":"Ronde","detail":"Tolérance — junior Varlion"}],"targetProfile":"Juniors. Le toucher Varlion pour les jeunes."},{"id":"varlion-canas-w-2025","name":"Varlion Cañas W 2025","shortName":"Cañas W 25","brand":"Varlion","shape":"Ronde","weight":"350-360g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-80€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/varlion-canas-w-2025.png","year":2025,"category":"debutant","scores":{"Puissance":5.5,"Contrôle":9,"Confort":8.5,"Spin":6,"Maniabilité":8,"Tolérance":9},"verdict":"Version allégée de la Cañas, parfaite pour débuter. Marque historique du padel, construction fiable et confortable.","editorial":"La Cañas W 2025 est la débutante de Varlion. Fibre de verre et Soft EVA en ronde. Le W indique une orientation féminine, mais la pala convient à tous les débutants. Construction légère et confortable avec le toucher Varlion.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre Varlion — toucher doux"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple pour le confort"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale"}],"targetProfile":"Débutants et débutantes. Varlion accessible et confortable."},{"id":"varlion-lw-carbon-2025","name":"Varlion Lethal Weapon Carbon 2025","shortName":"LW Carbon 25","brand":"Varlion","shape":"Goutte d\'eau","weight":"365-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"220-290€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/varlion-lethal-weapon-carbon-2025.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":7},"verdict":"Goutte d\'eau Varlion haut de gamme. Construction carbone traditionnelle pour joueurs avancés polyvalents.","editorial":"La LW Carbon 2025 est la polyvalente carbone de Varlion. Construction classique carbone/EVA standard en goutte d\'eau. Varlion apporte son toucher Pro-Touch distinctif, même sur ses modèles standard. C\'est une pala honnête et fiable pour les joueurs avancés qui veulent découvrir le toucher Varlion.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique Varlion — bonne rigidité de base"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse EVA polyvalente"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence — le format Varlion par excellence"}],"targetProfile":"Joueurs avancés polyvalents. Découverte du toucher Varlion à prix accessible."},{"id":"varlion-bourne-carbon-ti-26","brand":"Varlion","name":"Varlion Bourne Carbon Ti 2026","shortName":"Bourne C.Ti 26","year":2026,"category":"avance","shape":"Hybride","weight":"355-375","balance":"moyen-haut","surface":"Carbon + Titanium","core":"Pro-Touch EVA","description":"Hybride Bourne puissance/contrôle. Prisma Frame + Summum grip long 14.5cm. Polyvalence offensive.","scores":{"Puissance":8,"Contrôle":7,"Maniabilité":7,"Confort":7,"Spin":7.5,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/varlion-bourne-carbon-ti-2023.png","verdict":"Hybride offensive avec grip long 14.5cm. Bonne puissance avec suffisamment de contrôle pour construire avant de conclure.","price":"180-250€","editorial":"La Bourne Carbon Ti 2026 est la version signature de la gamme Varlion. Même alliage carbone-titane que la LW Ti, mais en forme hybride pour plus de polyvalence. Le Pro-Touch EVA offre le toucher distinctif Varlion. C\'est la pala complète de Varlion — la technologie titane dans un format polyvalent adapté à tous les styles.","techHighlights":[{"label":"Surface","value":"Carbon + Titanium","detail":"Alliage carbone-titane exclusif — dureté et résonance uniques"},{"label":"Noyau","value":"Pro-Touch EVA","detail":"Mousse exclusive Varlion — toucher distinctif et personnel"},{"label":"Forme","value":"Hybride","detail":"Polyvalence — sweet spot intermédiaire pour tous les coups"}],"targetProfile":"Joueurs avancés polyvalents. La technologie titane Varlion en format polyvalent."},{"id":"varlion-lw-orquidea-26","brand":"Varlion","name":"Varlion LW 8.8 Orquidea 2026","shortName":"LW Orquidea 26","year":2026,"category":"debutant","shape":"Ronde","weight":"340-360","balance":"bas","surface":"Fiberglass","core":"Soft EVA","description":"Ronde débutante confort. Ergoholes sweet spot élargi + Handlesafety. Légère et tolérante.","scores":{"Puissance":4,"Contrôle":8,"Maniabilité":9,"Confort":9.5,"Spin":5,"Tolérance":9},"imageUrl":"https://www.padelful.com/images/rackets/varlion-lw-8-8-orquidea-2026.png","verdict":"Ronde ultra-confortable et tolérante, parfaite pour débuter. Sweet spot généreux, poids plume — zéro stress sur le bras.","price":"60-100€","editorial":"La LW Orquídea 2026 est la nouvelle débutante Varlion. Fibre de verre et Soft EVA en ronde. Construction légère (LW = Light Weight) pour la maniabilité. L\'Orquídea est positionnée pour les femmes mais convient à tous les débutants légers.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — léger et accessible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple pour le confort"},{"label":"Forme","value":"Ronde","detail":"Tolérance — version Light Weight"}],"targetProfile":"Débutants et débutantes. Varlion ultra-légère pour les premiers matchs."},{"id":"varlion-lw-carbon-ti-26","brand":"Varlion","name":"Varlion LW Carbon Ti 2026","shortName":"LW Carbon Ti 26","year":2026,"category":"avance","shape":"Ronde","weight":"355-375","balance":"bas","surface":"Carbon + Titanium","core":"Pro-Touch EVA","description":"Ronde contrôle Lethal Weapon. Summum grip + Slice Texture. Précision défensive pour tacticiens.","scores":{"Puissance":6,"Contrôle":9,"Maniabilité":7.5,"Confort":7.5,"Spin":7.5,"Tolérance":8.5},"imageUrl":"https://www.padelful.com/images/rackets/varlion-lw-carbon-ti-2023.png","verdict":"Ronde de précision chirurgicale : contrôle à 9 et tolérance à 8.5. Le choix des tacticiens et constructeurs de points patients.","price":"180-250€","editorial":"La LW Carbon Ti 2026 est la pépite cachée de Varlion — la marque espagnole qui refuse de suivre les tendances. L\'alliage carbone-titane sur la surface est unique sur le marché : le titane ajoute une dureté que le carbone seul ne peut atteindre, pour un \'pop\' sonore distinctif à chaque frappe. Le noyau Pro-Touch EVA, développé en interne, offre un toucher particulier que les fidèles de Varlion ne trouvent nulle part ailleurs.","techHighlights":[{"label":"Surface","value":"Carbon + Titanium","detail":"Alliage exclusif carbone-titane — le titane apporte une dureté et une résonance uniques"},{"label":"Noyau","value":"Pro-Touch EVA","detail":"Mousse exclusive Varlion — toucher distinctif, ni trop dur ni trop mou, très personnel"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence assumée — Varlion privilégie l\'adaptabilité sur ses modèles phares"},{"label":"Sweet spot","value":"Central et réactif","detail":"Le titane en surface offre un retour d\'information très net — vous sentez exactement où la balle touche"},{"label":"Poids","value":"LW (Light Weight)","detail":"Version allégée pour la maniabilité — vitesse de bras accrue au filet"}],"targetProfile":"Joueurs avancés à experts polyvalents qui cherchent une sensation différente. Pour ceux qui en ont assez du carbone classique et veulent découvrir le toucher unique du carbone-titane.","proPlayerInfo":null},{"id":"vermont-origin-jr-2025","name":"Vermont Origin Junior 2025","shortName":"Origin Jr 25","brand":"Vermont","shape":"Ronde","weight":"326g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"50-70€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/vermont-origin-junior-2025.png","year":2025,"category":"junior","scores":{"Puissance":5.5,"Contrôle":8.5,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":9},"verdict":"Raquette junior d\'entrée de gamme très tolérante. Excellent rapport qualité/prix pour débuter sans se ruiner.","editorial":"La Vermont Origin Jr 2025 est la junior de Vermont — marque anglaise spécialisée dans l\'équipement sportif accessible. Fibre de verre et Soft EVA en ronde. Construction basique mais honnête pour les jeunes qui essaient le padel.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — construction basique"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort d\'initiation"},{"label":"Forme","value":"Ronde","detail":"Tolérance — l\'essentiel pour commencer"}],"targetProfile":"Juniors. Vermont pour les premiers essais à petit budget."},{"id":"wilson-minions-jr-face-2024","name":"Wilson Minions Junior Face 2024","shortName":"Minions Jr Face","brand":"Wilson","shape":"Goutte d\'eau","weight":"300g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"30-40€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/wilson-minions-junior-face-2024.png","year":2024,"category":"junior","scores":{"Puissance":6,"Contrôle":8,"Confort":8.5,"Spin":6,"Maniabilité":9.5,"Tolérance":7.5},"verdict":"Raquette junior entrée de gamme au design attractif. Construction fibre de verre simple mais efficace pour débuter. Excellent rapport qualité/prix.","editorial":"La Minions Jr Face 2024 est la junior fun de Wilson. Fibre de verre et Soft EVA en goutte d\'eau avec le design Minions — les enfants adorent. C\'est la pala pour que le padel rime avec plaisir chez les plus jeunes.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — fun et confortable"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — zéro vibration"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence fun — design Minions"}],"targetProfile":"Juniors 4-10 ans. Le fun Wilson avec les Minions pour les plus petits."},{"id":"wilson-bela-elite-pro-2025","name":"Wilson Bela Elite Pro V2 2025","shortName":"Bela Elite Pro","brand":"Wilson","shape":"Goutte d\'eau","weight":"370-385g","balance":"Mi-haut","surface":"Carbon 12K","core":"Hard EVA","antivib":"—","price":"300-380€","player":"Fernando Belasteguín","imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-elite-pro-v2-2025.png","year":2025,"category":"expert","scores":{"Puissance":8,"Contrôle":7,"Confort":5,"Spin":6,"Maniabilité":6,"Tolérance":6.5},"verdict":"Version compétition de la raquette de Bela. Carbone 12K pour une frappe sèche et précise. Exigeante physiquement.","editorial":"La Bela Elite Pro 2025 est le haut de gamme Wilson pour 2025. Carbone 12K et mousse Hard EVA en goutte d\'eau — une construction expert qui favorise l\'attaque tout en gardant la polyvalence de la forme goutte. C\'est la Wilson pour les joueurs offensifs qui veulent la qualité de construction Wilson au sommet de ses capacités.","techHighlights":[{"label":"Surface","value":"Carbon 12K","detail":"Carbone 12K — bonne rigidité pour les frappes offensives"},{"label":"Noyau","value":"Hard EVA","detail":"Mousse dure pour la puissance — le sommet de la gamme Bela"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence offensive — l\'ADN Bela"}],"targetProfile":"Joueurs experts offensifs-polyvalents. Le meilleur de Wilson pour 2025."},{"id":"wilson-bela-elite-v2-2025","name":"Wilson Bela Elite V2 2025","shortName":"Bela Elite V2","brand":"Wilson","shape":"Goutte d\'eau","weight":"365-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"250-330€","player":"Fernando Belasteguín","imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-elite-v2.png","year":2025,"category":"avance","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":7},"verdict":"La raquette de légende de Bela en version élite. Goutte d\'eau équilibrée pour les joueurs avancés complets.","editorial":"La Bela Elite V2 2025 est la version avancée de la gamme Bela. Carbone standard et EVA standard en goutte d\'eau — construction propre et fiable. C\'est le cran en dessous de la Pro, mais la qualité Wilson est là. Pour les joueurs avancés qui veulent une Bela solide sans le tarif expert.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique Wilson — fiable et prévisible"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse polyvalente pour un toucher neutre"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Bela — le format signature"}],"targetProfile":"Joueurs avancés polyvalents. La gamme Bela accessible avec qualité Wilson."},{"id":"wilson-bela-pro-v2-2025","name":"Wilson Bela Pro V2 2025","shortName":"Bela Pro V2","brand":"Wilson","shape":"Goutte d\'eau","weight":"360-375g","balance":"Mi-haut","surface":"Carbon","core":"Standard EVA","antivib":"—","price":"180-240€","player":"Fernando Belasteguín","imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-pro-v2-2025.png","year":2025,"category":"intermediaire","scores":{"Puissance":7.5,"Contrôle":7.5,"Confort":6,"Spin":6,"Maniabilité":6.5,"Tolérance":7},"verdict":"Raquette signature de la légende Belasteguín. Bon compromis puissance/contrôle mais confort limité par le carbone. Pour intermédiaires exigeants.","editorial":"La Bela Pro V2 2025 est l\'intermédiaire de la gamme Bela. Carbone standard et EVA standard en goutte d\'eau. La qualité de construction Wilson est là — finitions soignées, équilibre bien calibré. C\'est la Bela pour progresser avec la fiabilité Wilson avant de passer à l\'Elite ou à la V3.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique Wilson — construction soignée"},{"label":"Noyau","value":"Standard EVA","detail":"Mousse neutre — fiabilité Wilson"},{"label":"Forme","value":"Goutte d\'eau","detail":"L\'ADN polyvalent Bela"}],"targetProfile":"Joueurs intermédiaires polyvalents. La Bela de progression Wilson."},{"id":"wilson-blade-jr-v2","name":"Wilson Blade Junior V2 2025","shortName":"Blade Jr V2","brand":"Wilson","shape":"Goutte d\'eau","weight":"300g","balance":"Bas","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"79-102€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/wilson-blade-junior-v2.png","year":2025,"category":"junior","scores":{"Puissance":6,"Contrôle":8,"Confort":8.5,"Spin":6,"Maniabilité":10,"Tolérance":7.5},"verdict":"Ultra-légère (300g) avec forme goutte d\'eau pour un bon équilibre contrôle/puissance. Fibre de verre confortable et tolérante pour l\'apprentissage.","editorial":"La Blade Jr V2 est la junior technique de Wilson. Fibre de verre et Soft EVA en goutte d\'eau. L\'esprit Blade — toucher et finesse — pour les jeunes joueurs techniques. Pour les juniors qui développent un vrai jeu de placement.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher Blade junior"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort et toucher"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence technique pour les juniors"}],"targetProfile":"Juniors techniques. Le toucher Blade Wilson pour les jeunes."},{"id":"wilson-ultra-v2-2025","name":"Wilson Ultra V2 2025","shortName":"Ultra V2","brand":"Wilson","shape":"Goutte d\'eau","weight":"355-365g","balance":"Moyen","surface":"Fiberglass","core":"Soft EVA","antivib":"—","price":"60-85€","player":"—","imageUrl":"https://www.padelful.com/images/rackets/wilson-ultra-v2-2025.png","year":2025,"category":"debutant","scores":{"Puissance":6.5,"Contrôle":8,"Confort":8,"Spin":6,"Maniabilité":7.5,"Tolérance":7.5},"verdict":"Goutte d\'eau Wilson accessible et polyvalente. Fibre de verre pour un bon confort de frappe.","editorial":"L\'Ultra V2 2025 est la débutante de Wilson. Fibre de verre et Soft EVA en goutte d\'eau. La qualité de construction Wilson est perceptible même à ce niveau — finitions soignées, équilibre calibré. Pour les débutants qui veulent la fiabilité Wilson dès le premier match.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — qualité Wilson"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort Wilson"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence d\'apprentissage"}],"targetProfile":"Débutants. La fiabilité Wilson pour les premiers pas."},{"id":"wilson-accent-26","brand":"Wilson","name":"Wilson Accent 2026","shortName":"Accent 26","year":2026,"category":"debutant","shape":"Ronde","weight":"340-355","balance":"bas","surface":"Fiberglass","core":"Soft EVA","description":"Ronde confort-first pour débutants. Fibre de verre + EVA souple. Timing facile, légère, gateway Wilson.","scores":{"Puissance":4.5,"Contrôle":8,"Maniabilité":9,"Confort":9.5,"Spin":5,"Tolérance":9},"imageUrl":"https://www.padelful.com/images/rackets/wilson-accent-2026.png","verdict":"Porte d\'entrée Wilson : légère, confortable, très tolérante. Idéale pour découvrir le padel sans se soucier de la technique.","price":"60-100€","editorial":"L\'Accent 2026 est la pala la plus accessible de Wilson. Fibre de verre et Soft EVA en ronde. Construction simple, prix d\'appel, qualité Wilson. C\'est la pala pour tester le padel avec une marque de confiance.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — le plus accessible Wilson"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort basique"},{"label":"Forme","value":"Ronde","detail":"Tolérance maximale — zéro risque"}],"targetProfile":"Débutants et primo-accédants. Wilson au meilleur prix."},{"id":"wilson-bela-lt-v3-26","brand":"Wilson","name":"Wilson Bela LT V3 2026","shortName":"Bela LT V3 26","year":2026,"category":"intermediaire","shape":"Goutte d\'eau","weight":"340-350","balance":"moyen","surface":"3K Carbon","core":"Power Foam EVA","description":"Version allégée de la Bela V3. Maniabilité accrue sans perdre le spin et la puissance. Idéale progression.","scores":{"Puissance":6.5,"Contrôle":7,"Maniabilité":8.5,"Confort":8,"Spin":7.5,"Tolérance":7.5},"imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-ls-v3-2025.png","verdict":"Version allégée de la Bela : maniabilité boostée à 8.5 tout en gardant le spin. Parfaite pour progresser avec le confort en plus.","price":"180-250€","player":"Fernando Belasteguín","editorial":"La Bela LT V3 2026 est la version légère de la nouvelle gamme Wilson. Carbone 3K et Power Foam EVA en goutte d\'eau — la montée en gamme technologique par rapport à la V2. Plus légère pour la maniabilité, plus réactive grâce au Power Foam. C\'est la Bela nouvelle génération pour les joueurs intermédiaires ambitieux.","techHighlights":[{"label":"Surface","value":"3K Carbon","detail":"Carbone 3K — montée en gamme par rapport au carbone standard"},{"label":"Noyau","value":"Power Foam EVA","detail":"Mousse réactive — plus d\'explosivité que l\'EVA standard"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence Bela"}],"targetProfile":"Joueurs intermédiaires en progression. La nouvelle Bela pour monter de niveau."},{"id":"wilson-bela-pro-v3-26","brand":"Wilson","name":"Wilson Bela Pro V3 2026","shortName":"Bela Pro V3 26","year":2026,"category":"expert","shape":"Diamant","weight":"365-375","balance":"haut","surface":"24K Carbon","core":"Power Foam EVA","description":"Diamant offensive signature Bela. C2 Tubular Construction + 24K Carbon + DuoGrid. Puissance max pour joueurs confirmés.","scores":{"Puissance":9,"Contrôle":7,"Maniabilité":6,"Confort":6,"Spin":8,"Tolérance":5.5},"imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-pro-v3-2025.png","verdict":"Diamant signature Bela, carbone 24K et construction tubulaire. Puissance maximale pour experts — exige un bon niveau technique.","price":"220-300€","player":"Fernando Belasteguín","editorial":"La Bela Pro V3 2026 est le nouveau sommet de la gamme Wilson. Carbone 24K — le plus dense du catalogue — et Power Foam EVA en diamant. C\'est un virage pour Wilson qui passe de la goutte traditionnelle au diamant. La Bela Pro assume enfin une orientation offensive claire. Pour les experts qui veulent la signature Bela avec une puissance moderne.","techHighlights":[{"label":"Surface","value":"24K Carbon","detail":"Carbone 24K — le plus dense du catalogue Wilson, puissance maximale"},{"label":"Noyau","value":"Power Foam EVA","detail":"Mousse réactive pour les frappes offensives"},{"label":"Forme","value":"Diamant","detail":"Nouveau pour la gamme Bela — orientation offensive assumée"}],"targetProfile":"Joueurs experts offensifs. La Bela enfin en diamant — puissance Wilson au maximum."},{"id":"wilson-bela-v3-26","brand":"Wilson","name":"Wilson Bela V3 2026","shortName":"Bela V3 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"360-370","balance":"moyen","surface":"3K Carbon","core":"Power Foam EVA","description":"Goutte versatile co-design Bela. V-Bridge + Spin2 Texture + DuoGrid Holes. Polyvalence attaque/défense.","scores":{"Puissance":7.5,"Contrôle":7.5,"Maniabilité":7,"Confort":7,"Spin":8,"Tolérance":7},"imageUrl":"https://www.padelful.com/images/rackets/wilson-bela-v3-2025.png","verdict":"Co-design Bela en goutte versatile : spin à 8 et bon équilibre global. Polyvalente fiable qui s\'adapte à tous les styles de jeu.","price":"200-280€","player":"Fernando Belasteguín","editorial":"La Bela V3 2026 perpétue l\'héritage du plus grand joueur de l\'histoire du padel. Le carbone 3K offre une réactivité immédiate, tandis que le Power Foam EVA injecte de l\'explosivité dans chaque frappe. Wilson a fait le choix d\'une construction relativement simple mais maîtrisée — pas de gadget technologique, juste un équilibre parfaitement calibré entre puissance et contrôle. Comme Bela lui-même : efficace, sans fioritures, légendaire.","techHighlights":[{"label":"Surface","value":"Carbon 3K","detail":"Carbone classique à maillage serré — toucher direct et prévisible, frappe après frappe"},{"label":"Noyau","value":"Power Foam EVA","detail":"Mousse EVA haute performance — bon compromis entre rebond et absorption des chocs"},{"label":"Forme","value":"Goutte d\'eau","detail":"Le choix polyvalent de Bela — sweet spot intermédiaire pour attaquer et défendre"},{"label":"Sweet spot","value":"Polyvalent et fiable","detail":"Zone de frappe bien dimensionnée qui s\'adapte à tous les coups du répertoire"},{"label":"Héritage","value":"Signature Belasteguín","detail":"Conçue en collaboration avec le GOAT du padel — chaque détail reflète 20 ans d\'expérience au sommet"}],"targetProfile":"Joueurs avancés polyvalents qui veulent une pala fiable et complète. Le choix sûr pour progresser vers le haut niveau sans prendre de risque sur le matériel.","proPlayerInfo":{"name":"Fernando Belasteguín","context":"Le GOAT du padel. 16 ans N°1 mondial consécutifs, record absolu. Sa longévité et sa capacité d\'adaptation sont légendaires."}},{"id":"wilson-blade-pro-v2-26","brand":"Wilson","name":"Wilson Blade Pro V2 2026","shortName":"Blade Pro V2 26","year":2026,"category":"avance","shape":"Goutte d\'eau","weight":"360-370","balance":"moyen","surface":"Carbon","core":"Soft EVA","description":"Goutte toucher doux et précision. Dwell time long pour touches et défenses. Polyvalence attaque/défense.","scores":{"Puissance":7,"Contrôle":8,"Maniabilité":7.5,"Confort":8,"Spin":7.5,"Tolérance":7.5},"imageUrl":"https://www.padelful.com/images/rackets/wilson-blade-pro-v2-oro.png","verdict":"Toucher doux et dwell time long pour un contrôle précis. Excellente en défense et en construction — la patte du tacticien.","price":"200-280€","editorial":"La Blade Pro V2 2026 est la polyvalente avancée de Wilson — la pala qui fait tout bien sans se spécialiser. Carbone standard et Soft EVA en goutte d\'eau pour le confort et la maniabilité. C\'est la Wilson pour les joueurs techniques qui veulent du toucher et de la finesse plutôt que de la puissance brute.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique — toucher propre et direct"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort et toucher privilégiés"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence et maniabilité"}],"targetProfile":"Joueurs avancés techniques. La Wilson du toucher et de la finesse."},{"id":"wilson-blade-team-26","brand":"Wilson","name":"Wilson Blade Team 2026","shortName":"Blade Team 26","year":2026,"category":"intermediaire","shape":"Goutte d\'eau","weight":"350-360","balance":"moyen","surface":"Fiberglass","core":"Soft EVA","description":"Goutte polyvalente intermédiaire. Feel doux et sweet spot tolérant. Transition vers jeu technique.","scores":{"Puissance":6,"Contrôle":7,"Maniabilité":8,"Confort":8.5,"Spin":6.5,"Tolérance":8},"imageUrl":"https://www.padelful.com/images/rackets/wilson-blade-team-2026.png","verdict":"Goutte douce et tolérante pour intermédiaires. Sweet spot généreux, bon confort — transition idéale vers un jeu plus technique.","price":"100-150€","editorial":"La Blade Team 2026 est l\'intermédiaire de la gamme Blade. Fibre de verre et Soft EVA en goutte d\'eau. Construction souple et confortable, orientée toucher. C\'est la Wilson pour les joueurs intermédiaires techniques qui veulent sentir la balle plutôt que la frapper.","techHighlights":[{"label":"Surface","value":"Fiberglass","detail":"Fibre de verre — toucher souple et accessible"},{"label":"Noyau","value":"Soft EVA","detail":"Mousse souple — confort et sensation de balle prioritaires"},{"label":"Forme","value":"Goutte d\'eau","detail":"Polyvalence technique — la Blade accessible"}],"targetProfile":"Joueurs intermédiaires techniques. La Blade pour le toucher."},{"id":"wilson-prostaff-v2-26","brand":"Wilson","name":"Wilson Pro Staff V2 2026","shortName":"Pro Staff V2 26","year":2026,"category":"avance","shape":"Ronde","weight":"360-370","balance":"bas","surface":"Carbon","core":"Dense EVA","description":"Ronde précision chirurgicale. Sweet spot central, réponse crisp. Pour constructeurs de points tactiques.","scores":{"Puissance":6,"Contrôle":9,"Maniabilité":7.5,"Confort":7,"Spin":7,"Tolérance":8},"imageUrl":"https://www.padelful.com/images/rackets/wilson-pro-staff-v2-azul.png","verdict":"Ronde contrôle pur : réponse nette et précision chirurgicale. Pour constructeurs patients qui placent chaque balle avec intention.","price":"200-280€","editorial":"La Pro Staff V2 2026 est l\'icône de Wilson portée en padel — la même philosophie de contrôle que la légendaire raquette de tennis. Carbone standard et Dense EVA en forme ronde. La Dense EVA offre une mousse ferme mais pas dure — plus de répondant que le Soft EVA, moins brutal que le Hard EVA. C\'est la ronde pour les puristes du contrôle qui veulent du caractère dans leur pala.","techHighlights":[{"label":"Surface","value":"Carbon standard","detail":"Carbone classique Wilson — la base Pro Staff"},{"label":"Noyau","value":"Dense EVA","detail":"Mousse ferme propriétaire — entre Soft et Hard, le juste milieu Pro Staff"},{"label":"Forme","value":"Ronde","detail":"Contrôle maximum — la philosophie Pro Staff"}],"targetProfile":"Joueurs avancés défensifs-contrôle. La ronde avec du caractère pour les puristes."},{"id":"wilson-ultra-pro-v2-26","brand":"Wilson","name":"Wilson Ultra Pro V2 2026","shortName":"Ultra Pro V2 26","year":2026,"category":"expert","shape":"Diamant","weight":"365-375","balance":"haut","surface":"Double Carbon Wall 2.0","core":"Pro EVA","description":"Diamant puissance explosive. Infinity Edge sweet spot élargi + Power Pillar stabilité. Finisseur agressif.","scores":{"Puissance":9.5,"Contrôle":6,"Maniabilité":5.5,"Confort":6,"Spin":7.5,"Tolérance":6},"imageUrl":"https://www.padelful.com/images/rackets/wilson-ultra-pro-v2.png","verdict":"Diamant explosive à 9.5 en puissance avec sweet spot élargi. Finisseur agressif — demande du physique et de la précision.","price":"220-300€","editorial":"L\'Ultra Pro V2 2026 est la pala de puissance de Wilson. Double Carbon Wall 2.0 — un double mur de carbone — avec noyau Pro EVA en diamant. Le Double Carbon Wall est exclusif à Wilson : deux couches de carbone avec un espace intermédiaire qui amplifie l\'énergie de retour. C\'est la technologie la plus distinctive de Wilson, et elle est spectaculaire en puissance.","techHighlights":[{"label":"Surface","value":"Double Carbon Wall 2.0","detail":"Double mur de carbone exclusif Wilson — amplification de l\'énergie de retour"},{"label":"Noyau","value":"Pro EVA","detail":"Mousse haute performance Wilson — réactivité calibrée"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — la Wilson la plus agressive"},{"label":"Technologie","value":"Double Carbon Wall","detail":"Deux couches de carbone espacées qui créent un effet trampoline — puissance unique"}],"targetProfile":"Joueurs experts offensifs. La technologie Wilson la plus distinctive au service de la puissance."},{"id":"wilson-ultra-team-26","brand":"Wilson","name":"Wilson Ultra Team 2026","shortName":"Ultra Team 26","year":2026,"category":"intermediaire","shape":"Diamant","weight":"355-365","balance":"moyen-haut","surface":"Fiberglass/Carbon","core":"EVA","description":"Diamant puissance accessible. Infinity Edge pour sweet spot large. Progression vers jeu offensif.","scores":{"Puissance":7.5,"Contrôle":6.5,"Maniabilité":7,"Confort":7.5,"Spin":6.5,"Tolérance":6.5},"imageUrl":"https://www.padelful.com/images/rackets/wilson-ultra-team-2026.png","verdict":"Diamant accessible avec sweet spot large Infinity Edge. Bonne porte d\'entrée vers le jeu offensif sans trop de risque.","price":"120-170€","editorial":"L\'Ultra Team 2026 est l\'intermédiaire de la gamme puissance Wilson. Hybride fibre de verre/carbone et EVA en diamant. C\'est l\'initiation à la puissance Wilson — forme diamant et matériaux accessibles pour développer un jeu offensif. Le premier pas vers l\'Ultra Pro et son Double Carbon Wall.","techHighlights":[{"label":"Surface","value":"Fiberglass/Carbon hybride","detail":"Mix accessible — première touche de rigidité vers le Double Carbon Wall"},{"label":"Noyau","value":"EVA","detail":"Mousse neutre polyvalente"},{"label":"Forme","value":"Diamant","detail":"Puissance en tête — l\'ADN Ultra"}],"targetProfile":"Joueurs intermédiaires offensifs. L\'entrée dans la gamme Ultra Wilson."}]');
;// ./PadelAnalyzer.jsx
/* unused harmony import specifier */ var PadelAnalyzer_React;
function _regeneratorValues(e) { if (null != e) { var t = e["function" == typeof Symbol && Symbol.iterator || "@@iterator"], r = 0; if (t) return t.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) return { next: function next() { return e && r >= e.length && (e = void 0), { value: e && e[r++], done: !e }; } }; } throw new TypeError(_typeof(e) + " is not iterable"); }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function PadelAnalyzer_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function PadelAnalyzer_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? PadelAnalyzer_ownKeys(Object(t), !0).forEach(function (r) { PadelAnalyzer_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : PadelAnalyzer_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function PadelAnalyzer_defineProperty(e, r, t) { return (r = PadelAnalyzer_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function PadelAnalyzer_toPropertyKey(t) { var i = PadelAnalyzer_toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function PadelAnalyzer_toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }



var LEVEL_OPTIONS = [{
  value: "Débutant",
  label: "Débutant",
  desc: "Découverte, < 1 an"
}, {
  value: "Intermédiaire",
  label: "Intermédiaire",
  desc: "Bases acquises, 1-3 ans"
}, {
  value: "Avancé",
  label: "Avancé",
  desc: "Technique solide, tactique"
}, {
  value: "Expert",
  label: "Expert",
  desc: "Compétiteur classé"
}];
var SIDE_OPTIONS = ["Gauche", "Droite", "Les deux"];
var HAND_OPTIONS = ["Droitier", "Gaucher"];
var FREQ_OPTIONS = [{
  value: "Occasionnel (1-2x/mois)",
  label: "Occasionnel",
  desc: "1-2x/mois"
}, {
  value: "Régulier (1-2x/semaine)",
  label: "Régulier",
  desc: "1-2x/semaine"
}, {
  value: "Assidu (3-4x/semaine)",
  label: "Assidu",
  desc: "3-4x/semaine"
}, {
  value: "Intensif (5+/semaine)",
  label: "Intensif",
  desc: "5+/semaine"
}];
var BRAND_TAGS = [{
  id: "head",
  label: "Head"
}, {
  id: "bullpadel",
  label: "Bullpadel"
}, {
  id: "nox",
  label: "Nox"
}, {
  id: "babolat",
  label: "Babolat"
}, {
  id: "adidas",
  label: "Adidas"
}, {
  id: "siux",
  label: "Siux"
}, {
  id: "starvie",
  label: "StarVie"
}, {
  id: "wilson",
  label: "Wilson"
}];
var STYLE_TAGS = [{
  id: "offensif",
  label: "Offensif",
  tip: "Filet, volées, conclure vite"
}, {
  id: "defensif",
  label: "Défensif / Mur",
  tip: "Lobs, patience, attend l'erreur"
}, {
  id: "tactique",
  label: "Tactique",
  tip: "Placement, construction, rythme"
}, {
  id: "puissant",
  label: "Puissant / Frappeur",
  tip: "Remates, frappes lourdes"
}, {
  id: "veloce",
  label: "Véloce",
  tip: "Couverture terrain, rapidité"
}, {
  id: "endurant",
  label: "Endurant",
  tip: "Longs échanges, résistance"
}, {
  id: "contre",
  label: "Contre-attaquant",
  tip: "Défend puis retourne"
}, {
  id: "polyvalent",
  label: "Polyvalent",
  tip: "Mix attaque/défense, adaptatif"
}, {
  id: "technique",
  label: "Technique",
  tip: "Précision, toucher de balle"
}];
var INJURY_TAGS = [{
  id: "dos",
  label: "Dos"
}, {
  id: "poignet",
  label: "Poignet"
}, {
  id: "coude",
  label: "Coude (tennis elbow)"
}, {
  id: "epaule",
  label: "Épaule"
}, {
  id: "genou",
  label: "Genou"
}, {
  id: "cheville",
  label: "Cheville"
}, {
  id: "mollet",
  label: "Mollet"
}, {
  id: "hanche",
  label: "Hanche"
}, {
  id: "achille",
  label: "Tendon d'Achille"
}, {
  id: "aucune",
  label: "Aucune"
}];
var PRIORITY_TAGS = [{
  id: "confort",
  label: "Confort"
}, {
  id: "polyvalence",
  label: "Polyvalence"
}, {
  id: "puissance",
  label: "Puissance"
}, {
  id: "controle",
  label: "Contrôle"
}, {
  id: "spin",
  label: "Spin"
}, {
  id: "legerete",
  label: "Légèreté"
}, {
  id: "protection",
  label: "Protection bras"
}, {
  id: "reprise",
  label: "Reprise en douceur"
}];
var INITIAL_PROFILE = {
  age: "",
  weight: "",
  height: "",
  level: "Intermédiaire",
  side: "Droite",
  hand: "Droitier",
  styleTags: [],
  styleExtra: "",
  injuryTags: [],
  injuryExtra: "",
  priorityTags: [],
  priorityExtra: "",
  brandTags: [],
  frequency: "Occasionnel (1-2x/mois)",
  competition: false
};
var INITIAL_RACKETS = [];

// Load saved rackets from localStorage
function loadSavedRackets() {
  try {
    var raw = localStorage.getItem('padel_rackets');
    if (raw) {
      var arr = JSON.parse(raw);
      if (Array.isArray(arr) && arr.length) {
        // Refresh imageUrl from current DB (localStorage may have stale/missing URLs)
        return arr.map(function (r) {
          var dbMatch = rackets_db_namespaceObject.find(function (db) {
            return db.name.toLowerCase() === (r.name || "").toLowerCase();
          });
          if (dbMatch && dbMatch.imageUrl && (!r.imageUrl || r.imageUrl !== dbMatch.imageUrl)) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
              imageUrl: dbMatch.imageUrl
            });
          }
          return r;
        });
      }
    }
  } catch (_unused) {}
  return [];
}
function saveRackets(rackets) {
  try {
    localStorage.setItem('padel_rackets', JSON.stringify(rackets));
  } catch (_unused2) {}
}
// Load saved profile
function loadSavedProfile() {
  try {
    var raw = localStorage.getItem('padel_profile');
    if (raw) {
      var p = JSON.parse(raw);
      if (p && _typeof(p) === 'object') return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, INITIAL_PROFILE), p);
    }
  } catch (_unused3) {}
  return INITIAL_PROFILE;
}
// Multi-profile management
function loadProfilesList() {
  try {
    var raw = localStorage.getItem('padel_profiles_list');
    if (raw) {
      var arr = JSON.parse(raw);
      if (Array.isArray(arr)) return arr;
    }
  } catch (_unused4) {}
  return [];
}
function saveProfilesList(list) {
  try {
    localStorage.setItem('padel_profiles_list', JSON.stringify(list));
  } catch (_unused5) {}
}
function saveNamedProfile(name, profile) {
  var list = loadProfilesList();
  var existing = list.findIndex(function (p) {
    return p.name === name;
  });
  var entry = {
    name: name,
    profile: PadelAnalyzer_objectSpread({}, profile),
    savedAt: Date.now()
  };
  if (existing >= 0) list[existing] = entry;else list.push(entry);
  saveProfilesList(list);
  return list;
}
function deleteNamedProfile(name) {
  var list = loadProfilesList().filter(function (p) {
    return p.name !== name;
  });
  saveProfilesList(list);
  return list;
}
function toggleProfileLock(name) {
  var list = loadProfilesList();
  var idx = list.findIndex(function (p) {
    return p.name === name;
  });
  if (idx >= 0) list[idx].locked = !list[idx].locked;
  saveProfilesList(list);
  return list;
}
function getAdminPin() {
  return localStorage.getItem('padel_admin_pin') || '';
}
function setAdminPin(pin) {
  localStorage.setItem('padel_admin_pin', pin);
}
var ATTRS = ["Puissance", "Contrôle", "Confort", "Spin", "Maniabilité", "Tolérance"];

// Image URL helper — passthrough, no proxy needed (browser loads directly)
function proxyImg(url) {
  return url || null;
}
var COLORS_POOL = ["#E53935", "#FF9800", "#E91E63", "#4CAF50", "#009688", "#2196F3", "#1565C0", "#9C27B0", "#00BCD4", "#FF5722", "#8BC34A", "#795548", "#607D8B", "#D4E157", "#F06292", "#4DD0E1", "#FFB74D", "#AED581", "#BA68C8", "#4FC3F7"];
var explanations = {
  Puissance: "Vitesse de sortie de balle pour un effort donné. Diamant > goutte > ronde. Équilibre haut = plus d'inertie. Mousse réactive = catapulte.",
  Contrôle: "Capacité à placer la balle précisément. Ronde > goutte > diamant. Sweet spot large = tolérant. Équilibre bas = plus précis.",
  Confort: "Vibrations transmises au bras/dos. Mousse souple + fibre de verre + anti-vibrations = confort. Carbone rigide (12K, 18K) + mousse dure = inconfortable.",
  Spin: "Accroche de la surface sur la balle pour créer de la rotation. Texture 3D, sable = plus de spin.",
  Maniabilité: "Facilité à bouger la raquette rapidement. Poids léger + équilibre bas = réactif. >370g = pénalisant.",
  Tolérance: "Performance sur frappes décentrées. Grand sweet spot + forme ronde/goutte = pardonne les erreurs. Diamant + carbone rigide = exigeant."
};
var fyConfig = {
  recommended: {
    text: "RECOMMANDÉ",
    bg: "#1B5E20",
    border: "#4CAF50"
  },
  partial: {
    text: "JOUABLE",
    bg: "#E65100",
    border: "#FF9800"
  },
  no: {
    text: "DÉCONSEILLÉ",
    bg: "#B71C1C",
    border: "#E53935"
  }
};
var SCORING_SYSTEM_PROMPT = "You are a padel racket technical analyst. Score rackets using a HYBRID approach: reference scores from review sites calibrated with mechanical guard-rails.\n\nMETHODOLOGY:\n1. If REFERENCE SCORES are provided: use the calibration formula (ref_score - 50) / 5 as your starting point, then apply guard-rail caps below.\n2. If NO reference scores: use PURE MECHANICAL RULES below to calculate from scratch.\n3. Guard-rail caps ALWAYS apply regardless of source. They prevent marketing inflation.\n\nMECHANICAL RULES (1.0 to 10.0, use 0.5 increments):\n\nPUISSANCE (power = ball exit speed per effort):\n- Shape base: Diamant=8.0, Goutte d'eau/Teardrop=6.5, Hybride=6.0, Ronde/Round=5.5\n- Balance modifier: Haut/High: +1.0, Mi-haut/Medium-high: +0.5, Moyen/Medium: 0, Bas/Low: -0.5\n- Core modifier: Reactive/Power foam: +0.5, Standard EVA: 0, Soft/Control foam: -0.5\n- Cap at 10.0\n\nCONTR\xD4LE (control = shot placement precision):\n- Shape base: Ronde=8.5, Hybride=8.0, Goutte d'eau=7.5, Diamant=5.5\n- Surface modifier: Fiberglass/hybrid: +0.5, Pure carbon: 0, Very stiff carbon (12K+): -0.5\n- Sweet spot modifier: Optimized/large: +0.5, Standard: 0\n- GUARD-RAIL: Diamant shape CANNOT exceed 7.5 contr\xF4le (limited sweet spot)\n\nCONFORT (comfort = vibration absorption, arm protection):\n- Core base: Soft foam/Control foam: 7.5, Standard EVA/MLD: 6.0, Hard EVA (HR3 Black): 5.0, Reactive/Power foam: 5.0\n- Surface modifier: Fiberglass dominant: +1.5, Hybrid carbon/glass: +0.5, Pure carbon: 0, Stiff carbon (12K, 18K Xtrem): -0.5\n- Anti-vib tech modifier: Dedicated system (Ease Vibe, Pulse System, Auxetic+SoftCap): +0.5\n- Weight modifier: <355g: +0.5, 355-365g: 0, >365g: -0.5\n- GUARD-RAIL: Stiff carbon (12K Xtrem, TriCarbon) + hard EVA core = NEVER above 6.0\n- GUARD-RAIL: Any reactive/power foam without anti-vib = NEVER above 6.5\n- GUARD-RAIL: Fiberglass + soft foam + anti-vib = MINIMUM 7.0\n\nSPIN (spin = surface grip on ball):\n- Rough 3D + sand texture (Dual Spin, Extreme Spin): 8.5\n- 3D texture only: 7.5\n- Smooth/standard: 6.0\n\nMANIABILIT\xC9 (maneuverability = ease of quick movement):\n- Weight base: <350g: 9.0, 350-355g: 8.0, 355-360g: 7.5, 360-365g: 7.0, 365-370g: 6.5, >370g: 6.0\n- Balance modifier: Bas/Low: +1.0, Moyen/Medium: +0.5, Mi-haut: 0, Haut/High: -0.5\n- Adjustable balance (Weight Balance system): +0.5\n\nTOL\xC9RANCE (forgiveness on off-center hits):\n- Shape base: Ronde=8.5, Hybride=8.0, Goutte d'eau=7.0, Diamant=5.0\n- Sweet spot modifier: Optimized/large: +0.5, Standard: 0\n- Surface modifier: Fiberglass (softer rebound): +0.5, Hybrid: 0, Stiff carbon: -0.5\n- GUARD-RAIL: Diamant shape CANNOT exceed 6.5 tol\xE9rance\n\nVERDICT RULES for player profile \u2014 use these padel-specific style tags:\n- \"Offensif\" \u2192 needs power, rewards diamant/high balance\n- \"D\xE9fensif / Mur\" \u2192 needs control + tolerance, rewards ronde/goutte\n- \"Tactique\" \u2192 needs control + maneuverability\n- \"Puissant / Frappeur\" \u2192 needs power + spin, rewards diamant\n- \"V\xE9loce\" \u2192 needs maneuverability + light weight\n- \"Endurant\" \u2192 needs comfort + tolerance (long rallies = more vibrations)\n- \"Contre-attaquant\" \u2192 needs tolerance + control + decent power\n- \"Polyvalent\" \u2192 needs balanced scores, no extreme weakness\n- \"Technique\" \u2192 needs control + spin\n\nInjury tags impact:\n- \"Dos\" or \"Poignet\" or \"Coude\" \u2192 Confort MUST be >= 7 for \"recommended\"\n- \"Aucune\" \u2192 no injury constraint\n\nforYou classification:\n- \"recommended\": Confort >= 7 if injuries exist, AND matches play style well\n- \"partial\": Confort 5-7 with injuries, OR minor mismatch with style\n- \"no\": Confort < 5 with injuries, OR shape+weight too demanding, OR requires high technique level for casual play\n\nWrite verdict in French, 2 sentences max. Be direct and honest about risks for the player's injuries.\n\nReturn ONLY valid JSON, no markdown, no backticks.";
function getNextColor(rks) {
  var used = new Set(rks.map(function (r) {
    return r.color;
  }));
  return COLORS_POOL.find(function (c) {
    return !used.has(c);
  }) || '#' + Math.floor(Math.random() * 16777215).toString(16).padStart(6, '0');
}
function buildProfileText(p) {
  var styles = p.styleTags.map(function (id) {
    var _STYLE_TAGS$find;
    return (_STYLE_TAGS$find = STYLE_TAGS.find(function (t) {
      return t.id === id;
    })) === null || _STYLE_TAGS$find === void 0 ? void 0 : _STYLE_TAGS$find.label;
  }).filter(Boolean);
  var injuries = p.injuryTags.map(function (id) {
    var _INJURY_TAGS$find;
    return (_INJURY_TAGS$find = INJURY_TAGS.find(function (t) {
      return t.id === id;
    })) === null || _INJURY_TAGS$find === void 0 ? void 0 : _INJURY_TAGS$find.label;
  }).filter(Boolean);
  var priorities = p.priorityTags.map(function (id) {
    var _PRIORITY_TAGS$find;
    return (_PRIORITY_TAGS$find = PRIORITY_TAGS.find(function (t) {
      return t.id === id;
    })) === null || _PRIORITY_TAGS$find === void 0 ? void 0 : _PRIORITY_TAGS$find.label;
  }).filter(Boolean);
  var brands = p.brandTags.map(function (id) {
    var _BRAND_TAGS$find;
    return (_BRAND_TAGS$find = BRAND_TAGS.find(function (t) {
      return t.id === id;
    })) === null || _BRAND_TAGS$find === void 0 ? void 0 : _BRAND_TAGS$find.label;
  }).filter(Boolean);
  var styleStr = [].concat(_toConsumableArray(styles), [p.styleExtra]).filter(Boolean).join(", ") || "Non précisé";
  var injuryStr = [].concat(_toConsumableArray(injuries), [p.injuryExtra]).filter(Boolean).join(", ") || "Aucune";
  var prioStr = [].concat(_toConsumableArray(priorities), [p.priorityExtra]).filter(Boolean).join(", ") || "Non précisé";
  var brandStr = brands.length ? brands.join(", ") : "Toutes marques";
  var physique = [p.age ? "".concat(p.age, " ans") : null, p.height ? "".concat(p.height, "cm") : null, p.weight ? "".concat(p.weight, "kg") : null].filter(Boolean).join(", ");
  return "Joueur: ".concat(physique || "Non renseigné", ". Niveau: ").concat(p.level, ". Main: ").concat(p.hand || "Droitier", ". C\xF4t\xE9: ").concat(p.side, ". Style: ").concat(styleStr, ". Blessures: ").concat(injuryStr, ". Fr\xE9quence: ").concat(p.frequency, ". Comp\xE9tition: ").concat(p.competition ? "Oui" : "Non", ". Priorit\xE9: ").concat(prioStr, ". Marques pr\xE9f\xE9r\xE9es: ").concat(brandStr, ".");
}

// Weighted global score /10 based on player profile
function computeGlobalScore(scores, profile) {
  if (!scores || _typeof(scores) !== 'object') return 0;
  // Base weights (equal)
  var w = {
    Puissance: 1,
    Contrôle: 1,
    Confort: 1,
    Spin: 1,
    Maniabilité: 1,
    Tolérance: 1
  };

  // Priority tags boost relevant criteria
  var prioMap = {
    confort: {
      Confort: 1.5
    },
    polyvalence: {
      Contrôle: 0.5,
      Maniabilité: 0.5,
      Tolérance: 0.5
    },
    puissance: {
      Puissance: 1.5
    },
    controle: {
      Contrôle: 1.5
    },
    spin: {
      Spin: 1.5
    },
    legerete: {
      Maniabilité: 1.5
    },
    protection: {
      Confort: 1.5
    },
    reprise: {
      Confort: 1.5,
      Tolérance: 1.0,
      Maniabilité: 0.5
    }
  };
  var _iterator = _createForOfIteratorHelper(profile.priorityTags || []),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var tag = _step.value;
      var boosts = prioMap[tag];
      if (boosts) for (var _i = 0, _Object$entries = Object.entries(boosts); _i < _Object$entries.length; _i++) {
        var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
          k = _Object$entries$_i[0],
          v = _Object$entries$_i[1];
        w[k] = (w[k] || 1) + v;
      }
    }

    // Style tags influence
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var styleMap = {
    offensif: {
      Puissance: 0.5
    },
    defensif: {
      Contrôle: 0.5,
      Tolérance: 0.5
    },
    tactique: {
      Contrôle: 0.5,
      Maniabilité: 0.3
    },
    puissant: {
      Puissance: 0.5,
      Spin: 0.3
    },
    veloce: {
      Maniabilité: 0.8
    },
    endurant: {
      Confort: 0.5,
      Tolérance: 0.3
    },
    contre: {
      Tolérance: 0.5,
      Contrôle: 0.3
    },
    polyvalent: {
      Contrôle: 0.3,
      Tolérance: 0.3
    },
    technique: {
      Contrôle: 0.5,
      Spin: 0.3
    }
  };
  var _iterator2 = _createForOfIteratorHelper(profile.styleTags || []),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _tag = _step2.value;
      var _boosts = styleMap[_tag];
      if (_boosts) for (var _i2 = 0, _Object$entries2 = Object.entries(_boosts); _i2 < _Object$entries2.length; _i2++) {
        var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2),
          _k = _Object$entries2$_i[0],
          _v = _Object$entries2$_i[1];
        w[_k] = (w[_k] || 1) + _v;
      }
    }

    // Injuries → arm injuries boost Confort, leg injuries boost Maniabilité
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  var ARM_INJURIES = ["dos", "poignet", "coude", "epaule"];
  var LEG_INJURIES = ["genou", "cheville", "mollet", "hanche", "achille"];
  var tags = profile.injuryTags || [];
  var hasArmInjury = tags.some(function (t) {
    return ARM_INJURIES.includes(t);
  });
  var hasLegInjury = tags.some(function (t) {
    return LEG_INJURIES.includes(t);
  });
  if (hasArmInjury) w.Confort = (w.Confort || 1) + 2;
  if (hasLegInjury) w.Maniabilité = (w.Maniabilité || 1) + 1.5;

  // Height influence — shorter players need more maniabilité, taller tolerate more power
  var h = Number(profile.height) || 0;
  if (h > 0 && h < 170) w.Maniabilité = (w.Maniabilité || 1) + 0.5;
  if (h >= 185) w.Puissance = (w.Puissance || 1) + 0.3;

  // Age influence — older players need more comfort, tolerance, maniability
  var age = Number(profile.age) || 0;
  if (age >= 40) {
    w.Confort = (w.Confort || 1) + 0.5;
    w.Tolérance = (w.Tolérance || 1) + 0.3;
  }
  if (age >= 50) {
    w.Confort = (w.Confort || 1) + 0.5;
    w.Maniabilité = (w.Maniabilité || 1) + 0.5;
    w.Tolérance = (w.Tolérance || 1) + 0.3;
  }
  if (age >= 60) {
    w.Confort = (w.Confort || 1) + 0.5;
    w.Tolérance = (w.Tolérance || 1) + 0.5;
  }

  // Side + Hand → attacker vs constructor role
  // Forehand at center = attacker: Droitier+Gauche OR Gaucher+Droite
  // Backhand at center = constructor: Droitier+Droite OR Gaucher+Gauche
  var hand = profile.hand || "Droitier";
  var side = profile.side || "Droite";
  var isAttacker = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
  var isConstructor = hand === "Droitier" && side === "Droite" || hand === "Gaucher" && side === "Gauche";
  if (isAttacker) {
    w.Puissance = (w.Puissance || 1) + 0.5;
    w.Spin = (w.Spin || 1) + 0.3;
  }
  if (isConstructor) {
    w.Contrôle = (w.Contrôle || 1) + 0.5;
    w.Tolérance = (w.Tolérance || 1) + 0.3;
  }

  // Weighted average
  var total = 0,
    wSum = 0;
  var _iterator3 = _createForOfIteratorHelper(ATTRS),
    _step3;
  try {
    for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
      var attr = _step3.value;
      var weight = w[attr] || 1;
      total += (scores[attr] || 0) * weight;
      wSum += weight;
    }
  } catch (err) {
    _iterator3.e(err);
  } finally {
    _iterator3.f();
  }
  return total / wSum;
}

// Dynamic verdict based on pertinence score + injury constraints
// Format score as percentage with 2 decimals (7.724 → "77.24%")
function fmtPct(score) {
  return (score * 10).toFixed(2) + "%";
}
function computeForYou(scores, profile) {
  if (!scores || _typeof(scores) !== 'object') return "no";
  var gs = computeGlobalScore(scores, profile);
  var ARM_INJURIES = ["dos", "poignet", "coude", "epaule"];
  var hasArmInjury = (profile.injuryTags || []).some(function (t) {
    return ARM_INJURIES.includes(t);
  });
  var comfortOk = !hasArmInjury || (scores.Confort || 0) >= 7;
  if (gs >= 7.0 && comfortOk) return "recommended";
  if (hasArmInjury && scores.Confort < 7 && gs < 6.0) return "no";
  return "partial";
}
function TagGroup(_ref) {
  var tags = _ref.tags,
    selected = _ref.selected,
    onToggle = _ref.onToggle,
    _ref$color = _ref.color,
    color = _ref$color === void 0 ? "#f97316" : _ref$color;
  return /*#__PURE__*/PadelAnalyzer_React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 6,
      marginTop: 6,
      paddingLeft: 13
    }
  }, tags.map(function (t) {
    var active = selected.includes(t.id);
    return /*#__PURE__*/PadelAnalyzer_React.createElement("button", {
      key: t.id,
      className: "pa-tag",
      onClick: function onClick() {
        return onToggle(t.id);
      },
      title: t.tip || "",
      style: {
        padding: "5px 12px",
        borderRadius: 20,
        fontSize: 10,
        fontWeight: 600,
        cursor: "pointer",
        background: active ? "".concat(color, "18") : "rgba(255,255,255,0.03)",
        border: "1px solid ".concat(active ? color + "88" : "rgba(255,255,255,0.08)"),
        color: active ? color : "#64748b",
        fontFamily: "'Inter',sans-serif",
        boxShadow: active ? "0 2px 8px ".concat(color, "22") : "none"
      }
    }, active ? "✓ " : "", t.label);
  }));
}

// === RACKET DESCRIPTION GENERATOR ===
function generateRacketDescription(r) {
  var sc = r.scores || {};
  var attrs = ATTRS.map(function (a) {
    return {
      name: a,
      val: sc[a] || 0
    };
  }).sort(function (a, b) {
    return b.val - a.val;
  });
  var top2 = attrs.slice(0, 2);
  var low = attrs[attrs.length - 1];
  var cat = r.category || "intermediaire";
  var catLabels = {
    debutant: "débutant",
    intermediaire: "intermédiaire",
    avance: "avancé",
    expert: "expert",
    junior: "junior"
  };
  var shape = (r.shape || "").toLowerCase();
  var profile = "";
  // Shape-based
  if (shape.includes("diamant") || shape.includes("diamond")) profile = "joueurs offensifs qui cherchent de la puissance en smash";else if (shape.includes("ronde") || shape.includes("round")) profile = "joueurs défensifs ou polyvalents qui valorisent le contrôle";else if (shape.includes("goutte") || shape.includes("drop") || shape.includes("hybride")) profile = "joueurs polyvalents qui veulent un bon équilibre attaque/défense";else profile = "joueurs polyvalents";

  // Level
  var levelStr = catLabels[cat] || "intermédiaire";

  // Build description
  var desc = "Raquette de niveau ".concat(levelStr, ". ");
  desc += "Points forts : ".concat(top2[0].name, " (").concat(top2[0].val, "/10) et ").concat(top2[1].name, " (").concat(top2[1].val, "/10). ");
  if (low.val <= 5) desc += "Point faible : ".concat(low.name, " (").concat(low.val, "/10). ");
  desc += "Id\xE9ale pour les ".concat(profile, ". ");

  // Injury/comfort note
  if (sc["Confort"] >= 8) desc += "Son excellent confort la rend adaptée aux joueurs sujets aux tendinites.";else if (sc["Confort"] <= 5) desc += "⚠ Peu confortable, déconseillée si vous avez des douleurs articulaires.";
  return desc;
}

// === TOP RACKETS BY ATTRIBUTE ===
var MAGAZINE_CATEGORIES = [{
  id: "puissance",
  label: "💥 Puissance",
  attr: "Puissance",
  desc: "Les frappes les plus dévastatrices"
}, {
  id: "controle",
  label: "🎯 Contrôle",
  attr: "Contrôle",
  desc: "Précision chirurgicale"
}, {
  id: "confort",
  label: "🛡 Confort",
  attr: "Confort",
  desc: "Protège tes articulations"
}, {
  id: "spin",
  label: "🌀 Spin",
  attr: "Spin",
  desc: "Effets et rotations maximales"
}, {
  id: "polyvalence",
  label: "⚖️ Polyvalence",
  attr: null,
  desc: "Les meilleures all-round"
}, {
  id: "rapport",
  label: "💰 Rapport qualité/prix",
  attr: null,
  desc: "Performances au meilleur prix"
}];
function getTopByCategory(catId, year) {
  var n = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 5;
  var cat = MAGAZINE_CATEGORIES.find(function (c) {
    return c.id === catId;
  });
  if (!cat) return [];
  var pool = rackets_db_namespaceObject.filter(function (r) {
    return !year || r.year === year;
  });
  if (catId === "polyvalence") {
    // Score = min score across all attrs (best worst attribute)
    pool = pool.map(function (r) {
      var sc = r.scores || {};
      var vals = ATTRS.map(function (a) {
        return sc[a] || 0;
      });
      var minV = Math.min.apply(Math, _toConsumableArray(vals));
      var avgV = vals.reduce(function (a, b) {
        return a + b;
      }, 0) / vals.length;
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _sortScore: minV * 0.6 + avgV * 0.4
      });
    });
  } else if (catId === "rapport") {
    pool = pool.filter(function (r) {
      return r.price;
    }).map(function (r) {
      var sc = r.scores || {};
      var avg = ATTRS.map(function (a) {
        return sc[a] || 0;
      }).reduce(function (a, b) {
        return a + b;
      }, 0) / 6;
      var priceNums = (r.price || "").match(/\d+/g);
      var priceNum = priceNums ? priceNums.length > 1 ? (parseInt(priceNums[0]) + parseInt(priceNums[1])) / 2 : parseInt(priceNums[0]) : 300;
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _sortScore: priceNum > 0 ? avg / (priceNum / 100) : 0
      });
    });
  } else {
    pool = pool.map(function (r) {
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _sortScore: (r.scores || {})[cat.attr] || 0
      });
    });
  }
  pool.sort(function (a, b) {
    return b._sortScore - a._sortScore;
  });
  return pool.slice(0, n);
}

// === DEEP ANALYSIS — 100% deterministic, zero AI ===
function generateDeepAnalysis(profile, ranked, attrs) {
  if (!ranked.length) return [];
  var best = ranked[0],
    second = ranked[1],
    bestScore = best.globalScore;
  var lines = [];
  var styles = profile.styleTags || [],
    priorities = profile.priorityTags || [];
  var brands = profile.brandTags || [],
    injuries = (profile.injuryTags || []).filter(function (t) {
      return t !== "aucune";
    });
  var OFF_S = ["offensif", "puissant"],
    DEF_S = ["defensif", "contre", "endurant"],
    TECH_S = ["tactique", "technique"];
  var offC = styles.filter(function (s) {
      return OFF_S.includes(s);
    }).length,
    defC = styles.filter(function (s) {
      return DEF_S.includes(s);
    }).length;
  var techC = styles.filter(function (s) {
    return TECH_S.includes(s);
  }).length;
  var POW_P = ["puissance", "spin"],
    CTL_P = ["controle", "confort", "protection", "reprise"];
  var offP = priorities.filter(function (p) {
      return POW_P.includes(p);
    }).length,
    defP = priorities.filter(function (p) {
      return CTL_P.includes(p);
    }).length;
  var hasTension = offP > 0 && defC >= 2 || defP > 0 && offC >= 2;
  var isComplex = styles.length >= 4 || styles.length >= 3 && hasTension;
  // Compute weights (mirror computeGlobalScore)
  var w = {};
  attrs.forEach(function (a) {
    return w[a] = 1;
  });
  var PM = {
    confort: {
      Confort: 1.5
    },
    polyvalence: {
      Contrôle: 0.5,
      Maniabilité: 0.5,
      Tolérance: 0.5
    },
    puissance: {
      Puissance: 1.5
    },
    controle: {
      Contrôle: 1.5
    },
    spin: {
      Spin: 1.5
    },
    legerete: {
      Maniabilité: 1.5
    },
    protection: {
      Confort: 1.5
    },
    reprise: {
      Confort: 1.5,
      Tolérance: 1,
      Maniabilité: 0.5
    }
  };
  priorities.forEach(function (t) {
    var b = PM[t];
    if (b) Object.entries(b).forEach(function (_ref2) {
      var _ref3 = _slicedToArray(_ref2, 2),
        k = _ref3[0],
        v = _ref3[1];
      return w[k] = (w[k] || 1) + v;
    });
  });
  var SM = {
    offensif: {
      Puissance: 0.5
    },
    defensif: {
      Contrôle: 0.5,
      Tolérance: 0.5
    },
    tactique: {
      Contrôle: 0.5,
      Maniabilité: 0.3
    },
    puissant: {
      Puissance: 0.5,
      Spin: 0.3
    },
    veloce: {
      Maniabilité: 0.8
    },
    endurant: {
      Confort: 0.5,
      Tolérance: 0.3
    },
    contre: {
      Tolérance: 0.5,
      Contrôle: 0.3
    },
    polyvalent: {
      Contrôle: 0.3,
      Tolérance: 0.3
    },
    technique: {
      Contrôle: 0.5,
      Spin: 0.3
    }
  };
  styles.forEach(function (t) {
    var b = SM[t];
    if (b) Object.entries(b).forEach(function (_ref4) {
      var _ref5 = _slicedToArray(_ref4, 2),
        k = _ref5[0],
        v = _ref5[1];
      return w[k] = (w[k] || 1) + v;
    });
  });
  var ARM_I = ["dos", "poignet", "coude", "epaule"],
    LEG_I = ["genou", "cheville", "mollet", "hanche", "achille"];
  if (injuries.some(function (t) {
    return ARM_I.includes(t);
  })) w.Confort += 2;
  if (injuries.some(function (t) {
    return LEG_I.includes(t);
  })) w.Maniabilité += 1.5;
  var h = Number(profile.height) || 0,
    age = Number(profile.age) || 0;
  if (h > 0 && h < 170) w.Maniabilité += 0.5;
  if (h >= 185) w.Puissance += 0.3;
  if (age >= 40) {
    w.Confort += 0.5;
    w.Tolérance += 0.3;
  }
  if (age >= 50) {
    w.Confort += 0.5;
    w.Maniabilité += 0.5;
    w.Tolérance += 0.3;
  }
  if (age >= 60) {
    w.Confort += 0.5;
    w.Tolérance += 0.5;
  }
  var hand = profile.hand || "Droitier",
    side = profile.side || "Droite";
  var isAtt = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
  var isCon = hand === "Droitier" && side === "Droite" || hand === "Gaucher" && side === "Gauche";
  if (isAtt) {
    w.Puissance += 0.5;
    w.Spin += 0.3;
  }
  if (isCon) {
    w.Contrôle += 0.5;
    w.Tolérance += 0.3;
  }
  var wT = Object.values(w).reduce(function (s, v) {
    return s + v;
  }, 0);
  var wS = attrs.map(function (a) {
    return {
      attr: a,
      weight: w[a],
      pct: Math.round(w[a] / wT * 100)
    };
  }).sort(function (a, b) {
    return b.weight - a.weight;
  });
  var top2 = wS.slice(0, 2),
    top2pct = top2.reduce(function (s, x) {
      return s + x.pct;
    }, 0),
    rest4pct = 100 - top2pct;
  // Top 3 orientation
  var t3C = ranked.slice(0, Math.min(3, ranked.length)).reduce(function (s, r) {
    return s + (r.scores["Contrôle"] || 0);
  }, 0) / Math.min(3, ranked.length);
  var t3P = ranked.slice(0, Math.min(3, ranked.length)).reduce(function (s, r) {
    return s + (r.scores.Puissance || 0);
  }, 0) / Math.min(3, ranked.length);
  var t3CtrlOriented = t3C > t3P + 0.5;
  var wSaysPow = top2[0].attr === "Puissance" || top2[1].attr === "Puissance";
  var paradox = wSaysPow && t3CtrlOriented;
  var SL = {
    defensif: "Défensif/Mur",
    veloce: "Véloce",
    endurant: "Endurant",
    contre: "Contre-attaquant",
    technique: "Technique",
    puissant: "Puissant/Frappeur",
    offensif: "Offensif",
    tactique: "Tactique",
    polyvalent: "Polyvalent"
  };
  var PL = {
    puissance: "Puissance",
    spin: "Spin",
    controle: "Contrôle",
    confort: "Confort",
    legerete: "Légèreté",
    protection: "Protection bras",
    reprise: "Reprise en douceur",
    polyvalence: "Polyvalence"
  };
  // §1 Profile reading
  var prioLabelStr = priorities.map(function (p) {
    return PL[p] || p;
  }).filter(Boolean);
  var isYoung = age > 0 && age < 16;
  var noStyles = styles.length === 0;
  var hasInjuries = injuries.length > 0;
  var injLabels = injuries.map(function (t) {
    var m = {
      dos: "Dos",
      poignet: "Poignet",
      coude: "Tennis elbow",
      epaule: "Épaule",
      genou: "Genou",
      cheville: "Cheville",
      mollet: "Mollet",
      hanche: "Hanche",
      achille: "Tendon d'Achille"
    };
    return m[t] || t;
  });
  var tensionOffPrioDefStyle = offP > 0 && defC >= 2;
  var tensionDefPrioOffStyle = defP > 0 && offC >= 2;
  if (noStyles) {
    if (isYoung) {
      lines.push("Profil junior (".concat(age, " ans, ").concat(h, "cm). Pas de style de jeu d\xE9clar\xE9 \u2014 l'algorithme se base sur les priorit\xE9s (").concat(prioLabelStr.join(", "), ") et les caract\xE9ristiques physiques. Pour un jeune joueur, la maniabilit\xE9 et le confort sont naturellement surpond\xE9r\xE9s."));
    } else if (hasInjuries && priorities.length > 0) {
      lines.push("Pas de style d\xE9clar\xE9, mais les blessures (".concat(injLabels.join(", "), ") p\xE8sent lourd sur le scoring. Combin\xE9es aux priorit\xE9s ").concat(prioLabelStr.join(" et "), ", elles orientent le classement vers le confort et la protection."));
    } else if (priorities.length > 0) {
      lines.push("Pas de style de jeu d\xE9clar\xE9 \u2014 le classement repose sur les priorit\xE9s (".concat(prioLabelStr.join(", "), ") et le profil physique. Avec moins de contraintes \xE0 croiser, le score plafond sera plus \xE9lev\xE9."));
    } else {
      lines.push("Profil minimaliste : ni style ni priorit\xE9 d\xE9clar\xE9s. Le classement est bas\xE9 sur les raquettes les plus \xE9quilibr\xE9es globalement.");
    }
  } else if (isComplex && tensionOffPrioDefStyle) {
    var ds = styles.filter(function (s) {
      return DEF_S.includes(s);
    }).map(function (s) {
      return SL[s] || s;
    }).join(", ");
    var op = priorities.filter(function (p) {
      return POW_P.includes(p);
    }).map(function (p) {
      return PL[p] || p;
    }).join(" et ");
    var ts = styles.filter(function (s) {
      return TECH_S.includes(s);
    }).map(function (s) {
      return SL[s] || s;
    });
    var extra = ts.length ? ", plus ".concat(ts.join(" et "), " qui renforce").concat(ts.length > 1 ? "nt" : "", " le Contr\xF4le") : "";
    lines.push("Profil exigeant : ".concat(styles.length, " styles dont ").concat(defC, " d\xE9fensifs (").concat(ds, ")").concat(extra, ", face \xE0 des priorit\xE9s offensives (").concat(op, "). Le syst\xE8me doit arbitrer entre ces deux tendances."));
  } else if (isComplex && tensionDefPrioOffStyle) {
    var os = styles.filter(function (s) {
      return OFF_S.includes(s);
    }).map(function (s) {
      return SL[s] || s;
    }).join(", ");
    var dp = priorities.filter(function (p) {
      return CTL_P.includes(p);
    }).map(function (p) {
      return PL[p] || p;
    }).join(" et ");
    lines.push("Profil paradoxal : styles offensifs (".concat(os, ") mais priorit\xE9s orient\xE9es protection (").concat(dp, ")").concat(hasInjuries ? " — probablement lié aux blessures (" + injLabels.join(", ") + ")" : "", ". Le syst\xE8me cherche des raquettes qui tapent fort tout en pr\xE9servant le corps."));
  } else if (isComplex) {
    lines.push("Profil riche avec ".concat(styles.length, " styles. L'algorithme croise tous ces param\xE8tres pour pond\xE9rer les 6 crit\xE8res. Plus le profil est diversifi\xE9, plus le score plafond baisse \u2014 aucune raquette ne peut exceller partout."));
  } else if (offC > 0 && defC === 0 && !hasInjuries) {
    lines.push("Profil offensif cibl\xE9. L'algorithme surpond\xE8re Puissance et Spin \u2014 les diamants lourdes devraient dominer.");
  } else if (offC > 0 && defC === 0 && hasInjuries) {
    lines.push("Profil offensif temp\xE9r\xE9 par les blessures (".concat(injLabels.join(", "), "). Le Confort est boost\xE9 pour pr\xE9server le corps malgr\xE9 le style agressif."));
  } else if (defC > 0 && offC === 0) {
    if (hasInjuries) {
      lines.push("Profil d\xE9fensif renforc\xE9 par les blessures (".concat(injLabels.join(", "), "). Le Confort devient prioritaire \u2014 les rondes l\xE9g\xE8res et tol\xE9rantes domineront."));
    } else {
      lines.push("Profil orient\xE9 d\xE9fense et contr\xF4le. Les rondes et hybrides vont naturellement dominer le classement.");
    }
  } else if (styles.length <= 2) {
    var styleStr = styles.map(function (s) {
      return SL[s] || s;
    }).join(" + ");
    lines.push("Profil cibl\xE9 (".concat(styleStr).concat(prioLabelStr.length ? ", priorité " + prioLabelStr.join("/") : "", "). Peu de crit\xE8res en conflit \u2014 le classement sera tranch\xE9."));
  } else {
    lines.push("Profil \xE9quilibr\xE9 entre ".concat(styles.length, " styles. Le classement r\xE9compense les raquettes les plus polyvalentes."));
  }
  // §2 Weight explanation — prose, not data dump
  var pct = function pct(v) {
    return (v * 10).toFixed(1);
  };
  var pctRound = function pctRound(v) {
    return Math.round(v * 10);
  };
  // Paradox only makes sense when defensive styles actually exist AND outweigh priorities
  var realParadox = paradox && defC >= 2 && rest4pct > top2pct;
  if (realParadox) {
    lines.push("R\xE9sultat contre-intuitif mais logique : malgr\xE9 des priorit\xE9s ".concat(top2[0].attr, " et ").concat(top2[1].attr, ", ce sont des raquettes orient\xE9es contr\xF4le qui dominent le classement. Pourquoi ? Parce que les ").concat(defC, " styles d\xE9fensifs").concat(techC > 0 ? " et le style technique" : "", " gonflent le poids de 4 autres crit\xE8res (Contr\xF4le, Tol\xE9rance, Confort, Maniabilit\xE9) qui, ensemble, p\xE8sent plus lourd (").concat(rest4pct, "%) que les 2 priorit\xE9s (").concat(top2pct, "%). Une raquette qui score 8-9 sur ces 4 axes compense largement un 7 en Puissance."));
  } else if (noStyles && priorities.length > 0) {
    // Simple profile driven by priorities
    lines.push("Le classement est pilot\xE9 par les priorit\xE9s ".concat(prioLabelStr.join(" et "), " qui concentrent ").concat(top2pct, "% du poids. Les raquettes les mieux not\xE9es sur ").concat(top2[0].attr).concat(top2[1] ? " et " + top2[1].attr : "", " arrivent naturellement en t\xEAte."));
  } else if (top2[0].pct >= 25) {
    lines.push("Le profil tire clairement vers ".concat(top2[0].attr, " et ").concat(top2[1].attr, ", qui concentrent ").concat(top2pct, "% du poids total. Le classement favorise logiquement les raquettes fortes sur ces deux axes."));
  } else {
    lines.push("Aucun crit\xE8re ne domine massivement \u2014 le classement r\xE9compense les raquettes les plus \xE9quilibr\xE9es sur l'ensemble des 6 axes.");
  }
  // §3 Score ceiling
  if (bestScore < 7.8) {
    lines.push("Score plafond \xE0 ".concat(pct(bestScore), "% \u2014 ").concat(isComplex ? "normal pour un profil à " + styles.length + " styles qui tirent dans des directions différentes. " : "", "Aucune raquette du march\xE9 ne peut satisfaire tous les crit\xE8res \xE0 la fois. Un score de ").concat(pctRound(bestScore), "% signifie meilleur compromis r\xE9aliste, pas un choix m\xE9diocre."));
  } else if (bestScore >= 8.5) {
    lines.push("Score \xE9lev\xE9 \xE0 ".concat(pct(bestScore), "%").concat(noStyles || styles.length <= 2 ? " — logique pour un profil avec peu de contraintes à satisfaire" : " — profil cohérent, la n°1 répond bien à l'ensemble des critères", ".").concat(second && bestScore - second.globalScore >= 0.1 ? " L'avance est nette." : ""));
  } else {
    // 7.8 - 8.5
    var tight = second && bestScore - second.globalScore < 0.05;
    if (noStyles) {
      lines.push("Score \xE0 ".concat(pct(bestScore), "% \u2014 correct pour un profil sans style d\xE9clar\xE9, guid\xE9 par ").concat(prioLabelStr.length ? prioLabelStr.join(" et ") : "les paramètres physiques", ".").concat(tight ? " Le peloton de tête est très serré, le ressenti en main fera la différence." : ""));
    } else {
      lines.push("Score \xE0 ".concat(pct(bestScore), "% \u2014 bon compromis.").concat(tight ? " Le peloton de tête est très serré, le ressenti en main fera la différence." : ""));
    }
  }
  // §4 Brand analysis
  if (brands.length > 0) {
    // Check which brands are actually in Top 3
    var brandsInTop3 = brands.filter(function (b) {
      return ranked.slice(0, 3).some(function (r) {
        return (r.brand || "").toLowerCase().replace(/\s/g, "").includes(b);
      });
    });
    var brandsNotInTop3 = brands.filter(function (b) {
      return !brandsInTop3.includes(b);
    });
    var bInLabels = brandsInTop3.map(function (b) {
      return b.charAt(0).toUpperCase() + b.slice(1);
    });
    var bOutLabels = brandsNotInTop3.map(function (b) {
      return b.charAt(0).toUpperCase() + b.slice(1);
    });
    var bestBR = brandsNotInTop3.length > 0 ? ranked.find(function (r) {
      return brandsNotInTop3.some(function (b) {
        return (r.brand || "").toLowerCase().replace(/\s/g, "").includes(b);
      });
    }) : null;
    if (brandsInTop3.length === brands.length) {
      // All preferred brands are in Top 3
      var verb = bInLabels.length > 1 ? "sont représentées" : "est représentée";
      lines.push("".concat(bInLabels.join(", "), " ").concat(verb, " dans le Top 3 \u2014 ").concat(bInLabels.length > 1 ? "les marques préférées proposent" : "la marque préférée propose", " des mod\xE8les techniquement adapt\xE9s au profil."));
    } else if (brandsInTop3.length > 0 && brandsNotInTop3.length > 0) {
      // Mixed: some brands in, some out
      var inStr = bInLabels.join(", ");
      var outStr = bOutLabels.join(", ");
      var line = "".concat(inStr, " ").concat(bInLabels.length > 1 ? "sont dans" : "est dans", " le Top 3.");
      if (bestBR) {
        line += " ".concat(outStr, " absente \u2014 la meilleure est la ").concat(bestBR.name, " \xE0 ").concat((bestBR.globalScore * 10).toFixed(1), "%.");
      }
      lines.push(line);
    } else if (bestBR) {
      // No brand in Top 3
      var gap = (bestScore - bestBR.globalScore) * 10;
      lines.push("Marque pr\xE9f\xE9r\xE9e ".concat(bL.join("/"), " absente du Top 3 : la meilleure est la ").concat(bestBR.name, " \xE0 ").concat((bestBR.globalScore * 10).toFixed(1), "% (").concat(gap.toFixed(1), " pts sous la n\xB01). L'\xE9cart technique ne peut pas \xEAtre combl\xE9 par un simple bonus marque \u2014 le classement reste objectif."));
    }
  }
  // §5 Top 3 shapes
  if (ranked.length >= 3) {
    var shapes = ranked.slice(0, 3).map(function (r) {
      return r.shape;
    }).filter(Boolean);
    var uniq = _toConsumableArray(new Set(shapes));
    if (uniq.length === 1 && uniq[0] === "Ronde") lines.push("Les 3 finalistes sont des rondes : forme offrant le meilleur compromis contr\xF4le/tol\xE9rance, coh\xE9rent quand ces crit\xE8res p\xE8sent lourd.");else if (uniq.length === 1 && uniq[0] === "Diamant") lines.push("Les 3 finalistes sont des diamants : logique pour un profil puissance pure.");else if (uniq.length >= 2) lines.push("Le podium m\xE9lange ".concat(uniq.join(" et "), " \u2014 le profil est assez polyvalent pour que diff\xE9rentes g\xE9om\xE9tries scorent bien."));
  }
  return lines;
}
function PadelAnalyzer() {
  var _useState = (0,external_React_.useState)(function () {
      var saved = loadSavedRackets();
      return saved.length ? saved : INITIAL_RACKETS;
    }),
    _useState2 = _slicedToArray(_useState, 2),
    rackets = _useState2[0],
    setRackets = _useState2[1];
  var _useState3 = (0,external_React_.useState)(function () {
      var saved = loadSavedRackets();
      return saved.length ? saved.slice(0, Math.min(saved.length, 4)).map(function (r) {
        return r.id;
      }) : [];
    }),
    _useState4 = _slicedToArray(_useState3, 2),
    selected = _useState4[0],
    setSelected = _useState4[1];
  var _useState5 = (0,external_React_.useState)("radar"),
    _useState6 = _slicedToArray(_useState5, 2),
    tab = _useState6[0],
    setTab = _useState6[1];
  var _useState7 = (0,external_React_.useState)(false),
    _useState8 = _slicedToArray(_useState7, 2),
    showArena = _useState8[0],
    setShowArena = _useState8[1];
  var _useState9 = (0,external_React_.useState)(null),
    _useState0 = _slicedToArray(_useState9, 2),
    openAttr = _useState0[0],
    setOpenAttr = _useState0[1];
  var _useState1 = (0,external_React_.useState)(function () {
      return loadSavedProfile();
    }),
    _useState10 = _slicedToArray(_useState1, 2),
    profile = _useState10[0],
    setProfile = _useState10[1];
  var _useState11 = (0,external_React_.useState)(null),
    _useState12 = _slicedToArray(_useState11, 2),
    panel = _useState12[0],
    setPanel = _useState12[1];
  var _useState13 = (0,external_React_.useState)(""),
    _useState14 = _slicedToArray(_useState13, 2),
    searchQ = _useState14[0],
    setSearchQ = _useState14[1];
  var _useState15 = (0,external_React_.useState)(false),
    _useState16 = _slicedToArray(_useState15, 2),
    loading = _useState16[0],
    setLoading = _useState16[1];
  var _useState17 = (0,external_React_.useState)(""),
    _useState18 = _slicedToArray(_useState17, 2),
    loadMsg = _useState18[0],
    setLoadMsg = _useState18[1];
  var _useState19 = (0,external_React_.useState)(""),
    _useState20 = _slicedToArray(_useState19, 2),
    error = _useState20[0],
    setError = _useState20[1];
  var _useState21 = (0,external_React_.useState)(null),
    _useState22 = _slicedToArray(_useState21, 2),
    suggestions = _useState22[0],
    setSuggestions = _useState22[1];
  var _useState23 = (0,external_React_.useState)(null),
    _useState24 = _slicedToArray(_useState23, 2),
    suggestResults = _useState24[0],
    setSuggestResults = _useState24[1];
  var _useState25 = (0,external_React_.useState)(new Set()),
    _useState26 = _slicedToArray(_useState25, 2),
    suggestChecked = _useState26[0],
    setSuggestChecked = _useState26[1];
  var _useState27 = (0,external_React_.useState)(false),
    _useState28 = _slicedToArray(_useState27, 2),
    addingBatch = _useState28[0],
    setAddingBatch = _useState28[1];
  var _useState29 = (0,external_React_.useState)(""),
    _useState30 = _slicedToArray(_useState29, 2),
    batchProgress = _useState30[0],
    setBatchProgress = _useState30[1];
  var _useState31 = (0,external_React_.useState)(function () {
      var p = loadSavedProfile();
      return p._name || "";
    }),
    _useState32 = _slicedToArray(_useState31, 2),
    profileName = _useState32[0],
    setProfileName = _useState32[1];
  var _useState33 = (0,external_React_.useState)(function () {
      return loadProfilesList();
    }),
    _useState34 = _slicedToArray(_useState33, 2),
    savedProfiles = _useState34[0],
    setSavedProfiles = _useState34[1];
  var _useState35 = (0,external_React_.useState)(""),
    _useState36 = _slicedToArray(_useState35, 2),
    profileSearchTerm = _useState36[0],
    setProfileSearchTerm = _useState36[1];
  var _useState37 = (0,external_React_.useState)(0),
    _useState38 = _slicedToArray(_useState37, 2),
    activeProfileIdx = _useState38[0],
    setActiveProfileIdx = _useState38[1];
  var carouselRef = (0,external_React_.useRef)(null);
  var _useState39 = (0,external_React_.useState)(null),
    _useState40 = _slicedToArray(_useState39, 2),
    hoveredRacket = _useState40[0],
    setHoveredRacket = _useState40[1];
  var _useState41 = (0,external_React_.useState)(function () {
      try {
        return JSON.parse(localStorage.getItem('padel_db_extra') || '[]').length;
      } catch (_unused6) {
        return 0;
      }
    }),
    _useState42 = _slicedToArray(_useState41, 2),
    localDBCount = _useState42[0],
    setLocalDBCount = _useState42[1];
  var _useState43 = (0,external_React_.useState)(function () {
      var p = loadSavedProfile();
      return p._name ? "dashboard" : "home";
    }),
    _useState44 = _slicedToArray(_useState43, 2),
    screen = _useState44[0],
    setScreen = _useState44[1];
  var _useState45 = (0,external_React_.useState)(0),
    _useState46 = _slicedToArray(_useState45, 2),
    wizardStep = _useState46[0],
    setWizardStep = _useState46[1];
  var _useState47 = (0,external_React_.useState)(0),
    _useState48 = _slicedToArray(_useState47, 2),
    revealIdx = _useState48[0],
    setRevealIdx = _useState48[1];
  var _useState49 = (0,external_React_.useState)(null),
    _useState50 = _slicedToArray(_useState49, 2),
    confirmModal = _useState50[0],
    setConfirmModal = _useState50[1]; // { message, onConfirm }
  var _useState51 = (0,external_React_.useState)(null),
    _useState52 = _slicedToArray(_useState51, 2),
    passwordModal = _useState52[0],
    setPasswordModal = _useState52[1]; // { mode:'unlock'|'setpin'|'lock', profileName, onSuccess }
  var _useState53 = (0,external_React_.useState)(""),
    _useState54 = _slicedToArray(_useState53, 2),
    pinInput = _useState54[0],
    setPinInput = _useState54[1];
  var _useState55 = (0,external_React_.useState)(""),
    _useState56 = _slicedToArray(_useState55, 2),
    pinError = _useState56[0],
    setPinError = _useState56[1];
  var _useState57 = (0,external_React_.useState)("puissance"),
    _useState58 = _slicedToArray(_useState57, 2),
    magCat = _useState58[0],
    setMagCat = _useState58[1];
  var _useState59 = (0,external_React_.useState)(2026),
    _useState60 = _slicedToArray(_useState59, 2),
    magYear = _useState60[0],
    setMagYear = _useState60[1];
  var _useState61 = (0,external_React_.useState)(null),
    _useState62 = _slicedToArray(_useState61, 2),
    magDetail = _useState62[0],
    setMagDetail = _useState62[1]; // racket to show in detail
  var _useState63 = (0,external_React_.useState)(null),
    _useState64 = _slicedToArray(_useState63, 2),
    addDetail = _useState64[0],
    setAddDetail = _useState64[1]; // index of expanded search result

  // Auto-save rackets and profile to localStorage
  (0,external_React_.useEffect)(function () {
    saveRackets(rackets);
  }, [rackets]);
  (0,external_React_.useEffect)(function () {
    try {
      localStorage.setItem('padel_profile', JSON.stringify(PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, profile), {}, {
        _name: profileName
      })));
    } catch (_unused7) {}
  }, [profile, profileName]);
  var toggleRacket = function toggleRacket(id) {
    setSelected(function (p) {
      return p.includes(id) ? p.length > 1 ? p.filter(function (r) {
        return r !== id;
      }) : p : p.length < 4 ? [].concat(_toConsumableArray(p), [id]) : p;
    });
  };
  var removeRacket = function removeRacket(id) {
    setRackets(function (p) {
      return p.filter(function (r) {
        return r.id !== id;
      });
    });
    setSelected(function (p) {
      var n = p.filter(function (r) {
        return r !== id;
      });
      return n.length ? n : rackets.filter(function (r) {
        return r.id !== id;
      }).length ? [rackets.filter(function (r) {
        return r.id !== id;
      })[0].id] : [];
    });
  };
  var toggleTag = function toggleTag(field, id) {
    setProfile(function (p) {
      var cur = p[field];
      if (field === "injuryTags" && id === "aucune") return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, PadelAnalyzer_defineProperty({}, field, cur.includes("aucune") ? [] : ["aucune"]));
      if (field === "injuryTags" && cur.includes("aucune")) return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, PadelAnalyzer_defineProperty({}, field, [id]));
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, PadelAnalyzer_defineProperty({}, field, cur.includes(id) ? cur.filter(function (x) {
        return x !== id;
      }) : [].concat(_toConsumableArray(cur), [id])));
    });
  };

  // Home screen: select existing profile
  var selectHomeProfile = function selectHomeProfile(sp) {
    setProfile(PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, INITIAL_PROFILE), sp.profile));
    setProfileName(sp.name);
    setPanel(null);
    setScreen("dashboard");
  };
  // Home screen: create new profile → wizard flow
  var createNewProfile = function createNewProfile() {
    setProfile(PadelAnalyzer_objectSpread({}, INITIAL_PROFILE));
    setProfileName("");
    setWizardStep(0);
    setPanel(null);
    setScreen("wizard");
  };
  // Disconnect: clear session and back to home
  var disconnect = function disconnect() {
    setPanel(null);
    setRackets([]);
    setSelected([]);
    setSuggestResults(null);
    setSuggestChecked(new Set());
    setError("");
    setScreen("home");
  };
  // Dashboard → App with Top 3 loaded
  var launchAnalysis = function launchAnalysis(top3Rackets) {
    if (top3Rackets && top3Rackets.length) {
      var loaded = top3Rackets.map(function (r, i) {
        var color = COLORS_POOL[i % COLORS_POOL.length];
        return {
          id: r.id + '-' + Date.now() + '-' + i,
          name: r.name,
          shortName: r.shortName || r.name.slice(0, 28),
          brand: r.brand,
          shape: r.shape,
          weight: r.weight,
          balance: r.balance || "—",
          surface: r.surface || "—",
          core: r.core || "—",
          price: r.price || "—",
          player: r.player || "—",
          color: color,
          imageUrl: r.imageUrl || null,
          scores: r.scores,
          verdict: r.verdict || "Analyse non disponible",
          forYou: "partial",
          refSource: "Base Padel Analyzer",
          _fromDB: true,
          _incomplete: false
        };
      });
      setRackets(loaded);
      setSelected(loaded.slice(0, 4).map(function (r) {
        return r.id;
      }));
    }
    setPanel(null);
    setScreen("app");
  };
  // Dashboard → App without loading (keep existing session)
  var goToApp = function goToApp() {
    setPanel(null);
    setScreen("app");
  };
  // Go back to dashboard from app
  var goToDashboard = function goToDashboard() {
    setPanel(null);
    setScreen("dashboard");
  };
  var selRackets = rackets.filter(function (r) {
    return selected.includes(r.id);
  });
  var radarData = ATTRS.map(function (a) {
    var pt = {
      attribute: a,
      "— 10/10 —": 10
    };
    selRackets.forEach(function (r) {
      pt[r.shortName] = Number(r.scores[a]) || 0;
    });
    return pt;
  });
  var profileText = buildProfileText(profile);
  var rescoreRacket = /*#__PURE__*/function () {
    var _ref6 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(id) {
      var r, scored, _t;
      return _regenerator().w(function (_context) {
        while (1) switch (_context.p = _context.n) {
          case 0:
            r = rackets.find(function (x) {
              return x.id === id;
            });
            if (r) {
              _context.n = 1;
              break;
            }
            return _context.a(2);
          case 1:
            setLoading(true);
            setLoadMsg("\uD83D\uDD04 Re-scoring ".concat(r.shortName, "..."));
            _context.p = 2;
            // Clear cache for this racket
            try {
              localStorage.removeItem(getCacheKey(r.name, r.brand));
            } catch (_unused8) {}
            _context.n = 3;
            return fetchAndScoreRacket(r.name, r.brand, r.color);
          case 3:
            scored = _context.v;
            setRackets(function (p) {
              return p.map(function (x) {
                return x.id === id ? PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, scored), {}, {
                  id: id
                }) : x;
              });
            });
            _context.n = 5;
            break;
          case 4:
            _context.p = 4;
            _t = _context.v;
            setError("❌ Échec du re-scoring: " + _t.message);
          case 5:
            _context.p = 5;
            setLoading(false);
            setLoadMsg("");
            return _context.f(5);
          case 6:
            return _context.a(2);
        }
      }, _callee, null, [[2, 4, 5, 6]]);
    }));
    return function rescoreRacket(_x) {
      return _ref6.apply(this, arguments);
    };
  }();

  // ============================================================
  // FIXED: system prompt goes in body.system, not in messages
  // ============================================================
  function stripCiteTags(text) {
    return text.replace(/<\/?cite[^>]*>/gi, '').replace(/<\/?source[^>]*>/gi, '');
  }
  function callAI(_x2) {
    return _callAI.apply(this, arguments);
  }
  function _callAI() {
    _callAI = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee7(msgs) {
      var _ref26,
        _ref26$webSearch,
        webSearch,
        _ref26$systemPrompt,
        systemPrompt,
        _ref26$maxTokens,
        maxTokens,
        _ref26$retries,
        retries,
        body,
        _loop2,
        _ret,
        attempt,
        _args9 = arguments;
      return _regenerator().w(function (_context9) {
        while (1) switch (_context9.n) {
          case 0:
            _ref26 = _args9.length > 1 && _args9[1] !== undefined ? _args9[1] : {}, _ref26$webSearch = _ref26.webSearch, webSearch = _ref26$webSearch === void 0 ? false : _ref26$webSearch, _ref26$systemPrompt = _ref26.systemPrompt, systemPrompt = _ref26$systemPrompt === void 0 ? null : _ref26$systemPrompt, _ref26$maxTokens = _ref26.maxTokens, maxTokens = _ref26$maxTokens === void 0 ? 1500 : _ref26$maxTokens, _ref26$retries = _ref26.retries, retries = _ref26$retries === void 0 ? 4 : _ref26$retries;
            body = {
              model: "claude-sonnet-4-20250514",
              max_tokens: maxTokens,
              temperature: 0,
              messages: msgs
            };
            if (systemPrompt) body.system = systemPrompt;
            if (webSearch) body.tools = [{
              type: "web_search_20250305",
              name: "web_search"
            }];
            _loop2 = /*#__PURE__*/_regenerator().m(function _loop2() {
              var controller, timeout, _d$content, r, waitSec, errBody, d, raw, _e$message, _e$message2, _e$message3, _e$message4, _waitSec, _t8;
              return _regenerator().w(function (_context8) {
                while (1) switch (_context8.p = _context8.n) {
                  case 0:
                    controller = new AbortController();
                    timeout = setTimeout(function () {
                      return controller.abort();
                    }, 120000);
                    _context8.p = 1;
                    _context8.n = 2;
                    return fetch("/api/chat", {
                      method: "POST",
                      headers: {
                        "Content-Type": "application/json"
                      },
                      body: JSON.stringify(body),
                      signal: controller.signal
                    });
                  case 2:
                    r = _context8.v;
                    clearTimeout(timeout);
                    if (!((r.status === 529 || r.status === 429) && attempt < retries)) {
                      _context8.n = 4;
                      break;
                    }
                    waitSec = r.status === 429 ? 30 + attempt * 15 : 8 + attempt * 4;
                    console.warn("[API] ".concat(r.status, " rate limited, retry ").concat(attempt + 1, "/").concat(retries, " in ").concat(waitSec, "s..."));
                    setLoadMsg === null || setLoadMsg === void 0 || setLoadMsg("\u23F3 Limite API atteinte \u2014 pause ".concat(waitSec, "s avant de reprendre (").concat(attempt + 1, "/").concat(retries, ")..."));
                    _context8.n = 3;
                    return new Promise(function (ok) {
                      return setTimeout(ok, waitSec * 1000);
                    });
                  case 3:
                    return _context8.a(2, 0);
                  case 4:
                    if (r.ok) {
                      _context8.n = 6;
                      break;
                    }
                    _context8.n = 5;
                    return r.text()["catch"](function () {
                      return "";
                    });
                  case 5:
                    errBody = _context8.v;
                    throw new Error("API ".concat(r.status, ": ").concat(errBody.slice(0, 300)));
                  case 6:
                    _context8.n = 7;
                    return r.json();
                  case 7:
                    d = _context8.v;
                    raw = ((_d$content = d.content) === null || _d$content === void 0 ? void 0 : _d$content.filter(function (b) {
                      return b.type === "text";
                    }).map(function (b) {
                      return b.text;
                    }).join("\n")) || "";
                    return _context8.a(2, {
                      v: stripCiteTags(raw)
                    });
                  case 8:
                    _context8.p = 8;
                    _t8 = _context8.v;
                    clearTimeout(timeout);
                    if (!(_t8.name === "AbortError")) {
                      _context8.n = 9;
                      break;
                    }
                    throw new Error("Timeout — la requête a pris trop de temps (>120s). Réessaie.");
                  case 9:
                    if (!(attempt < retries && ((_e$message = _t8.message) !== null && _e$message !== void 0 && _e$message.includes("529") || (_e$message2 = _t8.message) !== null && _e$message2 !== void 0 && _e$message2.includes("429") || (_e$message3 = _t8.message) !== null && _e$message3 !== void 0 && _e$message3.includes("Overloaded") || (_e$message4 = _t8.message) !== null && _e$message4 !== void 0 && _e$message4.includes("rate_limit")))) {
                      _context8.n = 11;
                      break;
                    }
                    _waitSec = 30 + attempt * 15;
                    console.warn("[API] Retry ".concat(attempt + 1, "/").concat(retries, " after error:"), _t8.message);
                    _context8.n = 10;
                    return new Promise(function (ok) {
                      return setTimeout(ok, _waitSec * 1000);
                    });
                  case 10:
                    return _context8.a(2, 0);
                  case 11:
                    throw _t8;
                  case 12:
                    return _context8.a(2);
                }
              }, _loop2, null, [[1, 8]]);
            });
            attempt = 0;
          case 1:
            if (!(attempt <= retries)) {
              _context9.n = 5;
              break;
            }
            return _context9.d(_regeneratorValues(_loop2()), 2);
          case 2:
            _ret = _context9.v;
            if (!(_ret === 0)) {
              _context9.n = 3;
              break;
            }
            return _context9.a(3, 4);
          case 3:
            if (!_ret) {
              _context9.n = 4;
              break;
            }
            return _context9.a(2, _ret.v);
          case 4:
            attempt++;
            _context9.n = 1;
            break;
          case 5:
            return _context9.a(2);
        }
      }, _callee7);
    }));
    return _callAI.apply(this, arguments);
  }
  function parseJ(t) {
    var c = t.replace(/```json|```/g, '').trim();
    // Strip any remaining XML/HTML tags (cite, source, etc.)
    c = c.replace(/<\/?[a-z][^>]*>/gi, '');
    // Try direct parse first
    try {
      var m = c.match(/[\[{][\s\S]*[\]}]/);
      if (m) return JSON.parse(m[0]);
    } catch (_unused9) {}
    // Fix trailing commas before } or ]
    try {
      var fixed = c.replace(/,\s*([}\]])/g, '$1');
      var _m = fixed.match(/[\[{][\s\S]*[\]}]/);
      if (_m) return JSON.parse(_m[0]);
    } catch (_unused0) {}
    // Fix unquoted keys
    try {
      var _fixed = c.replace(/(\{|,)\s*([a-zA-Z_]\w*)\s*:/g, '$1"$2":');
      var _m2 = _fixed.match(/[\[{][\s\S]*[\]}]/);
      if (_m2) return JSON.parse(_m2[0]);
    } catch (_unused1) {}
    // Fix single quotes to double quotes
    try {
      var _fixed2 = c.replace(/'/g, '"');
      var _m3 = _fixed2.match(/[\[{][\s\S]*[\]}]/);
      if (_m3) return JSON.parse(_m3[0]);
    } catch (_unused10) {}
    // Last resort — try the whole thing
    try {
      return JSON.parse(c);
    } catch (_unused11) {}
    throw new Error("Impossible de parser la réponse. Réessaie.");
  }

  // ============================================================
  // SEARCH: manual racket lookup
  // ============================================================
  var searchRackets = (0,external_React_.useCallback)(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2() {
    var txt, res, _t2;
    return _regenerator().w(function (_context2) {
      while (1) switch (_context2.p = _context2.n) {
        case 0:
          if (searchQ.trim()) {
            _context2.n = 1;
            break;
          }
          return _context2.a(2);
        case 1:
          setLoading(true);
          setError("");
          setSuggestions(null);
          setLoadMsg("🔍 Recherche en cours...");
          _context2.p = 2;
          _context2.n = 3;
          return callAI([{
            role: "user",
            content: "Search the web for padel rackets matching: \"".concat(searchQ, "\". The user may have typed an approximate name, brand, player name, or partial model. Find the 2-4 most likely padel racket matches currently on sale (2024-2026 models).\nFor EACH match: exact full official name, brand, shape (Diamant/Goutte d'eau/Ronde/Hybride), approximate weight, one-line description in French.\nReturn ONLY a JSON array: [{\"name\":\"...\",\"brand\":\"...\",\"shape\":\"...\",\"weight\":\"...\",\"description\":\"...\"}]. No markdown.")
          }], {
            webSearch: true
          });
        case 3:
          txt = _context2.v;
          res = parseJ(txt);
          if (!(!Array.isArray(res) || !res.length)) {
            _context2.n = 4;
            break;
          }
          throw new Error("Aucun résultat");
        case 4:
          setSuggestions(res);
          _context2.n = 6;
          break;
        case 5:
          _context2.p = 5;
          _t2 = _context2.v;
          setError("❌ " + _t2.message + ". Essaie d'autres mots-clés.");
        case 6:
          _context2.p = 6;
          setLoading(false);
          setLoadMsg("");
          return _context2.f(6);
        case 7:
          return _context2.a(2);
      }
    }, _callee2, null, [[2, 5, 6, 7]]);
  })), [searchQ]);

  // Select from manual search → get specs + score
  var selectSuggestion = (0,external_React_.useCallback)(/*#__PURE__*/function () {
    var _ref8 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3(idx) {
      var sug, newR, _t3;
      return _regenerator().w(function (_context3) {
        while (1) switch (_context3.p = _context3.n) {
          case 0:
            sug = suggestions[idx];
            if (sug) {
              _context3.n = 1;
              break;
            }
            return _context3.a(2);
          case 1:
            setSuggestions(function (s) {
              return s.map(function (x, i) {
                return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, x), {}, {
                  _disabled: true,
                  _selected: i === idx
                });
              });
            });
            setLoading(true);
            setError("");
            setLoadMsg("🔍 Récupération des specs...");
            _context3.p = 2;
            _context3.n = 3;
            return fetchAndScoreRacket(sug.name, sug.brand, getNextColor(rackets));
          case 3:
            newR = _context3.v;
            setRackets(function (p) {
              return [].concat(_toConsumableArray(p), [newR]);
            });
            setSelected(function (p) {
              return p.length < 4 ? [].concat(_toConsumableArray(p), [newR.id]) : p;
            });
            setSearchQ("");
            setSuggestions(null);
            setLoadMsg("✅ " + newR.name + " ajoutée !");
            setTimeout(function () {
              return setLoadMsg("");
            }, 2500);
            _context3.n = 5;
            break;
          case 4:
            _context3.p = 4;
            _t3 = _context3.v;
            setError("❌ " + _t3.message);
            setSuggestions(function (s) {
              return s === null || s === void 0 ? void 0 : s.map(function (x) {
                return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, x), {}, {
                  _disabled: false,
                  _selected: false
                });
              });
            });
          case 5:
            _context3.p = 5;
            setLoading(false);
            return _context3.f(5);
          case 6:
            return _context3.a(2);
        }
      }, _callee3, null, [[2, 4, 5, 6]]);
    }));
    return function (_x3) {
      return _ref8.apply(this, arguments);
    };
  }(), [suggestions, rackets, profile, profileText]);

  // ============================================================
  // SHARED: fetch specs + score a single racket
  // ============================================================
  // Score cache — save scored rackets to avoid re-scoring
  function normalizeName(name) {
    return (name || '').toLowerCase().replace(/\b(head|bullpadel|adidas|wilson|babolat|nox|siux|starvie|varlion|drop shot|dunlop|star vie)\b/gi, '') // strip brand from name
    .replace(/\b(20\d{2})\b/g, '') // strip year
    .replace(/\b(v\d+)\b/gi, '') // strip version numbers like V2
    .replace(/[^a-z0-9]/g, '').replace(/\s+/g, '');
  }
  function getCacheKey(name, brand) {
    var n = normalizeName(name);
    var b = (brand || '').toLowerCase().replace(/[^a-z0-9]/g, '');
    return 'padel_score_' + b + '_' + n;
  }
  function getCachedScore(name, brand) {
    try {
      var raw = localStorage.getItem(getCacheKey(name, brand));
      if (!raw) return null;
      var cached = JSON.parse(raw);
      // Cache expires after 7 days
      if (Date.now() - cached.ts > 7 * 24 * 60 * 60 * 1000) {
        localStorage.removeItem(getCacheKey(name, brand));
        return null;
      }
      return cached.data;
    } catch (_unused12) {
      return null;
    }
  }
  function setCachedScore(name, brand, data) {
    try {
      localStorage.setItem(getCacheKey(name, brand), JSON.stringify({
        ts: Date.now(),
        data: data
      }));
    } catch (_unused13) {}
  }
  // Save web-found rackets to local DB supplement for future DB matching
  function saveToLocalDB(racket) {
    try {
      var extra = JSON.parse(localStorage.getItem('padel_db_extra') || '[]');
      // Check if already exists
      var nameLower = racket.name.toLowerCase();
      if (extra.some(function (r) {
        return r.name.toLowerCase() === nameLower;
      })) return;
      // Determine category from scores
      var avgScore = Object.values(racket.scores || {}).reduce(function (a, b) {
        return a + b;
      }, 0) / 6;
      var category = 'intermediaire';
      if (racket.name.toLowerCase().includes('junior') || racket.name.toLowerCase().includes('kids')) category = 'junior';else if (avgScore >= 7.5) category = 'expert';else if (avgScore >= 7) category = 'avance';else if (avgScore <= 5.5) category = 'debutant';
      extra.push({
        id: racket.id,
        name: racket.name,
        shortName: racket.shortName,
        brand: racket.brand,
        shape: racket.shape,
        weight: racket.weight,
        balance: racket.balance || "—",
        surface: racket.surface || "—",
        core: racket.core || "—",
        price: racket.price || "—",
        player: racket.player || "—",
        imageUrl: racket.imageUrl || null,
        year: new Date().getFullYear(),
        category: category,
        scores: racket.scores,
        verdict: racket.verdict || "—"
      });
      localStorage.setItem('padel_db_extra', JSON.stringify(extra));
      setLocalDBCount(extra.length);
      console.log("[DB+] Saved ".concat(racket.name, " to local DB supplement (total: ").concat(extra.length, ")"));
    } catch (e) {
      console.warn('[DB+] Save failed:', e.message);
    }
  }
  function exportLocalDB() {
    try {
      var extra = JSON.parse(localStorage.getItem('padel_db_extra') || '[]');
      if (!extra.length) {
        alert('Aucune raquette apprise localement.');
        return;
      }
      var blob = new Blob([JSON.stringify(extra, null, 2)], {
        type: 'application/json'
      });
      var url = URL.createObjectURL(blob);
      var a = document.createElement('a');
      a.href = url;
      a.download = "padel-local-db-".concat(new Date().toISOString().slice(0, 10), ".json");
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    } catch (e) {
      alert('Erreur export: ' + e.message);
    }
  }
  function clearLocalDB() {
    if (!localDBCount) return;
    setConfirmModal({
      message: "Supprimer les ".concat(localDBCount, " raquette(s) apprise(s) localement ?"),
      onConfirm: function onConfirm() {
        try {
          localStorage.removeItem('padel_db_extra');
          setLocalDBCount(0);
        } catch (_unused14) {}
        setConfirmModal(null);
      },
      onCancel: function onCancel() {
        return setConfirmModal(null);
      }
    });
  }

  // Load a racket directly from DB (no API needed)
  function loadRacketFromDB(name, brand, color) {
    var allDB = _toConsumableArray(rackets_db_namespaceObject);
    try {
      var extra = JSON.parse(localStorage.getItem('padel_db_extra') || '[]');
      if (Array.isArray(extra)) allDB = [].concat(_toConsumableArray(allDB), _toConsumableArray(extra));
    } catch (_unused15) {}
    var nameLower = name.toLowerCase();
    var entry = allDB.find(function (r) {
      return r.name.toLowerCase() === nameLower;
    }) || allDB.find(function (r) {
      return nameLower.includes(r.name.toLowerCase().slice(0, 15)) || r.name.toLowerCase().includes(nameLower.slice(0, 15));
    });
    if (!entry) return null;
    return {
      id: entry.id + '-' + Date.now(),
      name: entry.name,
      shortName: entry.shortName || entry.name.slice(0, 28),
      brand: entry.brand,
      shape: entry.shape,
      weight: entry.weight,
      balance: entry.balance || "—",
      surface: entry.surface || "—",
      core: entry.core || "—",
      price: entry.price || "—",
      player: entry.player || "—",
      color: color,
      imageUrl: entry.imageUrl || null,
      scores: entry.scores,
      verdict: entry.verdict || "Analyse non disponible",
      forYou: "partial",
      // Computed dynamically by computeForYou()
      refSource: "Base Padel Analyzer",
      _fromDB: true,
      _incomplete: false
    };
  }
  function fetchAndScoreRacket(_x4, _x5, _x6) {
    return _fetchAndScoreRacket.apply(this, arguments);
  } // ============================================================
  // DATABASE: match rackets from local DB
  // ============================================================
  function _fetchAndScoreRacket() {
    _fetchAndScoreRacket = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee8(name, brand, assignedColor) {
      var _specs$refScores$puis, _specs$refScores$cont, _specs$refScores$conf, _specs$refScores$spin, _specs$refScores$mani, _specs$refScores$tole, _specs$refScores, _specs$refScores2;
      var cached, dbResult, specsTxt, specs, hasRef, refBlock, scoreTxt, analysis, retry, newId, clean, result, _t9, _t0;
      return _regenerator().w(function (_context0) {
        while (1) switch (_context0.p = _context0.n) {
          case 0:
            // Check cache first
            cached = getCachedScore(name, brand);
            if (!cached) {
              _context0.n = 1;
              break;
            }
            console.log("[Cache] Hit for ".concat(name, " \u2014 using cached scores"));
            return _context0.a(2, PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, cached), {}, {
              color: assignedColor,
              id: cached.id + '-' + Date.now()
            }));
          case 1:
            // Check DB second (embedded + local supplement)
            dbResult = loadRacketFromDB(name, brand, assignedColor);
            if (!dbResult) {
              _context0.n = 2;
              break;
            }
            console.log("[DB] Found ".concat(name, " in database \u2014 no API call needed"));
            setCachedScore(name, brand, dbResult);
            return _context0.a(2, dbResult);
          case 2:
            _context0.n = 3;
            return callAI([{
              role: "user",
              content: "Search the web for complete technical specs AND review scores of this padel racket: \"".concat(name, "\" by ").concat(brand, ".\n\nPART A \u2014 TECHNICAL SPECS (search manufacturer site, retailers):\nbrand, full name, short display name (max 18 chars), shape (Diamant/Goutte d'eau/Ronde/Hybride), weight range in grams, balance (mm + Haut/Mi-haut/Moyen/Bas), hitting surface material (carbon, fiberglass, hybrid, 12K, 18K etc.), core/foam material (density: soft, medium, hard, reactive), anti-vibration tech if any, approximate price in \u20AC, pro player endorsement, product image URL.\n\nIMAGE URL RULES: Find a DIRECT URL to a product photo of the racket (jpg/png/webp). Search retailers like PadelNuestro, Padel Reference, PadelZone, Amazon, Decathlon, or the manufacturer's site. The URL must point to an actual photo of THIS specific racket model \u2014 not a logo, banner, category page, or generic image. Look for URLs containing the racket model name. If you cannot find a reliable product photo URL, set imageUrl to null.\n\nPART B \u2014 REFERENCE SCORES (search Esprit Padel Shop, PadelMania, Padel Reference, PadelZone, or any review site that gives scores/ratings):\nFind existing ratings/scores for this racket. Look for: power/puissance, control/contr\xF4le, comfort/confort, spin/effet, maneuverability/maniabilit\xE9/manoeuvrabilit\xE9, tolerance/tol\xE9rance/sweet spot. These may be on /100, /10, /5, or star ratings.\nIf you find scores, convert ALL to /100 scale. If a site uses /10 multiply by 10, if /5 multiply by 20.\nIf NO review scores found, set refScores to null.\n\nReturn ONLY JSON:\n{\n  \"brand\":\"...\",\"name\":\"...\",\"shortName\":\"...\",\"shape\":\"...\",\"weight\":\"...\",\"balance\":\"...\",\"surface\":\"...\",\"core\":\"...\",\"antivib\":\"...\",\"price\":\"...\",\"player\":\"...\",\"imageUrl\":\"https://...\",\n  \"refScores\": {\"puissance\":85,\"controle\":70,\"confort\":65,\"spin\":75,\"maniabilite\":60,\"tolerance\":70,\"source\":\"Esprit Padel Shop\"} or null\n}\nNo markdown.")
            }], {
              webSearch: true
            });
          case 3:
            specsTxt = _context0.v;
            specs = parseJ(specsTxt); // Step 2: Score using reference scores + mechanical rules as guard-rails
            hasRef = specs.refScores && _typeof(specs.refScores) === 'object';
            refBlock = hasRef ? "\nREFERENCE SCORES (from ".concat(specs.refScores.source || 'review site', ", scale /100):\nPuissance: ").concat((_specs$refScores$puis = specs.refScores.puissance) !== null && _specs$refScores$puis !== void 0 ? _specs$refScores$puis : 'N/A', ", Contr\xF4le: ").concat((_specs$refScores$cont = specs.refScores.controle) !== null && _specs$refScores$cont !== void 0 ? _specs$refScores$cont : 'N/A', ", Confort: ").concat((_specs$refScores$conf = specs.refScores.confort) !== null && _specs$refScores$conf !== void 0 ? _specs$refScores$conf : 'N/A', ", Spin: ").concat((_specs$refScores$spin = specs.refScores.spin) !== null && _specs$refScores$spin !== void 0 ? _specs$refScores$spin : 'N/A', ", Maniabilit\xE9: ").concat((_specs$refScores$mani = specs.refScores.maniabilite) !== null && _specs$refScores$mani !== void 0 ? _specs$refScores$mani : 'N/A', ", Tol\xE9rance: ").concat((_specs$refScores$tole = specs.refScores.tolerance) !== null && _specs$refScores$tole !== void 0 ? _specs$refScores$tole : 'N/A', "\n\nCALIBRATION METHOD:\n1. Start from reference scores. Convert /100 to /10: score_base = (ref_score / 10) - 0.5. This applies a mild deflation since review sites tend to inflate (they rarely go below 60/100). Examples: 90\u21928.5, 80\u21927.5, 73\u21926.8, 65\u21926.0.\n2. Apply GUARD-RAIL CAPS from mechanical rules (see system prompt). If a cap applies, use the LOWER of calibrated vs cap. For example: Diamant shape \u2192 Contr\xF4le capped at 7.5, Tol\xE9rance capped at 6.5. Stiff carbon + hard EVA \u2192 Confort max 6.0.\n3. Apply GUARD-RAIL FLOORS from mechanical rules. For example: Fiberglass + soft foam + anti-vib \u2192 Confort minimum 7.0.\n4. Round to nearest 0.5.\n5. Final scores must respect both the relative ordering from reviews AND the absolute caps/floors from mechanical rules.") : "\nNO REFERENCE SCORES FOUND. Use PURE MECHANICAL RULES from system prompt to calculate scores.";
            _context0.n = 4;
            return callAI([{
              role: "user",
              content: "Score this padel racket's INTRINSIC properties. Do NOT consider any player profile \u2014 score the racket itself.\n\nRACKET SPECS:\n- Name: ".concat(specs.name, "\n- Shape: ").concat(specs.shape, "\n- Weight: ").concat(specs.weight, "\n- Balance: ").concat(specs.balance, "\n- Surface: ").concat(specs.surface, "\n- Core/Foam: ").concat(specs.core, "\n- Anti-vibration tech: ").concat(specs.antivib || "none specified", "\n").concat(refBlock, "\n\nIMPORTANT: Scores must reflect the racket's own characteristics, NOT how well it fits any player. A heavy diamond racket scores high on Puissance and low on Maniabilit\xE9 regardless of who uses it.\n\nCalculate each score step by step: state the reference score (if available), the calibrated score, any guard-rail cap, then the final score.\n\nReturn JSON: {\"scores\":{\"Puissance\":X,\"Contr\xF4le\":X,\"Confort\":X,\"Spin\":X,\"Maniabilit\xE9\":X,\"Tol\xE9rance\":X},\"verdict\":\"French text describing the racket's character and best use case (2-3 sentences)\",\"reasoning\":\"brief calculation notes\",\"refSource\":\"").concat(((_specs$refScores = specs.refScores) === null || _specs$refScores === void 0 ? void 0 : _specs$refScores.source) || 'none', "\"}\nNo markdown, no backticks.")
            }], {
              systemPrompt: SCORING_SYSTEM_PROMPT
            });
          case 4:
            scoreTxt = _context0.v;
            _context0.p = 5;
            analysis = parseJ(scoreTxt);
            _context0.n = 10;
            break;
          case 6:
            _context0.p = 6;
            _t9 = _context0.v;
            console.warn("[Score] Parse failed for ".concat(specs.name, ", retrying..."), _t9.message);
            _context0.p = 7;
            _context0.n = 8;
            return callAI([{
              role: "user",
              content: "Score this padel racket's INTRINSIC properties. Specs: ".concat(specs.name, ", ").concat(specs.shape, ", ").concat(specs.weight, ", ").concat(specs.balance, ", ").concat(specs.surface, ", ").concat(specs.core, ".\nReturn ONLY valid JSON: {\"scores\":{\"Puissance\":7,\"Contr\xF4le\":7,\"Confort\":7,\"Spin\":7,\"Maniabilit\xE9\":7,\"Tol\xE9rance\":7},\"verdict\":\"French text describing the racket\"}\nNo markdown, no backticks, no explanation.")
            }], {
              systemPrompt: SCORING_SYSTEM_PROMPT,
              maxTokens: 600
            });
          case 8:
            retry = _context0.v;
            analysis = parseJ(retry);
            console.log("[Score] Retry succeeded for ".concat(specs.name));
            _context0.n = 10;
            break;
          case 9:
            _context0.p = 9;
            _t0 = _context0.v;
            console.warn("[Score] Retry also failed for ".concat(specs.name, ":"), _t0.message);
            analysis = {
              scores: {},
              verdict: "Scoring automatique indisponible — clique 🔄 pour réessayer.",
              forYou: "partial",
              _incomplete: true
            };
          case 10:
            newId = (specs.name || name).toLowerCase().replace(/[^a-z0-9]/g, '-').slice(0, 35) + '-' + Date.now(); // Sanitize specs — replace any null/undefined/"Not Found"/empty with "—"
            clean = function clean(v) {
              return !v || v === 'null' || v === 'undefined' || /not found|introuvable|n\/a|unknown/i.test(v) ? "—" : String(v).trim();
            };
            result = {
              id: newId,
              name: specs.name || name,
              shortName: (specs.shortName || specs.name || name).slice(0, 28),
              brand: clean(specs.brand) || brand || "—",
              shape: clean(specs.shape),
              weight: clean(specs.weight),
              balance: clean(specs.balance),
              surface: clean(specs.surface),
              core: clean(specs.core),
              price: clean(specs.price),
              player: clean(specs.player),
              color: assignedColor,
              imageUrl: specs.imageUrl || null,
              scores: Object.fromEntries(ATTRS.map(function (a) {
                var _analysis$scores;
                return [a, Math.round((Number((_analysis$scores = analysis.scores) === null || _analysis$scores === void 0 ? void 0 : _analysis$scores[a]) || 5) * 10) / 10];
              })),
              verdict: analysis.verdict || "Analyse non disponible",
              forYou: "partial",
              // Will be computed dynamically by computeForYou() based on current profile
              refSource: analysis.refSource || ((_specs$refScores2 = specs.refScores) === null || _specs$refScores2 === void 0 ? void 0 : _specs$refScores2.source) || null,
              _incomplete: analysis._incomplete || Object.keys(analysis.scores || {}).length < 6
            }; // Save to cache
            setCachedScore(specs.name || name, specs.brand || brand, result);
            // Also save to local DB supplement for future DB matching
            saveToLocalDB(result);
            return _context0.a(2, result);
        }
      }, _callee8, null, [[7, 9], [5, 6]]);
    }));
    return _fetchAndScoreRacket.apply(this, arguments);
  }
  function matchFromDB(profile, existingNames) {
    var age = Number(profile.age) || 0;
    var ht = Number(profile.height) || 0;
    var isJunior = age > 0 && age < 15 || ht > 0 && ht < 150;
    var brandPref = profile.brandTags.map(function (id) {
      var _BRAND_TAGS$find2;
      return (_BRAND_TAGS$find2 = BRAND_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _BRAND_TAGS$find2 === void 0 ? void 0 : _BRAND_TAGS$find2.label;
    }).filter(Boolean);

    // Merge embedded DB + any localStorage supplements
    var allDB = _toConsumableArray(rackets_db_namespaceObject);
    try {
      var extra = JSON.parse(localStorage.getItem('padel_db_extra') || '[]');
      if (Array.isArray(extra)) allDB = [].concat(_toConsumableArray(allDB), _toConsumableArray(extra));
    } catch (_unused16) {}

    // Filter by category
    var pool;
    if (isJunior) {
      pool = allDB.filter(function (r) {
        return r.category === 'junior';
      });
    } else {
      var lvl = profile.level || 'Débutant';
      var catMap = {
        'Débutant': ['debutant', 'intermediaire'],
        'Intermédiaire': ['intermediaire', 'debutant', 'avance'],
        'Avancé': ['avance', 'intermediaire', 'expert'],
        'Expert': ['expert', 'avance']
      };
      var cats = catMap[lvl] || ['debutant', 'intermediaire'];
      pool = allDB.filter(function (r) {
        return cats.includes(r.category);
      });
    }

    // Exclude already owned
    var exLower = existingNames.map(function (n) {
      return n.toLowerCase();
    });
    pool = pool.filter(function (r) {
      return !exLower.some(function (n) {
        return r.name.toLowerCase().includes(n.slice(0, 12)) || n.includes(r.name.toLowerCase().slice(0, 12));
      });
    });

    // Filter by brand preferences (if any)
    var brandPool = pool;
    if (brandPref.length) {
      var prefLower = brandPref.map(function (b) {
        return b.toLowerCase();
      });
      brandPool = pool.filter(function (r) {
        return prefLower.includes(r.brand.toLowerCase());
      });
      // Always include a few from other brands
      var otherPool = pool.filter(function (r) {
        return !prefLower.includes(r.brand.toLowerCase());
      });
      var otherTop = otherPool.sort(function (a, b) {
        return computeGlobalScore(a.scores, profile) - computeGlobalScore(b.scores, profile);
      }).reverse().slice(0, 2);
      brandPool = [].concat(_toConsumableArray(brandPool), _toConsumableArray(otherTop));
    }

    // Score all and sort
    var scored = brandPool.map(function (r) {
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _globalScore: computeGlobalScore(r.scores, profile)
      });
    });
    scored.sort(function (a, b) {
      return b._globalScore - a._globalScore;
    });

    // Split into heart (top matches) and priority (match priority tags)
    var prioTags = profile.priorityTags || [];
    var prioAttrs = [];
    if (prioTags.includes('puissance')) prioAttrs.push('Puissance');
    if (prioTags.includes('controle')) prioAttrs.push('Contrôle');
    if (prioTags.includes('confort') || prioTags.includes('protection') || prioTags.includes('reprise')) prioAttrs.push('Confort');
    if (prioTags.includes('spin')) prioAttrs.push('Spin');
    if (prioTags.includes('legerete')) prioAttrs.push('Maniabilité');
    var heart = scored.slice(0, 5);
    var heartIds = new Set(heart.map(function (r) {
      return r.id;
    }));

    // Priority: sort remaining by average of priority attributes
    var prioPool = scored.filter(function (r) {
      return !heartIds.has(r.id);
    });
    if (prioAttrs.length) {
      prioPool.sort(function (a, b) {
        var avgA = prioAttrs.reduce(function (s, k) {
          return s + (a.scores[k] || 0);
        }, 0) / prioAttrs.length;
        var avgB = prioAttrs.reduce(function (s, k) {
          return s + (b.scores[k] || 0);
        }, 0) / prioAttrs.length;
        return avgB - avgA;
      });
    }
    var priority = prioPool.slice(0, 3);
    return {
      heart: heart,
      priority: priority,
      totalPool: pool.length
    };
  }

  // ============================================================
  // SUGGEST: DB first, web search fallback
  // ============================================================
  var suggestRackets = (0,external_React_.useCallback)(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4() {
    var existingNames, brandPref, prioLabels, dbMatch, results, startTime, timer, txt, res, filtered, _t4, _t5;
    return _regenerator().w(function (_context4) {
      while (1) switch (_context4.p = _context4.n) {
        case 0:
          setLoading(true);
          setError("");
          setSuggestResults(null);
          setSuggestChecked(new Set());
          setLoadMsg("🎯 Recherche dans la base de données...");
          existingNames = rackets.map(function (r) {
            return r.name;
          });
          brandPref = profile.brandTags.map(function (id) {
            var _BRAND_TAGS$find3;
            return (_BRAND_TAGS$find3 = BRAND_TAGS.find(function (t) {
              return t.id === id;
            })) === null || _BRAND_TAGS$find3 === void 0 ? void 0 : _BRAND_TAGS$find3.label;
          }).filter(Boolean);
          prioLabels = profile.priorityTags.map(function (id) {
            var _PRIORITY_TAGS$find2;
            return (_PRIORITY_TAGS$find2 = PRIORITY_TAGS.find(function (t) {
              return t.id === id;
            })) === null || _PRIORITY_TAGS$find2 === void 0 ? void 0 : _PRIORITY_TAGS$find2.label;
          }).filter(Boolean);
          console.log("[Suggest] Starting. Existing:", existingNames, "Brands:", brandPref, "Priorities:", prioLabels);
          _context4.p = 1;
          // Phase 1: Try DB first
          dbMatch = matchFromDB(profile, existingNames);
          console.log("[DB] Pool:", dbMatch.totalPool, "Heart:", dbMatch.heart.length, "Priority:", dbMatch.priority.length);
          if (!(dbMatch.heart.length + dbMatch.priority.length >= 6)) {
            _context4.n = 2;
            break;
          }
          // Enough results from DB — format as suggestions
          results = [].concat(_toConsumableArray(dbMatch.heart.map(function (r) {
            return {
              name: r.name,
              brand: r.brand,
              shape: r.shape,
              weight: r.weight,
              price: r.price,
              category: "heart",
              description: r.verdict,
              _fromDB: true
            };
          })), _toConsumableArray(dbMatch.priority.map(function (r) {
            return {
              name: r.name,
              brand: r.brand,
              shape: r.shape,
              weight: r.weight,
              price: r.price,
              category: "priority",
              description: r.verdict,
              _fromDB: true
            };
          })));
          console.log("[DB] Sufficient results:", results.length, "— skipping web search");
          setSuggestResults(results);
          setLoadMsg("✅ " + results.length + " raquettes trouvées instantanément !");
          setTimeout(function () {
            return setLoadMsg("");
          }, 2500);
          setLoading(false);
          return _context4.a(2);
        case 2:
          // Phase 2: Not enough from DB — use web search
          console.log("[DB] Only", dbMatch.heart.length + dbMatch.priority.length, "results — falling back to web search");
          setLoadMsg("🌐 Recherche web complémentaire...");
          startTime = Date.now();
          timer = setInterval(function () {
            var elapsed = Math.floor((Date.now() - startTime) / 1000);
            setLoadMsg("\uD83C\uDF10 Recherche web compl\xE9mentaire... (".concat(elapsed, "s)"));
          }, 1000);
          _context4.p = 3;
          _context4.n = 4;
          return callAI([{
            role: "user",
            content: "Search the web for padel rackets suitable for this player. Find 8 different models (2024-2026).\n\nPLAYER: ".concat(profileText, "\n\nEXCLUDED (already owned, do NOT include): ").concat(existingNames.join('; '), "\n\n").concat(brandPref.length ? "PREFERRED BRANDS: ".concat(brandPref.join(', '), ". Include 5+ from these brands and 1-2 from other brands.") : '', "\n\nYou MUST return TWO categories:\n\nCATEGORY \"heart\" (4-5 rackets) \u2014 COUPS DE C\u0152UR: Best overall match for the player's FULL profile (style, level, side, priorities).").concat((profile.injuryTags || []).some(function (t) {
              return t !== "aucune";
            }) ? ' Player has injuries — prioritize comfort and safety. Avoid stiff carbon with hard EVA.' : ' No injuries — focus on best performance match for the player style and priorities.', "\n\nCATEGORY \"priority\" (3 rackets) \u2014 ALTERNATIVES PRIORIT\xC9: Rackets that specifically match the player's PRIORITY TAGS: ").concat(prioLabels.join(', '), ". These can sacrifice some comfort for performance in the priority areas. ").concat(prioLabels.includes('Puissance') ? 'Include powerful rackets (diamond/drop shapes, high balance) even if comfort is lower.' : '', " ").concat(prioLabels.includes('Spin') ? 'Include textured surface rackets for maximum spin.' : '', " Still exclude truly dangerous choices (no comfort below 4/10). Add a warning in description if comfort is limited.\n\nKey rules:\n").concat(function () {
              var a = Number(profile.age) || 0;
              var ht = Number(profile.height) || 0;
              var isJunior = a > 0 && a < 15 || ht > 0 && ht < 150;
              var isSenior = a >= 50;
              var lines = [];
              if (isJunior) {
                lines.push('- JUNIOR PLAYER: Search specifically for JUNIOR/KIDS padel rackets. Weight 300-340g max. Price range 30-120€. Smaller grip. Do NOT suggest adult rackets.');
              } else if (profile.level === "Avancé" || profile.level === "Expert") {
                lines.push('- Avancé/Expert level → price range 200-400€.');
              } else if (profile.level === "Intermédiaire") {
                lines.push('- Intermédiaire level → price range 100-250€.');
              } else {
                lines.push('- Débutant level → price range 50-150€. Prioritize round shapes, light weight, high tolerance.');
              }
              if (isSenior) lines.push('- Player is 50+ years old → prioritize lightweight rackets (340-360g), excellent comfort and vibration dampening.');
              lines.push('- Véloce/Endurant → light weight preferred.');
              lines.push('- Each racket: name (exact official name), brand, shape, weight, price (€), category ("heart" or "priority"), description (2 sentences French: WHY it fits + warning if comfort limited).');
              return lines.join('\n');
            }(), "\n\nReturn ONLY a JSON array, no markdown: [{\"name\":\"...\",\"brand\":\"...\",\"shape\":\"...\",\"weight\":\"...\",\"price\":\"...\",\"category\":\"heart\",\"description\":\"...\"}]")
          }], {
            webSearch: true,
            maxTokens: 2500
          });
        case 4:
          txt = _context4.v;
          res = parseJ(txt);
          if (!(!Array.isArray(res) || !res.length)) {
            _context4.n = 5;
            break;
          }
          throw new Error("Aucune suggestion trouvée");
        case 5:
          console.log("[WebSearch] Got results:", res.length, res.map(function (r) {
            return "".concat(r.category, ":").concat(r.name);
          }));
          filtered = res.filter(function (s) {
            return !existingNames.some(function (n) {
              var _s$name, _s$name2;
              return n.toLowerCase().includes((_s$name = s.name) === null || _s$name === void 0 || (_s$name = _s$name.toLowerCase()) === null || _s$name === void 0 ? void 0 : _s$name.slice(0, 15)) || ((_s$name2 = s.name) === null || _s$name2 === void 0 || (_s$name2 = _s$name2.toLowerCase()) === null || _s$name2 === void 0 ? void 0 : _s$name2.includes(n.toLowerCase().slice(0, 15)));
            });
          });
          setSuggestResults(filtered.length ? filtered : res);
          clearInterval(timer);
          setLoadMsg("");
          _context4.n = 7;
          break;
        case 6:
          _context4.p = 6;
          _t4 = _context4.v;
          clearInterval(timer);
          throw _t4;
        case 7:
          _context4.n = 9;
          break;
        case 8:
          _context4.p = 8;
          _t5 = _context4.v;
          console.error("[Suggest] Failed:", _t5.message, _t5);
          setError("❌ " + _t5.message);
        case 9:
          _context4.p = 9;
          setLoading(false);
          setLoadMsg("");
          return _context4.f(9);
        case 10:
          return _context4.a(2);
      }
    }, _callee4, null, [[3, 6], [1, 8, 9, 10]]);
  })), [profile, profileText, rackets]);

  // Toggle checkbox on a suggestion
  var toggleSuggestCheck = function toggleSuggestCheck(idx) {
    setSuggestChecked(function (prev) {
      var next = new Set(prev);
      if (next.has(idx)) next["delete"](idx);else next.add(idx);
      return next;
    });
  };

  // Load a racket directly from DB (no API needed)
  // Batch add all checked suggestions
  var addCheckedSuggestions = (0,external_React_.useCallback)(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee5() {
    var toAdd, sugsToScore, usedColors, availableColors, added, _loop, i;
    return _regenerator().w(function (_context6) {
      while (1) switch (_context6.n) {
        case 0:
          if (!(!suggestResults || suggestChecked.size === 0)) {
            _context6.n = 1;
            break;
          }
          return _context6.a(2);
        case 1:
          setAddingBatch(true);
          setError("");
          toAdd = _toConsumableArray(suggestChecked).sort(function (a, b) {
            return a - b;
          });
          sugsToScore = toAdd.map(function (idx) {
            return suggestResults[idx];
          }).filter(function (s) {
            return s && !s._added;
          });
          if (sugsToScore.length) {
            _context6.n = 2;
            break;
          }
          setAddingBatch(false);
          return _context6.a(2);
        case 2:
          usedColors = new Set(rackets.map(function (r) {
            return r.color;
          }));
          availableColors = COLORS_POOL.filter(function (c) {
            return !usedColors.has(c);
          });
          while (availableColors.length < sugsToScore.length) {
            availableColors.push("hsl(".concat(Math.floor(Math.random() * 360), ", 70%, 55%)"));
          }
          added = 0;
          _loop = /*#__PURE__*/_regenerator().m(function _loop() {
            var sug, color, newR, idx, _idx, _t6;
            return _regenerator().w(function (_context5) {
              while (1) switch (_context5.p = _context5.n) {
                case 0:
                  sug = sugsToScore[i];
                  color = availableColors[i] || "hsl(".concat(i * 60, ", 70%, 55%)");
                  setBatchProgress("\u23F3 ".concat(i + 1, "/").concat(sugsToScore.length, " \u2014 ").concat(sug.name, "..."));
                  _context5.p = 1;
                  if (!sug._fromDB) {
                    _context5.n = 3;
                    break;
                  }
                  // Direct from database — instant, no API call
                  newR = loadRacketFromDB(sug.name, sug.brand, color);
                  if (newR) {
                    _context5.n = 2;
                    break;
                  }
                  throw new Error("Raquette introuvable dans la base");
                case 2:
                  console.log("[DB] Loaded ".concat(sug.name, " directly \u2014 no API call"));
                  _context5.n = 5;
                  break;
                case 3:
                  _context5.n = 4;
                  return fetchAndScoreRacket(sug.name, sug.brand, color);
                case 4:
                  newR = _context5.v;
                  // Save to local DB supplement for future use
                  saveToLocalDB(newR);
                case 5:
                  setRackets(function (p) {
                    return [].concat(_toConsumableArray(p), [newR]);
                  });
                  setSelected(function (p) {
                    return p.length < 4 ? [].concat(_toConsumableArray(p), [newR.id]) : p;
                  });
                  idx = toAdd[i];
                  setSuggestResults(function (s) {
                    return s.map(function (x, j) {
                      return j === idx ? PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, x), {}, {
                        _added: true
                      }) : x;
                    });
                  });
                  added++;
                  if (!(!sug._fromDB && i < sugsToScore.length - 1)) {
                    _context5.n = 6;
                    break;
                  }
                  _context5.n = 6;
                  return new Promise(function (ok) {
                    return setTimeout(ok, 3000);
                  });
                case 6:
                  _context5.n = 8;
                  break;
                case 7:
                  _context5.p = 7;
                  _t6 = _context5.v;
                  _idx = toAdd[i];
                  setSuggestResults(function (s) {
                    return s.map(function (x, j) {
                      return j === _idx ? PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, x), {}, {
                        _error: _t6.message
                      }) : x;
                    });
                  });
                case 8:
                  return _context5.a(2);
              }
            }, _loop, null, [[1, 7]]);
          });
          i = 0;
        case 3:
          if (!(i < sugsToScore.length)) {
            _context6.n = 5;
            break;
          }
          return _context6.d(_regeneratorValues(_loop()), 4);
        case 4:
          i++;
          _context6.n = 3;
          break;
        case 5:
          setBatchProgress("\u2705 ".concat(added, " raquette").concat(added > 1 ? "s" : "", " ajout\xE9e").concat(added > 1 ? "s" : "", "!"));
          setSuggestChecked(new Set());
          setTimeout(function () {
            return setBatchProgress("");
          }, 3000);
          setAddingBatch(false);
        case 6:
          return _context6.a(2);
      }
    }, _callee5);
  })), [suggestResults, suggestChecked, rackets, profile, profileText]);

  // ============================================================
  // RE-ANALYZE all verdicts
  // ============================================================
  var reanalyzeAll = (0,external_React_.useCallback)(/*#__PURE__*/_asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee6() {
    var txt, res, _t7;
    return _regenerator().w(function (_context7) {
      while (1) switch (_context7.p = _context7.n) {
        case 0:
          setLoading(true);
          setError("");
          setLoadMsg("🔄 Ré-analyse en cours...");
          _context7.p = 1;
          _context7.n = 2;
          return callAI([{
            role: "user",
            content: "Re-evaluate ALL these rackets for this player profile. Apply STRICT MECHANICAL RULES.\n\nPLAYER: ".concat(profileText, "\n\nRACKETS:\n").concat(rackets.map(function (r) {
              return "- \"".concat(r.name, "\": shape=").concat(r.shape, ", weight=").concat(r.weight, ", balance=").concat(r.balance, ", surface=").concat(r.surface, ", core=").concat(r.core);
            }).join('\n'), "\n\nFor EACH racket provide ONLY forYou and verdict. Do NOT change the scores (those are mechanical).\nReturn JSON array: [{\"name\":\"exact name\",\"forYou\":\"recommended|partial|no\",\"verdict\":\"2 sentences French\"}]. No markdown.")
          }], {
            systemPrompt: SCORING_SYSTEM_PROMPT
          });
        case 2:
          txt = _context7.v;
          res = parseJ(txt);
          setRackets(function (p) {
            return p.map(function (r) {
              var m = res.find(function (x) {
                var _x$name, _x$name2;
                return r.name.toLowerCase().includes((_x$name = x.name) === null || _x$name === void 0 ? void 0 : _x$name.toLowerCase()) || ((_x$name2 = x.name) === null || _x$name2 === void 0 ? void 0 : _x$name2.toLowerCase().includes(r.name.toLowerCase()));
              });
              return m ? PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
                forYou: m.forYou,
                verdict: m.verdict
              }) : r;
            });
          });
          setLoadMsg("✅ Profil mis à jour !");
          setTimeout(function () {
            return setLoadMsg("");
          }, 2000);
          _context7.n = 4;
          break;
        case 3:
          _context7.p = 3;
          _t7 = _context7.v;
          setError("❌ " + _t7.message);
        case 4:
          _context7.p = 4;
          setLoading(false);
          return _context7.f(4);
        case 5:
          return _context7.a(2);
      }
    }, _callee6, null, [[1, 3, 4, 5]]);
  })), [profile, rackets, profileText]);

  // ============================================================
  // STYLES
  // ============================================================
  var S = {
    root: {
      fontFamily: "'Inter',sans-serif",
      background: "linear-gradient(160deg,#080c14,#0f1623,#0d1520,#0a0f1a)",
      color: "#e2e8f0",
      minHeight: "100vh",
      padding: "24px 16px",
      letterSpacing: "-0.01em"
    },
    card: {
      background: "rgba(255,255,255,0.025)",
      borderRadius: 16,
      padding: 18,
      border: "1px solid rgba(255,255,255,0.06)",
      marginBottom: 18,
      backdropFilter: "blur(12px)",
      WebkitBackdropFilter: "blur(12px)",
      animation: "fadeIn 0.3s ease"
    },
    title: {
      fontFamily: "'Outfit'",
      fontSize: 12,
      fontWeight: 700,
      color: "#f97316",
      marginBottom: 12,
      letterSpacing: "0.04em",
      textTransform: "uppercase"
    },
    btn: function btn(a) {
      return {
        padding: "8px 16px",
        background: a ? "rgba(249,115,22,0.15)" : "rgba(255,255,255,0.04)",
        border: "1px solid ".concat(a ? "#f97316" : "rgba(255,255,255,0.08)"),
        borderRadius: 10,
        color: a ? "#f97316" : "#94a3b8",
        fontSize: 12,
        fontWeight: 600,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s cubic-bezier(.4,0,.2,1)",
        letterSpacing: "-0.01em"
      };
    },
    btnGreen: {
      padding: "12px 16px",
      background: "linear-gradient(135deg,rgba(76,175,80,0.2),rgba(76,175,80,0.1))",
      border: "1px solid rgba(76,175,80,0.4)",
      borderRadius: 12,
      color: "#4CAF50",
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Inter',sans-serif",
      width: "100%",
      transition: "all 0.2s ease",
      letterSpacing: "-0.01em"
    },
    input: {
      width: "100%",
      padding: "9px 12px",
      background: "rgba(255,255,255,0.04)",
      border: "1px solid rgba(255,255,255,0.08)",
      borderRadius: 10,
      color: "#e2e8f0",
      fontSize: 12,
      outline: "none",
      boxSizing: "border-box",
      fontFamily: "'Inter',sans-serif",
      transition: "border-color 0.2s ease"
    },
    select: {
      width: "100%",
      padding: "9px 12px",
      background: "rgba(255,255,255,0.04)",
      border: "1px solid rgba(255,255,255,0.08)",
      borderRadius: 10,
      color: "#e2e8f0",
      fontSize: 12,
      outline: "none",
      boxSizing: "border-box",
      fontFamily: "'Inter',sans-serif",
      appearance: "auto",
      transition: "border-color 0.2s ease"
    },
    label: {
      fontSize: 10,
      color: "#64748b",
      fontWeight: 600,
      marginBottom: 4,
      display: "block",
      letterSpacing: "0.02em",
      textTransform: "uppercase"
    },
    sectionLabel: {
      fontSize: 12,
      color: "#e2e8f0",
      fontWeight: 700,
      marginTop: 14,
      marginBottom: 3
    }
  };
  return /*#__PURE__*/external_React_default().createElement("div", {
    style: S.root
  }, /*#__PURE__*/external_React_default().createElement("link", {
    href: "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@500;600;700;800&display=swap",
    rel: "stylesheet"
  }), /*#__PURE__*/external_React_default().createElement("style", null, "\n        @keyframes fadeIn { from { opacity:0; transform:translateY(6px); } to { opacity:1; transform:translateY(0); } }\n        @keyframes slideIn { from { opacity:0; transform:translateX(-8px); } to { opacity:1; transform:translateX(0); } }\n        @keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }\n        .pa-card { transition: all 0.25s cubic-bezier(.4,0,.2,1); }\n        .pa-card:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.3); }\n        .pa-carousel::-webkit-scrollbar { display: none; }\n        .pa-tag { transition: all 0.15s cubic-bezier(.4,0,.2,1); }\n        .pa-tag:active { transform: scale(0.94); }\n        .pa-tab { position: relative; transition: all 0.2s ease; }\n        .pa-tab::after { content: ''; position: absolute; bottom: -1px; left: 50%; width: 0; height: 2px; background: #f97316; border-radius: 1px; transition: all 0.25s cubic-bezier(.4,0,.2,1); transform: translateX(-50%); }\n        .pa-tab-active::after { width: 60%; }\n        .pa-badge { backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); }\n        .pa-score-cell { position: relative; }\n        .pa-score-cell::before { content: ''; position: absolute; left: 0; top: 50%; transform: translateY(-50%); height: 3px; border-radius: 2px; opacity: 0.15; }\n        .pa-section { border-left: 3px solid; padding-left: 10px; margin-top: 14px; margin-bottom: 4px; }\n        tr.pa-row { transition: background 0.15s ease; }\n        tr.pa-row:hover { background: rgba(255,255,255,0.04) !important; }\n      "), screen === "home" && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      minHeight: "80vh",
      animation: "fadeIn 0.5s ease",
      padding: "0 16px"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginBottom: 24,
      animation: "fadeIn 0.6s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("svg", {
    width: "80",
    height: "80",
    viewBox: "0 0 44 44",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    style: {
      filter: "drop-shadow(0 8px 24px rgba(249,115,22,0.35))"
    }
  }, /*#__PURE__*/external_React_default().createElement("defs", null, /*#__PURE__*/external_React_default().createElement("linearGradient", {
    id: "logoGradHome",
    x1: "0",
    y1: "0",
    x2: "44",
    y2: "44"
  }, /*#__PURE__*/external_React_default().createElement("stop", {
    offset: "0%",
    stopColor: "#f97316"
  }), /*#__PURE__*/external_React_default().createElement("stop", {
    offset: "100%",
    stopColor: "#ef4444"
  }))), /*#__PURE__*/external_React_default().createElement("rect", {
    width: "44",
    height: "44",
    rx: "10",
    fill: "url(#logoGradHome)"
  }), /*#__PURE__*/external_React_default().createElement("ellipse", {
    cx: "22",
    cy: "18",
    rx: "10",
    ry: "12",
    stroke: "#fff",
    strokeWidth: "2.2",
    fill: "none"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "10",
    x2: "22",
    y2: "26",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.4"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "14",
    y1: "18",
    x2: "30",
    y2: "18",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.4"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "30",
    x2: "22",
    y2: "38",
    stroke: "#fff",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }), /*#__PURE__*/external_React_default().createElement("circle", {
    cx: "33",
    cy: "32",
    r: "3.5",
    fill: "#fff",
    opacity: "0.85"
  }))), /*#__PURE__*/external_React_default().createElement("h1", {
    style: {
      fontFamily: "'Outfit'",
      fontSize: 32,
      fontWeight: 800,
      background: "linear-gradient(135deg,#f97316,#ef4444,#ec4899)",
      WebkitBackgroundClip: "text",
      WebkitTextFillColor: "transparent",
      margin: "0 0 6px",
      letterSpacing: "-0.03em",
      textAlign: "center"
    }
  }, "PADEL ANALYZER"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      color: "#64748b",
      fontSize: 13,
      margin: "0 0 8px",
      letterSpacing: "0.06em",
      textTransform: "uppercase",
      fontWeight: 500,
      textAlign: "center"
    }
  }, "Ton conseiller raquette intelligent"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      color: "#475569",
      fontSize: 11,
      margin: "0 0 36px",
      textAlign: "center",
      maxWidth: 340,
      lineHeight: 1.5
    }
  }, "Analyse ton profil, explore ", rackets_db_namespaceObject.length, "+ raquettes, trouve la pala parfaite pour ton jeu."), savedProfiles.length > 0 && function () {
    var profileSearch = profileSearchTerm;
    var filtered = savedProfiles.filter(function (sp) {
      return !profileSearch || sp.name.toLowerCase().includes(profileSearch.toLowerCase());
    });
    var CARD_W = 210;
    var GAP = 14;
    var scrollToIdx = function scrollToIdx(idx) {
      var el = carouselRef.current;
      if (!el) return;
      var target = idx * (CARD_W + GAP);
      el.scrollTo({
        left: target,
        behavior: "smooth"
      });
      setActiveProfileIdx(idx);
    };
    var handleScroll = function handleScroll() {
      var el = carouselRef.current;
      if (!el) return;
      var idx = Math.round(el.scrollLeft / (CARD_W + GAP));
      setActiveProfileIdx(Math.max(0, Math.min(idx, filtered.length - 1)));
    };
    var scrollDir = function scrollDir(dir) {
      var next = dir === "left" ? activeProfileIdx - 1 : activeProfileIdx + 1;
      if (next >= 0 && next < filtered.length) scrollToIdx(next);else if (next < 0) scrollToIdx(filtered.length - 1); // loop
      else scrollToIdx(0); // loop
    };
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: "100%",
        maxWidth: 560,
        marginBottom: 24
      }
    }, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        fontWeight: 700,
        letterSpacing: "0.06em",
        textTransform: "uppercase",
        marginBottom: 12,
        textAlign: "center"
      }
    }, "\uD83D\uDC64 Mes profils ", /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        color: "#64748b",
        fontWeight: 400
      }
    }, "(", savedProfiles.length, ")")), savedProfiles.length >= 4 && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "center",
        marginBottom: 12
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "relative",
        width: "100%",
        maxWidth: 280
      }
    }, /*#__PURE__*/external_React_default().createElement("input", {
      type: "text",
      placeholder: "Rechercher un profil\u2026",
      value: profileSearch,
      onChange: function onChange(e) {
        setProfileSearchTerm(e.target.value);
        setActiveProfileIdx(0);
      },
      style: {
        width: "100%",
        padding: "8px 12px 8px 32px",
        background: "rgba(255,255,255,0.05)",
        border: "1px solid rgba(255,255,255,0.1)",
        borderRadius: 10,
        color: "#e2e8f0",
        fontSize: 12,
        fontFamily: "'Inter',sans-serif",
        outline: "none",
        boxSizing: "border-box"
      }
    }), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        position: "absolute",
        left: 10,
        top: "50%",
        transform: "translateY(-50%)",
        fontSize: 13,
        color: "#64748b",
        pointerEvents: "none"
      }
    }, "\uD83D\uDD0D"), profileSearch && /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setProfileSearchTerm("");
        setActiveProfileIdx(0);
      },
      style: {
        position: "absolute",
        right: 8,
        top: "50%",
        transform: "translateY(-50%)",
        background: "none",
        border: "none",
        color: "#64748b",
        fontSize: 14,
        cursor: "pointer",
        padding: 2
      }
    }, "\u2715"))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "relative",
        display: "flex",
        alignItems: "center",
        gap: 4
      }
    }, filtered.length > 1 && /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return scrollDir("left");
      },
      "aria-label": "Pr\xE9c\xE9dent",
      style: {
        width: 36,
        height: 36,
        borderRadius: "50%",
        border: "1px solid rgba(255,255,255,0.12)",
        background: "rgba(255,255,255,0.06)",
        color: "#e2e8f0",
        fontSize: 18,
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexShrink: 0,
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        backdropFilter: "blur(8px)"
      },
      onMouseEnter: function onMouseEnter(e) {
        e.currentTarget.style.background = "rgba(249,115,22,0.15)";
        e.currentTarget.style.borderColor = "rgba(249,115,22,0.4)";
      },
      onMouseLeave: function onMouseLeave(e) {
        e.currentTarget.style.background = "rgba(255,255,255,0.06)";
        e.currentTarget.style.borderColor = "rgba(255,255,255,0.12)";
      }
    }, "\u2039"), /*#__PURE__*/external_React_default().createElement("div", {
      ref: carouselRef,
      className: "pa-carousel",
      onScroll: handleScroll,
      style: {
        display: "flex",
        gap: GAP,
        overflowX: "auto",
        scrollSnapType: "x mandatory",
        scrollBehavior: "smooth",
        flex: 1,
        padding: "6px 0 10px",
        msOverflowStyle: "none",
        scrollbarWidth: "none",
        WebkitOverflowScrolling: "touch"
      },
      onKeyDown: function onKeyDown(e) {
        if (e.key === "ArrowLeft") {
          e.preventDefault();
          scrollDir("left");
        }
        if (e.key === "ArrowRight") {
          e.preventDefault();
          scrollDir("right");
        }
      },
      tabIndex: 0
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        minWidth: "calc(50% - ".concat(CARD_W / 2, "px)"),
        flexShrink: 0
      },
      "aria-hidden": "true"
    }), filtered.map(function (sp, i) {
      var p = sp.profile || {};
      var styles = (p.styleTags || []).map(function (id) {
        var _STYLE_TAGS$find2;
        return (_STYLE_TAGS$find2 = STYLE_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _STYLE_TAGS$find2 === void 0 ? void 0 : _STYLE_TAGS$find2.label;
      }).filter(Boolean);
      var injuries = (p.injuryTags || []).filter(function (t) {
        return t !== "aucune";
      }).map(function (id) {
        var _INJURY_TAGS$find2;
        return (_INJURY_TAGS$find2 = INJURY_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _INJURY_TAGS$find2 === void 0 ? void 0 : _INJURY_TAGS$find2.label;
      }).filter(Boolean);
      var isJunior = p.age && parseInt(p.age) < 16;
      var levelColors = {
        Débutant: "#4CAF50",
        Intermédiaire: "#FF9800",
        Avancé: "#ef4444",
        Compétition: "#9C27B0"
      };
      var desc = [p.side && "C\xF4t\xE9 ".concat(p.side), p.hand].filter(Boolean).join(" · ");
      var stylesStr = styles.length ? styles.slice(0, 2).join(", ") : "";
      var isActive = i === activeProfileIdx;
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: sp.name,
        onClick: function onClick() {
          if (sp.locked) {
            setPasswordModal({
              mode: 'unlock',
              profileName: sp.name,
              onSuccess: function onSuccess() {
                selectHomeProfile(sp);
                setPasswordModal(null);
              }
            });
          } else {
            selectHomeProfile(sp);
          }
        },
        style: {
          background: isActive ? "rgba(249,115,22,0.08)" : "rgba(255,255,255,0.03)",
          border: isActive ? "1px solid rgba(249,115,22,0.35)" : "1px solid rgba(255,255,255,0.08)",
          borderRadius: 18,
          padding: "22px 16px 16px",
          cursor: "pointer",
          textAlign: "center",
          fontFamily: "'Inter',sans-serif",
          minWidth: CARD_W,
          maxWidth: CARD_W,
          flexShrink: 0,
          scrollSnapAlign: "center",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          gap: 8,
          position: "relative",
          transition: "all 0.3s cubic-bezier(0.4,0,0.2,1)",
          transform: isActive ? "scale(1.03)" : "scale(0.97)",
          opacity: isActive ? 1 : 0.7,
          boxShadow: isActive ? "0 4px 20px rgba(249,115,22,0.15)" : "none"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        onClick: function onClick(e) {
          e.stopPropagation();
          if (sp.locked) {
            setPasswordModal({
              mode: 'unlock-toggle',
              profileName: sp.name,
              onSuccess: function onSuccess() {
                var updated = toggleProfileLock(sp.name);
                setSavedProfiles(updated);
                setPasswordModal(null);
              }
            });
          } else {
            var pin = getAdminPin();
            if (!pin) {
              setPasswordModal({
                mode: 'setpin',
                profileName: sp.name,
                onSuccess: function onSuccess() {
                  var updated = toggleProfileLock(sp.name);
                  setSavedProfiles(updated);
                  setPasswordModal(null);
                }
              });
            } else {
              var updated = toggleProfileLock(sp.name);
              setSavedProfiles(updated);
            }
          }
        },
        style: {
          position: "absolute",
          top: 6,
          left: 6,
          width: 22,
          height: 22,
          borderRadius: "50%",
          background: sp.locked ? "rgba(99,102,241,0.15)" : "rgba(255,255,255,0.05)",
          border: "1px solid ".concat(sp.locked ? "rgba(99,102,241,0.3)" : "rgba(255,255,255,0.1)"),
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontSize: 11,
          color: sp.locked ? "#a5b4fc" : "#475569",
          cursor: "pointer",
          opacity: sp.locked ? 0.9 : 0.4,
          transition: "all 0.2s",
          zIndex: 2
        },
        onMouseEnter: function onMouseEnter(e) {
          e.currentTarget.style.opacity = "1";
        },
        onMouseLeave: function onMouseLeave(e) {
          e.currentTarget.style.opacity = sp.locked ? "0.9" : "0.4";
        },
        title: sp.locked ? "Déverrouiller" : "Verrouiller"
      }, sp.locked ? "🔒" : "🔓"), !sp.locked && /*#__PURE__*/external_React_default().createElement("div", {
        onClick: function onClick(e) {
          e.stopPropagation();
          setConfirmModal({
            message: "Supprimer le profil \"".concat(sp.name, "\" ?"),
            onConfirm: function onConfirm() {
              var updated = deleteNamedProfile(sp.name);
              setSavedProfiles(updated);
              setConfirmModal(null);
            },
            onCancel: function onCancel() {
              return setConfirmModal(null);
            }
          });
        },
        style: {
          position: "absolute",
          top: 6,
          right: 6,
          width: 22,
          height: 22,
          borderRadius: "50%",
          background: "rgba(239,68,68,0.1)",
          border: "1px solid rgba(239,68,68,0.2)",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontSize: 11,
          color: "#ef4444",
          cursor: "pointer",
          opacity: 0.5,
          transition: "all 0.2s",
          zIndex: 2
        },
        onMouseEnter: function onMouseEnter(e) {
          e.currentTarget.style.opacity = "1";
          e.currentTarget.style.background = "rgba(239,68,68,0.25)";
        },
        onMouseLeave: function onMouseLeave(e) {
          e.currentTarget.style.opacity = "0.5";
          e.currentTarget.style.background = "rgba(239,68,68,0.1)";
        }
      }, "\uD83D\uDDD1"), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: 56,
          height: 56,
          borderRadius: 16,
          background: isActive ? "linear-gradient(135deg,rgba(249,115,22,0.35),rgba(239,68,68,0.25))" : "linear-gradient(135deg,rgba(249,115,22,0.18),rgba(239,68,68,0.12))",
          border: isActive ? "2px solid rgba(249,115,22,0.5)" : "1px solid rgba(249,115,22,0.2)",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontSize: 24,
          fontWeight: 700,
          color: isActive ? "#f97316" : "#94a3b8",
          flexShrink: 0,
          transition: "all 0.3s"
        }
      }, sp.name.charAt(0).toUpperCase()), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 15,
          fontWeight: 700,
          color: isActive ? "#f1f5f9" : "#cbd5e1",
          lineHeight: 1.2,
          transition: "color 0.3s"
        }
      }, sp.name), p.level && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          fontWeight: 600,
          color: levelColors[p.level] || "#64748b",
          background: "".concat(levelColors[p.level] || "#64748b", "18"),
          padding: "2px 10px",
          borderRadius: 10,
          letterSpacing: "0.03em",
          textTransform: "uppercase"
        }
      }, p.level, isJunior ? " · Junior" : ""), desc && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 10,
          color: "#64748b",
          lineHeight: 1.3
        }
      }, desc), stylesStr && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#94a3b8",
          fontStyle: "italic"
        }
      }, stylesStr), injuries.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#ef4444",
          opacity: 0.8
        }
      }, "\uD83E\uDE79 ", injuries.join(", ")), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          marginTop: 6,
          fontSize: 10,
          color: sp.locked ? "#a5b4fc" : isActive ? "#f97316" : "#64748b",
          fontWeight: 600,
          letterSpacing: "0.04em",
          textTransform: "uppercase",
          transition: "color 0.3s"
        }
      }, sp.locked ? "🔒 Protégé" : isActive ? "▶ Ouvrir" : "Ouvrir →"));
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        minWidth: "calc(50% - ".concat(CARD_W / 2, "px)"),
        flexShrink: 0
      },
      "aria-hidden": "true"
    })), filtered.length > 1 && /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return scrollDir("right");
      },
      "aria-label": "Suivant",
      style: {
        width: 36,
        height: 36,
        borderRadius: "50%",
        border: "1px solid rgba(255,255,255,0.12)",
        background: "rgba(255,255,255,0.06)",
        color: "#e2e8f0",
        fontSize: 18,
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexShrink: 0,
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        backdropFilter: "blur(8px)"
      },
      onMouseEnter: function onMouseEnter(e) {
        e.currentTarget.style.background = "rgba(249,115,22,0.15)";
        e.currentTarget.style.borderColor = "rgba(249,115,22,0.4)";
      },
      onMouseLeave: function onMouseLeave(e) {
        e.currentTarget.style.background = "rgba(255,255,255,0.06)";
        e.currentTarget.style.borderColor = "rgba(255,255,255,0.12)";
      }
    }, "\u203A")), filtered.length > 1 && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "center",
        gap: 7,
        marginTop: 10
      }
    }, filtered.map(function (_, i) {
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: i,
        onClick: function onClick() {
          return scrollToIdx(i);
        },
        "aria-label": "Profil ".concat(i + 1),
        style: {
          width: i === activeProfileIdx ? 18 : 7,
          height: 7,
          borderRadius: 4,
          border: "none",
          cursor: "pointer",
          padding: 0,
          background: i === activeProfileIdx ? "#f97316" : "rgba(255,255,255,0.15)",
          transition: "all 0.3s cubic-bezier(0.4,0,0.2,1)"
        }
      });
    })), filtered.length > 1 && /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 8,
        color: "#334155",
        textAlign: "center",
        marginTop: 6,
        letterSpacing: "0.04em"
      }
    }, "\u2190 \u2192 Fl\xE8ches ou swipe pour naviguer"), profileSearch && filtered.length === 0 && /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#64748b",
        textAlign: "center",
        marginTop: 8
      }
    }, "Aucun profil trouv\xE9 pour \"", profileSearch, "\""));
  }(), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: createNewProfile,
    style: {
      padding: "14px 28px",
      background: "linear-gradient(135deg,rgba(249,115,22,0.2),rgba(239,68,68,0.15))",
      border: "1px solid rgba(249,115,22,0.35)",
      borderRadius: 14,
      color: "#f97316",
      fontSize: 14,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Inter',sans-serif",
      transition: "all 0.2s ease",
      letterSpacing: "-0.01em",
      width: "100%",
      maxWidth: 400
    }
  }, "+ Nouveau profil"), savedProfiles.length === 0 && /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#475569",
      marginTop: 12,
      textAlign: "center",
      lineHeight: 1.5
    }
  }, "Cr\xE9e ton premier profil pour commencer l'analyse"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginTop: 40,
      width: "100%",
      maxWidth: 500
    }
  }, /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      setMagCat("puissance");
      setMagYear(2026);
      setMagDetail(null);
      setScreen("magazine");
    },
    style: {
      width: "100%",
      padding: "20px 24px",
      borderRadius: 20,
      cursor: "pointer",
      background: "linear-gradient(135deg, rgba(249,115,22,0.08) 0%, rgba(168,85,247,0.06) 100%)",
      border: "1px solid rgba(249,115,22,0.2)",
      transition: "all 0.3s",
      position: "relative",
      overflow: "hidden"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      textAlign: "left"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 16,
      fontWeight: 800,
      fontFamily: "'Outfit'",
      color: "#f1f5f9",
      letterSpacing: "-0.02em"
    }
  }, "\uD83D\uDCF0 Tendances & Magazine"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#94a3b8",
      marginTop: 4
    }
  }, "Top raquettes, fiches techniques, articles")), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 24,
      color: "#f97316"
    }
  }, "\u2192")))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginTop: 40,
      fontSize: 8,
      color: "#334155",
      letterSpacing: "0.05em",
      textAlign: "center"
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontFamily: "'Outfit'",
      fontWeight: 600
    }
  }, "PADEL ANALYZER"), " V8.7 \xB7 ", rackets_db_namespaceObject.length, " raquettes \xB7 Scoring hybride calibr\xE9")), screen === "magazine" && function (_hero$proPlayerInfo) {
    var catIdx = MAGAZINE_CATEGORIES.findIndex(function (c) {
      return c.id === magCat;
    });
    var catInfo = MAGAZINE_CATEGORIES[catIdx];
    var top5 = getTopByCategory(magCat, magYear);
    var prevCat = function prevCat() {
      var i = (catIdx - 1 + MAGAZINE_CATEGORIES.length) % MAGAZINE_CATEGORIES.length;
      setMagCat(MAGAZINE_CATEGORIES[i].id);
      setMagDetail(null);
    };
    var nextCat = function nextCat() {
      var i = (catIdx + 1) % MAGAZINE_CATEGORIES.length;
      setMagCat(MAGAZINE_CATEGORIES[i].id);
      setMagDetail(null);
    };
    var hero = top5[0];
    var podium = top5.slice(1, 3);
    var rest = top5.slice(3, 5);

    // Helper: get display score for a racket
    var getScore = function getScore(r) {
      if (!r) return "—";
      if (catInfo !== null && catInfo !== void 0 && catInfo.attr) return (r.scores || {})[catInfo.attr] || 0;
      return (r._sortScore || 0).toFixed(1);
    };

    // DETAIL VIEW — single racket full page
    if (magDetail) {
      var r = magDetail;
      var sc = r.scores || {};
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          minHeight: "100dvh",
          display: "flex",
          flexDirection: "column",
          padding: "0 16px",
          maxWidth: 520,
          margin: "0 auto",
          fontFamily: "'Inter',sans-serif"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          alignItems: "center",
          padding: "16px 0 12px",
          gap: 12
        }
      }, /*#__PURE__*/external_React_default().createElement("button", {
        onClick: function onClick() {
          return setMagDetail(null);
        },
        style: {
          background: "none",
          border: "none",
          color: "#f97316",
          fontSize: 14,
          cursor: "pointer",
          fontWeight: 700,
          fontFamily: "'Outfit'"
        }
      }, "\u2190 Top 5"), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          flex: 1
        }
      }), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          color: "#475569",
          fontFamily: "'Outfit'"
        }
      }, catInfo === null || catInfo === void 0 ? void 0 : catInfo.label)), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center",
          marginBottom: 12
        }
      }, r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
        src: r.imageUrl,
        alt: r.name,
        style: {
          width: 80,
          height: 80,
          objectFit: "contain",
          margin: "0 auto 10px",
          display: "block",
          filter: "drop-shadow(0 4px 12px rgba(249,115,22,0.15))"
        },
        onError: function onError(e) {
          e.target.style.display = "none";
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 20,
          fontWeight: 900,
          fontFamily: "'Outfit'",
          color: "#f1f5f9",
          letterSpacing: "-0.02em"
        }
      }, r.shortName || r.name), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 11,
          color: "#94a3b8",
          marginTop: 4
        }
      }, r.brand, " \xB7 ", r.category, " \xB7 ", r.shape, " \xB7 ", r.year), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "inline-block",
          marginTop: 8,
          padding: "4px 14px",
          borderRadius: 20,
          background: "rgba(249,115,22,0.12)",
          border: "1px solid rgba(249,115,22,0.25)"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 22,
          fontWeight: 900,
          color: "#f97316",
          fontFamily: "'Outfit'"
        }
      }, getScore(r)), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          color: "#94a3b8",
          marginLeft: 2
        }
      }, "/10"))), r.editorial && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          padding: "14px 16px",
          background: "rgba(255,255,255,0.03)",
          borderRadius: 14,
          borderLeft: "3px solid rgba(249,115,22,0.3)",
          marginBottom: 14
        }
      }, /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 12,
          color: "#cbd5e1",
          lineHeight: 1.7,
          margin: 0
        }
      }, r.editorial)), r.targetProfile && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          padding: "10px 14px",
          background: "rgba(76,175,80,0.06)",
          borderRadius: 12,
          border: "1px solid rgba(76,175,80,0.15)",
          marginBottom: 14
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#4CAF50",
          fontWeight: 700,
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          marginBottom: 4
        }
      }, "\uD83C\uDFAF Cette raquette s'adresse \xE0"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 11,
          color: "#94a3b8",
          lineHeight: 1.6,
          margin: 0
        }
      }, r.targetProfile)), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "grid",
          gridTemplateColumns: "repeat(3,1fr)",
          gap: 6,
          marginBottom: 14
        }
      }, ATTRS.map(function (a) {
        var val = sc[a] || 0;
        var isHighlight = (catInfo === null || catInfo === void 0 ? void 0 : catInfo.attr) === a;
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: a,
          style: {
            textAlign: "center",
            padding: "8px 4px",
            borderRadius: 10,
            background: isHighlight ? "rgba(249,115,22,0.08)" : "rgba(255,255,255,0.02)",
            border: "1px solid ".concat(isHighlight ? "rgba(249,115,22,0.2)" : "rgba(255,255,255,0.04)")
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 20,
            fontWeight: 900,
            color: val >= 8 ? "#4CAF50" : val >= 6 ? "#e2e8f0" : "#f97316",
            fontFamily: "'Outfit'"
          }
        }, val), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 8,
            color: isHighlight ? "#f97316" : "#64748b",
            fontWeight: isHighlight ? 700 : 500,
            marginTop: 2
          }
        }, a));
      })), r.techHighlights && r.techHighlights.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          marginBottom: 14
        }
      }, r.techHighlights.map(function (h, i) {
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: i,
          style: {
            padding: "8px 12px",
            marginBottom: 4,
            borderRadius: 10,
            background: "rgba(255,255,255,0.02)",
            borderLeft: "2px solid rgba(249,115,22,0.15)"
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            justifyContent: "space-between",
            alignItems: "baseline"
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: 9,
            color: "#64748b",
            fontWeight: 600
          }
        }, h.label), /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: 10,
            color: "#e2e8f0",
            fontWeight: 700
          }
        }, h.value)), /*#__PURE__*/external_React_default().createElement("p", {
          style: {
            fontSize: 10,
            color: "#94a3b8",
            margin: "3px 0 0",
            lineHeight: 1.5
          }
        }, h.detail));
      })), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          flexWrap: "wrap",
          gap: 6,
          marginBottom: 14
        }
      }, [["Poids", r.weight], ["Prix", r.price], ["Équilibre", r.balance]].map(function (_ref10) {
        var _ref11 = _slicedToArray(_ref10, 2),
          k = _ref11[0],
          v = _ref11[1];
        return v && v !== "—" && /*#__PURE__*/external_React_default().createElement("span", {
          key: k,
          style: {
            fontSize: 9,
            padding: "4px 10px",
            borderRadius: 8,
            background: "rgba(255,255,255,0.04)",
            color: "#94a3b8"
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            color: "#64748b"
          }
        }, k, ":"), " ", /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontWeight: 600,
            color: "#cbd5e1"
          }
        }, v));
      })), r.proPlayerInfo && r.proPlayerInfo.name && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          padding: "10px 14px",
          background: "rgba(168,85,247,0.06)",
          borderRadius: 12,
          border: "1px solid rgba(168,85,247,0.15)",
          marginBottom: 14
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#a855f7",
          fontWeight: 700,
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          marginBottom: 4
        }
      }, "\uD83D\uDC64 Joueur Pro"), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 12,
          fontWeight: 700,
          color: "#e2e8f0"
        }
      }, r.proPlayerInfo.name), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 10,
          color: "#94a3b8",
          lineHeight: 1.5,
          margin: "3px 0 0"
        }
      }, r.proPlayerInfo.context)), r.verdict && r.verdict !== "—" && /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 10,
          color: "#64748b",
          lineHeight: 1.5,
          margin: "0 0 20px",
          fontStyle: "italic",
          textAlign: "center"
        }
      }, "\uD83D\uDCAC ", r.verdict));
    }

    // PODIUM VIEW — Top 5 one-page layout
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        minHeight: "100dvh",
        display: "flex",
        flexDirection: "column",
        padding: "0 16px",
        maxWidth: 520,
        margin: "0 auto",
        fontFamily: "'Inter',sans-serif"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "14px 0 8px"
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setScreen("home");
        setMagDetail(null);
      },
      style: {
        background: "none",
        border: "none",
        color: "#64748b",
        fontSize: 12,
        cursor: "pointer",
        fontFamily: "'Outfit'",
        fontWeight: 600
      }
    }, "\u2190 Accueil"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 4
      }
    }, [2026, 2025].map(function (y) {
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: y,
        onClick: function onClick() {
          return setMagYear(y);
        },
        style: {
          padding: "3px 12px",
          borderRadius: 14,
          fontSize: 10,
          fontWeight: magYear === y ? 700 : 500,
          cursor: "pointer",
          fontFamily: "'Inter'",
          background: magYear === y ? "rgba(249,115,22,0.15)" : "transparent",
          border: "1px solid ".concat(magYear === y ? "rgba(249,115,22,0.3)" : "rgba(255,255,255,0.08)"),
          color: magYear === y ? "#f97316" : "#64748b",
          transition: "all 0.2s"
        }
      }, y);
    }))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        gap: 12,
        padding: "6px 0 14px"
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: prevCat,
      style: {
        background: "none",
        border: "none",
        color: "#f97316",
        fontSize: 20,
        cursor: "pointer",
        padding: "4px 8px"
      }
    }, "\u2039"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        minWidth: 180
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 22,
        fontWeight: 900,
        fontFamily: "'Outfit'",
        color: "#f1f5f9",
        letterSpacing: "-0.02em"
      }
    }, catInfo === null || catInfo === void 0 ? void 0 : catInfo.label), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        fontStyle: "italic",
        marginTop: 2
      }
    }, catInfo === null || catInfo === void 0 ? void 0 : catInfo.desc)), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: nextCat,
      style: {
        background: "none",
        border: "none",
        color: "#f97316",
        fontSize: 20,
        cursor: "pointer",
        padding: "4px 8px"
      }
    }, "\u203A")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "center",
        gap: 6,
        marginBottom: 16
      }
    }, MAGAZINE_CATEGORIES.map(function (c, i) {
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: c.id,
        onClick: function onClick() {
          setMagCat(c.id);
          setMagDetail(null);
        },
        style: {
          width: i === catIdx ? 20 : 6,
          height: 6,
          borderRadius: 3,
          cursor: "pointer",
          transition: "all 0.3s",
          border: "none",
          background: i === catIdx ? "#f97316" : "rgba(255,255,255,0.15)"
        }
      });
    })), !top5.length && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        color: "#475569",
        fontSize: 13,
        textAlign: "center"
      }
    }, "Aucune raquette ", magYear, " dans cette cat\xE9gorie")), hero && /*#__PURE__*/external_React_default().createElement("div", {
      onClick: function onClick() {
        return setMagDetail(hero);
      },
      style: {
        cursor: "pointer",
        padding: "16px",
        borderRadius: 20,
        marginBottom: 10,
        position: "relative",
        overflow: "hidden",
        background: "linear-gradient(135deg, rgba(249,115,22,0.08) 0%, rgba(249,115,22,0.02) 100%)",
        border: "1px solid rgba(249,115,22,0.2)",
        transition: "all 0.3s"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "absolute",
        top: 10,
        left: 14,
        fontSize: 28
      }
    }, "\uD83E\uDD47"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 14,
        marginLeft: 38
      }
    }, hero.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
      src: hero.imageUrl,
      alt: hero.name,
      style: {
        width: 64,
        height: 64,
        objectFit: "contain",
        flexShrink: 0,
        filter: "drop-shadow(0 4px 8px rgba(0,0,0,0.3))"
      },
      onError: function onError(e) {
        e.target.style.display = "none";
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 16,
        fontWeight: 900,
        fontFamily: "'Outfit'",
        color: "#f1f5f9",
        letterSpacing: "-0.01em"
      }
    }, hero.shortName || hero.name), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        marginTop: 2
      }
    }, hero.brand, " \xB7 ", hero.shape, " \xB7 ", hero.weight), hero.editorial && /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#64748b",
        lineHeight: 1.5,
        margin: "6px 0 0",
        overflow: "hidden",
        display: "-webkit-box",
        WebkitLineClamp: 2,
        WebkitBoxOrient: "vertical"
      }
    }, hero.editorial.substring(0, 120), "\u2026")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        flexShrink: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 28,
        fontWeight: 900,
        fontFamily: "'Outfit'",
        color: "#f97316"
      }
    }, getScore(hero)), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 8,
        color: "#94a3b8"
      }
    }, "/10"))), ((_hero$proPlayerInfo = hero.proPlayerInfo) === null || _hero$proPlayerInfo === void 0 ? void 0 : _hero$proPlayerInfo.name) && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        marginTop: 8,
        marginLeft: 38,
        fontSize: 9,
        color: "#a855f7"
      }
    }, "\uD83D\uDC64 ", hero.proPlayerInfo.name)), podium.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: 8,
        marginBottom: 8
      }
    }, podium.map(function (r, i) {
      var _r$proPlayerInfo;
      var medal = i === 0 ? "🥈" : "🥉";
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: r.id,
        onClick: function onClick() {
          return setMagDetail(r);
        },
        style: {
          cursor: "pointer",
          padding: "12px",
          borderRadius: 16,
          background: "rgba(255,255,255,0.03)",
          border: "1px solid rgba(255,255,255,0.06)",
          transition: "all 0.3s"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          marginBottom: 6
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 18
        }
      }, medal), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "right"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 20,
          fontWeight: 900,
          fontFamily: "'Outfit'",
          color: "#e2e8f0"
        }
      }, getScore(r)), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 8,
          color: "#64748b"
        }
      }, "/10"))), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
        src: r.imageUrl,
        alt: r.name,
        style: {
          width: 40,
          height: 40,
          objectFit: "contain",
          margin: "0 auto 6px",
          display: "block"
        },
        onError: function onError(e) {
          e.target.style.display = "none";
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 12,
          fontWeight: 700,
          fontFamily: "'Outfit'",
          color: "#f1f5f9",
          overflow: "hidden",
          textOverflow: "ellipsis",
          whiteSpace: "nowrap"
        }
      }, r.shortName || r.name), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#64748b",
          marginTop: 2
        }
      }, r.brand, " \xB7 ", r.shape), ((_r$proPlayerInfo = r.proPlayerInfo) === null || _r$proPlayerInfo === void 0 ? void 0 : _r$proPlayerInfo.name) && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 8,
          color: "#a855f7",
          marginTop: 3
        }
      }, "\uD83D\uDC64 ", r.proPlayerInfo.name));
    })), rest.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: 8,
        marginBottom: 16
      }
    }, rest.map(function (r, i) {
      var badge = i === 0 ? "4" : "5";
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: r.id,
        onClick: function onClick() {
          return setMagDetail(r);
        },
        style: {
          cursor: "pointer",
          padding: "10px 12px",
          borderRadius: 14,
          background: "rgba(255,255,255,0.02)",
          border: "1px solid rgba(255,255,255,0.04)",
          transition: "all 0.3s"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          marginBottom: 4
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          fontWeight: 700,
          color: "#475569",
          background: "rgba(255,255,255,0.06)",
          borderRadius: 6,
          padding: "2px 6px"
        }
      }, badge), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 16,
          fontWeight: 800,
          fontFamily: "'Outfit'",
          color: "#94a3b8"
        }
      }, getScore(r)), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 7,
          color: "#475569"
        }
      }, "/10"))), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 11,
          fontWeight: 700,
          fontFamily: "'Outfit'",
          color: "#cbd5e1",
          overflow: "hidden",
          textOverflow: "ellipsis",
          whiteSpace: "nowrap"
        }
      }, r.shortName || r.name), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#475569",
          marginTop: 2
        }
      }, r.brand, " \xB7 ", r.shape));
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        padding: "8px 0 20px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 8,
        color: "#334155",
        letterSpacing: "0.05em"
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontFamily: "'Outfit'",
        fontWeight: 600
      }
    }, "PADEL ANALYZER"), " Magazine \xB7 ", rackets_db_namespaceObject.length, " raquettes")));
  }(), screen === "wizard" && function () {
    var TOTAL_STEPS = 9;
    var progress = (wizardStep + 1) / TOTAL_STEPS;
    var isJuniorW = Number(profile.age) > 0 && Number(profile.age) < 15 || Number(profile.height) > 0 && Number(profile.height) < 150;

    // Can advance?
    var canNext = [function () {
      return profileName.trim().length > 0;
    },
    // 0: name
    function () {
      return Number(profile.age) > 0;
    },
    // 1: gabarit (age required)
    function () {
      return !!profile.level;
    },
    // 2: level
    function () {
      return !!profile.hand && !!profile.side;
    },
    // 3: hand+side
    function () {
      return !!profile.frequency;
    },
    // 4: frequency
    function () {
      return (profile.styleTags || []).length > 0;
    },
    // 5: style
    function () {
      return (profile.injuryTags || []).length > 0;
    },
    // 6: injuries
    function () {
      return (profile.priorityTags || []).length > 0;
    },
    // 7: priorities
    function () {
      return true;
    } // 8: brands (optional)
    ][wizardStep]();
    var nextStep = function nextStep() {
      if (canNext && wizardStep < TOTAL_STEPS - 1) setWizardStep(function (s) {
        return s + 1;
      });
    };
    var prevStep = function prevStep() {
      if (wizardStep > 0) setWizardStep(function (s) {
        return s - 1;
      });
    };
    var goRecap = function goRecap() {
      if (canNext) setScreen("recap");
    };

    // Card select helper
    var CardSelect = function CardSelect(_ref12) {
      var options = _ref12.options,
        value = _ref12.value,
        onChange = _ref12.onChange,
        multi = _ref12.multi,
        _ref12$columns = _ref12.columns,
        columns = _ref12$columns === void 0 ? 2 : _ref12$columns;
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "grid",
          gridTemplateColumns: "repeat(".concat(columns, ",1fr)"),
          gap: 10,
          maxWidth: 460,
          margin: "0 auto",
          width: "100%"
        }
      }, options.map(function (o) {
        var sel = multi ? (value || []).includes(o.value) : value === o.value;
        return /*#__PURE__*/external_React_default().createElement("button", {
          key: o.value,
          onClick: function onClick() {
            if (multi) {
              if (o.value === "aucune") onChange(sel ? [] : [o.value]);else onChange(sel ? (value || []).filter(function (v) {
                return v !== o.value;
              }) : [].concat(_toConsumableArray((value || []).filter(function (v) {
                return v !== "aucune";
              })), [o.value]));
            } else onChange(o.value);
          },
          style: {
            padding: "16px 12px",
            borderRadius: 14,
            cursor: "pointer",
            textAlign: "center",
            fontFamily: "'Inter',sans-serif",
            background: sel ? "rgba(249,115,22,0.12)" : "rgba(255,255,255,0.03)",
            border: "2px solid ".concat(sel ? "#f97316" : "rgba(255,255,255,0.08)"),
            transition: "all 0.25s ease",
            transform: sel ? "scale(1.02)" : "scale(1)",
            boxShadow: sel ? "0 4px 16px rgba(249,115,22,0.15)" : "none"
          }
        }, o.icon && /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 24,
            marginBottom: 6
          }
        }, o.icon), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 14,
            fontWeight: 700,
            color: sel ? "#f97316" : "#e2e8f0"
          }
        }, o.label), o.desc && /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 10,
            color: sel ? "#fb923c" : "#64748b",
            marginTop: 3
          }
        }, o.desc));
      }));
    };

    // Tag multi-select helper
    var TagSelect = function TagSelect(_ref13) {
      var tags = _ref13.tags,
        field = _ref13.field,
        _ref13$colors = _ref13.colors,
        colors = _ref13$colors === void 0 ? {
          on: "#f97316",
          bg: "rgba(249,115,22,0.12)",
          border: "#f97316"
        } : _ref13$colors;
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          flexWrap: "wrap",
          gap: 8,
          justifyContent: "center",
          maxWidth: 500,
          margin: "0 auto"
        }
      }, tags.map(function (t) {
        var sel = (profile[field] || []).includes(t.id);
        var isNone = t.id === "aucune";
        var c = isNone ? {
          on: "#4CAF50",
          bg: "rgba(76,175,80,0.15)",
          border: "#4CAF50"
        } : colors;
        return /*#__PURE__*/external_React_default().createElement("button", {
          key: t.id,
          onClick: function onClick() {
            return toggleTag(field, t.id);
          },
          style: {
            padding: "10px 16px",
            borderRadius: 12,
            cursor: "pointer",
            fontFamily: "'Inter',sans-serif",
            background: sel ? c.bg : "rgba(255,255,255,0.03)",
            border: "2px solid ".concat(sel ? c.border : "rgba(255,255,255,0.08)"),
            color: sel ? c.on : "#94a3b8",
            fontSize: 12,
            fontWeight: sel ? 700 : 500,
            transition: "all 0.2s ease",
            transform: sel ? "scale(1.04)" : "scale(1)"
          }
        }, /*#__PURE__*/external_React_default().createElement("div", null, isNone ? "✓ " : "", t.label), t.tip && /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 9,
            color: sel ? "#fb923c" : "#475569",
            marginTop: 2,
            fontWeight: 400
          }
        }, t.tip));
      }));
    };
    var stepContent = [
    // Step 0: Name
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83D\uDC64"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Comment tu t'appelles ?"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Le nom de ton profil joueur."), /*#__PURE__*/external_React_default().createElement("input", {
        value: profileName,
        onChange: function onChange(e) {
          return setProfileName(e.target.value);
        },
        placeholder: "Ex: Bidou, Noah, Maman...",
        onKeyDown: function onKeyDown(e) {
          if (e.key === "Enter" && canNext) nextStep();
        },
        autoFocus: true,
        style: {
          width: "100%",
          maxWidth: 360,
          padding: "16px 20px",
          borderRadius: 14,
          fontSize: 18,
          fontWeight: 600,
          background: "rgba(255,255,255,0.05)",
          border: "2px solid rgba(249,115,22,0.3)",
          color: "#f1f5f9",
          fontFamily: "'Inter',sans-serif",
          outline: "none",
          textAlign: "center"
        }
      }));
    },
    // Step 1: Gabarit
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83D\uDCCF"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Ton gabarit"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Pour adapter le poids et la taille de raquette id\xE9ale."), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "grid",
          gridTemplateColumns: "1fr 1fr 1fr",
          gap: 12,
          maxWidth: 400,
          margin: "0 auto"
        }
      }, [{
        key: "age",
        label: "Âge",
        ph: "49",
        unit: "ans"
      }, {
        key: "height",
        label: "Taille",
        ph: "175",
        unit: "cm"
      }, {
        key: "weight",
        label: "Poids",
        ph: "80",
        unit: "kg"
      }].map(function (f) {
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: f.key,
          style: {
            textAlign: "center"
          }
        }, /*#__PURE__*/external_React_default().createElement("label", {
          style: {
            fontSize: 10,
            color: "#94a3b8",
            fontWeight: 600,
            textTransform: "uppercase",
            letterSpacing: "0.05em",
            display: "block",
            marginBottom: 6
          }
        }, f.label), /*#__PURE__*/external_React_default().createElement("input", {
          type: "number",
          value: profile[f.key],
          onChange: function onChange(e) {
            return setProfile(function (p) {
              return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, PadelAnalyzer_defineProperty({}, f.key, Number(e.target.value)));
            });
          },
          placeholder: f.ph,
          style: {
            width: "100%",
            padding: "14px 8px",
            borderRadius: 12,
            fontSize: 22,
            fontWeight: 700,
            background: "rgba(255,255,255,0.05)",
            border: "2px solid rgba(255,255,255,0.1)",
            color: "#f1f5f9",
            fontFamily: "'Inter',sans-serif",
            outline: "none",
            textAlign: "center"
          }
        }), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 9,
            color: "#475569",
            marginTop: 4
          }
        }, f.unit));
      })), isJuniorW && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          background: "rgba(59,130,246,0.1)",
          border: "1px solid rgba(59,130,246,0.3)",
          borderRadius: 10,
          padding: "10px 14px",
          marginTop: 16,
          fontSize: 11,
          color: "#60a5fa",
          fontWeight: 600,
          maxWidth: 400,
          margin: "16px auto 0"
        }
      }, "\uD83E\uDDD2 Profil junior d\xE9tect\xE9 \u2014 recommandations adapt\xE9es"), Number(profile.age) >= 50 && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          background: "rgba(245,158,11,0.08)",
          border: "1px solid rgba(245,158,11,0.2)",
          borderRadius: 10,
          padding: "10px 14px",
          marginTop: 16,
          fontSize: 11,
          color: "#fbbf24",
          fontWeight: 600,
          maxWidth: 400,
          margin: "16px auto 0"
        }
      }, "\uD83D\uDC64 Profil 50+ \u2014 Confort et Maniabilit\xE9 renforc\xE9s"));
    },
    // Step 2: Level
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83C\uDFC6"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Quel est ton niveau ?"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "\xC7a d\xE9termine la gamme de raquettes qu'on va te proposer."), /*#__PURE__*/external_React_default().createElement(CardSelect, {
        options: LEVEL_OPTIONS.map(function (o) {
          return {
            value: o.value,
            label: o.label,
            desc: o.desc,
            icon: {
              Débutant: "🌱",
              Intermédiaire: "🎾",
              Avancé: "🔥",
              Expert: "💎"
            }[o.value]
          };
        }),
        value: profile.level,
        onChange: function onChange(v) {
          setProfile(function (p) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
              level: v
            });
          });
          setTimeout(nextStep, 300);
        }
      }));
    },
    // Step 3: Hand + Side
    function () {
      var h = profile.hand || "Droitier",
        s = profile.side || "Droite";
      var atk = h === "Droitier" && s === "Gauche" || h === "Gaucher" && s === "Droite";
      var cst = h === "Droitier" && s === "Droite" || h === "Gaucher" && s === "Gauche";
      var role = s === "Les deux" ? "Polyvalent" : atk ? "Attaquant (coup droit au centre)" : "Constructeur (revers au centre)";
      var roleColor = atk ? "#f97316" : cst ? "#6366f1" : "#94a3b8";
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83E\uDD1A"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Ta prise de jeu"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Main + c\xF4t\xE9 = on d\xE9tecte ton r\xF4le sur le terrain."), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          maxWidth: 400,
          margin: "0 auto"
        }
      }, /*#__PURE__*/external_React_default().createElement("label", {
        style: {
          fontSize: 10,
          color: "#94a3b8",
          fontWeight: 600,
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          display: "block",
          marginBottom: 8
        }
      }, "Main dominante"), /*#__PURE__*/external_React_default().createElement(CardSelect, {
        columns: 2,
        options: HAND_OPTIONS.map(function (o) {
          return {
            value: o,
            label: o,
            icon: o === "Droitier" ? "🫲" : "🫱"
          };
        }),
        value: profile.hand,
        onChange: function onChange(v) {
          return setProfile(function (p) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
              hand: v
            });
          });
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          height: 20
        }
      }), /*#__PURE__*/external_React_default().createElement("label", {
        style: {
          fontSize: 10,
          color: "#94a3b8",
          fontWeight: 600,
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          display: "block",
          marginBottom: 8
        }
      }, "C\xF4t\xE9 de jeu"), /*#__PURE__*/external_React_default().createElement(CardSelect, {
        columns: 3,
        options: SIDE_OPTIONS.map(function (o) {
          return {
            value: o,
            label: o,
            icon: {
              Gauche: "⬅️",
              Droite: "➡️",
              "Les deux": "↔️"
            }[o]
          };
        }),
        value: profile.side,
        onChange: function onChange(v) {
          return setProfile(function (p) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
              side: v
            });
          });
        }
      })), profile.hand && profile.side && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          background: "".concat(roleColor, "15"),
          border: "1.5px solid ".concat(roleColor, "40"),
          borderRadius: 12,
          padding: "12px 16px",
          marginTop: 20,
          fontSize: 13,
          color: roleColor,
          fontWeight: 700,
          maxWidth: 400,
          margin: "20px auto 0"
        }
      }, "\uD83C\uDFAF R\xF4le d\xE9tect\xE9 : ", role));
    },
    // Step 4: Frequency + Competition
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83D\uDCC5"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Ton rythme de jeu"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Fr\xE9quence et comp\xE9tition influencent la durabilit\xE9 requise."), /*#__PURE__*/external_React_default().createElement(CardSelect, {
        options: FREQ_OPTIONS.map(function (o) {
          return {
            value: o.value,
            label: o.label,
            desc: o.desc,
            icon: {
              Occasionnel: "🌙",
              Régulier: "☀️",
              Assidu: "🔥",
              Intensif: "⚡"
            }[o.label]
          };
        }),
        value: profile.frequency,
        onChange: function onChange(v) {
          return setProfile(function (p) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
              frequency: v
            });
          });
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          marginTop: 24,
          maxWidth: 460,
          margin: "24px auto 0"
        }
      }, /*#__PURE__*/external_React_default().createElement("label", {
        style: {
          fontSize: 10,
          color: "#94a3b8",
          fontWeight: 600,
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          display: "block",
          marginBottom: 10
        }
      }, "Tu fais de la comp\xE9tition ?"), /*#__PURE__*/external_React_default().createElement(CardSelect, {
        columns: 2,
        options: [{
          value: true,
          label: "Oui",
          icon: "🏅",
          desc: "Tournois, classement"
        }, {
          value: false,
          label: "Non",
          icon: "🎾",
          desc: "Loisir, entre amis"
        }],
        value: profile.competition,
        onChange: function onChange(v) {
          return setProfile(function (p) {
            return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
              competition: v
            });
          });
        }
      })));
    },
    // Step 5: Style
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83C\uDFBE"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Comment tu joues ?"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "S\xE9lectionne tout ce qui te correspond. \xC7a influence le scoring."), /*#__PURE__*/external_React_default().createElement(TagSelect, {
        tags: STYLE_TAGS,
        field: "styleTags"
      }));
    },
    // Step 6: Injuries
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83E\uDE79"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Ton corps"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Les blessures boostent le crit\xE8re Confort dans les recommandations."), /*#__PURE__*/external_React_default().createElement(TagSelect, {
        tags: INJURY_TAGS,
        field: "injuryTags",
        colors: {
          on: "#ef4444",
          bg: "rgba(239,68,68,0.12)",
          border: "#ef4444"
        }
      }));
    },
    // Step 7: Priorities
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83C\uDFAF"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Qu'est-ce que tu cherches ?"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Ces crit\xE8res pond\xE8rent le score de chaque raquette."), /*#__PURE__*/external_React_default().createElement(TagSelect, {
        tags: PRIORITY_TAGS,
        field: "priorityTags",
        colors: {
          on: "#4CAF50",
          bg: "rgba(76,175,80,0.12)",
          border: "#4CAF50"
        }
      }));
    },
    // Step 8: Brands
    function () {
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 40,
          marginBottom: 16
        }
      }, "\uD83C\uDFF7"), /*#__PURE__*/external_React_default().createElement("h2", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 26,
          fontWeight: 800,
          color: "#f1f5f9",
          margin: "0 0 8px"
        }
      }, "Marques pr\xE9f\xE9r\xE9es"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          fontSize: 13,
          color: "#64748b",
          margin: "0 0 28px"
        }
      }, "Optionnel \u2014 laisse vide pour voir toutes les marques."), /*#__PURE__*/external_React_default().createElement(TagSelect, {
        tags: BRAND_TAGS,
        field: "brandTags",
        colors: {
          on: "#CE93D8",
          bg: "rgba(156,39,176,0.12)",
          border: "#9C27B0"
        }
      }));
    }];
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px 16px",
        animation: "fadeIn 0.3s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("style", null, "\n            @keyframes wizardSlideIn { from { opacity:0; transform:translateX(40px); } to { opacity:1; transform:translateX(0); } }\n            @keyframes wizardSlideOut { from { opacity:1; transform:translateX(0); } to { opacity:0; transform:translateX(-40px); } }\n          "), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        top: 0,
        left: 0,
        right: 0,
        height: 4,
        background: "rgba(255,255,255,0.06)",
        zIndex: 10
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        height: "100%",
        background: "linear-gradient(90deg,#f97316,#ef4444)",
        borderRadius: "0 2px 2px 0",
        transition: "width 0.5s cubic-bezier(.4,0,.2,1)",
        width: "".concat(progress * 100, "%")
      }
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        top: 16,
        right: 20,
        fontSize: 11,
        color: "#475569",
        fontWeight: 600,
        zIndex: 10
      }
    }, wizardStep + 1, "/", TOTAL_STEPS), wizardStep > 0 && /*#__PURE__*/external_React_default().createElement("button", {
      onClick: prevStep,
      style: {
        position: "fixed",
        top: 14,
        left: 16,
        background: "none",
        border: "1px solid rgba(255,255,255,0.1)",
        borderRadius: 10,
        padding: "6px 14px",
        color: "#94a3b8",
        fontSize: 11,
        cursor: "pointer",
        fontFamily: "inherit",
        zIndex: 10
      }
    }, "\u2190 Retour"), /*#__PURE__*/external_React_default().createElement("div", {
      key: wizardStep,
      style: {
        width: "100%",
        maxWidth: 540,
        animation: "wizardSlideIn 0.4s ease"
      }
    }, stepContent[wizardStep]()), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        marginTop: 36,
        display: "flex",
        gap: 12,
        alignItems: "center"
      }
    }, wizardStep === TOTAL_STEPS - 1 ? /*#__PURE__*/external_React_default().createElement("button", {
      onClick: goRecap,
      style: {
        padding: "14px 40px",
        borderRadius: 14,
        fontSize: 15,
        fontWeight: 800,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "linear-gradient(135deg,rgba(76,175,80,0.25),rgba(76,175,80,0.12))",
        border: "1.5px solid rgba(76,175,80,0.4)",
        color: "#4CAF50",
        transition: "all 0.3s ease"
      }
    }, "Voir le r\xE9cap \u2192") : /*#__PURE__*/external_React_default().createElement("button", {
      onClick: nextStep,
      disabled: !canNext,
      style: {
        padding: "14px 40px",
        borderRadius: 14,
        fontSize: 15,
        fontWeight: 800,
        cursor: canNext ? "pointer" : "not-allowed",
        fontFamily: "'Outfit',sans-serif",
        background: canNext ? "linear-gradient(135deg,rgba(249,115,22,0.25),rgba(239,68,68,0.15))" : "rgba(255,255,255,0.03)",
        border: "1.5px solid ".concat(canNext ? "rgba(249,115,22,0.4)" : "rgba(255,255,255,0.06)"),
        color: canNext ? "#f97316" : "#334155",
        transition: "all 0.3s ease",
        opacity: canNext ? 1 : 0.5
      }
    }, "Suivant \u2192")), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setScreen("home");
      },
      style: {
        position: "fixed",
        bottom: 20,
        left: "50%",
        transform: "translateX(-50%)",
        background: "none",
        border: "none",
        color: "#334155",
        fontSize: 10,
        cursor: "pointer",
        fontFamily: "inherit",
        padding: "6px 12px"
      }
    }, "\u2190 Retour \xE0 l'accueil"));
  }(), screen === "recap" && function (_FREQ_OPTIONS$find) {
    var styles = (profile.styleTags || []).map(function (id) {
      var _STYLE_TAGS$find3;
      return (_STYLE_TAGS$find3 = STYLE_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _STYLE_TAGS$find3 === void 0 ? void 0 : _STYLE_TAGS$find3.label;
    }).filter(Boolean);
    var injuries = (profile.injuryTags || []).filter(function (t) {
      return t !== "aucune";
    }).map(function (id) {
      var _INJURY_TAGS$find3;
      return (_INJURY_TAGS$find3 = INJURY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _INJURY_TAGS$find3 === void 0 ? void 0 : _INJURY_TAGS$find3.label;
    }).filter(Boolean);
    var priorities = (profile.priorityTags || []).map(function (id) {
      var _PRIORITY_TAGS$find3;
      return (_PRIORITY_TAGS$find3 = PRIORITY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _PRIORITY_TAGS$find3 === void 0 ? void 0 : _PRIORITY_TAGS$find3.label;
    }).filter(Boolean);
    var brands = (profile.brandTags || []).map(function (id) {
      var _BRAND_TAGS$find4;
      return (_BRAND_TAGS$find4 = BRAND_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _BRAND_TAGS$find4 === void 0 ? void 0 : _BRAND_TAGS$find4.label;
    }).filter(Boolean);
    var hand = profile.hand || "Droitier",
      side = profile.side || "Droite";
    var isAttacker = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
    var role = side === "Les deux" ? "Polyvalent" : isAttacker ? "Attaquant" : "Constructeur";
    var levelColors = {
      Débutant: "#4CAF50",
      Intermédiaire: "#FF9800",
      Avancé: "#ef4444",
      Expert: "#9C27B0"
    };
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px 16px",
        animation: "fadeIn 0.5s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: "100%",
        maxWidth: 480
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        marginBottom: 24
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 48,
        marginBottom: 12
      }
    }, "\uD83C\uDFBE"), /*#__PURE__*/external_React_default().createElement("h2", {
      style: {
        fontFamily: "'Outfit'",
        fontSize: 28,
        fontWeight: 800,
        color: "#f1f5f9",
        margin: "0 0 6px"
      }
    }, "C'est bien toi ?"), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 13,
        color: "#64748b",
        margin: 0
      }
    }, "V\xE9rifie ton profil avant de lancer l'analyse.")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.1)",
        borderRadius: 20,
        padding: "24px 22px",
        marginBottom: 24
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 14,
        marginBottom: 18
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 56,
        height: 56,
        borderRadius: 16,
        background: "linear-gradient(135deg,rgba(249,115,22,0.35),rgba(239,68,68,0.25))",
        border: "2px solid rgba(249,115,22,0.5)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 26,
        fontWeight: 700,
        color: "#f97316",
        flexShrink: 0
      }
    }, profileName.charAt(0).toUpperCase()), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 22,
        fontWeight: 800,
        color: "#f1f5f9",
        fontFamily: "'Outfit'"
      }
    }, profileName), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 8,
        alignItems: "center",
        marginTop: 4
      }
    }, profile.level && /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 10,
        fontWeight: 600,
        color: levelColors[profile.level],
        background: "".concat(levelColors[profile.level], "18"),
        padding: "3px 10px",
        borderRadius: 8,
        textTransform: "uppercase"
      }
    }, profile.level), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 11,
        color: "#94a3b8"
      }
    }, hand, " \xB7 C\xF4t\xE9 ", side)))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: 12
      }
    }, profile.age && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        borderRadius: 10,
        padding: "10px 12px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        textTransform: "uppercase",
        fontWeight: 600,
        marginBottom: 3
      }
    }, "Gabarit"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 12,
        color: "#e2e8f0",
        fontWeight: 600
      }
    }, profile.age, " ans", profile.height ? ", ".concat(profile.height, "cm") : "", profile.weight ? ", ".concat(profile.weight, "kg") : "")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        borderRadius: 10,
        padding: "10px 12px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        textTransform: "uppercase",
        fontWeight: 600,
        marginBottom: 3
      }
    }, "R\xF4le"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 12,
        color: isAttacker ? "#f97316" : "#a5b4fc",
        fontWeight: 600
      }
    }, "\uD83C\uDFAF ", role)), profile.frequency && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        borderRadius: 10,
        padding: "10px 12px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        textTransform: "uppercase",
        fontWeight: 600,
        marginBottom: 3
      }
    }, "Fr\xE9quence"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 12,
        color: "#e2e8f0",
        fontWeight: 600
      }
    }, ((_FREQ_OPTIONS$find = FREQ_OPTIONS.find(function (f) {
      return f.value === profile.frequency;
    })) === null || _FREQ_OPTIONS$find === void 0 ? void 0 : _FREQ_OPTIONS$find.label) || profile.frequency, profile.competition ? " · 🏅 Compétition" : "")), styles.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        borderRadius: 10,
        padding: "10px 12px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        textTransform: "uppercase",
        fontWeight: 600,
        marginBottom: 3
      }
    }, "Style"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        color: "#a5b4fc",
        fontWeight: 600
      }
    }, styles.join(", ")))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        flexWrap: "wrap",
        gap: 6,
        marginTop: 14,
        justifyContent: "center"
      }
    }, priorities.map(function (p) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: p,
        style: {
          fontSize: 10,
          background: "rgba(76,175,80,0.1)",
          border: "1px solid rgba(76,175,80,0.25)",
          borderRadius: 8,
          padding: "4px 10px",
          color: "#4CAF50",
          fontWeight: 600
        }
      }, "\uD83C\uDFAF ", p);
    }), injuries.map(function (i) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: i,
        style: {
          fontSize: 10,
          background: "rgba(239,68,68,0.1)",
          border: "1px solid rgba(239,68,68,0.25)",
          borderRadius: 8,
          padding: "4px 10px",
          color: "#ef4444",
          fontWeight: 600
        }
      }, "\uD83E\uDE79 ", i);
    }), brands.map(function (b) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: b,
        style: {
          fontSize: 10,
          background: "rgba(156,39,176,0.1)",
          border: "1px solid rgba(156,39,176,0.25)",
          borderRadius: 8,
          padding: "4px 10px",
          color: "#CE93D8",
          fontWeight: 600
        }
      }, "\uD83C\uDFF7 ", b);
    }))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 12
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setWizardStep(0);
        setScreen("wizard");
      },
      style: {
        flex: 1,
        padding: "14px",
        borderRadius: 14,
        fontSize: 14,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "rgba(255,255,255,0.03)",
        border: "1.5px solid rgba(255,255,255,0.1)",
        color: "#94a3b8"
      }
    }, "\u270F\uFE0F Modifier"), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        if (!profileName.trim()) {
          alert("Donne un nom à ton profil");
          return;
        }
        var list = saveNamedProfile(profileName.trim(), profile);
        setSavedProfiles(list);
        setScreen("analyzing");
        setRevealIdx(0);
      },
      style: {
        flex: 2,
        padding: "14px",
        borderRadius: 14,
        fontSize: 15,
        fontWeight: 800,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "linear-gradient(135deg,rgba(249,115,22,0.25),rgba(239,68,68,0.15))",
        border: "1.5px solid rgba(249,115,22,0.4)",
        color: "#f97316",
        boxShadow: "0 4px 20px rgba(249,115,22,0.2)",
        transition: "all 0.3s ease"
      }
    }, "\uD83D\uDE80 Lancer l'analyse"))));
  }(), screen === "analyzing" && function () {
    var styles = (profile.styleTags || []).map(function (id) {
      var _STYLE_TAGS$find4;
      return (_STYLE_TAGS$find4 = STYLE_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _STYLE_TAGS$find4 === void 0 ? void 0 : _STYLE_TAGS$find4.label;
    }).filter(Boolean);
    var injuries = (profile.injuryTags || []).filter(function (t) {
      return t !== "aucune";
    }).map(function (id) {
      var _INJURY_TAGS$find4;
      return (_INJURY_TAGS$find4 = INJURY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _INJURY_TAGS$find4 === void 0 ? void 0 : _INJURY_TAGS$find4.label;
    }).filter(Boolean);
    var priorities = (profile.priorityTags || []).map(function (id) {
      var _PRIORITY_TAGS$find4;
      return (_PRIORITY_TAGS$find4 = PRIORITY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _PRIORITY_TAGS$find4 === void 0 ? void 0 : _PRIORITY_TAGS$find4.label;
    }).filter(Boolean);
    var hand = profile.hand || "Droitier",
      side = profile.side || "Droite";
    var isAttacker = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
    var role = side === "Les deux" ? "Polyvalent" : isAttacker ? "Attaquant" : "Constructeur";
    var age = Number(profile.age) || 0;
    var isJuniorA = age > 0 && age < 15 || Number(profile.height) > 0 && Number(profile.height) < 150;

    // Build analysis lines (different from pertinence!)
    var lines = [];
    lines.push("Analyse du profil de ".concat(profileName, "..."));
    if (age) lines.push("".concat(age, " ans, ").concat(role.toLowerCase(), " c\xF4t\xE9 ").concat(side.toLowerCase()).concat(profile.competition ? " en compétition" : "", "."));
    if (styles.length > 0) {
      var defStyles = styles.filter(function (s) {
        return ["Défensif / Mur", "Endurant", "Contre-attaquant"].includes(s);
      });
      var atkStyles = styles.filter(function (s) {
        return ["Offensif", "Puissant / Frappeur"].includes(s);
      });
      if (defStyles.length > 0 && atkStyles.length > 0) lines.push("Profil mixte : ".concat(atkStyles.join(", "), " mais aussi ").concat(defStyles.join(", "), ". Le syst\xE8me doit trouver le bon \xE9quilibre."));else if (atkStyles.length > 0) lines.push("Orientation offensive claire : ".concat(atkStyles.join(", "), ". On cherche de la puissance et du rendement."));else if (defStyles.length > 0) lines.push("Jeu patient et solide : ".concat(defStyles.join(", "), ". Le contr\xF4le et la tol\xE9rance seront cl\xE9s."));else lines.push("Style ".concat(styles.slice(0, 3).join(", "), " \u2014 profil technique qui demande un bon \xE9quilibre des attributs."));
    } else {
      lines.push("Pas de style d\xE9clar\xE9 \u2014 on se base sur les priorit\xE9s et le profil physique.");
    }
    if (priorities.length > 0) lines.push("Priorit\xE9s : ".concat(priorities.join(", "), ". Ces crit\xE8res p\xE8sent le plus lourd dans le scoring."));
    if (injuries.length > 0) lines.push("\u26A0 Attention ".concat(injuries.join(", "), " \u2014 le confort sera un crit\xE8re non n\xE9gociable."));
    if (isJuniorA) lines.push("Profil junior : raquettes l\xE9g\xE8res et tol\xE9rantes en priorit\xE9.");
    lines.push("Calcul en cours sur ".concat(rackets_db_namespaceObject.length, " raquettes..."));
    lines.push("R\xE9sultats pr\xEAts.");
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        inset: 0,
        background: "#0b0f1a",
        zIndex: 1000,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        padding: "20px",
        animation: "fadeIn 0.4s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("style", null, "\n            @keyframes analyzeLineIn { from { opacity:0; transform:translateY(12px); } to { opacity:1; transform:translateY(0); } }\n            @keyframes analyzePulse { 0%,100% { opacity:0.4; } 50% { opacity:1; } }\n            @keyframes analyzeSpinner { to { transform:rotate(360deg); } }\n          "), /*#__PURE__*/external_React_default().createElement("svg", {
      width: "48",
      height: "48",
      viewBox: "0 0 44 44",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        marginBottom: 24,
        filter: "drop-shadow(0 8px 24px rgba(249,115,22,0.4))"
      }
    }, /*#__PURE__*/external_React_default().createElement("defs", null, /*#__PURE__*/external_React_default().createElement("linearGradient", {
      id: "logoAnalyze",
      x1: "0",
      y1: "0",
      x2: "44",
      y2: "44"
    }, /*#__PURE__*/external_React_default().createElement("stop", {
      offset: "0%",
      stopColor: "#f97316"
    }), /*#__PURE__*/external_React_default().createElement("stop", {
      offset: "100%",
      stopColor: "#ef4444"
    }))), /*#__PURE__*/external_React_default().createElement("rect", {
      width: "44",
      height: "44",
      rx: "10",
      fill: "url(#logoAnalyze)"
    }), /*#__PURE__*/external_React_default().createElement("ellipse", {
      cx: "22",
      cy: "18",
      rx: "10",
      ry: "12",
      stroke: "#fff",
      strokeWidth: "2.2",
      fill: "none"
    }), /*#__PURE__*/external_React_default().createElement("line", {
      x1: "22",
      y1: "30",
      x2: "22",
      y2: "38",
      stroke: "#fff",
      strokeWidth: "2.5",
      strokeLinecap: "round"
    }), /*#__PURE__*/external_React_default().createElement("circle", {
      cx: "33",
      cy: "32",
      r: "3.5",
      fill: "#fff",
      opacity: "0.85"
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        maxWidth: 480,
        width: "100%"
      }
    }, lines.map(function (line, i) {
      var isLast = i === lines.length - 1;
      var delay = i * 800;
      var totalDuration = lines.length * 800 + 600;
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: i,
        style: {
          fontSize: i === 0 ? 18 : isLast ? 15 : 13,
          fontWeight: i === 0 ? 800 : isLast ? 700 : 500,
          color: i === 0 ? "#f1f5f9" : isLast ? "#f97316" : "#94a3b8",
          fontFamily: i === 0 || isLast ? "'Outfit',sans-serif" : "'Inter',sans-serif",
          marginBottom: i === 0 ? 20 : 10,
          textAlign: "center",
          animation: "analyzeLineIn 0.5s ease ".concat(delay, "ms both"),
          lineHeight: 1.6
        }
      }, line);
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "center",
        marginTop: 20,
        animation: "analyzeLineIn 0.4s ease ".concat((lines.length - 2) * 800, "ms both")
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 28,
        height: 28,
        border: "3px solid rgba(249,115,22,0.2)",
        borderTopColor: "#f97316",
        borderRadius: "50%",
        animation: "analyzeSpinner 0.8s linear infinite"
      }
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        animation: "analyzeLineIn 0.5s ease ".concat(lines.length * 800 + 200, "ms both"),
        textAlign: "center",
        marginTop: 24
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setScreen("reveal");
      },
      style: {
        padding: "14px 36px",
        borderRadius: 14,
        fontSize: 15,
        fontWeight: 800,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "linear-gradient(135deg,rgba(249,115,22,0.3),rgba(239,68,68,0.2))",
        border: "1.5px solid rgba(249,115,22,0.5)",
        color: "#f97316",
        transition: "all 0.3s",
        boxShadow: "0 4px 20px rgba(249,115,22,0.25)"
      }
    }, "\uD83C\uDFC6 D\xE9couvrir mes r\xE9sultats"))));
  }(), screen === "reveal" && function () {
    var age = Number(profile.age) || 0;
    var ht = Number(profile.height) || 0;
    var isJunior = age > 0 && age < 15 || ht > 0 && ht < 150;
    var pool = isJunior ? rackets_db_namespaceObject.filter(function (r) {
      return r.category === 'junior';
    }) : function () {
      var lvl = profile.level || 'Débutant';
      var catMap = {
        'Débutant': ['debutant', 'intermediaire'],
        'Intermédiaire': ['intermediaire', 'debutant', 'avance'],
        'Avancé': ['avance', 'intermediaire', 'expert'],
        'Expert': ['expert', 'avance']
      };
      return rackets_db_namespaceObject.filter(function (r) {
        return (catMap[lvl] || ['debutant', 'intermediaire']).includes(r.category);
      });
    }();
    var scored = pool.map(function (r) {
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _gs: computeGlobalScore(r.scores, profile)
      });
    });
    scored.sort(function (a, b) {
      return b._gs - a._gs;
    });
    var top3 = scored.slice(0, 3);
    var prioLabels = (profile.priorityTags || []).map(function (id) {
      var _PRIORITY_TAGS$find5;
      return (_PRIORITY_TAGS$find5 = PRIORITY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _PRIORITY_TAGS$find5 === void 0 ? void 0 : _PRIORITY_TAGS$find5.label;
    }).filter(Boolean);
    var makeVerdict = function makeVerdict(r, rank) {
      var sc = r.scores || {};
      var best2 = ATTRS.filter(function (a) {
        return sc[a];
      }).sort(function (a, b) {
        return (sc[b] || 0) - (sc[a] || 0);
      }).slice(0, 2);
      var pct = (r._gs * 10).toFixed(1);
      if (rank === 0) return "Ta meilleure option \xE0 ".concat(pct, "%. ").concat(best2.length >= 2 ? "Elle excelle en ".concat(best2[0], " (").concat(sc[best2[0]], ") et ").concat(best2[1], " (").concat(sc[best2[1]], ").") : "").concat(prioLabels.length ? " Pile sur tes priorit\xE9s ".concat(prioLabels.join(" et "), ".") : "");
      if (rank === 1) return "Alternative solide \xE0 ".concat(pct, "%. ").concat(best2.length >= 2 ? "Point fort : ".concat(best2[0], " \xE0 ").concat(sc[best2[0]], ".") : "", " Un profil compl\xE9mentaire au n\xB01.");
      return "3\u1D49 option \xE0 ".concat(pct, "%. ").concat(best2.length >= 2 ? "Int\xE9ressante pour son ".concat(best2[0], " (").concat(sc[best2[0]], ").") : "", " \xC0 tester si les deux premi\xE8res ne conviennent pas.");
    };
    var medals = ["🥇", "🥈", "🥉"];
    var rankLabels = ["MEILLEUR CHOIX", "2ᵉ CHOIX", "3ᵉ CHOIX"];
    var rankColors = ["#f97316", "#94a3b8", "#cd7f32"];
    var r = top3[revealIdx];
    if (!r) return null;
    var sc = r.scores || {};
    var fyConfig = computeForYou(sc, profile);
    var badge = fyConfig === "recommended" ? {
      text: "RECOMMANDÉ",
      color: "#4CAF50"
    } : fyConfig === "partial" ? {
      text: "JOUABLE",
      color: "#FF9800"
    } : {
      text: "—",
      color: "#64748b"
    };
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        inset: 0,
        background: "#0b0f1a",
        zIndex: 1000,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        padding: "20px",
        overflow: "hidden"
      }
    }, /*#__PURE__*/external_React_default().createElement("style", null, "\n            @keyframes revealCardIn { from { opacity:0; transform:scale(0.9) translateY(20px); } to { opacity:1; transform:scale(1) translateY(0); } }\n            @keyframes revealPulse { 0%,100% { box-shadow: 0 0 30px rgba(249,115,22,0.15); } 50% { box-shadow: 0 0 50px rgba(249,115,22,0.35); } }\n          "), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setScreen("dashboard");
      },
      style: {
        position: "absolute",
        top: 16,
        right: 16,
        width: 40,
        height: 40,
        borderRadius: "50%",
        background: "rgba(255,255,255,0.06)",
        border: "1px solid rgba(255,255,255,0.1)",
        color: "#94a3b8",
        fontSize: 20,
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        zIndex: 10
      }
    }, "\u2715"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        marginBottom: 20
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 16,
        color: "#f97316",
        fontWeight: 800,
        fontFamily: "'Outfit'",
        letterSpacing: "0.04em",
        textTransform: "uppercase"
      }
    }, "\uD83C\uDFC6 Top 3 de ", profileName)), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 16,
        maxWidth: "100vw"
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setRevealIdx(function (i) {
          return Math.max(0, i - 1);
        });
      },
      disabled: revealIdx === 0,
      style: {
        width: 44,
        height: 44,
        borderRadius: "50%",
        border: "1px solid rgba(255,255,255,0.12)",
        background: revealIdx > 0 ? "rgba(249,115,22,0.1)" : "rgba(255,255,255,0.03)",
        color: revealIdx > 0 ? "#f97316" : "#334155",
        fontSize: 22,
        cursor: revealIdx > 0 ? "pointer" : "default",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexShrink: 0,
        transition: "all 0.2s"
      }
    }, "\u2039"), /*#__PURE__*/external_React_default().createElement("div", {
      key: revealIdx,
      style: {
        width: 340,
        maxWidth: "75vw",
        background: "rgba(255,255,255,0.04)",
        border: "2px solid ".concat(revealIdx === 0 ? "rgba(249,115,22,0.5)" : "rgba(255,255,255,0.1)"),
        borderRadius: 24,
        padding: "28px 24px",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: 14,
        animation: "revealCardIn 0.5s ease both".concat(revealIdx === 0 ? ", revealPulse 3s ease infinite" : "")
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 44
      }
    }, medals[revealIdx]), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        fontWeight: 700,
        color: rankColors[revealIdx],
        textTransform: "uppercase",
        letterSpacing: "0.08em"
      }
    }, rankLabels[revealIdx]), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
      src: r.imageUrl,
      alt: r.name,
      style: {
        width: 110,
        height: 110,
        objectFit: "contain",
        filter: "drop-shadow(0 4px 16px rgba(0,0,0,0.4))"
      },
      onError: function onError(e) {
        e.target.style.display = "none";
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 19,
        fontWeight: 800,
        color: "#f1f5f9",
        fontFamily: "'Outfit'",
        lineHeight: 1.3
      }
    }, r.name), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        color: "#64748b",
        marginTop: 4
      }
    }, r.brand, " \xB7 ", r.shape, r.weight ? " \xB7 ".concat(r.weight) : "")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 48,
        fontWeight: 900,
        fontFamily: "'Outfit'",
        color: revealIdx === 0 ? "#f97316" : "#e2e8f0",
        lineHeight: 1
      }
    }, (r._gs * 10).toFixed(1), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 20
      }
    }, "%")), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: badge.color,
        background: "".concat(badge.color, "18"),
        border: "1px solid ".concat(badge.color, "40"),
        borderRadius: 8,
        padding: "4px 14px",
        textTransform: "uppercase"
      }
    }, badge.text), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: 6,
        width: "100%"
      }
    }, ATTRS.map(function (a) {
      var val = sc[a] || 0;
      var isPrio = (profile.priorityTags || []).some(function (pid) {
        var m = {
          puissance: 'Puissance',
          controle: 'Contrôle',
          confort: 'Confort',
          spin: 'Spin',
          legerete: 'Maniabilité',
          protection: 'Confort'
        };
        return m[pid] === a;
      });
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: a,
        style: {
          display: "flex",
          justifyContent: "space-between",
          alignItems: "center",
          padding: "5px 10px",
          borderRadius: 8,
          background: isPrio ? "rgba(249,115,22,0.08)" : "rgba(255,255,255,0.02)"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          color: isPrio ? "#f97316" : "#94a3b8",
          fontWeight: isPrio ? 700 : 500
        }
      }, isPrio ? "★ " : "", a), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 13,
          fontWeight: 700,
          color: val >= 8 ? "#4CAF50" : val >= 6 ? "#e2e8f0" : "#f97316"
        }
      }, val));
    })), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#94a3b8",
        lineHeight: 1.6,
        textAlign: "center",
        margin: 0
      }
    }, makeVerdict(r, revealIdx)), r.price && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#475569"
      }
    }, "\uD83D\uDCB0 ", r.price)), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setRevealIdx(function (i) {
          return Math.min(top3.length - 1, i + 1);
        });
      },
      disabled: revealIdx >= top3.length - 1,
      style: {
        width: 44,
        height: 44,
        borderRadius: "50%",
        border: "1px solid rgba(255,255,255,0.12)",
        background: revealIdx < top3.length - 1 ? "rgba(249,115,22,0.1)" : "rgba(255,255,255,0.03)",
        color: revealIdx < top3.length - 1 ? "#f97316" : "#334155",
        fontSize: 22,
        cursor: revealIdx < top3.length - 1 ? "pointer" : "default",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexShrink: 0,
        transition: "all 0.2s"
      }
    }, "\u203A")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 8,
        marginTop: 16
      }
    }, top3.map(function (_, i) {
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: i,
        onClick: function onClick() {
          return setRevealIdx(i);
        },
        style: {
          width: i === revealIdx ? 20 : 8,
          height: 8,
          borderRadius: 4,
          border: "none",
          cursor: "pointer",
          padding: 0,
          background: i === revealIdx ? "#f97316" : "rgba(255,255,255,0.15)",
          transition: "all 0.3s"
        }
      });
    })), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setScreen("dashboard");
      },
      style: {
        marginTop: 20,
        padding: "12px 32px",
        borderRadius: 12,
        fontSize: 13,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "rgba(255,255,255,0.05)",
        border: "1px solid rgba(255,255,255,0.12)",
        color: "#94a3b8",
        transition: "all 0.3s"
      }
    }, "Voir l'analyse compl\xE8te \u2192"));
  }(), screen === "dashboard" && function () {
    // Compute Top 3 for this profile
    var age = Number(profile.age) || 0;
    var ht = Number(profile.height) || 0;
    var isJunior = age > 0 && age < 15 || ht > 0 && ht < 150;
    var pool = isJunior ? rackets_db_namespaceObject.filter(function (r) {
      return r.category === 'junior';
    }) : function () {
      var lvl = profile.level || 'Débutant';
      var catMap = {
        'Débutant': ['debutant', 'intermediaire'],
        'Intermédiaire': ['intermediaire', 'debutant', 'avance'],
        'Avancé': ['avance', 'intermediaire', 'expert'],
        'Expert': ['expert', 'avance']
      };
      return rackets_db_namespaceObject.filter(function (r) {
        return (catMap[lvl] || ['debutant', 'intermediaire']).includes(r.category);
      });
    }();
    // Brand preferences — used for display info only, NOT for filtering Top 3
    var brandPref = (profile.brandTags || []).map(function (id) {
      var _BRAND_TAGS$find5;
      return (_BRAND_TAGS$find5 = BRAND_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _BRAND_TAGS$find5 === void 0 ? void 0 : _BRAND_TAGS$find5.label;
    }).filter(Boolean);
    // Score ALL compatible rackets — unbiased Top 3
    var scored = pool.map(function (r) {
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _gs: computeGlobalScore(r.scores, profile),
        _fy: computeForYou(r.scores, profile)
      });
    });
    scored.sort(function (a, b) {
      return b._gs - a._gs;
    });
    var top3 = scored.slice(0, 3);

    // Compute player's ideal weights for radar
    var w = {
      Puissance: 1,
      Contrôle: 1,
      Confort: 1,
      Spin: 1,
      Maniabilité: 1,
      Tolérance: 1
    };
    var prioMap = {
      confort: {
        Confort: 1.5
      },
      polyvalence: {
        Contrôle: 0.5,
        Maniabilité: 0.5,
        Tolérance: 0.5
      },
      puissance: {
        Puissance: 1.5
      },
      controle: {
        Contrôle: 1.5
      },
      spin: {
        Spin: 1.5
      },
      legerete: {
        Maniabilité: 1.5
      },
      protection: {
        Confort: 1.5
      },
      reprise: {
        Confort: 1.5,
        Tolérance: 1.0,
        Maniabilité: 0.5
      }
    };
    var _iterator4 = _createForOfIteratorHelper(profile.priorityTags || []),
      _step4;
    try {
      for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
        var tag = _step4.value;
        var boosts = prioMap[tag];
        if (boosts) for (var _i3 = 0, _Object$entries3 = Object.entries(boosts); _i3 < _Object$entries3.length; _i3++) {
          var _Object$entries3$_i = _slicedToArray(_Object$entries3[_i3], 2),
            k = _Object$entries3$_i[0],
            v = _Object$entries3$_i[1];
          w[k] = (w[k] || 1) + v;
        }
      }
    } catch (err) {
      _iterator4.e(err);
    } finally {
      _iterator4.f();
    }
    var styleMap = {
      offensif: {
        Puissance: 0.5
      },
      defensif: {
        Contrôle: 0.5,
        Tolérance: 0.5
      },
      tactique: {
        Contrôle: 0.5,
        Maniabilité: 0.3
      },
      puissant: {
        Puissance: 0.5,
        Spin: 0.3
      },
      veloce: {
        Maniabilité: 0.8
      },
      endurant: {
        Confort: 0.5,
        Tolérance: 0.3
      },
      contre: {
        Tolérance: 0.5,
        Contrôle: 0.3
      },
      polyvalent: {
        Contrôle: 0.3,
        Tolérance: 0.3
      },
      technique: {
        Contrôle: 0.5,
        Spin: 0.3
      }
    };
    var _iterator5 = _createForOfIteratorHelper(profile.styleTags || []),
      _step5;
    try {
      for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
        var _tag2 = _step5.value;
        var _boosts2 = styleMap[_tag2];
        if (_boosts2) for (var _i4 = 0, _Object$entries4 = Object.entries(_boosts2); _i4 < _Object$entries4.length; _i4++) {
          var _Object$entries4$_i = _slicedToArray(_Object$entries4[_i4], 2),
            _k2 = _Object$entries4$_i[0],
            _v2 = _Object$entries4$_i[1];
          w[_k2] = (w[_k2] || 1) + _v2;
        }
      }
    } catch (err) {
      _iterator5.e(err);
    } finally {
      _iterator5.f();
    }
    var maxW = Math.max.apply(Math, _toConsumableArray(Object.values(w)));
    var idealRadar = ATTRS.map(function (a) {
      return {
        attribute: a,
        Idéal: Math.round(w[a] / maxW * 10 * 10) / 10
      };
    });

    // Profile summary pieces
    var levelColors = {
      Débutant: "#4CAF50",
      Intermédiaire: "#FF9800",
      Avancé: "#ef4444",
      Compétition: "#9C27B0",
      Expert: "#9C27B0"
    };
    var styles = (profile.styleTags || []).map(function (id) {
      var _STYLE_TAGS$find5;
      return (_STYLE_TAGS$find5 = STYLE_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _STYLE_TAGS$find5 === void 0 ? void 0 : _STYLE_TAGS$find5.label;
    }).filter(Boolean);
    var injuries = (profile.injuryTags || []).filter(function (t) {
      return t !== "aucune";
    }).map(function (id) {
      var _INJURY_TAGS$find5;
      return (_INJURY_TAGS$find5 = INJURY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _INJURY_TAGS$find5 === void 0 ? void 0 : _INJURY_TAGS$find5.label;
    }).filter(Boolean);
    var priorities = (profile.priorityTags || []).map(function (id) {
      var _PRIORITY_TAGS$find6;
      return (_PRIORITY_TAGS$find6 = PRIORITY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _PRIORITY_TAGS$find6 === void 0 ? void 0 : _PRIORITY_TAGS$find6.label;
    }).filter(Boolean);
    var hand = profile.hand || "Droitier";
    var side = profile.side || "Droite";
    var isAttacker = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
    var role = side === "Les deux" ? "Polyvalent" : isAttacker ? "Attaquant" : "Constructeur";
    var hasSession = rackets.length > 0;
    var fyConfig2 = {
      recommended: {
        text: "RECOMMANDÉ",
        bg: "#1B5E20",
        border: "#4CAF50",
        color: "#4CAF50"
      },
      partial: {
        text: "JOUABLE",
        bg: "#E65100",
        border: "#FF9800",
        color: "#FF9800"
      },
      no: {
        text: "DÉCONSEILLÉ",
        bg: "#B71C1C",
        border: "#E53935",
        color: "#E53935"
      }
    };
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        maxWidth: 1020,
        margin: "0 auto",
        padding: "0 24px",
        animation: "fadeIn 0.5s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        gap: 10,
        marginBottom: 18
      }
    }, /*#__PURE__*/external_React_default().createElement("svg", {
      width: "32",
      height: "32",
      viewBox: "0 0 44 44",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        flexShrink: 0,
        filter: "drop-shadow(0 4px 12px rgba(249,115,22,0.3))"
      }
    }, /*#__PURE__*/external_React_default().createElement("defs", null, /*#__PURE__*/external_React_default().createElement("linearGradient", {
      id: "logoGradDash",
      x1: "0",
      y1: "0",
      x2: "44",
      y2: "44"
    }, /*#__PURE__*/external_React_default().createElement("stop", {
      offset: "0%",
      stopColor: "#f97316"
    }), /*#__PURE__*/external_React_default().createElement("stop", {
      offset: "100%",
      stopColor: "#ef4444"
    }))), /*#__PURE__*/external_React_default().createElement("rect", {
      width: "44",
      height: "44",
      rx: "10",
      fill: "url(#logoGradDash)"
    }), /*#__PURE__*/external_React_default().createElement("ellipse", {
      cx: "22",
      cy: "18",
      rx: "10",
      ry: "12",
      stroke: "#fff",
      strokeWidth: "2.2",
      fill: "none"
    }), /*#__PURE__*/external_React_default().createElement("line", {
      x1: "22",
      y1: "30",
      x2: "22",
      y2: "38",
      stroke: "#fff",
      strokeWidth: "2.5",
      strokeLinecap: "round"
    }), /*#__PURE__*/external_React_default().createElement("circle", {
      cx: "33",
      cy: "32",
      r: "3.5",
      fill: "#fff",
      opacity: "0.85"
    })), /*#__PURE__*/external_React_default().createElement("h1", {
      style: {
        fontFamily: "'Outfit'",
        fontSize: 24,
        fontWeight: 800,
        background: "linear-gradient(135deg,#f97316,#ef4444,#ec4899)",
        WebkitBackgroundClip: "text",
        WebkitTextFillColor: "transparent",
        margin: 0
      }
    }, "PADEL ANALYZER")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 18,
        padding: "16px 22px",
        marginBottom: 20,
        animation: "fadeIn 0.3s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 14
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 48,
        height: 48,
        borderRadius: 14,
        background: "linear-gradient(135deg,rgba(249,115,22,0.3),rgba(239,68,68,0.2))",
        border: "2px solid rgba(249,115,22,0.4)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 22,
        fontWeight: 700,
        color: "#f97316",
        flexShrink: 0
      }
    }, profileName.charAt(0).toUpperCase()), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 10,
        flexWrap: "wrap"
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 18,
        fontWeight: 700,
        color: "#f1f5f9"
      }
    }, profileName), profile.level && /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 10,
        fontWeight: 600,
        color: levelColors[profile.level] || "#64748b",
        background: "".concat(levelColors[profile.level] || "#64748b", "18"),
        padding: "3px 10px",
        borderRadius: 8,
        textTransform: "uppercase"
      }
    }, profile.level, isJunior ? " · Junior" : ""), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 11,
        color: "#94a3b8"
      }
    }, hand, " \xB7 C\xF4t\xE9 ", side), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 11,
        color: "#a5b4fc",
        fontWeight: 600
      }
    }, "\uD83C\uDFAF ", role)), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 5,
        flexWrap: "wrap",
        marginTop: 6
      }
    }, styles.map(function (s) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: s,
        style: {
          fontSize: 10,
          background: "rgba(99,102,241,0.1)",
          border: "1px solid rgba(99,102,241,0.2)",
          borderRadius: 8,
          padding: "2px 8px",
          color: "#a5b4fc"
        }
      }, s);
    }), priorities.map(function (p) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: p,
        style: {
          fontSize: 10,
          background: "rgba(249,115,22,0.1)",
          border: "1px solid rgba(249,115,22,0.2)",
          borderRadius: 8,
          padding: "2px 8px",
          color: "#f97316"
        }
      }, p);
    }), injuries.map(function (inj) {
      return /*#__PURE__*/external_React_default().createElement("span", {
        key: inj,
        style: {
          fontSize: 10,
          background: "rgba(239,68,68,0.1)",
          border: "1px solid rgba(239,68,68,0.2)",
          borderRadius: 8,
          padding: "2px 8px",
          color: "#ef4444"
        }
      }, "\uD83E\uDE79 ", inj);
    }))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 6,
        flexShrink: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setScreen("home");
      },
      style: {
        background: "none",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 8,
        padding: "5px 8px",
        color: "#64748b",
        fontSize: 10,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s"
      },
      title: "Changer de profil"
    }, "\uD83D\uDC65"), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: disconnect,
      style: {
        background: "none",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 8,
        padding: "5px 8px",
        color: "#64748b",
        fontSize: 10,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s"
      },
      title: "D\xE9connexion"
    }, "\u23FB")))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 20,
        marginBottom: 20,
        alignItems: "stretch"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: "0 0 400px",
        display: "flex",
        flexDirection: "column",
        gap: 12
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 18,
        padding: "14px 12px",
        flex: 1,
        animation: "fadeIn 0.4s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        fontWeight: 700,
        color: "#94a3b8",
        letterSpacing: "0.06em",
        textTransform: "uppercase",
        textAlign: "center",
        marginBottom: 4,
        marginTop: 0
      }
    }, "\uD83D\uDCCA Raquette id\xE9ale pour ", profileName), /*#__PURE__*/external_React_default().createElement(ResponsiveContainer, {
      width: "100%",
      height: 195
    }, /*#__PURE__*/external_React_default().createElement(RadarChart, {
      data: idealRadar,
      margin: {
        top: 10,
        right: 34,
        bottom: 8,
        left: 34
      }
    }, /*#__PURE__*/external_React_default().createElement(PolarGrid, {
      stroke: "rgba(255,255,255,0.08)"
    }), /*#__PURE__*/external_React_default().createElement(PolarAngleAxis, {
      dataKey: "attribute",
      tick: {
        fill: "#94a3b8",
        fontSize: 10
      }
    }), /*#__PURE__*/external_React_default().createElement(PolarRadiusAxis, {
      angle: 90,
      domain: [0, 10],
      tick: false,
      axisLine: false
    }), /*#__PURE__*/external_React_default().createElement(Radar, {
      name: "Id\xE9al",
      dataKey: "Id\xE9al",
      stroke: "#f97316",
      fill: "#f97316",
      fillOpacity: 0.15,
      strokeWidth: 2
    })))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 10
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 14,
        padding: "14px 10px",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 28,
        fontWeight: 800,
        color: "#f97316",
        fontFamily: "'Outfit'"
      }
    }, pool.length), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 600,
        textTransform: "uppercase",
        letterSpacing: "0.04em"
      }
    }, "Compatibles")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 14,
        padding: "14px 10px",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 28,
        fontWeight: 800,
        color: "#4CAF50",
        fontFamily: "'Outfit'"
      }
    }, scored.filter(function (r) {
      return r._fy === "recommended";
    }).length), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 600,
        textTransform: "uppercase",
        letterSpacing: "0.04em"
      }
    }, "Recommand\xE9es")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 14,
        padding: "14px 10px",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 20,
        fontWeight: 800,
        color: "#a5b4fc",
        fontFamily: "'Outfit'"
      }
    }, top3.length > 0 ? top3[0]._gs.toFixed(1) : "—", /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 11,
        color: "#64748b"
      }
    }, "/10")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 600,
        textTransform: "uppercase",
        letterSpacing: "0.04em"
      }
    }, "Meilleur score")))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 18,
        padding: "18px 20px",
        animation: "fadeIn 0.5s ease",
        display: "flex",
        flexDirection: "column"
      }
    }, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        fontWeight: 700,
        color: "#94a3b8",
        letterSpacing: "0.06em",
        textTransform: "uppercase",
        textAlign: "center",
        marginTop: 0,
        marginBottom: 4
      }
    }, "\uD83C\uDFC6 Top 3 absolu pour ", profileName), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#475569",
        textAlign: "center",
        margin: "0 0 14px",
        lineHeight: 1.4
      }
    }, "Sur ", scored.length, " raquettes compatibles", brandPref.length > 0 ? /*#__PURE__*/external_React_default().createElement("span", null, " \xB7 Toutes marques confondues") : ""), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 10,
        flex: 1
      }
    }, top3.map(function (r, i) {
      var fy = fyConfig2[r._fy] || fyConfig2.partial;
      var medals = ["🥇", "🥈", "🥉"];
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: r.id,
        style: {
          display: "flex",
          alignItems: "center",
          gap: 12,
          padding: "12px 14px",
          background: i === 0 ? "rgba(249,115,22,0.06)" : "rgba(255,255,255,0.02)",
          border: "1px solid ".concat(i === 0 ? "rgba(249,115,22,0.2)" : "rgba(255,255,255,0.06)"),
          borderRadius: 14,
          transition: "all 0.2s"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 22,
          flexShrink: 0,
          width: 28,
          textAlign: "center"
        }
      }, medals[i]), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
        src: proxyImg(r.imageUrl),
        alt: "",
        style: {
          width: 48,
          height: 48,
          objectFit: "contain",
          borderRadius: 8,
          background: "rgba(255,255,255,0.06)",
          flexShrink: 0
        },
        onError: function onError(e) {
          e.target.style.display = 'none';
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          flex: 1,
          minWidth: 0
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 14,
          fontWeight: 700,
          color: "#f1f5f9",
          whiteSpace: "nowrap",
          overflow: "hidden",
          textOverflow: "ellipsis"
        }
      }, r.name), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 11,
          color: "#94a3b8",
          marginTop: 2
        }
      }, r.brand, " \xB7 ", r.shape, " \xB7 ", r.weight), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          alignItems: "center",
          gap: 6,
          marginTop: 3
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 9,
          fontWeight: 600,
          color: fy.color,
          background: "".concat(fy.bg, "30"),
          border: "1px solid ".concat(fy.border, "40"),
          borderRadius: 6,
          padding: "2px 7px",
          textTransform: "uppercase"
        }
      }, fy.text), r.price && r.price !== "—" && /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          color: "#64748b"
        }
      }, r.price))), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          gap: 2,
          flexShrink: 0
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 22,
          fontWeight: 800,
          color: i === 0 ? "#f97316" : "#cbd5e1",
          fontFamily: "'Outfit'"
        }
      }, r._gs.toFixed(1)), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 8,
          color: "#64748b",
          textTransform: "uppercase"
        }
      }, "Score")));
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        marginTop: 12,
        padding: "10px 14px",
        background: "rgba(99,102,241,0.04)",
        border: "1px solid rgba(99,102,241,0.1)",
        borderRadius: 12
      }
    }, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#a5b4fc",
        margin: "0 0 3px",
        fontWeight: 600
      }
    }, "\uD83D\uDCA1 Comment lire ce classement ?"), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#64748b",
        margin: 0,
        lineHeight: 1.6
      }
    }, "Calcul\xE9 sur ", rackets_db_namespaceObject.length, " raquettes en croisant profil, style et priorit\xE9s. Lance l'analyse d\xE9taill\xE9e pour les scores par crit\xE8re, radars comparatifs et l'Ar\xE8ne.")))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        gap: 12,
        marginBottom: 14,
        animation: "fadeIn 0.6s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return launchAnalysis(top3);
      },
      style: {
        flex: "1 1 220px",
        padding: "14px",
        background: "linear-gradient(135deg,rgba(249,115,22,0.2),rgba(239,68,68,0.15))",
        border: "1px solid rgba(249,115,22,0.35)",
        borderRadius: 14,
        color: "#f97316",
        fontSize: 14,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        letterSpacing: "-0.01em",
        textAlign: "center"
      }
    }, "\uD83D\uDCCA Analyser ce Top 3 en d\xE9tail"), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setPanel("suggest");
        goToApp();
      },
      style: {
        flex: "1 1 180px",
        padding: "14px 16px",
        background: "rgba(76,175,80,0.08)",
        border: "1px solid rgba(76,175,80,0.25)",
        borderRadius: 14,
        color: "#4CAF50",
        fontSize: 13,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", null, "\uD83C\uDFAF Sugg\xE8re-moi d'autres"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 400,
        marginTop: 3
      }
    }, brandPref.length > 0 ? "Priorit\xE9 ".concat(brandPref.join(", ")) : "Recommandations IA")), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        goToApp();
      },
      style: {
        flex: "1 1 180px",
        padding: "14px 16px",
        background: "rgba(99,102,241,0.08)",
        border: "1px solid rgba(99,102,241,0.25)",
        borderRadius: 14,
        color: "#a5b4fc",
        fontSize: 13,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", null, hasSession ? "📊 Reprendre l'analyse" : "📊 Explorer la base"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 400,
        marginTop: 3
      }
    }, hasSession ? "Session en cours" : "Comparer, radars, PDF")), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        setWizardStep(0);
        setPanel("profile");
        goToApp();
      },
      style: {
        flex: "0 1 150px",
        padding: "14px 16px",
        background: "rgba(255,255,255,0.03)",
        border: "1px solid rgba(255,255,255,0.1)",
        borderRadius: 14,
        color: "#94a3b8",
        fontSize: 13,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        transition: "all 0.2s",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", null, "\u270F\uFE0F Modifier profil"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#64748b",
        fontWeight: 400,
        marginTop: 3
      }
    }, "Affiner les r\xE9sultats"))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 7,
        color: "#334155",
        letterSpacing: "0.05em",
        textAlign: "center",
        marginTop: 8
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontFamily: "'Outfit'",
        fontWeight: 600
      }
    }, "PADEL ANALYZER"), " V8.7 \xB7 ", rackets_db_namespaceObject.length, " raquettes \xB7 Scoring hybride calibr\xE9"));
  }(), screen === "app" && /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      textAlign: "center",
      marginBottom: 28,
      paddingBottom: 20,
      borderBottom: "1px solid rgba(255,255,255,0.06)"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 10,
      marginBottom: 6
    }
  }, /*#__PURE__*/external_React_default().createElement("svg", {
    width: "32",
    height: "32",
    viewBox: "0 0 44 44",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    style: {
      flexShrink: 0,
      filter: "drop-shadow(0 4px 12px rgba(249,115,22,0.3))"
    }
  }, /*#__PURE__*/external_React_default().createElement("defs", null, /*#__PURE__*/external_React_default().createElement("linearGradient", {
    id: "logoGrad",
    x1: "0",
    y1: "0",
    x2: "44",
    y2: "44"
  }, /*#__PURE__*/external_React_default().createElement("stop", {
    offset: "0%",
    stopColor: "#f97316"
  }), /*#__PURE__*/external_React_default().createElement("stop", {
    offset: "100%",
    stopColor: "#ef4444"
  }))), /*#__PURE__*/external_React_default().createElement("rect", {
    width: "44",
    height: "44",
    rx: "10",
    fill: "url(#logoGrad)"
  }), /*#__PURE__*/external_React_default().createElement("ellipse", {
    cx: "22",
    cy: "18",
    rx: "10",
    ry: "12",
    stroke: "#fff",
    strokeWidth: "2.2",
    fill: "none"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "10",
    x2: "22",
    y2: "26",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.4"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "14",
    y1: "18",
    x2: "30",
    y2: "18",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.4"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "30",
    x2: "22",
    y2: "38",
    stroke: "#fff",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }), /*#__PURE__*/external_React_default().createElement("circle", {
    cx: "33",
    cy: "32",
    r: "3.5",
    fill: "#fff",
    opacity: "0.85"
  })), /*#__PURE__*/external_React_default().createElement("h1", {
    style: {
      fontFamily: "'Outfit'",
      fontSize: 22,
      fontWeight: 800,
      background: "linear-gradient(135deg,#f97316,#ef4444,#ec4899)",
      WebkitBackgroundClip: "text",
      WebkitTextFillColor: "transparent",
      margin: 0,
      letterSpacing: "-0.02em"
    }
  }, "PADEL ANALYZER")), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      color: "#475569",
      fontSize: 10,
      margin: 0,
      letterSpacing: "0.08em",
      textTransform: "uppercase",
      fontWeight: 500
    }
  }, "Recherche web \xB7 Notation calibr\xE9e \xB7 Profil personnalisable"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 8,
      color: "#334155",
      marginTop: 4,
      fontFamily: "'Outfit'",
      fontWeight: 500,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 8
    }
  }, /*#__PURE__*/external_React_default().createElement("span", null, "V8.7"), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      background: "rgba(249,115,22,0.1)",
      border: "1px solid rgba(249,115,22,0.2)",
      borderRadius: 10,
      padding: "1px 7px",
      color: "#f97316",
      fontSize: 8,
      fontWeight: 600
    }
  }, "\uD83D\uDDC3\uFE0F ", rackets_db_namespaceObject.length, localDBCount > 0 && /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      color: "#22c55e"
    }
  }, " + ", localDBCount))), profileName && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 8,
      marginTop: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 6,
      background: "rgba(99,102,241,0.08)",
      border: "1px solid rgba(99,102,241,0.2)",
      borderRadius: 20,
      padding: "4px 12px 4px 6px"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      width: 22,
      height: 22,
      borderRadius: 7,
      background: "linear-gradient(135deg,rgba(249,115,22,0.25),rgba(239,68,68,0.2))",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontSize: 10,
      fontWeight: 700,
      color: "#f97316",
      flexShrink: 0
    }
  }, profileName.charAt(0).toUpperCase()), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      color: "#a5b4fc"
    }
  }, profileName)), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: goToDashboard,
    style: {
      background: "rgba(249,115,22,0.08)",
      border: "1px solid rgba(249,115,22,0.2)",
      borderRadius: 20,
      padding: "4px 10px",
      color: "#f97316",
      fontSize: 10,
      fontWeight: 600,
      cursor: "pointer",
      fontFamily: "inherit",
      transition: "all 0.2s",
      display: "flex",
      alignItems: "center",
      gap: 4
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 11
    }
  }, "\uD83C\uDFE0"), " Dashboard"), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: disconnect,
    style: {
      background: "rgba(255,255,255,0.04)",
      border: "1px solid rgba(255,255,255,0.08)",
      borderRadius: 20,
      padding: "4px 10px",
      color: "#64748b",
      fontSize: 10,
      fontWeight: 600,
      cursor: "pointer",
      fontFamily: "inherit",
      transition: "all 0.2s",
      display: "flex",
      alignItems: "center",
      gap: 4
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 12
    }
  }, "\u23FB"), " D\xE9connexion"))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      marginBottom: 18,
      flexWrap: "wrap"
    }
  }, [["suggest", "🎯 Suggère-moi"], ["add", "+ Ajouter"], ["profile", "👤 Profil"], ["manage", "🗑 Gérer"]].map(function (_ref14) {
    var _ref15 = _slicedToArray(_ref14, 2),
      k = _ref15[0],
      l = _ref15[1];
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: k,
      onClick: function onClick() {
        if (k === "profile") setWizardStep(0);
        setPanel(function (p) {
          return p === k ? null : k;
        });
      },
      style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(panel === k)), {}, {
        borderRadius: 20
      })
    }, l);
  })), panel === "suggest" && /*#__PURE__*/external_React_default().createElement("div", {
    style: S.card
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: S.title
  }, "\uD83C\uDFAF RAQUETTES SUGG\xC9R\xC9ES POUR TON PROFIL"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      background: "rgba(249,115,22,0.06)",
      border: "1px solid rgba(249,115,22,0.15)",
      borderRadius: 10,
      padding: 10,
      marginBottom: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 10,
      color: "#f97316",
      fontWeight: 700,
      margin: "0 0 4px"
    }
  }, "Ton profil :"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 9,
      color: "#94a3b8",
      margin: 0,
      lineHeight: 1.5
    }
  }, profileText)), !suggestResults && !loading && /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 10,
      color: "#64748b",
      margin: "0 0 10px",
      lineHeight: 1.4
    }
  }, "Recherche des raquettes les plus adapt\xE9es \xE0 ton profil : ", /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      color: "#f97316",
      fontWeight: 600
    }
  }, "\u2B50 Coups de c\u0153ur"), " (meilleures correspondances) et ", /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      color: "#fbbf24",
      fontWeight: 600
    }
  }, "\u26A1 Alternatives Priorit\xE9"), " (orient\xE9es ", profile.priorityTags.map(function (id) {
    var _PRIORITY_TAGS$find7;
    return (_PRIORITY_TAGS$find7 = PRIORITY_TAGS.find(function (t) {
      return t.id === id;
    })) === null || _PRIORITY_TAGS$find7 === void 0 ? void 0 : _PRIORITY_TAGS$find7.label;
  }).filter(Boolean).join(', ') || "tes priorités", "). Coche celles qui t'int\xE9ressent puis valide en un clic."), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: suggestRackets,
    style: S.btnGreen
  }, "\uD83D\uDD0D Lancer la recherche")), loadMsg && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#f97316",
      marginTop: 10,
      display: "flex",
      alignItems: "center",
      gap: 6
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      display: "inline-block",
      animation: "pulse 1.5s ease-in-out infinite"
    }
  }, "\u23F3"), /*#__PURE__*/external_React_default().createElement("span", null, loadMsg), /*#__PURE__*/external_React_default().createElement("style", null, "@keyframes pulse { 0%,100% { opacity:1; } 50% { opacity:0.4; } }")), error && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#ef4444",
      marginTop: 8,
      background: "rgba(239,68,68,0.08)",
      border: "1px solid rgba(239,68,68,0.2)",
      borderRadius: 8,
      padding: "8px 10px",
      lineHeight: 1.4
    }
  }, error), batchProgress && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 12,
      color: "#4CAF50",
      marginTop: 8,
      fontWeight: 600
    }
  }, batchProgress), suggestResults && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginTop: 6
    }
  }, function () {
    var hearts = suggestResults.filter(function (s) {
      return s.category === "heart";
    });
    var prios = suggestResults.filter(function (s) {
      return s.category === "priority";
    });
    // Fallback if API didn't use categories
    var hasCategories = hearts.length > 0 || prios.length > 0;
    var topPicks = hasCategories ? hearts : suggestResults.slice(0, 4);
    var prioAlts = hasCategories ? prios : [];
    var others = hasCategories ? suggestResults.filter(function (s) {
      return s.category !== "heart" && s.category !== "priority";
    }) : suggestResults.slice(4);
    var prioLabels = profile.priorityTags.map(function (id) {
      var _PRIORITY_TAGS$find8;
      return (_PRIORITY_TAGS$find8 = PRIORITY_TAGS.find(function (t) {
        return t.id === id;
      })) === null || _PRIORITY_TAGS$find8 === void 0 ? void 0 : _PRIORITY_TAGS$find8.label;
    }).filter(Boolean);
    return /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, topPicks.length > 0 && /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#f97316",
        fontWeight: 700,
        marginBottom: 6
      }
    }, "\u2B50 Coups de c\u0153ur \u2014 meilleures correspondances :"), topPicks.map(function (s) {
      var ri = suggestResults.indexOf(s);
      return renderSuggestCard(s, ri, suggestChecked.has(ri), true);
    })), prioAlts.length > 0 && /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#fbbf24",
        fontWeight: 700,
        marginBottom: 4,
        marginTop: 14
      }
    }, "\u26A1 Alternatives Priorit\xE9 \u2014 ", prioLabels.join(', '), " :"), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 9,
        color: "#64748b",
        marginBottom: 6
      }
    }, "Raquettes orient\xE9es vers tes priorit\xE9s, confort parfois limit\xE9 \u2014 \xE0 tester avant d'acheter."), prioAlts.map(function (s) {
      var ri = suggestResults.indexOf(s);
      return renderSuggestCard(s, ri, suggestChecked.has(ri), false);
    })), others.length > 0 && /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#94a3b8",
        fontWeight: 700,
        marginBottom: 6,
        marginTop: 12
      }
    }, "\uD83D\uDCCB Autres suggestions :"), others.map(function (s) {
      var ri = suggestResults.indexOf(s);
      return renderSuggestCard(s, ri, suggestChecked.has(ri), false);
    })));
  }(), !addingBatch && suggestResults && suggestResults.length > 0 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      marginTop: 12,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      var allIdxs = suggestResults.map(function (_, i) {
        return i;
      }).filter(function (i) {
        return !suggestResults[i]._added;
      });
      var allSelected = allIdxs.every(function (i) {
        return suggestChecked.has(i);
      });
      if (allSelected) setSuggestChecked(new Set());else setSuggestChecked(new Set(allIdxs));
    },
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      padding: "8px 14px",
      fontSize: 11
    })
  }, suggestResults.filter(function (_, i) {
    return !suggestResults[i]._added;
  }).every(function (_, i) {
    return suggestChecked.has(i);
  }) ? "☐ Tout désélectionner" : "☑ Tout sélectionner"), suggestChecked.size > 0 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: addCheckedSuggestions,
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btnGreen), {}, {
      flex: 1,
      padding: "8px 14px"
    })
  }, "\u2705 Ajouter ", suggestChecked.size, " raquette", suggestChecked.size > 1 ? "s" : "", " au comparateur")), addingBatch && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      textAlign: "center",
      padding: "12px 0",
      color: "#f97316",
      fontSize: 12,
      fontWeight: 600
    }
  }, batchProgress), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      setSuggestResults(null);
      setSuggestChecked(new Set());
      setError("");
    },
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      marginTop: 8,
      width: "100%",
      padding: "8px 0",
      fontSize: 11
    })
  }, "\uD83D\uDD04 Relancer une nouvelle recherche"))), panel === "add" && /*#__PURE__*/external_React_default().createElement("div", {
    style: S.card
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: S.title
  }, "\uD83D\uDD0D RECHERCHER UNE RAQUETTE"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 10,
      color: "#64748b",
      margin: "0 0 8px",
      lineHeight: 1.4
    }
  }, "Tape un nom m\xEAme approximatif : \"nox tapia 12k\", \"bullpadel paquito\", \"babolat lebron\"..."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 6
    }
  }, /*#__PURE__*/external_React_default().createElement("input", {
    value: searchQ,
    onChange: function onChange(e) {
      return setSearchQ(e.target.value);
    },
    onKeyDown: function onKeyDown(e) {
      return e.key === "Enter" && !loading && searchRackets();
    },
    placeholder: "Ex: nox tapia 12k, adidas metalbone...",
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.input), {}, {
      flex: 1
    })
  }), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: searchRackets,
    disabled: loading || !searchQ.trim(),
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(true)), {}, {
      opacity: loading ? 0.5 : 1,
      minWidth: 80
    })
  }, loading && !suggestions ? "..." : "Chercher")), loadMsg && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#f97316",
      marginTop: 8
    }
  }, loadMsg), error && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#ef4444",
      marginTop: 8
    }
  }, error), suggestions && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#f97316",
      fontWeight: 700,
      marginBottom: 8
    }
  }, "\uD83D\uDCCB R\xE9sultats \u2014 clique pour voir la fiche technique :"), suggestions.map(function (s, i) {
    var isExpanded = addDetail === i;
    // Try to match with DB
    var nameLower = (s.name || "").toLowerCase();
    var dbMatch = rackets_db_namespaceObject.find(function (r) {
      return r.name.toLowerCase() === nameLower;
    }) || rackets_db_namespaceObject.find(function (r) {
      return nameLower.includes((r.shortName || r.name).toLowerCase().slice(0, 12)) || (r.shortName || r.name).toLowerCase().includes(nameLower.slice(0, 12));
    });
    var sc = (dbMatch === null || dbMatch === void 0 ? void 0 : dbMatch.scores) || {};
    var hasScores = dbMatch && Object.keys(sc).length > 0;
    var gs = hasScores ? computeGlobalScore(sc, profile) : null;
    var fyConfig = hasScores ? computeForYou(sc, profile) : null;
    var badge = fyConfig === "recommended" ? {
      text: "RECOMMANDÉ",
      color: "#4CAF50"
    } : fyConfig === "partial" ? {
      text: "JOUABLE",
      color: "#FF9800"
    } : fyConfig === "no" ? {
      text: "PEU ADAPTÉ",
      color: "#64748b"
    } : null;
    return /*#__PURE__*/external_React_default().createElement("div", {
      key: i,
      style: {
        background: s._selected ? "rgba(249,115,22,0.15)" : isExpanded ? "rgba(255,255,255,0.06)" : "rgba(255,255,255,0.04)",
        border: "1px solid ".concat(s._selected ? "#f97316" : isExpanded ? "rgba(249,115,22,0.2)" : "rgba(255,255,255,0.1)"),
        borderRadius: 12,
        marginBottom: 8,
        overflow: "hidden",
        cursor: s._disabled ? "default" : "pointer",
        opacity: s._disabled && !s._selected ? 0.3 : 1,
        transition: "all 0.2s"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      onClick: function onClick() {
        return !s._disabled && setAddDetail(isExpanded ? null : i);
      },
      style: {
        padding: "10px 12px",
        display: "flex",
        alignItems: "center",
        gap: 10
      }
    }, (dbMatch === null || dbMatch === void 0 ? void 0 : dbMatch.imageUrl) && /*#__PURE__*/external_React_default().createElement("img", {
      src: dbMatch.imageUrl,
      alt: s.name,
      style: {
        width: 38,
        height: 38,
        objectFit: "contain",
        borderRadius: 6,
        flexShrink: 0,
        background: "rgba(255,255,255,0.04)"
      },
      onError: function onError(e) {
        e.target.style.display = "none";
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 12,
        fontWeight: 700,
        color: "#e2e8f0"
      }
    }, s.name), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        marginTop: 2
      }
    }, s.brand, " \xB7 ", s.shape, " \xB7 ", s.weight)), gs !== null && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "right",
        flexShrink: 0
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 18,
        fontWeight: 800,
        fontFamily: "'Outfit'",
        color: gs >= 7 ? "#4CAF50" : gs >= 5 ? "#f97316" : "#ef4444"
      }
    }, (gs * 10).toFixed(0), "%"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 7,
        color: "#64748b",
        textTransform: "uppercase"
      }
    }, "pertinence")), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 12,
        color: "#475569",
        flexShrink: 0,
        transition: "transform 0.2s",
        transform: isExpanded ? "rotate(180deg)" : "rotate(0deg)"
      }
    }, "\u25BC")), isExpanded && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        padding: "0 12px 12px",
        borderTop: "1px solid rgba(255,255,255,0.05)",
        animation: "fadeIn 0.3s ease"
      }
    }, badge && profileName && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 8,
        marginTop: 10,
        marginBottom: 8
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: badge.color,
        background: "".concat(badge.color, "18"),
        border: "1px solid ".concat(badge.color, "40"),
        borderRadius: 8,
        padding: "3px 10px",
        textTransform: "uppercase"
      }
    }, badge.text), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 10,
        color: "#64748b"
      }
    }, "pour ", profileName)), hasScores ? /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr 1fr",
        gap: 6,
        marginBottom: 10
      }
    }, ATTRS.map(function (a) {
      var val = sc[a] || 0;
      var isPrio = (profile.priorityTags || []).some(function (pid) {
        var m = {
          puissance: 'Puissance',
          controle: 'Contrôle',
          confort: 'Confort',
          spin: 'Spin',
          legerete: 'Maniabilité',
          protection: 'Confort'
        };
        return m[pid] === a;
      });
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: a,
        style: {
          textAlign: "center",
          padding: "6px 4px",
          borderRadius: 8,
          background: isPrio ? "rgba(249,115,22,0.1)" : "rgba(255,255,255,0.02)"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 16,
          fontWeight: 800,
          color: val >= 8 ? "#4CAF50" : val >= 6 ? "#e2e8f0" : "#f97316",
          fontFamily: "'Outfit'"
        }
      }, val), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 8,
          color: isPrio ? "#f97316" : "#64748b",
          fontWeight: isPrio ? 700 : 500,
          marginTop: 2
        }
      }, isPrio ? "★ " : "", a));
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: 4,
        marginBottom: 8
      }
    }, [["Forme", dbMatch.shape], ["Poids", dbMatch.weight], ["Équilibre", dbMatch.balance], ["Surface", dbMatch.surface], ["Noyau", dbMatch.core], ["Prix", dbMatch.price], ["Année", dbMatch.year]].map(function (_ref16) {
      var _ref17 = _slicedToArray(_ref16, 2),
        k = _ref17[0],
        v = _ref17[1];
      return v && v !== "—" && /*#__PURE__*/external_React_default().createElement("div", {
        key: k,
        style: {
          fontSize: 9,
          color: "#94a3b8",
          padding: "2px 0"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          color: "#64748b"
        }
      }, k, ":"), " ", /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontWeight: 600,
          color: "#cbd5e1"
        }
      }, v));
    })), /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 11,
        color: "#94a3b8",
        lineHeight: 1.6,
        margin: "0 0 8px",
        padding: "10px 12px",
        background: "rgba(255,255,255,0.02)",
        borderRadius: 10,
        borderLeft: "3px solid rgba(249,115,22,0.3)"
      }
    }, "\uD83C\uDFAF ", generateRacketDescription(dbMatch))) : /*#__PURE__*/external_React_default().createElement("p", {
      style: {
        fontSize: 10,
        color: "#64748b",
        margin: "10px 0 8px",
        lineHeight: 1.4
      }
    }, s.description || "Raquette non référencée dans la base locale. Les scores ne sont pas disponibles."), !s._selected && !s._disabled && /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick(e) {
        e.stopPropagation();
        selectSuggestion(i);
        setAddDetail(null);
      },
      style: {
        width: "100%",
        padding: "10px",
        borderRadius: 10,
        fontSize: 12,
        fontWeight: 700,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "linear-gradient(135deg,rgba(249,115,22,0.2),rgba(239,68,68,0.12))",
        border: "1px solid rgba(249,115,22,0.4)",
        color: "#f97316",
        transition: "all 0.2s",
        marginTop: 4
      }
    }, "+ Ajouter au comparateur"), s._selected && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        textAlign: "center",
        padding: "8px",
        fontSize: 11,
        color: "#4CAF50",
        fontWeight: 600
      }
    }, "\u2705 Ajout\xE9e au comparateur")));
  }))), panel === "profile" && /*#__PURE__*/external_React_default().createElement("div", {
    style: S.card
  }, function () {
    var STEPS = [{
      icon: "👤",
      label: "Identité"
    }, {
      icon: "🎾",
      label: "Jeu"
    }, {
      icon: "🩹",
      label: "Corps"
    }, {
      icon: "🎯",
      label: "Priorités"
    }];
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        marginBottom: 18
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        position: "relative",
        padding: "0 4px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "absolute",
        top: 16,
        left: 24,
        right: 24,
        height: 2,
        background: "rgba(255,255,255,0.06)",
        borderRadius: 1,
        zIndex: 0
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "absolute",
        top: 16,
        left: 24,
        height: 2,
        background: "linear-gradient(90deg,#f97316,#ef4444)",
        borderRadius: 1,
        zIndex: 1,
        transition: "width 0.4s cubic-bezier(.4,0,.2,1)",
        width: "".concat(wizardStep / (STEPS.length - 1) * Math.max(0, 100 - 16), "%")
      }
    }), STEPS.map(function (s, i) {
      return /*#__PURE__*/external_React_default().createElement("button", {
        key: i,
        onClick: function onClick() {
          return setWizardStep(i);
        },
        style: {
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          gap: 4,
          background: "none",
          border: "none",
          cursor: "pointer",
          padding: 0,
          zIndex: 2,
          fontFamily: "inherit"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: 32,
          height: 32,
          borderRadius: 10,
          background: i <= wizardStep ? "linear-gradient(135deg,rgba(249,115,22,0.25),rgba(239,68,68,0.2))" : "rgba(255,255,255,0.04)",
          border: "2px solid ".concat(i <= wizardStep ? "#f97316" : "rgba(255,255,255,0.1)"),
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontSize: 15,
          transition: "all 0.3s ease",
          boxShadow: i === wizardStep ? "0 0 12px rgba(249,115,22,0.3)" : "none",
          transform: i === wizardStep ? "scale(1.1)" : "scale(1)"
        }
      }, s.icon), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 9,
          fontWeight: i === wizardStep ? 700 : 500,
          color: i <= wizardStep ? "#f97316" : "#475569",
          transition: "color 0.3s ease"
        }
      }, s.label));
    })));
  }(), wizardStep === 0 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      animation: "fadeIn 0.3s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 800,
      color: "#e2e8f0",
      marginBottom: 4,
      fontFamily: "'Outfit'"
    }
  }, "\uD83D\uDC64 Qui es-tu ?"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 14px",
      lineHeight: 1.5
    }
  }, "Ces infos permettent d'adapter les recommandations \xE0 ton gabarit et ton niveau."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Nom du profil"), /*#__PURE__*/external_React_default().createElement("input", {
    value: profileName,
    onChange: function onChange(e) {
      return setProfileName(e.target.value);
    },
    placeholder: "Ex: Bidou, Noah, Maman...",
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.input), {}, {
      fontSize: 13,
      padding: "11px 14px"
    })
  })), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr 1fr",
      gap: 8,
      marginBottom: 12
    }
  }, /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "\xC2ge"), /*#__PURE__*/external_React_default().createElement("input", {
    type: "number",
    value: profile.age,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          age: Number(e.target.value)
        });
      });
    },
    placeholder: "49",
    style: S.input
  })), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Taille (cm)"), /*#__PURE__*/external_React_default().createElement("input", {
    type: "number",
    value: profile.height,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          height: Number(e.target.value)
        });
      });
    },
    placeholder: "175",
    style: S.input
  })), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Poids (kg)"), /*#__PURE__*/external_React_default().createElement("input", {
    type: "number",
    value: profile.weight,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          weight: Number(e.target.value)
        });
      });
    },
    placeholder: "80",
    style: S.input
  }))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: 8,
      marginBottom: 12
    }
  }, /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Niveau"), /*#__PURE__*/external_React_default().createElement("select", {
    value: profile.level,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          level: e.target.value
        });
      });
    },
    style: S.select
  }, LEVEL_OPTIONS.map(function (o) {
    return /*#__PURE__*/external_React_default().createElement("option", {
      key: o.value,
      value: o.value
    }, o.label, " \u2014 ", o.desc);
  }))), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Fr\xE9quence"), /*#__PURE__*/external_React_default().createElement("select", {
    value: profile.frequency,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          frequency: e.target.value
        });
      });
    },
    style: S.select
  }, FREQ_OPTIONS.map(function (o) {
    return /*#__PURE__*/external_React_default().createElement("option", {
      key: o.value,
      value: o.value
    }, o.label, " \u2014 ", o.desc);
  })))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr 1fr",
      gap: 8
    }
  }, /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Main"), /*#__PURE__*/external_React_default().createElement("select", {
    value: profile.hand,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          hand: e.target.value
        });
      });
    },
    style: S.select
  }, HAND_OPTIONS.map(function (o) {
    return /*#__PURE__*/external_React_default().createElement("option", {
      key: o,
      value: o
    }, o);
  }))), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "C\xF4t\xE9 de jeu"), /*#__PURE__*/external_React_default().createElement("select", {
    value: profile.side,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          side: e.target.value
        });
      });
    },
    style: S.select
  }, SIDE_OPTIONS.map(function (o) {
    return /*#__PURE__*/external_React_default().createElement("option", {
      key: o,
      value: o
    }, o);
  }))), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("label", {
    style: S.label
  }, "Comp\xE9tition"), /*#__PURE__*/external_React_default().createElement("select", {
    value: profile.competition ? "oui" : "non",
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          competition: e.target.value === "oui"
        });
      });
    },
    style: S.select
  }, /*#__PURE__*/external_React_default().createElement("option", {
    value: "non"
  }, "Non"), /*#__PURE__*/external_React_default().createElement("option", {
    value: "oui"
  }, "Oui")))), (Number(profile.age) > 0 && Number(profile.age) < 15 || Number(profile.height) > 0 && Number(profile.height) < 150) && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      background: "rgba(59,130,246,0.1)",
      border: "1px solid rgba(59,130,246,0.3)",
      borderRadius: 8,
      padding: "8px 10px",
      marginTop: 12,
      fontSize: 10,
      color: "#60a5fa",
      fontWeight: 600
    }
  }, "\uD83E\uDDD2 Profil junior d\xE9tect\xE9 \u2014 recommandations adapt\xE9es (poids l\xE9ger, grip r\xE9duit)"), Number(profile.age) >= 50 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      background: "rgba(245,158,11,0.08)",
      border: "1px solid rgba(245,158,11,0.2)",
      borderRadius: 8,
      padding: "8px 10px",
      marginTop: 12,
      fontSize: 10,
      color: "#fbbf24",
      fontWeight: 600
    }
  }, "\uD83D\uDC64 Profil 50+ \u2014 Confort, Tol\xE9rance et Maniabilit\xE9 renforc\xE9s automatiquement")), wizardStep === 1 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      animation: "fadeIn 0.3s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 800,
      color: "#e2e8f0",
      marginBottom: 4,
      fontFamily: "'Outfit'"
    }
  }, "\uD83C\uDFBE Comment tu joues ?"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 14px",
      lineHeight: 1.5
    }
  }, "Ton style de jeu influence directement quels crit\xE8res sont prioritaires dans le scoring. S\xE9lectionne tout ce qui te correspond."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 8,
      marginBottom: 14
    }
  }, STYLE_TAGS.map(function (t) {
    var sel = profile.styleTags.includes(t.id);
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: t.id,
      onClick: function onClick() {
        return toggleTag("styleTags", t.id);
      },
      className: "pa-tag",
      style: {
        padding: "10px 14px",
        borderRadius: 12,
        background: sel ? "rgba(249,115,22,0.15)" : "rgba(255,255,255,0.03)",
        border: "1.5px solid ".concat(sel ? "#f97316" : "rgba(255,255,255,0.08)"),
        color: sel ? "#f97316" : "#94a3b8",
        fontSize: 12,
        fontWeight: sel ? 700 : 500,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s ease",
        textAlign: "left"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", null, t.label), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: sel ? "#fb923c" : "#475569",
        marginTop: 2,
        fontWeight: 400
      }
    }, t.tip));
  })), /*#__PURE__*/external_React_default().createElement("label", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.label), {}, {
      marginBottom: 6
    })
  }, "Pr\xE9cisions (optionnel)"), /*#__PURE__*/external_React_default().createElement("input", {
    value: profile.styleExtra,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          styleExtra: e.target.value
        });
      });
    },
    placeholder: "Ex: Je monte beaucoup au filet, je joue avec du lift...",
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.input), {}, {
      fontSize: 11
    })
  })), wizardStep === 2 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      animation: "fadeIn 0.3s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 800,
      color: "#e2e8f0",
      marginBottom: 4,
      fontFamily: "'Outfit'"
    }
  }, "\uD83E\uDE79 Ton corps"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 14px",
      lineHeight: 1.5
    }
  }, "Les blessures et contraintes physiques impactent directement le crit\xE8re ", /*#__PURE__*/external_React_default().createElement("strong", {
    style: {
      color: "#ef4444"
    }
  }, "Confort"), " dans les verdicts. Une raquette inadapt\xE9e peut aggraver les douleurs."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 8,
      marginBottom: 14
    }
  }, INJURY_TAGS.map(function (t) {
    var sel = profile.injuryTags.includes(t.id);
    var isNone = t.id === "aucune";
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: t.id,
      onClick: function onClick() {
        return toggleTag("injuryTags", t.id);
      },
      className: "pa-tag",
      style: {
        padding: "10px 14px",
        borderRadius: 12,
        background: sel ? isNone ? "rgba(76,175,80,0.15)" : "rgba(239,68,68,0.12)" : "rgba(255,255,255,0.03)",
        border: "1.5px solid ".concat(sel ? isNone ? "#4CAF50" : "#ef4444" : "rgba(255,255,255,0.08)"),
        color: sel ? isNone ? "#4CAF50" : "#ef4444" : "#94a3b8",
        fontSize: 12,
        fontWeight: sel ? 700 : 500,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s ease"
      }
    }, isNone ? "✓ " : "", t.label);
  })), /*#__PURE__*/external_React_default().createElement("label", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.label), {}, {
      marginBottom: 6
    })
  }, "Pr\xE9cisions (optionnel)"), /*#__PURE__*/external_React_default().createElement("input", {
    value: profile.injuryExtra,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          injuryExtra: e.target.value
        });
      });
    },
    placeholder: "Ex: Tendinite chronique, post-op\xE9ration \xE9paule...",
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.input), {}, {
      fontSize: 11
    })
  })), wizardStep === 3 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      animation: "fadeIn 0.3s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 800,
      color: "#e2e8f0",
      marginBottom: 4,
      fontFamily: "'Outfit'"
    }
  }, "\uD83C\uDFAF Qu'est-ce que tu cherches ?"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 14px",
      lineHeight: 1.5
    }
  }, "Ces crit\xE8res pond\xE8rent le score global. Les suggestions seront tri\xE9es en fonction de tes priorit\xE9s."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 700,
      color: "#4CAF50",
      marginBottom: 6
    }
  }, "Priorit\xE9s dans ta raquette"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 8,
      marginBottom: 14
    }
  }, PRIORITY_TAGS.map(function (t) {
    var sel = profile.priorityTags.includes(t.id);
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: t.id,
      onClick: function onClick() {
        return toggleTag("priorityTags", t.id);
      },
      className: "pa-tag",
      style: {
        padding: "10px 14px",
        borderRadius: 12,
        background: sel ? "rgba(76,175,80,0.12)" : "rgba(255,255,255,0.03)",
        border: "1.5px solid ".concat(sel ? "#4CAF50" : "rgba(255,255,255,0.08)"),
        color: sel ? "#4CAF50" : "#94a3b8",
        fontSize: 12,
        fontWeight: sel ? 700 : 500,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s ease"
      }
    }, t.label);
  })), /*#__PURE__*/external_React_default().createElement("input", {
    value: profile.priorityExtra,
    onChange: function onChange(e) {
      return setProfile(function (p) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, p), {}, {
          priorityExtra: e.target.value
        });
      });
    },
    placeholder: "Ex: Budget max 200\u20AC, raquette pas trop lourde...",
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.input), {}, {
      fontSize: 11,
      marginBottom: 16
    })
  }), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 700,
      color: "#9C27B0",
      marginBottom: 6
    }
  }, "\uD83C\uDFF7 Marques pr\xE9f\xE9r\xE9es ", /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontWeight: 400,
      color: "#64748b"
    }
  }, "(optionnel \u2014 vide = toutes)")), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 8
    }
  }, BRAND_TAGS.map(function (t) {
    var sel = profile.brandTags.includes(t.id);
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: t.id,
      onClick: function onClick() {
        return toggleTag("brandTags", t.id);
      },
      className: "pa-tag",
      style: {
        padding: "8px 12px",
        borderRadius: 10,
        background: sel ? "rgba(156,39,176,0.12)" : "rgba(255,255,255,0.03)",
        border: "1.5px solid ".concat(sel ? "#9C27B0" : "rgba(255,255,255,0.08)"),
        color: sel ? "#CE93D8" : "#94a3b8",
        fontSize: 11,
        fontWeight: sel ? 700 : 500,
        cursor: "pointer",
        fontFamily: "inherit",
        transition: "all 0.2s ease"
      }
    }, t.label);
  }))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      marginTop: 20,
      alignItems: "center"
    }
  }, wizardStep > 0 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      return setWizardStep(function (s) {
        return s - 1;
      });
    },
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      flex: 1,
      padding: "12px 0",
      borderRadius: 12,
      fontSize: 12
    })
  }, "\u2190 Pr\xE9c\xE9dent"), wizardStep < 3 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      if (wizardStep === 0 && !profileName.trim()) {
        alert("Donne un nom à ton profil pour continuer");
        return;
      }
      setWizardStep(function (s) {
        return s + 1;
      });
    },
    style: {
      flex: 2,
      padding: "12px 0",
      borderRadius: 12,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "inherit",
      background: "linear-gradient(135deg,rgba(249,115,22,0.2),rgba(239,68,68,0.15))",
      border: "1px solid rgba(249,115,22,0.35)",
      color: "#f97316",
      transition: "all 0.2s ease"
    }
  }, "Suivant \u2192"), wizardStep === 3 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      if (!profileName.trim()) {
        alert("Retourne à l'étape 1 pour nommer ton profil");
        return;
      }
      var list = saveNamedProfile(profileName.trim(), profile);
      setSavedProfiles(list);
      setPanel(null);
      if (rackets.length > 0) reanalyzeAll();
    },
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btnGreen), {}, {
      flex: 2,
      padding: "14px 0",
      borderRadius: 12,
      fontSize: 14,
      background: "linear-gradient(135deg,rgba(76,175,80,0.25),rgba(76,175,80,0.15))"
    })
  }, "\u2705 Sauvegarder", rackets.length > 0 ? " & Ré-analyser" : "")), error && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#ef4444",
      marginTop: 8
    }
  }, error)), panel === "manage" && /*#__PURE__*/external_React_default().createElement("div", {
    style: S.card
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: S.title
  }, "\uD83D\uDDD1 G\xC9RER LES RAQUETTES"), rackets.length === 0 && /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      color: "#64748b",
      fontSize: 11,
      textAlign: "center",
      padding: "12px 0"
    }
  }, "Aucune raquette. Utilise \"\uD83C\uDFAF Sugg\xE8re-moi\" ou \"+ Ajouter\" pour commencer."), rackets.map(function (r) {
    return /*#__PURE__*/external_React_default().createElement("div", {
      key: r.id,
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "6px 0",
        borderBottom: "1px solid rgba(255,255,255,0.05)"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 8
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 8,
        height: 8,
        borderRadius: "50%",
        background: r.color
      }
    }), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 11,
        color: "#e2e8f0"
      }
    }, r.name)), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return removeRacket(r.id);
      },
      style: {
        background: "rgba(239,68,68,0.15)",
        border: "1px solid rgba(239,68,68,0.3)",
        borderRadius: 6,
        padding: "3px 8px",
        color: "#ef4444",
        fontSize: 10,
        cursor: "pointer",
        fontWeight: 600,
        fontFamily: "inherit"
      }
    }, "Supprimer"));
  }), rackets.length > 1 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      setConfirmModal({
        message: "Supprimer toutes les raquettes ?",
        onConfirm: function onConfirm() {
          setRackets([]);
          setSelected([]);
          setConfirmModal(null);
        },
        onCancel: function onCancel() {
          return setConfirmModal(null);
        }
      });
    },
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      width: "100%",
      marginTop: 12,
      padding: "8px 0",
      fontSize: 11,
      color: "#ef4444",
      borderColor: "rgba(239,68,68,0.3)"
    })
  }, "\uD83D\uDDD1 Tout effacer"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      marginTop: 16,
      paddingTop: 12,
      borderTop: "1px solid rgba(255,255,255,0.06)"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 10,
      fontWeight: 700,
      color: "#94a3b8",
      marginBottom: 8,
      letterSpacing: "0.04em"
    }
  }, "\uD83D\uDDC3\uFE0F BASE DE DONN\xC9ES"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 6,
      alignItems: "center",
      marginBottom: 8
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      flex: 1,
      background: "rgba(255,255,255,0.03)",
      borderRadius: 8,
      padding: "6px 10px",
      border: "1px solid rgba(255,255,255,0.06)"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 9,
      color: "#64748b"
    }
  }, "Embarqu\xE9es"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 16,
      fontWeight: 700,
      color: "#f97316",
      fontFamily: "'Outfit'"
    }
  }, rackets_db_namespaceObject.length)), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 14,
      color: "#334155"
    }
  }, "+"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      flex: 1,
      background: "rgba(255,255,255,0.03)",
      borderRadius: 8,
      padding: "6px 10px",
      border: "1px solid rgba(255,255,255,0.06)"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 9,
      color: "#64748b"
    }
  }, "Apprises (local)"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 16,
      fontWeight: 700,
      color: localDBCount > 0 ? "#22c55e" : "#334155",
      fontFamily: "'Outfit'"
    }
  }, localDBCount)), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 14,
      color: "#334155"
    }
  }, "="), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      flex: 1,
      background: "rgba(249,115,22,0.05)",
      borderRadius: 8,
      padding: "6px 10px",
      border: "1px solid rgba(249,115,22,0.15)"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 9,
      color: "#f97316"
    }
  }, "Total"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 16,
      fontWeight: 700,
      color: "#fff",
      fontFamily: "'Outfit'"
    }
  }, rackets_db_namespaceObject.length + localDBCount))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 6
    }
  }, /*#__PURE__*/external_React_default().createElement("button", {
    onClick: exportLocalDB,
    disabled: localDBCount === 0,
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      flex: 1,
      padding: "7px 0",
      fontSize: 10,
      opacity: localDBCount === 0 ? 0.4 : 1,
      cursor: localDBCount === 0 ? "default" : "pointer"
    })
  }, "\uD83D\uDCE4 Exporter local (", localDBCount, ")"), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: clearLocalDB,
    disabled: localDBCount === 0,
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.btn(false)), {}, {
      flex: 1,
      padding: "7px 0",
      fontSize: 10,
      color: "#ef4444",
      borderColor: "rgba(239,68,68,0.2)",
      opacity: localDBCount === 0 ? 0.4 : 1,
      cursor: localDBCount === 0 ? "default" : "pointer"
    })
  }, "\uD83E\uDDF9 Vider local")), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 8,
      color: "#475569",
      margin: "6px 0 0",
      lineHeight: 1.4
    }
  }, "Les raquettes \xAB apprises \xBB sont celles trouv\xE9es via recherche web. Exporte-les pour les int\xE9grer \xE0 la base embarqu\xE9e."))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fill,minmax(150px,1fr))",
      gap: 10,
      marginBottom: 20
    }
  }, rackets.length === 0 && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      gridColumn: "1/-1",
      textAlign: "center",
      padding: "30px 16px",
      color: "#64748b",
      fontSize: 12
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 32,
      marginBottom: 8
    }
  }, "\uD83C\uDFBE"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      margin: "0 0 4px",
      fontWeight: 600,
      color: "#94a3b8"
    }
  }, "Aucune raquette"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      margin: 0
    }
  }, "Clique sur ", /*#__PURE__*/external_React_default().createElement("strong", null, "\uD83C\uDFAF Sugg\xE8re-moi"), " pour des recommandations personnalis\xE9es", /*#__PURE__*/external_React_default().createElement("br", null), "ou ", /*#__PURE__*/external_React_default().createElement("strong", null, "+ Ajouter"), " pour chercher un mod\xE8le pr\xE9cis")), rackets.map(function (r) {
    var isSel = selected.includes(r.id);
    var fy = fyConfig[computeForYou(r.scores, profile)] || fyConfig.partial;
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: r.id,
      className: "pa-card",
      onClick: function onClick() {
        return toggleRacket(r.id);
      },
      onMouseEnter: function onMouseEnter() {
        return setHoveredRacket(r.id);
      },
      onMouseLeave: function onMouseLeave() {
        return setHoveredRacket(null);
      },
      style: {
        background: isSel ? "linear-gradient(165deg,".concat(r.color, "18,").concat(r.color, "08,transparent)") : "rgba(255,255,255,0.02)",
        border: "2px solid ".concat(isSel ? r.color + "cc" : "rgba(255,255,255,0.06)"),
        borderRadius: 14,
        padding: "12px 10px",
        cursor: "pointer",
        textAlign: "left",
        position: "relative",
        fontFamily: "'Inter',sans-serif",
        boxShadow: isSel ? "0 4px 16px ".concat(r.color, "22") : "0 2px 8px rgba(0,0,0,0.15)",
        transform: hoveredRacket === r.id ? "translateY(-3px) scale(1.02)" : "none",
        transition: "all 0.2s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      className: "pa-badge",
      style: {
        position: "absolute",
        top: -8,
        right: 8,
        background: fy.bg + "dd",
        border: "1px solid ".concat(fy.border),
        borderRadius: 20,
        padding: "2px 8px",
        fontSize: 7,
        fontWeight: 700,
        color: "#fff",
        letterSpacing: "0.03em",
        boxShadow: "0 2px 8px ".concat(fy.bg, "44")
      }
    }, fy.text), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
      src: proxyImg(r.imageUrl),
      alt: "",
      style: {
        width: 38,
        height: 38,
        objectFit: "contain",
        borderRadius: 6,
        marginBottom: 4,
        background: "rgba(255,255,255,0.06)"
      },
      onError: function onError(e) {
        e.target.style.display = 'none';
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 8,
        height: 8,
        borderRadius: "50%",
        background: r.color,
        marginBottom: 6,
        boxShadow: isSel ? "0 0 8px ".concat(r.color) : "none",
        transition: "box-shadow 0.2s ease"
      }
    }), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        fontWeight: 700,
        color: isSel ? "#fff" : "#94a3b8",
        lineHeight: 1.3,
        transition: "color 0.2s ease"
      }
    }, r.shortName), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#475569",
        marginTop: 3
      }
    }, r.shape, " \xB7 ", r.weight), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#475569"
      }
    }, r.brand, " \xB7 ", r.price), r._incomplete && /*#__PURE__*/external_React_default().createElement("div", {
      onClick: function onClick(e) {
        e.stopPropagation();
        rescoreRacket(r.id);
      },
      style: {
        position: "absolute",
        bottom: 4,
        right: 4,
        background: "rgba(249,115,22,0.15)",
        border: "1px solid rgba(249,115,22,0.4)",
        borderRadius: 6,
        padding: "2px 6px",
        fontSize: 8,
        color: "#f97316",
        fontWeight: 700,
        cursor: "pointer"
      }
    }, "\uD83D\uDD04 Re-scorer"));
  })), selRackets.length >= 2 && /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      return setShowArena(true);
    },
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 8,
      width: "100%",
      padding: "14px 20px",
      marginBottom: 18,
      borderRadius: 14,
      cursor: "pointer",
      fontFamily: "'Outfit',sans-serif",
      background: "linear-gradient(135deg,rgba(124,58,237,0.15),rgba(79,70,229,0.1),rgba(236,72,153,0.08))",
      border: "1.5px solid rgba(124,58,237,0.35)",
      transition: "all 0.3s ease",
      boxShadow: "0 4px 24px rgba(124,58,237,0.15)"
    },
    onMouseEnter: function onMouseEnter(e) {
      e.currentTarget.style.background = "linear-gradient(135deg,rgba(124,58,237,0.25),rgba(79,70,229,0.18),rgba(236,72,153,0.12))";
      e.currentTarget.style.boxShadow = "0 6px 32px rgba(124,58,237,0.25)";
      e.currentTarget.style.transform = "translateY(-1px)";
    },
    onMouseLeave: function onMouseLeave(e) {
      e.currentTarget.style.background = "linear-gradient(135deg,rgba(124,58,237,0.15),rgba(79,70,229,0.1),rgba(236,72,153,0.08))";
      e.currentTarget.style.boxShadow = "0 4px 24px rgba(124,58,237,0.15)";
      e.currentTarget.style.transform = "none";
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 18
    }
  }, "\u2694\uFE0F"), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 700,
      color: "#c4b5fd",
      letterSpacing: "-0.01em"
    }
  }, "Entrer dans l'Ar\xE8ne"), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 10,
      color: "#7c3aed",
      fontWeight: 600,
      background: "rgba(124,58,237,0.15)",
      padding: "2px 8px",
      borderRadius: 8
    }
  }, selRackets.length, " raquettes")), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 2,
      marginBottom: 18,
      background: "rgba(255,255,255,0.03)",
      borderRadius: 12,
      padding: 4,
      border: "1px solid rgba(255,255,255,0.04)"
    }
  }, [["radar", "🕸 Radar"], ["bars", "📊 Barres"], ["table", "📋 Détails"], ["fit", "🎯 Pertinence"]].map(function (_ref18) {
    var _ref19 = _slicedToArray(_ref18, 2),
      k = _ref19[0],
      l = _ref19[1];
    return /*#__PURE__*/external_React_default().createElement("button", {
      key: k,
      className: "pa-tab ".concat(tab === k ? "pa-tab-active" : ""),
      onClick: function onClick() {
        return setTab(k);
      },
      style: {
        flex: 1,
        padding: "9px 0",
        background: tab === k ? "rgba(255,255,255,0.06)" : "transparent",
        border: "none",
        borderRadius: 9,
        color: tab === k ? "#fff" : "#64748b",
        fontSize: 11,
        fontWeight: tab === k ? 700 : 500,
        cursor: "pointer",
        fontFamily: "'Inter',sans-serif",
        letterSpacing: "-0.01em",
        transition: "all 0.2s ease"
      }
    }, l);
  })), tab === "radar" && /*#__PURE__*/external_React_default().createElement("div", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.card), {}, {
      padding: 20,
      position: "relative",
      overflow: "hidden"
    })
  }, /*#__PURE__*/external_React_default().createElement("style", null, "\n          @keyframes racketFadeIn {\n            from { opacity: 0; transform: scale(0.9); }\n            to { opacity: 1; transform: scale(1); }\n          }\n        "), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 0,
      minHeight: 400
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      width: 280,
      flexShrink: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      minHeight: 400
    }
  }, function () {
    var hr = hoveredRacket ? selRackets.find(function (r) {
      return r.id === hoveredRacket;
    }) : null;
    if (!hr || !hr.imageUrl) return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        height: "100%",
        opacity: 0.3
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 100,
        height: 100,
        borderRadius: "50%",
        border: "2px dashed rgba(255,255,255,0.1)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginBottom: 10
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 32,
        opacity: 0.4
      }
    }, "\uD83D\uDC46")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#334155",
        textAlign: "center",
        lineHeight: 1.4
      }
    }, "Survole une raquette", /*#__PURE__*/external_React_default().createElement("br", null), "pour voir son visuel"));
    return /*#__PURE__*/external_React_default().createElement("div", {
      key: hr.id,
      style: {
        animation: "racketFadeIn 0.3s ease-out",
        textAlign: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(255,255,255,0.03)",
        border: "2px solid ".concat(hr.color, "40"),
        borderRadius: 20,
        padding: 20,
        boxShadow: "0 0 40px ".concat(hr.color, "15, inset 0 0 20px ").concat(hr.color, "08"),
        transition: "border-color 0.3s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("img", {
      src: proxyImg(hr.imageUrl),
      alt: hr.name,
      style: {
        width: 240,
        height: 280,
        objectFit: "contain",
        display: "block",
        margin: "0 auto",
        filter: "drop-shadow(0 8px 24px rgba(0,0,0,0.5))"
      },
      onError: function onError(e) {
        e.target.style.display = 'none';
      }
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        marginTop: 12
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 13,
        fontWeight: 700,
        color: hr.color,
        lineHeight: 1.2
      }
    }, hr.name), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#64748b",
        marginTop: 4
      }
    }, hr.shape, " \xB7 ", hr.weight), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#64748b"
      }
    }, hr.brand, hr.price && hr.price !== "—" ? " \xB7 ".concat(hr.price) : ""), hr.player && hr.player !== "—" && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#475569",
        marginTop: 3
      }
    }, "\uD83C\uDFBE ", hr.player)));
  }()), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      position: "relative"
    }
  }, /*#__PURE__*/external_React_default().createElement(ResponsiveContainer, {
    width: "100%",
    height: 400
  }, /*#__PURE__*/external_React_default().createElement(RadarChart, {
    data: radarData,
    cx: "50%",
    cy: "50%",
    outerRadius: "75%"
  }, /*#__PURE__*/external_React_default().createElement(PolarGrid, {
    stroke: "rgba(255,255,255,0.12)",
    strokeDasharray: "3 3",
    gridType: "polygon"
  }), /*#__PURE__*/external_React_default().createElement(PolarAngleAxis, {
    dataKey: "attribute",
    tick: {
      fill: "#94a3b8",
      fontSize: 11,
      fontWeight: 600,
      fontFamily: "Inter"
    }
  }), /*#__PURE__*/external_React_default().createElement(PolarRadiusAxis, {
    angle: 90,
    domain: [0, 10],
    tick: {
      fill: "#64748b",
      fontSize: 9,
      fontWeight: 500
    },
    tickCount: 6,
    axisLine: false
  }), /*#__PURE__*/external_React_default().createElement(Radar, {
    name: "\u2014 10/10 \u2014",
    dataKey: "\u2014 10/10 \u2014",
    stroke: "rgba(255,255,255,0.25)",
    fill: "none",
    strokeWidth: 1.5,
    strokeDasharray: "6 3",
    strokeOpacity: 1,
    fillOpacity: 0,
    dot: false,
    legendType: "none"
  }), selRackets.map(function (r, i) {
    var isHovered = hoveredRacket === r.id;
    var anyHovered = hoveredRacket !== null;
    return /*#__PURE__*/external_React_default().createElement(Radar, {
      key: r.id,
      name: r.shortName,
      dataKey: r.shortName,
      stroke: r.color,
      fill: r.color,
      fillOpacity: isHovered ? 0.35 : anyHovered ? 0.03 : 0.08 + i * 0.03,
      strokeWidth: isHovered ? 3.5 : anyHovered ? 1 : 2.5,
      strokeOpacity: isHovered ? 1 : anyHovered ? 0.3 : 1
    });
  }), /*#__PURE__*/external_React_default().createElement(Legend, {
    wrapperStyle: {
      fontSize: 10,
      color: "#94a3b8",
      paddingTop: 10,
      fontFamily: "Inter"
    }
  }))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      position: "absolute",
      top: 8,
      right: 12,
      fontSize: 9,
      color: "#475569",
      display: "flex",
      alignItems: "center",
      gap: 5
    }
  }, /*#__PURE__*/external_React_default().createElement("svg", {
    width: "20",
    height: "8"
  }, /*#__PURE__*/external_React_default().createElement("line", {
    x1: "0",
    y1: "4",
    x2: "20",
    y2: "4",
    stroke: "rgba(255,255,255,0.3)",
    strokeWidth: "1.5",
    strokeDasharray: "4 2"
  })), /*#__PURE__*/external_React_default().createElement("span", null, "Score parfait 10/10"))))), tab === "bars" && /*#__PURE__*/external_React_default().createElement("div", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.card), {}, {
      padding: 20
    })
  }, /*#__PURE__*/external_React_default().createElement(ResponsiveContainer, {
    width: "100%",
    height: 360
  }, /*#__PURE__*/external_React_default().createElement(BarChart, {
    data: radarData,
    layout: "vertical",
    margin: {
      left: 80,
      right: 20
    }
  }, /*#__PURE__*/external_React_default().createElement(CartesianGrid, {
    strokeDasharray: "3 3",
    stroke: "rgba(255,255,255,0.04)"
  }), /*#__PURE__*/external_React_default().createElement(XAxis, {
    type: "number",
    domain: [0, 10],
    tick: {
      fill: "#475569",
      fontSize: 9,
      fontFamily: "Inter"
    }
  }), /*#__PURE__*/external_React_default().createElement(YAxis, {
    dataKey: "attribute",
    type: "category",
    tick: {
      fill: "#94a3b8",
      fontSize: 10,
      fontWeight: 600,
      fontFamily: "Inter"
    },
    width: 75
  }), /*#__PURE__*/external_React_default().createElement(Tooltip, {
    contentStyle: {
      background: "#1a2236",
      border: "1px solid rgba(255,255,255,0.08)",
      borderRadius: 10,
      fontSize: 11,
      fontFamily: "Inter",
      boxShadow: "0 8px 24px rgba(0,0,0,0.4)"
    }
  }), selRackets.map(function (r) {
    return /*#__PURE__*/external_React_default().createElement(Bar, {
      key: r.id,
      dataKey: r.shortName,
      fill: r.color,
      radius: [0, 5, 5, 0],
      barSize: 10
    });
  })))), tab === "table" && /*#__PURE__*/external_React_default().createElement("div", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.card), {}, {
      overflowX: "auto"
    })
  }, /*#__PURE__*/external_React_default().createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "separate",
      borderSpacing: 0,
      fontSize: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("thead", null, /*#__PURE__*/external_React_default().createElement("tr", null, /*#__PURE__*/external_React_default().createElement("th", {
    style: {
      textAlign: "left",
      padding: "8px 6px",
      color: "#475569",
      borderBottom: "2px solid rgba(255,255,255,0.06)",
      fontSize: 9,
      letterSpacing: "0.03em"
    }
  }), selRackets.map(function (r) {
    return /*#__PURE__*/external_React_default().createElement("th", {
      key: r.id,
      style: {
        textAlign: "center",
        padding: "8px 4px",
        color: r.color,
        fontWeight: 700,
        borderBottom: "2px solid rgba(255,255,255,0.06)",
        fontSize: 9,
        minWidth: 85,
        fontFamily: "'Outfit'",
        letterSpacing: "0.02em"
      }
    }, r.shortName);
  }))), /*#__PURE__*/external_React_default().createElement("tbody", null, [{
    l: "Marque",
    k: "brand"
  }, {
    l: "Forme",
    k: "shape"
  }, {
    l: "Poids",
    k: "weight"
  }, {
    l: "Équilibre",
    k: "balance"
  }, {
    l: "Surface",
    k: "surface"
  }, {
    l: "Mousse",
    k: "core"
  }, {
    l: "Joueur",
    k: "player"
  }, {
    l: "Prix indicatif",
    k: "price"
  }].map(function (row, i) {
    return /*#__PURE__*/external_React_default().createElement("tr", {
      key: row.k,
      className: "pa-row",
      style: {
        background: i % 2 === 0 ? "rgba(255,255,255,0.015)" : "transparent"
      }
    }, /*#__PURE__*/external_React_default().createElement("td", {
      style: {
        padding: "6px 6px",
        color: "#94a3b8",
        fontWeight: 600,
        fontSize: 10
      }
    }, row.l), selRackets.map(function (r) {
      return /*#__PURE__*/external_React_default().createElement("td", {
        key: r.id,
        style: {
          padding: "6px 4px",
          textAlign: "center",
          color: "#cbd5e1",
          fontSize: 10
        }
      }, r[row.k]);
    }));
  }), /*#__PURE__*/external_React_default().createElement("tr", null, /*#__PURE__*/external_React_default().createElement("td", {
    colSpan: selRackets.length + 1,
    style: {
      padding: "12px 6px 4px",
      color: "#f97316",
      fontWeight: 700,
      fontSize: 10,
      borderTop: "2px solid rgba(249,115,22,0.15)",
      fontFamily: "'Outfit'",
      letterSpacing: "0.04em",
      textTransform: "uppercase"
    }
  }, "Notes brutes /10")), selRackets.some(function (r) {
    return r.refSource;
  }) && /*#__PURE__*/external_React_default().createElement("tr", null, /*#__PURE__*/external_React_default().createElement("td", {
    style: {
      padding: "2px 6px",
      fontSize: 8,
      color: "#334155",
      fontStyle: "italic"
    }
  }, "Source"), selRackets.map(function (r) {
    return /*#__PURE__*/external_React_default().createElement("td", {
      key: r.id,
      style: {
        padding: "2px 4px",
        textAlign: "center",
        fontSize: 7,
        color: "#334155",
        fontStyle: "italic"
      }
    }, r.refSource || "Règles méca.");
  })), ATTRS.map(function (attr, i) {
    var mx = Math.max.apply(Math, _toConsumableArray(selRackets.map(function (r) {
      return r.scores[attr];
    })));
    return /*#__PURE__*/external_React_default().createElement("tr", {
      key: attr,
      className: "pa-row",
      style: {
        background: i % 2 === 0 ? "rgba(255,255,255,0.015)" : "transparent"
      }
    }, /*#__PURE__*/external_React_default().createElement("td", {
      style: {
        padding: "6px 6px",
        color: "#94a3b8",
        fontWeight: 600
      }
    }, attr), selRackets.map(function (r) {
      var v = r.scores[attr];
      var best = v === mx && selRackets.length > 1;
      return /*#__PURE__*/external_React_default().createElement("td", {
        key: r.id,
        style: {
          padding: "6px 4px",
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          gap: 2
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          color: best ? "#4ade80" : "#cbd5e1",
          fontWeight: best ? 700 : 500,
          fontFamily: "'Outfit'",
          fontSize: 12
        }
      }, v), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: "70%",
          height: 3,
          borderRadius: 2,
          background: "rgba(255,255,255,0.06)",
          overflow: "hidden"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: "".concat(v * 10, "%"),
          height: "100%",
          borderRadius: 2,
          background: v >= 8 ? r.color : v >= 6.5 ? "#64748b" : "#ef444466",
          transition: "width 0.4s ease"
        }
      }))));
    }));
  }), function () {
    var avgs = selRackets.map(function (r) {
      return Math.round(ATTRS.reduce(function (s, a) {
        return s + r.scores[a];
      }, 0) / ATTRS.length * 10) / 10;
    });
    var mxA = Math.max.apply(Math, _toConsumableArray(avgs));
    return /*#__PURE__*/external_React_default().createElement("tr", {
      style: {
        borderTop: "2px solid rgba(249,115,22,0.2)",
        background: "rgba(249,115,22,0.04)"
      }
    }, /*#__PURE__*/external_React_default().createElement("td", {
      style: {
        padding: "10px 6px",
        color: "#f97316",
        fontWeight: 800,
        fontSize: 11,
        fontFamily: "'Outfit'"
      }
    }, "\u2B50 MOYENNE"), selRackets.map(function (r, i) {
      var a = avgs[i];
      var best = a === mxA && selRackets.length > 1;
      return /*#__PURE__*/external_React_default().createElement("td", {
        key: r.id,
        style: {
          padding: "10px 4px",
          textAlign: "center",
          color: best ? "#4ade80" : "#f97316",
          fontWeight: 800,
          fontSize: 14,
          fontFamily: "'Outfit'"
        }
      }, a, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 9,
          color: "#64748b",
          fontWeight: 500
        }
      }, "/10"));
    }));
  }()))), tab === "fit" && /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("style", null, "\n          @media print {\n            * { box-sizing: border-box !important; }\n            html, body { background: white !important; margin: 0 !important; padding: 0 !important; }\n            body * { visibility: hidden !important; }\n            #print-pertinence, #print-pertinence * { visibility: visible !important; }\n            #print-pertinence {\n              position: absolute !important; left: 0 !important; top: 0 !important;\n              width: 100% !important; max-width: 100% !important;\n              padding: 8mm 8mm !important;\n              background: white !important; color: #1a1a1a !important;\n              overflow: visible !important; font-size: 10px !important;\n              -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important;\n            }\n            #print-pertinence .print-header { display: block !important; }\n            #print-pertinence .print-section-title { display: block !important; }\n            #print-pertinence .no-print { display: none !important; }\n            #print-pertinence .print-radar-section { display: flex !important; gap: 12px !important; align-items: stretch !important; }\n            #print-pertinence .print-radar-section > div:first-child { background: #f0f4ff !important; border: 1.5px solid #818cf8 !important; border-radius: 10px !important; padding: 8px 0 0 !important; display: flex !important; flex-direction: column !important; align-items: center !important; }\n            #print-pertinence .print-radar-section .recharts-polar-grid line,\n            #print-pertinence .print-radar-section .recharts-polar-grid polygon,\n            #print-pertinence .print-radar-section .recharts-polar-grid-concentric-polygon { stroke: #c7d2fe !important; stroke-opacity: 1 !important; }\n            #print-pertinence .print-radar-section .recharts-polygon { stroke-width: 2.5px !important; fill-opacity: 0.25 !important; print-color-adjust: exact !important; -webkit-print-color-adjust: exact !important; }\n            #print-pertinence .print-radar-section .recharts-polar-angle-axis-tick text { fill: #374151 !important; }\n            #print-pertinence .print-card {\n              border: 1px solid #e2e8f0 !important; background: #fafafa !important;\n              color: #1a1a1a !important; page-break-inside: avoid; break-inside: avoid;\n              overflow: hidden !important; max-width: 100% !important;\n              margin-bottom: 8px !important; border-radius: 10px !important;\n            }\n            #print-pertinence .print-card-gold {\n              background: linear-gradient(135deg, #fffbeb, #fef3c7) !important;\n              border: 2px solid #d4a017 !important;\n              box-shadow: 0 2px 12px rgba(184,134,11,0.12) !important;\n              margin-bottom: 12px !important;\n            }\n            #print-pertinence .print-card-silver {\n              background: #f8fafc !important;\n              border: 1.5px solid #94a3b8 !important;\n              margin-bottom: 10px !important;\n            }\n            #print-pertinence .print-card-bronze {\n              background: #fefaf5 !important;\n              border: 1.5px solid #cd7f32 !important;\n              margin-bottom: 10px !important;\n            }\n            #print-pertinence .print-card * { color: #1a1a1a !important; }\n            #print-pertinence .print-card-gold .print-medal-label { color: #8b6914 !important; }\n            #print-pertinence .print-card-silver .print-medal-label { color: #64748b !important; }\n            #print-pertinence .print-card-bronze .print-medal-label { color: #92400e !important; }\n            #print-pertinence .print-bar-bg { background: #e5e7eb !important; height: 6px !important; border-radius: 3px !important; }\n            #print-pertinence .print-bar-fill-green { background: #22c55e !important; }\n            #print-pertinence .print-bar-fill-gray { background: #9ca3af !important; }\n            #print-pertinence .print-bar-fill-red { background: #ef4444 !important; }\n            #print-pertinence .print-bar-fill-yellow { background: #f59e0b !important; }\n            #print-pertinence .print-score-green { color: #16a34a !important; }\n            #print-pertinence .print-score-yellow { color: #d97706 !important; }\n            #print-pertinence .print-score-red { color: #dc2626 !important; }\n            #print-pertinence .print-badge { border: 1px solid #666 !important; padding: 3px 8px !important; border-radius: 6px !important; font-size: 8px !important; font-weight: 700 !important; }\n            #print-pertinence .print-badge-green { background: #dcfce7 !important; color: #166534 !important; border-color: #22c55e !important; }\n            #print-pertinence .print-badge-orange { background: #fff7ed !important; color: #9a3412 !important; border-color: #f97316 !important; }\n            #print-pertinence .print-badge-red { background: #fef2f2 !important; color: #991b1b !important; border-color: #ef4444 !important; }\n            #print-pertinence .print-profile-box { border: 1.5px solid #f97316 !important; background: #fff7ed !important; padding: 12px !important; border-radius: 10px !important; }\n            #print-pertinence .print-warn { color: #dc2626 !important; }\n            #print-pertinence .print-verdict { color: #374151 !important; font-style: normal !important; }\n            #print-pertinence .print-deep-analysis { background: #f0f4ff !important; border: 1.5px solid #818cf8 !important; border-radius: 8px !important; padding: 10px 12px !important; margin-bottom: 12px !important; page-break-inside: avoid; break-inside: avoid; }\n            #print-pertinence .print-deep-analysis * { color: #374151 !important; }\n            #print-pertinence .print-smart-verdict { background: #f0fdf4 !important; border: 1.5px solid #22c55e !important; border-radius: 8px !important; padding: 10px 12px !important; margin-bottom: 12px !important; font-size: 10px !important; line-height: 1.7 !important; }\n            #print-pertinence .print-smart-verdict, #print-pertinence .print-smart-verdict * { color: #374151 !important; }\n            #print-pertinence .print-smart-verdict strong { color: #111827 !important; font-weight: 700 !important; }\n            #print-pertinence .print-smart-verdict p { margin: 0 !important; }\n            #print-pertinence .print-deep-analysis .deep-title { color: #4338ca !important; font-weight: 700 !important; font-size: 11px !important; margin-bottom: 6px !important; }\n            #print-pertinence .print-section-divider { border-top: 2px solid #e5e7eb !important; margin: 16px 0 10px !important; padding-top: 8px !important; }\n            #print-pertinence .print-footer-wrap { page-break-before: avoid; break-before: avoid; page-break-inside: avoid; break-inside: avoid; }\n            #print-pertinence .print-racket-img { width: 60px !important; height: 72px !important; object-fit: contain !important; border-radius: 8px !important; background: #f1f5f9 !important; padding: 4px !important; }\n            #print-pertinence .print-racket-img-sm { width: 40px !important; height: 48px !important; }\n            @page { margin: 8mm 6mm; size: A4; }\n          }\n        "), /*#__PURE__*/external_React_default().createElement("div", {
    id: "print-pertinence",
    style: S.card
  }, /*#__PURE__*/external_React_default().createElement("div", {
    className: "print-header",
    style: {
      display: "none",
      marginBottom: 14
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      alignItems: "flex-start",
      borderBottom: "3px solid #f97316",
      paddingBottom: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("svg", {
    width: "44",
    height: "44",
    viewBox: "0 0 44 44",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/external_React_default().createElement("rect", {
    width: "44",
    height: "44",
    rx: "10",
    fill: "#f97316"
  }), /*#__PURE__*/external_React_default().createElement("ellipse", {
    cx: "22",
    cy: "18",
    rx: "10",
    ry: "12",
    stroke: "#fff",
    strokeWidth: "2.2",
    fill: "none"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "10",
    x2: "22",
    y2: "26",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.5"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "14",
    y1: "18",
    x2: "30",
    y2: "18",
    stroke: "#fff",
    strokeWidth: "1.2",
    opacity: "0.5"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "30",
    x2: "22",
    y2: "38",
    stroke: "#fff",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }), /*#__PURE__*/external_React_default().createElement("circle", {
    cx: "33",
    cy: "32",
    r: "3.5",
    fill: "#fff",
    opacity: "0.9"
  })), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 800,
      color: "#f97316",
      fontFamily: "'Outfit'",
      letterSpacing: "-0.01em"
    }
  }, "PADEL ANALYZER"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 9,
      color: "#64748b",
      marginTop: 1
    }
  }, "Analyse de pertinence personnalis\xE9e"))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      textAlign: "right"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 12,
      fontWeight: 700,
      color: "#1a1a1a"
    }
  }, profileName || "Analyse joueur"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 9,
      color: "#64748b",
      marginTop: 2
    }
  }, new Date().toLocaleDateString('fr-FR', {
    day: 'numeric',
    month: 'long',
    year: 'numeric'
  }))))), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      marginBottom: 10,
      alignItems: "center"
    },
    className: "no-print"
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      flex: 1,
      fontSize: 12,
      fontWeight: 700,
      color: "#e2e8f0"
    }
  }, profileName ? "\uD83D\uDC64 ".concat(profileName) : /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      color: "#475569",
      fontWeight: 400,
      fontSize: 11
    }
  }, "D\xE9finis un nom dans \uD83D\uDC64 Profil \u2192 \uD83D\uDCBE Sauvegarder")), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      return window.print();
    },
    style: {
      padding: "8px 16px",
      background: "rgba(249,115,22,0.2)",
      border: "1px solid #f97316",
      borderRadius: 8,
      color: "#f97316",
      fontSize: 12,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "inherit",
      whiteSpace: "nowrap"
    }
  }, "\uD83D\uDDA8 Imprimer")), /*#__PURE__*/external_React_default().createElement("div", {
    className: "print-profile-box",
    style: {
      background: "rgba(249,115,22,0.06)",
      border: "1px solid rgba(249,115,22,0.15)",
      borderRadius: 10,
      padding: 12,
      marginBottom: 12,
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 10,
      color: "#f97316",
      fontWeight: 700,
      margin: "0 0 3px"
    }
  }, "\uD83D\uDC64 Profil actif :"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 9,
      color: "#94a3b8",
      margin: 0,
      lineHeight: 1.5
    }
  }, profileText), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 8,
      color: "#475569",
      margin: "4px 0 0"
    }
  }, function () {
    var w = {
      Puissance: 1,
      Contrôle: 1,
      Confort: 1,
      Spin: 1,
      Maniabilité: 1,
      Tolérance: 1
    };
    var prioMap = {
      confort: {
        Confort: 1.5
      },
      polyvalence: {
        Contrôle: 0.5,
        Maniabilité: 0.5,
        Tolérance: 0.5
      },
      puissance: {
        Puissance: 1.5
      },
      controle: {
        Contrôle: 1.5
      },
      spin: {
        Spin: 1.5
      },
      legerete: {
        Maniabilité: 1.5
      },
      protection: {
        Confort: 1.5
      },
      reprise: {
        Confort: 1.5,
        Tolérance: 1.0,
        Maniabilité: 0.5
      }
    };
    var styleMap = {
      offensif: {
        Puissance: 0.5
      },
      defensif: {
        Contrôle: 0.5,
        Tolérance: 0.5
      },
      tactique: {
        Contrôle: 0.5,
        Maniabilité: 0.3
      },
      puissant: {
        Puissance: 0.5,
        Spin: 0.3
      },
      veloce: {
        Maniabilité: 0.8
      },
      endurant: {
        Confort: 0.5,
        Tolérance: 0.3
      },
      contre: {
        Tolérance: 0.5,
        Contrôle: 0.3
      },
      polyvalent: {
        Contrôle: 0.3,
        Tolérance: 0.3
      },
      technique: {
        Contrôle: 0.5,
        Spin: 0.3
      }
    };
    var _iterator6 = _createForOfIteratorHelper(profile.priorityTags || []),
      _step6;
    try {
      for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
        var tag = _step6.value;
        var b = prioMap[tag];
        if (b) for (var _i5 = 0, _Object$entries5 = Object.entries(b); _i5 < _Object$entries5.length; _i5++) {
          var _Object$entries5$_i = _slicedToArray(_Object$entries5[_i5], 2),
            k = _Object$entries5$_i[0],
            v = _Object$entries5$_i[1];
          w[k] = (w[k] || 1) + v;
        }
      }
    } catch (err) {
      _iterator6.e(err);
    } finally {
      _iterator6.f();
    }
    var _iterator7 = _createForOfIteratorHelper(profile.styleTags || []),
      _step7;
    try {
      for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
        var _tag3 = _step7.value;
        var _b = styleMap[_tag3];
        if (_b) for (var _i6 = 0, _Object$entries6 = Object.entries(_b); _i6 < _Object$entries6.length; _i6++) {
          var _Object$entries6$_i = _slicedToArray(_Object$entries6[_i6], 2),
            _k3 = _Object$entries6$_i[0],
            _v3 = _Object$entries6$_i[1];
          w[_k3] = (w[_k3] || 1) + _v3;
        }
      }
    } catch (err) {
      _iterator7.e(err);
    } finally {
      _iterator7.f();
    }
    var ARM = ["dos", "poignet", "coude", "epaule"];
    var LEG = ["genou", "cheville", "mollet", "hanche", "achille"];
    if ((profile.injuryTags || []).some(function (t) {
      return ARM.includes(t);
    })) w.Confort += 2;
    if ((profile.injuryTags || []).some(function (t) {
      return LEG.includes(t);
    })) w.Maniabilité += 1.5;
    var h = Number(profile.height) || 0;
    if (h > 0 && h < 170) w.Maniabilité += 0.5;
    if (h >= 185) w.Puissance += 0.3;
    var age = Number(profile.age) || 0;
    if (age >= 40) {
      w.Confort += 0.5;
      w.Tolérance += 0.3;
    }
    if (age >= 50) {
      w.Confort += 0.5;
      w.Maniabilité += 0.5;
      w.Tolérance += 0.3;
    }
    if (age >= 60) {
      w.Confort += 0.5;
      w.Tolérance += 0.5;
    }
    var hand = profile.hand || "Droitier";
    var side = profile.side || "Droite";
    var isAtk = hand === "Droitier" && side === "Gauche" || hand === "Gaucher" && side === "Droite";
    var isCon = hand === "Droitier" && side === "Droite" || hand === "Gaucher" && side === "Gauche";
    if (isAtk) {
      w.Puissance += 0.5;
      w.Spin += 0.3;
    }
    if (isCon) {
      w.Contrôle += 0.5;
      w.Tolérance += 0.3;
    }
    var sorted = Object.entries(w).sort(function (a, b) {
      return b[1] - a[1];
    });
    var top = sorted.filter(function (_ref20) {
      var _ref21 = _slicedToArray(_ref20, 2),
        v = _ref21[1];
      return v > 1.5;
    }).map(function (_ref22) {
      var _ref23 = _slicedToArray(_ref22, 2),
        k = _ref23[0],
        v = _ref23[1];
      return "".concat(k, " ~").concat(Math.round(v), "x");
    });
    return top.length ? "Crit\xE8res renforc\xE9s : ".concat(top.join(", "), ".") : "";
  }())), function () {
    try {
      var _ranked$0$name;
      var w2 = {
        Puissance: 1,
        Contrôle: 1,
        Confort: 1,
        Spin: 1,
        Maniabilité: 1,
        Tolérance: 1
      };
      var prioMap2 = {
        confort: {
          Confort: 1.5
        },
        polyvalence: {
          Contrôle: 0.5,
          Maniabilité: 0.5,
          Tolérance: 0.5
        },
        puissance: {
          Puissance: 1.5
        },
        controle: {
          Contrôle: 1.5
        },
        spin: {
          Spin: 1.5
        },
        legerete: {
          Maniabilité: 1.5
        },
        protection: {
          Confort: 1.5
        },
        reprise: {
          Confort: 1.5,
          Tolérance: 1.0,
          Maniabilité: 0.5
        }
      };
      var styleMap2 = {
        offensif: {
          Puissance: 0.5
        },
        defensif: {
          Contrôle: 0.5,
          Tolérance: 0.5
        },
        tactique: {
          Contrôle: 0.5,
          Maniabilité: 0.3
        },
        puissant: {
          Puissance: 0.5,
          Spin: 0.3
        },
        veloce: {
          Maniabilité: 0.8
        },
        endurant: {
          Confort: 0.5,
          Tolérance: 0.3
        },
        contre: {
          Tolérance: 0.5,
          Contrôle: 0.3
        },
        polyvalent: {
          Contrôle: 0.3,
          Tolérance: 0.3
        },
        technique: {
          Contrôle: 0.5,
          Spin: 0.3
        }
      };
      var _iterator8 = _createForOfIteratorHelper(profile.priorityTags || []),
        _step8;
      try {
        for (_iterator8.s(); !(_step8 = _iterator8.n()).done;) {
          var tag = _step8.value;
          var b = prioMap2[tag];
          if (b) for (var _i7 = 0, _Object$entries7 = Object.entries(b); _i7 < _Object$entries7.length; _i7++) {
            var _Object$entries7$_i = _slicedToArray(_Object$entries7[_i7], 2),
              k = _Object$entries7$_i[0],
              v = _Object$entries7$_i[1];
            w2[k] = (w2[k] || 1) + v;
          }
        }
      } catch (err) {
        _iterator8.e(err);
      } finally {
        _iterator8.f();
      }
      var _iterator9 = _createForOfIteratorHelper(profile.styleTags || []),
        _step9;
      try {
        for (_iterator9.s(); !(_step9 = _iterator9.n()).done;) {
          var _tag4 = _step9.value;
          var _b2 = styleMap2[_tag4];
          if (_b2) for (var _i8 = 0, _Object$entries8 = Object.entries(_b2); _i8 < _Object$entries8.length; _i8++) {
            var _Object$entries8$_i = _slicedToArray(_Object$entries8[_i8], 2),
              _k4 = _Object$entries8$_i[0],
              _v4 = _Object$entries8$_i[1];
            w2[_k4] = (w2[_k4] || 1) + _v4;
          }
        }
      } catch (err) {
        _iterator9.e(err);
      } finally {
        _iterator9.f();
      }
      var maxW2 = Math.max.apply(Math, _toConsumableArray(Object.values(w2)));
      var idealRadar2 = ATTRS.map(function (a) {
        return {
          attribute: a,
          "Raquette idéale": Math.round(w2[a] / maxW2 * 10 * 10) / 10
        };
      });

      // Best racket overlay
      var ranked = rackets.map(function (r) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
          globalScore: computeGlobalScore(r.scores, profile)
        });
      }).sort(function (a, b) {
        return b.globalScore - a.globalScore;
      });
      var bestShort = ranked.length > 0 ? ranked[0].shortName || ((_ranked$0$name = ranked[0].name) === null || _ranked$0$name === void 0 ? void 0 : _ranked$0$name.slice(0, 28)) || "N°1" : "";
      if (ranked.length > 0) {
        idealRadar2.forEach(function (pt) {
          var _ranked$0$scores;
          pt["🥇 " + bestShort] = Number((_ranked$0$scores = ranked[0].scores) === null || _ranked$0$scores === void 0 ? void 0 : _ranked$0$scores[pt.attribute]) || 0;
        });
      }
      return /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          gap: 12,
          marginBottom: 12,
          alignItems: "stretch"
        },
        className: "print-radar-section"
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          flex: "1 1 50%",
          background: "rgba(99,102,241,0.04)",
          border: "1px solid rgba(99,102,241,0.12)",
          borderRadius: 10,
          padding: "8px 0 0",
          minHeight: 200,
          display: "flex",
          flexDirection: "column",
          alignItems: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          fontWeight: 700,
          color: "#a5b4fc",
          textAlign: "center",
          textTransform: "uppercase",
          letterSpacing: "0.05em",
          marginBottom: 2
        }
      }, "\uD83D\uDCCA Raquette id\xE9ale", ranked.length > 0 ? " vs ".concat(bestShort) : ""), /*#__PURE__*/external_React_default().createElement(RadarChart, {
        width: 340,
        height: 220,
        data: idealRadar2,
        margin: {
          top: 10,
          right: 50,
          bottom: 10,
          left: 50
        }
      }, /*#__PURE__*/external_React_default().createElement(PolarGrid, {
        stroke: "rgba(255,255,255,0.08)"
      }), /*#__PURE__*/external_React_default().createElement(PolarAngleAxis, {
        dataKey: "attribute",
        tick: {
          fill: "#94a3b8",
          fontSize: 8
        }
      }), /*#__PURE__*/external_React_default().createElement(PolarRadiusAxis, {
        angle: 90,
        domain: [0, 10],
        tick: false,
        axisLine: false
      }), /*#__PURE__*/external_React_default().createElement(Radar, {
        name: "Raquette id\xE9ale",
        dataKey: "Raquette id\xE9ale",
        stroke: "#6366f1",
        fill: "#6366f1",
        fillOpacity: 0.12,
        strokeWidth: 2,
        strokeDasharray: "6 3"
      }), ranked.length > 0 && /*#__PURE__*/external_React_default().createElement(Radar, {
        name: "🥇 " + bestShort,
        dataKey: "🥇 " + bestShort,
        stroke: "#f97316",
        fill: "#f97316",
        fillOpacity: 0.15,
        strokeWidth: 2
      }), /*#__PURE__*/external_React_default().createElement(Legend, {
        wrapperStyle: {
          fontSize: 8,
          color: "#94a3b8",
          paddingTop: 2
        }
      }))), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          flex: "1 1 50%",
          display: "flex",
          flexDirection: "column",
          gap: 8
        }
      }, ranked.slice(0, 3).map(function (r, i) {
        var medals = ["🥇", "🥈", "🥉"];
        var gs = r.globalScore;
        var fy2 = computeForYou(r.scores, profile);
        var fyC = fy2 === "recommended" ? "#22c55e" : fy2 === "no" ? "#ef4444" : "#FF9800";
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: r.id,
          style: {
            display: "flex",
            alignItems: "center",
            gap: 8,
            padding: "8px 10px",
            background: i === 0 ? "rgba(249,115,22,0.06)" : "rgba(255,255,255,0.02)",
            border: "1px solid ".concat(i === 0 ? "rgba(249,115,22,0.2)" : "rgba(255,255,255,0.06)"),
            borderRadius: 10,
            flex: 1
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: 18
          }
        }, medals[i]), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
          src: proxyImg(r.imageUrl),
          alt: "",
          className: "print-racket-img-sm",
          style: {
            width: 32,
            height: 40,
            objectFit: "contain",
            borderRadius: 6,
            background: "rgba(255,255,255,0.06)",
            flexShrink: 0
          },
          onError: function onError(e) {
            e.target.style.display = 'none';
          }
        }), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            flex: 1,
            minWidth: 0
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 10,
            fontWeight: 700,
            color: "#e2e8f0",
            whiteSpace: "nowrap",
            overflow: "hidden",
            textOverflow: "ellipsis"
          }
        }, r.shortName), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 8,
            color: "#64748b"
          }
        }, r.brand, " \xB7 ", r.shape)), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            textAlign: "right",
            flexShrink: 0
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 14,
            fontWeight: 800,
            color: gs >= 7.5 ? "#4ade80" : gs >= 6.5 ? "#fbbf24" : "#f87171",
            fontFamily: "'Outfit'",
            lineHeight: 1
          }
        }, fmtPct(gs)), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 7,
            color: fyC,
            fontWeight: 700,
            marginTop: 2
          }
        }, fy2 === "recommended" ? "RECO" : fy2 === "no" ? "NON" : "JOUABLE")));
      })));
    } catch (e) {
      console.error("[Pertinence:radar]", e);
      return null;
    }
  }(), function () {
    try {
      var ranked = rackets.map(function (r) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
          globalScore: computeGlobalScore(r.scores, profile)
        });
      }).sort(function (a, b) {
        return b.globalScore - a.globalScore;
      });
      if (!ranked.length) return null;
      var best = ranked[0];
      var second = ranked[1];

      // Priority attribute mapping
      var prioAttrMap = {
        puissance: 'Puissance',
        controle: 'Contrôle',
        confort: 'Confort',
        spin: 'Spin',
        legerete: 'Maniabilité',
        protection: 'Confort',
        reprise: 'Confort',
        polyvalence: 'Contrôle'
      };
      var prioTags = profile.priorityTags || [];
      var prioAttrs = _toConsumableArray(new Set(prioTags.map(function (t) {
        return prioAttrMap[t];
      }).filter(Boolean)));
      var prioLabels = prioTags.map(function (id) {
        var _PRIORITY_TAGS$find9;
        return (_PRIORITY_TAGS$find9 = PRIORITY_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _PRIORITY_TAGS$find9 === void 0 ? void 0 : _PRIORITY_TAGS$find9.label;
      }).filter(Boolean);

      // Injury context
      var ARM_INJ = ["dos", "poignet", "coude", "epaule"];
      var LEG_INJ = ["genou", "cheville", "mollet", "hanche", "achille"];
      var injTags = profile.injuryTags || [];
      var hasArmInj = injTags.some(function (t) {
        return ARM_INJ.includes(t);
      });
      var hasLegInj = injTags.some(function (t) {
        return LEG_INJ.includes(t);
      });
      var injLabels = injTags.filter(function (t) {
        return t !== "aucune";
      }).map(function (id) {
        var _INJURY_TAGS$find6;
        return (_INJURY_TAGS$find6 = INJURY_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _INJURY_TAGS$find6 === void 0 ? void 0 : _INJURY_TAGS$find6.label;
      }).filter(Boolean);

      // Find best's top 2 priority scores
      var bestPrioScores = prioAttrs.map(function (a) {
        return {
          attr: a,
          val: best.scores[a] || 0
        };
      }).sort(function (a, b) {
        return b.val - a.val;
      });

      // Find best's weakest score overall
      var bestWeakest = ATTRS.map(function (a) {
        return {
          attr: a,
          val: best.scores[a] || 0
        };
      }).sort(function (a, b) {
        return a.val - b.val;
      })[0];

      // Gap with #2
      var gap = second ? best.globalScore - second.globalScore : 0;

      // Build the verdict lines
      var lines = [];

      // Line 1: The recommendation with why
      if (prioLabels.length > 0) {
        var prioStr = prioLabels.length === 1 ? prioLabels[0] : prioLabels.slice(0, -1).join(", ") + " et " + prioLabels[prioLabels.length - 1];
        if (bestPrioScores.length > 0 && bestPrioScores[0].val >= 7.5) {
          lines.push("Avec tes priorit\xE9s ".concat(prioStr, ", la **").concat(best.name, "** (").concat(fmtPct(best.globalScore), ") s'impose : elle affiche ").concat(bestPrioScores.map(function (s) {
            return "".concat(s.attr, " \xE0 ").concat(s.val);
          }).join(", "), "."));
        } else if (bestPrioScores.length > 0) {
          lines.push("Pour tes priorit\xE9s ".concat(prioStr, ", la **").concat(best.name, "** (").concat(fmtPct(best.globalScore), ") offre le meilleur compromis du lot avec ").concat(bestPrioScores.map(function (s) {
            return "".concat(s.attr, " \xE0 ").concat(s.val);
          }).join(", "), "."));
        } else {
          lines.push("La **".concat(best.name, "** (").concat(fmtPct(best.globalScore), ") est la plus adapt\xE9e \xE0 ton profil."));
        }
      } else {
        lines.push("La **".concat(best.name, "** (").concat(fmtPct(best.globalScore), ") est la plus adapt\xE9e \xE0 ton profil."));
      }

      // Line 2: Injury warning or comfort note
      if (hasArmInj) {
        var comfortBest = best.scores.Confort || 0;
        if (comfortBest >= 7.5) {
          lines.push("Bon point pour ton ".concat(injLabels.join("/"), " : son Confort \xE0 ").concat(comfortBest, " devrait m\xE9nager tes articulations."));
        } else if (comfortBest >= 6) {
          lines.push("Attention ".concat(injLabels.join("/"), " : son Confort \xE0 ").concat(comfortBest, " est correct mais pas optimal \u2014 surveille tes sensations."));
        } else {
          lines.push("\u26A0 Point de vigilance ".concat(injLabels.join("/"), " : Confort \xE0 ").concat(comfortBest, " seulement \u2014 risque de douleurs sur longues sessions."));
        }
      } else if (hasLegInj) {
        var manBest = best.scores["Maniabilité"] || 0;
        if (manBest >= 7) {
          lines.push("Avec ta fragilit\xE9 ".concat(injLabels.join("/"), ", sa Maniabilit\xE9 \xE0 ").concat(manBest, " te permet de compenser sans forcer."));
        } else {
          lines.push("Attention ".concat(injLabels.join("/"), " : sa Maniabilit\xE9 \xE0 ").concat(manBest, " demandera plus d'efforts en d\xE9placement."));
        }
      }

      // Line 3: Comparison with #2 (if exists and meaningful gap)
      if (second) {
        if (gap >= 0.5) {
          // Clear winner — find what #2 lacks
          var secondWorse = prioAttrs.length > 0 ? prioAttrs.map(function (a) {
            return {
              attr: a,
              diff: (best.scores[a] || 0) - (second.scores[a] || 0)
            };
          }).filter(function (d) {
            return d.diff > 0;
          }).sort(function (a, b) {
            return b.diff - a.diff;
          })[0] : ATTRS.map(function (a) {
            return {
              attr: a,
              diff: (best.scores[a] || 0) - (second.scores[a] || 0)
            };
          }).filter(function (d) {
            return d.diff > 0;
          }).sort(function (a, b) {
            return b.diff - a.diff;
          })[0];
          if (secondWorse) {
            lines.push("La **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") suit mais perd du terrain en ").concat(secondWorse.attr, " (").concat(second.scores[secondWorse.attr], " vs ").concat(best.scores[secondWorse.attr], ")."));
          } else {
            lines.push("La **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") est une alternative solide."));
          }
        } else if (gap >= 0.2) {
          // Close call — find what #2 does better
          var secondBetter = ATTRS.map(function (a) {
            return {
              attr: a,
              diff: (second.scores[a] || 0) - (best.scores[a] || 0)
            };
          }).filter(function (d) {
            return d.diff > 0;
          }).sort(function (a, b) {
            return b.diff - a.diff;
          })[0];
          if (secondBetter) {
            lines.push("La **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") talonne de pr\xE8s et pousse m\xEAme plus fort en ").concat(secondBetter.attr, " (").concat(second.scores[secondBetter.attr], " vs ").concat(best.scores[secondBetter.attr], ") \u2014 \xE0 essayer aussi."));
          } else {
            lines.push("La **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") est au coude-\xE0-coude \u2014 les deux m\xE9ritent un essai."));
          }
        } else {
          // Virtually tied — find what differentiates them
          var secondBetter2 = ATTRS.map(function (a) {
            return {
              attr: a,
              diff: (second.scores[a] || 0) - (best.scores[a] || 0)
            };
          }).filter(function (d) {
            return d.diff > 0;
          }).sort(function (a, b) {
            return b.diff - a.diff;
          })[0];
          var bestBetter2 = ATTRS.map(function (a) {
            return {
              attr: a,
              diff: (best.scores[a] || 0) - (second.scores[a] || 0)
            };
          }).filter(function (d) {
            return d.diff > 0;
          }).sort(function (a, b) {
            return b.diff - a.diff;
          })[0];
          if (secondBetter2 && bestBetter2) {
            lines.push("Quasi ex-\xE6quo avec la **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") \u2014 elle pousse en ").concat(secondBetter2.attr, " (").concat(second.scores[secondBetter2.attr], "), la n\xB01 domine en ").concat(bestBetter2.attr, " (").concat(best.scores[bestBetter2.attr], "). Deux profils compl\xE9mentaires \xE0 tester."));
          } else {
            lines.push("Quasi ex-\xE6quo avec la **".concat(second.name, "** (").concat(fmtPct(second.globalScore), ") \u2014 profils tr\xE8s proches, la diff\xE9rence se jouera au toucher et \xE0 l'\xE9quilibre en main."));
          }
        }
      }

      // Render as plain text — no inline <strong> tags that Chrome PDF fragments
      var plainText = lines.map(function (l) {
        return l.replace(/\*\*/g, "");
      }).join(" ");
      return /*#__PURE__*/external_React_default().createElement("div", {
        className: "print-smart-verdict",
        style: {
          background: "rgba(34,197,94,0.06)",
          border: "1px solid rgba(34,197,94,0.15)",
          borderRadius: 8,
          padding: "10px 12px",
          marginBottom: 12,
          fontSize: 10,
          color: "#94a3b8",
          lineHeight: 1.6
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontWeight: 700,
          color: "#4ade80",
          marginBottom: 4,
          fontSize: 11
        }
      }, "\uD83C\uDFAF Notre verdict"), /*#__PURE__*/external_React_default().createElement("p", {
        style: {
          margin: 0
        }
      }, plainText));
    } catch (e) {
      console.error("[Pertinence:verdict]", e);
      return null;
    }
  }(), function () {
    try {
      var ranked = rackets.map(function (r) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
          globalScore: computeGlobalScore(r.scores, profile)
        });
      }).sort(function (a, b) {
        return b.globalScore - a.globalScore;
      });
      if (ranked.length < 2) return null;
      var deepLines = generateDeepAnalysis(profile, ranked, ATTRS);
      if (!deepLines.length) return null;
      var renderBold = function renderBold(text, idx) {
        var parts = text.split(/(\*\*[^*]+\*\*)/g);
        return /*#__PURE__*/external_React_default().createElement("p", {
          key: idx,
          style: {
            margin: "0 0 6px",
            fontSize: 9,
            color: "#94a3b8",
            lineHeight: 1.6
          }
        }, parts.map(function (p, j) {
          return p.startsWith("**") ? /*#__PURE__*/external_React_default().createElement("strong", {
            key: j,
            style: {
              color: "#e2e8f0"
            }
          }, p.replace(/\*\*/g, "")) : p;
        }));
      };
      return /*#__PURE__*/external_React_default().createElement("div", {
        className: "print-deep-analysis",
        style: {
          background: "rgba(99,102,241,0.05)",
          border: "1px solid rgba(99,102,241,0.12)",
          borderRadius: 8,
          padding: "10px 12px",
          marginBottom: 12
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        className: "deep-title",
        style: {
          fontWeight: 700,
          color: "#a5b4fc",
          marginBottom: 6,
          fontSize: 11
        }
      }, "\uD83D\uDD2C Analyse du profil"), deepLines.map(function (l, i) {
        return renderBold(l, i);
      }));
    } catch (e) {
      console.error("[Pertinence:deep]", e);
      return null;
    }
  }(), rackets.length >= 3 && /*#__PURE__*/external_React_default().createElement("div", {
    className: "print-section-title",
    style: {
      display: "none",
      fontSize: 12,
      fontWeight: 800,
      color: "#1a1a1a",
      marginBottom: 8,
      paddingBottom: 4
    }
  }, "\uD83C\uDFC6 PODIUM \u2014 Top 3"), function () {
    try {
      var ranked = rackets.map(function (r) {
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
          globalScore: computeGlobalScore(r.scores, profile)
        });
      }).sort(function (a, b) {
        return b.globalScore - a.globalScore;
      });
      var cards = [];
      ranked.forEach(function (r, i) {
        // Insert section divider after top 3
        if (i === 3 && ranked.length > 3) {
          cards.push(/*#__PURE__*/external_React_default().createElement("div", {
            key: "divider",
            className: "print-section-divider",
            style: {
              borderTop: "2px solid rgba(255,255,255,0.06)",
              margin: "16px 0 10px",
              paddingTop: 8
            }
          }, /*#__PURE__*/external_React_default().createElement("div", {
            className: "print-section-title",
            style: {
              display: "none",
              fontSize: 11,
              fontWeight: 700,
              color: "#64748b",
              marginBottom: 4
            }
          }, "\uD83D\uDCCB AUTRES RAQUETTES ANALYS\xC9ES")));
        }
        var forYouVal = computeForYou(r.scores, profile);
        var fy = fyConfig[forYouVal] || fyConfig.partial;
        var gs = r.globalScore;
        var medal = i === 0 ? "🥇" : i === 1 ? "🥈" : i === 2 ? "🥉" : "";
        var cardClass = "print-card" + (i === 0 ? " print-card-gold" : i === 1 ? " print-card-silver" : i === 2 ? " print-card-bronze" : "");
        var badgeClass = forYouVal === "recommended" ? "print-badge-green" : forYouVal === "no" ? "print-badge-red" : "print-badge-orange";
        var scoreClass = gs >= 7.5 ? "print-score-green" : gs >= 6.5 ? "print-score-yellow" : "print-score-red";
        var ARM_INJ = ["dos", "poignet", "coude", "epaule"];
        var LEG_INJ = ["genou", "cheville", "mollet", "hanche", "achille"];
        var ptags = profile.injuryTags || [];
        var hasArmInj = ptags.some(function (t) {
          return ARM_INJ.includes(t);
        });
        var hasLegInj = ptags.some(function (t) {
          return LEG_INJ.includes(t);
        });
        var criticalLow = hasArmInj && r.scores.Confort < 7;
        var isPodium = i < 3;
        cards.push(/*#__PURE__*/external_React_default().createElement("div", {
          key: r.id,
          className: cardClass,
          style: {
            background: i === 0 ? "rgba(250,204,21,0.08)" : i === 1 ? "rgba(148,163,184,0.06)" : i === 2 ? "rgba(217,119,6,0.06)" : "rgba(255,255,255,0.02)",
            border: i === 0 ? "2px solid rgba(250,204,21,0.5)" : i === 1 ? "2px solid rgba(148,163,184,0.4)" : i === 2 ? "2px solid rgba(217,119,6,0.35)" : "1px solid rgba(255,255,255,0.06)",
            borderRadius: 12,
            padding: isPodium ? "14px 16px" : "10px 12px",
            marginBottom: isPodium ? 12 : 6,
            boxSizing: "border-box",
            overflow: "hidden",
            pageBreakInside: "avoid",
            breakInside: "avoid"
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            alignItems: "center",
            gap: isPodium ? 12 : 8,
            marginBottom: isPodium ? 10 : 6
          }
        }, medal && /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            flexShrink: 0,
            minWidth: 40
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: i === 0 ? 32 : 26,
            lineHeight: 1
          }
        }, medal), /*#__PURE__*/external_React_default().createElement("span", {
          className: "print-medal-label",
          style: {
            fontSize: 7,
            fontWeight: 800,
            letterSpacing: "0.02em",
            color: i === 0 ? "#b8860b" : i === 1 ? "#6b7280" : "#92400e",
            marginTop: 2,
            whiteSpace: "nowrap"
          }
        }, i === 0 ? "MEILLEUR" : i === 1 ? "2ᵉ choix" : "3ᵉ choix")), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
          src: proxyImg(r.imageUrl),
          alt: "",
          className: isPodium ? "print-racket-img" : "print-racket-img-sm",
          style: {
            width: isPodium ? 60 : 36,
            height: isPodium ? 72 : 44,
            objectFit: "contain",
            borderRadius: isPodium ? 10 : 6,
            flexShrink: 0,
            background: isPodium ? "rgba(255,255,255,0.06)" : "rgba(255,255,255,0.04)",
            padding: isPodium ? 4 : 2,
            border: isPodium ? "1px solid ".concat(i === 0 ? "rgba(250,204,21,0.3)" : i === 1 ? "rgba(148,163,184,0.2)" : "rgba(217,119,6,0.2)") : "none"
          },
          onError: function onError(e) {
            e.target.style.display = 'none';
          }
        }), !medal && !r.imageUrl && /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            width: 10,
            height: 10,
            borderRadius: "50%",
            background: r.color,
            border: "1px solid #999",
            flexShrink: 0,
            printColorAdjust: "exact",
            WebkitPrintColorAdjust: "exact"
          }
        }), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            flex: 1,
            minWidth: 0
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            alignItems: "center",
            gap: 6,
            flexWrap: "wrap"
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: i === 0 ? 15 : isPodium ? 13 : 11,
            fontWeight: 800,
            color: "#e2e8f0"
          }
        }, r.name), /*#__PURE__*/external_React_default().createElement("span", {
          className: "print-badge ".concat(badgeClass),
          style: {
            background: fy.bg,
            border: "1px solid ".concat(fy.border),
            borderRadius: 6,
            padding: "3px 8px",
            fontSize: 7,
            fontWeight: 700,
            color: "#fff",
            flexShrink: 0,
            whiteSpace: "nowrap"
          }
        }, fy.text)), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: isPodium ? 9 : 8,
            color: "#64748b",
            marginTop: 3
          }
        }, r.shape, " \xB7 ", r.weight, " \xB7 ", r.brand, r.player && r.player !== "—" ? " \xB7 \uD83C\uDFBE ".concat(r.player) : "", r.price && r.price !== "—" ? " \xB7 ".concat(r.price) : "")), /*#__PURE__*/external_React_default().createElement("div", {
          className: scoreClass,
          style: {
            fontSize: i === 0 ? 28 : isPodium ? 22 : 16,
            fontWeight: 800,
            color: gs >= 7.5 ? "#4ade80" : gs >= 6.5 ? "#fbbf24" : "#f87171",
            fontFamily: "'Outfit'",
            lineHeight: 1,
            flexShrink: 0,
            marginLeft: 8
          }
        }, fmtPct(gs))), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "grid",
            gridTemplateColumns: "repeat(3,1fr)",
            gap: isPodium ? "6px 14px" : "4px 10px",
            marginBottom: isPodium ? 8 : 5
          }
        }, ATTRS.map(function (attr) {
          var v = r.scores[attr];
          var isKey = attr === "Confort" && hasArmInj || attr === "Maniabilité" && (hasLegInj || (profile.styleTags || []).includes("veloce")) || (profile.priorityTags || []).some(function (t) {
            return t === "confort" && attr === "Confort" || t === "polyvalence" && ["Contrôle", "Tolérance", "Maniabilité"].includes(attr) || t === "puissance" && attr === "Puissance" || t === "controle" && attr === "Contrôle" || t === "spin" && attr === "Spin" || t === "legerete" && attr === "Maniabilité" || t === "protection" && attr === "Confort" || t === "reprise" && ["Confort", "Tolérance", "Maniabilité"].includes(attr);
          });
          var low = hasArmInj && attr === "Confort" && v < 7;
          var barH = isPodium ? 7 : 4;
          var barClass = low ? "print-bar-fill-red" : v >= 7.5 ? "print-bar-fill-green" : v >= 6 ? "print-bar-fill-gray" : "print-bar-fill-yellow";
          return /*#__PURE__*/external_React_default().createElement("div", {
            key: attr,
            style: {
              minWidth: 0
            }
          }, /*#__PURE__*/external_React_default().createElement("div", {
            style: {
              display: "flex",
              justifyContent: "space-between",
              alignItems: "center"
            }
          }, /*#__PURE__*/external_React_default().createElement("span", {
            style: {
              fontSize: isPodium ? 9 : 8,
              color: isKey ? "#f97316" : "#64748b",
              fontWeight: isKey ? 700 : 500
            }
          }, isKey ? "★ " : "", attr), /*#__PURE__*/external_React_default().createElement("span", {
            style: {
              fontSize: isPodium ? 10 : 9,
              color: low ? "#f87171" : v >= 7.5 ? "#4ade80" : v >= 6 ? "#cbd5e1" : "#fbbf24",
              fontWeight: 700,
              flexShrink: 0,
              marginLeft: 4
            }
          }, v)), /*#__PURE__*/external_React_default().createElement("div", {
            className: "print-bar-bg",
            style: {
              height: barH,
              background: "rgba(255,255,255,0.06)",
              borderRadius: barH / 2,
              marginTop: 2
            }
          }, /*#__PURE__*/external_React_default().createElement("div", {
            className: barClass,
            style: {
              height: barH,
              borderRadius: barH / 2,
              width: "".concat(v * 10, "%"),
              background: low ? "#f87171" : v >= 7.5 ? "#4ade80" : v >= 6 ? "#64748b" : "#fbbf24",
              printColorAdjust: "exact",
              WebkitPrintColorAdjust: "exact"
            }
          })));
        })), criticalLow && /*#__PURE__*/external_React_default().createElement("div", {
          className: "print-warn",
          style: {
            fontSize: 9,
            color: "#f87171",
            fontWeight: 600,
            marginBottom: 3
          }
        }, "\u26A0 Confort insuffisant (", r.scores.Confort, "/10) pour blessures ", ptags.filter(function (t) {
          return ARM_INJ.includes(t);
        }).map(function (t) {
          return {
            dos: "Dos",
            poignet: "Poignet",
            coude: "Coude",
            epaule: "Épaule"
          }[t];
        }).join("/"), " \u2014 risque de douleurs"), /*#__PURE__*/external_React_default().createElement("div", {
          className: "print-verdict",
          style: {
            fontSize: isPodium ? 10 : 9,
            color: "#94a3b8",
            lineHeight: 1.6
          }
        }, r.verdict)));
      });
      return cards;
    } catch (e) {
      console.error("[Pertinence:ranking]", e);
      return null;
    }
  }(), function () {
    try {
      var prioTagIds = profile.priorityTags || [];
      if (!prioTagIds.length || !rackets.length) return null;
      var prioAttrMap = {
        puissance: 'Puissance',
        controle: 'Contrôle',
        confort: 'Confort',
        spin: 'Spin',
        legerete: 'Maniabilité',
        protection: 'Confort',
        reprise: 'Confort',
        polyvalence: 'Contrôle'
      };
      var prioAttrs = _toConsumableArray(new Set(prioTagIds.map(function (t) {
        return prioAttrMap[t];
      }).filter(Boolean)));
      var prioLabels = prioTagIds.map(function (id) {
        var _PRIORITY_TAGS$find0;
        return (_PRIORITY_TAGS$find0 = PRIORITY_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _PRIORITY_TAGS$find0 === void 0 ? void 0 : _PRIORITY_TAGS$find0.label;
      }).filter(Boolean);
      if (!prioAttrs.length) return null;

      // Get existing racket names to exclude (normalized) — IDs don't match because user rackets have timestamp suffixes
      var existingNames = new Set(rackets.map(function (r) {
        return (r.name || "").toLowerCase().trim();
      }));

      // Filter DB pool by level category (same logic as matchFromDB)
      var age = Number(profile.age) || 0;
      var ht = Number(profile.height) || 0;
      var isJunior = age > 0 && age < 15 || ht > 0 && ht < 150;
      var pool;
      if (isJunior) {
        pool = rackets_db_namespaceObject.filter(function (r) {
          return r.category === 'junior';
        });
      } else {
        var lvl = profile.level || 'Débutant';
        var catMap = {
          'Débutant': ['debutant', 'intermediaire'],
          'Intermédiaire': ['intermediaire', 'debutant', 'avance'],
          'Avancé': ['avance', 'intermediaire', 'expert'],
          'Expert': ['expert', 'avance']
        };
        var cats = catMap[lvl] || ['debutant', 'intermediaire'];
        pool = rackets_db_namespaceObject.filter(function (r) {
          return cats.includes(r.category);
        });
      }

      // Exclude already analyzed rackets
      pool = pool.filter(function (r) {
        return !existingNames.has((r.name || "").toLowerCase().trim());
      });

      // Filter by brand preferences if any
      var brandPref = (profile.brandTags || []).map(function (id) {
        var _BRAND_TAGS$find6;
        return (_BRAND_TAGS$find6 = BRAND_TAGS.find(function (t) {
          return t.id === id;
        })) === null || _BRAND_TAGS$find6 === void 0 || (_BRAND_TAGS$find6 = _BRAND_TAGS$find6.label) === null || _BRAND_TAGS$find6 === void 0 ? void 0 : _BRAND_TAGS$find6.toLowerCase();
      }).filter(Boolean);
      if (brandPref.length) {
        var brandPool = pool.filter(function (r) {
          return brandPref.includes(r.brand.toLowerCase());
        });
        var otherTop = pool.filter(function (r) {
          return !brandPref.includes(r.brand.toLowerCase());
        }).map(function (r) {
          return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
            _pa: prioAttrs.reduce(function (s, k) {
              return s + (r.scores[k] || 0);
            }, 0) / prioAttrs.length
          });
        }).sort(function (a, b) {
          return b._pa - a._pa;
        }).slice(0, 2);
        pool = [].concat(_toConsumableArray(brandPool), _toConsumableArray(otherTop));
      }

      // Score by priority attributes average (70%) + global score (30%)
      var scored = pool.map(function (r) {
        var prioAvg = prioAttrs.reduce(function (s, k) {
          return s + (r.scores[k] || 0);
        }, 0) / prioAttrs.length;
        var gs = computeGlobalScore(r.scores, profile);
        return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
          _prioAvg: Math.round(prioAvg * 10) / 10,
          _prioScore: prioAvg * 0.7 + gs * 0.3,
          globalScore: gs
        });
      });
      scored.sort(function (a, b) {
        return b.globalScore - a.globalScore;
      });
      var picks = scored.slice(0, 4);
      if (!picks.length) return null;
      var prioTitle = prioLabels.join(' & ');
      return /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, null, /*#__PURE__*/external_React_default().createElement("div", {
        className: "print-section-divider",
        style: {
          borderTop: "2px solid rgba(249,115,22,0.15)",
          margin: "16px 0 8px",
          paddingTop: 8
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        className: "no-print",
        style: {
          fontSize: 12,
          fontWeight: 800,
          color: "#f97316",
          marginBottom: 4
        }
      }, "\uD83C\uDFAF \xC0 D\xC9COUVRIR \u2014 Top ", prioTitle), /*#__PURE__*/external_React_default().createElement("div", {
        className: "print-section-title",
        style: {
          display: "none",
          fontSize: 11,
          fontWeight: 700,
          color: "#f97316",
          marginBottom: 4
        }
      }, "\uD83C\uDFAF \xC0 D\xC9COUVRIR \u2014 Top ", prioTitle), /*#__PURE__*/external_React_default().createElement("div", {
        className: "no-print",
        style: {
          fontSize: 9,
          color: "#64748b",
          marginBottom: 8
        }
      }, "Raquettes de la base hors s\xE9lection, class\xE9es par ", prioLabels.join(' + '))), picks.map(function (r, idx) {
        var gs = r.globalScore;
        var forYouVal = computeForYou(r.scores, profile);
        var fy = fyConfig[forYouVal] || fyConfig.partial;
        var badgeClass = forYouVal === "recommended" ? "print-badge-green" : forYouVal === "no" ? "print-badge-red" : "print-badge-orange";
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: "disco-" + r.id,
          className: "print-card",
          style: {
            background: "rgba(249,115,22,0.04)",
            border: "1px solid rgba(249,115,22,0.2)",
            borderRadius: 12,
            padding: "12px 14px",
            marginBottom: 8,
            boxSizing: "border-box",
            overflow: "hidden",
            borderLeft: "5px solid rgba(249,115,22,0.5)",
            pageBreakInside: "avoid",
            breakInside: "avoid"
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            alignItems: "center",
            gap: 10,
            marginBottom: 8
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            flexShrink: 0,
            minWidth: 28
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: 18,
            lineHeight: 1
          }
        }, "\uD83C\uDFAF")), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
          src: proxyImg(r.imageUrl),
          alt: "",
          className: "print-racket-img-sm",
          style: {
            width: 40,
            height: 48,
            objectFit: "contain",
            borderRadius: 8,
            flexShrink: 0,
            background: "rgba(255,255,255,0.06)",
            padding: 2
          },
          onError: function onError(e) {
            e.target.style.display = 'none';
          }
        }), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            flex: 1,
            minWidth: 0
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "flex",
            alignItems: "center",
            gap: 6,
            flexWrap: "wrap"
          }
        }, /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            fontSize: 12,
            fontWeight: 700,
            color: "#e2e8f0"
          }
        }, r.name), /*#__PURE__*/external_React_default().createElement("span", {
          className: "print-badge ".concat(badgeClass),
          style: {
            background: fy.bg,
            border: "1px solid ".concat(fy.border),
            borderRadius: 6,
            padding: "3px 8px",
            fontSize: 7,
            fontWeight: 700,
            color: "#fff",
            flexShrink: 0,
            whiteSpace: "nowrap"
          }
        }, fy.text), /*#__PURE__*/external_React_default().createElement("span", {
          style: {
            background: "rgba(249,115,22,0.15)",
            border: "1px solid rgba(249,115,22,0.3)",
            borderRadius: 6,
            padding: "3px 8px",
            fontSize: 7,
            fontWeight: 700,
            color: "#f97316",
            flexShrink: 0,
            whiteSpace: "nowrap"
          }
        }, "\u2605 ", r._prioAvg, "/10")), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 9,
            color: "#64748b",
            marginTop: 3
          }
        }, r.shape, " \xB7 ", r.weight, " \xB7 ", r.brand, r.player && r.player !== "—" ? " \xB7 \uD83C\uDFBE ".concat(r.player) : "", r.price && r.price !== "—" ? " \xB7 ".concat(r.price) : "")), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            fontSize: 18,
            fontWeight: 800,
            color: gs >= 7.5 ? "#4ade80" : gs >= 6.5 ? "#fbbf24" : "#f87171",
            fontFamily: "'Outfit'",
            lineHeight: 1,
            flexShrink: 0,
            marginLeft: 8
          }
        }, fmtPct(gs))), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            display: "grid",
            gridTemplateColumns: "repeat(3,1fr)",
            gap: "5px 12px",
            marginBottom: 6
          }
        }, ATTRS.map(function (attr) {
          var v = r.scores[attr];
          var isKey = prioAttrs.includes(attr);
          return /*#__PURE__*/external_React_default().createElement("div", {
            key: attr,
            style: {
              minWidth: 0
            }
          }, /*#__PURE__*/external_React_default().createElement("div", {
            style: {
              display: "flex",
              justifyContent: "space-between",
              alignItems: "center"
            }
          }, /*#__PURE__*/external_React_default().createElement("span", {
            style: {
              fontSize: 9,
              color: isKey ? "#f97316" : "#64748b",
              fontWeight: isKey ? 700 : 500
            }
          }, isKey ? "★ " : "", attr), /*#__PURE__*/external_React_default().createElement("span", {
            style: {
              fontSize: 10,
              color: v >= 7.5 ? "#4ade80" : v >= 6 ? "#cbd5e1" : "#fbbf24",
              fontWeight: 700,
              flexShrink: 0,
              marginLeft: 4
            }
          }, v)), /*#__PURE__*/external_React_default().createElement("div", {
            className: "print-bar-bg",
            style: {
              height: 6,
              background: "rgba(255,255,255,0.06)",
              borderRadius: 3,
              marginTop: 2
            }
          }, /*#__PURE__*/external_React_default().createElement("div", {
            className: v >= 7.5 ? "print-bar-fill-green" : v >= 6 ? "print-bar-fill-gray" : "print-bar-fill-yellow",
            style: {
              height: 6,
              borderRadius: 3,
              width: "".concat(v * 10, "%"),
              background: v >= 7.5 ? "#4ade80" : v >= 6 ? "#64748b" : "#fbbf24",
              printColorAdjust: "exact",
              WebkitPrintColorAdjust: "exact"
            }
          })));
        })), /*#__PURE__*/external_React_default().createElement("div", {
          className: "print-verdict",
          style: {
            fontSize: 10,
            color: "#94a3b8",
            lineHeight: 1.6
          }
        }, r.verdict));
      }));
    } catch (e) {
      console.error("[Pertinence:discovery]", e);
      return null;
    }
  }(), /*#__PURE__*/external_React_default().createElement("div", {
    className: "print-footer-wrap print-header",
    style: {
      display: "none",
      marginTop: 16,
      borderTop: "2px solid #e5e7eb",
      paddingTop: 8
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      alignItems: "center"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 6
    }
  }, /*#__PURE__*/external_React_default().createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 44 44",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, /*#__PURE__*/external_React_default().createElement("rect", {
    width: "44",
    height: "44",
    rx: "10",
    fill: "#f97316"
  }), /*#__PURE__*/external_React_default().createElement("ellipse", {
    cx: "22",
    cy: "18",
    rx: "10",
    ry: "12",
    stroke: "#fff",
    strokeWidth: "2.5",
    fill: "none"
  }), /*#__PURE__*/external_React_default().createElement("line", {
    x1: "22",
    y1: "30",
    x2: "22",
    y2: "38",
    stroke: "#fff",
    strokeWidth: "3",
    strokeLinecap: "round"
  }), /*#__PURE__*/external_React_default().createElement("circle", {
    cx: "33",
    cy: "32",
    r: "3.5",
    fill: "#fff",
    opacity: "0.9"
  })), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 8,
      color: "#999"
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      color: "#f97316",
      fontWeight: 700
    }
  }, "Padel Analyzer"), " V8.7 \xB7 Scoring hybride calibr\xE9")), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 8,
      color: "#999",
      textAlign: "right"
    }
  }, new Date().toLocaleDateString('fr-FR'), " \xB7 Prix indicatifs \u2014 v\xE9rifier en boutique"))))), /*#__PURE__*/external_React_default().createElement("div", {
    style: PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, S.card), {}, {
      marginTop: 4
    })
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: S.title
  }, "\uD83D\uDCD6 Lexique des crit\xE8res"), ATTRS.map(function (a) {
    return /*#__PURE__*/external_React_default().createElement("div", {
      key: a,
      onClick: function onClick() {
        return setOpenAttr(function (o) {
          return o === a ? null : a;
        });
      },
      style: {
        padding: "8px 10px",
        marginBottom: 3,
        borderRadius: 10,
        background: openAttr === a ? "rgba(249,115,22,0.06)" : "transparent",
        cursor: "pointer",
        transition: "background 0.15s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        fontWeight: 700,
        color: "#e2e8f0",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("span", null, a), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 9,
        color: "#475569",
        transition: "transform 0.2s ease",
        transform: openAttr === a ? "rotate(90deg)" : "none"
      }
    }, "\u25B8")), openAttr === a && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        marginTop: 5,
        lineHeight: 1.6,
        animation: "fadeIn 0.2s ease"
      }
    }, explanations[a]));
  })), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      textAlign: "center",
      marginTop: 18,
      paddingTop: 16,
      borderTop: "1px solid rgba(255,255,255,0.04)",
      fontSize: 8,
      color: "#334155",
      letterSpacing: "0.05em"
    }
  }, /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontFamily: "'Outfit'",
      fontWeight: 600
    }
  }, "PADEL ANALYZER"), " V8.7 \xB7 Analyse personnalis\xE9e \xB7 ", new Date().toLocaleDateString('fr-FR'), /*#__PURE__*/external_React_default().createElement("br", null), /*#__PURE__*/external_React_default().createElement("span", {
    style: {
      fontSize: 7,
      opacity: 0.7
    }
  }, "Prix indicatifs \u2014 v\xE9rifier en boutique"))), showArena && function () {
    var r2 = selRackets;
    if (r2.length < 2) {
      setShowArena(false);
      return null;
    }

    // Compute all data
    var scored = r2.map(function (r) {
      return PadelAnalyzer_objectSpread(PadelAnalyzer_objectSpread({}, r), {}, {
        _gs: computeGlobalScore(r.scores, profile),
        _fy: computeForYou(r.scores, profile)
      });
    });
    scored.sort(function (a, b) {
      return b._gs - a._gs;
    });
    var champion = scored[0];

    // Per-attribute winners
    var attrWinners = {};
    ATTRS.forEach(function (a) {
      var mx = 0;
      r2.forEach(function (r) {
        var v = Number(r.scores[a]) || 0;
        if (v > mx) mx = v;
      });
      attrWinners[a] = r2.filter(function (r) {
        return (Number(r.scores[a]) || 0) === mx && r2.some(function (x) {
          return (Number(x.scores[a]) || 0) < mx;
        });
      }).map(function (r) {
        return r.id;
      });
    });
    scored.forEach(function (r) {
      r._wins = ATTRS.filter(function (a) {
        return attrWinners[a].includes(r.id);
      }).length;
    });
    var compareRadar = ATTRS.map(function (a) {
      var pt = {
        attribute: a
      };
      r2.forEach(function (r) {
        pt[r.shortName] = Number(r.scores[a]) || 0;
      });
      return pt;
    });
    var fyConfig4 = {
      recommended: {
        text: "RECOMMANDÉ",
        color: "#4CAF50"
      },
      partial: {
        text: "JOUABLE",
        color: "#FF9800"
      },
      no: {
        text: "DÉCONSEILLÉ",
        color: "#ef4444"
      }
    };
    var specRows = [{
      label: "Forme",
      key: "shape"
    }, {
      label: "Poids",
      key: "weight"
    }, {
      label: "Balance",
      key: "balance"
    }, {
      label: "Surface",
      key: "surface"
    }, {
      label: "Noyau",
      key: "core"
    }, {
      label: "Prix",
      key: "price"
    }];
    return /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        zIndex: 9999,
        background: "radial-gradient(ellipse at 30% 20%, rgba(124,58,237,0.15), transparent 60%), radial-gradient(ellipse at 70% 80%, rgba(236,72,153,0.1), transparent 50%), linear-gradient(180deg, #0a0a1a 0%, #0f0a24 30%, #120a2e 60%, #0a0a1a 100%)",
        overflowY: "auto",
        overflowX: "hidden"
      }
    }, /*#__PURE__*/external_React_default().createElement("style", null, "\n            @keyframes arenaSlideIn { from{opacity:0;transform:translateY(30px)} to{opacity:1;transform:translateY(0)} }\n            @keyframes arenaFadeIn { from{opacity:0} to{opacity:1} }\n            @keyframes arenaPulse { 0%,100%{opacity:0.6;transform:scale(1)} 50%{opacity:1;transform:scale(1.05)} }\n            @keyframes vsGlow { 0%,100%{text-shadow:0 0 20px rgba(124,58,237,0.5),0 0 40px rgba(236,72,153,0.3)} 50%{text-shadow:0 0 40px rgba(124,58,237,0.8),0 0 80px rgba(236,72,153,0.5)} }\n            @keyframes barGrow { from{width:0} }\n            @keyframes crownDrop { from{opacity:0;transform:translateY(-20px) scale(0.5)} to{opacity:1;transform:translateY(0) scale(1)} }\n            @keyframes starPop { 0%{transform:scale(0) rotate(-180deg);opacity:0} 60%{transform:scale(1.3) rotate(10deg);opacity:1} 100%{transform:scale(1) rotate(0deg);opacity:1} }\n          "), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "fixed",
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
        pointerEvents: "none",
        overflow: "hidden",
        zIndex: 0
      }
    }, _toConsumableArray(Array(12)).map(function (_, i) {
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: i,
        style: {
          position: "absolute",
          width: Math.random() * 3 + 1,
          height: Math.random() * 3 + 1,
          borderRadius: "50%",
          background: i % 3 === 0 ? "rgba(124,58,237,0.4)" : i % 3 === 1 ? "rgba(236,72,153,0.3)" : "rgba(99,102,241,0.3)",
          left: "".concat(Math.random() * 100, "%"),
          top: "".concat(Math.random() * 100, "%"),
          animation: "arenaPulse ".concat(3 + Math.random() * 4, "s ease-in-out infinite"),
          animationDelay: "".concat(Math.random() * 3, "s")
        }
      });
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        position: "relative",
        zIndex: 1,
        maxWidth: 700,
        margin: "0 auto",
        padding: "20px 16px 40px"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        marginBottom: 24,
        animation: "arenaFadeIn 0.4s ease"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 10
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 24
      }
    }, "\u2694\uFE0F"), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("h1", {
      style: {
        fontFamily: "'Outfit'",
        fontSize: 22,
        fontWeight: 800,
        background: "linear-gradient(135deg,#c4b5fd,#a78bfa,#ec4899)",
        WebkitBackgroundClip: "text",
        WebkitTextFillColor: "transparent",
        margin: 0,
        letterSpacing: "-0.02em"
      }
    }, "L'AR\xC8NE"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#7c3aed",
        fontWeight: 600,
        letterSpacing: "0.08em",
        textTransform: "uppercase"
      }
    }, "Comparateur immersif"))), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setShowArena(false);
      },
      style: {
        width: 40,
        height: 40,
        borderRadius: 12,
        border: "1px solid rgba(124,58,237,0.3)",
        background: "rgba(124,58,237,0.1)",
        color: "#c4b5fd",
        fontSize: 18,
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "inherit",
        transition: "all 0.2s"
      },
      onMouseEnter: function onMouseEnter(e) {
        e.currentTarget.style.background = "rgba(124,58,237,0.25)";
      },
      onMouseLeave: function onMouseLeave(e) {
        e.currentTarget.style.background = "rgba(124,58,237,0.1)";
      }
    }, "\u2715")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "stretch",
        gap: 0,
        marginBottom: 28,
        animation: "arenaSlideIn 0.6s ease"
      }
    }, scored.map(function (r, i) {
      var isChamp = r.id === champion.id;
      var fy = fyConfig4[r._fy] || fyConfig4.partial;
      return /*#__PURE__*/external_React_default().createElement((external_React_default()).Fragment, {
        key: r.id
      }, i > 0 && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          flexShrink: 0,
          width: 50,
          zIndex: 2
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontFamily: "'Outfit'",
          fontSize: 20,
          fontWeight: 900,
          color: "#7c3aed",
          animation: "vsGlow 2s ease-in-out infinite",
          textAlign: "center",
          lineHeight: 1
        }
      }, "VS")), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          flex: 1,
          minWidth: 0,
          background: isChamp ? "linear-gradient(165deg,rgba(124,58,237,0.12),rgba(236,72,153,0.06),rgba(0,0,0,0.2))" : "rgba(255,255,255,0.02)",
          border: "1.5px solid ".concat(isChamp ? "rgba(124,58,237,0.4)" : "rgba(255,255,255,0.06)"),
          borderRadius: 18,
          padding: "20px 14px",
          textAlign: "center",
          position: "relative",
          boxShadow: isChamp ? "0 0 40px rgba(124,58,237,0.1)" : "none",
          transition: "all 0.3s"
        }
      }, isChamp && scored.length > 1 && /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          position: "absolute",
          top: -14,
          left: "50%",
          transform: "translateX(-50%)",
          animation: "crownDrop 0.6s ease 0.8s both"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 22
        }
      }, "\uD83D\uDC51")), r.imageUrl && /*#__PURE__*/external_React_default().createElement("img", {
        src: proxyImg(r.imageUrl),
        alt: "",
        style: {
          width: 64,
          height: 80,
          objectFit: "contain",
          margin: "0 auto 8px",
          display: "block",
          filter: isChamp ? "drop-shadow(0 4px 16px rgba(124,58,237,0.3))" : "none",
          borderRadius: 8
        },
        onError: function onError(e) {
          e.target.style.display = 'none';
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: 10,
          height: 10,
          borderRadius: "50%",
          background: r.color,
          margin: "0 auto 6px",
          boxShadow: "0 0 12px ".concat(r.color, "60")
        }
      }), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 13,
          fontWeight: 700,
          color: isChamp ? "#e2e8f0" : "#94a3b8",
          lineHeight: 1.2
        }
      }, r.shortName), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 9,
          color: "#64748b",
          marginTop: 3
        }
      }, r.brand), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 24,
          fontWeight: 800,
          color: isChamp ? "#c4b5fd" : "#475569",
          fontFamily: "'Outfit'",
          marginTop: 8
        }
      }, r._gs.toFixed(1), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          color: "#64748b"
        }
      }, "/10")), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          marginTop: 4
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 8,
          fontWeight: 700,
          color: fy.color,
          background: "".concat(fy.color, "15"),
          border: "1px solid ".concat(fy.color, "30"),
          borderRadius: 6,
          padding: "2px 8px"
        }
      }, fy.text)), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 10,
          color: "#7c3aed",
          fontWeight: 600,
          marginTop: 6
        }
      }, r._wins, " crit\xE8re", r._wins > 1 ? "s" : "", " gagn\xE9", r._wins > 1 ? "s" : "")));
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(124,58,237,0.04)",
        border: "1px solid rgba(124,58,237,0.15)",
        borderRadius: 20,
        padding: 16,
        marginBottom: 20,
        animation: "arenaSlideIn 0.6s ease 0.2s both"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: "#7c3aed",
        textAlign: "center",
        textTransform: "uppercase",
        letterSpacing: "0.08em",
        marginBottom: 8
      }
    }, "Radar comparatif"), /*#__PURE__*/external_React_default().createElement(ResponsiveContainer, {
      width: "100%",
      height: 280
    }, /*#__PURE__*/external_React_default().createElement(RadarChart, {
      data: compareRadar,
      cx: "50%",
      cy: "50%",
      outerRadius: "70%"
    }, /*#__PURE__*/external_React_default().createElement(PolarGrid, {
      stroke: "rgba(124,58,237,0.12)",
      gridType: "polygon"
    }), /*#__PURE__*/external_React_default().createElement(PolarAngleAxis, {
      dataKey: "attribute",
      tick: {
        fill: "#a78bfa",
        fontSize: 10,
        fontWeight: 600
      }
    }), /*#__PURE__*/external_React_default().createElement(PolarRadiusAxis, {
      angle: 90,
      domain: [0, 10],
      tick: {
        fill: "#4c1d95",
        fontSize: 8
      },
      tickCount: 6,
      axisLine: false
    }), r2.map(function (r, i) {
      return /*#__PURE__*/external_React_default().createElement(Radar, {
        key: r.id,
        name: r.shortName,
        dataKey: r.shortName,
        stroke: r.color,
        fill: r.color,
        fillOpacity: 0.1,
        strokeWidth: 2.5
      });
    }), /*#__PURE__*/external_React_default().createElement(Legend, {
      wrapperStyle: {
        fontSize: 10,
        color: "#a78bfa",
        paddingTop: 6
      }
    })))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(124,58,237,0.04)",
        border: "1px solid rgba(124,58,237,0.15)",
        borderRadius: 20,
        padding: 16,
        marginBottom: 20,
        animation: "arenaSlideIn 0.6s ease 0.3s both"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: "#7c3aed",
        textAlign: "center",
        textTransform: "uppercase",
        letterSpacing: "0.08em",
        marginBottom: 14
      }
    }, "Duel par crit\xE8re"), ATTRS.map(function (attr, ai) {
      var _r2$find;
      var vals = r2.map(function (r) {
        return {
          r: r,
          v: Number(r.scores[attr]) || 0
        };
      });
      var maxV = Math.max.apply(Math, _toConsumableArray(vals.map(function (x) {
        return x.v;
      })));
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: attr,
        style: {
          marginBottom: 14
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          fontSize: 10,
          fontWeight: 700,
          color: "#c4b5fd",
          marginBottom: 6,
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between"
        }
      }, /*#__PURE__*/external_React_default().createElement("span", null, attr), attrWinners[attr].length === 1 && /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 8,
          color: "#7c3aed",
          background: "rgba(124,58,237,0.15)",
          padding: "1px 6px",
          borderRadius: 4
        }
      }, "\uD83D\uDC51 ", (_r2$find = r2.find(function (r) {
        return r.id === attrWinners[attr][0];
      })) === null || _r2$find === void 0 ? void 0 : _r2$find.shortName), attrWinners[attr].length === 0 && /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 8,
          color: "#475569"
        }
      }, "\xC9galit\xE9")), vals.map(function (_ref24) {
        var r = _ref24.r,
          v = _ref24.v;
        var isW = attrWinners[attr].includes(r.id);
        return /*#__PURE__*/external_React_default().createElement("div", {
          key: r.id,
          style: {
            display: "flex",
            alignItems: "center",
            gap: 8,
            marginBottom: 3
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            width: 8,
            height: 8,
            borderRadius: "50%",
            background: r.color,
            flexShrink: 0,
            boxShadow: isW ? "0 0 8px ".concat(r.color) : ""
          }
        }), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            width: 55,
            fontSize: 9,
            color: "#94a3b8",
            overflow: "hidden",
            textOverflow: "ellipsis",
            whiteSpace: "nowrap",
            flexShrink: 0
          }
        }, r.shortName), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            flex: 1,
            height: 8,
            background: "rgba(124,58,237,0.08)",
            borderRadius: 4,
            overflow: "hidden"
          }
        }, /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            height: "100%",
            width: "".concat(v * 10, "%"),
            background: isW ? "linear-gradient(90deg,#7c3aed,#ec4899)" : r.color,
            borderRadius: 4,
            animation: "barGrow 0.8s ease ".concat(0.3 + ai * 0.05, "s both"),
            opacity: isW ? 1 : 0.5
          }
        })), /*#__PURE__*/external_React_default().createElement("div", {
          style: {
            width: 32,
            textAlign: "right",
            fontSize: 11,
            fontWeight: isW ? 800 : 500,
            color: isW ? "#c4b5fd" : "#64748b",
            fontFamily: "'Outfit'"
          }
        }, v.toFixed(1)));
      }));
    })), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "rgba(124,58,237,0.04)",
        border: "1px solid rgba(124,58,237,0.15)",
        borderRadius: 20,
        padding: 16,
        marginBottom: 20,
        animation: "arenaSlideIn 0.6s ease 0.4s both",
        overflowX: "auto"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: "#7c3aed",
        textAlign: "center",
        textTransform: "uppercase",
        letterSpacing: "0.08em",
        marginBottom: 14
      }
    }, "Fiche technique"), /*#__PURE__*/external_React_default().createElement("table", {
      style: {
        width: "100%",
        borderCollapse: "collapse"
      }
    }, /*#__PURE__*/external_React_default().createElement("thead", null, /*#__PURE__*/external_React_default().createElement("tr", null, /*#__PURE__*/external_React_default().createElement("th", {
      style: {
        padding: "6px 8px",
        textAlign: "left",
        color: "#4c1d95",
        fontSize: 9,
        fontWeight: 600
      }
    }), scored.map(function (r) {
      return /*#__PURE__*/external_React_default().createElement("th", {
        key: r.id,
        style: {
          padding: "6px 8px",
          textAlign: "center"
        }
      }, /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: 10,
          height: 10,
          borderRadius: "50%",
          background: r.color,
          margin: "0 auto 3px"
        }
      }), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          fontWeight: 700,
          color: r.id === champion.id ? "#c4b5fd" : "#64748b"
        }
      }, r.shortName));
    }))), /*#__PURE__*/external_React_default().createElement("tbody", null, specRows.map(function (_ref25, si) {
      var label = _ref25.label,
        key = _ref25.key;
      return /*#__PURE__*/external_React_default().createElement("tr", {
        key: key,
        style: {
          background: si % 2 === 0 ? "rgba(124,58,237,0.03)" : "transparent"
        }
      }, /*#__PURE__*/external_React_default().createElement("td", {
        style: {
          padding: "5px 8px",
          fontSize: 9,
          color: "#a78bfa",
          fontWeight: 600
        }
      }, label), scored.map(function (r) {
        return /*#__PURE__*/external_React_default().createElement("td", {
          key: r.id,
          style: {
            padding: "5px 8px",
            textAlign: "center",
            fontSize: 10,
            color: "#cbd5e1"
          }
        }, r[key] || "—");
      }));
    })))), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        background: "linear-gradient(135deg,rgba(124,58,237,0.1),rgba(236,72,153,0.06))",
        border: "1.5px solid rgba(124,58,237,0.3)",
        borderRadius: 20,
        padding: 20,
        textAlign: "center",
        animation: "arenaSlideIn 0.6s ease 0.5s both",
        boxShadow: "0 8px 40px rgba(124,58,237,0.1)"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        fontWeight: 700,
        color: "#7c3aed",
        textTransform: "uppercase",
        letterSpacing: "0.1em",
        marginBottom: 8
      }
    }, "\uD83C\uDFC6 Verdict de l'Ar\xE8ne"), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        animation: "starPop 0.5s ease 1.2s both"
      }
    }, /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 36
      }
    }, "\uD83D\uDC51")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        marginTop: 4
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 14,
        height: 14,
        borderRadius: "50%",
        background: champion.color,
        boxShadow: "0 0 16px ".concat(champion.color, "60")
      }
    }), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 20,
        fontWeight: 800,
        color: "#e2e8f0",
        fontFamily: "'Outfit'"
      }
    }, champion.shortName)), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 26,
        fontWeight: 900,
        color: "#c4b5fd",
        fontFamily: "'Outfit'",
        marginTop: 2
      }
    }, champion._gs.toFixed(1), /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 12,
        color: "#7c3aed"
      }
    }, "/10")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 11,
        color: "#a78bfa",
        marginTop: 4
      }
    }, "Meilleure correspondance pour ", /*#__PURE__*/external_React_default().createElement("strong", {
      style: {
        color: "#c4b5fd"
      }
    }, profileName)), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#7c3aed",
        marginTop: 2
      }
    }, champion._wins, " crit\xE8re", champion._wins > 1 ? "s" : "", " remport\xE9", champion._wins > 1 ? "s" : "", " sur ", ATTRS.length), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "center",
        gap: 8,
        marginTop: 16,
        flexWrap: "wrap"
      }
    }, scored.map(function (r, i) {
      return /*#__PURE__*/external_React_default().createElement("div", {
        key: r.id,
        style: {
          display: "flex",
          alignItems: "center",
          gap: 6,
          padding: "6px 14px",
          background: i === 0 ? "rgba(124,58,237,0.15)" : "rgba(255,255,255,0.02)",
          border: "1px solid ".concat(i === 0 ? "rgba(124,58,237,0.4)" : "rgba(255,255,255,0.06)"),
          borderRadius: 10
        }
      }, /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 14
        }
      }, ["🥇", "🥈", "🥉", "4️⃣"][i] || ""), /*#__PURE__*/external_React_default().createElement("div", {
        style: {
          width: 8,
          height: 8,
          borderRadius: "50%",
          background: r.color
        }
      }), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 10,
          fontWeight: i === 0 ? 700 : 500,
          color: i === 0 ? "#c4b5fd" : "#94a3b8"
        }
      }, r.shortName), /*#__PURE__*/external_React_default().createElement("span", {
        style: {
          fontSize: 11,
          fontWeight: 800,
          color: i === 0 ? "#c4b5fd" : "#64748b",
          fontFamily: "'Outfit'"
        }
      }, r._gs.toFixed(1)));
    })), /*#__PURE__*/external_React_default().createElement("button", {
      onClick: function onClick() {
        return setShowArena(false);
      },
      style: {
        marginTop: 20,
        padding: "12px 32px",
        borderRadius: 12,
        cursor: "pointer",
        fontFamily: "'Outfit',sans-serif",
        background: "rgba(124,58,237,0.15)",
        border: "1px solid rgba(124,58,237,0.35)",
        color: "#c4b5fd",
        fontSize: 12,
        fontWeight: 700,
        transition: "all 0.2s"
      },
      onMouseEnter: function onMouseEnter(e) {
        e.currentTarget.style.background = "rgba(124,58,237,0.25)";
      },
      onMouseLeave: function onMouseLeave(e) {
        e.currentTarget.style.background = "rgba(124,58,237,0.15)";
      }
    }, "\u2190 Retour \xE0 l'analyse"))));
  }(), confirmModal && /*#__PURE__*/external_React_default().createElement("div", {
    onClick: function onClick() {
      return confirmModal.onCancel();
    },
    style: {
      position: "fixed",
      inset: 0,
      background: "rgba(0,0,0,0.6)",
      backdropFilter: "blur(4px)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      zIndex: 2000,
      animation: "fadeIn 0.2s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    onClick: function onClick(e) {
      return e.stopPropagation();
    },
    style: {
      background: "#1a1f2e",
      border: "1px solid rgba(255,255,255,0.12)",
      borderRadius: 20,
      padding: "28px 24px 22px",
      maxWidth: 340,
      width: "90%",
      textAlign: "center",
      boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
      animation: "fadeIn 0.25s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 32,
      marginBottom: 12
    }
  }, "\uD83D\uDDD1"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 700,
      color: "#f1f5f9",
      fontFamily: "'Outfit',sans-serif",
      marginBottom: 6
    }
  }, confirmModal.message), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 20px"
    }
  }, "Cette action est irr\xE9versible."), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 10
    }
  }, /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      return confirmModal.onCancel();
    },
    style: {
      flex: 1,
      padding: "12px",
      borderRadius: 12,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Outfit',sans-serif",
      background: "rgba(255,255,255,0.05)",
      border: "1px solid rgba(255,255,255,0.1)",
      color: "#94a3b8"
    }
  }, "Annuler"), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      return confirmModal.onConfirm();
    },
    style: {
      flex: 1,
      padding: "12px",
      borderRadius: 12,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Outfit',sans-serif",
      background: "rgba(239,68,68,0.15)",
      border: "1px solid rgba(239,68,68,0.4)",
      color: "#ef4444"
    }
  }, "Supprimer")))), passwordModal && /*#__PURE__*/external_React_default().createElement("div", {
    onClick: function onClick() {
      setPasswordModal(null);
      setPinInput("");
      setPinError("");
    },
    style: {
      position: "fixed",
      inset: 0,
      background: "rgba(0,0,0,0.6)",
      backdropFilter: "blur(4px)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      zIndex: 2100,
      animation: "fadeIn 0.2s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    onClick: function onClick(e) {
      return e.stopPropagation();
    },
    style: {
      background: "#1a1f2e",
      border: "1px solid rgba(255,255,255,0.12)",
      borderRadius: 20,
      padding: "28px 24px 22px",
      maxWidth: 340,
      width: "90%",
      textAlign: "center",
      boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
      animation: "fadeIn 0.25s ease"
    }
  }, /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 32,
      marginBottom: 12
    }
  }, passwordModal.mode === "setpin" ? "🔑" : "🔒"), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 700,
      color: "#f1f5f9",
      fontFamily: "'Outfit',sans-serif",
      marginBottom: 6
    }
  }, passwordModal.mode === "setpin" ? "Créer un code administrateur" : "Code administrateur requis"), /*#__PURE__*/external_React_default().createElement("p", {
    style: {
      fontSize: 11,
      color: "#64748b",
      margin: "0 0 16px"
    }
  }, passwordModal.mode === "setpin" ? "Ce code unique protégera tous les profils verrouillés." : "Entrez le code pour acc\xE9der au profil \"".concat(passwordModal.profileName, "\".")), /*#__PURE__*/external_React_default().createElement("input", {
    type: "password",
    value: pinInput,
    onChange: function onChange(e) {
      setPinInput(e.target.value);
      setPinError("");
    },
    onKeyDown: function onKeyDown(e) {
      if (e.key === "Enter") {
        if (passwordModal.mode === "setpin") {
          if (pinInput.length < 4) {
            setPinError("4 caractères minimum");
            return;
          }
          setAdminPin(pinInput);
          passwordModal.onSuccess();
          setPinInput("");
          setPinError("");
        } else {
          if (pinInput === getAdminPin()) {
            passwordModal.onSuccess();
            setPinInput("");
            setPinError("");
          } else {
            setPinError("Code incorrect");
          }
        }
      }
    },
    placeholder: passwordModal.mode === "setpin" ? "Code (4+ caractères)" : "Entrer le code",
    autoFocus: true,
    style: {
      width: "100%",
      padding: "12px 14px",
      borderRadius: 12,
      fontSize: 18,
      fontWeight: 700,
      textAlign: "center",
      letterSpacing: "0.3em",
      background: "rgba(255,255,255,0.06)",
      border: "1px solid ".concat(pinError ? "rgba(239,68,68,0.5)" : "rgba(255,255,255,0.12)"),
      color: "#f1f5f9",
      fontFamily: "'Outfit',sans-serif",
      outline: "none",
      boxSizing: "border-box"
    }
  }), pinError && /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      fontSize: 11,
      color: "#ef4444",
      marginTop: 8,
      fontWeight: 600
    }
  }, pinError), /*#__PURE__*/external_React_default().createElement("div", {
    style: {
      display: "flex",
      gap: 10,
      marginTop: 16
    }
  }, /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      setPasswordModal(null);
      setPinInput("");
      setPinError("");
    },
    style: {
      flex: 1,
      padding: "12px",
      borderRadius: 12,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Outfit',sans-serif",
      background: "rgba(255,255,255,0.05)",
      border: "1px solid rgba(255,255,255,0.1)",
      color: "#94a3b8"
    }
  }, "Annuler"), /*#__PURE__*/external_React_default().createElement("button", {
    onClick: function onClick() {
      if (passwordModal.mode === "setpin") {
        if (pinInput.length < 4) {
          setPinError("4 caractères minimum");
          return;
        }
        setAdminPin(pinInput);
        passwordModal.onSuccess();
        setPinInput("");
        setPinError("");
      } else {
        if (pinInput === getAdminPin()) {
          passwordModal.onSuccess();
          setPinInput("");
          setPinError("");
        } else {
          setPinError("Code incorrect");
        }
      }
    },
    style: {
      flex: 1,
      padding: "12px",
      borderRadius: 12,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      fontFamily: "'Outfit',sans-serif",
      background: "rgba(99,102,241,0.15)",
      border: "1px solid rgba(99,102,241,0.4)",
      color: "#a5b4fc"
    }
  }, passwordModal.mode === "setpin" ? "Créer" : "Valider")))));

  // ============================================================
  // HELPER: Render a suggestion card with checkbox
  // ============================================================
  function renderSuggestCard(s, realIdx, checked, isTopPick) {
    var isPrio = s.category === "priority";
    var accentColor = isPrio ? "#fbbf24" : "#f97316";
    return /*#__PURE__*/external_React_default().createElement("div", {
      key: realIdx,
      onClick: function onClick() {
        return !s._added && toggleSuggestCheck(realIdx);
      },
      style: {
        background: s._added ? "rgba(76,175,80,0.08)" : checked ? "".concat(isPrio ? "rgba(251,191,36,0.12)" : "rgba(249,115,22,0.12)") : "rgba(255,255,255,0.04)",
        border: "1px solid ".concat(s._added ? "#4CAF50" : checked ? accentColor : "rgba(255,255,255,0.1)"),
        borderRadius: 10,
        padding: "10px 12px",
        marginBottom: 6,
        cursor: s._added ? "default" : "pointer",
        opacity: s._added ? 0.7 : 1,
        transition: "all 0.2s"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center"
      }
    }, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 8
      }
    }, !s._added && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        width: 18,
        height: 18,
        borderRadius: 4,
        border: "2px solid ".concat(checked ? accentColor : "rgba(255,255,255,0.2)"),
        background: checked ? "".concat(accentColor, "33") : "transparent",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 11,
        color: accentColor,
        fontWeight: 700,
        flexShrink: 0
      }
    }, checked ? "✓" : ""), /*#__PURE__*/external_React_default().createElement("div", null, /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 12,
        fontWeight: 700,
        color: "#e2e8f0"
      }
    }, isTopPick && !s._added ? "⭐ " : isPrio && !s._added ? "⚡ " : "", s.name), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#94a3b8",
        marginTop: 1
      }
    }, s.brand, " \xB7 ", s.shape, " \xB7 ", s.weight, " \xB7 ", s.price))), s._added && /*#__PURE__*/external_React_default().createElement("span", {
      style: {
        fontSize: 9,
        background: "#1B5E20",
        border: "1px solid #4CAF50",
        borderRadius: 4,
        padding: "2px 6px",
        color: "#fff",
        fontWeight: 700,
        flexShrink: 0
      }
    }, "AJOUT\xC9E \u2713")), /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 10,
        color: "#cbd5e1",
        marginTop: 5,
        lineHeight: 1.4,
        fontStyle: "italic",
        marginLeft: s._added ? 0 : 26
      }
    }, s.description), s._error && /*#__PURE__*/external_React_default().createElement("div", {
      style: {
        fontSize: 9,
        color: "#ef4444",
        marginTop: 3,
        marginLeft: 26
      }
    }, "\u26A0 Erreur: ", s._error));
  }
}
})();

/******/ })()
;